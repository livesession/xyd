# Code generated by opensdk. DO NOT EDIT.

module Openai
  module Resources
    class Beta
      attr_reader :threads

      # @api private
      def initialize(transport)
        @transport = transport
        @threads = BetaThreads.new(transport)
      end
    end

    class BetaThreads
      attr_reader :runs

      # @api private
      def initialize(transport)
        @transport = transport
        @runs = BetaThreadsRuns.new(transport)
      end
    end

    class BetaThreadsRuns
      attr_reader :submit_tool_outputs

      # @api private
      def initialize(transport)
        @transport = transport
        @submit_tool_outputs = BetaThreadsRunsSubmitToolOutputs.new(transport)
      end
    end

    class BetaThreadsRunsSubmitToolOutputs
      # @api private
      def initialize(transport)
        @transport = transport
      end

      # When a run has the `status: "requires_action"` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request.
      #
      # @param thread_id [String]
      # @param run_id [String]
      # @param tool_outputs [Array<Models::SubmitToolOutputsRunRequestToolOutputsItem>]
      # @param stream [Boolean]
      # @return [Models::RunObject]
      def create(thread_id, run_id, tool_outputs:, stream: nil)
        raise ArgumentError, "Expected a non-empty value for `thread_id`" if thread_id.nil? || thread_id.to_s.empty?

        raise ArgumentError, "Expected a non-empty value for `run_id`" if run_id.nil? || run_id.to_s.empty?

        @transport.request(
          method: :post,
          path: "/threads/#{thread_id}/runs/#{run_id}/submit_tool_outputs",
          body: { "tool_outputs" => tool_outputs, "stream" => stream }
        )
      end
    end
  end
end

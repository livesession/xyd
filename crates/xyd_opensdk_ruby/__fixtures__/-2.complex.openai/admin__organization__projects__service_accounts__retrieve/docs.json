{
  "chain": [
    "admin",
    "organization",
    "projects",
    "service-accounts"
  ],
  "action": "retrieve",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.admin.organization.projects.service_accounts.retrieve(\"project_id\", \"service_account_id\")\npp result\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.service_accounts.retrieve(project_id, service_account_id) -> Models::ProjectServiceAccount",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "Models::ProjectServiceAccount",
      "fields": [
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "The object type, which is always `organization.project.service_account`"
        },
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "name",
          "langType": "String",
          "required": true,
          "description": "The name of the service account"
        },
        {
          "name": "role",
          "langType": "Models::ProjectServiceAccountRole",
          "required": true,
          "description": "`owner` or `member`",
          "refTypeName": "ProjectServiceAccountRole"
        },
        {
          "name": "created_at",
          "langType": "Integer",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the service account was created"
        }
      ]
    }
  }
}

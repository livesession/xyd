{
  "chain": [
    "containers",
    "files"
  ],
  "action": "list",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.containers.files.list(\"container_id\", limit: 20, order: \"asc\", after: \"after\")\npp result\n",
  "typeReference": {
    "signature": "client.containers.files.list(container_id, limit: nil, order: nil, after: nil) -> Page<Models::ContainerFileResource>",
    "request": {
      "fields": [
        {
          "name": "limit",
          "langType": "Integer",
          "required": false,
          "description": "A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.\n"
        },
        {
          "name": "order",
          "langType": "Models::ContainersFilesListOrder",
          "required": false,
          "description": "Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.\n",
          "refTypeName": "ContainersFilesListOrder"
        },
        {
          "name": "after",
          "langType": "String",
          "required": false,
          "description": "A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n"
        }
      ]
    },
    "response": {
      "typeName": "Page<Models::ContainerFileResource>",
      "note": "paginated (Page)",
      "fields": [
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "Unique identifier for the file."
        },
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "The type of this object (`container.file`)."
        },
        {
          "name": "container_id",
          "langType": "String",
          "required": true,
          "description": "The container this file belongs to."
        },
        {
          "name": "created_at",
          "langType": "Integer",
          "required": true,
          "description": "Unix timestamp (in seconds) when the file was created."
        },
        {
          "name": "bytes",
          "langType": "Integer",
          "required": true,
          "description": "Size of the file in bytes."
        },
        {
          "name": "path",
          "langType": "String",
          "required": true,
          "description": "Path of the file in the container."
        },
        {
          "name": "source",
          "langType": "String",
          "required": true,
          "description": "Source of the file (e.g., `user`, `assistant`)."
        }
      ]
    }
  }
}

{
  "chain": [
    "beta",
    "chat-kit",
    "threads"
  ],
  "action": "retrieve",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.beta.chat_kit.threads.retrieve(\"cthr_123\")\npp result\n",
  "typeReference": {
    "signature": "client.beta.chat_kit.threads.retrieve(thread_id) -> Models::ThreadResource",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "Models::ThreadResource",
      "fields": [
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "Identifier of the thread."
        },
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "Type discriminator that is always `chatkit.thread`."
        },
        {
          "name": "created_at",
          "langType": "Integer",
          "required": true,
          "description": "Unix timestamp (in seconds) for when the thread was created."
        },
        {
          "name": "title",
          "langType": "String",
          "required": true
        },
        {
          "name": "status",
          "langType": "Models::ThreadResourceStatus",
          "required": true,
          "description": "Current status for the thread. Defaults to `active` for newly created threads.",
          "refTypeName": "ThreadResourceStatus"
        },
        {
          "name": "user",
          "langType": "String",
          "required": true,
          "description": "Free-form string that identifies your end user who owns the thread."
        }
      ]
    }
  }
}

{
  "chain": [
    "audio",
    "voice-consents"
  ],
  "action": "create",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.audio.voice_consents.create(name: \"string\", recording: StringIO.new(\"Example data\"), language: \"string\")\npp result\n",
  "typeReference": {
    "signature": "client.audio.voice_consents.create(name:, recording:, language:) -> Models::VoiceConsentResource",
    "request": {
      "fields": [
        {
          "name": "name",
          "langType": "String",
          "required": true,
          "description": "The label to use for this consent recording."
        },
        {
          "name": "recording",
          "langType": "IO, String",
          "required": true,
          "description": "The consent audio recording file. Maximum size is 10 MiB.\n\nSupported MIME types:\n`audio/mpeg`, `audio/wav`, `audio/x-wav`, `audio/ogg`, `audio/aac`, `audio/flac`, `audio/webm`, `audio/mp4`.\n"
        },
        {
          "name": "language",
          "langType": "String",
          "required": true,
          "description": "The BCP 47 language tag for the consent phrase (for example, `en-US`)."
        }
      ]
    },
    "response": {
      "typeName": "Models::VoiceConsentResource",
      "fields": [
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "The object type, which is always `audio.voice_consent`."
        },
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "The consent recording identifier."
        },
        {
          "name": "name",
          "langType": "String",
          "required": true,
          "description": "The label provided when the consent recording was uploaded."
        },
        {
          "name": "language",
          "langType": "String",
          "required": true,
          "description": "The BCP 47 language tag for the consent phrase (for example, `en-US`)."
        },
        {
          "name": "created_at",
          "langType": "Integer",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the consent recording was created."
        }
      ]
    }
  }
}

{
  "chain": [
    "fine-tuning",
    "jobs",
    "checkpoints"
  ],
  "action": "list",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.fine_tuning.jobs.checkpoints.list(\"ft-AF1WoRqd3aJAHsqc9NY7iL8F\", after: \"after\", limit: 10)\npp result\n",
  "typeReference": {
    "signature": "client.fine_tuning.jobs.checkpoints.list(fine_tuning_job_id, after: nil, limit: nil) -> Page<Models::FineTuningJobCheckpoint>",
    "request": {
      "fields": [
        {
          "name": "after",
          "langType": "String",
          "required": false,
          "description": "Identifier for the last checkpoint ID from the previous pagination request."
        },
        {
          "name": "limit",
          "langType": "Integer",
          "required": false,
          "description": "Number of checkpoints to retrieve."
        }
      ]
    },
    "response": {
      "typeName": "Page<Models::FineTuningJobCheckpoint>",
      "note": "paginated (Page)",
      "fields": [
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "The checkpoint identifier, which can be referenced in the API endpoints."
        },
        {
          "name": "created_at",
          "langType": "Integer",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the checkpoint was created."
        },
        {
          "name": "fine_tuned_model_checkpoint",
          "langType": "String",
          "required": true,
          "description": "The name of the fine-tuned checkpoint model that is created."
        },
        {
          "name": "step_number",
          "langType": "Integer",
          "required": true,
          "description": "The step number that the checkpoint was created at."
        },
        {
          "name": "metrics",
          "langType": "Models::FineTuningJobCheckpointMetrics",
          "required": true,
          "description": "Metrics at the step number during the fine-tuning job.",
          "refTypeName": "FineTuningJobCheckpointMetrics"
        },
        {
          "name": "fine_tuning_job_id",
          "langType": "String",
          "required": true,
          "description": "The name of the fine-tuning job that this checkpoint was created from."
        },
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "The object type, which is always \"fine_tuning.job.checkpoint\"."
        }
      ]
    }
  }
}

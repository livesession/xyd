# Code generated by opensdk. DO NOT EDIT.

module Rooty
  # Client drives the rooty CLI (the `rooty` binary).
  class Client
    # Creates a client that runs `rooty` (override with the ROOTY_BIN env var or bin_path:).
    def initialize(bin_path: nil, env: nil, cwd: nil, timeout_ms: nil)
      @runner = Rooty::Runner.new(bin_path: bin_path, env: env, cwd: cwd, timeout_ms: timeout_ms)
    end

    # Initialize a project
    #
    # @return [CommandResult]
    def init
      argv = ["init"]
      @runner.run(argv)
    end

    # Print help
    #
    # @return [CommandResult]
    def opt_help
      argv = []
      argv << "--help"
      @runner.run(argv)
    end

    # Print version
    #
    # @return [CommandResult]
    def opt_version
      argv = []
      argv << "--version"
      @runner.run(argv)
    end
  end
end

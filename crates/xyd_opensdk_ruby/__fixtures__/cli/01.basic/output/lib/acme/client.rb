# Code generated by opensdk. DO NOT EDIT.

module Acme
  # Client drives the acme CLI (the `acme` binary).
  class Client
    attr_reader :projects

    # Creates a client that runs `acme` (override with the ACME_BIN env var or bin_path:).
    def initialize(bin_path: nil, env: nil, cwd: nil, timeout_ms: nil)
      @runner = Acme::Runner.new(bin_path: bin_path, env: env, cwd: cwd, timeout_ms: timeout_ms)
      @projects = Acme::Resources::Projects.new(@runner)
    end

    # Print the CLI version and exit
    #
    # @return [CommandResult]
    def opt_version
      argv = []
      argv << "--version"
      @runner.run(argv)
    end

    # Show status
    #
    # @param format [String]
    # @return [CommandResult]
    def status(format: nil)
      argv = ["status"]
      argv.push("--format", format) unless format.nil?
      @runner.run(argv)
    end
  end
end

# Code generated by opensdk. DO NOT EDIT.

module Openai
  module Resources
    class Audio
      attr_reader :speech, :transcriptions, :translations, :voice_consents, :voices

      # @api private
      def initialize(transport)
        @transport = transport
        @speech = AudioSpeech.new(transport)
        @transcriptions = AudioTranscriptions.new(transport)
        @translations = AudioTranslations.new(transport)
        @voice_consents = AudioVoiceConsents.new(transport)
        @voices = AudioVoices.new(transport)
      end
    end

    class AudioSpeech
      # @api private
      def initialize(transport)
        @transport = transport
      end

      # Generates audio from the input text.
      #
      # Returns the audio file content, or a stream of audio events.
      #
      # @param model [Models::CreateSpeechRequestModel]
      # @param input [String]
      # @param instructions [String]
      # @param voice [Models::VoiceIdsOrCustomVoice]
      # @param response_format [Models::CreateSpeechRequestResponseFormat]
      # @param speed [Float]
      # @param stream_format [Models::CreateSpeechRequestStreamFormat]
      # @return [String]
      def create(model:, input:, instructions: nil, voice:, response_format: nil, speed: nil, stream_format: nil)
        @transport.request(
          method: :post,
          path: "/audio/speech",
          body: { "model" => model, "input" => input, "instructions" => instructions, "voice" => voice, "response_format" => response_format, "speed" => speed, "stream_format" => stream_format },
          headers: { "Accept" => "application/octet-stream" },
          raw: true
        )
      end
    end

    class AudioTranscriptions
      # @api private
      def initialize(transport)
        @transport = transport
      end

      # Transcribes audio into the input language.
      #
      # Returns a transcription object in `json`, `diarized_json`, or `verbose_json`
      # format, or a stream of transcript events.
      #
      # @param file [IO, String]
      # @param model [Models::CreateTranscriptionRequestModel]
      # @param language [String]
      # @param prompt [String]
      # @param response_format [Models::AudioResponseFormat]
      # @param temperature [Float]
      # @param include [Array<Models::TranscriptionInclude>]
      # @param timestamp_granularities [Array<Models::CreateTranscriptionRequestTimestampGranularitiesItem>]
      # @param stream [Boolean]
      # @param chunking_strategy [Models::CreateTranscriptionRequestChunkingStrategy]
      # @param known_speaker_names [Array<String>]
      # @param known_speaker_references [Array<String>]
      # @return [Models::AudioTranscriptionsCreateResponse]
      def create(file:, model:, language: nil, prompt: nil, response_format: nil, temperature: nil, include: nil, timestamp_granularities: nil, stream: nil, chunking_strategy: nil, known_speaker_names: nil, known_speaker_references: nil)
        @transport.request(
          method: :post,
          path: "/audio/transcriptions",
          body: { "file" => file, "model" => model, "language" => language, "prompt" => prompt, "response_format" => response_format, "temperature" => temperature, "include" => include, "timestamp_granularities" => timestamp_granularities, "stream" => stream, "chunking_strategy" => chunking_strategy, "known_speaker_names" => known_speaker_names, "known_speaker_references" => known_speaker_references },
          encoding: "multipart"
        )
      end
    end

    class AudioTranslations
      # @api private
      def initialize(transport)
        @transport = transport
      end

      # Translates audio into English.
      #
      # @param file [IO, String]
      # @param model [Models::CreateTranslationRequestModel]
      # @param prompt [String]
      # @param response_format [Models::CreateTranslationRequestResponseFormat]
      # @param temperature [Float]
      # @return [Models::AudioTranslationsCreateResponse]
      def create(file:, model:, prompt: nil, response_format: nil, temperature: nil)
        @transport.request(
          method: :post,
          path: "/audio/translations",
          body: { "file" => file, "model" => model, "prompt" => prompt, "response_format" => response_format, "temperature" => temperature },
          encoding: "multipart"
        )
      end
    end

    class AudioVoiceConsents
      # @api private
      def initialize(transport)
        @transport = transport
      end

      # Upload a consent recording that authorizes creation of a custom voice.
      #
      # See the [custom voices guide](/docs/guides/text-to-speech#custom-voices) for requirements and best practices. Custom voices are limited to eligible customers.
      #
      # @param name [String]
      # @param recording [IO, String]
      # @param language [String]
      # @return [Models::VoiceConsentResource]
      def create(name:, recording:, language:)
        @transport.request(
          method: :post,
          path: "/audio/voice_consents",
          body: { "name" => name, "recording" => recording, "language" => language },
          encoding: "multipart"
        )
      end

      # Delete a consent recording that was uploaded for creating custom voices.
      #
      # See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
      #
      # @param consent_id [String]
      # @return [Models::VoiceConsentDeletedResource]
      def delete(consent_id)
        raise ArgumentError, "Expected a non-empty value for `consent_id`" if consent_id.nil? || consent_id.to_s.empty?

        @transport.request(
          method: :delete,
          path: "/audio/voice_consents/#{consent_id}"
        )
      end

      # List consent recordings available to your organization for creating custom voices.
      #
      # See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
      #
      # @param after [String]
      # @param limit [Integer]
      # @return [Page<Models::VoiceConsentResource>]
      def list(after: nil, limit: nil)
        response = @transport.request(
          method: :get,
          path: "/audio/voice_consents",
          query: { "after" => after, "limit" => limit }
        )
        Openai::Page.new(data: (response || {})[:data], has_more: (response || {})[:has_more])
      end

      # Retrieve consent recording metadata used for creating custom voices.
      #
      # See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
      #
      # @param consent_id [String]
      # @return [Models::VoiceConsentResource]
      def retrieve(consent_id)
        raise ArgumentError, "Expected a non-empty value for `consent_id`" if consent_id.nil? || consent_id.to_s.empty?

        @transport.request(
          method: :get,
          path: "/audio/voice_consents/#{consent_id}"
        )
      end

      # Update consent recording metadata used for creating custom voices. This endpoint updates metadata only and does not replace the underlying audio.
      #
      # See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
      #
      # @param consent_id [String]
      # @param name [String]
      # @return [Models::VoiceConsentResource]
      def update(consent_id, name:)
        raise ArgumentError, "Expected a non-empty value for `consent_id`" if consent_id.nil? || consent_id.to_s.empty?

        @transport.request(
          method: :post,
          path: "/audio/voice_consents/#{consent_id}",
          body: { "name" => name }
        )
      end
    end

    class AudioVoices
      # @api private
      def initialize(transport)
        @transport = transport
      end

      # Create a custom voice you can use for audio output (for example, in Text-to-Speech and the Realtime API). This requires an audio sample and a previously uploaded consent recording.
      #
      # See the [custom voices guide](/docs/guides/text-to-speech#custom-voices) for requirements and best practices. Custom voices are limited to eligible customers.
      #
      # @param name [String]
      # @param audio_sample [IO, String]
      # @param consent [String]
      # @return [Models::VoiceResource]
      def create(name:, audio_sample:, consent:)
        @transport.request(
          method: :post,
          path: "/audio/voices",
          body: { "name" => name, "audio_sample" => audio_sample, "consent" => consent },
          encoding: "multipart"
        )
      end
    end
  end
end

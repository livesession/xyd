// Code generated by opensdk. DO NOT EDIT.

import { CLIClient } from './core/command';
import type { CommandResult } from './core/command';

/** The flags CLI client (drives the `flags` binary; override with the FLAGS_BIN env var or the binPath option). */
export class Flags extends CLIClient {
  /**
   * Run the thing
   *
   * Runs `flags run`.
   */
  run(params?: ClientRunParams): Promise<CommandResult> {
    const argv: string[] = ["run"];
    if (params?.verbose) {
      argv.push("--verbose");
    }
    if (params?.q) {
      argv.push("-q");
    }
    if (params?.count !== undefined) {
      argv.push("--count", String(params.count));
    }
    if (params?.ratio !== undefined) {
      argv.push("--ratio", String(params.ratio));
    }
    return this._run(argv);
  }
}

export interface ClientRunParams {
  verbose?: boolean;
  q?: boolean;
  count?: number;
  ratio?: number;
}

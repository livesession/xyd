// Code generated by opensdk. DO NOT EDIT.

import { CLIClient } from './core/command';
import type { ClientOptions, CommandResult } from './core/command';
import { Projects } from './resources/projects';

/** The acme CLI client (drives the `acme` binary; override with the ACME_BIN env var or the binPath option). */
export class Acme extends CLIClient {
  readonly projects: Projects;

  constructor(options: ClientOptions = {}) {
    super(options);
    this.projects = new Projects(this);
  }

  /**
   * Print the CLI version and exit
   *
   * Runs `acme`.
   */
  optVersion(): Promise<CommandResult> {
    const argv: string[] = [];
    argv.push("--version");
    return this._run(argv);
  }

  /**
   * Show status
   *
   * Runs `acme status`.
   */
  status(params?: ClientStatusParams): Promise<CommandResult> {
    const argv: string[] = ["status"];
    if (params?.format !== undefined) {
      argv.push("--format", params.format);
    }
    return this._run(argv);
  }
}

export interface ClientStatusParams {
  format?: string;
}

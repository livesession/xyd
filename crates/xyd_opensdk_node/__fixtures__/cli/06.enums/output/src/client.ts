// Code generated by opensdk. DO NOT EDIT.

import { CLIClient } from './core/command';
import type { CommandResult } from './core/command';
import type { ConvertLevel, ConvertTarget } from './models';

/** The acme CLI client (drives the `acme` binary; override with the ACME_BIN env var or the binPath option). */
export class Acme extends CLIClient {
  /**
   * Convert a document
   *
   * Runs `acme convert`.
   */
  convert(target: ConvertTarget, params?: ClientConvertParams): Promise<CommandResult> {
    const argv: string[] = ["convert"];
    argv.push(String(target));
    if (params?.level !== undefined) {
      argv.push("--level", String(params.level));
    }
    return this._run(argv);
  }
}

export interface ClientConvertParams {
  level?: ConvertLevel;
}

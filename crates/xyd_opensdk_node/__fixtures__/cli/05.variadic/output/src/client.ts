// Code generated by opensdk. DO NOT EDIT.

import { CLIClient } from './core/command';
import type { CommandResult } from './core/command';

/** The packer CLI client (drives the `packer` binary; override with the PACKER_BIN env var or the binPath option). */
export class Packer extends CLIClient {
  /**
   * Pack files
   *
   * Runs `packer pack`.
   */
  pack(dest: string, files: string[], params?: ClientPackParams): Promise<CommandResult> {
    if (!dest) {
      throw new Error("missing required dest parameter");
    }
    const argv: string[] = ["pack"];
    argv.push(dest);
    for (const value of files) {
      argv.push(value);
    }
    for (const value of params?.tag ?? []) {
      argv.push("--tag", value);
    }
    if (params?.meta !== undefined) {
      argv.push("--meta", JSON.stringify(params.meta));
    }
    return this._run(argv);
  }
}

export interface ClientPackParams {
  tag?: string[];
  meta?: unknown;
}

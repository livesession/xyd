// Code generated by opensdk. DO NOT EDIT.

import { CLIClient } from './core/command';
import type { CommandResult } from './core/command';

/** The rooty CLI client (drives the `rooty` binary; override with the ROOTY_BIN env var or the binPath option). */
export class Rooty extends CLIClient {
  /**
   * Initialize a project
   *
   * Runs `rooty init`.
   */
  init(): Promise<CommandResult> {
    const argv: string[] = ["init"];
    return this._run(argv);
  }

  /**
   * Print help
   *
   * Runs `rooty`.
   */
  optHelp(): Promise<CommandResult> {
    const argv: string[] = [];
    argv.push("--help");
    return this._run(argv);
  }

  /**
   * Print version
   *
   * Runs `rooty`.
   */
  optVersion(): Promise<CommandResult> {
    const argv: string[] = [];
    argv.push("--version");
    return this._run(argv);
  }
}

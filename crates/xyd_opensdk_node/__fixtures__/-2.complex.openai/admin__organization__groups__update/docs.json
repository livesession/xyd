{
  "chain": [
    "admin",
    "organization",
    "groups"
  ],
  "action": "update",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.admin.organization.groups.update(\"group_id\", { name: \"string\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.admin.organization.groups.update(groupId, body): Promise<GroupResourceWithSuccess>",
    "request": {
      "typeName": "AdminOrganizationGroupUpdateParams",
      "argName": "body",
      "fields": [
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "New display name for the group."
        }
      ]
    },
    "response": {
      "typeName": "GroupResourceWithSuccess",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "Identifier for the group."
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "Updated display name for the group."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "Unix timestamp (in seconds) when the group was created."
        },
        {
          "name": "is_scim_managed",
          "langType": "boolean",
          "required": true,
          "description": "Whether the group is managed through SCIM and controlled by your identity provider."
        }
      ]
    }
  }
}

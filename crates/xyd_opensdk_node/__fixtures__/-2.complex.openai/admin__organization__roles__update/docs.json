{
  "chain": [
    "admin",
    "organization",
    "roles"
  ],
  "action": "update",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.admin.organization.roles.update(\"role_id\", { permissions: [\"string\"], description: \"string\", role_name: \"string\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.admin.organization.roles.update(roleId, body): Promise<Role>",
    "request": {
      "typeName": "AdminOrganizationRoleUpdateParams",
      "argName": "body",
      "fields": [
        {
          "name": "permissions",
          "langType": "string[] | null",
          "required": false,
          "description": "Updated set of permissions for the role."
        },
        {
          "name": "description",
          "langType": "string | null",
          "required": false,
          "description": "New description for the role."
        },
        {
          "name": "role_name",
          "langType": "string | null",
          "required": false,
          "description": "New name for the role."
        }
      ]
    },
    "response": {
      "typeName": "Role",
      "fields": [
        {
          "name": "object",
          "langType": "\"role\"",
          "required": true,
          "description": "Always `role`."
        },
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "Identifier for the role."
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "Unique name for the role."
        },
        {
          "name": "description",
          "langType": "string | null",
          "required": true,
          "description": "Optional description of the role."
        },
        {
          "name": "permissions",
          "langType": "string[]",
          "required": true,
          "description": "Permissions granted by the role."
        },
        {
          "name": "resource_type",
          "langType": "string",
          "required": true,
          "description": "Resource type the role is bound to (for example `api.organization` or `api.project`)."
        },
        {
          "name": "predefined_role",
          "langType": "boolean",
          "required": true,
          "description": "Whether the role is predefined and managed by OpenAI."
        }
      ]
    }
  }
}

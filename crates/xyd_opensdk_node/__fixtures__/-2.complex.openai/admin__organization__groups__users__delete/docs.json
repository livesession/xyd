{
  "chain": [
    "admin",
    "organization",
    "groups",
    "users"
  ],
  "action": "delete",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.admin.organization.groups.users.delete(\"group_id\", \"user_id\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.admin.organization.groups.users.delete(groupId, userId): Promise<GroupUserDeletedResource>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "GroupUserDeletedResource",
      "fields": [
        {
          "name": "object",
          "langType": "\"group.user.deleted\"",
          "required": true,
          "description": "Always `group.user.deleted`."
        },
        {
          "name": "deleted",
          "langType": "boolean",
          "required": true,
          "description": "Whether the group membership was removed."
        }
      ]
    }
  }
}

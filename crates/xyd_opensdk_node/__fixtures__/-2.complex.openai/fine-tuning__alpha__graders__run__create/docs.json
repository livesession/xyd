{
  "chain": [
    "fine-tuning",
    "alpha",
    "graders",
    "run"
  ],
  "action": "create",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.fineTuning.alpha.graders.run.create({ grader: { type: \"string_check\", name: \"string\", input: \"string\", reference: \"string\", operation: \"eq\" }, model_sample: \"string\", item: { key: {} as any } });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.fineTuning.alpha.graders.run.create(body): Promise<RunGraderResponse>",
    "request": {
      "typeName": "FineTuningAlphaGraderRunCreateParams",
      "argName": "body",
      "fields": [
        {
          "name": "grader",
          "langType": "RunGraderRequestGrader",
          "required": true,
          "description": "The grader used for the fine-tuning job.",
          "refTypeName": "RunGraderRequestGrader"
        },
        {
          "name": "item",
          "langType": "Record<string, unknown>",
          "required": false,
          "description": "The dataset item provided to the grader. This will be used to populate \nthe `item` namespace. See [the guide](/docs/guides/graders) for more details. \n"
        },
        {
          "name": "model_sample",
          "langType": "string",
          "required": true,
          "description": "The model sample to be evaluated. This value will be used to populate \nthe `sample` namespace. See [the guide](/docs/guides/graders) for more details.\nThe `output_json` variable will be populated if the model sample is a \nvalid JSON string.\n \n"
        }
      ]
    },
    "response": {
      "typeName": "RunGraderResponse",
      "fields": [
        {
          "name": "reward",
          "langType": "number",
          "required": true
        },
        {
          "name": "metadata",
          "langType": "RunGraderResponseMetadata",
          "required": true,
          "refTypeName": "RunGraderResponseMetadata"
        },
        {
          "name": "sub_rewards",
          "langType": "Record<string, unknown>",
          "required": true
        },
        {
          "name": "model_grader_token_usage_per_model",
          "langType": "Record<string, unknown>",
          "required": true
        }
      ]
    }
  }
}

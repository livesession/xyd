{
  "chain": [
    "admin",
    "organization",
    "groups",
    "users"
  ],
  "action": "list",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.admin.organization.groups.users.list(\"group_id\", { limit: 100, after: \"after\", order: \"asc\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.admin.organization.groups.users.list(groupId, query?): Promise<CursorPage<GroupUser>>",
    "request": {
      "typeName": "AdminOrganizationGroupUserListParams",
      "argName": "query",
      "fields": [
        {
          "name": "limit",
          "langType": "number",
          "required": false,
          "description": "A limit on the number of users to be returned. Limit can range between 0 and 1000, and the default is 100.\n"
        },
        {
          "name": "after",
          "langType": "string",
          "required": false,
          "description": "A cursor for use in pagination. Provide the ID of the last user from the previous list response to retrieve the next page.\n"
        },
        {
          "name": "order",
          "langType": "AdminOrganizationGroupsUsersListOrder",
          "required": false,
          "description": "Specifies the sort order of users in the list.",
          "refTypeName": "AdminOrganizationGroupsUsersListOrder"
        }
      ]
    },
    "response": {
      "typeName": "GroupUser",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "The name of the user."
        },
        {
          "name": "email",
          "langType": "string | null",
          "required": true,
          "description": "The email address of the user."
        }
      ]
    }
  }
}

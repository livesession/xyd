{
  "chain": [
    "chat",
    "completions"
  ],
  "action": "update",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.chat.completions.update(\"completion_id\", { metadata: { key: \"string\" } });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.chat.completions.update(completionId, body): Promise<CreateChatCompletionResponse>",
    "request": {
      "typeName": "ChatCompletionUpdateParams",
      "argName": "body",
      "fields": [
        {
          "name": "metadata",
          "langType": "Metadata",
          "required": true,
          "refTypeName": "Metadata"
        }
      ]
    },
    "response": {
      "typeName": "CreateChatCompletionResponse",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "A unique identifier for the chat completion."
        },
        {
          "name": "choices",
          "langType": "CreateChatCompletionResponseChoicesItem[]",
          "required": true,
          "description": "A list of chat completion choices. Can be more than one if `n` is greater than 1."
        },
        {
          "name": "created",
          "langType": "number",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the chat completion was created."
        },
        {
          "name": "model",
          "langType": "string",
          "required": true,
          "description": "The model used for the chat completion."
        },
        {
          "name": "service_tier",
          "langType": "ServiceTier",
          "required": false,
          "refTypeName": "ServiceTier"
        },
        {
          "name": "system_fingerprint",
          "langType": "string",
          "required": false,
          "description": "This fingerprint represents the backend configuration that the model runs with.\n\nCan be used in conjunction with the `seed` request parameter to understand when backend changes have been made that might impact determinism.\n",
          "deprecated": true
        },
        {
          "name": "object",
          "langType": "\"chat.completion\"",
          "required": true,
          "description": "The object type, which is always `chat.completion`."
        },
        {
          "name": "usage",
          "langType": "CompletionUsage",
          "required": false,
          "refTypeName": "CompletionUsage"
        }
      ]
    }
  }
}

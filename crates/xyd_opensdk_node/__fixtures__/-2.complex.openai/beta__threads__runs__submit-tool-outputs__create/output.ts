// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { APIClient, RequestOptions } from '../core/request';
import type { RunObject, SubmitToolOutputsRunRequestToolOutputsItem } from '../models';

export class Beta extends APIResource {
  readonly threads: BetaThreads;

  constructor(client: APIClient) {
    super(client);
    this.threads = new BetaThreads(client);
  }
}

export class BetaThreads extends APIResource {
  readonly runs: BetaThreadsRuns;

  constructor(client: APIClient) {
    super(client);
    this.runs = new BetaThreadsRuns(client);
  }
}

export class BetaThreadsRuns extends APIResource {
  readonly submitToolOutputs: BetaThreadsRunsSubmitToolOutputs;

  constructor(client: APIClient) {
    super(client);
    this.submitToolOutputs = new BetaThreadsRunsSubmitToolOutputs(client);
  }
}

export class BetaThreadsRunsSubmitToolOutputs extends APIResource {
  /** When a run has the `status: "requires_action"` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request. */
  create(threadId: string, runId: string, body: BetaThreadRunSubmitToolOutputCreateParams, options?: RequestOptions): Promise<RunObject> {
    if (!threadId) {
      throw new Error("missing required thread_id parameter");
    }
    if (!runId) {
      throw new Error("missing required run_id parameter");
    }
    return this._client.request<RunObject>({ method: "POST", path: `/threads/${threadId}/runs/${runId}/submit_tool_outputs`, body }, options);
  }
}

export interface BetaThreadRunSubmitToolOutputCreateParams {
  tool_outputs: SubmitToolOutputsRunRequestToolOutputsItem[];
  stream?: boolean | null;
}

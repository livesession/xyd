{
  "chain": [
    "beta",
    "chat-kit",
    "threads"
  ],
  "action": "delete",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.beta.chatKit.threads.delete(\"cthr_123\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.beta.chatKit.threads.delete(threadId): Promise<DeletedThreadResource>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "DeletedThreadResource",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "Identifier of the deleted thread."
        },
        {
          "name": "object",
          "langType": "\"chatkit.thread.deleted\"",
          "required": true,
          "description": "Type discriminator that is always `chatkit.thread.deleted`."
        },
        {
          "name": "deleted",
          "langType": "boolean",
          "required": true,
          "description": "Indicates that the thread has been deleted."
        }
      ]
    }
  }
}

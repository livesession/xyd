{
  "chain": [
    "admin",
    "organization",
    "projects",
    "groups"
  ],
  "action": "list",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.admin.organization.projects.groups.list(\"project_id\", { limit: 20, after: \"after\", order: \"asc\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.groups.list(projectId, query?): Promise<CursorPage<ProjectGroup>>",
    "request": {
      "typeName": "AdminOrganizationProjectGroupListParams",
      "argName": "query",
      "fields": [
        {
          "name": "limit",
          "langType": "number",
          "required": false,
          "description": "A limit on the number of project groups to return. Defaults to 20."
        },
        {
          "name": "after",
          "langType": "string",
          "required": false,
          "description": "Cursor for pagination. Provide the ID of the last group from the previous response to fetch the next page."
        },
        {
          "name": "order",
          "langType": "AdminOrganizationProjectsGroupsListOrder",
          "required": false,
          "description": "Sort order for the returned groups.",
          "refTypeName": "AdminOrganizationProjectsGroupsListOrder"
        }
      ]
    },
    "response": {
      "typeName": "ProjectGroup",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "object",
          "langType": "\"project.group\"",
          "required": true,
          "description": "Always `project.group`."
        },
        {
          "name": "project_id",
          "langType": "string",
          "required": true,
          "description": "Identifier of the project."
        },
        {
          "name": "group_id",
          "langType": "string",
          "required": true,
          "description": "Identifier of the group that has access to the project."
        },
        {
          "name": "group_name",
          "langType": "string",
          "required": true,
          "description": "Display name of the group."
        },
        {
          "name": "group_type",
          "langType": "string",
          "required": true,
          "description": "The type of the group."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "Unix timestamp (in seconds) when the group was granted project access."
        }
      ]
    }
  }
}

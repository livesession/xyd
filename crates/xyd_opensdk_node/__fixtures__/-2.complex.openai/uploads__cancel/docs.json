{
  "chain": [
    "uploads"
  ],
  "action": "cancel",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.uploads.cancel(\"upload_abc123\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.uploads.cancel(uploadId): Promise<Upload>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "Upload",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The Upload unique identifier, which can be referenced in API endpoints."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the Upload was created."
        },
        {
          "name": "filename",
          "langType": "string",
          "required": true,
          "description": "The name of the file to be uploaded."
        },
        {
          "name": "bytes",
          "langType": "number",
          "required": true,
          "description": "The intended number of bytes to be uploaded."
        },
        {
          "name": "purpose",
          "langType": "string",
          "required": true,
          "description": "The intended purpose of the file. [Please refer here](/docs/api-reference/files/object#files/object-purpose) for acceptable values."
        },
        {
          "name": "status",
          "langType": "UploadStatus",
          "required": true,
          "description": "The status of the Upload.",
          "refTypeName": "UploadStatus"
        },
        {
          "name": "expires_at",
          "langType": "number",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the Upload will expire."
        },
        {
          "name": "object",
          "langType": "\"upload\"",
          "required": false,
          "description": "The object type, which is always \"upload\"."
        },
        {
          "name": "file",
          "langType": "OpenAiFile | null",
          "required": false,
          "refTypeName": "OpenAiFile"
        }
      ]
    }
  }
}

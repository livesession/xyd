{
  "chain": [
    "admin",
    "organization",
    "projects",
    "users"
  ],
  "action": "retrieve",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.admin.organization.projects.users.retrieve(\"project_id\", \"user_id\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.users.retrieve(projectId, userId): Promise<ProjectUser>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "ProjectUser",
      "fields": [
        {
          "name": "object",
          "langType": "\"organization.project.user\"",
          "required": true,
          "description": "The object type, which is always `organization.project.user`"
        },
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "name",
          "langType": "string | null",
          "required": false,
          "description": "The name of the user"
        },
        {
          "name": "email",
          "langType": "string | null",
          "required": false,
          "description": "The email address of the user"
        },
        {
          "name": "role",
          "langType": "string",
          "required": true,
          "description": "`owner` or `member`"
        },
        {
          "name": "added_at",
          "langType": "number",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the project was added."
        }
      ]
    }
  }
}

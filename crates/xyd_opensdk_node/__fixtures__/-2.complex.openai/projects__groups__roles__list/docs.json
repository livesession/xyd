{
  "chain": [
    "projects",
    "groups",
    "roles"
  ],
  "action": "list",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.projects.groups.roles.list(\"project_id\", \"group_id\", { limit: 1, after: \"after\", order: \"asc\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.projects.groups.roles.list(projectId, groupId, query?): Promise<CursorPage<AssignedRoleDetails>>",
    "request": {
      "typeName": "ProjectGroupRoleListParams",
      "argName": "query",
      "fields": [
        {
          "name": "limit",
          "langType": "number",
          "required": false,
          "description": "A limit on the number of project role assignments to return."
        },
        {
          "name": "after",
          "langType": "string",
          "required": false,
          "description": "Cursor for pagination. Provide the value from the previous response's `next` field to continue listing project roles."
        },
        {
          "name": "order",
          "langType": "ProjectsGroupsRolesListOrder",
          "required": false,
          "description": "Sort order for the returned project roles.",
          "refTypeName": "ProjectsGroupsRolesListOrder"
        }
      ]
    },
    "response": {
      "typeName": "AssignedRoleDetails",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "Identifier for the role."
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "Name of the role."
        },
        {
          "name": "permissions",
          "langType": "string[]",
          "required": true,
          "description": "Permissions associated with the role."
        },
        {
          "name": "resource_type",
          "langType": "string",
          "required": true,
          "description": "Resource type the role applies to."
        },
        {
          "name": "predefined_role",
          "langType": "boolean",
          "required": true,
          "description": "Whether the role is predefined by OpenAI."
        },
        {
          "name": "description",
          "langType": "string | null",
          "required": true,
          "description": "Description of the role."
        },
        {
          "name": "created_at",
          "langType": "number | null",
          "required": true,
          "description": "When the role was created."
        },
        {
          "name": "updated_at",
          "langType": "number | null",
          "required": true,
          "description": "When the role was last updated."
        },
        {
          "name": "created_by",
          "langType": "string | null",
          "required": true,
          "description": "Identifier of the actor who created the role."
        },
        {
          "name": "created_by_user_obj",
          "langType": "Record<string, unknown> | null",
          "required": true,
          "description": "User details for the actor that created the role, when available."
        },
        {
          "name": "metadata",
          "langType": "Record<string, unknown> | null",
          "required": true,
          "description": "Arbitrary metadata stored on the role."
        }
      ]
    }
  }
}

{
  "chain": [
    "evals"
  ],
  "action": "create",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.evals.create({ data_source_config: { type: \"custom\", item_schema: { key: {} as any }, include_sample_schema: false }, testing_criteria: [{ type: \"label_model\", name: \"string\", model: \"string\", input: [{ role: \"string\", content: \"string\" }], labels: [\"string\"], passing_labels: [\"string\"] }], name: \"string\", metadata: { key: \"string\" } });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.evals.create(body): Promise<Eval>",
    "request": {
      "typeName": "EvalCreateParams",
      "argName": "body",
      "fields": [
        {
          "name": "name",
          "langType": "string",
          "required": false,
          "description": "The name of the evaluation."
        },
        {
          "name": "metadata",
          "langType": "Metadata",
          "required": false,
          "refTypeName": "Metadata"
        },
        {
          "name": "data_source_config",
          "langType": "CreateEvalRequestDataSourceConfig",
          "required": true,
          "description": "The configuration for the data source used for the evaluation runs. Dictates the schema of the data used in the evaluation.",
          "refTypeName": "CreateEvalRequestDataSourceConfig"
        },
        {
          "name": "testing_criteria",
          "langType": "CreateEvalRequestTestingCriteriaItem[]",
          "required": true,
          "description": "A list of graders for all eval runs in this group. Graders can reference variables in the data source using double curly braces notation, like `{{item.variable_name}}`. To reference the model's output, use the `sample` namespace (ie, `{{sample.output_text}}`)."
        }
      ]
    },
    "response": {
      "typeName": "Eval",
      "fields": [
        {
          "name": "object",
          "langType": "\"eval\"",
          "required": true,
          "description": "The object type."
        },
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "Unique identifier for the evaluation."
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "The name of the evaluation."
        },
        {
          "name": "data_source_config",
          "langType": "EvalDataSourceConfig",
          "required": true,
          "description": "Configuration of data sources used in runs of the evaluation.",
          "refTypeName": "EvalDataSourceConfig"
        },
        {
          "name": "testing_criteria",
          "langType": "EvalTestingCriteriaItem[]",
          "required": true,
          "description": "A list of testing criteria."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the eval was created."
        },
        {
          "name": "metadata",
          "langType": "Metadata",
          "required": true,
          "refTypeName": "Metadata"
        }
      ]
    }
  }
}

{
  "chain": [
    "projects",
    "roles"
  ],
  "action": "create",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.projects.roles.create(\"project_id\", { role_name: \"string\", permissions: [\"string\"], description: \"string\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.projects.roles.create(projectId, body): Promise<Role>",
    "request": {
      "typeName": "ProjectRoleCreateParams",
      "argName": "body",
      "fields": [
        {
          "name": "role_name",
          "langType": "string",
          "required": true,
          "description": "Unique name for the role."
        },
        {
          "name": "permissions",
          "langType": "string[]",
          "required": true,
          "description": "Permissions to grant to the role."
        },
        {
          "name": "description",
          "langType": "string | null",
          "required": false,
          "description": "Optional description of the role."
        }
      ]
    },
    "response": {
      "typeName": "Role",
      "fields": [
        {
          "name": "object",
          "langType": "\"role\"",
          "required": true,
          "description": "Always `role`."
        },
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "Identifier for the role."
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "Unique name for the role."
        },
        {
          "name": "description",
          "langType": "string | null",
          "required": true,
          "description": "Optional description of the role."
        },
        {
          "name": "permissions",
          "langType": "string[]",
          "required": true,
          "description": "Permissions granted by the role."
        },
        {
          "name": "resource_type",
          "langType": "string",
          "required": true,
          "description": "Resource type the role is bound to (for example `api.organization` or `api.project`)."
        },
        {
          "name": "predefined_role",
          "langType": "boolean",
          "required": true,
          "description": "Whether the role is predefined and managed by OpenAI."
        }
      ]
    }
  }
}

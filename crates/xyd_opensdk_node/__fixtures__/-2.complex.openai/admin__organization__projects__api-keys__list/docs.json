{
  "chain": [
    "admin",
    "organization",
    "projects",
    "api-keys"
  ],
  "action": "list",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.admin.organization.projects.apiKeys.list(\"project_id\", { limit: 20, after: \"after\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.apiKeys.list(projectId, query?): Promise<CursorPage<ProjectApiKey>>",
    "request": {
      "typeName": "AdminOrganizationProjectApiKeyListParams",
      "argName": "query",
      "fields": [
        {
          "name": "limit",
          "langType": "number",
          "required": false,
          "description": "A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.\n"
        },
        {
          "name": "after",
          "langType": "string",
          "required": false,
          "description": "A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n"
        }
      ]
    },
    "response": {
      "typeName": "ProjectApiKey",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "object",
          "langType": "\"organization.project.api_key\"",
          "required": true,
          "description": "The object type, which is always `organization.project.api_key`"
        },
        {
          "name": "redacted_value",
          "langType": "string",
          "required": true,
          "description": "The redacted value of the API key"
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "The name of the API key"
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the API key was created"
        },
        {
          "name": "last_used_at",
          "langType": "number | null",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the API key was last used."
        },
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "owner",
          "langType": "ProjectApiKeyOwner",
          "required": true,
          "refTypeName": "ProjectApiKeyOwner"
        }
      ]
    }
  }
}

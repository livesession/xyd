{
  "chain": [
    "containers"
  ],
  "action": "list",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.containers.list({ limit: 20, order: \"asc\", after: \"after\", name: \"name\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.containers.list(query?): Promise<CursorPage<ContainerResource>>",
    "request": {
      "typeName": "ContainerListParams",
      "argName": "query",
      "fields": [
        {
          "name": "limit",
          "langType": "number",
          "required": false,
          "description": "A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.\n"
        },
        {
          "name": "order",
          "langType": "ContainersListOrder",
          "required": false,
          "description": "Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.\n",
          "refTypeName": "ContainersListOrder"
        },
        {
          "name": "after",
          "langType": "string",
          "required": false,
          "description": "A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n"
        },
        {
          "name": "name",
          "langType": "string",
          "required": false,
          "description": "Filter results by container name."
        }
      ]
    },
    "response": {
      "typeName": "ContainerResource",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "Unique identifier for the container."
        },
        {
          "name": "object",
          "langType": "string",
          "required": true,
          "description": "The type of this object."
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "Name of the container."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "Unix timestamp (in seconds) when the container was created."
        },
        {
          "name": "status",
          "langType": "string",
          "required": true,
          "description": "Status of the container (e.g., active, deleted)."
        },
        {
          "name": "last_active_at",
          "langType": "number",
          "required": false,
          "description": "Unix timestamp (in seconds) when the container was last active."
        },
        {
          "name": "expires_after",
          "langType": "ContainerResourceExpiresAfter",
          "required": false,
          "description": "The container will expire after this time period.\nThe anchor is the reference point for the expiration.\nThe minutes is the number of minutes after the anchor before the container expires.\n",
          "refTypeName": "ContainerResourceExpiresAfter"
        },
        {
          "name": "memory_limit",
          "langType": "ContainerResourceMemoryLimit",
          "required": false,
          "description": "The memory limit configured for the container.",
          "refTypeName": "ContainerResourceMemoryLimit"
        },
        {
          "name": "network_policy",
          "langType": "ContainerResourceNetworkPolicy",
          "required": false,
          "description": "Network access policy for the container.",
          "refTypeName": "ContainerResourceNetworkPolicy"
        }
      ]
    }
  }
}

// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { APIClient, RequestOptions } from '../core/request';
import type { DeleteCertificateResponse } from '../models';

export class Admin extends APIResource {
  readonly organization: AdminOrganization;

  constructor(client: APIClient) {
    super(client);
    this.organization = new AdminOrganization(client);
  }
}

export class AdminOrganization extends APIResource {
  readonly certificates: AdminOrganizationCertificates;

  constructor(client: APIClient) {
    super(client);
    this.certificates = new AdminOrganizationCertificates(client);
  }
}

export class AdminOrganizationCertificates extends APIResource {
  /**
   * Delete a certificate from the organization.
   *
   * The certificate must be inactive for the organization and all projects.
   */
  delete(certificateId: string, options?: RequestOptions): Promise<DeleteCertificateResponse> {
    if (!certificateId) {
      throw new Error("missing required certificate_id parameter");
    }
    return this._client.request<DeleteCertificateResponse>({ method: "DELETE", path: `/organization/certificates/${certificateId}` }, options);
  }
}

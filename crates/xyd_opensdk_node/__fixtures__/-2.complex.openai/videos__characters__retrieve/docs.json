{
  "chain": [
    "videos",
    "characters"
  ],
  "action": "retrieve",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.videos.characters.retrieve(\"char_123\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.videos.characters.retrieve(characterId): Promise<VideoCharacterResource>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "VideoCharacterResource",
      "fields": [
        {
          "name": "id",
          "langType": "string | null",
          "required": true
        },
        {
          "name": "name",
          "langType": "string | null",
          "required": true
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "Unix timestamp (in seconds) when the character was created."
        }
      ]
    }
  }
}

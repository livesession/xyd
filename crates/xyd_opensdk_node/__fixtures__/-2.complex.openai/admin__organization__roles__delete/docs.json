{
  "chain": [
    "admin",
    "organization",
    "roles"
  ],
  "action": "delete",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.admin.organization.roles.delete(\"role_id\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.admin.organization.roles.delete(roleId): Promise<RoleDeletedResource>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "RoleDeletedResource",
      "fields": [
        {
          "name": "object",
          "langType": "\"role.deleted\"",
          "required": true,
          "description": "Always `role.deleted`."
        },
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "Identifier of the deleted role."
        },
        {
          "name": "deleted",
          "langType": "boolean",
          "required": true,
          "description": "Whether the role was deleted."
        }
      ]
    }
  }
}

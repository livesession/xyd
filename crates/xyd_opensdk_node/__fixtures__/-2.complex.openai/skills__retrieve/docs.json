{
  "chain": [
    "skills"
  ],
  "action": "retrieve",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.skills.retrieve(\"skill_123\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.skills.retrieve(skillId): Promise<SkillResource>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "SkillResource",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "Unique identifier for the skill."
        },
        {
          "name": "object",
          "langType": "\"skill\"",
          "required": true,
          "description": "The object type, which is `skill`."
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "Name of the skill."
        },
        {
          "name": "description",
          "langType": "string",
          "required": true,
          "description": "Description of the skill."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "Unix timestamp (seconds) for when the skill was created."
        },
        {
          "name": "default_version",
          "langType": "string",
          "required": true,
          "description": "Default version for the skill."
        },
        {
          "name": "latest_version",
          "langType": "string",
          "required": true,
          "description": "Latest version for the skill."
        }
      ]
    }
  }
}

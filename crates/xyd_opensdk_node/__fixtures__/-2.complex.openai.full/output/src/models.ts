// Code generated by opensdk. DO NOT EDIT.

export interface AdminOrganizationAdminApiKeysCreateRequest {
  name: string;
}

export interface AdminApiKeyCreateResponse {
  /** The object type, which is always `organization.admin_api_key` */
  object: "organization.admin_api_key";
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the API key */
  name?: string | null;
  /** The redacted value of the API key */
  redacted_value: string;
  /** The Unix timestamp (in seconds) of when the API key was created */
  created_at: number;
  last_used_at?: number | null;
  owner: AdminApiKeyCreateResponseOwner;
  /** The value of the API key. Only shown on create. */
  value: string;
}

export interface AdminApiKeyCreateResponseOwner {
  /** Always `user` */
  type?: string;
  /** The object type, which is always organization.user */
  object?: string;
  /** The identifier, which can be referenced in API endpoints */
  id?: string;
  /** The name of the user */
  name?: string;
  /** The Unix timestamp (in seconds) of when the user was created */
  created_at?: number;
  /** Always `owner` */
  role?: string;
}

export interface AdminOrganizationAdminApiKeysDeleteResponse {
  id: string;
  object: "organization.admin_api_key.deleted";
  deleted: boolean;
}

/** Order results by creation time, ascending or descending. */
export type AdminOrganizationAdminApiKeysListOrder = "asc" | "desc";

export interface ApiKeyList {
  object: "list";
  data: AdminApiKey[];
  has_more: boolean;
  first_id?: string | null;
  last_id?: string | null;
}

/** Represents an individual Admin API key in an org. */
export interface AdminApiKey {
  /** The object type, which is always `organization.admin_api_key` */
  object: "organization.admin_api_key";
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the API key */
  name?: string | null;
  /** The redacted value of the API key */
  redacted_value: string;
  /** The Unix timestamp (in seconds) of when the API key was created */
  created_at: number;
  last_used_at?: number | null;
  owner: AdminApiKeyOwner;
}

export interface AdminApiKeyOwner {
  /** Always `user` */
  type?: string;
  /** The object type, which is always organization.user */
  object?: string;
  /** The identifier, which can be referenced in API endpoints */
  id?: string;
  /** The name of the user */
  name?: string;
  /** The Unix timestamp (in seconds) of when the user was created */
  created_at?: number;
  /** Always `owner` */
  role?: string;
}

export interface AdminOrganizationAuditLogsListEffectiveAt {
  /** Return only events whose `effective_at` (Unix seconds) is greater than this value. */
  gt?: number;
  /** Return only events whose `effective_at` (Unix seconds) is greater than or equal to this value. */
  gte?: number;
  /** Return only events whose `effective_at` (Unix seconds) is less than this value. */
  lt?: number;
  /** Return only events whose `effective_at` (Unix seconds) is less than or equal to this value. */
  lte?: number;
}

/** The event type. */
export type AuditLogEventType = "api_key.created" | "api_key.updated" | "api_key.deleted" | "certificate.created" | "certificate.updated" | "certificate.deleted" | "certificates.activated" | "certificates.deactivated" | "checkpoint.permission.created" | "checkpoint.permission.deleted" | "external_key.registered" | "external_key.removed" | "group.created" | "group.updated" | "group.deleted" | "invite.sent" | "invite.accepted" | "invite.deleted" | "ip_allowlist.created" | "ip_allowlist.updated" | "ip_allowlist.deleted" | "ip_allowlist.config.activated" | "ip_allowlist.config.deactivated" | "login.succeeded" | "login.failed" | "logout.succeeded" | "logout.failed" | "organization.updated" | "project.created" | "project.updated" | "project.archived" | "project.deleted" | "rate_limit.updated" | "rate_limit.deleted" | "resource.deleted" | "tunnel.created" | "tunnel.updated" | "tunnel.deleted" | "role.created" | "role.updated" | "role.deleted" | "role.assignment.created" | "role.assignment.deleted" | "scim.enabled" | "scim.disabled" | "service_account.created" | "service_account.updated" | "service_account.deleted" | "user.added" | "user.updated" | "user.deleted";

export interface ListAuditLogsResponse {
  object: "list";
  data: AuditLog[];
  first_id?: string | null;
  last_id?: string | null;
  has_more: boolean;
}

/** A log of a user action or configuration change within this organization. */
export interface AuditLog {
  /** The ID of this log. */
  id: string;
  type: AuditLogEventType;
  /** The Unix timestamp (in seconds) of the event. */
  effective_at: number;
  /** The project that the action was scoped to. Absent for actions not scoped to projects. Note that any admin actions taken via Admin API keys are associated with the default project. */
  project?: AuditLogProject;
  actor?: AuditLogActor | null;
  /** The details for events with this `type`. */
  "api_key.created"?: AuditLogApiKeyCreated;
  /** The details for events with this `type`. */
  "api_key.updated"?: AuditLogApiKeyUpdated;
  /** The details for events with this `type`. */
  "api_key.deleted"?: AuditLogApiKeyDeleted;
  /** The project and fine-tuned model checkpoint that the checkpoint permission was created for. */
  "checkpoint.permission.created"?: AuditLogCheckpointPermissionCreated;
  /** The details for events with this `type`. */
  "checkpoint.permission.deleted"?: AuditLogCheckpointPermissionDeleted;
  /** The details for events with this `type`. */
  "external_key.registered"?: AuditLogExternalKeyRegistered;
  /** The details for events with this `type`. */
  "external_key.removed"?: AuditLogExternalKeyRemoved;
  /** The details for events with this `type`. */
  "group.created"?: AuditLogGroupCreated;
  /** The details for events with this `type`. */
  "group.updated"?: AuditLogGroupUpdated;
  /** The details for events with this `type`. */
  "group.deleted"?: AuditLogGroupDeleted;
  /** The details for events with this `type`. */
  "scim.enabled"?: AuditLogScimEnabled;
  /** The details for events with this `type`. */
  "scim.disabled"?: AuditLogScimDisabled;
  /** The details for events with this `type`. */
  "invite.sent"?: AuditLogInviteSent;
  /** The details for events with this `type`. */
  "invite.accepted"?: AuditLogInviteAccepted;
  /** The details for events with this `type`. */
  "invite.deleted"?: AuditLogInviteAccepted;
  /** The details for events with this `type`. */
  "ip_allowlist.created"?: AuditLogIpAllowlistCreated;
  /** The details for events with this `type`. */
  "ip_allowlist.updated"?: AuditLogIpAllowlistUpdated;
  /** The details for events with this `type`. */
  "ip_allowlist.deleted"?: AuditLogIpAllowlistDeleted;
  /** The details for events with this `type`. */
  "ip_allowlist.config.activated"?: AuditLogIpAllowlistConfigActivated;
  /** The details for events with this `type`. */
  "ip_allowlist.config.deactivated"?: AuditLogIpAllowlistConfigDeactivated;
  /** This event has no additional fields beyond the standard audit log attributes. */
  "login.succeeded"?: Record<string, unknown>;
  /** The details for events with this `type`. */
  "login.failed"?: AuditLogLoginFailed;
  /** This event has no additional fields beyond the standard audit log attributes. */
  "logout.succeeded"?: Record<string, unknown>;
  /** The details for events with this `type`. */
  "logout.failed"?: AuditLogLoginFailed;
  /** The details for events with this `type`. */
  "organization.updated"?: AuditLogOrganizationUpdated;
  /** The details for events with this `type`. */
  "project.created"?: AuditLogProjectCreated;
  /** The details for events with this `type`. */
  "project.updated"?: AuditLogProjectUpdated;
  /** The details for events with this `type`. */
  "project.archived"?: AuditLogProjectArchived;
  /** The details for events with this `type`. */
  "project.deleted"?: AuditLogProjectArchived;
  /** The details for events with this `type`. */
  "rate_limit.updated"?: AuditLogRateLimitUpdated;
  /** The details for events with this `type`. */
  "rate_limit.deleted"?: AuditLogRateLimitDeleted;
  /** The details for events with this `type`. */
  "role.created"?: AuditLogRoleCreated;
  /** The details for events with this `type`. */
  "role.updated"?: AuditLogRoleUpdated;
  /** The details for events with this `type`. */
  "role.deleted"?: AuditLogRoleDeleted;
  /** The details for events with this `type`. */
  "role.assignment.created"?: AuditLogRoleAssignmentCreated;
  /** The details for events with this `type`. */
  "role.assignment.deleted"?: AuditLogRoleAssignmentDeleted;
  /** The details for events with this `type`. */
  "service_account.created"?: AuditLogServiceAccountCreated;
  /** The details for events with this `type`. */
  "service_account.updated"?: AuditLogServiceAccountUpdated;
  /** The details for events with this `type`. */
  "service_account.deleted"?: AuditLogServiceAccountDeleted;
  /** The details for events with this `type`. */
  "user.added"?: AuditLogUserAdded;
  /** The details for events with this `type`. */
  "user.updated"?: AuditLogUserUpdated;
  /** The details for events with this `type`. */
  "user.deleted"?: AuditLogUserDeleted;
  /** The details for events with this `type`. */
  "certificate.created"?: AuditLogCertificateCreated;
  /** The details for events with this `type`. */
  "certificate.updated"?: AuditLogCertificateCreated;
  /** The details for events with this `type`. */
  "certificate.deleted"?: AuditLogCertificateDeleted;
  /** The details for events with this `type`. */
  "certificates.activated"?: AuditLogCertificatesActivated;
  /** The details for events with this `type`. */
  "certificates.deactivated"?: AuditLogCertificatesActivated;
}

/** The project that the action was scoped to. Absent for actions not scoped to projects. Note that any admin actions taken via Admin API keys are associated with the default project. */
export interface AuditLogProject {
  /** The project ID. */
  id?: string;
  /** The project title. */
  name?: string;
}

/** The actor who performed the audit logged action. */
export interface AuditLogActor {
  /** The type of actor. Is either `session` or `api_key`. */
  type?: AuditLogActorType;
  session?: AuditLogActorSession;
  api_key?: AuditLogActorApiKey;
}

/** The type of actor. Is either `session` or `api_key`. */
export type AuditLogActorType = "session" | "api_key";

/** The session in which the audit logged action was performed. */
export interface AuditLogActorSession {
  user?: AuditLogActorUser;
  /** The IP address from which the action was performed. */
  ip_address?: string;
}

/** The user who performed the audit logged action. */
export interface AuditLogActorUser {
  /** The user id. */
  id?: string;
  /** The user email. */
  email?: string;
}

/** The API Key used to perform the audit logged action. */
export interface AuditLogActorApiKey {
  /** The tracking id of the API key. */
  id?: string;
  /** The type of API key. Can be either `user` or `service_account`. */
  type?: AuditLogActorApiKeyType;
  user?: AuditLogActorUser;
  service_account?: AuditLogActorServiceAccount;
}

/** The type of API key. Can be either `user` or `service_account`. */
export type AuditLogActorApiKeyType = "user" | "service_account";

/** The service account that performed the audit logged action. */
export interface AuditLogActorServiceAccount {
  /** The service account id. */
  id?: string;
}

/** The details for events with this `type`. */
export interface AuditLogApiKeyCreated {
  /** The tracking ID of the API key. */
  id?: string;
  /** The payload used to create the API key. */
  data?: AuditLogApiKeyCreatedData;
}

/** The payload used to create the API key. */
export interface AuditLogApiKeyCreatedData {
  /** A list of scopes allowed for the API key, e.g. `["api.model.request"]` */
  scopes?: string[];
}

/** The details for events with this `type`. */
export interface AuditLogApiKeyUpdated {
  /** The tracking ID of the API key. */
  id?: string;
  /** The payload used to update the API key. */
  changes_requested?: AuditLogApiKeyCreatedData;
}

/** The details for events with this `type`. */
export interface AuditLogApiKeyDeleted {
  /** The tracking ID of the API key. */
  id?: string;
}

/** The project and fine-tuned model checkpoint that the checkpoint permission was created for. */
export interface AuditLogCheckpointPermissionCreated {
  /** The ID of the checkpoint permission. */
  id?: string;
  /** The payload used to create the checkpoint permission. */
  data?: AuditLogCheckpointPermissionCreatedData;
}

/** The payload used to create the checkpoint permission. */
export interface AuditLogCheckpointPermissionCreatedData {
  /** The ID of the project that the checkpoint permission was created for. */
  project_id?: string;
  /** The ID of the fine-tuned model checkpoint. */
  fine_tuned_model_checkpoint?: string;
}

/** The details for events with this `type`. */
export interface AuditLogCheckpointPermissionDeleted {
  /** The ID of the checkpoint permission. */
  id?: string;
}

/** The details for events with this `type`. */
export interface AuditLogExternalKeyRegistered {
  /** The ID of the external key configuration. */
  id?: string;
  /** The configuration for the external key. */
  data?: Record<string, unknown>;
}

/** The details for events with this `type`. */
export interface AuditLogExternalKeyRemoved {
  /** The ID of the external key configuration. */
  id?: string;
}

/** The details for events with this `type`. */
export interface AuditLogGroupCreated {
  /** The ID of the group. */
  id?: string;
  /** Information about the created group. */
  data?: AuditLogGroupCreatedData;
}

/** Information about the created group. */
export interface AuditLogGroupCreatedData {
  /** The group name. */
  group_name?: string;
}

/** The details for events with this `type`. */
export interface AuditLogGroupUpdated {
  /** The ID of the group. */
  id?: string;
  /** The payload used to update the group. */
  changes_requested?: AuditLogGroupUpdatedChangesRequested;
}

/** The payload used to update the group. */
export interface AuditLogGroupUpdatedChangesRequested {
  /** The updated group name. */
  group_name?: string;
}

/** The details for events with this `type`. */
export interface AuditLogGroupDeleted {
  /** The ID of the group. */
  id?: string;
}

/** The details for events with this `type`. */
export interface AuditLogScimEnabled {
  /** The ID of the SCIM was enabled for. */
  id?: string;
}

/** The details for events with this `type`. */
export interface AuditLogScimDisabled {
  /** The ID of the SCIM was disabled for. */
  id?: string;
}

/** The details for events with this `type`. */
export interface AuditLogInviteSent {
  /** The ID of the invite. */
  id?: string;
  /** The payload used to create the invite. */
  data?: AuditLogInviteSentData;
}

/** The payload used to create the invite. */
export interface AuditLogInviteSentData {
  /** The email invited to the organization. */
  email?: string;
  /** The role the email was invited to be. Is either `owner` or `member`. */
  role?: string;
}

/** The details for events with this `type`. */
export interface AuditLogInviteAccepted {
  /** The ID of the invite. */
  id?: string;
}

/** The details for events with this `type`. */
export interface AuditLogIpAllowlistCreated {
  /** The ID of the IP allowlist configuration. */
  id?: string;
  /** The name of the IP allowlist configuration. */
  name?: string;
  /** The IP addresses or CIDR ranges included in the configuration. */
  allowed_ips?: string[];
}

/** The details for events with this `type`. */
export interface AuditLogIpAllowlistUpdated {
  /** The ID of the IP allowlist configuration. */
  id?: string;
  /** The updated set of IP addresses or CIDR ranges in the configuration. */
  allowed_ips?: string[];
}

/** The details for events with this `type`. */
export interface AuditLogIpAllowlistDeleted {
  /** The ID of the IP allowlist configuration. */
  id?: string;
  /** The name of the IP allowlist configuration. */
  name?: string;
  /** The IP addresses or CIDR ranges that were in the configuration. */
  allowed_ips?: string[];
}

/** The details for events with this `type`. */
export interface AuditLogIpAllowlistConfigActivated {
  /** The configurations that were activated. */
  configs?: AuditLogIpAllowlistConfigActivatedConfigsItem[];
}

export interface AuditLogIpAllowlistConfigActivatedConfigsItem {
  /** The ID of the IP allowlist configuration. */
  id?: string;
  /** The name of the IP allowlist configuration. */
  name?: string;
}

/** The details for events with this `type`. */
export interface AuditLogIpAllowlistConfigDeactivated {
  /** The configurations that were deactivated. */
  configs?: AuditLogIpAllowlistConfigActivatedConfigsItem[];
}

/** The details for events with this `type`. */
export interface AuditLogLoginFailed {
  /** The error code of the failure. */
  error_code?: string;
  /** The error message of the failure. */
  error_message?: string;
}

/** The details for events with this `type`. */
export interface AuditLogOrganizationUpdated {
  /** The organization ID. */
  id?: string;
  /** The payload used to update the organization settings. */
  changes_requested?: AuditLogOrganizationUpdatedChangesRequested;
}

/** The payload used to update the organization settings. */
export interface AuditLogOrganizationUpdatedChangesRequested {
  /** The organization title. */
  title?: string;
  /** The organization description. */
  description?: string;
  /** The organization name. */
  name?: string;
  /** Visibility of the threads page which shows messages created with the Assistants API and Playground. One of `ANY_ROLE`, `OWNERS`, or `NONE`. */
  threads_ui_visibility?: string;
  /** Visibility of the usage dashboard which shows activity and costs for your organization. One of `ANY_ROLE` or `OWNERS`. */
  usage_dashboard_visibility?: string;
  /** How your organization logs data from supported API calls. One of `disabled`, `enabled_per_call`, `enabled_for_all_projects`, or `enabled_for_selected_projects` */
  api_call_logging?: string;
  /** The list of project ids if api_call_logging is set to `enabled_for_selected_projects` */
  api_call_logging_project_ids?: string;
}

/** The details for events with this `type`. */
export interface AuditLogProjectCreated {
  /** The project ID. */
  id?: string;
  /** The payload used to create the project. */
  data?: AuditLogProjectCreatedData;
}

/** The payload used to create the project. */
export interface AuditLogProjectCreatedData {
  /** The project name. */
  name?: string;
  /** The title of the project as seen on the dashboard. */
  title?: string;
}

/** The details for events with this `type`. */
export interface AuditLogProjectUpdated {
  /** The project ID. */
  id?: string;
  /** The payload used to update the project. */
  changes_requested?: AuditLogProjectUpdatedChangesRequested;
}

/** The payload used to update the project. */
export interface AuditLogProjectUpdatedChangesRequested {
  /** The title of the project as seen on the dashboard. */
  title?: string;
}

/** The details for events with this `type`. */
export interface AuditLogProjectArchived {
  /** The project ID. */
  id?: string;
}

/** The details for events with this `type`. */
export interface AuditLogRateLimitUpdated {
  /** The rate limit ID */
  id?: string;
  /** The payload used to update the rate limits. */
  changes_requested?: AuditLogRateLimitUpdatedChangesRequested;
}

/** The payload used to update the rate limits. */
export interface AuditLogRateLimitUpdatedChangesRequested {
  /** The maximum requests per minute. */
  max_requests_per_1_minute?: number;
  /** The maximum tokens per minute. */
  max_tokens_per_1_minute?: number;
  /** The maximum images per minute. Only relevant for certain models. */
  max_images_per_1_minute?: number;
  /** The maximum audio megabytes per minute. Only relevant for certain models. */
  max_audio_megabytes_per_1_minute?: number;
  /** The maximum requests per day. Only relevant for certain models. */
  max_requests_per_1_day?: number;
  /** The maximum batch input tokens per day. Only relevant for certain models. */
  batch_1_day_max_input_tokens?: number;
}

/** The details for events with this `type`. */
export interface AuditLogRateLimitDeleted {
  /** The rate limit ID */
  id?: string;
}

/** The details for events with this `type`. */
export interface AuditLogRoleCreated {
  /** The role ID. */
  id?: string;
  /** The name of the role. */
  role_name?: string;
  /** The permissions granted by the role. */
  permissions?: string[];
  /** The type of resource the role belongs to. */
  resource_type?: string;
  /** The resource the role is scoped to. */
  resource_id?: string;
}

/** The details for events with this `type`. */
export interface AuditLogRoleUpdated {
  /** The role ID. */
  id?: string;
  /** The payload used to update the role. */
  changes_requested?: AuditLogRoleUpdatedChangesRequested;
}

/** The payload used to update the role. */
export interface AuditLogRoleUpdatedChangesRequested {
  /** The updated role name, when provided. */
  role_name?: string;
  /** The resource the role is scoped to. */
  resource_id?: string;
  /** The type of resource the role belongs to. */
  resource_type?: string;
  /** The permissions added to the role. */
  permissions_added?: string[];
  /** The permissions removed from the role. */
  permissions_removed?: string[];
  /** The updated role description, when provided. */
  description?: string;
  /** Additional metadata stored on the role. */
  metadata?: Record<string, unknown>;
}

/** The details for events with this `type`. */
export interface AuditLogRoleDeleted {
  /** The role ID. */
  id?: string;
}

/** The details for events with this `type`. */
export interface AuditLogRoleAssignmentCreated {
  /** The identifier of the role assignment. */
  id?: string;
  /** The principal (user or group) that received the role. */
  principal_id?: string;
  /** The type of principal (user or group) that received the role. */
  principal_type?: string;
  /** The resource the role assignment is scoped to. */
  resource_id?: string;
  /** The type of resource the role assignment is scoped to. */
  resource_type?: string;
}

/** The details for events with this `type`. */
export interface AuditLogRoleAssignmentDeleted {
  /** The identifier of the role assignment. */
  id?: string;
  /** The principal (user or group) that had the role removed. */
  principal_id?: string;
  /** The type of principal (user or group) that had the role removed. */
  principal_type?: string;
  /** The resource the role assignment was scoped to. */
  resource_id?: string;
  /** The type of resource the role assignment was scoped to. */
  resource_type?: string;
}

/** The details for events with this `type`. */
export interface AuditLogServiceAccountCreated {
  /** The service account ID. */
  id?: string;
  /** The payload used to create the service account. */
  data?: AuditLogServiceAccountCreatedData;
}

/** The payload used to create the service account. */
export interface AuditLogServiceAccountCreatedData {
  /** The role of the service account. Is either `owner` or `member`. */
  role?: string;
}

/** The details for events with this `type`. */
export interface AuditLogServiceAccountUpdated {
  /** The service account ID. */
  id?: string;
  /** The payload used to updated the service account. */
  changes_requested?: AuditLogServiceAccountCreatedData;
}

/** The details for events with this `type`. */
export interface AuditLogServiceAccountDeleted {
  /** The service account ID. */
  id?: string;
}

/** The details for events with this `type`. */
export interface AuditLogUserAdded {
  /** The user ID. */
  id?: string;
  /** The payload used to add the user to the project. */
  data?: AuditLogUserAddedData;
}

/** The payload used to add the user to the project. */
export interface AuditLogUserAddedData {
  /** The role of the user. Is either `owner` or `member`. */
  role?: string;
}

/** The details for events with this `type`. */
export interface AuditLogUserUpdated {
  /** The project ID. */
  id?: string;
  /** The payload used to update the user. */
  changes_requested?: AuditLogUserAddedData;
}

/** The details for events with this `type`. */
export interface AuditLogUserDeleted {
  /** The user ID. */
  id?: string;
}

/** The details for events with this `type`. */
export interface AuditLogCertificateCreated {
  /** The certificate ID. */
  id?: string;
  /** The name of the certificate. */
  name?: string;
}

/** The details for events with this `type`. */
export interface AuditLogCertificateDeleted {
  /** The certificate ID. */
  id?: string;
  /** The name of the certificate. */
  name?: string;
  /** The certificate content in PEM format. */
  certificate?: string;
}

/** The details for events with this `type`. */
export interface AuditLogCertificatesActivated {
  certificates?: AuditLogCertificateCreated[];
}

export interface ToggleCertificatesRequest {
  certificate_ids: string[];
}

export interface OrganizationCertificateActivationResponse {
  /** The organization certificate activation result type. */
  object: "organization.certificate.activation";
  data: OrganizationCertificate[];
}

/** Represents an individual certificate configured at the organization level. */
export interface OrganizationCertificate {
  /** The object type, which is always `organization.certificate`. */
  object: "organization.certificate";
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the certificate. */
  name: string | null;
  /** The Unix timestamp (in seconds) of when the certificate was uploaded. */
  created_at: number;
  certificate_details: OrganizationCertificateCertificateDetails;
  /** Whether the certificate is currently active at the organization level. */
  active: boolean;
}

export interface OrganizationCertificateCertificateDetails {
  /** The Unix timestamp (in seconds) of when the certificate becomes valid. */
  valid_at?: number;
  /** The Unix timestamp (in seconds) of when the certificate expires. */
  expires_at?: number;
}

export interface UploadCertificateRequest {
  /** An optional name for the certificate */
  name?: string;
  /** The certificate content in PEM format */
  certificate: string;
}

/** Represents an individual `certificate` uploaded to the organization. */
export interface Certificate {
  /**
   * The object type.
   *
   * - If creating, updating, or getting a specific certificate, the object type is `certificate`.
   * - If listing, activating, or deactivating certificates for the organization, the object type is `organization.certificate`.
   * - If listing, activating, or deactivating certificates for a project, the object type is `organization.project.certificate`.
   */
  object: CertificateObject;
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the certificate. */
  name: string | null;
  /** The Unix timestamp (in seconds) of when the certificate was uploaded. */
  created_at: number;
  certificate_details: CertificateCertificateDetails;
  /** Whether the certificate is currently active at the specified scope. Not returned when getting details for a specific certificate. */
  active?: boolean;
}

/**
 * The object type.
 *
 * - If creating, updating, or getting a specific certificate, the object type is `certificate`.
 * - If listing, activating, or deactivating certificates for the organization, the object type is `organization.certificate`.
 * - If listing, activating, or deactivating certificates for a project, the object type is `organization.project.certificate`.
 */
export type CertificateObject = "certificate" | "organization.certificate" | "organization.project.certificate";

export interface CertificateCertificateDetails {
  /** The Unix timestamp (in seconds) of when the certificate becomes valid. */
  valid_at?: number;
  /** The Unix timestamp (in seconds) of when the certificate expires. */
  expires_at?: number;
  /** The content of the certificate in PEM format. */
  content?: string;
}

export interface OrganizationCertificateDeactivationResponse {
  /** The organization certificate deactivation result type. */
  object: "organization.certificate.deactivation";
  data: OrganizationCertificate[];
}

export interface DeleteCertificateResponse {
  /** The object type, must be `certificate.deleted`. */
  object: "certificate.deleted";
  /** The ID of the certificate that was deleted. */
  id: string;
}

export type AdminOrganizationCertificatesListOrder = "asc" | "desc";

export interface ListCertificatesResponse {
  data: OrganizationCertificate[];
  first_id: string | null;
  last_id: string | null;
  has_more: boolean;
  object: "list";
}

export interface ModifyCertificateRequest {
  /** The updated name for the certificate */
  name?: string;
}

export type AdminOrganizationCostsListGroupByItem = "project_id" | "line_item" | "api_key_id";

export interface UsageResponse {
  object: "page";
  data: UsageTimeBucket[];
  has_more: boolean;
  next_page: string | null;
}

export interface UsageTimeBucket {
  object: "bucket";
  start_time: number;
  end_time: number;
  results: UsageTimeBucketResultsItem[];
}

export type UsageTimeBucketResultsItem = UsageCompletionsResult | UsageEmbeddingsResult | UsageModerationsResult | UsageImagesResult | UsageAudioSpeechesResult | UsageAudioTranscriptionsResult | UsageVectorStoresResult | UsageCodeInterpreterSessionsResult | CostsResult;

/** Decode a `UsageTimeBucketResultsItem` by its `object` discriminator (unknown value falls through). */
export function decodeUsageTimeBucketResultsItem(data: unknown): UsageTimeBucketResultsItem {
  switch ((data as { object?: string } | null | undefined)?.object) {
    case "organization.usage.completions.result":
      return data as UsageCompletionsResult;
    case "organization.usage.embeddings.result":
      return data as UsageEmbeddingsResult;
    case "organization.usage.moderations.result":
      return data as UsageModerationsResult;
    case "organization.usage.images.result":
      return data as UsageImagesResult;
    case "organization.usage.audio_speeches.result":
      return data as UsageAudioSpeechesResult;
    case "organization.usage.audio_transcriptions.result":
      return data as UsageAudioTranscriptionsResult;
    case "organization.usage.vector_stores.result":
      return data as UsageVectorStoresResult;
    case "organization.usage.code_interpreter_sessions.result":
      return data as UsageCodeInterpreterSessionsResult;
    case "organization.costs.result":
      return data as CostsResult;
    default:
      return data as UsageTimeBucketResultsItem;
  }
}

/** The aggregated completions usage details of the specific time bucket. */
export interface UsageCompletionsResult {
  object: "organization.usage.completions.result";
  /** The aggregated number of text input tokens used, including cached tokens. For customers subscribe to scale tier, this includes scale tier tokens. */
  input_tokens: number;
  /** The aggregated number of text input tokens that has been cached from previous requests. For customers subscribe to scale tier, this includes scale tier tokens. */
  input_cached_tokens?: number;
  /** The aggregated number of text output tokens used. For customers subscribe to scale tier, this includes scale tier tokens. */
  output_tokens: number;
  /** The aggregated number of audio input tokens used, including cached tokens. */
  input_audio_tokens?: number;
  /** The aggregated number of audio output tokens used. */
  output_audio_tokens?: number;
  /** The count of requests made to the model. */
  num_model_requests: number;
  project_id?: string | null;
  user_id?: string | null;
  api_key_id?: string | null;
  model?: string | null;
  batch?: boolean | null;
  service_tier?: string | null;
}

/** The aggregated embeddings usage details of the specific time bucket. */
export interface UsageEmbeddingsResult {
  object: "organization.usage.embeddings.result";
  /** The aggregated number of input tokens used. */
  input_tokens: number;
  /** The count of requests made to the model. */
  num_model_requests: number;
  project_id?: string | null;
  user_id?: string | null;
  api_key_id?: string | null;
  model?: string | null;
}

/** The aggregated moderations usage details of the specific time bucket. */
export interface UsageModerationsResult {
  object: "organization.usage.moderations.result";
  /** The aggregated number of input tokens used. */
  input_tokens: number;
  /** The count of requests made to the model. */
  num_model_requests: number;
  project_id?: string | null;
  user_id?: string | null;
  api_key_id?: string | null;
  model?: string | null;
}

/** The aggregated images usage details of the specific time bucket. */
export interface UsageImagesResult {
  object: "organization.usage.images.result";
  /** The number of images processed. */
  images: number;
  /** The count of requests made to the model. */
  num_model_requests: number;
  source?: string | null;
  size?: string | null;
  project_id?: string | null;
  user_id?: string | null;
  api_key_id?: string | null;
  model?: string | null;
}

/** The aggregated audio speeches usage details of the specific time bucket. */
export interface UsageAudioSpeechesResult {
  object: "organization.usage.audio_speeches.result";
  /** The number of characters processed. */
  characters: number;
  /** The count of requests made to the model. */
  num_model_requests: number;
  project_id?: string | null;
  user_id?: string | null;
  api_key_id?: string | null;
  model?: string | null;
}

/** The aggregated audio transcriptions usage details of the specific time bucket. */
export interface UsageAudioTranscriptionsResult {
  object: "organization.usage.audio_transcriptions.result";
  /** The number of seconds processed. */
  seconds: number;
  /** The count of requests made to the model. */
  num_model_requests: number;
  project_id?: string | null;
  user_id?: string | null;
  api_key_id?: string | null;
  model?: string | null;
}

/** The aggregated vector stores usage details of the specific time bucket. */
export interface UsageVectorStoresResult {
  object: "organization.usage.vector_stores.result";
  /** The vector stores usage in bytes. */
  usage_bytes: number;
  project_id?: string | null;
}

/** The aggregated code interpreter sessions usage details of the specific time bucket. */
export interface UsageCodeInterpreterSessionsResult {
  object: "organization.usage.code_interpreter_sessions.result";
  /** The number of code interpreter sessions. */
  num_sessions: number;
  project_id?: string | null;
}

/** The aggregated costs details of the specific time bucket. */
export interface CostsResult {
  object: "organization.costs.result";
  /** The monetary value in its associated currency. */
  amount?: CostsResultAmount;
  line_item?: string | null;
  project_id?: string | null;
  api_key_id?: string | null;
  quantity?: number | null;
}

/** The monetary value in its associated currency. */
export interface CostsResultAmount {
  /** The numeric value of the cost. */
  value?: number;
  /** Lowercase ISO-4217 currency e.g. "usd" */
  currency?: string;
}

/** Request payload for creating a new group in the organization. */
export interface CreateGroupBody {
  /** Human readable name for the group. */
  name: string;
}

/** Details about an organization group. */
export interface GroupResponse {
  /** Identifier for the group. */
  id: string;
  /** Display name of the group. */
  name: string;
  /** Unix timestamp (in seconds) when the group was created. */
  created_at: number;
  /** Whether the group is managed through SCIM and controlled by your identity provider. */
  is_scim_managed: boolean;
  /** The type of the group. */
  group_type: string;
}

/** Confirmation payload returned after deleting a group. */
export interface GroupDeletedResource {
  /** Always `group.deleted`. */
  object: "group.deleted";
  /** Identifier of the deleted group. */
  id: string;
  /** Whether the group was deleted. */
  deleted: boolean;
}

export type AdminOrganizationGroupsListOrder = "asc" | "desc";

/** Paginated list of organization groups. */
export interface GroupListResource {
  /** Always `list`. */
  object: "list";
  /** Groups returned in the current page. */
  data: GroupResponse[];
  /** Whether additional groups are available when paginating. */
  has_more: boolean;
  /** Cursor to fetch the next page of results, or `null` if there are no more results. */
  next: string | null;
}

/** Request payload for assigning a role to a group or user. */
export interface PublicAssignOrganizationGroupRoleBody {
  /** Identifier of the role to assign. */
  role_id: string;
}

/** Role assignment linking a group to a role. */
export interface GroupRoleAssignment {
  /** Always `group.role`. */
  object: "group.role";
  group: Group;
  role: Role;
}

/** Summary information about a group returned in role assignment responses. */
export interface Group {
  /** Always `group`. */
  object: "group";
  /** Identifier for the group. */
  id: string;
  /** Display name of the group. */
  name: string;
  /** Unix timestamp (in seconds) when the group was created. */
  created_at: number;
  /** Whether the group is managed through SCIM. */
  scim_managed: boolean;
}

/** Details about a role that can be assigned through the public Roles API. */
export interface Role {
  /** Always `role`. */
  object: "role";
  /** Identifier for the role. */
  id: string;
  /** Unique name for the role. */
  name: string;
  /** Optional description of the role. */
  description: string | null;
  /** Permissions granted by the role. */
  permissions: string[];
  /** Resource type the role is bound to (for example `api.organization` or `api.project`). */
  resource_type: string;
  /** Whether the role is predefined and managed by OpenAI. */
  predefined_role: boolean;
}

/** Confirmation payload returned after unassigning a role. */
export interface DeletedRoleAssignmentResource {
  /** Identifier for the deleted assignment, such as `group.role.deleted` or `user.role.deleted`. */
  object: string;
  /** Whether the assignment was removed. */
  deleted: boolean;
}

export type AdminOrganizationGroupsRolesListOrder = "asc" | "desc";

/** Paginated list of roles assigned to a principal. */
export interface RoleListResource {
  /** Always `list`. */
  object: "list";
  /** Role assignments returned in the current page. */
  data: AssignedRoleDetails[];
  /** Whether additional assignments are available when paginating. */
  has_more: boolean;
  /** Cursor to fetch the next page of results, or `null` when there are no more assignments. */
  next: string | null;
}

/** Detailed information about a role assignment entry returned when listing assignments. */
export interface AssignedRoleDetails {
  /** Identifier for the role. */
  id: string;
  /** Name of the role. */
  name: string;
  /** Permissions associated with the role. */
  permissions: string[];
  /** Resource type the role applies to. */
  resource_type: string;
  /** Whether the role is predefined by OpenAI. */
  predefined_role: boolean;
  /** Description of the role. */
  description: string | null;
  /** When the role was created. */
  created_at: number | null;
  /** When the role was last updated. */
  updated_at: number | null;
  /** Identifier of the actor who created the role. */
  created_by: string | null;
  /** User details for the actor that created the role, when available. */
  created_by_user_obj: Record<string, unknown> | null;
  /** Arbitrary metadata stored on the role. */
  metadata: Record<string, unknown> | null;
}

/** Request payload for updating the details of an existing group. */
export interface UpdateGroupBody {
  /** New display name for the group. */
  name: string;
}

/** Response returned after updating a group. */
export interface GroupResourceWithSuccess {
  /** Identifier for the group. */
  id: string;
  /** Updated display name for the group. */
  name: string;
  /** Unix timestamp (in seconds) when the group was created. */
  created_at: number;
  /** Whether the group is managed through SCIM and controlled by your identity provider. */
  is_scim_managed: boolean;
}

/** Request payload for adding a user to a group. */
export interface CreateGroupUserBody {
  /** Identifier of the user to add to the group. */
  user_id: string;
}

/** Confirmation payload returned after adding a user to a group. */
export interface GroupUserAssignment {
  /** Always `group.user`. */
  object: "group.user";
  /** Identifier of the user that was added. */
  user_id: string;
  /** Identifier of the group the user was added to. */
  group_id: string;
}

/** Confirmation payload returned after removing a user from a group. */
export interface GroupUserDeletedResource {
  /** Always `group.user.deleted`. */
  object: "group.user.deleted";
  /** Whether the group membership was removed. */
  deleted: boolean;
}

export type AdminOrganizationGroupsUsersListOrder = "asc" | "desc";

/** Paginated list of user objects returned when inspecting group membership. */
export interface UserListResource {
  /** Always `list`. */
  object: "list";
  /** Users in the current page. */
  data: GroupUser[];
  /** Whether more users are available when paginating. */
  has_more: boolean;
  /** Cursor to fetch the next page of results, or `null` when no further users are available. */
  next: string | null;
}

/** Represents an individual user returned when inspecting group membership. */
export interface GroupUser {
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the user. */
  name: string;
  /** The email address of the user. */
  email: string | null;
}

export interface InviteRequest {
  /** Send an email to this address */
  email: string;
  /** `owner` or `reader` */
  role: InviteRequestRole;
  /** An array of projects to which membership is granted at the same time the org invite is accepted. If omitted, the user will be invited to the default project for compatibility with legacy behavior. */
  projects?: InviteRequestProjectsItem[];
}

/** `owner` or `reader` */
export type InviteRequestRole = "reader" | "owner";

export interface InviteRequestProjectsItem {
  /** Project's public ID */
  id: string;
  /** Project membership role */
  role: InviteRequestProjectsItemRole;
}

/** Project membership role */
export type InviteRequestProjectsItemRole = "member" | "owner";

/** Represents an individual `invite` to the organization. */
export interface Invite {
  /** The object type, which is always `organization.invite` */
  object: "organization.invite";
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The email address of the individual to whom the invite was sent */
  email: string;
  /** `owner` or `reader` */
  role: InviteRole;
  /** `accepted`,`expired`, or `pending` */
  status: InviteStatus;
  /** The Unix timestamp (in seconds) of when the invite was sent. */
  created_at: number;
  /** The Unix timestamp (in seconds) of when the invite expires. */
  expires_at?: number | null;
  /** The Unix timestamp (in seconds) of when the invite was accepted. */
  accepted_at?: number | null;
  /** The projects that were granted membership upon acceptance of the invite. */
  projects: InviteRequestProjectsItem[];
}

/** `owner` or `reader` */
export type InviteRole = "owner" | "reader";

/** `accepted`,`expired`, or `pending` */
export type InviteStatus = "accepted" | "expired" | "pending";

export interface InviteDeleteResponse {
  /** The object type, which is always `organization.invite.deleted` */
  object: "organization.invite.deleted";
  id: string;
  deleted: boolean;
}

export interface InviteListResponse {
  /** The object type, which is always `list` */
  object: "list";
  data: Invite[];
  /** The first `invite_id` in the retrieved `list` */
  first_id?: string | null;
  /** The last `invite_id` in the retrieved `list` */
  last_id?: string | null;
  /** The `has_more` property is used for pagination to indicate there are additional results. */
  has_more: boolean;
}

export interface InviteProjectsItem {
  /** Project's public ID */
  id: string;
  /** Project membership role */
  role: InviteProjectsItemRole;
}

/** Project membership role */
export type InviteProjectsItemRole = "member" | "owner";

export interface ProjectApiKeyDeleteResponse {
  object: "organization.project.api_key.deleted";
  id: string;
  deleted: boolean;
}

export interface ErrorResponse {
  error: Error;
}

export interface Error {
  code: string | null;
  message: string;
  param: string | null;
  type: string;
}

export interface ProjectApiKeyListResponse {
  object: "list";
  data: ProjectApiKey[];
  first_id?: string | null;
  last_id?: string | null;
  has_more: boolean;
}

/** Represents an individual API key in a project. */
export interface ProjectApiKey {
  /** The object type, which is always `organization.project.api_key` */
  object: "organization.project.api_key";
  /** The redacted value of the API key */
  redacted_value: string;
  /** The name of the API key */
  name: string;
  /** The Unix timestamp (in seconds) of when the API key was created */
  created_at: number;
  /** The Unix timestamp (in seconds) of when the API key was last used. */
  last_used_at: number | null;
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  owner: ProjectApiKeyOwner;
}

export interface ProjectApiKeyOwner {
  /** `user` or `service_account` */
  type?: ProjectApiKeyOwnerType;
  user?: ProjectApiKeyOwnerUser;
  service_account?: ProjectApiKeyOwnerServiceAccount;
}

/** `user` or `service_account` */
export type ProjectApiKeyOwnerType = "user" | "service_account";

/** The user that owns a project API key. */
export interface ProjectApiKeyOwnerUser {
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The email address of the user. */
  email: string;
  /** The name of the user. */
  name: string;
  /** The Unix timestamp (in seconds) of when the user was created. */
  created_at: number;
  /** The user's project role. */
  role: string;
}

/** The service account that owns a project API key. */
export interface ProjectApiKeyOwnerServiceAccount {
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the service account. */
  name: string;
  /** The Unix timestamp (in seconds) of when the service account was created. */
  created_at: number;
  /** The service account's project role. */
  role: string;
}

/** Represents an individual project. */
export interface Project {
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The object type, which is always `organization.project` */
  object: "organization.project";
  /** The name of the project. This appears in reporting. */
  name?: string | null;
  /** The Unix timestamp (in seconds) of when the project was created. */
  created_at: number;
  archived_at?: number | null;
  /** `active` or `archived` */
  status?: string | null;
  /** The external key associated with the project. */
  external_key_id?: string | null;
}

export interface OrganizationProjectCertificateActivationResponse {
  /** The project certificate activation result type. */
  object: "organization.project.certificate.activation";
  data: OrganizationProjectCertificate[];
}

/** Represents an individual certificate configured at the project level. */
export interface OrganizationProjectCertificate {
  /** The object type, which is always `organization.project.certificate`. */
  object: "organization.project.certificate";
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the certificate. */
  name: string | null;
  /** The Unix timestamp (in seconds) of when the certificate was uploaded. */
  created_at: number;
  certificate_details: OrganizationProjectCertificateCertificateDetails;
  /** Whether the certificate is currently active at the project level. */
  active: boolean;
}

export interface OrganizationProjectCertificateCertificateDetails {
  /** The Unix timestamp (in seconds) of when the certificate becomes valid. */
  valid_at?: number;
  /** The Unix timestamp (in seconds) of when the certificate expires. */
  expires_at?: number;
}

export interface OrganizationProjectCertificateDeactivationResponse {
  /** The project certificate deactivation result type. */
  object: "organization.project.certificate.deactivation";
  data: OrganizationProjectCertificate[];
}

export type AdminOrganizationProjectsCertificatesListOrder = "asc" | "desc";

export interface ListProjectCertificatesResponse {
  data: OrganizationProjectCertificate[];
  first_id: string | null;
  last_id: string | null;
  has_more: boolean;
  object: "list";
}

export interface ProjectCreateRequest {
  /** The friendly name of the project, this name appears in reports. */
  name: string;
  /** Create the project with the specified data residency region. Your organization must have access to Data residency functionality in order to use. See [data residency controls](/docs/guides/your-data#data-residency-controls) to review the functionality and limitations of setting this field. */
  geography?: string | null;
  /** External key ID to associate with the project. */
  external_key_id?: string | null;
}

/** Request payload for granting a group access to a project. */
export interface InviteProjectGroupBody {
  /** Identifier of the group to add to the project. */
  group_id: string;
  /** Identifier of the project role to grant to the group. */
  role: string;
}

/** Details about a group's membership in a project. */
export interface ProjectGroup {
  /** Always `project.group`. */
  object: "project.group";
  /** Identifier of the project. */
  project_id: string;
  /** Identifier of the group that has access to the project. */
  group_id: string;
  /** Display name of the group. */
  group_name: string;
  /** The type of the group. */
  group_type: string;
  /** Unix timestamp (in seconds) when the group was granted project access. */
  created_at: number;
}

/** Confirmation payload returned after removing a group from a project. */
export interface ProjectGroupDeletedResource {
  /** Always `project.group.deleted`. */
  object: "project.group.deleted";
  /** Whether the group membership in the project was removed. */
  deleted: boolean;
}

export type AdminOrganizationProjectsGroupsListOrder = "asc" | "desc";

/** Paginated list of groups that have access to a project. */
export interface ProjectGroupListResource {
  /** Always `list`. */
  object: "list";
  /** Project group memberships returned in the current page. */
  data: ProjectGroup[];
  /** Whether additional project group memberships are available. */
  has_more: boolean;
  /** Cursor to fetch the next page of results, or `null` when there are no more results. */
  next: string | null;
}

export interface ProjectListResponse {
  object: "list";
  data: Project[];
  first_id?: string | null;
  last_id?: string | null;
  has_more: boolean;
}

export interface ProjectRateLimitListResponse {
  object: "list";
  data: ProjectRateLimit[];
  first_id?: string | null;
  last_id?: string | null;
  has_more: boolean;
}

/** Represents a project rate limit config. */
export interface ProjectRateLimit {
  /** The object type, which is always `project.rate_limit` */
  object: "project.rate_limit";
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The model this rate limit applies to. */
  model: string;
  /** The maximum requests per minute. */
  max_requests_per_1_minute: number;
  /** The maximum tokens per minute. */
  max_tokens_per_1_minute: number;
  /** The maximum images per minute. Only present for relevant models. */
  max_images_per_1_minute?: number;
  /** The maximum audio megabytes per minute. Only present for relevant models. */
  max_audio_megabytes_per_1_minute?: number;
  /** The maximum requests per day. Only present for relevant models. */
  max_requests_per_1_day?: number;
  /** The maximum batch input tokens per day. Only present for relevant models. */
  batch_1_day_max_input_tokens?: number;
}

export interface ProjectRateLimitUpdateRequest {
  /** The maximum requests per minute. */
  max_requests_per_1_minute?: number;
  /** The maximum tokens per minute. */
  max_tokens_per_1_minute?: number;
  /** The maximum images per minute. Only relevant for certain models. */
  max_images_per_1_minute?: number;
  /** The maximum audio megabytes per minute. Only relevant for certain models. */
  max_audio_megabytes_per_1_minute?: number;
  /** The maximum requests per day. Only relevant for certain models. */
  max_requests_per_1_day?: number;
  /** The maximum batch input tokens per day. Only relevant for certain models. */
  batch_1_day_max_input_tokens?: number;
}

export interface ProjectServiceAccountCreateRequest {
  /** The name of the service account being created. */
  name: string;
}

export interface ProjectServiceAccountCreateResponse {
  object: "organization.project.service_account";
  id: string;
  name: string;
  /** Service accounts can only have one role of type `member` */
  role: "member";
  created_at: number;
  api_key: ProjectServiceAccountApiKey | null;
}

export interface ProjectServiceAccountApiKey {
  /** The object type, which is always `organization.project.service_account.api_key` */
  object: "organization.project.service_account.api_key";
  value: string;
  name: string;
  created_at: number;
  id: string;
}

export interface ProjectServiceAccountDeleteResponse {
  object: "organization.project.service_account.deleted";
  id: string;
  deleted: boolean;
}

export interface ProjectServiceAccountListResponse {
  object: "list";
  data: ProjectServiceAccount[];
  first_id?: string | null;
  last_id?: string | null;
  has_more: boolean;
}

/** Represents an individual service account in a project. */
export interface ProjectServiceAccount {
  /** The object type, which is always `organization.project.service_account` */
  object: "organization.project.service_account";
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the service account */
  name: string;
  /** `owner` or `member` */
  role: ProjectServiceAccountRole;
  /** The Unix timestamp (in seconds) of when the service account was created */
  created_at: number;
}

/** `owner` or `member` */
export type ProjectServiceAccountRole = "owner" | "member";

export interface ProjectUpdateRequest {
  /** The updated name of the project, this name appears in reports. */
  name?: string | null;
  /** External key ID to associate with the project. */
  external_key_id?: string | null;
  /** Geography for the project. */
  geography?: string | null;
}

export interface ProjectUserCreateRequest {
  /** The ID of the user. */
  user_id?: string | null;
  /** Email of the user to add. */
  email?: string | null;
  /** `owner` or `member` */
  role: string;
}

/** Represents an individual user in a project. */
export interface ProjectUser {
  /** The object type, which is always `organization.project.user` */
  object: "organization.project.user";
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the user */
  name?: string | null;
  /** The email address of the user */
  email?: string | null;
  /** `owner` or `member` */
  role: string;
  /** The Unix timestamp (in seconds) of when the project was added. */
  added_at: number;
}

export interface ProjectUserDeleteResponse {
  object: "organization.project.user.deleted";
  id: string;
  deleted: boolean;
}

export interface ProjectUserListResponse {
  object: string;
  data: ProjectUser[];
  first_id?: string | null;
  last_id?: string | null;
  has_more: boolean;
}

export interface ProjectUserUpdateRequest {
  /** `owner` or `member` */
  role?: string | null;
}

/** Request payload for creating a custom role. */
export interface PublicCreateOrganizationRoleBody {
  /** Unique name for the role. */
  role_name: string;
  /** Permissions to grant to the role. */
  permissions: string[];
  /** Optional description of the role. */
  description?: string | null;
}

/** Confirmation payload returned after deleting a role. */
export interface RoleDeletedResource {
  /** Always `role.deleted`. */
  object: "role.deleted";
  /** Identifier of the deleted role. */
  id: string;
  /** Whether the role was deleted. */
  deleted: boolean;
}

export type AdminOrganizationRolesListOrder = "asc" | "desc";

/** Paginated list of roles available on an organization or project. */
export interface PublicRoleListResource {
  /** Always `list`. */
  object: "list";
  /** Roles returned in the current page. */
  data: Role[];
  /** Whether more roles are available when paginating. */
  has_more: boolean;
  /** Cursor to fetch the next page of results, or `null` when there are no additional roles. */
  next: string | null;
}

/** Request payload for updating an existing role. */
export interface PublicUpdateOrganizationRoleBody {
  /** Updated set of permissions for the role. */
  permissions?: string[] | null;
  /** New description for the role. */
  description?: string | null;
  /** New name for the role. */
  role_name?: string | null;
}

export type AdminOrganizationUsageAudioSpeechesListBucketWidth = "1m" | "1h" | "1d";

export type AdminOrganizationUsageAudioSpeechesListGroupByItem = "project_id" | "user_id" | "api_key_id" | "model";

export type AdminOrganizationUsageAudioTranscriptionsListBucketWidth = "1m" | "1h" | "1d";

export type AdminOrganizationUsageAudioTranscriptionsListGroupByItem = "project_id" | "user_id" | "api_key_id" | "model";

export type AdminOrganizationUsageCodeInterpreterSessionsListBucketWidth = "1m" | "1h" | "1d";

export type AdminOrganizationUsageCompletionsListBucketWidth = "1m" | "1h" | "1d";

export type AdminOrganizationUsageCompletionsListGroupByItem = "project_id" | "user_id" | "api_key_id" | "model" | "batch" | "service_tier";

export type AdminOrganizationUsageEmbeddingsListBucketWidth = "1m" | "1h" | "1d";

export type AdminOrganizationUsageEmbeddingsListGroupByItem = "project_id" | "user_id" | "api_key_id" | "model";

export type AdminOrganizationUsageImagesListBucketWidth = "1m" | "1h" | "1d";

export type AdminOrganizationUsageImagesListSourcesItem = "image.generation" | "image.edit" | "image.variation";

export type AdminOrganizationUsageImagesListSizesItem = "256x256" | "512x512" | "1024x1024" | "1792x1792" | "1024x1792";

export type AdminOrganizationUsageImagesListGroupByItem = "project_id" | "user_id" | "api_key_id" | "model" | "size" | "source";

export type AdminOrganizationUsageModerationsListBucketWidth = "1m" | "1h" | "1d";

export type AdminOrganizationUsageModerationsListGroupByItem = "project_id" | "user_id" | "api_key_id" | "model";

export type AdminOrganizationUsageVectorStoresListBucketWidth = "1m" | "1h" | "1d";

export interface UserDeleteResponse {
  object: "organization.user.deleted";
  id: string;
  deleted: boolean;
}

export interface UserListResponse {
  object: "list";
  data: User[];
  first_id?: string | null;
  last_id?: string | null;
  has_more: boolean;
}

/** Represents an individual `user` within an organization. */
export interface User {
  /** The object type, which is always `organization.user` */
  object: "organization.user";
  /** The identifier, which can be referenced in API endpoints */
  id: string;
  /** The name of the user */
  name?: string | null;
  /** The email address of the user */
  email?: string | null;
  /** `owner` or `reader` */
  role?: string | null;
  /** The Unix timestamp (in seconds) of when the user was added. */
  added_at: number;
  /** Whether this is the organization's default user. */
  is_default?: boolean;
  /** The Unix timestamp (in seconds) of when the user was created. */
  created?: number;
  /** Nested user details. */
  user?: UserUser;
  /** Whether the user is a service account. */
  is_service_account?: boolean;
  /** Whether the user is an authorized purchaser for Scale Tier. */
  is_scale_tier_authorized_purchaser?: boolean | null;
  /** Whether the user is managed through SCIM. */
  is_scim_managed?: boolean;
  /** The Unix timestamp (in seconds) of the user's last API key usage. */
  api_key_last_used_at?: number | null;
  /** The technical level metadata for the user. */
  technical_level?: string | null;
  /** The developer persona metadata for the user. */
  developer_persona?: string | null;
  /** Projects associated with the user, if included. */
  projects?: UserProjects | null;
}

/** Nested user details. */
export interface UserUser {
  object: "user";
  id: string;
  email?: string | null;
  name?: string | null;
  picture?: string | null;
  enabled?: boolean | null;
  banned?: boolean | null;
  banned_at?: number | null;
}

export interface UserProjects {
  object: "list";
  data: UserProjectsDataItem[];
}

export interface UserProjectsDataItem {
  id?: string | null;
  name?: string | null;
  role?: string | null;
}

/** Role assignment linking a user to a role. */
export interface UserRoleAssignment {
  /** Always `user.role`. */
  object: "user.role";
  user: User;
  role: Role;
}

export type AdminOrganizationUsersRolesListOrder = "asc" | "desc";

export interface UserRoleUpdateRequest {
  /** `owner` or `reader` */
  role?: string | null;
  /** Role ID to assign to the user. */
  role_id?: string | null;
  /** Technical level metadata. */
  technical_level?: string | null;
  /** Developer persona metadata. */
  developer_persona?: string | null;
}

export interface CreateSpeechRequest {
  /** One of the available [TTS models](/docs/models#tts): `tts-1`, `tts-1-hd`, `gpt-4o-mini-tts`, or `gpt-4o-mini-tts-2025-12-15`. */
  model: CreateSpeechRequestModel;
  /** The text to generate audio for. The maximum length is 4096 characters. */
  input: string;
  /** Control the voice of your generated audio with additional instructions. Does not work with `tts-1` or `tts-1-hd`. */
  instructions?: string;
  voice: VoiceIdsOrCustomVoice;
  /** The format to audio in. Supported formats are `mp3`, `opus`, `aac`, `flac`, `wav`, and `pcm`. */
  response_format?: CreateSpeechRequestResponseFormat;
  /** The speed of the generated audio. Select a value from `0.25` to `4.0`. `1.0` is the default. */
  speed?: number;
  /** The format to stream the audio in. Supported formats are `sse` and `audio`. `sse` is not supported for `tts-1` or `tts-1-hd`. */
  stream_format?: CreateSpeechRequestStreamFormat;
}

/** One of the available [TTS models](/docs/models#tts): `tts-1`, `tts-1-hd`, `gpt-4o-mini-tts`, or `gpt-4o-mini-tts-2025-12-15`. */
export type CreateSpeechRequestModel = string | CreateSpeechRequestModelString;

export type CreateSpeechRequestModelString = "tts-1" | "tts-1-hd" | "gpt-4o-mini-tts" | "gpt-4o-mini-tts-2025-12-15";

/** A built-in voice name or a custom voice reference. */
export type VoiceIdsOrCustomVoice = VoiceIdsShared | VoiceIdsOrCustomVoiceVariant1;

export type VoiceIdsShared = string | VoiceIdsSharedString;

export type VoiceIdsSharedString = "alloy" | "ash" | "ballad" | "coral" | "echo" | "sage" | "shimmer" | "verse" | "marin" | "cedar";

/** Custom voice reference. */
export interface VoiceIdsOrCustomVoiceVariant1 {
  /** The custom voice ID, e.g. `voice_1234`. */
  id: string;
}

/** The format to audio in. Supported formats are `mp3`, `opus`, `aac`, `flac`, `wav`, and `pcm`. */
export type CreateSpeechRequestResponseFormat = "mp3" | "opus" | "aac" | "flac" | "wav" | "pcm";

/** The format to stream the audio in. Supported formats are `sse` and `audio`. `sse` is not supported for `tts-1` or `tts-1-hd`. */
export type CreateSpeechRequestStreamFormat = "sse" | "audio";

export interface CreateTranscriptionRequest {
  /** The audio file object (not file name) to transcribe, in one of these formats: flac, mp3, mp4, mpeg, mpga, m4a, ogg, wav, or webm. */
  file: Uint8Array | Blob;
  /** ID of the model to use. The options are `gpt-4o-transcribe`, `gpt-4o-mini-transcribe`, `gpt-4o-mini-transcribe-2025-12-15`, `whisper-1` (which is powered by our open source Whisper V2 model), and `gpt-4o-transcribe-diarize`. */
  model: CreateTranscriptionRequestModel;
  /** The language of the input audio. Supplying the input language in [ISO-639-1](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) (e.g. `en`) format will improve accuracy and latency. */
  language?: string;
  /** An optional text to guide the model's style or continue a previous audio segment. The [prompt](/docs/guides/speech-to-text#prompting) should match the audio language. This field is not supported when using `gpt-4o-transcribe-diarize`. */
  prompt?: string;
  response_format?: AudioResponseFormat;
  /** The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. If set to 0, the model will use [log probability](https://en.wikipedia.org/wiki/Log_probability) to automatically increase the temperature until certain thresholds are hit. */
  temperature?: number;
  /**
   * Additional information to include in the transcription response.
   * `logprobs` will return the log probabilities of the tokens in the
   * response to understand the model's confidence in the transcription.
   * `logprobs` only works with response_format set to `json` and only with
   * the models `gpt-4o-transcribe`, `gpt-4o-mini-transcribe`, and `gpt-4o-mini-transcribe-2025-12-15`. This field is not supported when using `gpt-4o-transcribe-diarize`.
   */
  include?: TranscriptionInclude[];
  /**
   * The timestamp granularities to populate for this transcription. `response_format` must be set `verbose_json` to use timestamp granularities. Either or both of these options are supported: `word`, or `segment`. Note: There is no additional latency for segment timestamps, but generating word timestamps incurs additional latency.
   * This option is not available for `gpt-4o-transcribe-diarize`.
   */
  timestamp_granularities?: CreateTranscriptionRequestTimestampGranularitiesItem[];
  stream?: boolean | null;
  chunking_strategy?: CreateTranscriptionRequestChunkingStrategy | null;
  /** Optional list of speaker names that correspond to the audio samples provided in `known_speaker_references[]`. Each entry should be a short identifier (for example `customer` or `agent`). Up to 4 speakers are supported. */
  known_speaker_names?: string[];
  /** Optional list of audio samples (as [data URLs](https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/Data_URLs)) that contain known speaker references matching `known_speaker_names[]`. Each sample must be between 2 and 10 seconds, and can use any of the same input audio formats supported by `file`. */
  known_speaker_references?: string[];
}

/** ID of the model to use. The options are `gpt-4o-transcribe`, `gpt-4o-mini-transcribe`, `gpt-4o-mini-transcribe-2025-12-15`, `whisper-1` (which is powered by our open source Whisper V2 model), and `gpt-4o-transcribe-diarize`. */
export type CreateTranscriptionRequestModel = string | CreateTranscriptionRequestModelString;

export type CreateTranscriptionRequestModelString = "whisper-1" | "gpt-4o-transcribe" | "gpt-4o-mini-transcribe" | "gpt-4o-mini-transcribe-2025-12-15" | "gpt-4o-transcribe-diarize";

/** The format of the output, in one of these options: `json`, `text`, `srt`, `verbose_json`, `vtt`, or `diarized_json`. For `gpt-4o-transcribe` and `gpt-4o-mini-transcribe`, the only supported format is `json`. For `gpt-4o-transcribe-diarize`, the supported formats are `json`, `text`, and `diarized_json`, with `diarized_json` required to receive speaker annotations. */
export type AudioResponseFormat = "json" | "text" | "srt" | "verbose_json" | "vtt" | "diarized_json";

export type TranscriptionInclude = "logprobs";

export type CreateTranscriptionRequestTimestampGranularitiesItem = "word" | "segment";

/** Controls how the audio is cut into chunks. When set to `"auto"`, the server first normalizes loudness and then uses voice activity detection (VAD) to choose boundaries. `server_vad` object can be provided to tweak VAD detection parameters manually. If unset, the audio is transcribed as a single block. Required when using `gpt-4o-transcribe-diarize` for inputs longer than 30 seconds. */
export type CreateTranscriptionRequestChunkingStrategy = "auto" | VadConfig;

export interface VadConfig {
  /** Must be set to `server_vad` to enable manual chunking using server side VAD. */
  type: "server_vad";
  /**
   * Amount of audio to include before the VAD detected speech (in
   * milliseconds).
   */
  prefix_padding_ms?: number;
  /**
   * Duration of silence to detect speech stop (in milliseconds).
   * With shorter values the model will respond more quickly,
   * but may jump in on short pauses from the user.
   */
  silence_duration_ms?: number;
  /**
   * Sensitivity threshold (0.0 to 1.0) for voice activity detection. A
   * higher threshold will require louder audio to activate the model, and
   * thus might perform better in noisy environments.
   */
  threshold?: number;
}

export type AudioTranscriptionsCreateResponse = CreateTranscriptionResponseJson | CreateTranscriptionResponseDiarizedJson | CreateTranscriptionResponseVerboseJson;

/** Represents a transcription response returned by model, based on the provided input. */
export interface CreateTranscriptionResponseJson {
  /** The transcribed text. */
  text: string;
  /** The log probabilities of the tokens in the transcription. Only returned with the models `gpt-4o-transcribe` and `gpt-4o-mini-transcribe` if `logprobs` is added to the `include` array. */
  logprobs?: CreateTranscriptionResponseJsonLogprobsItem[];
  /** Token usage statistics for the request. */
  usage?: CreateTranscriptionResponseJsonUsage;
}

export interface CreateTranscriptionResponseJsonLogprobsItem {
  /** The token in the transcription. */
  token?: string;
  /** The log probability of the token. */
  logprob?: number;
  /** The bytes of the token. */
  bytes?: number[];
}

/** Token usage statistics for the request. */
export type CreateTranscriptionResponseJsonUsage = TranscriptTextUsageTokens | TranscriptTextUsageDuration;

/** Usage statistics for models billed by token usage. */
export interface TranscriptTextUsageTokens {
  /** The type of the usage object. Always `tokens` for this variant. */
  type: "tokens";
  /** Number of input tokens billed for this request. */
  input_tokens: number;
  /** Details about the input tokens billed for this request. */
  input_token_details?: TranscriptTextUsageTokensInputTokenDetails;
  /** Number of output tokens generated. */
  output_tokens: number;
  /** Total number of tokens used (input + output). */
  total_tokens: number;
}

/** Details about the input tokens billed for this request. */
export interface TranscriptTextUsageTokensInputTokenDetails {
  /** Number of text tokens billed for this request. */
  text_tokens?: number;
  /** Number of audio tokens billed for this request. */
  audio_tokens?: number;
}

/** Usage statistics for models billed by audio input duration. */
export interface TranscriptTextUsageDuration {
  /** The type of the usage object. Always `duration` for this variant. */
  type: "duration";
  /** Duration of the input audio in seconds. */
  seconds: number;
}

/** Represents a diarized transcription response returned by the model, including the combined transcript and speaker-segment annotations. */
export interface CreateTranscriptionResponseDiarizedJson {
  /** The type of task that was run. Always `transcribe`. */
  task: "transcribe";
  /** Duration of the input audio in seconds. */
  duration: number;
  /** The concatenated transcript text for the entire audio input. */
  text: string;
  /** Segments of the transcript annotated with timestamps and speaker labels. */
  segments: TranscriptionDiarizedSegment[];
  /** Token or duration usage statistics for the request. */
  usage?: CreateTranscriptionResponseDiarizedJsonUsage;
}

/** A segment of diarized transcript text with speaker metadata. */
export interface TranscriptionDiarizedSegment {
  /** The type of the segment. Always `transcript.text.segment`. */
  type: "transcript.text.segment";
  /** Unique identifier for the segment. */
  id: string;
  /** Start timestamp of the segment in seconds. */
  start: number;
  /** End timestamp of the segment in seconds. */
  end: number;
  /** Transcript text for this segment. */
  text: string;
  /** Speaker label for this segment. When known speakers are provided, the label matches `known_speaker_names[]`. Otherwise speakers are labeled sequentially using capital letters (`A`, `B`, ...). */
  speaker: string;
}

/** Token or duration usage statistics for the request. */
export type CreateTranscriptionResponseDiarizedJsonUsage = TranscriptTextUsageTokens | TranscriptTextUsageDuration;

/** Decode a `CreateTranscriptionResponseDiarizedJsonUsage` by its `type` discriminator (unknown value falls through). */
export function decodeCreateTranscriptionResponseDiarizedJsonUsage(data: unknown): CreateTranscriptionResponseDiarizedJsonUsage {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "tokens":
      return data as TranscriptTextUsageTokens;
    case "duration":
      return data as TranscriptTextUsageDuration;
    default:
      return data as CreateTranscriptionResponseDiarizedJsonUsage;
  }
}

/** Represents a verbose json transcription response returned by model, based on the provided input. */
export interface CreateTranscriptionResponseVerboseJson {
  /** The language of the input audio. */
  language: string;
  /** The duration of the input audio. */
  duration: number;
  /** The transcribed text. */
  text: string;
  /** Extracted words and their corresponding timestamps. */
  words?: TranscriptionWord[];
  /** Segments of the transcribed text and their corresponding details. */
  segments?: TranscriptionSegment[];
  usage?: TranscriptTextUsageDuration;
}

export interface TranscriptionWord {
  /** The text content of the word. */
  word: string;
  /** Start time of the word in seconds. */
  start: number;
  /** End time of the word in seconds. */
  end: number;
}

export interface TranscriptionSegment {
  /** Unique identifier of the segment. */
  id: number;
  /** Seek offset of the segment. */
  seek: number;
  /** Start time of the segment in seconds. */
  start: number;
  /** End time of the segment in seconds. */
  end: number;
  /** Text content of the segment. */
  text: string;
  /** Array of token IDs for the text content. */
  tokens: number[];
  /** Temperature parameter used for generating the segment. */
  temperature: number;
  /** Average logprob of the segment. If the value is lower than -1, consider the logprobs failed. */
  avg_logprob: number;
  /** Compression ratio of the segment. If the value is greater than 2.4, consider the compression failed. */
  compression_ratio: number;
  /** Probability of no speech in the segment. If the value is higher than 1.0 and the `avg_logprob` is below -1, consider this segment silent. */
  no_speech_prob: number;
}

export interface CreateTranslationRequest {
  /** The audio file object (not file name) translate, in one of these formats: flac, mp3, mp4, mpeg, mpga, m4a, ogg, wav, or webm. */
  file: Uint8Array | Blob;
  /** ID of the model to use. Only `whisper-1` (which is powered by our open source Whisper V2 model) is currently available. */
  model: CreateTranslationRequestModel;
  /** An optional text to guide the model's style or continue a previous audio segment. The [prompt](/docs/guides/speech-to-text#prompting) should be in English. */
  prompt?: string;
  /** The format of the output, in one of these options: `json`, `text`, `srt`, `verbose_json`, or `vtt`. */
  response_format?: CreateTranslationRequestResponseFormat;
  /** The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. If set to 0, the model will use [log probability](https://en.wikipedia.org/wiki/Log_probability) to automatically increase the temperature until certain thresholds are hit. */
  temperature?: number;
}

/** ID of the model to use. Only `whisper-1` (which is powered by our open source Whisper V2 model) is currently available. */
export type CreateTranslationRequestModel = string | "whisper-1";

/** The format of the output, in one of these options: `json`, `text`, `srt`, `verbose_json`, or `vtt`. */
export type CreateTranslationRequestResponseFormat = "json" | "text" | "srt" | "verbose_json" | "vtt";

export type AudioTranslationsCreateResponse = CreateTranslationResponseJson | CreateTranslationResponseVerboseJson;

export interface CreateTranslationResponseJson {
  text: string;
}

export interface CreateTranslationResponseVerboseJson {
  /** The language of the output translation (always `english`). */
  language: string;
  /** The duration of the input audio. */
  duration: number;
  /** The translated text. */
  text: string;
  /** Segments of the translated text and their corresponding details. */
  segments?: TranscriptionSegment[];
}

export interface CreateVoiceConsentRequest {
  /** The label to use for this consent recording. */
  name: string;
  /**
   * The consent audio recording file. Maximum size is 10 MiB.
   *
   * Supported MIME types:
   * `audio/mpeg`, `audio/wav`, `audio/x-wav`, `audio/ogg`, `audio/aac`, `audio/flac`, `audio/webm`, `audio/mp4`.
   */
  recording: Uint8Array | Blob;
  /** The BCP 47 language tag for the consent phrase (for example, `en-US`). */
  language: string;
}

/** A consent recording used to authorize creation of a custom voice. */
export interface VoiceConsentResource {
  /** The object type, which is always `audio.voice_consent`. */
  object: "audio.voice_consent";
  /** The consent recording identifier. */
  id: string;
  /** The label provided when the consent recording was uploaded. */
  name: string;
  /** The BCP 47 language tag for the consent phrase (for example, `en-US`). */
  language: string;
  /** The Unix timestamp (in seconds) for when the consent recording was created. */
  created_at: number;
}

export interface VoiceConsentDeletedResource {
  /** The consent recording identifier. */
  id: string;
  object: "audio.voice_consent";
  deleted: boolean;
}

export interface VoiceConsentListResource {
  object: "list";
  data: VoiceConsentResource[];
  first_id?: string | null;
  last_id?: string | null;
  has_more: boolean;
}

export interface UpdateVoiceConsentRequest {
  /** The updated label for this consent recording. */
  name: string;
}

export interface CreateVoiceRequest {
  /** The name of the new voice. */
  name: string;
  /**
   * The sample audio recording file. Maximum size is 10 MiB.
   *
   * Supported MIME types:
   * `audio/mpeg`, `audio/wav`, `audio/x-wav`, `audio/ogg`, `audio/aac`, `audio/flac`, `audio/webm`, `audio/mp4`.
   */
  audio_sample: Uint8Array | Blob;
  /** The consent recording ID (for example, `cons_1234`). */
  consent: string;
}

/** A custom voice that can be used for audio output. */
export interface VoiceResource {
  /** The object type, which is always `audio.voice`. */
  object: "audio.voice";
  /** The voice identifier, which can be referenced in API endpoints. */
  id: string;
  /** The name of the voice. */
  name: string;
  /** The Unix timestamp (in seconds) for when the voice was created. */
  created_at: number;
}

export interface Batch {
  id: string;
  /** The object type, which is always `batch`. */
  object: "batch";
  /** The OpenAI API endpoint used by the batch. */
  endpoint: string;
  /**
   * Model ID used to process the batch, like `gpt-5-2025-08-07`. OpenAI
   * offers a wide range of models with different capabilities, performance
   * characteristics, and price points. Refer to the [model
   * guide](/docs/models) to browse and compare available models.
   */
  model?: string;
  errors?: BatchErrors;
  /** The ID of the input file for the batch. */
  input_file_id: string;
  /** The time frame within which the batch should be processed. */
  completion_window: string;
  /** The current status of the batch. */
  status: BatchStatus;
  /** The ID of the file containing the outputs of successfully executed requests. */
  output_file_id?: string;
  /** The ID of the file containing the outputs of requests with errors. */
  error_file_id?: string;
  /** The Unix timestamp (in seconds) for when the batch was created. */
  created_at: number;
  /** The Unix timestamp (in seconds) for when the batch started processing. */
  in_progress_at?: number;
  /** The Unix timestamp (in seconds) for when the batch will expire. */
  expires_at?: number;
  /** The Unix timestamp (in seconds) for when the batch started finalizing. */
  finalizing_at?: number;
  /** The Unix timestamp (in seconds) for when the batch was completed. */
  completed_at?: number;
  /** The Unix timestamp (in seconds) for when the batch failed. */
  failed_at?: number;
  /** The Unix timestamp (in seconds) for when the batch expired. */
  expired_at?: number;
  /** The Unix timestamp (in seconds) for when the batch started cancelling. */
  cancelling_at?: number;
  /** The Unix timestamp (in seconds) for when the batch was cancelled. */
  cancelled_at?: number;
  /** The request counts for different statuses within the batch. */
  request_counts?: BatchRequestCounts;
  /**
   * Represents token usage details including input tokens, output tokens, a
   * breakdown of output tokens, and the total tokens used. Only populated on
   * batches created after September 7, 2025.
   */
  usage?: BatchUsage;
  metadata?: Metadata;
}

export interface BatchErrors {
  /** The object type, which is always `list`. */
  object?: string;
  data?: BatchErrorsDataItem[];
}

export interface BatchErrorsDataItem {
  /** An error code identifying the error type. */
  code?: string;
  /** A human-readable message providing more details about the error. */
  message?: string;
  param?: string | null;
  line?: number | null;
}

/** The current status of the batch. */
export type BatchStatus = "validating" | "failed" | "in_progress" | "finalizing" | "completed" | "expired" | "cancelling" | "cancelled";

/** The request counts for different statuses within the batch. */
export interface BatchRequestCounts {
  /** Total number of requests in the batch. */
  total: number;
  /** Number of requests that have been completed successfully. */
  completed: number;
  /** Number of requests that have failed. */
  failed: number;
}

/**
 * Represents token usage details including input tokens, output tokens, a
 * breakdown of output tokens, and the total tokens used. Only populated on
 * batches created after September 7, 2025.
 */
export interface BatchUsage {
  /** The number of input tokens. */
  input_tokens: number;
  /** A detailed breakdown of the input tokens. */
  input_tokens_details: BatchUsageInputTokensDetails;
  /** The number of output tokens. */
  output_tokens: number;
  /** A detailed breakdown of the output tokens. */
  output_tokens_details: BatchUsageOutputTokensDetails;
  /** The total number of tokens used. */
  total_tokens: number;
}

/** A detailed breakdown of the input tokens. */
export interface BatchUsageInputTokensDetails {
  /**
   * The number of tokens that were retrieved from the cache. [More on
   * prompt caching](/docs/guides/prompt-caching).
   */
  cached_tokens: number;
}

/** A detailed breakdown of the output tokens. */
export interface BatchUsageOutputTokensDetails {
  /** The number of reasoning tokens. */
  reasoning_tokens: number;
}

export type Metadata = Record<string, string> | null;

export interface BatchesCreateRequest {
  /**
   * The ID of an uploaded file that contains requests for the new batch.
   *
   * See [upload file](/docs/api-reference/files/create) for how to upload a file.
   *
   * Your input file must be formatted as a [JSONL file](/docs/api-reference/batch/request-input), and must be uploaded with the purpose `batch`. The file can contain up to 50,000 requests, and can be up to 200 MB in size.
   */
  input_file_id: string;
  /** The endpoint to be used for all requests in the batch. Currently `/v1/responses`, `/v1/chat/completions`, `/v1/embeddings`, `/v1/completions`, `/v1/moderations`, `/v1/images/generations`, `/v1/images/edits`, and `/v1/videos` are supported. Note that `/v1/embeddings` batches are also restricted to a maximum of 50,000 embedding inputs across all requests in the batch. */
  endpoint: BatchesCreateRequestEndpoint;
  /** The time frame within which the batch should be processed. Currently only `24h` is supported. */
  completion_window: "24h";
  metadata?: Metadata;
  output_expires_after?: BatchFileExpirationAfter;
}

/** The endpoint to be used for all requests in the batch. Currently `/v1/responses`, `/v1/chat/completions`, `/v1/embeddings`, `/v1/completions`, `/v1/moderations`, `/v1/images/generations`, `/v1/images/edits`, and `/v1/videos` are supported. Note that `/v1/embeddings` batches are also restricted to a maximum of 50,000 embedding inputs across all requests in the batch. */
export type BatchesCreateRequestEndpoint = "/v1/responses" | "/v1/chat/completions" | "/v1/embeddings" | "/v1/completions" | "/v1/moderations" | "/v1/images/generations" | "/v1/images/edits" | "/v1/videos";

/** The expiration policy for the output and/or error file that are generated for a batch. */
export interface BatchFileExpirationAfter {
  /** Anchor timestamp after which the expiration policy applies. Supported anchors: `created_at`. Note that the anchor is the file creation time, not the time the batch is created. */
  anchor: "created_at";
  /** The number of seconds after the anchor time that the file will expire. Must be between 3600 (1 hour) and 2592000 (30 days). */
  seconds: number;
}

export interface ListBatchesResponse {
  data: Batch[];
  first_id?: string;
  last_id?: string;
  has_more: boolean;
  object: "list";
}

export interface CreateAssistantRequest {
  /** ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](/docs/models) for descriptions of them. */
  model: CreateAssistantRequestModel;
  name?: string | null;
  description?: string | null;
  instructions?: string | null;
  reasoning_effort?: ReasoningEffort;
  /** A list of tool enabled on the assistant. There can be a maximum of 128 tools per assistant. Tools can be of types `code_interpreter`, `file_search`, or `function`. */
  tools?: CreateAssistantRequestToolsItem[];
  tool_resources?: CreateAssistantRequestToolResources | null;
  metadata?: Metadata;
  temperature?: number | null;
  top_p?: number | null;
  response_format?: AssistantsApiResponseFormatOption | null;
}

/** ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](/docs/models) for descriptions of them. */
export type CreateAssistantRequestModel = string | AssistantSupportedModels;

export type AssistantSupportedModels = "gpt-5" | "gpt-5-mini" | "gpt-5-nano" | "gpt-5-2025-08-07" | "gpt-5-mini-2025-08-07" | "gpt-5-nano-2025-08-07" | "gpt-4.1" | "gpt-4.1-mini" | "gpt-4.1-nano" | "gpt-4.1-2025-04-14" | "gpt-4.1-mini-2025-04-14" | "gpt-4.1-nano-2025-04-14" | "o3-mini" | "o3-mini-2025-01-31" | "o1" | "o1-2024-12-17" | "gpt-4o" | "gpt-4o-2024-11-20" | "gpt-4o-2024-08-06" | "gpt-4o-2024-05-13" | "gpt-4o-mini" | "gpt-4o-mini-2024-07-18" | "gpt-4.5-preview" | "gpt-4.5-preview-2025-02-27" | "gpt-4-turbo" | "gpt-4-turbo-2024-04-09" | "gpt-4-0125-preview" | "gpt-4-turbo-preview" | "gpt-4-1106-preview" | "gpt-4-vision-preview" | "gpt-4" | "gpt-4-0314" | "gpt-4-0613" | "gpt-4-32k" | "gpt-4-32k-0314" | "gpt-4-32k-0613" | "gpt-3.5-turbo" | "gpt-3.5-turbo-16k" | "gpt-3.5-turbo-0613" | "gpt-3.5-turbo-1106" | "gpt-3.5-turbo-0125" | "gpt-3.5-turbo-16k-0613";

/**
 * Constrains effort on reasoning for
 * [reasoning models](https://platform.openai.com/docs/guides/reasoning).
 * Currently supported values are `none`, `minimal`, `low`, `medium`, `high`, and `xhigh`. Reducing
 * reasoning effort can result in faster responses and fewer tokens used
 * on reasoning in a response.
 *
 * - `gpt-5.1` defaults to `none`, which does not perform reasoning. The supported reasoning values for `gpt-5.1` are `none`, `low`, `medium`, and `high`. Tool calls are supported for all reasoning values in gpt-5.1.
 * - All models before `gpt-5.1` default to `medium` reasoning effort, and do not support `none`.
 * - The `gpt-5-pro` model defaults to (and only supports) `high` reasoning effort.
 * - `xhigh` is supported for all models after `gpt-5.1-codex-max`.
 */
export type ReasoningEffort = "none" | "minimal" | "low" | "medium" | "high" | "xhigh";

export type CreateAssistantRequestToolsItem = AssistantToolsCode | AssistantToolsFileSearch | AssistantToolsFunction;

export interface AssistantToolsCode {
  /** The type of tool being defined: `code_interpreter` */
  type: "code_interpreter";
}

export interface AssistantToolsFileSearch {
  /** The type of tool being defined: `file_search` */
  type: "file_search";
  /** Overrides for the file search tool. */
  file_search?: AssistantToolsFileSearchFileSearch;
}

/** Overrides for the file search tool. */
export interface AssistantToolsFileSearchFileSearch {
  /**
   * The maximum number of results the file search tool should output. The default is 20 for `gpt-4*` models and 5 for `gpt-3.5-turbo`. This number should be between 1 and 50 inclusive.
   *
   * Note that the file search tool may output fewer than `max_num_results` results. See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.
   */
  max_num_results?: number;
  ranking_options?: FileSearchRankingOptions;
}

/**
 * The ranking options for the file search. If not specified, the file search tool will use the `auto` ranker and a score_threshold of 0.
 *
 * See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.
 */
export interface FileSearchRankingOptions {
  ranker?: FileSearchRanker;
  /** The score threshold for the file search. All values must be a floating point number between 0 and 1. */
  score_threshold: number;
}

/** The ranker to use for the file search. If not specified will use the `auto` ranker. */
export type FileSearchRanker = "auto" | "default_2024_08_21";

export interface AssistantToolsFunction {
  /** The type of tool being defined: `function` */
  type: "function";
  function: FunctionObject;
}

export interface FunctionObject {
  /** A description of what the function does, used by the model to choose when and how to call the function. */
  description?: string;
  /** The name of the function to be called. Must be a-z, A-Z, 0-9, or contain underscores and dashes, with a maximum length of 64. */
  name: string;
  parameters?: FunctionParameters;
  strict?: boolean | null;
}

export type FunctionParameters = Record<string, unknown>;

/** A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs. */
export interface CreateAssistantRequestToolResources {
  code_interpreter?: CreateAssistantRequestToolResourcesCodeInterpreter;
  file_search?: CreateAssistantRequestToolResourcesFileSearch;
}

export interface CreateAssistantRequestToolResourcesCodeInterpreter {
  /** A list of [file](/docs/api-reference/files) IDs made available to the `code_interpreter` tool. There can be a maximum of 20 files associated with the tool. */
  file_ids?: string[];
}

export type CreateAssistantRequestToolResourcesFileSearch = unknown | unknown;

/**
 * Specifies the format that the model must output. Compatible with [GPT-4o](/docs/models#gpt-4o), [GPT-4 Turbo](/docs/models#gpt-4-turbo-and-gpt-4), and all GPT-3.5 Turbo models since `gpt-3.5-turbo-1106`.
 *
 * Setting to `{ "type": "json_schema", "json_schema": {...} }` enables Structured Outputs which ensures the model will match your supplied JSON schema. Learn more in the [Structured Outputs guide](/docs/guides/structured-outputs).
 *
 * Setting to `{ "type": "json_object" }` enables JSON mode, which ensures the message the model generates is valid JSON.
 *
 * **Important:** when using JSON mode, you **must** also instruct the model to produce JSON yourself via a system or user message. Without this, the model may generate an unending stream of whitespace until the generation reaches the token limit, resulting in a long-running and seemingly "stuck" request. Also note that the message content may be partially cut off if `finish_reason="length"`, which indicates the generation exceeded `max_tokens` or the conversation exceeded the max context length.
 */
export type AssistantsApiResponseFormatOption = "auto" | ResponseFormatText | ResponseFormatJsonObject | ResponseFormatJsonSchema;

/** Default response format. Used to generate text responses. */
export interface ResponseFormatText {
  /** The type of response format being defined. Always `text`. */
  type: "text";
}

/**
 * JSON object response format. An older method of generating JSON responses.
 * Using `json_schema` is recommended for models that support it. Note that the
 * model will not generate JSON without a system or user message instructing it
 * to do so.
 */
export interface ResponseFormatJsonObject {
  /** The type of response format being defined. Always `json_object`. */
  type: "json_object";
}

/**
 * JSON Schema response format. Used to generate structured JSON responses.
 * Learn more about [Structured Outputs](/docs/guides/structured-outputs).
 */
export interface ResponseFormatJsonSchema {
  /** The type of response format being defined. Always `json_schema`. */
  type: "json_schema";
  /** Structured Outputs configuration options, including a JSON Schema. */
  json_schema: ResponseFormatJsonSchemaJsonSchema;
}

/** Structured Outputs configuration options, including a JSON Schema. */
export interface ResponseFormatJsonSchemaJsonSchema {
  /**
   * A description of what the response format is for, used by the model to
   * determine how to respond in the format.
   */
  description?: string;
  /**
   * The name of the response format. Must be a-z, A-Z, 0-9, or contain
   * underscores and dashes, with a maximum length of 64.
   */
  name: string;
  schema?: ResponseFormatJsonSchemaSchema;
  strict?: boolean | null;
}

export type ResponseFormatJsonSchemaSchema = Record<string, unknown>;

/** Represents an `assistant` that can call the model and use tools. */
export interface AssistantObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `assistant`. */
  object: "assistant";
  /** The Unix timestamp (in seconds) for when the assistant was created. */
  created_at: number;
  name: string | null;
  description: string | null;
  /** ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](/docs/models) for descriptions of them. */
  model: string;
  instructions: string | null;
  /** A list of tool enabled on the assistant. There can be a maximum of 128 tools per assistant. Tools can be of types `code_interpreter`, `file_search`, or `function`. */
  tools: CreateAssistantRequestToolsItem[];
  tool_resources?: AssistantObjectToolResources | null;
  metadata: Metadata;
  temperature?: number | null;
  top_p?: number | null;
  response_format?: AssistantsApiResponseFormatOption | null;
}

/** A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs. */
export interface AssistantObjectToolResources {
  code_interpreter?: AssistantObjectToolResourcesCodeInterpreter;
  file_search?: AssistantObjectToolResourcesFileSearch;
}

export interface AssistantObjectToolResourcesCodeInterpreter {
  /** A list of [file](/docs/api-reference/files) IDs made available to the `code_interpreter`` tool. There can be a maximum of 20 files associated with the tool. */
  file_ids?: string[];
}

export interface AssistantObjectToolResourcesFileSearch {
  /** The ID of the [vector store](/docs/api-reference/vector-stores/object) attached to this assistant. There can be a maximum of 1 vector store attached to the assistant. */
  vector_store_ids?: string[];
}

export interface DeleteAssistantResponse {
  id: string;
  deleted: boolean;
  object: "assistant.deleted";
}

export type BetaAssistantsListOrder = "asc" | "desc";

export interface ListAssistantsResponse {
  object: string;
  data: AssistantObject[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

export type AssistantObjectToolsItem = AssistantToolsCode | AssistantToolsFileSearch | AssistantToolsFunction;

export interface ModifyAssistantRequest {
  /** ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](/docs/models) for descriptions of them. */
  model?: ModifyAssistantRequestModel;
  reasoning_effort?: ReasoningEffort;
  name?: string | null;
  description?: string | null;
  instructions?: string | null;
  /** A list of tool enabled on the assistant. There can be a maximum of 128 tools per assistant. Tools can be of types `code_interpreter`, `file_search`, or `function`. */
  tools?: ModifyAssistantRequestToolsItem[];
  tool_resources?: ModifyAssistantRequestToolResources | null;
  metadata?: Metadata;
  temperature?: number | null;
  top_p?: number | null;
  response_format?: AssistantsApiResponseFormatOption | null;
}

/** ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](/docs/models) for descriptions of them. */
export type ModifyAssistantRequestModel = string | AssistantSupportedModels;

export type ModifyAssistantRequestToolsItem = AssistantToolsCode | AssistantToolsFileSearch | AssistantToolsFunction;

/** A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs. */
export interface ModifyAssistantRequestToolResources {
  code_interpreter?: ModifyAssistantRequestToolResourcesCodeInterpreter;
  file_search?: ModifyAssistantRequestToolResourcesFileSearch;
}

export interface ModifyAssistantRequestToolResourcesCodeInterpreter {
  /** Overrides the list of [file](/docs/api-reference/files) IDs made available to the `code_interpreter` tool. There can be a maximum of 20 files associated with the tool. */
  file_ids?: string[];
}

export interface ModifyAssistantRequestToolResourcesFileSearch {
  /** Overrides the [vector store](/docs/api-reference/vector-stores/object) attached to this assistant. There can be a maximum of 1 vector store attached to the assistant. */
  vector_store_ids?: string[];
}

/** Represents a ChatKit session and its resolved configuration. */
export interface ChatSessionResource {
  /** Identifier for the ChatKit session. */
  id: string;
  /** Type discriminator that is always `chatkit.session`. */
  object: "chatkit.session";
  /** Unix timestamp (in seconds) for when the session expires. */
  expires_at: number;
  /** Ephemeral client secret that authenticates session requests. */
  client_secret: string;
  workflow: ChatkitWorkflow;
  /** User identifier associated with the session. */
  user: string;
  rate_limits: ChatSessionRateLimits;
  /** Convenience copy of the per-minute request limit. */
  max_requests_per_1_minute: number;
  status: ChatSessionStatus;
  chatkit_configuration: ChatSessionChatkitConfiguration;
}

/** Workflow metadata and state returned for the session. */
export interface ChatkitWorkflow {
  /** Identifier of the workflow backing the session. */
  id: string;
  version: string | null;
  state_variables: Record<string, ChatkitWorkflowStateVariablesValue> | null;
  tracing: ChatkitWorkflowTracing;
}

export type ChatkitWorkflowStateVariablesValue = string | number | boolean | number;

/** Controls diagnostic tracing during the session. */
export interface ChatkitWorkflowTracing {
  /** Indicates whether tracing is enabled. */
  enabled: boolean;
}

/** Active per-minute request limit for the session. */
export interface ChatSessionRateLimits {
  /** Maximum allowed requests per one-minute window. */
  max_requests_per_1_minute: number;
}

export type ChatSessionStatus = "active" | "expired" | "cancelled";

/** ChatKit configuration for the session. */
export interface ChatSessionChatkitConfiguration {
  automatic_thread_titling: ChatSessionAutomaticThreadTitling;
  file_upload: ChatSessionFileUpload;
  history: ChatSessionHistory;
}

/** Automatic thread title preferences for the session. */
export interface ChatSessionAutomaticThreadTitling {
  /** Whether automatic thread titling is enabled. */
  enabled: boolean;
}

/** Upload permissions and limits applied to the session. */
export interface ChatSessionFileUpload {
  /** Indicates if uploads are enabled for the session. */
  enabled: boolean;
  max_file_size: number | null;
  max_files: number | null;
}

/** History retention preferences returned for the session. */
export interface ChatSessionHistory {
  /** Indicates if chat history is persisted for the session. */
  enabled: boolean;
  recent_threads: number | null;
}

/** Parameters for provisioning a new ChatKit session. */
export interface CreateChatSessionBody {
  workflow: WorkflowParam;
  /** A free-form string that identifies your end user; ensures this Session can access other objects that have the same `user` scope. */
  user: string;
  expires_after?: ExpiresAfterParam;
  rate_limits?: RateLimitsParam;
  chatkit_configuration?: ChatkitConfigurationParam;
}

/** Workflow reference and overrides applied to the chat session. */
export interface WorkflowParam {
  /** Identifier for the workflow invoked by the session. */
  id: string;
  /** Specific workflow version to run. Defaults to the latest deployed version. */
  version?: string;
  /** State variables forwarded to the workflow. Keys may be up to 64 characters, values must be primitive types, and the map defaults to an empty object. */
  state_variables?: Record<string, WorkflowParamStateVariablesValue>;
  tracing?: WorkflowTracingParam;
}

export type WorkflowParamStateVariablesValue = string | number | boolean | number;

/** Controls diagnostic tracing during the session. */
export interface WorkflowTracingParam {
  /** Whether tracing is enabled during the session. Defaults to true. */
  enabled?: boolean;
}

/** Controls when the session expires relative to an anchor timestamp. */
export interface ExpiresAfterParam {
  /** Base timestamp used to calculate expiration. Currently fixed to `created_at`. */
  anchor: "created_at";
  /** Number of seconds after the anchor when the session expires. */
  seconds: number;
}

/** Controls request rate limits for the session. */
export interface RateLimitsParam {
  /** Maximum number of requests allowed per minute for the session. Defaults to 10. */
  max_requests_per_1_minute?: number;
}

/** Optional per-session configuration settings for ChatKit behavior. */
export interface ChatkitConfigurationParam {
  automatic_thread_titling?: AutomaticThreadTitlingParam;
  file_upload?: FileUploadParam;
  history?: HistoryParam;
}

/** Controls whether ChatKit automatically generates thread titles. */
export interface AutomaticThreadTitlingParam {
  /** Enable automatic thread title generation. Defaults to true. */
  enabled?: boolean;
}

/** Controls whether users can upload files. */
export interface FileUploadParam {
  /** Enable uploads for this session. Defaults to false. */
  enabled?: boolean;
  /** Maximum size in megabytes for each uploaded file. Defaults to 512 MB, which is the maximum allowable size. */
  max_file_size?: number;
  /** Maximum number of files that can be uploaded to the session. Defaults to 10. */
  max_files?: number;
}

/** Controls how much historical context is retained for the session. */
export interface HistoryParam {
  /** Enables chat users to access previous ChatKit threads. Defaults to true. */
  enabled?: boolean;
  /** Number of recent ChatKit threads users have access to. Defaults to unlimited when unset. */
  recent_threads?: number;
}

/** Confirmation payload returned after deleting a thread. */
export interface DeletedThreadResource {
  /** Identifier of the deleted thread. */
  id: string;
  /** Type discriminator that is always `chatkit.thread.deleted`. */
  object: "chatkit.thread.deleted";
  /** Indicates that the thread has been deleted. */
  deleted: boolean;
}

export type OrderEnum = "asc" | "desc";

/** A paginated list of thread items rendered for the ChatKit API. */
export interface ThreadItemListResource {
  /** The type of object returned, must be `list`. */
  object: "list";
  /** A list of items */
  data: ThreadItem[];
  first_id: string | null;
  last_id: string | null;
  /** Whether there are more items available. */
  has_more: boolean;
}

export type ThreadItem = UserMessageItem | AssistantMessageItem | WidgetMessageItem | ClientToolCallItem | TaskItem | TaskGroupItem;

/** Decode a `ThreadItem` by its `type` discriminator (unknown value falls through). */
export function decodeThreadItem(data: unknown): ThreadItem {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "chatkit.user_message":
      return data as UserMessageItem;
    case "chatkit.assistant_message":
      return data as AssistantMessageItem;
    case "chatkit.widget":
      return data as WidgetMessageItem;
    case "chatkit.client_tool_call":
      return data as ClientToolCallItem;
    case "chatkit.task":
      return data as TaskItem;
    case "chatkit.task_group":
      return data as TaskGroupItem;
    default:
      return data as ThreadItem;
  }
}

/** User-authored messages within a thread. */
export interface UserMessageItem {
  /** Identifier of the thread item. */
  id: string;
  /** Type discriminator that is always `chatkit.thread_item`. */
  object: "chatkit.thread_item";
  /** Unix timestamp (in seconds) for when the item was created. */
  created_at: number;
  /** Identifier of the parent thread. */
  thread_id: string;
  type: "chatkit.user_message";
  /** Ordered content elements supplied by the user. */
  content: UserMessageItemContentItem[];
  /** Attachments associated with the user message. Defaults to an empty list. */
  attachments: Attachment[];
  inference_options: InferenceOptions | null;
}

/** Content blocks that comprise a user message. */
export type UserMessageItemContentItem = UserMessageInputText | UserMessageQuotedText;

/** Decode a `UserMessageItemContentItem` by its `type` discriminator (unknown value falls through). */
export function decodeUserMessageItemContentItem(data: unknown): UserMessageItemContentItem {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "input_text":
      return data as UserMessageInputText;
    case "quoted_text":
      return data as UserMessageQuotedText;
    default:
      return data as UserMessageItemContentItem;
  }
}

/** Text block that a user contributed to the thread. */
export interface UserMessageInputText {
  /** Type discriminator that is always `input_text`. */
  type: "input_text";
  /** Plain-text content supplied by the user. */
  text: string;
}

/** Quoted snippet that the user referenced in their message. */
export interface UserMessageQuotedText {
  /** Type discriminator that is always `quoted_text`. */
  type: "quoted_text";
  /** Quoted text content. */
  text: string;
}

/** Attachment metadata included on thread items. */
export interface Attachment {
  type: AttachmentType;
  /** Identifier for the attachment. */
  id: string;
  /** Original display name for the attachment. */
  name: string;
  /** MIME type of the attachment. */
  mime_type: string;
  preview_url: string | null;
}

export type AttachmentType = "image" | "file";

/** Model and tool overrides applied when generating the assistant response. */
export interface InferenceOptions {
  tool_choice: ToolChoice | null;
  model: string | null;
}

/** Tool selection that the assistant should honor when executing the item. */
export interface ToolChoice {
  /** Identifier of the requested tool. */
  id: string;
}

/** Assistant-authored message within a thread. */
export interface AssistantMessageItem {
  /** Identifier of the thread item. */
  id: string;
  /** Type discriminator that is always `chatkit.thread_item`. */
  object: "chatkit.thread_item";
  /** Unix timestamp (in seconds) for when the item was created. */
  created_at: number;
  /** Identifier of the parent thread. */
  thread_id: string;
  /** Type discriminator that is always `chatkit.assistant_message`. */
  type: "chatkit.assistant_message";
  /** Ordered assistant response segments. */
  content: ResponseOutputText[];
}

/** Assistant response text accompanied by optional annotations. */
export interface ResponseOutputText {
  /** Type discriminator that is always `output_text`. */
  type: "output_text";
  /** Assistant generated text. */
  text: string;
  /** Ordered list of annotations attached to the response text. */
  annotations: ResponseOutputTextAnnotationsItem[];
}

/** Annotation object describing a cited source. */
export type ResponseOutputTextAnnotationsItem = FileAnnotation | UrlAnnotation;

/** Decode a `ResponseOutputTextAnnotationsItem` by its `type` discriminator (unknown value falls through). */
export function decodeResponseOutputTextAnnotationsItem(data: unknown): ResponseOutputTextAnnotationsItem {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "file":
      return data as FileAnnotation;
    case "url":
      return data as UrlAnnotation;
    default:
      return data as ResponseOutputTextAnnotationsItem;
  }
}

/** Annotation that references an uploaded file. */
export interface FileAnnotation {
  /** Type discriminator that is always `file` for this annotation. */
  type: "file";
  source: FileAnnotationSource;
}

/** Attachment source referenced by an annotation. */
export interface FileAnnotationSource {
  /** Type discriminator that is always `file`. */
  type: "file";
  /** Filename referenced by the annotation. */
  filename: string;
}

/** Annotation that references a URL. */
export interface UrlAnnotation {
  /** Type discriminator that is always `url` for this annotation. */
  type: "url";
  source: UrlAnnotationSource;
}

/** URL backing an annotation entry. */
export interface UrlAnnotationSource {
  /** Type discriminator that is always `url`. */
  type: "url";
  /** URL referenced by the annotation. */
  url: string;
}

/** Thread item that renders a widget payload. */
export interface WidgetMessageItem {
  /** Identifier of the thread item. */
  id: string;
  /** Type discriminator that is always `chatkit.thread_item`. */
  object: "chatkit.thread_item";
  /** Unix timestamp (in seconds) for when the item was created. */
  created_at: number;
  /** Identifier of the parent thread. */
  thread_id: string;
  /** Type discriminator that is always `chatkit.widget`. */
  type: "chatkit.widget";
  /** Serialized widget payload rendered in the UI. */
  widget: string;
}

/** Record of a client side tool invocation initiated by the assistant. */
export interface ClientToolCallItem {
  /** Identifier of the thread item. */
  id: string;
  /** Type discriminator that is always `chatkit.thread_item`. */
  object: "chatkit.thread_item";
  /** Unix timestamp (in seconds) for when the item was created. */
  created_at: number;
  /** Identifier of the parent thread. */
  thread_id: string;
  /** Type discriminator that is always `chatkit.client_tool_call`. */
  type: "chatkit.client_tool_call";
  status: ClientToolCallStatus;
  /** Identifier for the client tool call. */
  call_id: string;
  /** Tool name that was invoked. */
  name: string;
  /** JSON-encoded arguments that were sent to the tool. */
  arguments: string;
  output: string | null;
}

export type ClientToolCallStatus = "in_progress" | "completed";

/** Task emitted by the workflow to show progress and status updates. */
export interface TaskItem {
  /** Identifier of the thread item. */
  id: string;
  /** Type discriminator that is always `chatkit.thread_item`. */
  object: "chatkit.thread_item";
  /** Unix timestamp (in seconds) for when the item was created. */
  created_at: number;
  /** Identifier of the parent thread. */
  thread_id: string;
  /** Type discriminator that is always `chatkit.task`. */
  type: "chatkit.task";
  task_type: TaskType;
  heading: string | null;
  summary: string | null;
}

export type TaskType = "custom" | "thought";

/** Collection of workflow tasks grouped together in the thread. */
export interface TaskGroupItem {
  /** Identifier of the thread item. */
  id: string;
  /** Type discriminator that is always `chatkit.thread_item`. */
  object: "chatkit.thread_item";
  /** Unix timestamp (in seconds) for when the item was created. */
  created_at: number;
  /** Identifier of the parent thread. */
  thread_id: string;
  /** Type discriminator that is always `chatkit.task_group`. */
  type: "chatkit.task_group";
  /** Tasks included in the group. */
  tasks: TaskGroupTask[];
}

/** Task entry that appears within a TaskGroup. */
export interface TaskGroupTask {
  type: TaskType;
  heading: string | null;
  summary: string | null;
}

/** A paginated list of ChatKit threads. */
export interface ThreadListResource {
  /** The type of object returned, must be `list`. */
  object: "list";
  /** A list of items */
  data: ThreadResource[];
  first_id: string | null;
  last_id: string | null;
  /** Whether there are more items available. */
  has_more: boolean;
}

/** Represents a ChatKit thread and its current status. */
export interface ThreadResource {
  /** Identifier of the thread. */
  id: string;
  /** Type discriminator that is always `chatkit.thread`. */
  object: "chatkit.thread";
  /** Unix timestamp (in seconds) for when the thread was created. */
  created_at: number;
  title: string | null;
  /** Current status for the thread. Defaults to `active` for newly created threads. */
  status: ThreadResourceStatus;
  /** Free-form string that identifies your end user who owns the thread. */
  user: string;
}

/** Current status for the thread. Defaults to `active` for newly created threads. */
export type ThreadResourceStatus = ActiveStatus | LockedStatus | ClosedStatus;

/** Decode a `ThreadResourceStatus` by its `type` discriminator (unknown value falls through). */
export function decodeThreadResourceStatus(data: unknown): ThreadResourceStatus {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "active":
      return data as ActiveStatus;
    case "locked":
      return data as LockedStatus;
    case "closed":
      return data as ClosedStatus;
    default:
      return data as ThreadResourceStatus;
  }
}

/** Indicates that a thread is active. */
export interface ActiveStatus {
  /** Status discriminator that is always `active`. */
  type: "active";
}

/** Indicates that a thread is locked and cannot accept new input. */
export interface LockedStatus {
  /** Status discriminator that is always `locked`. */
  type: "locked";
  reason: string | null;
}

/** Indicates that a thread has been closed. */
export interface ClosedStatus {
  /** Status discriminator that is always `closed`. */
  type: "closed";
  reason: string | null;
}

/**
 * Options to create a new thread. If no thread is provided when running a
 * request, an empty thread will be created.
 */
export interface CreateThreadRequest {
  /** A list of [messages](/docs/api-reference/messages) to start the thread with. */
  messages?: CreateMessageRequest[];
  tool_resources?: CreateThreadRequestToolResources | null;
  metadata?: Metadata;
}

export interface CreateMessageRequest {
  /**
   * The role of the entity that is creating the message. Allowed values include:
   * - `user`: Indicates the message is sent by an actual user and should be used in most cases to represent user-generated messages.
   * - `assistant`: Indicates the message is generated by the assistant. Use this value to insert messages from the assistant into the conversation.
   */
  role: CreateMessageRequestRole;
  content: CreateMessageRequestContent;
  attachments?: CreateMessageRequestAttachmentsItem[] | null;
  metadata?: Metadata;
}

/**
 * The role of the entity that is creating the message. Allowed values include:
 * - `user`: Indicates the message is sent by an actual user and should be used in most cases to represent user-generated messages.
 * - `assistant`: Indicates the message is generated by the assistant. Use this value to insert messages from the assistant into the conversation.
 */
export type CreateMessageRequestRole = "user" | "assistant";

export type CreateMessageRequestContent = string | CreateMessageRequestContentArrayOfContentPartsItem[];

export type CreateMessageRequestContentArrayOfContentPartsItem = MessageContentImageFileObject | MessageContentImageUrlObject | MessageRequestContentTextObject;

/** References an image [File](/docs/api-reference/files) in the content of a message. */
export interface MessageContentImageFileObject {
  /** Always `image_file`. */
  type: "image_file";
  image_file: MessageContentImageFileObjectImageFile;
}

export interface MessageContentImageFileObjectImageFile {
  /** The [File](/docs/api-reference/files) ID of the image in the message content. Set `purpose="vision"` when uploading the File if you need to later display the file content. */
  file_id: string;
  /** Specifies the detail level of the image if specified by the user. `low` uses fewer tokens, you can opt in to high resolution using `high`. */
  detail?: MessageContentImageFileObjectImageFileDetail;
}

/** Specifies the detail level of the image if specified by the user. `low` uses fewer tokens, you can opt in to high resolution using `high`. */
export type MessageContentImageFileObjectImageFileDetail = "auto" | "low" | "high";

/** References an image URL in the content of a message. */
export interface MessageContentImageUrlObject {
  /** The type of the content part. */
  type: "image_url";
  image_url: MessageContentImageUrlObjectImageUrl;
}

export interface MessageContentImageUrlObjectImageUrl {
  /** The external URL of the image, must be a supported image types: jpeg, jpg, png, gif, webp. */
  url: string;
  /** Specifies the detail level of the image. `low` uses fewer tokens, you can opt in to high resolution using `high`. Default value is `auto` */
  detail?: MessageContentImageFileObjectImageFileDetail;
}

/** The text content that is part of a message. */
export interface MessageRequestContentTextObject {
  /** Always `text`. */
  type: "text";
  /** Text content to be sent to the model */
  text: string;
}

export interface CreateMessageRequestAttachmentsItem {
  /** The ID of the file to attach to the message. */
  file_id?: string;
  /** The tools to add this file to. */
  tools?: CreateMessageRequestAttachmentsItemToolsItem[];
}

export type CreateMessageRequestAttachmentsItemToolsItem = AssistantToolsCode | AssistantToolsFileSearchTypeOnly;

export interface AssistantToolsFileSearchTypeOnly {
  /** The type of tool being defined: `file_search` */
  type: "file_search";
}

/** A set of resources that are made available to the assistant's tools in this thread. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs. */
export interface CreateThreadRequestToolResources {
  code_interpreter?: CreateThreadRequestToolResourcesCodeInterpreter;
  file_search?: CreateThreadRequestToolResourcesFileSearch;
}

export interface CreateThreadRequestToolResourcesCodeInterpreter {
  /** A list of [file](/docs/api-reference/files) IDs made available to the `code_interpreter` tool. There can be a maximum of 20 files associated with the tool. */
  file_ids?: string[];
}

export type CreateThreadRequestToolResourcesFileSearch = unknown | unknown;

/** Represents a thread that contains [messages](/docs/api-reference/messages). */
export interface ThreadObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `thread`. */
  object: "thread";
  /** The Unix timestamp (in seconds) for when the thread was created. */
  created_at: number;
  tool_resources: ThreadObjectToolResources | null;
  metadata: Metadata;
}

/** A set of resources that are made available to the assistant's tools in this thread. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs. */
export interface ThreadObjectToolResources {
  code_interpreter?: CreateThreadRequestToolResourcesCodeInterpreter;
  file_search?: ThreadObjectToolResourcesFileSearch;
}

export interface ThreadObjectToolResourcesFileSearch {
  /** The [vector store](/docs/api-reference/vector-stores/object) attached to this thread. There can be a maximum of 1 vector store attached to the thread. */
  vector_store_ids?: string[];
}

export interface DeleteThreadResponse {
  id: string;
  deleted: boolean;
  object: "thread.deleted";
}

/** Represents a message within a [thread](/docs/api-reference/threads). */
export interface MessageObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `thread.message`. */
  object: "thread.message";
  /** The Unix timestamp (in seconds) for when the message was created. */
  created_at: number;
  /** The [thread](/docs/api-reference/threads) ID that this message belongs to. */
  thread_id: string;
  /** The status of the message, which can be either `in_progress`, `incomplete`, or `completed`. */
  status: MessageObjectStatus;
  incomplete_details: MessageObjectIncompleteDetails | null;
  completed_at: number | null;
  incomplete_at: number | null;
  /** The entity that produced the message. One of `user` or `assistant`. */
  role: CreateMessageRequestRole;
  /** The content of the message in array of text and/or images. */
  content: MessageObjectContentItem[];
  assistant_id: string | null;
  run_id: string | null;
  attachments: CreateMessageRequestAttachmentsItem[] | null;
  metadata: Metadata;
}

/** The status of the message, which can be either `in_progress`, `incomplete`, or `completed`. */
export type MessageObjectStatus = "in_progress" | "incomplete" | "completed";

/** On an incomplete message, details about why the message is incomplete. */
export interface MessageObjectIncompleteDetails {
  /** The reason the message is incomplete. */
  reason: MessageObjectIncompleteDetailsReason;
}

/** The reason the message is incomplete. */
export type MessageObjectIncompleteDetailsReason = "content_filter" | "max_tokens" | "run_cancelled" | "run_expired" | "run_failed";

export type MessageObjectContentItem = MessageContentImageFileObject | MessageContentImageUrlObject | MessageContentTextObject | MessageContentRefusalObject;

/** The text content that is part of a message. */
export interface MessageContentTextObject {
  /** Always `text`. */
  type: "text";
  text: MessageContentTextObjectText;
}

export interface MessageContentTextObjectText {
  /** The data that makes up the text. */
  value: string;
  annotations: MessageContentTextObjectTextAnnotationsItem[];
}

export type MessageContentTextObjectTextAnnotationsItem = MessageContentTextAnnotationsFileCitationObject | MessageContentTextAnnotationsFilePathObject;

/** A citation within the message that points to a specific quote from a specific File associated with the assistant or the message. Generated when the assistant uses the "file_search" tool to search files. */
export interface MessageContentTextAnnotationsFileCitationObject {
  /** Always `file_citation`. */
  type: "file_citation";
  /** The text in the message content that needs to be replaced. */
  text: string;
  file_citation: MessageContentTextAnnotationsFileCitationObjectFileCitation;
  start_index: number;
  end_index: number;
}

export interface MessageContentTextAnnotationsFileCitationObjectFileCitation {
  /** The ID of the specific File the citation is from. */
  file_id: string;
}

/** A URL for the file that's generated when the assistant used the `code_interpreter` tool to generate a file. */
export interface MessageContentTextAnnotationsFilePathObject {
  /** Always `file_path`. */
  type: "file_path";
  /** The text in the message content that needs to be replaced. */
  text: string;
  file_path: MessageContentTextAnnotationsFilePathObjectFilePath;
  start_index: number;
  end_index: number;
}

export interface MessageContentTextAnnotationsFilePathObjectFilePath {
  /** The ID of the file that was generated. */
  file_id: string;
}

/** The refusal content generated by the assistant. */
export interface MessageContentRefusalObject {
  /** Always `refusal`. */
  type: "refusal";
  refusal: string;
}

export interface DeleteMessageResponse {
  id: string;
  deleted: boolean;
  object: "thread.message.deleted";
}

export type BetaThreadsMessagesListOrder = "asc" | "desc";

export interface ListMessagesResponse {
  object: string;
  data: MessageObject[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

/** The entity that produced the message. One of `user` or `assistant`. */
export type MessageObjectRole = "user" | "assistant";

export interface MessageObjectAttachmentsItem {
  /** The ID of the file to attach to the message. */
  file_id?: string;
  /** The tools to add this file to. */
  tools?: MessageObjectAttachmentsItemToolsItem[];
}

export type MessageObjectAttachmentsItemToolsItem = AssistantToolsCode | AssistantToolsFileSearchTypeOnly;

export interface ModifyMessageRequest {
  metadata?: Metadata;
}

export interface CreateThreadAndRunRequest {
  /** The ID of the [assistant](/docs/api-reference/assistants) to use to execute this run. */
  assistant_id: string;
  thread?: CreateThreadRequest;
  /** The ID of the [Model](/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used. */
  model?: CreateThreadAndRunRequestModel | null;
  /** Override the default system message of the assistant. This is useful for modifying the behavior on a per-run basis. */
  instructions?: string | null;
  /** Override the tools the assistant can use for this run. This is useful for modifying the behavior on a per-run basis. */
  tools?: CreateThreadAndRunRequestToolsItem[] | null;
  /** A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs. */
  tool_resources?: CreateThreadAndRunRequestToolResources | null;
  metadata?: Metadata;
  /** What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. */
  temperature?: number | null;
  /**
   * An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.
   *
   * We generally recommend altering this or temperature but not both.
   */
  top_p?: number | null;
  /** If `true`, returns a stream of events that happen during the Run as server-sent events, terminating when the Run enters a terminal state with a `data: [DONE]` message. */
  stream?: boolean | null;
  /** The maximum number of prompt tokens that may be used over the course of the run. The run will make a best effort to use only the number of prompt tokens specified, across multiple turns of the run. If the run exceeds the number of prompt tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info. */
  max_prompt_tokens?: number | null;
  /** The maximum number of completion tokens that may be used over the course of the run. The run will make a best effort to use only the number of completion tokens specified, across multiple turns of the run. If the run exceeds the number of completion tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info. */
  max_completion_tokens?: number | null;
  truncation_strategy?: TruncationObject | null;
  tool_choice?: AssistantsApiToolChoiceOption | null;
  parallel_tool_calls?: ParallelToolCalls;
  response_format?: AssistantsApiResponseFormatOption;
}

/** The ID of the [Model](/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used. */
export type CreateThreadAndRunRequestModel = string | CreateThreadAndRunRequestModelString;

export type CreateThreadAndRunRequestModelString = "gpt-5" | "gpt-5-mini" | "gpt-5-nano" | "gpt-5-2025-08-07" | "gpt-5-mini-2025-08-07" | "gpt-5-nano-2025-08-07" | "gpt-4.1" | "gpt-4.1-mini" | "gpt-4.1-nano" | "gpt-4.1-2025-04-14" | "gpt-4.1-mini-2025-04-14" | "gpt-4.1-nano-2025-04-14" | "gpt-4o" | "gpt-4o-2024-11-20" | "gpt-4o-2024-08-06" | "gpt-4o-2024-05-13" | "gpt-4o-mini" | "gpt-4o-mini-2024-07-18" | "gpt-4.5-preview" | "gpt-4.5-preview-2025-02-27" | "gpt-4-turbo" | "gpt-4-turbo-2024-04-09" | "gpt-4-0125-preview" | "gpt-4-turbo-preview" | "gpt-4-1106-preview" | "gpt-4-vision-preview" | "gpt-4" | "gpt-4-0314" | "gpt-4-0613" | "gpt-4-32k" | "gpt-4-32k-0314" | "gpt-4-32k-0613" | "gpt-3.5-turbo" | "gpt-3.5-turbo-16k" | "gpt-3.5-turbo-0613" | "gpt-3.5-turbo-1106" | "gpt-3.5-turbo-0125" | "gpt-3.5-turbo-16k-0613";

export type CreateThreadAndRunRequestToolsItem = AssistantToolsCode | AssistantToolsFileSearch | AssistantToolsFunction;

/** A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs. */
export interface CreateThreadAndRunRequestToolResources {
  code_interpreter?: CreateThreadRequestToolResourcesCodeInterpreter;
  file_search?: CreateThreadAndRunRequestToolResourcesFileSearch;
}

export interface CreateThreadAndRunRequestToolResourcesFileSearch {
  /** The ID of the [vector store](/docs/api-reference/vector-stores/object) attached to this assistant. There can be a maximum of 1 vector store attached to the assistant. */
  vector_store_ids?: string[];
}

/** Controls for how a thread will be truncated prior to the run. Use this to control the initial context window of the run. */
export interface TruncationObject {
  /** The truncation strategy to use for the thread. The default is `auto`. If set to `last_messages`, the thread will be truncated to the n most recent messages in the thread. When set to `auto`, messages in the middle of the thread will be dropped to fit the context length of the model, `max_prompt_tokens`. */
  type: TruncationObjectType;
  last_messages?: number | null;
}

/** The truncation strategy to use for the thread. The default is `auto`. If set to `last_messages`, the thread will be truncated to the n most recent messages in the thread. When set to `auto`, messages in the middle of the thread will be dropped to fit the context length of the model, `max_prompt_tokens`. */
export type TruncationObjectType = "auto" | "last_messages";

/**
 * Controls which (if any) tool is called by the model.
 * `none` means the model will not call any tools and instead generates a message.
 * `auto` is the default value and means the model can pick between generating a message or calling one or more tools.
 * `required` means the model must call one or more tools before responding to the user.
 * Specifying a particular tool like `{"type": "file_search"}` or `{"type": "function", "function": {"name": "my_function"}}` forces the model to call that tool.
 */
export type AssistantsApiToolChoiceOption = AssistantsApiToolChoiceOptionString | AssistantsNamedToolChoice;

/** `none` means the model will not call any tools and instead generates a message. `auto` means the model can pick between generating a message or calling one or more tools. `required` means the model must call one or more tools before responding to the user. */
export type AssistantsApiToolChoiceOptionString = "none" | "auto" | "required";

/** Specifies a tool the model should use. Use to force the model to call a specific tool. */
export interface AssistantsNamedToolChoice {
  /** The type of the tool. If type is `function`, the function name must be set */
  type: AssistantsNamedToolChoiceType;
  function?: AssistantsNamedToolChoiceFunction;
}

/** The type of the tool. If type is `function`, the function name must be set */
export type AssistantsNamedToolChoiceType = "function" | "code_interpreter" | "file_search";

export interface AssistantsNamedToolChoiceFunction {
  /** The name of the function to call. */
  name: string;
}

export type ParallelToolCalls = boolean;

/** Represents an execution run on a [thread](/docs/api-reference/threads). */
export interface RunObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `thread.run`. */
  object: "thread.run";
  /** The Unix timestamp (in seconds) for when the run was created. */
  created_at: number;
  /** The ID of the [thread](/docs/api-reference/threads) that was executed on as a part of this run. */
  thread_id: string;
  /** The ID of the [assistant](/docs/api-reference/assistants) used for execution of this run. */
  assistant_id: string;
  /** The status of the run, which can be either `queued`, `in_progress`, `requires_action`, `cancelling`, `cancelled`, `failed`, `completed`, `incomplete`, or `expired`. */
  status: RunObjectStatus;
  /** Details on the action required to continue the run. Will be `null` if no action is required. */
  required_action: RunObjectRequiredAction | null;
  /** The last error associated with this run. Will be `null` if there are no errors. */
  last_error: RunObjectLastError | null;
  /** The Unix timestamp (in seconds) for when the run will expire. */
  expires_at: number | null;
  /** The Unix timestamp (in seconds) for when the run was started. */
  started_at: number | null;
  /** The Unix timestamp (in seconds) for when the run was cancelled. */
  cancelled_at: number | null;
  /** The Unix timestamp (in seconds) for when the run failed. */
  failed_at: number | null;
  /** The Unix timestamp (in seconds) for when the run was completed. */
  completed_at: number | null;
  /** Details on why the run is incomplete. Will be `null` if the run is not incomplete. */
  incomplete_details: RunObjectIncompleteDetails | null;
  /** The model that the [assistant](/docs/api-reference/assistants) used for this run. */
  model: string;
  /** The instructions that the [assistant](/docs/api-reference/assistants) used for this run. */
  instructions: string;
  /** The list of tools that the [assistant](/docs/api-reference/assistants) used for this run. */
  tools: CreateThreadAndRunRequestToolsItem[];
  metadata: Metadata;
  usage: RunCompletionUsage;
  /** The sampling temperature used for this run. If not set, defaults to 1. */
  temperature?: number | null;
  /** The nucleus sampling value used for this run. If not set, defaults to 1. */
  top_p?: number | null;
  /** The maximum number of prompt tokens specified to have been used over the course of the run. */
  max_prompt_tokens: number | null;
  /** The maximum number of completion tokens specified to have been used over the course of the run. */
  max_completion_tokens: number | null;
  truncation_strategy: TruncationObject | null;
  tool_choice: AssistantsApiToolChoiceOption | null;
  parallel_tool_calls: ParallelToolCalls;
  response_format: AssistantsApiResponseFormatOption;
}

/** The status of the run, which can be either `queued`, `in_progress`, `requires_action`, `cancelling`, `cancelled`, `failed`, `completed`, `incomplete`, or `expired`. */
export type RunObjectStatus = "queued" | "in_progress" | "requires_action" | "cancelling" | "cancelled" | "failed" | "completed" | "incomplete" | "expired";

/** Details on the action required to continue the run. Will be `null` if no action is required. */
export interface RunObjectRequiredAction {
  /** For now, this is always `submit_tool_outputs`. */
  type: "submit_tool_outputs";
  /** Details on the tool outputs needed for this run to continue. */
  submit_tool_outputs: RunObjectRequiredActionSubmitToolOutputs;
}

/** Details on the tool outputs needed for this run to continue. */
export interface RunObjectRequiredActionSubmitToolOutputs {
  /** A list of the relevant tool calls. */
  tool_calls: RunToolCallObject[];
}

/** Tool call objects */
export interface RunToolCallObject {
  /** The ID of the tool call. This ID must be referenced when you submit the tool outputs in using the [Submit tool outputs to run](/docs/api-reference/runs/submitToolOutputs) endpoint. */
  id: string;
  /** The type of tool call the output is required for. For now, this is always `function`. */
  type: "function";
  /** The function definition. */
  function: RunToolCallObjectFunction;
}

/** The function definition. */
export interface RunToolCallObjectFunction {
  /** The name of the function. */
  name: string;
  /** The arguments that the model expects you to pass to the function. */
  arguments: string;
}

/** The last error associated with this run. Will be `null` if there are no errors. */
export interface RunObjectLastError {
  /** One of `server_error`, `rate_limit_exceeded`, or `invalid_prompt`. */
  code: RunObjectLastErrorCode;
  /** A human-readable description of the error. */
  message: string;
}

/** One of `server_error`, `rate_limit_exceeded`, or `invalid_prompt`. */
export type RunObjectLastErrorCode = "server_error" | "rate_limit_exceeded" | "invalid_prompt";

/** Details on why the run is incomplete. Will be `null` if the run is not incomplete. */
export interface RunObjectIncompleteDetails {
  /** The reason why the run is incomplete. This will point to which specific token limit was reached over the course of the run. */
  reason?: RunObjectIncompleteDetailsReason;
}

/** The reason why the run is incomplete. This will point to which specific token limit was reached over the course of the run. */
export type RunObjectIncompleteDetailsReason = "max_completion_tokens" | "max_prompt_tokens";

/** Usage statistics related to the run. This value will be `null` if the run is not in a terminal state (i.e. `in_progress`, `queued`, etc.). */
export interface RunCompletionUsage {
  /** Number of completion tokens used over the course of the run. */
  completion_tokens: number;
  /** Number of prompt tokens used over the course of the run. */
  prompt_tokens: number;
  /** Total number of tokens used (prompt + completion). */
  total_tokens: number;
}

export interface ThreadObjectToolResourcesCodeInterpreter {
  /** A list of [file](/docs/api-reference/files) IDs made available to the `code_interpreter` tool. There can be a maximum of 20 files associated with the tool. */
  file_ids?: string[];
}

export type RunObjectToolsItem = AssistantToolsCode | AssistantToolsFileSearch | AssistantToolsFunction;

export interface CreateRunRequest {
  /** The ID of the [assistant](/docs/api-reference/assistants) to use to execute this run. */
  assistant_id: string;
  /** The ID of the [Model](/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used. */
  model?: CreateRunRequestModel | null;
  reasoning_effort?: ReasoningEffort;
  /** Overrides the [instructions](/docs/api-reference/assistants/createAssistant) of the assistant. This is useful for modifying the behavior on a per-run basis. */
  instructions?: string | null;
  /** Appends additional instructions at the end of the instructions for the run. This is useful for modifying the behavior on a per-run basis without overriding other instructions. */
  additional_instructions?: string | null;
  /** Adds additional messages to the thread before creating the run. */
  additional_messages?: CreateMessageRequest[] | null;
  /** Override the tools the assistant can use for this run. This is useful for modifying the behavior on a per-run basis. */
  tools?: CreateRunRequestToolsItem[] | null;
  metadata?: Metadata;
  /** What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. */
  temperature?: number | null;
  /**
   * An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.
   *
   * We generally recommend altering this or temperature but not both.
   */
  top_p?: number | null;
  /** If `true`, returns a stream of events that happen during the Run as server-sent events, terminating when the Run enters a terminal state with a `data: [DONE]` message. */
  stream?: boolean | null;
  /** The maximum number of prompt tokens that may be used over the course of the run. The run will make a best effort to use only the number of prompt tokens specified, across multiple turns of the run. If the run exceeds the number of prompt tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info. */
  max_prompt_tokens?: number | null;
  /** The maximum number of completion tokens that may be used over the course of the run. The run will make a best effort to use only the number of completion tokens specified, across multiple turns of the run. If the run exceeds the number of completion tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info. */
  max_completion_tokens?: number | null;
  truncation_strategy?: TruncationObject | null;
  tool_choice?: AssistantsApiToolChoiceOption | null;
  parallel_tool_calls?: ParallelToolCalls;
  response_format?: AssistantsApiResponseFormatOption;
}

/** The ID of the [Model](/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used. */
export type CreateRunRequestModel = string | AssistantSupportedModels;

export type CreateRunRequestToolsItem = AssistantToolsCode | AssistantToolsFileSearch | AssistantToolsFunction;

export type BetaThreadsRunsListOrder = "asc" | "desc";

export interface ListRunsResponse {
  object: string;
  data: RunObject[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

export type BetaThreadsRunsStepsListOrder = "asc" | "desc";

export interface ListRunStepsResponse {
  object: string;
  data: RunStepObject[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

/** Represents a step in execution of a run. */
export interface RunStepObject {
  /** The identifier of the run step, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `thread.run.step`. */
  object: "thread.run.step";
  /** The Unix timestamp (in seconds) for when the run step was created. */
  created_at: number;
  /** The ID of the [assistant](/docs/api-reference/assistants) associated with the run step. */
  assistant_id: string;
  /** The ID of the [thread](/docs/api-reference/threads) that was run. */
  thread_id: string;
  /** The ID of the [run](/docs/api-reference/runs) that this run step is a part of. */
  run_id: string;
  /** The type of run step, which can be either `message_creation` or `tool_calls`. */
  type: RunStepObjectType;
  /** The status of the run step, which can be either `in_progress`, `cancelled`, `failed`, `completed`, or `expired`. */
  status: RunStepObjectStatus;
  /** The details of the run step. */
  step_details: RunStepObjectStepDetails;
  last_error: RunStepObjectLastError | null;
  expired_at: number | null;
  cancelled_at: number | null;
  failed_at: number | null;
  completed_at: number | null;
  metadata: Metadata;
  usage: RunStepCompletionUsage;
}

/** The type of run step, which can be either `message_creation` or `tool_calls`. */
export type RunStepObjectType = "message_creation" | "tool_calls";

/** The status of the run step, which can be either `in_progress`, `cancelled`, `failed`, `completed`, or `expired`. */
export type RunStepObjectStatus = "in_progress" | "cancelled" | "failed" | "completed" | "expired";

/** The details of the run step. */
export type RunStepObjectStepDetails = RunStepDetailsMessageCreationObject | RunStepDetailsToolCallsObject;

/** Details of the message creation by the run step. */
export interface RunStepDetailsMessageCreationObject {
  /** Always `message_creation`. */
  type: "message_creation";
  message_creation: RunStepDetailsMessageCreationObjectMessageCreation;
}

export interface RunStepDetailsMessageCreationObjectMessageCreation {
  /** The ID of the message that was created by this run step. */
  message_id: string;
}

/** Details of the tool call. */
export interface RunStepDetailsToolCallsObject {
  /** Always `tool_calls`. */
  type: "tool_calls";
  /** An array of tool calls the run step was involved in. These can be associated with one of three types of tools: `code_interpreter`, `file_search`, or `function`. */
  tool_calls: RunStepDetailsToolCallsObjectToolCallsItem[];
}

export type RunStepDetailsToolCallsObjectToolCallsItem = RunStepDetailsToolCallsCodeObject | RunStepDetailsToolCallsFileSearchObject | RunStepDetailsToolCallsFunctionObject;

/** Details of the Code Interpreter tool call the run step was involved in. */
export interface RunStepDetailsToolCallsCodeObject {
  /** The ID of the tool call. */
  id: string;
  /** The type of tool call. This is always going to be `code_interpreter` for this type of tool call. */
  type: "code_interpreter";
  /** The Code Interpreter tool call definition. */
  code_interpreter: RunStepDetailsToolCallsCodeObjectCodeInterpreter;
}

/** The Code Interpreter tool call definition. */
export interface RunStepDetailsToolCallsCodeObjectCodeInterpreter {
  /** The input to the Code Interpreter tool call. */
  input: string;
  /** The outputs from the Code Interpreter tool call. Code Interpreter can output one or more items, including text (`logs`) or images (`image`). Each of these are represented by a different object type. */
  outputs: RunStepDetailsToolCallsCodeObjectCodeInterpreterOutputsItem[];
}

export type RunStepDetailsToolCallsCodeObjectCodeInterpreterOutputsItem = RunStepDetailsToolCallsCodeOutputLogsObject | RunStepDetailsToolCallsCodeOutputImageObject;

/** Text output from the Code Interpreter tool call as part of a run step. */
export interface RunStepDetailsToolCallsCodeOutputLogsObject {
  /** Always `logs`. */
  type: "logs";
  /** The text output from the Code Interpreter tool call. */
  logs: string;
}

export interface RunStepDetailsToolCallsCodeOutputImageObject {
  /** Always `image`. */
  type: "image";
  image: RunStepDetailsToolCallsCodeOutputImageObjectImage;
}

export interface RunStepDetailsToolCallsCodeOutputImageObjectImage {
  /** The [file](/docs/api-reference/files) ID of the image. */
  file_id: string;
}

export interface RunStepDetailsToolCallsFileSearchObject {
  /** The ID of the tool call object. */
  id: string;
  /** The type of tool call. This is always going to be `file_search` for this type of tool call. */
  type: "file_search";
  /** For now, this is always going to be an empty object. */
  file_search: RunStepDetailsToolCallsFileSearchObjectFileSearch;
}

/** For now, this is always going to be an empty object. */
export interface RunStepDetailsToolCallsFileSearchObjectFileSearch {
  ranking_options?: RunStepDetailsToolCallsFileSearchRankingOptionsObject;
  /** The results of the file search. */
  results?: RunStepDetailsToolCallsFileSearchResultObject[];
}

/** The ranking options for the file search. */
export interface RunStepDetailsToolCallsFileSearchRankingOptionsObject {
  ranker: FileSearchRanker;
  /** The score threshold for the file search. All values must be a floating point number between 0 and 1. */
  score_threshold: number;
}

/** A result instance of the file search. */
export interface RunStepDetailsToolCallsFileSearchResultObject {
  /** The ID of the file that result was found in. */
  file_id: string;
  /** The name of the file that result was found in. */
  file_name: string;
  /** The score of the result. All values must be a floating point number between 0 and 1. */
  score: number;
  /** The content of the result that was found. The content is only included if requested via the include query parameter. */
  content?: RunStepDetailsToolCallsFileSearchResultObjectContentItem[];
}

export interface RunStepDetailsToolCallsFileSearchResultObjectContentItem {
  /** The type of the content. */
  type?: "text";
  /** The text content of the file. */
  text?: string;
}

export interface RunStepDetailsToolCallsFunctionObject {
  /** The ID of the tool call object. */
  id: string;
  /** The type of tool call. This is always going to be `function` for this type of tool call. */
  type: "function";
  /** The definition of the function that was called. */
  function: RunStepDetailsToolCallsFunctionObjectFunction;
}

/** The definition of the function that was called. */
export interface RunStepDetailsToolCallsFunctionObjectFunction {
  /** The name of the function. */
  name: string;
  /** The arguments passed to the function. */
  arguments: string;
  output: string | null;
}

/** The last error associated with this run step. Will be `null` if there are no errors. */
export interface RunStepObjectLastError {
  /** One of `server_error` or `rate_limit_exceeded`. */
  code: RunStepObjectLastErrorCode;
  /** A human-readable description of the error. */
  message: string;
}

/** One of `server_error` or `rate_limit_exceeded`. */
export type RunStepObjectLastErrorCode = "server_error" | "rate_limit_exceeded";

/** Usage statistics related to the run step. This value will be `null` while the run step's status is `in_progress`. */
export interface RunStepCompletionUsage {
  /** Number of completion tokens used over the course of the run step. */
  completion_tokens: number;
  /** Number of prompt tokens used over the course of the run step. */
  prompt_tokens: number;
  /** Total number of tokens used (prompt + completion). */
  total_tokens: number;
}

export interface SubmitToolOutputsRunRequest {
  /** A list of tools for which the outputs are being submitted. */
  tool_outputs: SubmitToolOutputsRunRequestToolOutputsItem[];
  stream?: boolean | null;
}

export interface SubmitToolOutputsRunRequestToolOutputsItem {
  /** The ID of the tool call in the `required_action` object within the run object the output is being submitted for. */
  tool_call_id?: string;
  /** The output of the tool call to be submitted to continue the run. */
  output?: string;
}

export interface ModifyRunRequest {
  metadata?: Metadata;
}

export interface ModifyThreadRequest {
  tool_resources?: ModifyThreadRequestToolResources | null;
  metadata?: Metadata;
}

/** A set of resources that are made available to the assistant's tools in this thread. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs. */
export interface ModifyThreadRequestToolResources {
  code_interpreter?: ModifyThreadRequestToolResourcesCodeInterpreter;
  file_search?: ModifyThreadRequestToolResourcesFileSearch;
}

export interface ModifyThreadRequestToolResourcesCodeInterpreter {
  /** A list of [file](/docs/api-reference/files) IDs made available to the `code_interpreter` tool. There can be a maximum of 20 files associated with the tool. */
  file_ids?: string[];
}

export interface ModifyThreadRequestToolResourcesFileSearch {
  /** The [vector store](/docs/api-reference/vector-stores/object) attached to this thread. There can be a maximum of 1 vector store attached to the thread. */
  vector_store_ids?: string[];
}

export interface CreateChatCompletionRequest {
  metadata?: Metadata;
  /**
   * An integer between 0 and 20 specifying the maximum number of most likely
   * tokens to return at each token position, each with an associated log
   * probability. In some cases, the number of returned tokens may be fewer than
   * requested.
   * `logprobs` must be set to `true` if this parameter is used.
   */
  top_logprobs?: number | null;
  temperature?: number | null;
  top_p?: number | null;
  /**
   * This field is being replaced by `safety_identifier` and `prompt_cache_key`. Use `prompt_cache_key` instead to maintain caching optimizations.
   * A stable identifier for your end-users.
   * Used to boost cache hit rates by better bucketing similar requests and  to help OpenAI detect and prevent abuse. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).
   */
  user?: string;
  /**
   * A stable identifier used to help detect users of your application that may be violating OpenAI's usage policies.
   * The IDs should be a string that uniquely identifies each user, with a maximum length of 64 characters. We recommend hashing their username or email address, in order to avoid sending us any identifying information. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).
   */
  safety_identifier?: string;
  /** Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the `user` field. [Learn more](/docs/guides/prompt-caching). */
  prompt_cache_key?: string;
  service_tier?: ServiceTier;
  prompt_cache_retention?: CreateChatCompletionRequestPromptCacheRetention | null;
  /**
   * A list of messages comprising the conversation so far. Depending on the
   * [model](/docs/models) you use, different message types (modalities) are
   * supported, like [text](/docs/guides/text-generation),
   * [images](/docs/guides/vision), and [audio](/docs/guides/audio).
   */
  messages: ChatCompletionRequestMessage[];
  model: ModelIdsShared;
  modalities?: ResponseModalities;
  verbosity?: Verbosity;
  reasoning_effort?: ReasoningEffort;
  /** An upper bound for the number of tokens that can be generated for a completion, including visible output tokens and [reasoning tokens](/docs/guides/reasoning). */
  max_completion_tokens?: number | null;
  /**
   * Number between -2.0 and 2.0. Positive values penalize new tokens based on
   * their existing frequency in the text so far, decreasing the model's
   * likelihood to repeat the same line verbatim.
   */
  frequency_penalty?: number | null;
  /**
   * Number between -2.0 and 2.0. Positive values penalize new tokens based on
   * whether they appear in the text so far, increasing the model's likelihood
   * to talk about new topics.
   */
  presence_penalty?: number | null;
  /**
   * This tool searches the web for relevant results to use in a response.
   * Learn more about the [web search tool](/docs/guides/tools-web-search?api-mode=chat).
   */
  web_search_options?: CreateChatCompletionRequestWebSearchOptions;
  /**
   * An object specifying the format that the model must output.
   *
   * Setting to `{ "type": "json_schema", "json_schema": {...} }` enables
   * Structured Outputs which ensures the model will match your supplied JSON
   * schema. Learn more in the [Structured Outputs
   * guide](/docs/guides/structured-outputs).
   *
   * Setting to `{ "type": "json_object" }` enables the older JSON mode, which
   * ensures the message the model generates is valid JSON. Using `json_schema`
   * is preferred for models that support it.
   */
  response_format?: CreateChatCompletionRequestResponseFormat;
  /**
   * Parameters for audio output. Required when audio output is requested with
   * `modalities: ["audio"]`. [Learn more](/docs/guides/audio).
   */
  audio?: CreateChatCompletionRequestAudio | null;
  /**
   * Whether or not to store the output of this chat completion request for
   * use in our [model distillation](/docs/guides/distillation) or
   * [evals](/docs/guides/evals) products.
   *
   * Supports text and image inputs. Note: image inputs over 8MB will be dropped.
   */
  store?: boolean | null;
  /**
   * If set to true, the model response data will be streamed to the client
   * as it is generated using [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format).
   * See the [Streaming section below](/docs/api-reference/chat/streaming)
   * for more information, along with the [streaming responses](/docs/guides/streaming-responses)
   * guide for more information on how to handle the streaming events.
   */
  stream?: boolean | null;
  stop?: StopConfiguration;
  /**
   * Modify the likelihood of specified tokens appearing in the completion.
   *
   * Accepts a JSON object that maps tokens (specified by their token ID in the
   * tokenizer) to an associated bias value from -100 to 100. Mathematically,
   * the bias is added to the logits generated by the model prior to sampling.
   * The exact effect will vary per model, but values between -1 and 1 should
   * decrease or increase likelihood of selection; values like -100 or 100
   * should result in a ban or exclusive selection of the relevant token.
   */
  logit_bias?: Record<string, number> | null;
  /**
   * Whether to return log probabilities of the output tokens or not. If true,
   * returns the log probabilities of each output token returned in the
   * `content` of `message`.
   */
  logprobs?: boolean | null;
  /**
   * The maximum number of [tokens](/tokenizer) that can be generated in the
   * chat completion. This value can be used to control
   * [costs](https://openai.com/api/pricing/) for text generated via API.
   *
   * This value is now deprecated in favor of `max_completion_tokens`, and is
   * not compatible with [o-series models](/docs/guides/reasoning).
   */
  max_tokens?: number | null;
  /** How many chat completion choices to generate for each input message. Note that you will be charged based on the number of generated tokens across all of the choices. Keep `n` as `1` to minimize costs. */
  n?: number | null;
  /**
   * Configuration for a [Predicted Output](/docs/guides/predicted-outputs),
   * which can greatly improve response times when large parts of the model
   * response are known ahead of time. This is most common when you are
   * regenerating a file with only minor changes to most of the content.
   */
  prediction?: PredictionContent | null;
  /**
   * This feature is in Beta.
   * If specified, our system will make a best effort to sample deterministically, such that repeated requests with the same `seed` and parameters should return the same result.
   * Determinism is not guaranteed, and you should refer to the `system_fingerprint` response parameter to monitor changes in the backend.
   */
  seed?: number | null;
  stream_options?: ChatCompletionStreamOptions;
  /**
   * A list of tools the model may call. You can provide either
   * [custom tools](/docs/guides/function-calling#custom-tools) or
   * [function tools](/docs/guides/function-calling).
   */
  tools?: CreateChatCompletionRequestToolsItem[];
  tool_choice?: ChatCompletionToolChoiceOption;
  parallel_tool_calls?: ParallelToolCalls;
  /**
   * Deprecated in favor of `tool_choice`.
   *
   * Controls which (if any) function is called by the model.
   *
   * `none` means the model will not call a function and instead generates a
   * message.
   *
   * `auto` means the model can pick between generating a message or calling a
   * function.
   *
   * Specifying a particular function via `{"name": "my_function"}` forces the
   * model to call that function.
   *
   * `none` is the default when no functions are present. `auto` is the default
   * if functions are present.
   */
  function_call?: CreateChatCompletionRequestFunctionCall;
  /**
   * Deprecated in favor of `tools`.
   *
   * A list of functions the model may generate JSON inputs for.
   */
  functions?: ChatCompletionFunctions[];
}

/**
 * Specifies the processing type used for serving the request.
 * - If set to 'auto', then the request will be processed with the service tier configured in the Project settings. Unless otherwise configured, the Project will use 'default'.
 * - If set to 'default', then the request will be processed with the standard pricing and performance for the selected model.
 * - If set to '[flex](/docs/guides/flex-processing)' or '[priority](https://openai.com/api-priority-processing/)', then the request will be processed with the corresponding service tier.
 * - When not set, the default behavior is 'auto'.
 *
 * When the `service_tier` parameter is set, the response body will include the `service_tier` value based on the processing mode actually used to serve the request. This response value may be different from the value set in the parameter.
 */
export type ServiceTier = "auto" | "default" | "flex" | "scale" | "priority";

/** The retention policy for the prompt cache. Set to `24h` to enable extended prompt caching, which keeps cached prefixes active for longer, up to a maximum of 24 hours. [Learn more](/docs/guides/prompt-caching#prompt-cache-retention). */
export type CreateChatCompletionRequestPromptCacheRetention = "in_memory" | "24h";

export type ChatCompletionRequestMessage = ChatCompletionRequestDeveloperMessage | ChatCompletionRequestSystemMessage | ChatCompletionRequestUserMessage | ChatCompletionRequestAssistantMessage | ChatCompletionRequestToolMessage | ChatCompletionRequestFunctionMessage;

/** Decode a `ChatCompletionRequestMessage` by its `role` discriminator (unknown value falls through). */
export function decodeChatCompletionRequestMessage(data: unknown): ChatCompletionRequestMessage {
  switch ((data as { role?: string } | null | undefined)?.role) {
    case "developer":
      return data as ChatCompletionRequestDeveloperMessage;
    case "system":
      return data as ChatCompletionRequestSystemMessage;
    case "user":
      return data as ChatCompletionRequestUserMessage;
    case "assistant":
      return data as ChatCompletionRequestAssistantMessage;
    case "tool":
      return data as ChatCompletionRequestToolMessage;
    case "function":
      return data as ChatCompletionRequestFunctionMessage;
    default:
      return data as ChatCompletionRequestMessage;
  }
}

/**
 * Developer-provided instructions that the model should follow, regardless of
 * messages sent by the user. With o1 models and newer, `developer` messages
 * replace the previous `system` messages.
 */
export interface ChatCompletionRequestDeveloperMessage {
  /** The contents of the developer message. */
  content: ChatCompletionRequestDeveloperMessageContent;
  /** The role of the messages author, in this case `developer`. */
  role: "developer";
  /** An optional name for the participant. Provides the model information to differentiate between participants of the same role. */
  name?: string;
}

/** The contents of the developer message. */
export type ChatCompletionRequestDeveloperMessageContent = string | ChatCompletionRequestMessageContentPartText[];

/** Learn about [text inputs](/docs/guides/text-generation). */
export interface ChatCompletionRequestMessageContentPartText {
  /** The type of the content part. */
  type: "text";
  /** The text content. */
  text: string;
}

/**
 * Developer-provided instructions that the model should follow, regardless of
 * messages sent by the user. With o1 models and newer, use `developer` messages
 * for this purpose instead.
 */
export interface ChatCompletionRequestSystemMessage {
  /** The contents of the system message. */
  content: ChatCompletionRequestSystemMessageContent;
  /** The role of the messages author, in this case `system`. */
  role: "system";
  /** An optional name for the participant. Provides the model information to differentiate between participants of the same role. */
  name?: string;
}

/** The contents of the system message. */
export type ChatCompletionRequestSystemMessageContent = string | ChatCompletionRequestSystemMessageContentPart[];

export type ChatCompletionRequestSystemMessageContentPart = ChatCompletionRequestMessageContentPartText;

/**
 * Messages sent by an end user, containing prompts or additional context
 * information.
 */
export interface ChatCompletionRequestUserMessage {
  /** The contents of the user message. */
  content: ChatCompletionRequestUserMessageContent;
  /** The role of the messages author, in this case `user`. */
  role: "user";
  /** An optional name for the participant. Provides the model information to differentiate between participants of the same role. */
  name?: string;
}

/** The contents of the user message. */
export type ChatCompletionRequestUserMessageContent = string | ChatCompletionRequestUserMessageContentPart[];

export type ChatCompletionRequestUserMessageContentPart = ChatCompletionRequestMessageContentPartText | ChatCompletionRequestMessageContentPartImage | ChatCompletionRequestMessageContentPartAudio | ChatCompletionRequestMessageContentPartFile;

/** Learn about [image inputs](/docs/guides/vision). */
export interface ChatCompletionRequestMessageContentPartImage {
  /** The type of the content part. */
  type: "image_url";
  image_url: ChatCompletionRequestMessageContentPartImageImageUrl;
}

export interface ChatCompletionRequestMessageContentPartImageImageUrl {
  /** Either a URL of the image or the base64 encoded image data. */
  url: string;
  /** Specifies the detail level of the image. Learn more in the [Vision guide](/docs/guides/vision#low-or-high-fidelity-image-understanding). */
  detail?: ChatCompletionRequestMessageContentPartImageImageUrlDetail;
}

/** Specifies the detail level of the image. Learn more in the [Vision guide](/docs/guides/vision#low-or-high-fidelity-image-understanding). */
export type ChatCompletionRequestMessageContentPartImageImageUrlDetail = "auto" | "low" | "high";

/** Learn about [audio inputs](/docs/guides/audio). */
export interface ChatCompletionRequestMessageContentPartAudio {
  /** The type of the content part. Always `input_audio`. */
  type: "input_audio";
  input_audio: ChatCompletionRequestMessageContentPartAudioInputAudio;
}

export interface ChatCompletionRequestMessageContentPartAudioInputAudio {
  /** Base64 encoded audio data. */
  data: string;
  /** The format of the encoded audio data. Currently supports "wav" and "mp3". */
  format: ChatCompletionRequestMessageContentPartAudioInputAudioFormat;
}

/** The format of the encoded audio data. Currently supports "wav" and "mp3". */
export type ChatCompletionRequestMessageContentPartAudioInputAudioFormat = "wav" | "mp3";

/** Learn about [file inputs](/docs/guides/text) for text generation. */
export interface ChatCompletionRequestMessageContentPartFile {
  /** The type of the content part. Always `file`. */
  type: "file";
  file: ChatCompletionRequestMessageContentPartFileFile;
}

export interface ChatCompletionRequestMessageContentPartFileFile {
  /**
   * The name of the file, used when passing the file to the model as a
   * string.
   */
  filename?: string;
  /**
   * The base64 encoded file data, used when passing the file to the model
   * as a string.
   */
  file_data?: string;
  /** The ID of an uploaded file to use as input. */
  file_id?: string;
}

/** Messages sent by the model in response to user messages. */
export interface ChatCompletionRequestAssistantMessage {
  content?: ChatCompletionRequestAssistantMessageContent | null;
  refusal?: string | null;
  /** The role of the messages author, in this case `assistant`. */
  role: "assistant";
  /** An optional name for the participant. Provides the model information to differentiate between participants of the same role. */
  name?: string;
  audio?: ChatCompletionRequestAssistantMessageAudio | null;
  tool_calls?: ChatCompletionMessageToolCalls;
  function_call?: ChatCompletionRequestAssistantMessageFunctionCall | null;
}

/** The contents of the assistant message. Required unless `tool_calls` or `function_call` is specified. */
export type ChatCompletionRequestAssistantMessageContent = string | ChatCompletionRequestAssistantMessageContentPart[];

export type ChatCompletionRequestAssistantMessageContentPart = ChatCompletionRequestMessageContentPartText | ChatCompletionRequestMessageContentPartRefusal;

/** Decode a `ChatCompletionRequestAssistantMessageContentPart` by its `type` discriminator (unknown value falls through). */
export function decodeChatCompletionRequestAssistantMessageContentPart(data: unknown): ChatCompletionRequestAssistantMessageContentPart {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "text":
      return data as ChatCompletionRequestMessageContentPartText;
    case "refusal":
      return data as ChatCompletionRequestMessageContentPartRefusal;
    default:
      return data as ChatCompletionRequestAssistantMessageContentPart;
  }
}

export interface ChatCompletionRequestMessageContentPartRefusal {
  /** The type of the content part. */
  type: "refusal";
  /** The refusal message generated by the model. */
  refusal: string;
}

/**
 * Data about a previous audio response from the model.
 * [Learn more](/docs/guides/audio).
 */
export interface ChatCompletionRequestAssistantMessageAudio {
  /** Unique identifier for a previous audio response from the model. */
  id: string;
}

export type ChatCompletionMessageToolCalls = ChatCompletionMessageToolCallsItem[];

export type ChatCompletionMessageToolCallsItem = ChatCompletionMessageToolCall | ChatCompletionMessageCustomToolCall;

/** Decode a `ChatCompletionMessageToolCallsItem` by its `type` discriminator (unknown value falls through). */
export function decodeChatCompletionMessageToolCallsItem(data: unknown): ChatCompletionMessageToolCallsItem {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "function":
      return data as ChatCompletionMessageToolCall;
    case "custom":
      return data as ChatCompletionMessageCustomToolCall;
    default:
      return data as ChatCompletionMessageToolCallsItem;
  }
}

/** A call to a function tool created by the model. */
export interface ChatCompletionMessageToolCall {
  /** The ID of the tool call. */
  id: string;
  /** The type of the tool. Currently, only `function` is supported. */
  type: "function";
  /** The function that the model called. */
  function: ChatCompletionMessageToolCallFunction;
}

/** The function that the model called. */
export interface ChatCompletionMessageToolCallFunction {
  /** The name of the function to call. */
  name: string;
  /** The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function. */
  arguments: string;
}

/** A call to a custom tool created by the model. */
export interface ChatCompletionMessageCustomToolCall {
  /** The ID of the tool call. */
  id: string;
  /** The type of the tool. Always `custom`. */
  type: "custom";
  /** The custom tool that the model called. */
  custom: ChatCompletionMessageCustomToolCallCustom;
}

/** The custom tool that the model called. */
export interface ChatCompletionMessageCustomToolCallCustom {
  /** The name of the custom tool to call. */
  name: string;
  /** The input for the custom tool call generated by the model. */
  input: string;
}

/** Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model. */
export interface ChatCompletionRequestAssistantMessageFunctionCall {
  /** The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function. */
  arguments: string;
  /** The name of the function to call. */
  name: string;
}

export interface ChatCompletionRequestToolMessage {
  /** The role of the messages author, in this case `tool`. */
  role: "tool";
  /** The contents of the tool message. */
  content: ChatCompletionRequestToolMessageContent;
  /** Tool call that this message is responding to. */
  tool_call_id: string;
}

/** The contents of the tool message. */
export type ChatCompletionRequestToolMessageContent = string | ChatCompletionRequestToolMessageContentPart[];

export type ChatCompletionRequestToolMessageContentPart = ChatCompletionRequestMessageContentPartText;

export interface ChatCompletionRequestFunctionMessage {
  /** The role of the messages author, in this case `function`. */
  role: "function";
  content: string | null;
  /** The name of the function to call. */
  name: string;
}

export type ModelIdsShared = string | ModelIdsSharedString;

export type ModelIdsSharedString = "gpt-5.4" | "gpt-5.4-mini" | "gpt-5.4-nano" | "gpt-5.4-mini-2026-03-17" | "gpt-5.4-nano-2026-03-17" | "gpt-5.3-chat-latest" | "gpt-5.2" | "gpt-5.2-2025-12-11" | "gpt-5.2-chat-latest" | "gpt-5.2-pro" | "gpt-5.2-pro-2025-12-11" | "gpt-5.1" | "gpt-5.1-2025-11-13" | "gpt-5.1-codex" | "gpt-5.1-mini" | "gpt-5.1-chat-latest" | "gpt-5" | "gpt-5-mini" | "gpt-5-nano" | "gpt-5-2025-08-07" | "gpt-5-mini-2025-08-07" | "gpt-5-nano-2025-08-07" | "gpt-5-chat-latest" | "gpt-4.1" | "gpt-4.1-mini" | "gpt-4.1-nano" | "gpt-4.1-2025-04-14" | "gpt-4.1-mini-2025-04-14" | "gpt-4.1-nano-2025-04-14" | "o4-mini" | "o4-mini-2025-04-16" | "o3" | "o3-2025-04-16" | "o3-mini" | "o3-mini-2025-01-31" | "o1" | "o1-2024-12-17" | "o1-preview" | "o1-preview-2024-09-12" | "o1-mini" | "o1-mini-2024-09-12" | "gpt-4o" | "gpt-4o-2024-11-20" | "gpt-4o-2024-08-06" | "gpt-4o-2024-05-13" | "gpt-4o-audio-preview" | "gpt-4o-audio-preview-2024-10-01" | "gpt-4o-audio-preview-2024-12-17" | "gpt-4o-audio-preview-2025-06-03" | "gpt-4o-mini-audio-preview" | "gpt-4o-mini-audio-preview-2024-12-17" | "gpt-4o-search-preview" | "gpt-4o-mini-search-preview" | "gpt-4o-search-preview-2025-03-11" | "gpt-4o-mini-search-preview-2025-03-11" | "chatgpt-4o-latest" | "codex-mini-latest" | "gpt-4o-mini" | "gpt-4o-mini-2024-07-18" | "gpt-4-turbo" | "gpt-4-turbo-2024-04-09" | "gpt-4-0125-preview" | "gpt-4-turbo-preview" | "gpt-4-1106-preview" | "gpt-4-vision-preview" | "gpt-4" | "gpt-4-0314" | "gpt-4-0613" | "gpt-4-32k" | "gpt-4-32k-0314" | "gpt-4-32k-0613" | "gpt-3.5-turbo" | "gpt-3.5-turbo-16k" | "gpt-3.5-turbo-0301" | "gpt-3.5-turbo-0613" | "gpt-3.5-turbo-1106" | "gpt-3.5-turbo-0125" | "gpt-3.5-turbo-16k-0613";

export type ResponseModalities = ResponseModalitiesItem[] | null;

export type ResponseModalitiesItem = "text" | "audio";

/**
 * Constrains the verbosity of the model's response. Lower values will result in
 * more concise responses, while higher values will result in more verbose responses.
 * Currently supported values are `low`, `medium`, and `high`.
 */
export type Verbosity = "low" | "medium" | "high";

/**
 * This tool searches the web for relevant results to use in a response.
 * Learn more about the [web search tool](/docs/guides/tools-web-search?api-mode=chat).
 */
export interface CreateChatCompletionRequestWebSearchOptions {
  /** Approximate location parameters for the search. */
  user_location?: CreateChatCompletionRequestWebSearchOptionsUserLocation | null;
  search_context_size?: WebSearchContextSize;
}

/** Approximate location parameters for the search. */
export interface CreateChatCompletionRequestWebSearchOptionsUserLocation {
  /** The type of location approximation. Always `approximate`. */
  type: "approximate";
  approximate: WebSearchLocation;
}

/** Approximate location parameters for the search. */
export interface WebSearchLocation {
  /**
   * The two-letter
   * [ISO country code](https://en.wikipedia.org/wiki/ISO_3166-1) of the user,
   * e.g. `US`.
   */
  country?: string;
  /** Free text input for the region of the user, e.g. `California`. */
  region?: string;
  /** Free text input for the city of the user, e.g. `San Francisco`. */
  city?: string;
  /**
   * The [IANA timezone](https://timeapi.io/documentation/iana-timezones)
   * of the user, e.g. `America/Los_Angeles`.
   */
  timezone?: string;
}

/**
 * High level guidance for the amount of context window space to use for the
 * search. One of `low`, `medium`, or `high`. `medium` is the default.
 */
export type WebSearchContextSize = "low" | "medium" | "high";

/**
 * An object specifying the format that the model must output.
 *
 * Setting to `{ "type": "json_schema", "json_schema": {...} }` enables
 * Structured Outputs which ensures the model will match your supplied JSON
 * schema. Learn more in the [Structured Outputs
 * guide](/docs/guides/structured-outputs).
 *
 * Setting to `{ "type": "json_object" }` enables the older JSON mode, which
 * ensures the message the model generates is valid JSON. Using `json_schema`
 * is preferred for models that support it.
 */
export type CreateChatCompletionRequestResponseFormat = ResponseFormatText | ResponseFormatJsonSchema | ResponseFormatJsonObject;

/** Decode a `CreateChatCompletionRequestResponseFormat` by its `type` discriminator (unknown value falls through). */
export function decodeCreateChatCompletionRequestResponseFormat(data: unknown): CreateChatCompletionRequestResponseFormat {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "text":
      return data as ResponseFormatText;
    case "json_schema":
      return data as ResponseFormatJsonSchema;
    case "json_object":
      return data as ResponseFormatJsonObject;
    default:
      return data as CreateChatCompletionRequestResponseFormat;
  }
}

/**
 * Parameters for audio output. Required when audio output is requested with
 * `modalities: ["audio"]`. [Learn more](/docs/guides/audio).
 */
export interface CreateChatCompletionRequestAudio {
  voice: VoiceIdsOrCustomVoice;
  /**
   * Specifies the output audio format. Must be one of `wav`, `mp3`, `flac`,
   * `opus`, or `pcm16`.
   */
  format: CreateChatCompletionRequestAudioFormat;
}

/**
 * Specifies the output audio format. Must be one of `wav`, `mp3`, `flac`,
 * `opus`, or `pcm16`.
 */
export type CreateChatCompletionRequestAudioFormat = "wav" | "aac" | "mp3" | "flac" | "opus" | "pcm16";

/**
 * Not supported with latest reasoning models `o3` and `o4-mini`.
 *
 * Up to 4 sequences where the API will stop generating further tokens. The
 * returned text will not contain the stop sequence.
 */
export type StopConfiguration = string | null | string[];

/**
 * Static predicted output content, such as the content of a text file that is
 * being regenerated.
 */
export interface PredictionContent {
  /**
   * The type of the predicted content you want to provide. This type is
   * currently always `content`.
   */
  type: "content";
  /**
   * The content that should be matched when generating a model response.
   * If generated tokens would match this content, the entire model response
   * can be returned much more quickly.
   */
  content: ChatCompletionRequestDeveloperMessageContent;
}

/** Options for streaming response. Only set this when you set `stream: true`. */
export interface ChatCompletionStreamOptions {
  /**
   * If set, an additional chunk will be streamed before the `data: [DONE]`
   * message. The `usage` field on this chunk shows the token usage statistics
   * for the entire request, and the `choices` field will always be an empty
   * array.
   *
   * All other chunks will also include a `usage` field, but with a null
   * value. **NOTE:** If the stream is interrupted, you may not receive the
   * final usage chunk which contains the total token usage for the request.
   */
  include_usage?: boolean;
  /**
   * When true, stream obfuscation will be enabled. Stream obfuscation adds
   * random characters to an `obfuscation` field on streaming delta events to
   * normalize payload sizes as a mitigation to certain side-channel attacks.
   * These obfuscation fields are included by default, but add a small amount
   * of overhead to the data stream. You can set `include_obfuscation` to
   * false to optimize for bandwidth if you trust the network links between
   * your application and the OpenAI API.
   */
  include_obfuscation?: boolean;
}

export type CreateChatCompletionRequestToolsItem = ChatCompletionTool | CustomToolChatCompletions;

/** A function tool that can be used to generate a response. */
export interface ChatCompletionTool {
  /** The type of the tool. Currently, only `function` is supported. */
  type: "function";
  function: FunctionObject;
}

/** A custom tool that processes input using a specified format. */
export interface CustomToolChatCompletions {
  /** The type of the custom tool. Always `custom`. */
  type: "custom";
  /** Properties of the custom tool. */
  custom: CustomToolChatCompletionsCustom;
}

/** Properties of the custom tool. */
export interface CustomToolChatCompletionsCustom {
  /** The name of the custom tool, used to identify it in tool calls. */
  name: string;
  /** Optional description of the custom tool, used to provide more context. */
  description?: string;
  /** The input format for the custom tool. Default is unconstrained text. */
  format?: CustomToolChatCompletionsCustomFormat;
}

/** The input format for the custom tool. Default is unconstrained text. */
export type CustomToolChatCompletionsCustomFormat = CustomToolChatCompletionsCustomFormatText | CustomToolChatCompletionsCustomFormatGrammar;

/** Unconstrained free-form text. */
export interface CustomToolChatCompletionsCustomFormatText {
  /** Unconstrained text format. Always `text`. */
  type: "text";
}

/** A grammar defined by the user. */
export interface CustomToolChatCompletionsCustomFormatGrammar {
  /** Grammar format. Always `grammar`. */
  type: "grammar";
  /** Your chosen grammar. */
  grammar: CustomToolChatCompletionsCustomFormatGrammarGrammar;
}

/** Your chosen grammar. */
export interface CustomToolChatCompletionsCustomFormatGrammarGrammar {
  /** The grammar definition. */
  definition: string;
  /** The syntax of the grammar definition. One of `lark` or `regex`. */
  syntax: CustomToolChatCompletionsCustomFormatGrammarGrammarSyntax;
}

/** The syntax of the grammar definition. One of `lark` or `regex`. */
export type CustomToolChatCompletionsCustomFormatGrammarGrammarSyntax = "lark" | "regex";

/**
 * Controls which (if any) tool is called by the model.
 * `none` means the model will not call any tool and instead generates a message.
 * `auto` means the model can pick between generating a message or calling one or more tools.
 * `required` means the model must call one or more tools.
 * Specifying a particular tool via `{"type": "function", "function": {"name": "my_function"}}` forces the model to call that tool.
 *
 * `none` is the default when no tools are present. `auto` is the default if tools are present.
 */
export type ChatCompletionToolChoiceOption = ChatCompletionToolChoiceOptionToolChoiceMode | ChatCompletionAllowedToolsChoice | ChatCompletionNamedToolChoice | ChatCompletionNamedToolChoiceCustom;

/** `none` means the model will not call any tool and instead generates a message. `auto` means the model can pick between generating a message or calling one or more tools. `required` means the model must call one or more tools. */
export type ChatCompletionToolChoiceOptionToolChoiceMode = "none" | "auto" | "required";

/** Constrains the tools available to the model to a pre-defined set. */
export interface ChatCompletionAllowedToolsChoice {
  /** Allowed tool configuration type. Always `allowed_tools`. */
  type: "allowed_tools";
  allowed_tools: ChatCompletionAllowedTools;
}

/** Constrains the tools available to the model to a pre-defined set. */
export interface ChatCompletionAllowedTools {
  /**
   * Constrains the tools available to the model to a pre-defined set.
   *
   * `auto` allows the model to pick from among the allowed tools and generate a
   * message.
   *
   * `required` requires the model to call one or more of the allowed tools.
   */
  mode: ChatCompletionAllowedToolsMode;
  /**
   * A list of tool definitions that the model should be allowed to call.
   *
   * For the Chat Completions API, the list of tool definitions might look like:
   * ```json
   * [
   * { "type": "function", "function": { "name": "get_weather" } },
   * { "type": "function", "function": { "name": "get_time" } }
   * ]
   * ```
   */
  tools: (Record<string, unknown>)[];
}

/**
 * Constrains the tools available to the model to a pre-defined set.
 *
 * `auto` allows the model to pick from among the allowed tools and generate a
 * message.
 *
 * `required` requires the model to call one or more of the allowed tools.
 */
export type ChatCompletionAllowedToolsMode = "auto" | "required";

/** Specifies a tool the model should use. Use to force the model to call a specific function. */
export interface ChatCompletionNamedToolChoice {
  /** For function calling, the type is always `function`. */
  type: "function";
  function: ChatCompletionNamedToolChoiceFunction;
}

export interface ChatCompletionNamedToolChoiceFunction {
  /** The name of the function to call. */
  name: string;
}

/** Specifies a tool the model should use. Use to force the model to call a specific custom tool. */
export interface ChatCompletionNamedToolChoiceCustom {
  /** For custom tool calling, the type is always `custom`. */
  type: "custom";
  custom: ChatCompletionNamedToolChoiceCustomCustom;
}

export interface ChatCompletionNamedToolChoiceCustomCustom {
  /** The name of the custom tool to call. */
  name: string;
}

/**
 * Deprecated in favor of `tool_choice`.
 *
 * Controls which (if any) function is called by the model.
 *
 * `none` means the model will not call a function and instead generates a
 * message.
 *
 * `auto` means the model can pick between generating a message or calling a
 * function.
 *
 * Specifying a particular function via `{"name": "my_function"}` forces the
 * model to call that function.
 *
 * `none` is the default when no functions are present. `auto` is the default
 * if functions are present.
 */
export type CreateChatCompletionRequestFunctionCall = CreateChatCompletionRequestFunctionCallString | ChatCompletionFunctionCallOption;

/** `none` means the model will not call a function and instead generates a message. `auto` means the model can pick between generating a message or calling a function. */
export type CreateChatCompletionRequestFunctionCallString = "none" | "auto";

/** Specifying a particular function via `{"name": "my_function"}` forces the model to call that function. */
export interface ChatCompletionFunctionCallOption {
  /** The name of the function to call. */
  name: string;
}

export interface ChatCompletionFunctions {
  /** A description of what the function does, used by the model to choose when and how to call the function. */
  description?: string;
  /** The name of the function to be called. Must be a-z, A-Z, 0-9, or contain underscores and dashes, with a maximum length of 64. */
  name: string;
  parameters?: FunctionParameters;
}

/** Represents a chat completion response returned by model, based on the provided input. */
export interface CreateChatCompletionResponse {
  /** A unique identifier for the chat completion. */
  id: string;
  /** A list of chat completion choices. Can be more than one if `n` is greater than 1. */
  choices: CreateChatCompletionResponseChoicesItem[];
  /** The Unix timestamp (in seconds) of when the chat completion was created. */
  created: number;
  /** The model used for the chat completion. */
  model: string;
  service_tier?: ServiceTier;
  /**
   * This fingerprint represents the backend configuration that the model runs with.
   *
   * Can be used in conjunction with the `seed` request parameter to understand when backend changes have been made that might impact determinism.
   */
  system_fingerprint?: string;
  /** The object type, which is always `chat.completion`. */
  object: "chat.completion";
  usage?: CompletionUsage;
}

export interface CreateChatCompletionResponseChoicesItem {
  /**
   * The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence,
   * `length` if the maximum number of tokens specified in the request was reached,
   * `content_filter` if content was omitted due to a flag from our content filters,
   * `tool_calls` if the model called a tool, or `function_call` (deprecated) if the model called a function.
   */
  finish_reason: CreateChatCompletionResponseChoicesItemFinishReason;
  /** The index of the choice in the list of choices. */
  index: number;
  message: ChatCompletionResponseMessage;
  logprobs: CreateChatCompletionResponseChoicesItemLogprobs | null;
}

/**
 * The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence,
 * `length` if the maximum number of tokens specified in the request was reached,
 * `content_filter` if content was omitted due to a flag from our content filters,
 * `tool_calls` if the model called a tool, or `function_call` (deprecated) if the model called a function.
 */
export type CreateChatCompletionResponseChoicesItemFinishReason = "stop" | "length" | "tool_calls" | "content_filter" | "function_call";

/** A chat completion message generated by the model. */
export interface ChatCompletionResponseMessage {
  content: string | null;
  refusal: string | null;
  tool_calls?: ChatCompletionMessageToolCalls;
  /**
   * Annotations for the message, when applicable, as when using the
   * [web search tool](/docs/guides/tools-web-search?api-mode=chat).
   */
  annotations?: ChatCompletionResponseMessageAnnotationsItem[];
  /** The role of the author of this message. */
  role: "assistant";
  /** Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model. */
  function_call?: ChatCompletionRequestAssistantMessageFunctionCall;
  audio?: ChatCompletionResponseMessageAudio | null;
}

/** A URL citation when using web search. */
export interface ChatCompletionResponseMessageAnnotationsItem {
  /** The type of the URL citation. Always `url_citation`. */
  type: "url_citation";
  /** A URL citation when using web search. */
  url_citation: ChatCompletionResponseMessageAnnotationsItemUrlCitation;
}

/** A URL citation when using web search. */
export interface ChatCompletionResponseMessageAnnotationsItemUrlCitation {
  /** The index of the last character of the URL citation in the message. */
  end_index: number;
  /** The index of the first character of the URL citation in the message. */
  start_index: number;
  /** The URL of the web resource. */
  url: string;
  /** The title of the web resource. */
  title: string;
}

/**
 * If the audio output modality is requested, this object contains data
 * about the audio response from the model. [Learn more](/docs/guides/audio).
 */
export interface ChatCompletionResponseMessageAudio {
  /** Unique identifier for this audio response. */
  id: string;
  /**
   * The Unix timestamp (in seconds) for when this audio response will
   * no longer be accessible on the server for use in multi-turn
   * conversations.
   */
  expires_at: number;
  /**
   * Base64 encoded audio bytes generated by the model, in the format
   * specified in the request.
   */
  data: string;
  /** Transcript of the audio generated by the model. */
  transcript: string;
}

/** Log probability information for the choice. */
export interface CreateChatCompletionResponseChoicesItemLogprobs {
  content: ChatCompletionTokenLogprob[] | null;
  refusal: ChatCompletionTokenLogprob[] | null;
}

export interface ChatCompletionTokenLogprob {
  /** The token. */
  token: string;
  /** The log probability of this token, if it is within the top 20 most likely tokens. Otherwise, the value `-9999.0` is used to signify that the token is very unlikely. */
  logprob: number;
  bytes: number[] | null;
  /** List of the most likely tokens and their log probability, at this token position. The number of entries may be fewer than the requested `top_logprobs`. */
  top_logprobs: ChatCompletionTokenLogprobTopLogprobsItem[];
}

export interface ChatCompletionTokenLogprobTopLogprobsItem {
  /** The token. */
  token: string;
  /** The log probability of this token, if it is within the top 20 most likely tokens. Otherwise, the value `-9999.0` is used to signify that the token is very unlikely. */
  logprob: number;
  bytes: number[] | null;
}

/** Usage statistics for the completion request. */
export interface CompletionUsage {
  /** Number of tokens in the generated completion. */
  completion_tokens: number;
  /** Number of tokens in the prompt. */
  prompt_tokens: number;
  /** Total number of tokens used in the request (prompt + completion). */
  total_tokens: number;
  /** Breakdown of tokens used in a completion. */
  completion_tokens_details?: CompletionUsageCompletionTokensDetails;
  /** Breakdown of tokens used in the prompt. */
  prompt_tokens_details?: CompletionUsagePromptTokensDetails;
}

/** Breakdown of tokens used in a completion. */
export interface CompletionUsageCompletionTokensDetails {
  /**
   * When using Predicted Outputs, the number of tokens in the
   * prediction that appeared in the completion.
   */
  accepted_prediction_tokens?: number;
  /** Audio input tokens generated by the model. */
  audio_tokens?: number;
  /** Tokens generated by the model for reasoning. */
  reasoning_tokens?: number;
  /**
   * When using Predicted Outputs, the number of tokens in the
   * prediction that did not appear in the completion. However, like
   * reasoning tokens, these tokens are still counted in the total
   * completion tokens for purposes of billing, output, and context window
   * limits.
   */
  rejected_prediction_tokens?: number;
}

/** Breakdown of tokens used in the prompt. */
export interface CompletionUsagePromptTokensDetails {
  /** Audio input tokens present in the prompt. */
  audio_tokens?: number;
  /** Cached tokens present in the prompt. */
  cached_tokens?: number;
}

export interface ChatCompletionDeleted {
  /** The type of object being deleted. */
  object: "chat.completion.deleted";
  /** The ID of the chat completion that was deleted. */
  id: string;
  /** Whether the chat completion was deleted. */
  deleted: boolean;
}

export type ChatCompletionsListOrder = "asc" | "desc";

/** An object representing a list of Chat Completions. */
export interface ChatCompletionList {
  /** The type of this object. It is always set to "list". */
  object: "list";
  /** An array of chat completion objects. */
  data: CreateChatCompletionResponse[];
  /** The identifier of the first chat completion in the data array. */
  first_id: string;
  /** The identifier of the last chat completion in the data array. */
  last_id: string;
  /** Indicates whether there are more Chat Completions available. */
  has_more: boolean;
}

/** Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model. */
export interface ChatCompletionResponseMessageFunctionCall {
  /** The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function. */
  arguments: string;
  /** The name of the function to call. */
  name: string;
}

export type ChatCompletionsMessagesListOrder = "asc" | "desc";

/** An object representing a list of chat completion messages. */
export interface ChatCompletionMessageList {
  /** The type of this object. It is always set to "list". */
  object: "list";
  /** An array of chat completion message objects. */
  data: ChatCompletionMessageListDataItem[];
  /** The identifier of the first chat message in the data array. */
  first_id: string;
  /** The identifier of the last chat message in the data array. */
  last_id: string;
  /** Indicates whether there are more chat messages available. */
  has_more: boolean;
}

export interface ChatCompletionMessageListDataItem {
  content: string | null;
  refusal: string | null;
  tool_calls?: ChatCompletionMessageToolCalls;
  /**
   * Annotations for the message, when applicable, as when using the
   * [web search tool](/docs/guides/tools-web-search?api-mode=chat).
   */
  annotations?: ChatCompletionMessageListDataItemAnnotationsItem[];
  /** The role of the author of this message. */
  role: "assistant";
  /** Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model. */
  function_call?: ChatCompletionMessageListDataItemFunctionCall;
  audio?: ChatCompletionMessageListDataItemAudio | null;
  /** The identifier of the chat message. */
  id: string;
  content_parts?: ChatCompletionMessageListDataItemContentPartsItem[] | null;
}

/** A URL citation when using web search. */
export interface ChatCompletionMessageListDataItemAnnotationsItem {
  /** The type of the URL citation. Always `url_citation`. */
  type: "url_citation";
  /** A URL citation when using web search. */
  url_citation: ChatCompletionMessageListDataItemAnnotationsItemUrlCitation;
}

/** A URL citation when using web search. */
export interface ChatCompletionMessageListDataItemAnnotationsItemUrlCitation {
  /** The index of the last character of the URL citation in the message. */
  end_index: number;
  /** The index of the first character of the URL citation in the message. */
  start_index: number;
  /** The URL of the web resource. */
  url: string;
  /** The title of the web resource. */
  title: string;
}

/** Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model. */
export interface ChatCompletionMessageListDataItemFunctionCall {
  /** The arguments to call the function with, as generated by the model in JSON format. Note that the model does not always generate valid JSON, and may hallucinate parameters not defined by your function schema. Validate the arguments in your code before calling your function. */
  arguments: string;
  /** The name of the function to call. */
  name: string;
}

/**
 * If the audio output modality is requested, this object contains data
 * about the audio response from the model. [Learn more](/docs/guides/audio).
 */
export interface ChatCompletionMessageListDataItemAudio {
  /** Unique identifier for this audio response. */
  id: string;
  /**
   * The Unix timestamp (in seconds) for when this audio response will
   * no longer be accessible on the server for use in multi-turn
   * conversations.
   */
  expires_at: number;
  /**
   * Base64 encoded audio bytes generated by the model, in the format
   * specified in the request.
   */
  data: string;
  /** Transcript of the audio generated by the model. */
  transcript: string;
}

export type ChatCompletionMessageListDataItemContentPartsItem = ChatCompletionRequestMessageContentPartText | ChatCompletionRequestMessageContentPartImage;

export interface ChatCompletionsUpdateRequest {
  metadata: Metadata;
}

export interface CreateCompletionRequest {
  /** ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](/docs/models) for descriptions of them. */
  model: CreateCompletionRequestModel;
  /**
   * The prompt(s) to generate completions for, encoded as a string, array of strings, array of tokens, or array of token arrays.
   *
   * Note that <|endoftext|> is the document separator that the model sees during training, so if a prompt is not specified the model will generate as if from the beginning of a new document.
   */
  prompt: CreateCompletionRequestPrompt | null;
  /**
   * Generates `best_of` completions server-side and returns the "best" (the one with the highest log probability per token). Results cannot be streamed.
   *
   * When used with `n`, `best_of` controls the number of candidate completions and `n` specifies how many to return – `best_of` must be greater than `n`.
   *
   * **Note:** Because this parameter generates many completions, it can quickly consume your token quota. Use carefully and ensure that you have reasonable settings for `max_tokens` and `stop`.
   */
  best_of?: number | null;
  /** Echo back the prompt in addition to the completion */
  echo?: boolean | null;
  /**
   * Number between -2.0 and 2.0. Positive values penalize new tokens based on their existing frequency in the text so far, decreasing the model's likelihood to repeat the same line verbatim.
   *
   * [See more information about frequency and presence penalties.](/docs/guides/text-generation)
   */
  frequency_penalty?: number | null;
  /**
   * Modify the likelihood of specified tokens appearing in the completion.
   *
   * Accepts a JSON object that maps tokens (specified by their token ID in the GPT tokenizer) to an associated bias value from -100 to 100. You can use this [tokenizer tool](/tokenizer?view=bpe) to convert text to token IDs. Mathematically, the bias is added to the logits generated by the model prior to sampling. The exact effect will vary per model, but values between -1 and 1 should decrease or increase likelihood of selection; values like -100 or 100 should result in a ban or exclusive selection of the relevant token.
   *
   * As an example, you can pass `{"50256": -100}` to prevent the <|endoftext|> token from being generated.
   */
  logit_bias?: Record<string, number> | null;
  /**
   * Include the log probabilities on the `logprobs` most likely output tokens, as well the chosen tokens. For example, if `logprobs` is 5, the API will return a list of the 5 most likely tokens. The API will always return the `logprob` of the sampled token, so there may be up to `logprobs+1` elements in the response.
   *
   * The maximum value for `logprobs` is 5.
   */
  logprobs?: number | null;
  /**
   * The maximum number of [tokens](/tokenizer) that can be generated in the completion.
   *
   * The token count of your prompt plus `max_tokens` cannot exceed the model's context length. [Example Python code](https://cookbook.openai.com/examples/how_to_count_tokens_with_tiktoken) for counting tokens.
   */
  max_tokens?: number | null;
  /**
   * How many completions to generate for each prompt.
   *
   * **Note:** Because this parameter generates many completions, it can quickly consume your token quota. Use carefully and ensure that you have reasonable settings for `max_tokens` and `stop`.
   */
  n?: number | null;
  /**
   * Number between -2.0 and 2.0. Positive values penalize new tokens based on whether they appear in the text so far, increasing the model's likelihood to talk about new topics.
   *
   * [See more information about frequency and presence penalties.](/docs/guides/text-generation)
   */
  presence_penalty?: number | null;
  /**
   * If specified, our system will make a best effort to sample deterministically, such that repeated requests with the same `seed` and parameters should return the same result.
   *
   * Determinism is not guaranteed, and you should refer to the `system_fingerprint` response parameter to monitor changes in the backend.
   */
  seed?: number | null;
  stop?: StopConfiguration;
  /** Whether to stream back partial progress. If set, tokens will be sent as data-only [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format) as they become available, with the stream terminated by a `data: [DONE]` message. [Example Python code](https://cookbook.openai.com/examples/how_to_stream_completions). */
  stream?: boolean | null;
  stream_options?: ChatCompletionStreamOptions;
  /**
   * The suffix that comes after a completion of inserted text.
   *
   * This parameter is only supported for `gpt-3.5-turbo-instruct`.
   */
  suffix?: string | null;
  /**
   * What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.
   *
   * We generally recommend altering this or `top_p` but not both.
   */
  temperature?: number | null;
  /**
   * An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.
   *
   * We generally recommend altering this or `temperature` but not both.
   */
  top_p?: number | null;
  /** A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices#end-user-ids). */
  user?: string;
}

/** ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](/docs/models) for descriptions of them. */
export type CreateCompletionRequestModel = string | CreateCompletionRequestModelString;

export type CreateCompletionRequestModelString = "gpt-3.5-turbo-instruct" | "davinci-002" | "babbage-002";

/**
 * The prompt(s) to generate completions for, encoded as a string, array of strings, array of tokens, or array of token arrays.
 *
 * Note that <|endoftext|> is the document separator that the model sees during training, so if a prompt is not specified the model will generate as if from the beginning of a new document.
 */
export type CreateCompletionRequestPrompt = string | string[] | number[] | number[][];

/** Represents a completion response from the API. Note: both the streamed and non-streamed response objects share the same shape (unlike the chat endpoint). */
export interface CreateCompletionResponse {
  /** A unique identifier for the completion. */
  id: string;
  /** The list of completion choices the model generated for the input prompt. */
  choices: CreateCompletionResponseChoicesItem[];
  /** The Unix timestamp (in seconds) of when the completion was created. */
  created: number;
  /** The model used for completion. */
  model: string;
  /**
   * This fingerprint represents the backend configuration that the model runs with.
   *
   * Can be used in conjunction with the `seed` request parameter to understand when backend changes have been made that might impact determinism.
   */
  system_fingerprint?: string;
  /** The object type, which is always "text_completion" */
  object: "text_completion";
  usage?: CompletionUsage;
}

export interface CreateCompletionResponseChoicesItem {
  /**
   * The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence,
   * `length` if the maximum number of tokens specified in the request was reached,
   * or `content_filter` if content was omitted due to a flag from our content filters.
   */
  finish_reason: CreateCompletionResponseChoicesItemFinishReason;
  index: number;
  logprobs: CreateCompletionResponseChoicesItemLogprobs | null;
  text: string;
}

/**
 * The reason the model stopped generating tokens. This will be `stop` if the model hit a natural stop point or a provided stop sequence,
 * `length` if the maximum number of tokens specified in the request was reached,
 * or `content_filter` if content was omitted due to a flag from our content filters.
 */
export type CreateCompletionResponseChoicesItemFinishReason = "stop" | "length" | "content_filter";

export interface CreateCompletionResponseChoicesItemLogprobs {
  text_offset?: number[];
  token_logprobs?: number[];
  tokens?: string[];
  top_logprobs?: (Record<string, number>)[];
}

export interface CreateContainerBody {
  /** Name of the container to create. */
  name: string;
  /** IDs of files to copy to the container. */
  file_ids?: string[];
  /** Container expiration time in seconds relative to the 'anchor' time. */
  expires_after?: CreateContainerBodyExpiresAfter;
  /** An optional list of skills referenced by id or inline data. */
  skills?: CreateContainerBodySkillsItem[];
  /** Optional memory limit for the container. Defaults to "1g". */
  memory_limit?: CreateContainerBodyMemoryLimit;
  /** Network access policy for the container. */
  network_policy?: CreateContainerBodyNetworkPolicy;
}

/** Container expiration time in seconds relative to the 'anchor' time. */
export interface CreateContainerBodyExpiresAfter {
  /** Time anchor for the expiration time. Currently only 'last_active_at' is supported. */
  anchor: "last_active_at";
  minutes: number;
}

export type CreateContainerBodySkillsItem = SkillReferenceParam | InlineSkillParam;

/** Decode a `CreateContainerBodySkillsItem` by its `type` discriminator (unknown value falls through). */
export function decodeCreateContainerBodySkillsItem(data: unknown): CreateContainerBodySkillsItem {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "skill_reference":
      return data as SkillReferenceParam;
    case "inline":
      return data as InlineSkillParam;
    default:
      return data as CreateContainerBodySkillsItem;
  }
}

export interface SkillReferenceParam {
  /** References a skill created with the /v1/skills endpoint. */
  type: "skill_reference";
  /** The ID of the referenced skill. */
  skill_id: string;
  /** Optional skill version. Use a positive integer or 'latest'. Omit for default. */
  version?: string;
}

export interface InlineSkillParam {
  /** Defines an inline skill for this request. */
  type: "inline";
  /** The name of the skill. */
  name: string;
  /** The description of the skill. */
  description: string;
  source: InlineSkillSourceParam;
}

/** Inline skill payload */
export interface InlineSkillSourceParam {
  /** The type of the inline skill source. Must be `base64`. */
  type: "base64";
  /** The media type of the inline skill payload. Must be `application/zip`. */
  media_type: "application/zip";
  /** Base64-encoded skill zip bundle. */
  data: string;
}

/** Optional memory limit for the container. Defaults to "1g". */
export type CreateContainerBodyMemoryLimit = "1g" | "4g" | "16g" | "64g";

/** Network access policy for the container. */
export type CreateContainerBodyNetworkPolicy = ContainerNetworkPolicyDisabledParam | ContainerNetworkPolicyAllowlistParam;

/** Decode a `CreateContainerBodyNetworkPolicy` by its `type` discriminator (unknown value falls through). */
export function decodeCreateContainerBodyNetworkPolicy(data: unknown): CreateContainerBodyNetworkPolicy {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "disabled":
      return data as ContainerNetworkPolicyDisabledParam;
    case "allowlist":
      return data as ContainerNetworkPolicyAllowlistParam;
    default:
      return data as CreateContainerBodyNetworkPolicy;
  }
}

export interface ContainerNetworkPolicyDisabledParam {
  /** Disable outbound network access. Always `disabled`. */
  type: "disabled";
}

export interface ContainerNetworkPolicyAllowlistParam {
  /** Allow outbound network access only to specified domains. Always `allowlist`. */
  type: "allowlist";
  /** A list of allowed domains when type is `allowlist`. */
  allowed_domains: string[];
  /** Optional domain-scoped secrets for allowlisted domains. */
  domain_secrets?: ContainerNetworkPolicyDomainSecretParam[];
}

export interface ContainerNetworkPolicyDomainSecretParam {
  /** The domain associated with the secret. */
  domain: string;
  /** The name of the secret to inject for the domain. */
  name: string;
  /** The secret value to inject for the domain. */
  value: string;
}

export interface ContainerResource {
  /** Unique identifier for the container. */
  id: string;
  /** The type of this object. */
  object: string;
  /** Name of the container. */
  name: string;
  /** Unix timestamp (in seconds) when the container was created. */
  created_at: number;
  /** Status of the container (e.g., active, deleted). */
  status: string;
  /** Unix timestamp (in seconds) when the container was last active. */
  last_active_at?: number;
  /**
   * The container will expire after this time period.
   * The anchor is the reference point for the expiration.
   * The minutes is the number of minutes after the anchor before the container expires.
   */
  expires_after?: ContainerResourceExpiresAfter;
  /** The memory limit configured for the container. */
  memory_limit?: CreateContainerBodyMemoryLimit;
  /** Network access policy for the container. */
  network_policy?: ContainerResourceNetworkPolicy;
}

/**
 * The container will expire after this time period.
 * The anchor is the reference point for the expiration.
 * The minutes is the number of minutes after the anchor before the container expires.
 */
export interface ContainerResourceExpiresAfter {
  /** The reference point for the expiration. */
  anchor?: "last_active_at";
  /** The number of minutes after the anchor before the container expires. */
  minutes?: number;
}

/** Network access policy for the container. */
export interface ContainerResourceNetworkPolicy {
  /** The network policy mode. */
  type: ContainerResourceNetworkPolicyType;
  /** Allowed outbound domains when `type` is `allowlist`. */
  allowed_domains?: string[];
}

/** The network policy mode. */
export type ContainerResourceNetworkPolicyType = "allowlist" | "disabled";

export interface CreateContainerFileBody {
  /** Name of the file to create. */
  file_id?: string;
  /** The File object (not file name) to be uploaded. */
  file?: Uint8Array | Blob;
}

export interface ContainerFileResource {
  /** Unique identifier for the file. */
  id: string;
  /** The type of this object (`container.file`). */
  object: string;
  /** The container this file belongs to. */
  container_id: string;
  /** Unix timestamp (in seconds) when the file was created. */
  created_at: number;
  /** Size of the file in bytes. */
  bytes: number;
  /** Path of the file in the container. */
  path: string;
  /** Source of the file (e.g., `user`, `assistant`). */
  source: string;
}

export type ContainersFilesListOrder = "asc" | "desc";

export interface ContainerFileListResource {
  /** The type of object returned, must be 'list'. */
  object: "list";
  /** A list of container files. */
  data: ContainerFileResource[];
  /** The ID of the first file in the list. */
  first_id: string;
  /** The ID of the last file in the list. */
  last_id: string;
  /** Whether there are more files available. */
  has_more: boolean;
}

export type ContainersListOrder = "asc" | "desc";

export interface ContainerListResource {
  /** The type of object returned, must be 'list'. */
  object: "list";
  /** A list of containers. */
  data: ContainerResource[];
  /** The ID of the first container in the list. */
  first_id: string;
  /** The ID of the last container in the list. */
  last_id: string;
  /** Whether there are more containers available. */
  has_more: boolean;
}

/** The memory limit configured for the container. */
export type ContainerResourceMemoryLimit = "1g" | "4g" | "16g" | "64g";

export interface CreateConversationBody {
  metadata?: Metadata | null;
  items?: InputItem[] | null;
}

export type InputItem = EasyInputMessage | Item | ItemReferenceParam;

/** Decode a `InputItem` by its `type` discriminator (unknown value falls through). */
export function decodeInputItem(data: unknown): InputItem {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "message":
      return data as EasyInputMessage;
    default:
      return data as InputItem;
  }
}

/**
 * A message input to the model with a role indicating instruction following
 * hierarchy. Instructions given with the `developer` or `system` role take
 * precedence over instructions given with the `user` role. Messages with the
 * `assistant` role are presumed to have been generated by the model in previous
 * interactions.
 */
export interface EasyInputMessage {
  /**
   * The role of the message input. One of `user`, `assistant`, `system`, or
   * `developer`.
   */
  role: EasyInputMessageRole;
  /**
   * Text, image, or audio input to the model, used to generate a response.
   * Can also contain previous assistant responses.
   */
  content: EasyInputMessageContent;
  phase?: MessagePhase | null;
  /** The type of the message input. Always `message`. */
  type?: "message";
}

/**
 * The role of the message input. One of `user`, `assistant`, `system`, or
 * `developer`.
 */
export type EasyInputMessageRole = "user" | "assistant" | "system" | "developer";

/**
 * Text, image, or audio input to the model, used to generate a response.
 * Can also contain previous assistant responses.
 */
export type EasyInputMessageContent = string | InputMessageContentList;

export type InputMessageContentList = InputContent[];

export type InputContent = InputTextContent | InputImageContent | InputFileContent;

/** Decode a `InputContent` by its `type` discriminator (unknown value falls through). */
export function decodeInputContent(data: unknown): InputContent {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "input_text":
      return data as InputTextContent;
    case "input_image":
      return data as InputImageContent;
    case "input_file":
      return data as InputFileContent;
    default:
      return data as InputContent;
  }
}

/** A text input to the model. */
export interface InputTextContent {
  /** The type of the input item. Always `input_text`. */
  type: "input_text";
  /** The text input to the model. */
  text: string;
}

/** An image input to the model. Learn about [image inputs](/docs/guides/vision). */
export interface InputImageContent {
  /** The type of the input item. Always `input_image`. */
  type: "input_image";
  image_url?: string | null;
  file_id?: string | null;
  detail: ImageDetail;
}

export type ImageDetail = "low" | "high" | "auto" | "original";

/** A file input to the model. */
export interface InputFileContent {
  /** The type of the input item. Always `input_file`. */
  type: "input_file";
  file_id?: string | null;
  /** The name of the file to be sent to the model. */
  filename?: string;
  /** The content of the file to be sent to the model. */
  file_data?: string;
  /** The URL of the file to be sent to the model. */
  file_url?: string;
  detail?: FileInputDetail;
}

export type FileInputDetail = "low" | "high";

/**
 * Labels an `assistant` message as intermediate commentary (`commentary`) or the final answer (`final_answer`).
 * For models like `gpt-5.3-codex` and beyond, when sending follow-up requests, preserve and resend
 * phase on all assistant messages — dropping it can degrade performance. Not used for user messages.
 */
export type MessagePhase = "commentary" | "final_answer";

/** Content item used to generate a response. */
export type Item = InputMessage | OutputMessage | FileSearchToolCall | ComputerToolCall | ComputerCallOutputItemParam | WebSearchToolCall | FunctionToolCall | FunctionCallOutputItemParam | ToolSearchCallItemParam | ToolSearchOutputItemParam | ReasoningItem | CompactionSummaryItemParam | ImageGenToolCall | CodeInterpreterToolCall | LocalShellToolCall | LocalShellToolCallOutput | FunctionShellCallItemParam | FunctionShellCallOutputItemParam | ApplyPatchToolCallItemParam | ApplyPatchToolCallOutputItemParam | McpListTools | McpApprovalRequest | McpApprovalResponse | McpToolCall | CustomToolCallOutput | CustomToolCall;

/** Decode a `Item` by its `type` discriminator (unknown value falls through). */
export function decodeItem(data: unknown): Item {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "message":
      return data as OutputMessage;
    case "file_search_call":
      return data as FileSearchToolCall;
    case "computer_call":
      return data as ComputerToolCall;
    case "computer_call_output":
      return data as ComputerCallOutputItemParam;
    case "web_search_call":
      return data as WebSearchToolCall;
    case "function_call":
      return data as FunctionToolCall;
    case "function_call_output":
      return data as FunctionCallOutputItemParam;
    case "tool_search_call":
      return data as ToolSearchCallItemParam;
    case "tool_search_output":
      return data as ToolSearchOutputItemParam;
    case "reasoning":
      return data as ReasoningItem;
    case "compaction":
      return data as CompactionSummaryItemParam;
    case "image_generation_call":
      return data as ImageGenToolCall;
    case "code_interpreter_call":
      return data as CodeInterpreterToolCall;
    case "local_shell_call":
      return data as LocalShellToolCall;
    case "local_shell_call_output":
      return data as LocalShellToolCallOutput;
    case "shell_call":
      return data as FunctionShellCallItemParam;
    case "shell_call_output":
      return data as FunctionShellCallOutputItemParam;
    case "apply_patch_call":
      return data as ApplyPatchToolCallItemParam;
    case "apply_patch_call_output":
      return data as ApplyPatchToolCallOutputItemParam;
    case "mcp_list_tools":
      return data as McpListTools;
    case "mcp_approval_request":
      return data as McpApprovalRequest;
    case "mcp_approval_response":
      return data as McpApprovalResponse;
    case "mcp_call":
      return data as McpToolCall;
    case "custom_tool_call_output":
      return data as CustomToolCallOutput;
    case "custom_tool_call":
      return data as CustomToolCall;
    default:
      return data as Item;
  }
}

/**
 * A message input to the model with a role indicating instruction following
 * hierarchy. Instructions given with the `developer` or `system` role take
 * precedence over instructions given with the `user` role.
 */
export interface InputMessage {
  /** The type of the message input. Always set to `message`. */
  type?: "message";
  /** The role of the message input. One of `user`, `system`, or `developer`. */
  role: InputMessageRole;
  /**
   * The status of item. One of `in_progress`, `completed`, or
   * `incomplete`. Populated when items are returned via API.
   */
  status?: InputMessageStatus;
  content: InputMessageContentList;
}

/** The role of the message input. One of `user`, `system`, or `developer`. */
export type InputMessageRole = "user" | "system" | "developer";

/**
 * The status of item. One of `in_progress`, `completed`, or
 * `incomplete`. Populated when items are returned via API.
 */
export type InputMessageStatus = "in_progress" | "completed" | "incomplete";

/** An output message from the model. */
export interface OutputMessage {
  /** The unique ID of the output message. */
  id: string;
  /** The type of the output message. Always `message`. */
  type: "message";
  /** The role of the output message. Always `assistant`. */
  role: "assistant";
  /** The content of the output message. */
  content: OutputMessageContent[];
  phase?: MessagePhase | null;
  /**
   * The status of the message input. One of `in_progress`, `completed`, or
   * `incomplete`. Populated when input items are returned via API.
   */
  status: InputMessageStatus;
}

export type OutputMessageContent = OutputTextContent | RefusalContent;

/** Decode a `OutputMessageContent` by its `type` discriminator (unknown value falls through). */
export function decodeOutputMessageContent(data: unknown): OutputMessageContent {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "output_text":
      return data as OutputTextContent;
    case "refusal":
      return data as RefusalContent;
    default:
      return data as OutputMessageContent;
  }
}

/** A text output from the model. */
export interface OutputTextContent {
  /** The type of the output text. Always `output_text`. */
  type: "output_text";
  /** The text output from the model. */
  text: string;
  /** The annotations of the text output. */
  annotations: Annotation[];
  logprobs: LogProb[];
}

/** An annotation that applies to a span of output text. */
export type Annotation = FileCitationBody | UrlCitationBody | ContainerFileCitationBody | FilePath;

/** Decode a `Annotation` by its `type` discriminator (unknown value falls through). */
export function decodeAnnotation(data: unknown): Annotation {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "file_citation":
      return data as FileCitationBody;
    case "url_citation":
      return data as UrlCitationBody;
    case "container_file_citation":
      return data as ContainerFileCitationBody;
    case "file_path":
      return data as FilePath;
    default:
      return data as Annotation;
  }
}

/** A citation to a file. */
export interface FileCitationBody {
  /** The type of the file citation. Always `file_citation`. */
  type: "file_citation";
  /** The ID of the file. */
  file_id: string;
  /** The index of the file in the list of files. */
  index: number;
  /** The filename of the file cited. */
  filename: string;
}

/** A citation for a web resource used to generate a model response. */
export interface UrlCitationBody {
  /** The type of the URL citation. Always `url_citation`. */
  type: "url_citation";
  /** The URL of the web resource. */
  url: string;
  /** The index of the first character of the URL citation in the message. */
  start_index: number;
  /** The index of the last character of the URL citation in the message. */
  end_index: number;
  /** The title of the web resource. */
  title: string;
}

/** A citation for a container file used to generate a model response. */
export interface ContainerFileCitationBody {
  /** The type of the container file citation. Always `container_file_citation`. */
  type: "container_file_citation";
  /** The ID of the container file. */
  container_id: string;
  /** The ID of the file. */
  file_id: string;
  /** The index of the first character of the container file citation in the message. */
  start_index: number;
  /** The index of the last character of the container file citation in the message. */
  end_index: number;
  /** The filename of the container file cited. */
  filename: string;
}

/** A path to a file. */
export interface FilePath {
  /** The type of the file path. Always `file_path`. */
  type: "file_path";
  /** The ID of the file. */
  file_id: string;
  /** The index of the file in the list of files. */
  index: number;
}

/** The log probability of a token. */
export interface LogProb {
  token: string;
  logprob: number;
  bytes: number[];
  top_logprobs: TopLogProb[];
}

/** The top log probability of a token. */
export interface TopLogProb {
  token: string;
  logprob: number;
  bytes: number[];
}

/** A refusal from the model. */
export interface RefusalContent {
  /** The type of the refusal. Always `refusal`. */
  type: "refusal";
  /** The refusal explanation from the model. */
  refusal: string;
}

/**
 * The results of a file search tool call. See the
 * [file search guide](/docs/guides/tools-file-search) for more information.
 */
export interface FileSearchToolCall {
  /** The unique ID of the file search tool call. */
  id: string;
  /** The type of the file search tool call. Always `file_search_call`. */
  type: "file_search_call";
  /**
   * The status of the file search tool call. One of `in_progress`,
   * `searching`, `incomplete` or `failed`,
   */
  status: FileSearchToolCallStatus;
  /** The queries used to search for files. */
  queries: string[];
  results?: FileSearchToolCallResultsItem[] | null;
}

/**
 * The status of the file search tool call. One of `in_progress`,
 * `searching`, `incomplete` or `failed`,
 */
export type FileSearchToolCallStatus = "in_progress" | "searching" | "completed" | "incomplete" | "failed";

export interface FileSearchToolCallResultsItem {
  /** The unique ID of the file. */
  file_id?: string;
  /** The text that was retrieved from the file. */
  text?: string;
  /** The name of the file. */
  filename?: string;
  attributes?: VectorStoreFileAttributes;
  /** The relevance score of the file - a value between 0 and 1. */
  score?: number;
}

export type VectorStoreFileAttributes = Record<string, VectorStoreFileAttributesValue> | null;

export type VectorStoreFileAttributesValue = string | number | boolean;

/**
 * A tool call to a computer use tool. See the
 * [computer use guide](/docs/guides/tools-computer-use) for more information.
 */
export interface ComputerToolCall {
  /** The type of the computer call. Always `computer_call`. */
  type: "computer_call";
  /** The unique ID of the computer call. */
  id: string;
  /** An identifier used when responding to the tool call with output. */
  call_id: string;
  action?: ComputerAction;
  actions?: ComputerActionList;
  /** The pending safety checks for the computer call. */
  pending_safety_checks: ComputerCallSafetyCheckParam[];
  /**
   * The status of the item. One of `in_progress`, `completed`, or
   * `incomplete`. Populated when items are returned via API.
   */
  status: InputMessageStatus;
}

export type ComputerAction = ClickParam | DoubleClickAction | DragParam | KeyPressAction | MoveParam | ScreenshotParam | ScrollParam | TypeParam | WaitParam;

/** Decode a `ComputerAction` by its `type` discriminator (unknown value falls through). */
export function decodeComputerAction(data: unknown): ComputerAction {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "click":
      return data as ClickParam;
    case "double_click":
      return data as DoubleClickAction;
    case "drag":
      return data as DragParam;
    case "keypress":
      return data as KeyPressAction;
    case "move":
      return data as MoveParam;
    case "screenshot":
      return data as ScreenshotParam;
    case "scroll":
      return data as ScrollParam;
    case "type":
      return data as TypeParam;
    case "wait":
      return data as WaitParam;
    default:
      return data as ComputerAction;
  }
}

/** A click action. */
export interface ClickParam {
  /** Specifies the event type. For a click action, this property is always `click`. */
  type: "click";
  button: ClickButtonType;
  /** The x-coordinate where the click occurred. */
  x: number;
  /** The y-coordinate where the click occurred. */
  y: number;
  keys?: string[] | null;
}

export type ClickButtonType = "left" | "right" | "wheel" | "back" | "forward";

/** A double click action. */
export interface DoubleClickAction {
  /** Specifies the event type. For a double click action, this property is always set to `double_click`. */
  type: "double_click";
  /** The x-coordinate where the double click occurred. */
  x: number;
  /** The y-coordinate where the double click occurred. */
  y: number;
  keys: string[] | null;
}

/** A drag action. */
export interface DragParam {
  /** Specifies the event type. For a drag action, this property is always set to `drag`. */
  type: "drag";
  /**
   * An array of coordinates representing the path of the drag action. Coordinates will appear as an array of objects, eg
   * ```
   * [
   * { x: 100, y: 200 },
   * { x: 200, y: 300 }
   * ]
   * ```
   */
  path: CoordParam[];
  keys?: string[] | null;
}

/** An x/y coordinate pair, e.g. `{ x: 100, y: 200 }`. */
export interface CoordParam {
  /** The x-coordinate. */
  x: number;
  /** The y-coordinate. */
  y: number;
}

/** A collection of keypresses the model would like to perform. */
export interface KeyPressAction {
  /** Specifies the event type. For a keypress action, this property is always set to `keypress`. */
  type: "keypress";
  /** The combination of keys the model is requesting to be pressed. This is an array of strings, each representing a key. */
  keys: string[];
}

/** A mouse move action. */
export interface MoveParam {
  /** Specifies the event type. For a move action, this property is always set to `move`. */
  type: "move";
  /** The x-coordinate to move to. */
  x: number;
  /** The y-coordinate to move to. */
  y: number;
  keys?: string[] | null;
}

/** A screenshot action. */
export interface ScreenshotParam {
  /** Specifies the event type. For a screenshot action, this property is always set to `screenshot`. */
  type: "screenshot";
}

/** A scroll action. */
export interface ScrollParam {
  /** Specifies the event type. For a scroll action, this property is always set to `scroll`. */
  type: "scroll";
  /** The x-coordinate where the scroll occurred. */
  x: number;
  /** The y-coordinate where the scroll occurred. */
  y: number;
  /** The horizontal scroll distance. */
  scroll_x: number;
  /** The vertical scroll distance. */
  scroll_y: number;
  keys?: string[] | null;
}

/** An action to type in text. */
export interface TypeParam {
  /** Specifies the event type. For a type action, this property is always set to `type`. */
  type: "type";
  /** The text to type. */
  text: string;
}

/** A wait action. */
export interface WaitParam {
  /** Specifies the event type. For a wait action, this property is always set to `wait`. */
  type: "wait";
}

export type ComputerActionList = ComputerAction[];

/** A pending safety check for the computer call. */
export interface ComputerCallSafetyCheckParam {
  /** The ID of the pending safety check. */
  id: string;
  code?: string | null;
  message?: string | null;
}

/** The output of a computer tool call. */
export interface ComputerCallOutputItemParam {
  id?: string | null;
  /** The ID of the computer tool call that produced the output. */
  call_id: string;
  /** The type of the computer tool call output. Always `computer_call_output`. */
  type: "computer_call_output";
  output: ComputerScreenshotImage;
  acknowledged_safety_checks?: ComputerCallSafetyCheckParam[] | null;
  status?: FunctionCallItemStatus | null;
}

/** A computer screenshot image used with the computer use tool. */
export interface ComputerScreenshotImage {
  /**
   * Specifies the event type. For a computer screenshot, this property is
   * always set to `computer_screenshot`.
   */
  type: "computer_screenshot";
  /** The URL of the screenshot image. */
  image_url?: string;
  /** The identifier of an uploaded file that contains the screenshot. */
  file_id?: string;
}

export type FunctionCallItemStatus = "in_progress" | "completed" | "incomplete";

/**
 * The results of a web search tool call. See the
 * [web search guide](/docs/guides/tools-web-search) for more information.
 */
export interface WebSearchToolCall {
  /** The unique ID of the web search tool call. */
  id: string;
  /** The type of the web search tool call. Always `web_search_call`. */
  type: "web_search_call";
  /** The status of the web search tool call. */
  status: WebSearchToolCallStatus;
  /**
   * An object describing the specific action taken in this web search call.
   * Includes details on how the model used the web (search, open_page, find_in_page).
   */
  action: WebSearchToolCallAction;
}

/** The status of the web search tool call. */
export type WebSearchToolCallStatus = "in_progress" | "searching" | "completed" | "failed";

/**
 * An object describing the specific action taken in this web search call.
 * Includes details on how the model used the web (search, open_page, find_in_page).
 */
export type WebSearchToolCallAction = WebSearchActionSearch | WebSearchActionOpenPage | WebSearchActionFind;

/** Decode a `WebSearchToolCallAction` by its `type` discriminator (unknown value falls through). */
export function decodeWebSearchToolCallAction(data: unknown): WebSearchToolCallAction {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "search":
      return data as WebSearchActionSearch;
    case "open_page":
      return data as WebSearchActionOpenPage;
    case "find_in_page":
      return data as WebSearchActionFind;
    default:
      return data as WebSearchToolCallAction;
  }
}

/** Action type "search" - Performs a web search query. */
export interface WebSearchActionSearch {
  /** The action type. */
  type: "search";
  /** [DEPRECATED] The search query. */
  query: string;
  /** The search queries. */
  queries?: string[];
  /** The sources used in the search. */
  sources?: WebSearchActionSearchSourcesItem[];
}

/** A source used in the search. */
export interface WebSearchActionSearchSourcesItem {
  /** The type of source. Always `url`. */
  type: "url";
  /** The URL of the source. */
  url: string;
}

/** Action type "open_page" - Opens a specific URL from search results. */
export interface WebSearchActionOpenPage {
  /** The action type. */
  type: "open_page";
  /** The URL opened by the model. */
  url?: string | null;
}

/** Action type "find_in_page": Searches for a pattern within a loaded page. */
export interface WebSearchActionFind {
  /** The action type. */
  type: "find_in_page";
  /** The URL of the page searched for the pattern. */
  url: string;
  /** The pattern or text to search for within the page. */
  pattern: string;
}

/**
 * A tool call to run a function. See the
 * [function calling guide](/docs/guides/function-calling) for more information.
 */
export interface FunctionToolCall {
  /** The unique ID of the function tool call. */
  id?: string;
  /** The type of the function tool call. Always `function_call`. */
  type: "function_call";
  /** The unique ID of the function tool call generated by the model. */
  call_id: string;
  /** The namespace of the function to run. */
  namespace?: string;
  /** The name of the function to run. */
  name: string;
  /** A JSON string of the arguments to pass to the function. */
  arguments: string;
  /**
   * The status of the item. One of `in_progress`, `completed`, or
   * `incomplete`. Populated when items are returned via API.
   */
  status?: InputMessageStatus;
}

/** The output of a function tool call. */
export interface FunctionCallOutputItemParam {
  id?: string | null;
  /** The unique ID of the function tool call generated by the model. */
  call_id: string;
  /** The type of the function tool call output. Always `function_call_output`. */
  type: "function_call_output";
  /** Text, image, or file output of the function tool call. */
  output: FunctionCallOutputItemParamOutput;
  status?: FunctionCallItemStatus | null;
}

/** Text, image, or file output of the function tool call. */
export type FunctionCallOutputItemParamOutput = string | FunctionCallOutputItemParamOutputArrayItem[];

/** A piece of message content, such as text, an image, or a file. */
export type FunctionCallOutputItemParamOutputArrayItem = InputTextContentParam | InputImageContentParamAutoParam | InputFileContentParam;

/** Decode a `FunctionCallOutputItemParamOutputArrayItem` by its `type` discriminator (unknown value falls through). */
export function decodeFunctionCallOutputItemParamOutputArrayItem(data: unknown): FunctionCallOutputItemParamOutputArrayItem {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "input_text":
      return data as InputTextContentParam;
    case "input_image":
      return data as InputImageContentParamAutoParam;
    case "input_file":
      return data as InputFileContentParam;
    default:
      return data as FunctionCallOutputItemParamOutputArrayItem;
  }
}

/** A text input to the model. */
export interface InputTextContentParam {
  /** The type of the input item. Always `input_text`. */
  type: "input_text";
  /** The text input to the model. */
  text: string;
}

/** An image input to the model. Learn about [image inputs](/docs/guides/vision) */
export interface InputImageContentParamAutoParam {
  /** The type of the input item. Always `input_image`. */
  type: "input_image";
  image_url?: string | null;
  file_id?: string | null;
  detail?: DetailEnum | null;
}

export type DetailEnum = "low" | "high" | "auto" | "original";

/** A file input to the model. */
export interface InputFileContentParam {
  /** The type of the input item. Always `input_file`. */
  type: "input_file";
  file_id?: string | null;
  filename?: string | null;
  file_data?: string | null;
  file_url?: string | null;
  detail?: FileDetailEnum;
}

export type FileDetailEnum = "low" | "high";

export interface ToolSearchCallItemParam {
  id?: string | null;
  call_id?: string | null;
  /** The item type. Always `tool_search_call`. */
  type: "tool_search_call";
  execution?: ToolSearchExecutionType;
  arguments: EmptyModelParam;
  status?: FunctionCallItemStatus | null;
}

export type ToolSearchExecutionType = "server" | "client";

export type EmptyModelParam = Record<string, unknown>;

export interface ToolSearchOutputItemParam {
  id?: string | null;
  call_id?: string | null;
  /** The item type. Always `tool_search_output`. */
  type: "tool_search_output";
  execution?: ToolSearchExecutionType;
  /** The loaded tool definitions returned by the tool search output. */
  tools: Tool[];
  status?: FunctionCallItemStatus | null;
}

/** A tool that can be used to generate a response. */
export type Tool = FunctionTool | FileSearchTool | ComputerTool | ComputerUsePreviewTool | WebSearchTool | McpTool | CodeInterpreterTool | ImageGenTool | LocalShellToolParam | FunctionShellToolParam | CustomToolParam | NamespaceToolParam | ToolSearchToolParam | WebSearchPreviewTool | ApplyPatchToolParam;

/** Decode a `Tool` by its `type` discriminator (unknown value falls through). */
export function decodeTool(data: unknown): Tool {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "function":
      return data as FunctionTool;
    case "file_search":
      return data as FileSearchTool;
    case "computer":
      return data as ComputerTool;
    case "computer_use_preview":
      return data as ComputerUsePreviewTool;
    case "mcp":
      return data as McpTool;
    case "code_interpreter":
      return data as CodeInterpreterTool;
    case "image_generation":
      return data as ImageGenTool;
    case "local_shell":
      return data as LocalShellToolParam;
    case "shell":
      return data as FunctionShellToolParam;
    case "custom":
      return data as CustomToolParam;
    case "namespace":
      return data as NamespaceToolParam;
    case "tool_search":
      return data as ToolSearchToolParam;
    case "apply_patch":
      return data as ApplyPatchToolParam;
    default:
      return data as Tool;
  }
}

/** Defines a function in your own code the model can choose to call. Learn more about [function calling](https://platform.openai.com/docs/guides/function-calling). */
export interface FunctionTool {
  /** The type of the function tool. Always `function`. */
  type: "function";
  /** The name of the function to call. */
  name: string;
  description?: string | null;
  parameters: Record<string, unknown> | null;
  strict: boolean | null;
  /** Whether this function is deferred and loaded via tool search. */
  defer_loading?: boolean;
}

/** A tool that searches for relevant content from uploaded files. Learn more about the [file search tool](https://platform.openai.com/docs/guides/tools-file-search). */
export interface FileSearchTool {
  /** The type of the file search tool. Always `file_search`. */
  type: "file_search";
  /** The IDs of the vector stores to search. */
  vector_store_ids: string[];
  /** The maximum number of results to return. This number should be between 1 and 50 inclusive. */
  max_num_results?: number;
  ranking_options?: RankingOptions;
  filters?: Filters | null;
}

export interface RankingOptions {
  ranker?: RankerVersionType;
  /** The score threshold for the file search, a number between 0 and 1. Numbers closer to 1 will attempt to return only the most relevant results, but may return fewer results. */
  score_threshold?: number;
  hybrid_search?: HybridSearchOptions;
}

export type RankerVersionType = "auto" | "default-2024-11-15";

export interface HybridSearchOptions {
  /** The weight of the embedding in the reciprocal ranking fusion. */
  embedding_weight: number;
  /** The weight of the text in the reciprocal ranking fusion. */
  text_weight: number;
}

export type Filters = ComparisonFilter | CompoundFilter;

/** A filter used to compare a specified attribute key to a given value using a defined comparison operation. */
export interface ComparisonFilter {
  /**
   * Specifies the comparison operator: `eq`, `ne`, `gt`, `gte`, `lt`, `lte`, `in`, `nin`.
   * - `eq`: equals
   * - `ne`: not equal
   * - `gt`: greater than
   * - `gte`: greater than or equal
   * - `lt`: less than
   * - `lte`: less than or equal
   * - `in`: in
   * - `nin`: not in
   */
  type: ComparisonFilterType;
  /** The key to compare against the value. */
  key: string;
  /** The value to compare against the attribute key; supports string, number, or boolean types. */
  value: ComparisonFilterValue;
}

/**
 * Specifies the comparison operator: `eq`, `ne`, `gt`, `gte`, `lt`, `lte`, `in`, `nin`.
 * - `eq`: equals
 * - `ne`: not equal
 * - `gt`: greater than
 * - `gte`: greater than or equal
 * - `lt`: less than
 * - `lte`: less than or equal
 * - `in`: in
 * - `nin`: not in
 */
export type ComparisonFilterType = "eq" | "ne" | "gt" | "gte" | "lt" | "lte" | "in" | "nin";

/** The value to compare against the attribute key; supports string, number, or boolean types. */
export type ComparisonFilterValue = string | number | boolean | ComparisonFilterValueArrayItem[];

export type ComparisonFilterValueArrayItem = string | number;

/** Combine multiple filters using `and` or `or`. */
export interface CompoundFilter {
  /** Type of operation: `and` or `or`. */
  type: CompoundFilterType;
  /** Array of filters to combine. Items can be `ComparisonFilter` or `CompoundFilter`. */
  filters: CompoundFilterFiltersItem[];
}

/** Type of operation: `and` or `or`. */
export type CompoundFilterType = "and" | "or";

export type CompoundFilterFiltersItem = ComparisonFilter | unknown;

/** A tool that controls a virtual computer. Learn more about the [computer tool](https://platform.openai.com/docs/guides/tools-computer-use). */
export interface ComputerTool {
  /** The type of the computer tool. Always `computer`. */
  type: "computer";
}

/** A tool that controls a virtual computer. Learn more about the [computer tool](https://platform.openai.com/docs/guides/tools-computer-use). */
export interface ComputerUsePreviewTool {
  /** The type of the computer use tool. Always `computer_use_preview`. */
  type: "computer_use_preview";
  environment: ComputerEnvironment;
  /** The width of the computer display. */
  display_width: number;
  /** The height of the computer display. */
  display_height: number;
}

export type ComputerEnvironment = "windows" | "mac" | "linux" | "ubuntu" | "browser";

/**
 * Search the Internet for sources related to the prompt. Learn more about the
 * [web search tool](/docs/guides/tools-web-search).
 */
export interface WebSearchTool {
  /** The type of the web search tool. One of `web_search` or `web_search_2025_08_26`. */
  type: WebSearchToolType;
  filters?: WebSearchToolFilters | null;
  user_location?: WebSearchApproximateLocation;
  /** High level guidance for the amount of context window space to use for the search. One of `low`, `medium`, or `high`. `medium` is the default. */
  search_context_size?: WebSearchToolSearchContextSize;
}

/** The type of the web search tool. One of `web_search` or `web_search_2025_08_26`. */
export type WebSearchToolType = "web_search" | "web_search_2025_08_26";

/** Filters for the search. */
export interface WebSearchToolFilters {
  allowed_domains?: string[] | null;
}

/** The approximate location of the user. */
export interface WebSearchApproximateLocation {
  /** The type of location approximation. Always `approximate`. */
  type?: "approximate";
  country?: string | null;
  region?: string | null;
  city?: string | null;
  timezone?: string | null;
}

/** High level guidance for the amount of context window space to use for the search. One of `low`, `medium`, or `high`. `medium` is the default. */
export type WebSearchToolSearchContextSize = "low" | "medium" | "high";

/**
 * Give the model access to additional tools via remote Model Context Protocol
 * (MCP) servers. [Learn more about MCP](/docs/guides/tools-remote-mcp).
 */
export interface McpTool {
  /** The type of the MCP tool. Always `mcp`. */
  type: "mcp";
  /** A label for this MCP server, used to identify it in tool calls. */
  server_label: string;
  /**
   * The URL for the MCP server. One of `server_url` or `connector_id` must be
   * provided.
   */
  server_url?: string;
  /**
   * Identifier for service connectors, like those available in ChatGPT. One of
   * `server_url` or `connector_id` must be provided. Learn more about service
   * connectors [here](/docs/guides/tools-remote-mcp#connectors).
   *
   * Currently supported `connector_id` values are:
   *
   * - Dropbox: `connector_dropbox`
   * - Gmail: `connector_gmail`
   * - Google Calendar: `connector_googlecalendar`
   * - Google Drive: `connector_googledrive`
   * - Microsoft Teams: `connector_microsoftteams`
   * - Outlook Calendar: `connector_outlookcalendar`
   * - Outlook Email: `connector_outlookemail`
   * - SharePoint: `connector_sharepoint`
   */
  connector_id?: McpToolConnectorId;
  /**
   * An OAuth access token that can be used with a remote MCP server, either
   * with a custom MCP server URL or a service connector. Your application
   * must handle the OAuth authorization flow and provide the token here.
   */
  authorization?: string;
  /** Optional description of the MCP server, used to provide more context. */
  server_description?: string;
  headers?: Record<string, string> | null;
  allowed_tools?: McpToolAllowedTools | null;
  require_approval?: McpToolRequireApproval | null;
  /** Whether this MCP tool is deferred and discovered via tool search. */
  defer_loading?: boolean;
}

/**
 * Identifier for service connectors, like those available in ChatGPT. One of
 * `server_url` or `connector_id` must be provided. Learn more about service
 * connectors [here](/docs/guides/tools-remote-mcp#connectors).
 *
 * Currently supported `connector_id` values are:
 *
 * - Dropbox: `connector_dropbox`
 * - Gmail: `connector_gmail`
 * - Google Calendar: `connector_googlecalendar`
 * - Google Drive: `connector_googledrive`
 * - Microsoft Teams: `connector_microsoftteams`
 * - Outlook Calendar: `connector_outlookcalendar`
 * - Outlook Email: `connector_outlookemail`
 * - SharePoint: `connector_sharepoint`
 */
export type McpToolConnectorId = "connector_dropbox" | "connector_gmail" | "connector_googlecalendar" | "connector_googledrive" | "connector_microsoftteams" | "connector_outlookcalendar" | "connector_outlookemail" | "connector_sharepoint";

/** List of allowed tool names or a filter object. */
export type McpToolAllowedTools = string[] | McpToolFilter;

/** A filter object to specify which tools are allowed. */
export interface McpToolFilter {
  /** List of allowed tool names. */
  tool_names?: string[];
  /**
   * Indicates whether or not a tool modifies data or is read-only. If an
   * MCP server is [annotated with `readOnlyHint`](https://modelcontextprotocol.io/specification/2025-06-18/schema#toolannotations-readonlyhint),
   * it will match this filter.
   */
  read_only?: boolean;
}

/** Specify which of the MCP server's tools require approval. */
export type McpToolRequireApproval = McpToolRequireApprovalMcpToolApprovalFilter | McpToolRequireApprovalMcpToolApprovalSetting;

/**
 * Specify which of the MCP server's tools require approval. Can be
 * `always`, `never`, or a filter object associated with tools
 * that require approval.
 */
export interface McpToolRequireApprovalMcpToolApprovalFilter {
  always?: McpToolFilter;
  never?: McpToolFilter;
}

/**
 * Specify a single approval policy for all tools. One of `always` or
 * `never`. When set to `always`, all tools will require approval. When
 * set to `never`, all tools will not require approval.
 */
export type McpToolRequireApprovalMcpToolApprovalSetting = "always" | "never";

/** A tool that runs Python code to help generate a response to a prompt. */
export interface CodeInterpreterTool {
  /** The type of the code interpreter tool. Always `code_interpreter`. */
  type: "code_interpreter";
  /**
   * The code interpreter container. Can be a container ID or an object that
   * specifies uploaded file IDs to make available to your code, along with an
   * optional `memory_limit` setting.
   */
  container: CodeInterpreterToolContainer;
}

/**
 * The code interpreter container. Can be a container ID or an object that
 * specifies uploaded file IDs to make available to your code, along with an
 * optional `memory_limit` setting.
 */
export type CodeInterpreterToolContainer = string | AutoCodeInterpreterToolParam;

/** Configuration for a code interpreter container. Optionally specify the IDs of the files to run the code on. */
export interface AutoCodeInterpreterToolParam {
  /** Always `auto`. */
  type: "auto";
  /** An optional list of uploaded files to make available to your code. */
  file_ids?: string[];
  memory_limit?: ContainerMemoryLimit | null;
  /** Network access policy for the container. */
  network_policy?: AutoCodeInterpreterToolParamNetworkPolicy;
}

export type ContainerMemoryLimit = "1g" | "4g" | "16g" | "64g";

/** Network access policy for the container. */
export type AutoCodeInterpreterToolParamNetworkPolicy = ContainerNetworkPolicyDisabledParam | ContainerNetworkPolicyAllowlistParam;

/** Decode a `AutoCodeInterpreterToolParamNetworkPolicy` by its `type` discriminator (unknown value falls through). */
export function decodeAutoCodeInterpreterToolParamNetworkPolicy(data: unknown): AutoCodeInterpreterToolParamNetworkPolicy {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "disabled":
      return data as ContainerNetworkPolicyDisabledParam;
    case "allowlist":
      return data as ContainerNetworkPolicyAllowlistParam;
    default:
      return data as AutoCodeInterpreterToolParamNetworkPolicy;
  }
}

/** A tool that generates images using the GPT image models. */
export interface ImageGenTool {
  /** The type of the image generation tool. Always `image_generation`. */
  type: "image_generation";
  model?: ImageGenToolModel;
  /**
   * The quality of the generated image. One of `low`, `medium`, `high`,
   * or `auto`. Default: `auto`.
   */
  quality?: ImageGenToolQuality;
  /** The size of the generated images. For `gpt-image-2` and `gpt-image-2-2026-04-21`, arbitrary resolutions are supported as `WIDTHxHEIGHT` strings, for example `1536x864`. Width and height must both be divisible by 16 and the requested aspect ratio must be between 1:3 and 3:1. Resolutions above `2560x1440` are experimental, and the maximum supported resolution is `3840x2160`. The requested size must also satisfy the model's current pixel and edge limits. The standard sizes `1024x1024`, `1536x1024`, and `1024x1536` are supported by the GPT image models; `auto` is supported for models that allow automatic sizing. For `dall-e-2`, use one of `256x256`, `512x512`, or `1024x1024`. For `dall-e-3`, use one of `1024x1024`, `1792x1024`, or `1024x1792`. */
  size?: ImageGenToolSize;
  /**
   * The output format of the generated image. One of `png`, `webp`, or
   * `jpeg`. Default: `png`.
   */
  output_format?: ImageGenToolOutputFormat;
  /** Compression level for the output image. Default: 100. */
  output_compression?: number;
  /** Moderation level for the generated image. Default: `auto`. */
  moderation?: ImageGenToolModeration;
  /**
   * Background type for the generated image. One of `transparent`,
   * `opaque`, or `auto`. Default: `auto`.
   */
  background?: ImageGenToolBackground;
  input_fidelity?: InputFidelity | null;
  /**
   * Optional mask for inpainting. Contains `image_url`
   * (string, optional) and `file_id` (string, optional).
   */
  input_image_mask?: ImageGenToolInputImageMask;
  /** Number of partial images to generate in streaming mode, from 0 (default value) to 3. */
  partial_images?: number;
  action?: ImageGenActionEnum;
}

export type ImageGenToolModel = string | ImageGenToolModelString;

/** The image generation model to use. Default: `gpt-image-1`. */
export type ImageGenToolModelString = "gpt-image-1" | "gpt-image-1-mini" | "gpt-image-1.5";

/**
 * The quality of the generated image. One of `low`, `medium`, `high`,
 * or `auto`. Default: `auto`.
 */
export type ImageGenToolQuality = "low" | "medium" | "high" | "auto";

/** The size of the generated images. For `gpt-image-2` and `gpt-image-2-2026-04-21`, arbitrary resolutions are supported as `WIDTHxHEIGHT` strings, for example `1536x864`. Width and height must both be divisible by 16 and the requested aspect ratio must be between 1:3 and 3:1. Resolutions above `2560x1440` are experimental, and the maximum supported resolution is `3840x2160`. The requested size must also satisfy the model's current pixel and edge limits. The standard sizes `1024x1024`, `1536x1024`, and `1024x1536` are supported by the GPT image models; `auto` is supported for models that allow automatic sizing. For `dall-e-2`, use one of `256x256`, `512x512`, or `1024x1024`. For `dall-e-3`, use one of `1024x1024`, `1792x1024`, or `1024x1792`. */
export type ImageGenToolSize = string | ImageGenToolSizeString;

export type ImageGenToolSizeString = "1024x1024" | "1024x1536" | "1536x1024" | "auto";

/**
 * The output format of the generated image. One of `png`, `webp`, or
 * `jpeg`. Default: `png`.
 */
export type ImageGenToolOutputFormat = "png" | "webp" | "jpeg";

/** Moderation level for the generated image. Default: `auto`. */
export type ImageGenToolModeration = "auto" | "low";

/**
 * Background type for the generated image. One of `transparent`,
 * `opaque`, or `auto`. Default: `auto`.
 */
export type ImageGenToolBackground = "transparent" | "opaque" | "auto";

/** Control how much effort the model will exert to match the style and features, especially facial features, of input images. This parameter is only supported for `gpt-image-1` and `gpt-image-1.5` and later models, unsupported for `gpt-image-1-mini`. Supports `high` and `low`. Defaults to `low`. */
export type InputFidelity = "high" | "low";

/**
 * Optional mask for inpainting. Contains `image_url`
 * (string, optional) and `file_id` (string, optional).
 */
export interface ImageGenToolInputImageMask {
  /** Base64-encoded mask image. */
  image_url?: string;
  /** File ID for the mask image. */
  file_id?: string;
}

export type ImageGenActionEnum = "generate" | "edit" | "auto";

/** A tool that allows the model to execute shell commands in a local environment. */
export interface LocalShellToolParam {
  /** The type of the local shell tool. Always `local_shell`. */
  type: "local_shell";
}

/** A tool that allows the model to execute shell commands. */
export interface FunctionShellToolParam {
  /** The type of the shell tool. Always `shell`. */
  type: "shell";
  environment?: FunctionShellToolParamEnvironment | null;
}

export type FunctionShellToolParamEnvironment = ContainerAutoParam | LocalEnvironmentParam | ContainerReferenceParam;

/** Decode a `FunctionShellToolParamEnvironment` by its `type` discriminator (unknown value falls through). */
export function decodeFunctionShellToolParamEnvironment(data: unknown): FunctionShellToolParamEnvironment {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "container_auto":
      return data as ContainerAutoParam;
    case "local":
      return data as LocalEnvironmentParam;
    case "container_reference":
      return data as ContainerReferenceParam;
    default:
      return data as FunctionShellToolParamEnvironment;
  }
}

export interface ContainerAutoParam {
  /** Automatically creates a container for this request */
  type: "container_auto";
  /** An optional list of uploaded files to make available to your code. */
  file_ids?: string[];
  memory_limit?: ContainerMemoryLimit | null;
  /** Network access policy for the container. */
  network_policy?: AutoCodeInterpreterToolParamNetworkPolicy;
  /** An optional list of skills referenced by id or inline data. */
  skills?: ContainerAutoParamSkillsItem[];
}

export type ContainerAutoParamSkillsItem = SkillReferenceParam | InlineSkillParam;

/** Decode a `ContainerAutoParamSkillsItem` by its `type` discriminator (unknown value falls through). */
export function decodeContainerAutoParamSkillsItem(data: unknown): ContainerAutoParamSkillsItem {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "skill_reference":
      return data as SkillReferenceParam;
    case "inline":
      return data as InlineSkillParam;
    default:
      return data as ContainerAutoParamSkillsItem;
  }
}

export interface LocalEnvironmentParam {
  /** Use a local computer environment. */
  type: "local";
  /** An optional list of skills. */
  skills?: LocalSkillParam[];
}

export interface LocalSkillParam {
  /** The name of the skill. */
  name: string;
  /** The description of the skill. */
  description: string;
  /** The path to the directory containing the skill. */
  path: string;
}

export interface ContainerReferenceParam {
  /** References a container created with the /v1/containers endpoint */
  type: "container_reference";
  /** The ID of the referenced container. */
  container_id: string;
}

/** A custom tool that processes input using a specified format. Learn more about   [custom tools](/docs/guides/function-calling#custom-tools) */
export interface CustomToolParam {
  /** The type of the custom tool. Always `custom`. */
  type: "custom";
  /** The name of the custom tool, used to identify it in tool calls. */
  name: string;
  /** Optional description of the custom tool, used to provide more context. */
  description?: string;
  /** The input format for the custom tool. Default is unconstrained text. */
  format?: CustomToolParamFormat;
  /** Whether this tool should be deferred and discovered via tool search. */
  defer_loading?: boolean;
}

/** The input format for the custom tool. Default is unconstrained text. */
export type CustomToolParamFormat = CustomTextFormatParam | CustomGrammarFormatParam;

/** Decode a `CustomToolParamFormat` by its `type` discriminator (unknown value falls through). */
export function decodeCustomToolParamFormat(data: unknown): CustomToolParamFormat {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "text":
      return data as CustomTextFormatParam;
    case "grammar":
      return data as CustomGrammarFormatParam;
    default:
      return data as CustomToolParamFormat;
  }
}

/** Unconstrained free-form text. */
export interface CustomTextFormatParam {
  /** Unconstrained text format. Always `text`. */
  type: "text";
}

/** A grammar defined by the user. */
export interface CustomGrammarFormatParam {
  /** Grammar format. Always `grammar`. */
  type: "grammar";
  syntax: GrammarSyntax1;
  /** The grammar definition. */
  definition: string;
}

export type GrammarSyntax1 = "lark" | "regex";

/** Groups function/custom tools under a shared namespace. */
export interface NamespaceToolParam {
  /** The type of the tool. Always `namespace`. */
  type: "namespace";
  /** The namespace name used in tool calls (for example, `crm`). */
  name: string;
  /** A description of the namespace shown to the model. */
  description: string;
  /** The function/custom tools available inside this namespace. */
  tools: NamespaceToolParamToolsItem[];
}

/** A function or custom tool that belongs to a namespace. */
export type NamespaceToolParamToolsItem = FunctionToolParam | CustomToolParam;

/** Decode a `NamespaceToolParamToolsItem` by its `type` discriminator (unknown value falls through). */
export function decodeNamespaceToolParamToolsItem(data: unknown): NamespaceToolParamToolsItem {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "function":
      return data as FunctionToolParam;
    case "custom":
      return data as CustomToolParam;
    default:
      return data as NamespaceToolParamToolsItem;
  }
}

export interface FunctionToolParam {
  name: string;
  description?: string | null;
  parameters?: EmptyModelParam | null;
  strict?: boolean | null;
  type: "function";
  /** Whether this function should be deferred and discovered via tool search. */
  defer_loading?: boolean;
}

/** Hosted or BYOT tool search configuration for deferred tools. */
export interface ToolSearchToolParam {
  /** The type of the tool. Always `tool_search`. */
  type: "tool_search";
  execution?: ToolSearchExecutionType;
  description?: string | null;
  parameters?: EmptyModelParam | null;
}

/** This tool searches the web for relevant results to use in a response. Learn more about the [web search tool](https://platform.openai.com/docs/guides/tools-web-search). */
export interface WebSearchPreviewTool {
  /** The type of the web search tool. One of `web_search_preview` or `web_search_preview_2025_03_11`. */
  type: WebSearchPreviewToolType;
  user_location?: ApproximateLocation | null;
  search_context_size?: SearchContextSize;
  search_content_types?: SearchContentType[];
}

/** The type of the web search tool. One of `web_search_preview` or `web_search_preview_2025_03_11`. */
export type WebSearchPreviewToolType = "web_search_preview" | "web_search_preview_2025_03_11";

export interface ApproximateLocation {
  /** The type of location approximation. Always `approximate`. */
  type: "approximate";
  country?: string | null;
  region?: string | null;
  city?: string | null;
  timezone?: string | null;
}

export type SearchContextSize = "low" | "medium" | "high";

export type SearchContentType = "text" | "image";

/** Allows the assistant to create, delete, or update files using unified diffs. */
export interface ApplyPatchToolParam {
  /** The type of the tool. Always `apply_patch`. */
  type: "apply_patch";
}

/**
 * A description of the chain of thought used by a reasoning model while generating
 * a response. Be sure to include these items in your `input` to the Responses API
 * for subsequent turns of a conversation if you are manually
 * [managing context](/docs/guides/conversation-state).
 */
export interface ReasoningItem {
  /** The type of the object. Always `reasoning`. */
  type: "reasoning";
  /** The unique identifier of the reasoning content. */
  id: string;
  encrypted_content?: string | null;
  /** Reasoning summary content. */
  summary: SummaryTextContent[];
  /** Reasoning text content. */
  content?: ReasoningTextContent[];
  /**
   * The status of the item. One of `in_progress`, `completed`, or
   * `incomplete`. Populated when items are returned via API.
   */
  status?: InputMessageStatus;
}

/** A summary text from the model. */
export interface SummaryTextContent {
  /** The type of the object. Always `summary_text`. */
  type: "summary_text";
  /** A summary of the reasoning output from the model so far. */
  text: string;
}

/** Reasoning text from the model. */
export interface ReasoningTextContent {
  /** The type of the reasoning text. Always `reasoning_text`. */
  type: "reasoning_text";
  /** The reasoning text from the model. */
  text: string;
}

/** A compaction item generated by the [`v1/responses/compact` API](/docs/api-reference/responses/compact). */
export interface CompactionSummaryItemParam {
  id?: string | null;
  /** The type of the item. Always `compaction`. */
  type: "compaction";
  /** The encrypted content of the compaction summary. */
  encrypted_content: string;
}

/** An image generation request made by the model. */
export interface ImageGenToolCall {
  /** The type of the image generation call. Always `image_generation_call`. */
  type: "image_generation_call";
  /** The unique ID of the image generation call. */
  id: string;
  /** The status of the image generation call. */
  status: ImageGenToolCallStatus;
  result: string | null;
}

/** The status of the image generation call. */
export type ImageGenToolCallStatus = "in_progress" | "completed" | "generating" | "failed";

/** A tool call to run code. */
export interface CodeInterpreterToolCall {
  /** The type of the code interpreter tool call. Always `code_interpreter_call`. */
  type: "code_interpreter_call";
  /** The unique ID of the code interpreter tool call. */
  id: string;
  /** The status of the code interpreter tool call. Valid values are `in_progress`, `completed`, `incomplete`, `interpreting`, and `failed`. */
  status: CodeInterpreterToolCallStatus;
  /** The ID of the container used to run the code. */
  container_id: string;
  code: string | null;
  outputs: CodeInterpreterToolCallOutputsItem[] | null;
}

/** The status of the code interpreter tool call. Valid values are `in_progress`, `completed`, `incomplete`, `interpreting`, and `failed`. */
export type CodeInterpreterToolCallStatus = "in_progress" | "completed" | "incomplete" | "interpreting" | "failed";

export type CodeInterpreterToolCallOutputsItem = CodeInterpreterOutputLogs | CodeInterpreterOutputImage;

/** Decode a `CodeInterpreterToolCallOutputsItem` by its `type` discriminator (unknown value falls through). */
export function decodeCodeInterpreterToolCallOutputsItem(data: unknown): CodeInterpreterToolCallOutputsItem {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "logs":
      return data as CodeInterpreterOutputLogs;
    case "image":
      return data as CodeInterpreterOutputImage;
    default:
      return data as CodeInterpreterToolCallOutputsItem;
  }
}

/** The logs output from the code interpreter. */
export interface CodeInterpreterOutputLogs {
  /** The type of the output. Always `logs`. */
  type: "logs";
  /** The logs output from the code interpreter. */
  logs: string;
}

/** The image output from the code interpreter. */
export interface CodeInterpreterOutputImage {
  /** The type of the output. Always `image`. */
  type: "image";
  /** The URL of the image output from the code interpreter. */
  url: string;
}

/** A tool call to run a command on the local shell. */
export interface LocalShellToolCall {
  /** The type of the local shell call. Always `local_shell_call`. */
  type: "local_shell_call";
  /** The unique ID of the local shell call. */
  id: string;
  /** The unique ID of the local shell tool call generated by the model. */
  call_id: string;
  action: LocalShellExecAction;
  /** The status of the local shell call. */
  status: InputMessageStatus;
}

/** Execute a shell command on the server. */
export interface LocalShellExecAction {
  /** The type of the local shell action. Always `exec`. */
  type: "exec";
  /** The command to run. */
  command: string[];
  timeout_ms?: number | null;
  working_directory?: string | null;
  /** Environment variables to set for the command. */
  env: Record<string, string>;
  user?: string | null;
}

/** The output of a local shell tool call. */
export interface LocalShellToolCallOutput {
  /** The type of the local shell tool call output. Always `local_shell_call_output`. */
  type: "local_shell_call_output";
  /** The unique ID of the local shell tool call generated by the model. */
  id: string;
  /** A JSON string of the output of the local shell tool call. */
  output: string;
  status?: InputMessageStatus | null;
}

/** A tool representing a request to execute one or more shell commands. */
export interface FunctionShellCallItemParam {
  id?: string | null;
  /** The unique ID of the shell tool call generated by the model. */
  call_id: string;
  /** The type of the item. Always `shell_call`. */
  type: "shell_call";
  action: FunctionShellActionParam;
  status?: FunctionShellCallItemStatus | null;
  environment?: FunctionShellCallItemParamEnvironment | null;
}

/** Commands and limits describing how to run the shell tool call. */
export interface FunctionShellActionParam {
  /** Ordered shell commands for the execution environment to run. */
  commands: string[];
  timeout_ms?: number | null;
  max_output_length?: number | null;
}

/** Status values reported for shell tool calls. */
export type FunctionShellCallItemStatus = "in_progress" | "completed" | "incomplete";

/** The environment to execute the shell commands in. */
export type FunctionShellCallItemParamEnvironment = LocalEnvironmentParam | ContainerReferenceParam;

/** Decode a `FunctionShellCallItemParamEnvironment` by its `type` discriminator (unknown value falls through). */
export function decodeFunctionShellCallItemParamEnvironment(data: unknown): FunctionShellCallItemParamEnvironment {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "local":
      return data as LocalEnvironmentParam;
    case "container_reference":
      return data as ContainerReferenceParam;
    default:
      return data as FunctionShellCallItemParamEnvironment;
  }
}

/** The streamed output items emitted by a shell tool call. */
export interface FunctionShellCallOutputItemParam {
  id?: string | null;
  /** The unique ID of the shell tool call generated by the model. */
  call_id: string;
  /** The type of the item. Always `shell_call_output`. */
  type: "shell_call_output";
  /** Captured chunks of stdout and stderr output, along with their associated outcomes. */
  output: FunctionShellCallOutputContentParam[];
  status?: FunctionShellCallItemStatus | null;
  max_output_length?: number | null;
}

/** Captured stdout and stderr for a portion of a shell tool call output. */
export interface FunctionShellCallOutputContentParam {
  /** Captured stdout output for the shell call. */
  stdout: string;
  /** Captured stderr output for the shell call. */
  stderr: string;
  outcome: FunctionShellCallOutputOutcomeParam;
}

/** The exit or timeout outcome associated with this shell call. */
export type FunctionShellCallOutputOutcomeParam = FunctionShellCallOutputTimeoutOutcomeParam | FunctionShellCallOutputExitOutcomeParam;

/** Decode a `FunctionShellCallOutputOutcomeParam` by its `type` discriminator (unknown value falls through). */
export function decodeFunctionShellCallOutputOutcomeParam(data: unknown): FunctionShellCallOutputOutcomeParam {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "timeout":
      return data as FunctionShellCallOutputTimeoutOutcomeParam;
    case "exit":
      return data as FunctionShellCallOutputExitOutcomeParam;
    default:
      return data as FunctionShellCallOutputOutcomeParam;
  }
}

/** Indicates that the shell call exceeded its configured time limit. */
export interface FunctionShellCallOutputTimeoutOutcomeParam {
  /** The outcome type. Always `timeout`. */
  type: "timeout";
}

/** Indicates that the shell commands finished and returned an exit code. */
export interface FunctionShellCallOutputExitOutcomeParam {
  /** The outcome type. Always `exit`. */
  type: "exit";
  /** The exit code returned by the shell process. */
  exit_code: number;
}

/** A tool call representing a request to create, delete, or update files using diff patches. */
export interface ApplyPatchToolCallItemParam {
  /** The type of the item. Always `apply_patch_call`. */
  type: "apply_patch_call";
  id?: string | null;
  /** The unique ID of the apply patch tool call generated by the model. */
  call_id: string;
  status: ApplyPatchCallStatusParam;
  operation: ApplyPatchOperationParam;
}

/** Status values reported for apply_patch tool calls. */
export type ApplyPatchCallStatusParam = "in_progress" | "completed";

/** One of the create_file, delete_file, or update_file operations supplied to the apply_patch tool. */
export type ApplyPatchOperationParam = ApplyPatchCreateFileOperationParam | ApplyPatchDeleteFileOperationParam | ApplyPatchUpdateFileOperationParam;

/** Decode a `ApplyPatchOperationParam` by its `type` discriminator (unknown value falls through). */
export function decodeApplyPatchOperationParam(data: unknown): ApplyPatchOperationParam {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "create_file":
      return data as ApplyPatchCreateFileOperationParam;
    case "delete_file":
      return data as ApplyPatchDeleteFileOperationParam;
    case "update_file":
      return data as ApplyPatchUpdateFileOperationParam;
    default:
      return data as ApplyPatchOperationParam;
  }
}

/** Instruction for creating a new file via the apply_patch tool. */
export interface ApplyPatchCreateFileOperationParam {
  /** The operation type. Always `create_file`. */
  type: "create_file";
  /** Path of the file to create relative to the workspace root. */
  path: string;
  /** Unified diff content to apply when creating the file. */
  diff: string;
}

/** Instruction for deleting an existing file via the apply_patch tool. */
export interface ApplyPatchDeleteFileOperationParam {
  /** The operation type. Always `delete_file`. */
  type: "delete_file";
  /** Path of the file to delete relative to the workspace root. */
  path: string;
}

/** Instruction for updating an existing file via the apply_patch tool. */
export interface ApplyPatchUpdateFileOperationParam {
  /** The operation type. Always `update_file`. */
  type: "update_file";
  /** Path of the file to update relative to the workspace root. */
  path: string;
  /** Unified diff content to apply to the existing file. */
  diff: string;
}

/** The streamed output emitted by an apply patch tool call. */
export interface ApplyPatchToolCallOutputItemParam {
  /** The type of the item. Always `apply_patch_call_output`. */
  type: "apply_patch_call_output";
  id?: string | null;
  /** The unique ID of the apply patch tool call generated by the model. */
  call_id: string;
  status: ApplyPatchCallOutputStatusParam;
  output?: string | null;
}

/** Outcome values reported for apply_patch tool call outputs. */
export type ApplyPatchCallOutputStatusParam = "completed" | "failed";

/** A list of tools available on an MCP server. */
export interface McpListTools {
  /** The type of the item. Always `mcp_list_tools`. */
  type: "mcp_list_tools";
  /** The unique ID of the list. */
  id: string;
  /** The label of the MCP server. */
  server_label: string;
  /** The tools available on the server. */
  tools: McpListToolsTool[];
  error?: string | null;
}

/** A tool available on an MCP server. */
export interface McpListToolsTool {
  /** The name of the tool. */
  name: string;
  description?: string | null;
  /** The JSON schema describing the tool's input. */
  input_schema: Record<string, unknown>;
  annotations?: Record<string, unknown> | null;
}

/** A request for human approval of a tool invocation. */
export interface McpApprovalRequest {
  /** The type of the item. Always `mcp_approval_request`. */
  type: "mcp_approval_request";
  /** The unique ID of the approval request. */
  id: string;
  /** The label of the MCP server making the request. */
  server_label: string;
  /** The name of the tool to run. */
  name: string;
  /** A JSON string of arguments for the tool. */
  arguments: string;
}

/** A response to an MCP approval request. */
export interface McpApprovalResponse {
  /** The type of the item. Always `mcp_approval_response`. */
  type: "mcp_approval_response";
  id?: string | null;
  /** The ID of the approval request being answered. */
  approval_request_id: string;
  /** Whether the request was approved. */
  approve: boolean;
  reason?: string | null;
}

/** An invocation of a tool on an MCP server. */
export interface McpToolCall {
  /** The type of the item. Always `mcp_call`. */
  type: "mcp_call";
  /** The unique ID of the tool call. */
  id: string;
  /** The label of the MCP server running the tool. */
  server_label: string;
  /** The name of the tool that was run. */
  name: string;
  /** A JSON string of the arguments passed to the tool. */
  arguments: string;
  output?: string | null;
  error?: string | null;
  status?: McpToolCallStatus;
  approval_request_id?: string | null;
}

export type McpToolCallStatus = "in_progress" | "completed" | "incomplete" | "calling" | "failed";

/** The output of a custom tool call from your code, being sent back to the model. */
export interface CustomToolCallOutput {
  /** The type of the custom tool call output. Always `custom_tool_call_output`. */
  type: "custom_tool_call_output";
  /** The unique ID of the custom tool call output in the OpenAI platform. */
  id?: string;
  /** The call ID, used to map this custom tool call output to a custom tool call. */
  call_id: string;
  /**
   * The output from the custom tool call generated by your code.
   * Can be a string or an list of output content.
   */
  output: CustomToolCallOutputOutput;
}

/**
 * The output from the custom tool call generated by your code.
 * Can be a string or an list of output content.
 */
export type CustomToolCallOutputOutput = string | FunctionAndCustomToolCallOutput[];

export type FunctionAndCustomToolCallOutput = InputTextContent | InputImageContent | InputFileContent;

/** Decode a `FunctionAndCustomToolCallOutput` by its `type` discriminator (unknown value falls through). */
export function decodeFunctionAndCustomToolCallOutput(data: unknown): FunctionAndCustomToolCallOutput {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "input_text":
      return data as InputTextContent;
    case "input_image":
      return data as InputImageContent;
    case "input_file":
      return data as InputFileContent;
    default:
      return data as FunctionAndCustomToolCallOutput;
  }
}

/** A call to a custom tool created by the model. */
export interface CustomToolCall {
  /** The type of the custom tool call. Always `custom_tool_call`. */
  type: "custom_tool_call";
  /** The unique ID of the custom tool call in the OpenAI platform. */
  id?: string;
  /** An identifier used to map this custom tool call to a tool call output. */
  call_id: string;
  /** The namespace of the custom tool being called. */
  namespace?: string;
  /** The name of the custom tool being called. */
  name: string;
  /** The input for the custom tool call generated by the model. */
  input: string;
}

/** An internal identifier for an item to reference. */
export interface ItemReferenceParam {
  type?: "item_reference" | null;
  /** The ID of the item to reference. */
  id: string;
}

export interface ConversationResource {
  /** The unique ID of the conversation. */
  id: string;
  /** The object type, which is always `conversation`. */
  object: "conversation";
  /**
   * Set of 16 key-value pairs that can be attached to an object. This can be         useful for storing additional information about the object in a structured         format, and querying for objects via API or the dashboard.
   * Keys are strings with a maximum length of 64 characters. Values are strings         with a maximum length of 512 characters.
   */
  metadata: unknown;
  /** The time at which the conversation was created, measured in seconds since the Unix epoch. */
  created_at: number;
}

export interface DeletedConversationResource {
  object: "conversation.deleted";
  deleted: boolean;
  id: string;
}

/**
 * Specify additional output data to include in the model response. Currently supported values are:
 * - `web_search_call.results`: Include the search results of the web search tool call.
 * - `web_search_call.action.sources`: Include the sources of the web search tool call.
 * - `code_interpreter_call.outputs`: Includes the outputs of python code execution in code interpreter tool call items.
 * - `computer_call_output.output.image_url`: Include image urls from the computer call output.
 * - `file_search_call.results`: Include the search results of the file search tool call.
 * - `message.input_image.image_url`: Include image urls from the input message.
 * - `message.output_text.logprobs`: Include logprobs with assistant messages.
 * - `reasoning.encrypted_content`: Includes an encrypted version of reasoning tokens in reasoning item outputs. This enables reasoning items to be used in multi-turn conversations when using the Responses API statelessly (like when the `store` parameter is set to `false`, or when an organization is enrolled in the zero data retention program).
 */
export type IncludeEnum = "file_search_call.results" | "web_search_call.results" | "web_search_call.action.sources" | "message.input_image.image_url" | "computer_call_output.output.image_url" | "code_interpreter_call.outputs" | "reasoning.encrypted_content" | "message.output_text.logprobs";

export interface ConversationsItemsCreateRequest {
  /** The items to add to the conversation. You may add up to 20 items at a time. */
  items: InputItem[];
}

/** A list of Conversation items. */
export interface ConversationItemList {
  /** The type of object returned, must be `list`. */
  object: "list";
  /** A list of conversation items. */
  data: ConversationItem[];
  /** Whether there are more items available. */
  has_more: boolean;
  /** The ID of the first item in the list. */
  first_id: string;
  /** The ID of the last item in the list. */
  last_id: string;
}

/** A single item within a conversation. The set of possible types are the same as the `output` type of a [Response object](/docs/api-reference/responses/object#responses/object-output). */
export type ConversationItem = Message | FunctionToolCallResource | FunctionToolCallOutputResource | FileSearchToolCall | WebSearchToolCall | ImageGenToolCall | ComputerToolCall | ComputerToolCallOutputResource | ToolSearchCall | ToolSearchOutput | ReasoningItem | CompactionBody | CodeInterpreterToolCall | LocalShellToolCall | LocalShellToolCallOutput | FunctionShellCall | FunctionShellCallOutput | ApplyPatchToolCall | ApplyPatchToolCallOutput | McpListTools | McpApprovalRequest | McpApprovalResponseResource | McpToolCall | CustomToolCall | CustomToolCallOutput;

/** Decode a `ConversationItem` by its `type` discriminator (unknown value falls through). */
export function decodeConversationItem(data: unknown): ConversationItem {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "message":
      return data as Message;
    case "function_call":
      return data as FunctionToolCallResource;
    case "function_call_output":
      return data as FunctionToolCallOutputResource;
    case "file_search_call":
      return data as FileSearchToolCall;
    case "web_search_call":
      return data as WebSearchToolCall;
    case "image_generation_call":
      return data as ImageGenToolCall;
    case "computer_call":
      return data as ComputerToolCall;
    case "computer_call_output":
      return data as ComputerToolCallOutputResource;
    case "tool_search_call":
      return data as ToolSearchCall;
    case "tool_search_output":
      return data as ToolSearchOutput;
    case "reasoning":
      return data as ReasoningItem;
    case "compaction":
      return data as CompactionBody;
    case "code_interpreter_call":
      return data as CodeInterpreterToolCall;
    case "local_shell_call":
      return data as LocalShellToolCall;
    case "local_shell_call_output":
      return data as LocalShellToolCallOutput;
    case "shell_call":
      return data as FunctionShellCall;
    case "shell_call_output":
      return data as FunctionShellCallOutput;
    case "apply_patch_call":
      return data as ApplyPatchToolCall;
    case "apply_patch_call_output":
      return data as ApplyPatchToolCallOutput;
    case "mcp_list_tools":
      return data as McpListTools;
    case "mcp_approval_request":
      return data as McpApprovalRequest;
    case "mcp_approval_response":
      return data as McpApprovalResponseResource;
    case "mcp_call":
      return data as McpToolCall;
    case "custom_tool_call":
      return data as CustomToolCall;
    case "custom_tool_call_output":
      return data as CustomToolCallOutput;
    default:
      return data as ConversationItem;
  }
}

/** A message to or from the model. */
export interface Message {
  /** The type of the message. Always set to `message`. */
  type: "message";
  /** The unique ID of the message. */
  id: string;
  status: MessageStatus;
  role: MessageRole;
  /** The content of the message */
  content: MessageContentItem[];
  phase?: MessagePhase2 | null;
}

export type MessageStatus = "in_progress" | "completed" | "incomplete";

export type MessageRole = "unknown" | "user" | "assistant" | "system" | "critic" | "discriminator" | "developer" | "tool";

/** A content part that makes up an input or output item. */
export type MessageContentItem = InputTextContent | OutputTextContent | TextContent | SummaryTextContent | ReasoningTextContent | RefusalContent | InputImageContent | ComputerScreenshotContent | InputFileContent;

/** Decode a `MessageContentItem` by its `type` discriminator (unknown value falls through). */
export function decodeMessageContentItem(data: unknown): MessageContentItem {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "input_text":
      return data as InputTextContent;
    case "output_text":
      return data as OutputTextContent;
    case "text":
      return data as TextContent;
    case "summary_text":
      return data as SummaryTextContent;
    case "reasoning_text":
      return data as ReasoningTextContent;
    case "refusal":
      return data as RefusalContent;
    case "input_image":
      return data as InputImageContent;
    case "computer_screenshot":
      return data as ComputerScreenshotContent;
    case "input_file":
      return data as InputFileContent;
    default:
      return data as MessageContentItem;
  }
}

/** A text content. */
export interface TextContent {
  type: "text";
  text: string;
}

/** A screenshot of a computer. */
export interface ComputerScreenshotContent {
  /** Specifies the event type. For a computer screenshot, this property is always set to `computer_screenshot`. */
  type: "computer_screenshot";
  image_url: string | null;
  file_id: string | null;
  detail: ImageDetail;
}

export type MessagePhase2 = "commentary" | "final_answer";

export interface FunctionToolCallResource {
  /** The unique ID of the function tool call. */
  id: string;
  /** The type of the function tool call. Always `function_call`. */
  type: "function_call";
  /** The unique ID of the function tool call generated by the model. */
  call_id: string;
  /** The namespace of the function to run. */
  namespace?: string;
  /** The name of the function to run. */
  name: string;
  /** A JSON string of the arguments to pass to the function. */
  arguments: string;
  status: FunctionCallStatus;
  /** The identifier of the actor that created the item. */
  created_by?: string;
}

export type FunctionCallStatus = "in_progress" | "completed" | "incomplete";

export interface FunctionToolCallOutputResource {
  /** The unique ID of the function call tool output. */
  id: string;
  /** The type of the function tool call output. Always `function_call_output`. */
  type: "function_call_output";
  /** The unique ID of the function tool call generated by the model. */
  call_id: string;
  /**
   * The output from the function call generated by your code.
   * Can be a string or an list of output content.
   */
  output: CustomToolCallOutputOutput;
  status: FunctionCallOutputStatusEnum;
  /** The identifier of the actor that created the item. */
  created_by?: string;
}

export type FunctionCallOutputStatusEnum = "in_progress" | "completed" | "incomplete";

export interface ComputerToolCallOutputResource {
  /** The type of the computer tool call output. Always `computer_call_output`. */
  type: "computer_call_output";
  /** The unique ID of the computer call tool output. */
  id: string;
  /** The ID of the computer tool call that produced the output. */
  call_id: string;
  /**
   * The safety checks reported by the API that have been acknowledged by the
   * developer.
   */
  acknowledged_safety_checks?: ComputerCallSafetyCheckParam[];
  output: ComputerScreenshotImage;
  status: ComputerCallOutputStatus;
  /** The identifier of the actor that created the item. */
  created_by?: string;
}

export type ComputerCallOutputStatus = "completed" | "incomplete" | "failed";

export interface ToolSearchCall {
  /** The type of the item. Always `tool_search_call`. */
  type: "tool_search_call";
  /** The unique ID of the tool search call item. */
  id: string;
  call_id: string | null;
  execution: ToolSearchExecutionType;
  /** Arguments used for the tool search call. */
  arguments: unknown;
  status: FunctionCallStatus;
  /** The identifier of the actor that created the item. */
  created_by?: string;
}

export interface ToolSearchOutput {
  /** The type of the item. Always `tool_search_output`. */
  type: "tool_search_output";
  /** The unique ID of the tool search output item. */
  id: string;
  call_id: string | null;
  execution: ToolSearchExecutionType;
  /** The loaded tool definitions returned by tool search. */
  tools: Tool[];
  status: FunctionCallOutputStatusEnum;
  /** The identifier of the actor that created the item. */
  created_by?: string;
}

/** A compaction item generated by the [`v1/responses/compact` API](/docs/api-reference/responses/compact). */
export interface CompactionBody {
  /** The type of the item. Always `compaction`. */
  type: "compaction";
  /** The unique ID of the compaction item. */
  id: string;
  /** The encrypted content that was produced by compaction. */
  encrypted_content: string;
  /** The identifier of the actor that created the item. */
  created_by?: string;
}

/** A tool call that executes one or more shell commands in a managed environment. */
export interface FunctionShellCall {
  /** The type of the item. Always `shell_call`. */
  type: "shell_call";
  /** The unique ID of the shell tool call. Populated when this item is returned via API. */
  id: string;
  /** The unique ID of the shell tool call generated by the model. */
  call_id: string;
  action: FunctionShellAction;
  status: FunctionShellCallStatus;
  environment: FunctionShellCallEnvironment | null;
  /** The ID of the entity that created this tool call. */
  created_by?: string;
}

/** Execute a shell command. */
export interface FunctionShellAction {
  commands: string[];
  timeout_ms: number | null;
  max_output_length: number | null;
}

export type FunctionShellCallStatus = "in_progress" | "completed" | "incomplete";

export type FunctionShellCallEnvironment = LocalEnvironmentResource | ContainerReferenceResource;

/** Decode a `FunctionShellCallEnvironment` by its `type` discriminator (unknown value falls through). */
export function decodeFunctionShellCallEnvironment(data: unknown): FunctionShellCallEnvironment {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "local":
      return data as LocalEnvironmentResource;
    case "container_reference":
      return data as ContainerReferenceResource;
    default:
      return data as FunctionShellCallEnvironment;
  }
}

/** Represents the use of a local environment to perform shell actions. */
export interface LocalEnvironmentResource {
  /** The environment type. Always `local`. */
  type: "local";
}

/** Represents a container created with /v1/containers. */
export interface ContainerReferenceResource {
  /** The environment type. Always `container_reference`. */
  type: "container_reference";
  container_id: string;
}

/** The output of a shell tool call that was emitted. */
export interface FunctionShellCallOutput {
  /** The type of the shell call output. Always `shell_call_output`. */
  type: "shell_call_output";
  /** The unique ID of the shell call output. Populated when this item is returned via API. */
  id: string;
  /** The unique ID of the shell tool call generated by the model. */
  call_id: string;
  status: FunctionShellCallOutputStatusEnum;
  /** An array of shell call output contents */
  output: FunctionShellCallOutputContent[];
  max_output_length: number | null;
  /** The identifier of the actor that created the item. */
  created_by?: string;
}

export type FunctionShellCallOutputStatusEnum = "in_progress" | "completed" | "incomplete";

/** The content of a shell tool call output that was emitted. */
export interface FunctionShellCallOutputContent {
  /** The standard output that was captured. */
  stdout: string;
  /** The standard error output that was captured. */
  stderr: string;
  /** Represents either an exit outcome (with an exit code) or a timeout outcome for a shell call output chunk. */
  outcome: FunctionShellCallOutputContentOutcome;
  /** The identifier of the actor that created the item. */
  created_by?: string;
}

/** Represents either an exit outcome (with an exit code) or a timeout outcome for a shell call output chunk. */
export type FunctionShellCallOutputContentOutcome = FunctionShellCallOutputTimeoutOutcome | FunctionShellCallOutputExitOutcome;

/** Decode a `FunctionShellCallOutputContentOutcome` by its `type` discriminator (unknown value falls through). */
export function decodeFunctionShellCallOutputContentOutcome(data: unknown): FunctionShellCallOutputContentOutcome {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "timeout":
      return data as FunctionShellCallOutputTimeoutOutcome;
    case "exit":
      return data as FunctionShellCallOutputExitOutcome;
    default:
      return data as FunctionShellCallOutputContentOutcome;
  }
}

/** Indicates that the shell call exceeded its configured time limit. */
export interface FunctionShellCallOutputTimeoutOutcome {
  /** The outcome type. Always `timeout`. */
  type: "timeout";
}

/** Indicates that the shell commands finished and returned an exit code. */
export interface FunctionShellCallOutputExitOutcome {
  /** The outcome type. Always `exit`. */
  type: "exit";
  /** Exit code from the shell process. */
  exit_code: number;
}

/** A tool call that applies file diffs by creating, deleting, or updating files. */
export interface ApplyPatchToolCall {
  /** The type of the item. Always `apply_patch_call`. */
  type: "apply_patch_call";
  /** The unique ID of the apply patch tool call. Populated when this item is returned via API. */
  id: string;
  /** The unique ID of the apply patch tool call generated by the model. */
  call_id: string;
  status: ApplyPatchCallStatus;
  /** One of the create_file, delete_file, or update_file operations applied via apply_patch. */
  operation: ApplyPatchToolCallOperation;
  /** The ID of the entity that created this tool call. */
  created_by?: string;
}

export type ApplyPatchCallStatus = "in_progress" | "completed";

/** One of the create_file, delete_file, or update_file operations applied via apply_patch. */
export type ApplyPatchToolCallOperation = ApplyPatchCreateFileOperation | ApplyPatchDeleteFileOperation | ApplyPatchUpdateFileOperation;

/** Decode a `ApplyPatchToolCallOperation` by its `type` discriminator (unknown value falls through). */
export function decodeApplyPatchToolCallOperation(data: unknown): ApplyPatchToolCallOperation {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "create_file":
      return data as ApplyPatchCreateFileOperation;
    case "delete_file":
      return data as ApplyPatchDeleteFileOperation;
    case "update_file":
      return data as ApplyPatchUpdateFileOperation;
    default:
      return data as ApplyPatchToolCallOperation;
  }
}

/** Instruction describing how to create a file via the apply_patch tool. */
export interface ApplyPatchCreateFileOperation {
  /** Create a new file with the provided diff. */
  type: "create_file";
  /** Path of the file to create. */
  path: string;
  /** Diff to apply. */
  diff: string;
}

/** Instruction describing how to delete a file via the apply_patch tool. */
export interface ApplyPatchDeleteFileOperation {
  /** Delete the specified file. */
  type: "delete_file";
  /** Path of the file to delete. */
  path: string;
}

/** Instruction describing how to update a file via the apply_patch tool. */
export interface ApplyPatchUpdateFileOperation {
  /** Update an existing file with the provided diff. */
  type: "update_file";
  /** Path of the file to update. */
  path: string;
  /** Diff to apply. */
  diff: string;
}

/** The output emitted by an apply patch tool call. */
export interface ApplyPatchToolCallOutput {
  /** The type of the item. Always `apply_patch_call_output`. */
  type: "apply_patch_call_output";
  /** The unique ID of the apply patch tool call output. Populated when this item is returned via API. */
  id: string;
  /** The unique ID of the apply patch tool call generated by the model. */
  call_id: string;
  status: ApplyPatchCallOutputStatus;
  output?: string | null;
  /** The ID of the entity that created this tool call output. */
  created_by?: string;
}

export type ApplyPatchCallOutputStatus = "completed" | "failed";

/** A response to an MCP approval request. */
export interface McpApprovalResponseResource {
  /** The type of the item. Always `mcp_approval_response`. */
  type: "mcp_approval_response";
  /** The unique ID of the approval response */
  id: string;
  /** The ID of the approval request being answered. */
  approval_request_id: string;
  /** Whether the request was approved. */
  approve: boolean;
  reason?: string | null;
}

export type ConversationsItemsListOrder = "asc" | "desc";

/**
 * The output from the function call generated by your code.
 * Can be a string or an list of output content.
 */
export type FunctionToolCallOutputResourceOutput = string | FunctionAndCustomToolCallOutput[];

/**
 * The status of the item. One of `in_progress`, `completed`, or
 * `incomplete`. Populated when items are returned via API.
 */
export type ComputerToolCallStatus = "in_progress" | "completed" | "incomplete";

export interface UpdateConversationBody {
  metadata: Metadata;
}

export interface CreateEmbeddingRequest {
  /** Input text to embed, encoded as a string or array of tokens. To embed multiple inputs in a single request, pass an array of strings or array of token arrays. The input must not exceed the max input tokens for the model (8192 tokens for all embedding models), cannot be an empty string, and any array must be 2048 dimensions or less. [Example Python code](https://cookbook.openai.com/examples/how_to_count_tokens_with_tiktoken) for counting tokens. In addition to the per-input token limit, all embedding  models enforce a maximum of 300,000 tokens summed across all inputs in a  single request. */
  input: CreateEmbeddingRequestInput;
  /** ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](/docs/models) for descriptions of them. */
  model: CreateEmbeddingRequestModel;
  /** The format to return the embeddings in. Can be either `float` or [`base64`](https://pypi.org/project/pybase64/). */
  encoding_format?: CreateEmbeddingRequestEncodingFormat;
  /** The number of dimensions the resulting output embeddings should have. Only supported in `text-embedding-3` and later models. */
  dimensions?: number;
  /** A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices#end-user-ids). */
  user?: string;
}

/** Input text to embed, encoded as a string or array of tokens. To embed multiple inputs in a single request, pass an array of strings or array of token arrays. The input must not exceed the max input tokens for the model (8192 tokens for all embedding models), cannot be an empty string, and any array must be 2048 dimensions or less. [Example Python code](https://cookbook.openai.com/examples/how_to_count_tokens_with_tiktoken) for counting tokens. In addition to the per-input token limit, all embedding  models enforce a maximum of 300,000 tokens summed across all inputs in a  single request. */
export type CreateEmbeddingRequestInput = string | string[] | number[] | number[][];

/** ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](/docs/models) for descriptions of them. */
export type CreateEmbeddingRequestModel = string | CreateEmbeddingRequestModelString;

export type CreateEmbeddingRequestModelString = "text-embedding-ada-002" | "text-embedding-3-small" | "text-embedding-3-large";

/** The format to return the embeddings in. Can be either `float` or [`base64`](https://pypi.org/project/pybase64/). */
export type CreateEmbeddingRequestEncodingFormat = "float" | "base64";

export interface CreateEmbeddingResponse {
  /** The list of embeddings generated by the model. */
  data: Embedding[];
  /** The name of the model used to generate the embedding. */
  model: string;
  /** The object type, which is always "list". */
  object: "list";
  /** The usage information for the request. */
  usage: CreateEmbeddingResponseUsage;
}

/** Represents an embedding vector returned by embedding endpoint. */
export interface Embedding {
  /** The index of the embedding in the list of embeddings. */
  index: number;
  /** The embedding vector, which is a list of floats. The length of vector depends on the model as listed in the [embedding guide](/docs/guides/embeddings). */
  embedding: number[];
  /** The object type, which is always "embedding". */
  object: "embedding";
}

/** The usage information for the request. */
export interface CreateEmbeddingResponseUsage {
  /** The number of tokens used by the prompt. */
  prompt_tokens: number;
  /** The total number of tokens used by the request. */
  total_tokens: number;
}

export interface CreateEvalRequest {
  /** The name of the evaluation. */
  name?: string;
  metadata?: Metadata;
  /** The configuration for the data source used for the evaluation runs. Dictates the schema of the data used in the evaluation. */
  data_source_config: CreateEvalRequestDataSourceConfig;
  /** A list of graders for all eval runs in this group. Graders can reference variables in the data source using double curly braces notation, like `{{item.variable_name}}`. To reference the model's output, use the `sample` namespace (ie, `{{sample.output_text}}`). */
  testing_criteria: CreateEvalRequestTestingCriteriaItem[];
}

/** The configuration for the data source used for the evaluation runs. Dictates the schema of the data used in the evaluation. */
export type CreateEvalRequestDataSourceConfig = CreateEvalCustomDataSourceConfig | CreateEvalLogsDataSourceConfig | CreateEvalStoredCompletionsDataSourceConfig;

/**
 * A CustomDataSourceConfig object that defines the schema for the data source used for the evaluation runs.
 * This schema is used to define the shape of the data that will be:
 * - Used to define your testing criteria and
 * - What data is required when creating a run
 */
export interface CreateEvalCustomDataSourceConfig {
  /** The type of data source. Always `custom`. */
  type: "custom";
  /** The json schema for each row in the data source. */
  item_schema: Record<string, unknown>;
  /** Whether the eval should expect you to populate the sample namespace (ie, by generating responses off of your data source) */
  include_sample_schema?: boolean;
}

/**
 * A data source config which specifies the metadata property of your logs query.
 * This is usually metadata like `usecase=chatbot` or `prompt-version=v2`, etc.
 */
export interface CreateEvalLogsDataSourceConfig {
  /** The type of data source. Always `logs`. */
  type: "logs";
  /** Metadata filters for the logs data source. */
  metadata?: Record<string, unknown>;
}

/** Deprecated in favor of LogsDataSourceConfig. */
export interface CreateEvalStoredCompletionsDataSourceConfig {
  /** The type of data source. Always `stored_completions`. */
  type: "stored_completions";
  /** Metadata filters for the stored completions data source. */
  metadata?: Record<string, unknown>;
}

export type CreateEvalRequestTestingCriteriaItem = CreateEvalLabelModelGrader | EvalGraderStringCheck | EvalGraderTextSimilarity | EvalGraderPython | EvalGraderScoreModel;

/**
 * A LabelModelGrader object which uses a model to assign labels to each item
 * in the evaluation.
 */
export interface CreateEvalLabelModelGrader {
  /** The object type, which is always `label_model`. */
  type: "label_model";
  /** The name of the grader. */
  name: string;
  /** The model to use for the evaluation. Must support structured outputs. */
  model: string;
  /** A list of chat messages forming the prompt or context. May include variable references to the `item` namespace, ie {{item.name}}. */
  input: CreateEvalItem[];
  /** The labels to classify to each item in the evaluation. */
  labels: string[];
  /** The labels that indicate a passing result. Must be a subset of labels. */
  passing_labels: string[];
}

/** A chat message that makes up the prompt or context. May include variable references to the `item` namespace, ie {{item.name}}. */
export type CreateEvalItem = CreateEvalItemSimpleInputMessage | EvalItem;

export interface CreateEvalItemSimpleInputMessage {
  /** The role of the message (e.g. "system", "assistant", "user"). */
  role: string;
  /** The content of the message. */
  content: string;
}

/**
 * A message input to the model with a role indicating instruction following
 * hierarchy. Instructions given with the `developer` or `system` role take
 * precedence over instructions given with the `user` role. Messages with the
 * `assistant` role are presumed to have been generated by the model in previous
 * interactions.
 */
export interface EvalItem {
  /**
   * The role of the message input. One of `user`, `assistant`, `system`, or
   * `developer`.
   */
  role: EvalItemRole;
  content: EvalItemContent;
  /** The type of the message input. Always `message`. */
  type?: "message";
}

/**
 * The role of the message input. One of `user`, `assistant`, `system`, or
 * `developer`.
 */
export type EvalItemRole = "user" | "assistant" | "system" | "developer";

/** Inputs to the model - can contain template strings. Supports text, output text, input images, and input audio, either as a single item or an array of items. */
export type EvalItemContent = EvalItemContentItem | EvalItemContentArray;

/** A single content item: input text, output text, input image, or input audio. */
export type EvalItemContentItem = EvalItemContentText | InputTextContent | EvalItemContentOutputText | EvalItemInputImage | InputAudio;

export type EvalItemContentText = string;

/** A text output from the model. */
export interface EvalItemContentOutputText {
  /** The type of the output text. Always `output_text`. */
  type: "output_text";
  /** The text output from the model. */
  text: string;
}

/** An image input block used within EvalItem content arrays. */
export interface EvalItemInputImage {
  /** The type of the image input. Always `input_image`. */
  type: "input_image";
  /** The URL of the image input. */
  image_url: string;
  /** The detail level of the image to be sent to the model. One of `high`, `low`, or `auto`. Defaults to `auto`. */
  detail?: string;
}

/** An audio input to the model. */
export interface InputAudio {
  /** The type of the input item. Always `input_audio`. */
  type: "input_audio";
  input_audio: InputAudioInputAudio;
}

export interface InputAudioInputAudio {
  /** Base64-encoded audio data. */
  data: string;
  /**
   * The format of the audio data. Currently supported formats are `mp3` and
   * `wav`.
   */
  format: InputAudioInputAudioFormat;
}

/**
 * The format of the audio data. Currently supported formats are `mp3` and
 * `wav`.
 */
export type InputAudioInputAudioFormat = "mp3" | "wav";

export type EvalItemContentArray = EvalItemContentItem[];

export type EvalGraderStringCheck = GraderStringCheck;

/** A StringCheckGrader object that performs a string comparison between input and reference using a specified operation. */
export interface GraderStringCheck {
  /** The object type, which is always `string_check`. */
  type: "string_check";
  /** The name of the grader. */
  name: string;
  /** The input text. This may include template strings. */
  input: string;
  /** The reference text. This may include template strings. */
  reference: string;
  /** The string check operation to perform. One of `eq`, `ne`, `like`, or `ilike`. */
  operation: GraderStringCheckOperation;
}

/** The string check operation to perform. One of `eq`, `ne`, `like`, or `ilike`. */
export type GraderStringCheckOperation = "eq" | "ne" | "like" | "ilike";

export interface EvalGraderTextSimilarity {
  /** The type of grader. */
  type: "text_similarity";
  /** The name of the grader. */
  name: string;
  /** The text being graded. */
  input: string;
  /** The text being graded against. */
  reference: string;
  /**
   * The evaluation metric to use. One of `cosine`, `fuzzy_match`, `bleu`,
   * `gleu`, `meteor`, `rouge_1`, `rouge_2`, `rouge_3`, `rouge_4`, `rouge_5`,
   * or `rouge_l`.
   */
  evaluation_metric: EvalGraderTextSimilarityEvaluationMetric;
  /** The threshold for the score. */
  pass_threshold: number;
}

/**
 * The evaluation metric to use. One of `cosine`, `fuzzy_match`, `bleu`,
 * `gleu`, `meteor`, `rouge_1`, `rouge_2`, `rouge_3`, `rouge_4`, `rouge_5`,
 * or `rouge_l`.
 */
export type EvalGraderTextSimilarityEvaluationMetric = "cosine" | "fuzzy_match" | "bleu" | "gleu" | "meteor" | "rouge_1" | "rouge_2" | "rouge_3" | "rouge_4" | "rouge_5" | "rouge_l";

export interface EvalGraderPython {
  /** The object type, which is always `python`. */
  type: "python";
  /** The name of the grader. */
  name: string;
  /** The source code of the python script. */
  source: string;
  /** The image tag to use for the python script. */
  image_tag?: string;
  /** The threshold for the score. */
  pass_threshold?: number;
}

export interface EvalGraderScoreModel {
  /** The object type, which is always `score_model`. */
  type: "score_model";
  /** The name of the grader. */
  name: string;
  /** The model to use for the evaluation. */
  model: string;
  /** The sampling parameters for the model. */
  sampling_params?: EvalGraderScoreModelSamplingParams;
  /** The input messages evaluated by the grader. Supports text, output text, input image, and input audio content blocks, and may include template strings. */
  input: EvalItem[];
  /** The range of the score. Defaults to `[0, 1]`. */
  range?: number[];
  /** The threshold for the score. */
  pass_threshold?: number;
}

/** The sampling parameters for the model. */
export interface EvalGraderScoreModelSamplingParams {
  seed?: number | null;
  top_p?: number | null;
  temperature?: number | null;
  max_completions_tokens?: number | null;
  reasoning_effort?: ReasoningEffort;
}

/**
 * An Eval object with a data source config and testing criteria.
 * An Eval represents a task to be done for your LLM integration.
 * Like:
 * - Improve the quality of my chatbot
 * - See how well my chatbot handles customer support
 * - Check if o4-mini is better at my usecase than gpt-4o
 */
export interface Eval {
  /** The object type. */
  object: "eval";
  /** Unique identifier for the evaluation. */
  id: string;
  /** The name of the evaluation. */
  name: string;
  /** Configuration of data sources used in runs of the evaluation. */
  data_source_config: EvalDataSourceConfig;
  /** A list of testing criteria. */
  testing_criteria: EvalTestingCriteriaItem[];
  /** The Unix timestamp (in seconds) for when the eval was created. */
  created_at: number;
  metadata: Metadata;
}

/** Configuration of data sources used in runs of the evaluation. */
export type EvalDataSourceConfig = EvalCustomDataSourceConfig | EvalLogsDataSourceConfig | EvalStoredCompletionsDataSourceConfig;

/**
 * A CustomDataSourceConfig which specifies the schema of your `item` and optionally `sample` namespaces.
 * The response schema defines the shape of the data that will be:
 * - Used to define your testing criteria and
 * - What data is required when creating a run
 */
export interface EvalCustomDataSourceConfig {
  /** The type of data source. Always `custom`. */
  type: "custom";
  /**
   * The json schema for the run data source items.
   * Learn how to build JSON schemas [here](https://json-schema.org/).
   */
  schema: Record<string, unknown>;
}

/**
 * A LogsDataSourceConfig which specifies the metadata property of your logs query.
 * This is usually metadata like `usecase=chatbot` or `prompt-version=v2`, etc.
 * The schema returned by this data source config is used to defined what variables are available in your evals.
 * `item` and `sample` are both defined when using this data source config.
 */
export interface EvalLogsDataSourceConfig {
  /** The type of data source. Always `logs`. */
  type: "logs";
  metadata?: Metadata;
  /**
   * The json schema for the run data source items.
   * Learn how to build JSON schemas [here](https://json-schema.org/).
   */
  schema: Record<string, unknown>;
}

/** Deprecated in favor of LogsDataSourceConfig. */
export interface EvalStoredCompletionsDataSourceConfig {
  /** The type of data source. Always `stored_completions`. */
  type: "stored_completions";
  metadata?: Metadata;
  /**
   * The json schema for the run data source items.
   * Learn how to build JSON schemas [here](https://json-schema.org/).
   */
  schema: Record<string, unknown>;
}

export type EvalTestingCriteriaItem = EvalGraderLabelModel | EvalGraderStringCheck | EvalGraderTextSimilarity | EvalGraderPython | EvalGraderScoreModel;

export type EvalGraderLabelModel = GraderLabelModel;

/**
 * A LabelModelGrader object which uses a model to assign labels to each item
 * in the evaluation.
 */
export interface GraderLabelModel {
  /** The object type, which is always `label_model`. */
  type: "label_model";
  /** The name of the grader. */
  name: string;
  /** The model to use for the evaluation. Must support structured outputs. */
  model: string;
  input: EvalItem[];
  /** The labels to assign to each item in the evaluation. */
  labels: string[];
  /** The labels that indicate a passing result. Must be a subset of labels. */
  passing_labels: string[];
}

export interface EvalsDeleteResponse {
  object: string;
  deleted: boolean;
  eval_id: string;
}

export type EvalsListOrder = "asc" | "desc";

export type EvalsListOrderBy = "created_at" | "updated_at";

/** An object representing a list of evals. */
export interface EvalList {
  /** The type of this object. It is always set to "list". */
  object: "list";
  /** An array of eval objects. */
  data: Eval[];
  /** The identifier of the first eval in the data array. */
  first_id: string;
  /** The identifier of the last eval in the data array. */
  last_id: string;
  /** Indicates whether there are more evals available. */
  has_more: boolean;
}

/** A schema representing an evaluation run. */
export interface EvalRun {
  /** The type of the object. Always "eval.run". */
  object: "eval.run";
  /** Unique identifier for the evaluation run. */
  id: string;
  /** The identifier of the associated evaluation. */
  eval_id: string;
  /** The status of the evaluation run. */
  status: string;
  /** The model that is evaluated, if applicable. */
  model: string;
  /** The name of the evaluation run. */
  name: string;
  /** Unix timestamp (in seconds) when the evaluation run was created. */
  created_at: number;
  /** The URL to the rendered evaluation run report on the UI dashboard. */
  report_url: string;
  /** Counters summarizing the outcomes of the evaluation run. */
  result_counts: EvalRunResultCounts;
  /** Usage statistics for each model during the evaluation run. */
  per_model_usage: EvalRunPerModelUsageItem[];
  /** Results per testing criteria applied during the evaluation run. */
  per_testing_criteria_results: EvalRunPerTestingCriteriaResultsItem[];
  /** Information about the run's data source. */
  data_source: EvalRunDataSource;
  metadata: Metadata;
  error: EvalApiError;
}

/** Counters summarizing the outcomes of the evaluation run. */
export interface EvalRunResultCounts {
  /** Total number of executed output items. */
  total: number;
  /** Number of output items that resulted in an error. */
  errored: number;
  /** Number of output items that failed to pass the evaluation. */
  failed: number;
  /** Number of output items that passed the evaluation. */
  passed: number;
}

export interface EvalRunPerModelUsageItem {
  /** The name of the model. */
  model_name: string;
  /** The number of invocations. */
  invocation_count: number;
  /** The number of prompt tokens used. */
  prompt_tokens: number;
  /** The number of completion tokens generated. */
  completion_tokens: number;
  /** The total number of tokens used. */
  total_tokens: number;
  /** The number of tokens retrieved from cache. */
  cached_tokens: number;
}

export interface EvalRunPerTestingCriteriaResultsItem {
  /** A description of the testing criteria. */
  testing_criteria: string;
  /** Number of tests passed for this criteria. */
  passed: number;
  /** Number of tests failed for this criteria. */
  failed: number;
}

/** Information about the run's data source. */
export type EvalRunDataSource = CreateEvalJsonlRunDataSource | CreateEvalCompletionsRunDataSource | CreateEvalResponsesRunDataSource;

/** A JsonlRunDataSource object with that specifies a JSONL file that matches the eval */
export interface CreateEvalJsonlRunDataSource {
  /** The type of data source. Always `jsonl`. */
  type: "jsonl";
  /** Determines what populates the `item` namespace in the data source. */
  source: CreateEvalJsonlRunDataSourceSource;
}

/** Determines what populates the `item` namespace in the data source. */
export type CreateEvalJsonlRunDataSourceSource = EvalJsonlFileContentSource | EvalJsonlFileIdSource;

export interface EvalJsonlFileContentSource {
  /** The type of jsonl source. Always `file_content`. */
  type: "file_content";
  /** The content of the jsonl file. */
  content: EvalJsonlFileContentSourceContentItem[];
}

export interface EvalJsonlFileContentSourceContentItem {
  item: Record<string, unknown>;
  sample?: Record<string, unknown>;
}

export interface EvalJsonlFileIdSource {
  /** The type of jsonl source. Always `file_id`. */
  type: "file_id";
  /** The identifier of the file. */
  id: string;
}

/** A CompletionsRunDataSource object describing a model sampling configuration. */
export interface CreateEvalCompletionsRunDataSource {
  /** The type of run data source. Always `completions`. */
  type: "completions";
  /** Used when sampling from a model. Dictates the structure of the messages passed into the model. Can either be a reference to a prebuilt trajectory (ie, `item.input_trajectory`), or a template with variable references to the `item` namespace. */
  input_messages?: CreateEvalCompletionsRunDataSourceInputMessages;
  sampling_params?: CreateEvalCompletionsRunDataSourceSamplingParams;
  /** The name of the model to use for generating completions (e.g. "o3-mini"). */
  model?: string;
  /** Determines what populates the `item` namespace in this run's data source. */
  source: CreateEvalCompletionsRunDataSourceSource;
}

/** Used when sampling from a model. Dictates the structure of the messages passed into the model. Can either be a reference to a prebuilt trajectory (ie, `item.input_trajectory`), or a template with variable references to the `item` namespace. */
export type CreateEvalCompletionsRunDataSourceInputMessages = CreateEvalCompletionsRunDataSourceInputMessagesTemplate | CreateEvalCompletionsRunDataSourceInputMessagesItemReference;

export interface CreateEvalCompletionsRunDataSourceInputMessagesTemplate {
  /** The type of input messages. Always `template`. */
  type: "template";
  /** A list of chat messages forming the prompt or context. May include variable references to the `item` namespace, ie {{item.name}}. */
  template: CreateEvalCompletionsRunDataSourceInputMessagesTemplateTemplateItem[];
}

export type CreateEvalCompletionsRunDataSourceInputMessagesTemplateTemplateItem = EasyInputMessage | EvalItem;

export interface CreateEvalCompletionsRunDataSourceInputMessagesItemReference {
  /** The type of input messages. Always `item_reference`. */
  type: "item_reference";
  /** A reference to a variable in the `item` namespace. Ie, "item.input_trajectory" */
  item_reference: string;
}

export interface CreateEvalCompletionsRunDataSourceSamplingParams {
  reasoning_effort?: ReasoningEffort;
  /** A higher temperature increases randomness in the outputs. */
  temperature?: number;
  /** The maximum number of tokens in the generated output. */
  max_completion_tokens?: number;
  /** An alternative to temperature for nucleus sampling; 1.0 includes all tokens. */
  top_p?: number;
  /** A seed value to initialize the randomness, during sampling. */
  seed?: number;
  /**
   * An object specifying the format that the model must output.
   *
   * Setting to `{ "type": "json_schema", "json_schema": {...} }` enables
   * Structured Outputs which ensures the model will match your supplied JSON
   * schema. Learn more in the [Structured Outputs
   * guide](/docs/guides/structured-outputs).
   *
   * Setting to `{ "type": "json_object" }` enables the older JSON mode, which
   * ensures the message the model generates is valid JSON. Using `json_schema`
   * is preferred for models that support it.
   */
  response_format?: CreateEvalCompletionsRunDataSourceSamplingParamsResponseFormat;
  /** A list of tools the model may call. Currently, only functions are supported as a tool. Use this to provide a list of functions the model may generate JSON inputs for. A max of 128 functions are supported. */
  tools?: ChatCompletionTool[];
}

/**
 * An object specifying the format that the model must output.
 *
 * Setting to `{ "type": "json_schema", "json_schema": {...} }` enables
 * Structured Outputs which ensures the model will match your supplied JSON
 * schema. Learn more in the [Structured Outputs
 * guide](/docs/guides/structured-outputs).
 *
 * Setting to `{ "type": "json_object" }` enables the older JSON mode, which
 * ensures the message the model generates is valid JSON. Using `json_schema`
 * is preferred for models that support it.
 */
export type CreateEvalCompletionsRunDataSourceSamplingParamsResponseFormat = ResponseFormatText | ResponseFormatJsonSchema | ResponseFormatJsonObject;

/** Determines what populates the `item` namespace in this run's data source. */
export type CreateEvalCompletionsRunDataSourceSource = EvalJsonlFileContentSource | EvalJsonlFileIdSource | EvalStoredCompletionsSource;

/** A StoredCompletionsRunDataSource configuration describing a set of filters */
export interface EvalStoredCompletionsSource {
  /** The type of source. Always `stored_completions`. */
  type: "stored_completions";
  metadata?: Metadata;
  model?: string | null;
  created_after?: number | null;
  created_before?: number | null;
  limit?: number | null;
}

/** A ResponsesRunDataSource object describing a model sampling configuration. */
export interface CreateEvalResponsesRunDataSource {
  /** The type of run data source. Always `responses`. */
  type: "responses";
  /** Used when sampling from a model. Dictates the structure of the messages passed into the model. Can either be a reference to a prebuilt trajectory (ie, `item.input_trajectory`), or a template with variable references to the `item` namespace. */
  input_messages?: CreateEvalResponsesRunDataSourceInputMessages;
  sampling_params?: CreateEvalResponsesRunDataSourceSamplingParams;
  /** The name of the model to use for generating completions (e.g. "o3-mini"). */
  model?: string;
  /** Determines what populates the `item` namespace in this run's data source. */
  source: CreateEvalResponsesRunDataSourceSource;
}

/** Used when sampling from a model. Dictates the structure of the messages passed into the model. Can either be a reference to a prebuilt trajectory (ie, `item.input_trajectory`), or a template with variable references to the `item` namespace. */
export type CreateEvalResponsesRunDataSourceInputMessages = CreateEvalResponsesRunDataSourceInputMessagesTemplate | CreateEvalResponsesRunDataSourceInputMessagesItemReference;

export interface CreateEvalResponsesRunDataSourceInputMessagesTemplate {
  /** The type of input messages. Always `template`. */
  type: "template";
  /** A list of chat messages forming the prompt or context. May include variable references to the `item` namespace, ie {{item.name}}. */
  template: CreateEvalResponsesRunDataSourceInputMessagesTemplateTemplateItem[];
}

export type CreateEvalResponsesRunDataSourceInputMessagesTemplateTemplateItem = CreateEvalResponsesRunDataSourceInputMessagesTemplateTemplateItemChatMessage | EvalItem;

export interface CreateEvalResponsesRunDataSourceInputMessagesTemplateTemplateItemChatMessage {
  /** The role of the message (e.g. "system", "assistant", "user"). */
  role: string;
  /** The content of the message. */
  content: string;
}

export interface CreateEvalResponsesRunDataSourceInputMessagesItemReference {
  /** The type of input messages. Always `item_reference`. */
  type: "item_reference";
  /** A reference to a variable in the `item` namespace. Ie, "item.name" */
  item_reference: string;
}

export interface CreateEvalResponsesRunDataSourceSamplingParams {
  reasoning_effort?: ReasoningEffort;
  /** A higher temperature increases randomness in the outputs. */
  temperature?: number;
  /** The maximum number of tokens in the generated output. */
  max_completion_tokens?: number;
  /** An alternative to temperature for nucleus sampling; 1.0 includes all tokens. */
  top_p?: number;
  /** A seed value to initialize the randomness, during sampling. */
  seed?: number;
  /**
   * An array of tools the model may call while generating a response. You
   * can specify which tool to use by setting the `tool_choice` parameter.
   *
   * The two categories of tools you can provide the model are:
   *
   * - **Built-in tools**: Tools that are provided by OpenAI that extend the
   * model's capabilities, like [web search](/docs/guides/tools-web-search)
   * or [file search](/docs/guides/tools-file-search). Learn more about
   * [built-in tools](/docs/guides/tools).
   * - **Function calls (custom tools)**: Functions that are defined by you,
   * enabling the model to call your own code. Learn more about
   * [function calling](/docs/guides/function-calling).
   */
  tools?: Tool[];
  /**
   * Configuration options for a text response from the model. Can be plain
   * text or structured JSON data. Learn more:
   * - [Text inputs and outputs](/docs/guides/text)
   * - [Structured Outputs](/docs/guides/structured-outputs)
   */
  text?: CreateEvalResponsesRunDataSourceSamplingParamsText;
}

/**
 * Configuration options for a text response from the model. Can be plain
 * text or structured JSON data. Learn more:
 * - [Text inputs and outputs](/docs/guides/text)
 * - [Structured Outputs](/docs/guides/structured-outputs)
 */
export interface CreateEvalResponsesRunDataSourceSamplingParamsText {
  format?: TextResponseFormatConfiguration;
}

/**
 * An object specifying the format that the model must output.
 *
 * Configuring `{ "type": "json_schema" }` enables Structured Outputs,
 * which ensures the model will match your supplied JSON schema. Learn more in the
 * [Structured Outputs guide](/docs/guides/structured-outputs).
 *
 * The default format is `{ "type": "text" }` with no additional options.
 *
 * **Not recommended for gpt-4o and newer models:**
 *
 * Setting to `{ "type": "json_object" }` enables the older JSON mode, which
 * ensures the message the model generates is valid JSON. Using `json_schema`
 * is preferred for models that support it.
 */
export type TextResponseFormatConfiguration = ResponseFormatText | TextResponseFormatJsonSchema | ResponseFormatJsonObject;

/**
 * JSON Schema response format. Used to generate structured JSON responses.
 * Learn more about [Structured Outputs](/docs/guides/structured-outputs).
 */
export interface TextResponseFormatJsonSchema {
  /** The type of response format being defined. Always `json_schema`. */
  type: "json_schema";
  /**
   * A description of what the response format is for, used by the model to
   * determine how to respond in the format.
   */
  description?: string;
  /**
   * The name of the response format. Must be a-z, A-Z, 0-9, or contain
   * underscores and dashes, with a maximum length of 64.
   */
  name: string;
  schema: ResponseFormatJsonSchemaSchema;
  strict?: boolean | null;
}

/** Determines what populates the `item` namespace in this run's data source. */
export type CreateEvalResponsesRunDataSourceSource = EvalJsonlFileContentSource | EvalJsonlFileIdSource | EvalResponsesSource;

/** A EvalResponsesSource object describing a run data source configuration. */
export interface EvalResponsesSource {
  /** The type of run data source. Always `responses`. */
  type: "responses";
  metadata?: Record<string, unknown> | null;
  model?: string | null;
  instructions_search?: string | null;
  created_after?: number | null;
  created_before?: number | null;
  reasoning_effort?: ReasoningEffort | null;
  temperature?: number | null;
  top_p?: number | null;
  users?: string[] | null;
  tools?: string[] | null;
}

/** An object representing an error response from the Eval API. */
export interface EvalApiError {
  /** The error code. */
  code: string;
  /** The error message. */
  message: string;
}

export interface CreateEvalRunRequest {
  /** The name of the run. */
  name?: string;
  metadata?: Metadata;
  /** Details about the run's data source. */
  data_source: CreateEvalRunRequestDataSource;
}

/** Details about the run's data source. */
export type CreateEvalRunRequestDataSource = CreateEvalJsonlRunDataSource | CreateEvalCompletionsRunDataSource | CreateEvalResponsesRunDataSource;

export interface EvalsRunsDeleteResponse {
  object?: string;
  deleted?: boolean;
  run_id?: string;
}

export type EvalsRunsListOrder = "asc" | "desc";

export type EvalsRunsListStatus = "queued" | "in_progress" | "completed" | "canceled" | "failed";

/** An object representing a list of runs for an evaluation. */
export interface EvalRunList {
  /** The type of this object. It is always set to "list". */
  object: "list";
  /** An array of eval run objects. */
  data: EvalRun[];
  /** The identifier of the first eval run in the data array. */
  first_id: string;
  /** The identifier of the last eval run in the data array. */
  last_id: string;
  /** Indicates whether there are more evals available. */
  has_more: boolean;
}

export type EvalsRunsOutputItemsListStatus = "fail" | "pass";

export type EvalsRunsOutputItemsListOrder = "asc" | "desc";

/** An object representing a list of output items for an evaluation run. */
export interface EvalRunOutputItemList {
  /** The type of this object. It is always set to "list". */
  object: "list";
  /** An array of eval run output item objects. */
  data: EvalRunOutputItem[];
  /** The identifier of the first eval run output item in the data array. */
  first_id: string;
  /** The identifier of the last eval run output item in the data array. */
  last_id: string;
  /** Indicates whether there are more eval run output items available. */
  has_more: boolean;
}

/** A schema representing an evaluation run output item. */
export interface EvalRunOutputItem {
  /** The type of the object. Always "eval.run.output_item". */
  object: "eval.run.output_item";
  /** Unique identifier for the evaluation run output item. */
  id: string;
  /** The identifier of the evaluation run associated with this output item. */
  run_id: string;
  /** The identifier of the evaluation group. */
  eval_id: string;
  /** Unix timestamp (in seconds) when the evaluation run was created. */
  created_at: number;
  /** The status of the evaluation run. */
  status: string;
  /** The identifier for the data source item. */
  datasource_item_id: number;
  /** Details of the input data source item. */
  datasource_item: Record<string, unknown>;
  /** A list of grader results for this output item. */
  results: EvalRunOutputItemResult[];
  /** A sample containing the input and output of the evaluation run. */
  sample: EvalRunOutputItemSample;
}

/** A single grader result for an evaluation run output item. */
export interface EvalRunOutputItemResult {
  /** The name of the grader. */
  name: string;
  /** The grader type (for example, "string-check-grader"). */
  type?: string;
  /** The numeric score produced by the grader. */
  score: number;
  /** Whether the grader considered the output a pass. */
  passed: boolean;
  /** Optional sample or intermediate data produced by the grader. */
  sample?: Record<string, unknown> | null;
}

/** A sample containing the input and output of the evaluation run. */
export interface EvalRunOutputItemSample {
  /** An array of input messages. */
  input: EvalRunOutputItemSampleInputItem[];
  /** An array of output messages. */
  output: EvalRunOutputItemSampleOutputItem[];
  /** The reason why the sample generation was finished. */
  finish_reason: string;
  /** The model used for generating the sample. */
  model: string;
  /** Token usage details for the sample. */
  usage: EvalRunOutputItemSampleUsage;
  error: EvalApiError;
  /** The sampling temperature used. */
  temperature: number;
  /** The maximum number of tokens allowed for completion. */
  max_completion_tokens: number;
  /** The top_p value used for sampling. */
  top_p: number;
  /** The seed used for generating the sample. */
  seed: number;
}

/** An input message. */
export interface EvalRunOutputItemSampleInputItem {
  /** The role of the message sender (e.g., system, user, developer). */
  role: string;
  /** The content of the message. */
  content: string;
}

export interface EvalRunOutputItemSampleOutputItem {
  /** The role of the message (e.g. "system", "assistant", "user"). */
  role?: string;
  /** The content of the message. */
  content?: string;
}

/** Token usage details for the sample. */
export interface EvalRunOutputItemSampleUsage {
  /** The total number of tokens used. */
  total_tokens: number;
  /** The number of completion tokens generated. */
  completion_tokens: number;
  /** The number of prompt tokens used. */
  prompt_tokens: number;
  /** The number of tokens retrieved from cache. */
  cached_tokens: number;
}

export interface EvalsUpdateRequest {
  /** Rename the evaluation. */
  name?: string;
  metadata?: Metadata;
}

export interface CreateFileRequest {
  /** The File object (not file name) to be uploaded. */
  file: Uint8Array | Blob;
  /**
   * The intended purpose of the uploaded file. One of:
   * - `assistants`: Used in the Assistants API
   * - `batch`: Used in the Batch API
   * - `fine-tune`: Used for fine-tuning
   * - `vision`: Images used for vision fine-tuning
   * - `user_data`: Flexible file type for any purpose
   * - `evals`: Used for eval data sets
   */
  purpose: CreateFileRequestPurpose;
  expires_after?: FileExpirationAfter;
}

/**
 * The intended purpose of the uploaded file. One of:
 * - `assistants`: Used in the Assistants API
 * - `batch`: Used in the Batch API
 * - `fine-tune`: Used for fine-tuning
 * - `vision`: Images used for vision fine-tuning
 * - `user_data`: Flexible file type for any purpose
 * - `evals`: Used for eval data sets
 */
export type CreateFileRequestPurpose = "assistants" | "batch" | "fine-tune" | "vision" | "user_data" | "evals";

/** The expiration policy for a file. By default, files with `purpose=batch` expire after 30 days and all other files are persisted until they are manually deleted. */
export interface FileExpirationAfter {
  /** Anchor timestamp after which the expiration policy applies. Supported anchors: `created_at`. */
  anchor: "created_at";
  /** The number of seconds after the anchor time that the file will expire. Must be between 3600 (1 hour) and 2592000 (30 days). */
  seconds: number;
}

/** The `File` object represents a document that has been uploaded to OpenAI. */
export interface OpenAiFile {
  /** The file identifier, which can be referenced in the API endpoints. */
  id: string;
  /** The size of the file, in bytes. */
  bytes: number;
  /** The Unix timestamp (in seconds) for when the file was created. */
  created_at: number;
  /** The Unix timestamp (in seconds) for when the file will expire. */
  expires_at?: number;
  /** The name of the file. */
  filename: string;
  /** The object type, which is always `file`. */
  object: "file";
  /** The intended purpose of the file. Supported values are `assistants`, `assistants_output`, `batch`, `batch_output`, `fine-tune`, `fine-tune-results`, `vision`, and `user_data`. */
  purpose: OpenAiFilePurpose;
  /** Deprecated. The current status of the file, which can be either `uploaded`, `processed`, or `error`. */
  status: OpenAiFileStatus;
  /** Deprecated. For details on why a fine-tuning training file failed validation, see the `error` field on `fine_tuning.job`. */
  status_details?: string;
}

/** The intended purpose of the file. Supported values are `assistants`, `assistants_output`, `batch`, `batch_output`, `fine-tune`, `fine-tune-results`, `vision`, and `user_data`. */
export type OpenAiFilePurpose = "assistants" | "assistants_output" | "batch" | "batch_output" | "fine-tune" | "fine-tune-results" | "vision" | "user_data";

/** Deprecated. The current status of the file, which can be either `uploaded`, `processed`, or `error`. */
export type OpenAiFileStatus = "uploaded" | "processed" | "error";

export interface DeleteFileResponse {
  id: string;
  object: "file";
  deleted: boolean;
}

export type FilesListOrder = "asc" | "desc";

export interface ListFilesResponse {
  object: string;
  data: OpenAiFile[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

export interface RunGraderRequest {
  /** The grader used for the fine-tuning job. */
  grader: RunGraderRequestGrader;
  /**
   * The dataset item provided to the grader. This will be used to populate
   * the `item` namespace. See [the guide](/docs/guides/graders) for more details.
   */
  item?: Record<string, unknown>;
  /**
   * The model sample to be evaluated. This value will be used to populate
   * the `sample` namespace. See [the guide](/docs/guides/graders) for more details.
   * The `output_json` variable will be populated if the model sample is a
   * valid JSON string.
   */
  model_sample: string;
}

/** The grader used for the fine-tuning job. */
export type RunGraderRequestGrader = GraderStringCheck | GraderTextSimilarity | GraderPython | GraderScoreModel | GraderMulti;

/** A TextSimilarityGrader object which grades text based on similarity metrics. */
export interface GraderTextSimilarity {
  /** The type of grader. */
  type: "text_similarity";
  /** The name of the grader. */
  name: string;
  /** The text being graded. */
  input: string;
  /** The text being graded against. */
  reference: string;
  /**
   * The evaluation metric to use. One of `cosine`, `fuzzy_match`, `bleu`,
   * `gleu`, `meteor`, `rouge_1`, `rouge_2`, `rouge_3`, `rouge_4`, `rouge_5`,
   * or `rouge_l`.
   */
  evaluation_metric: GraderTextSimilarityEvaluationMetric;
}

/**
 * The evaluation metric to use. One of `cosine`, `fuzzy_match`, `bleu`,
 * `gleu`, `meteor`, `rouge_1`, `rouge_2`, `rouge_3`, `rouge_4`, `rouge_5`,
 * or `rouge_l`.
 */
export type GraderTextSimilarityEvaluationMetric = "cosine" | "fuzzy_match" | "bleu" | "gleu" | "meteor" | "rouge_1" | "rouge_2" | "rouge_3" | "rouge_4" | "rouge_5" | "rouge_l";

/** A PythonGrader object that runs a python script on the input. */
export interface GraderPython {
  /** The object type, which is always `python`. */
  type: "python";
  /** The name of the grader. */
  name: string;
  /** The source code of the python script. */
  source: string;
  /** The image tag to use for the python script. */
  image_tag?: string;
}

/** A ScoreModelGrader object that uses a model to assign a score to the input. */
export interface GraderScoreModel {
  /** The object type, which is always `score_model`. */
  type: "score_model";
  /** The name of the grader. */
  name: string;
  /** The model to use for the evaluation. */
  model: string;
  /** The sampling parameters for the model. */
  sampling_params?: GraderScoreModelSamplingParams;
  /** The input messages evaluated by the grader. Supports text, output text, input image, and input audio content blocks, and may include template strings. */
  input: EvalItem[];
  /** The range of the score. Defaults to `[0, 1]`. */
  range?: number[];
}

/** The sampling parameters for the model. */
export interface GraderScoreModelSamplingParams {
  seed?: number | null;
  top_p?: number | null;
  temperature?: number | null;
  max_completions_tokens?: number | null;
  reasoning_effort?: ReasoningEffort;
}

/** A MultiGrader object combines the output of multiple graders to produce a single score. */
export interface GraderMulti {
  /** The object type, which is always `multi`. */
  type: "multi";
  /** The name of the grader. */
  name: string;
  graders: GraderMultiGraders;
  /** A formula to calculate the output based on grader results. */
  calculate_output: string;
}

export type GraderMultiGraders = GraderStringCheck | GraderTextSimilarity | GraderPython | GraderScoreModel | GraderLabelModel;

export interface RunGraderResponse {
  reward: number;
  metadata: RunGraderResponseMetadata;
  sub_rewards: Record<string, unknown>;
  model_grader_token_usage_per_model: Record<string, unknown>;
}

export interface RunGraderResponseMetadata {
  name: string;
  type: string;
  errors: RunGraderResponseMetadataErrors;
  execution_time: number;
  scores: Record<string, unknown>;
  token_usage: number | null;
  sampled_model_name: string | null;
}

export interface RunGraderResponseMetadataErrors {
  formula_parse_error: boolean;
  sample_parse_error: boolean;
  truncated_observation_error: boolean;
  unresponsive_reward_error: boolean;
  invalid_variable_error: boolean;
  other_error: boolean;
  python_grader_server_error: boolean;
  python_grader_server_error_type: string | null;
  python_grader_runtime_error: boolean;
  python_grader_runtime_error_details: string | null;
  model_grader_server_error: boolean;
  model_grader_refusal_error: boolean;
  model_grader_parse_error: boolean;
  model_grader_server_error_details: string | null;
}

export interface ValidateGraderRequest {
  /** The grader used for the fine-tuning job. */
  grader: ValidateGraderRequestGrader;
}

/** The grader used for the fine-tuning job. */
export type ValidateGraderRequestGrader = GraderStringCheck | GraderTextSimilarity | GraderPython | GraderScoreModel | GraderMulti;

export interface ValidateGraderResponse {
  /** The grader used for the fine-tuning job. */
  grader?: ValidateGraderRequestGrader;
}

export interface CreateFineTuningCheckpointPermissionRequest {
  /** The project identifiers to grant access to. */
  project_ids: string[];
}

export interface ListFineTuningCheckpointPermissionResponse {
  data: FineTuningCheckpointPermission[];
  object: "list";
  first_id?: string | null;
  last_id?: string | null;
  has_more: boolean;
}

/** The `checkpoint.permission` object represents a permission for a fine-tuned model checkpoint. */
export interface FineTuningCheckpointPermission {
  /** The permission identifier, which can be referenced in the API endpoints. */
  id: string;
  /** The Unix timestamp (in seconds) for when the permission was created. */
  created_at: number;
  /** The project identifier that the permission is for. */
  project_id: string;
  /** The object type, which is always "checkpoint.permission". */
  object: "checkpoint.permission";
}

export interface DeleteFineTuningCheckpointPermissionResponse {
  /** The ID of the fine-tuned model checkpoint permission that was deleted. */
  id: string;
  /** The object type, which is always "checkpoint.permission". */
  object: "checkpoint.permission";
  /** Whether the fine-tuned model checkpoint permission was successfully deleted. */
  deleted: boolean;
}

export type FineTuningCheckpointsPermissionsListOrder = "ascending" | "descending";

/** The `fine_tuning.job` object represents a fine-tuning job that has been created through the API. */
export interface FineTuningJob {
  /** The object identifier, which can be referenced in the API endpoints. */
  id: string;
  /** The Unix timestamp (in seconds) for when the fine-tuning job was created. */
  created_at: number;
  error: FineTuningJobError | null;
  fine_tuned_model: string | null;
  finished_at: number | null;
  /** The hyperparameters used for the fine-tuning job. This value will only be returned when running `supervised` jobs. */
  hyperparameters: FineTuningJobHyperparameters;
  /** The base model that is being fine-tuned. */
  model: string;
  /** The object type, which is always "fine_tuning.job". */
  object: "fine_tuning.job";
  /** The organization that owns the fine-tuning job. */
  organization_id: string;
  /** The compiled results file ID(s) for the fine-tuning job. You can retrieve the results with the [Files API](/docs/api-reference/files/retrieve-contents). */
  result_files: string[];
  /** The current status of the fine-tuning job, which can be either `validating_files`, `queued`, `running`, `succeeded`, `failed`, or `cancelled`. */
  status: FineTuningJobStatus;
  trained_tokens: number | null;
  /** The file ID used for training. You can retrieve the training data with the [Files API](/docs/api-reference/files/retrieve-contents). */
  training_file: string;
  validation_file: string | null;
  integrations?: FineTuningIntegration[] | null;
  /** The seed used for the fine-tuning job. */
  seed: number;
  estimated_finish?: number | null;
  method?: FineTuneMethod;
  metadata?: Metadata;
}

/** For fine-tuning jobs that have `failed`, this will contain more information on the cause of the failure. */
export interface FineTuningJobError {
  /** A machine-readable error code. */
  code: string;
  /** A human-readable error message. */
  message: string;
  param: string | null;
}

/** The hyperparameters used for the fine-tuning job. This value will only be returned when running `supervised` jobs. */
export interface FineTuningJobHyperparameters {
  batch_size?: FineTuningJobHyperparametersBatchSize | null;
  /**
   * Scaling factor for the learning rate. A smaller learning rate may be useful to avoid
   * overfitting.
   */
  learning_rate_multiplier?: FineTuningJobHyperparametersLearningRateMultiplier;
  /**
   * The number of epochs to train the model for. An epoch refers to one full cycle
   * through the training dataset.
   */
  n_epochs?: FineTuningJobHyperparametersBatchSize;
}

/**
 * Number of examples in each batch. A larger batch size means that model parameters
 * are updated less frequently, but with lower variance.
 */
export type FineTuningJobHyperparametersBatchSize = "auto" | number;

/**
 * Scaling factor for the learning rate. A smaller learning rate may be useful to avoid
 * overfitting.
 */
export type FineTuningJobHyperparametersLearningRateMultiplier = "auto" | number;

/** The current status of the fine-tuning job, which can be either `validating_files`, `queued`, `running`, `succeeded`, `failed`, or `cancelled`. */
export type FineTuningJobStatus = "validating_files" | "queued" | "running" | "succeeded" | "failed" | "cancelled";

export interface FineTuningIntegration {
  /** The type of the integration being enabled for the fine-tuning job */
  type: "wandb";
  /**
   * The settings for your integration with Weights and Biases. This payload specifies the project that
   * metrics will be sent to. Optionally, you can set an explicit display name for your run, add tags
   * to your run, and set a default entity (team, username, etc) to be associated with your run.
   */
  wandb: FineTuningIntegrationWandb;
}

/**
 * The settings for your integration with Weights and Biases. This payload specifies the project that
 * metrics will be sent to. Optionally, you can set an explicit display name for your run, add tags
 * to your run, and set a default entity (team, username, etc) to be associated with your run.
 */
export interface FineTuningIntegrationWandb {
  /** The name of the project that the new run will be created under. */
  project: string;
  name?: string | null;
  entity?: string | null;
  /**
   * A list of tags to be attached to the newly created run. These tags are passed through directly to WandB. Some
   * default tags are generated by OpenAI: "openai/finetune", "openai/{base-model}", "openai/{ftjob-abcdef}".
   */
  tags?: string[];
}

/** The method used for fine-tuning. */
export interface FineTuneMethod {
  /** The type of method. Is either `supervised`, `dpo`, or `reinforcement`. */
  type: FineTuneMethodType;
  supervised?: FineTuneSupervisedMethod;
  dpo?: FineTuneDpoMethod;
  reinforcement?: FineTuneReinforcementMethod;
}

/** The type of method. Is either `supervised`, `dpo`, or `reinforcement`. */
export type FineTuneMethodType = "supervised" | "dpo" | "reinforcement";

/** Configuration for the supervised fine-tuning method. */
export interface FineTuneSupervisedMethod {
  hyperparameters?: FineTuneSupervisedHyperparameters;
}

/** The hyperparameters used for the fine-tuning job. */
export interface FineTuneSupervisedHyperparameters {
  /** Number of examples in each batch. A larger batch size means that model parameters are updated less frequently, but with lower variance. */
  batch_size?: FineTuningJobHyperparametersBatchSize;
  /** Scaling factor for the learning rate. A smaller learning rate may be useful to avoid overfitting. */
  learning_rate_multiplier?: FineTuningJobHyperparametersLearningRateMultiplier;
  /** The number of epochs to train the model for. An epoch refers to one full cycle through the training dataset. */
  n_epochs?: FineTuningJobHyperparametersBatchSize;
}

/** Configuration for the DPO fine-tuning method. */
export interface FineTuneDpoMethod {
  hyperparameters?: FineTuneDpoHyperparameters;
}

/** The hyperparameters used for the DPO fine-tuning job. */
export interface FineTuneDpoHyperparameters {
  /** The beta value for the DPO method. A higher beta value will increase the weight of the penalty between the policy and reference model. */
  beta?: FineTuningJobHyperparametersLearningRateMultiplier;
  /** Number of examples in each batch. A larger batch size means that model parameters are updated less frequently, but with lower variance. */
  batch_size?: FineTuningJobHyperparametersBatchSize;
  /** Scaling factor for the learning rate. A smaller learning rate may be useful to avoid overfitting. */
  learning_rate_multiplier?: FineTuningJobHyperparametersLearningRateMultiplier;
  /** The number of epochs to train the model for. An epoch refers to one full cycle through the training dataset. */
  n_epochs?: FineTuningJobHyperparametersBatchSize;
}

/** Configuration for the reinforcement fine-tuning method. */
export interface FineTuneReinforcementMethod {
  /** The grader used for the fine-tuning job. */
  grader: FineTuneReinforcementMethodGrader;
  hyperparameters?: FineTuneReinforcementHyperparameters;
}

/** The grader used for the fine-tuning job. */
export type FineTuneReinforcementMethodGrader = GraderStringCheck | GraderTextSimilarity | GraderPython | GraderScoreModel | GraderMulti;

/** The hyperparameters used for the reinforcement fine-tuning job. */
export interface FineTuneReinforcementHyperparameters {
  /** Number of examples in each batch. A larger batch size means that model parameters are updated less frequently, but with lower variance. */
  batch_size?: FineTuningJobHyperparametersBatchSize;
  /** Scaling factor for the learning rate. A smaller learning rate may be useful to avoid overfitting. */
  learning_rate_multiplier?: FineTuningJobHyperparametersLearningRateMultiplier;
  /** The number of epochs to train the model for. An epoch refers to one full cycle through the training dataset. */
  n_epochs?: FineTuningJobHyperparametersBatchSize;
  /** Level of reasoning effort. */
  reasoning_effort?: FineTuneReinforcementHyperparametersReasoningEffort;
  /** Multiplier on amount of compute used for exploring search space during training. */
  compute_multiplier?: FineTuningJobHyperparametersLearningRateMultiplier;
  /** The number of training steps between evaluation runs. */
  eval_interval?: FineTuningJobHyperparametersBatchSize;
  /** Number of evaluation samples to generate per training step. */
  eval_samples?: FineTuningJobHyperparametersBatchSize;
}

/** Level of reasoning effort. */
export type FineTuneReinforcementHyperparametersReasoningEffort = "default" | "low" | "medium" | "high";

export interface ListFineTuningJobCheckpointsResponse {
  data: FineTuningJobCheckpoint[];
  object: "list";
  first_id?: string | null;
  last_id?: string | null;
  has_more: boolean;
}

/** The `fine_tuning.job.checkpoint` object represents a model checkpoint for a fine-tuning job that is ready to use. */
export interface FineTuningJobCheckpoint {
  /** The checkpoint identifier, which can be referenced in the API endpoints. */
  id: string;
  /** The Unix timestamp (in seconds) for when the checkpoint was created. */
  created_at: number;
  /** The name of the fine-tuned checkpoint model that is created. */
  fine_tuned_model_checkpoint: string;
  /** The step number that the checkpoint was created at. */
  step_number: number;
  /** Metrics at the step number during the fine-tuning job. */
  metrics: FineTuningJobCheckpointMetrics;
  /** The name of the fine-tuning job that this checkpoint was created from. */
  fine_tuning_job_id: string;
  /** The object type, which is always "fine_tuning.job.checkpoint". */
  object: "fine_tuning.job.checkpoint";
}

/** Metrics at the step number during the fine-tuning job. */
export interface FineTuningJobCheckpointMetrics {
  step?: number;
  train_loss?: number;
  train_mean_token_accuracy?: number;
  valid_loss?: number;
  valid_mean_token_accuracy?: number;
  full_valid_loss?: number;
  full_valid_mean_token_accuracy?: number;
}

export interface CreateFineTuningJobRequest {
  /**
   * The name of the model to fine-tune. You can select one of the
   * [supported models](/docs/guides/fine-tuning#which-models-can-be-fine-tuned).
   */
  model: CreateFineTuningJobRequestModel;
  /**
   * The ID of an uploaded file that contains training data.
   *
   * See [upload file](/docs/api-reference/files/create) for how to upload a file.
   *
   * Your dataset must be formatted as a JSONL file. Additionally, you must upload your file with the purpose `fine-tune`.
   *
   * The contents of the file should differ depending on if the model uses the [chat](/docs/api-reference/fine-tuning/chat-input), [completions](/docs/api-reference/fine-tuning/completions-input) format, or if the fine-tuning method uses the [preference](/docs/api-reference/fine-tuning/preference-input) format.
   *
   * See the [fine-tuning guide](/docs/guides/model-optimization) for more details.
   */
  training_file: string;
  /**
   * The hyperparameters used for the fine-tuning job.
   * This value is now deprecated in favor of `method`, and should be passed in under the `method` parameter.
   */
  hyperparameters?: CreateFineTuningJobRequestHyperparameters;
  /**
   * A string of up to 64 characters that will be added to your fine-tuned model name.
   *
   * For example, a `suffix` of "custom-model-name" would produce a model name like `ft:gpt-4o-mini:openai:custom-model-name:7p4lURel`.
   */
  suffix?: string | null;
  /**
   * The ID of an uploaded file that contains validation data.
   *
   * If you provide this file, the data is used to generate validation
   * metrics periodically during fine-tuning. These metrics can be viewed in
   * the fine-tuning results file.
   * The same data should not be present in both train and validation files.
   *
   * Your dataset must be formatted as a JSONL file. You must upload your file with the purpose `fine-tune`.
   *
   * See the [fine-tuning guide](/docs/guides/model-optimization) for more details.
   */
  validation_file?: string | null;
  /** A list of integrations to enable for your fine-tuning job. */
  integrations?: CreateFineTuningJobRequestIntegrationsItem[] | null;
  /**
   * The seed controls the reproducibility of the job. Passing in the same seed and job parameters should produce the same results, but may differ in rare cases.
   * If a seed is not specified, one will be generated for you.
   */
  seed?: number | null;
  method?: FineTuneMethod;
  metadata?: Metadata;
}

/**
 * The name of the model to fine-tune. You can select one of the
 * [supported models](/docs/guides/fine-tuning#which-models-can-be-fine-tuned).
 */
export type CreateFineTuningJobRequestModel = string | CreateFineTuningJobRequestModelString;

export type CreateFineTuningJobRequestModelString = "babbage-002" | "davinci-002" | "gpt-3.5-turbo" | "gpt-4o-mini";

/**
 * The hyperparameters used for the fine-tuning job.
 * This value is now deprecated in favor of `method`, and should be passed in under the `method` parameter.
 */
export interface CreateFineTuningJobRequestHyperparameters {
  /**
   * Number of examples in each batch. A larger batch size means that model parameters
   * are updated less frequently, but with lower variance.
   */
  batch_size?: CreateFineTuningJobRequestHyperparametersBatchSize;
  /**
   * Scaling factor for the learning rate. A smaller learning rate may be useful to avoid
   * overfitting.
   */
  learning_rate_multiplier?: CreateFineTuningJobRequestHyperparametersLearningRateMultiplier;
  /**
   * The number of epochs to train the model for. An epoch refers to one full cycle
   * through the training dataset.
   */
  n_epochs?: CreateFineTuningJobRequestHyperparametersBatchSize;
}

/**
 * Number of examples in each batch. A larger batch size means that model parameters
 * are updated less frequently, but with lower variance.
 */
export type CreateFineTuningJobRequestHyperparametersBatchSize = "auto" | number;

/**
 * Scaling factor for the learning rate. A smaller learning rate may be useful to avoid
 * overfitting.
 */
export type CreateFineTuningJobRequestHyperparametersLearningRateMultiplier = "auto" | number;

export interface CreateFineTuningJobRequestIntegrationsItem {
  /** The type of integration to enable. Currently, only "wandb" (Weights and Biases) is supported. */
  type: "wandb";
  /**
   * The settings for your integration with Weights and Biases. This payload specifies the project that
   * metrics will be sent to. Optionally, you can set an explicit display name for your run, add tags
   * to your run, and set a default entity (team, username, etc) to be associated with your run.
   */
  wandb: CreateFineTuningJobRequestIntegrationsItemWandb;
}

/**
 * The settings for your integration with Weights and Biases. This payload specifies the project that
 * metrics will be sent to. Optionally, you can set an explicit display name for your run, add tags
 * to your run, and set a default entity (team, username, etc) to be associated with your run.
 */
export interface CreateFineTuningJobRequestIntegrationsItemWandb {
  /** The name of the project that the new run will be created under. */
  project: string;
  /** A display name to set for the run. If not set, we will use the Job ID as the name. */
  name?: string | null;
  /**
   * The entity to use for the run. This allows you to set the team or username of the WandB user that you would
   * like associated with the run. If not set, the default entity for the registered WandB API key is used.
   */
  entity?: string | null;
  /**
   * A list of tags to be attached to the newly created run. These tags are passed through directly to WandB. Some
   * default tags are generated by OpenAI: "openai/finetune", "openai/{base-model}", "openai/{ftjob-abcdef}".
   */
  tags?: string[];
}

export interface ListPaginatedFineTuningJobsResponse {
  data: FineTuningJob[];
  has_more: boolean;
  object: "list";
}

export interface ListFineTuningJobEventsResponse {
  data: FineTuningJobEvent[];
  object: "list";
  has_more: boolean;
}

/** Fine-tuning job event object */
export interface FineTuningJobEvent {
  /** The object type, which is always "fine_tuning.job.event". */
  object: "fine_tuning.job.event";
  /** The object identifier. */
  id: string;
  /** The Unix timestamp (in seconds) for when the fine-tuning job was created. */
  created_at: number;
  /** The log level of the event. */
  level: FineTuningJobEventLevel;
  /** The message of the event. */
  message: string;
  /** The type of event. */
  type?: FineTuningJobEventType;
  /** The data associated with the event. */
  data?: Record<string, unknown>;
}

/** The log level of the event. */
export type FineTuningJobEventLevel = "info" | "warn" | "error";

/** The type of event. */
export type FineTuningJobEventType = "message" | "metrics";

/**
 * JSON request body for image edits.
 *
 * Use `images` (array of `ImageRefParam`) instead of multipart `image` uploads.
 * You can reference images via external URLs, data URLs, or uploaded file IDs.
 * JSON edits support GPT image models only; DALL-E edits require multipart (`dall-e-2` only).
 */
export interface EditImageBodyJsonParam {
  /** The model to use for image editing. */
  model?: EditImageBodyJsonParamModel | null;
  /**
   * Input image references to edit.
   * For GPT image models, you can provide up to 16 images.
   */
  images: ImageRefParam[];
  mask?: ImageRefParam;
  /** A text description of the desired image edit. */
  prompt: string;
  /** The number of edited images to generate. */
  n?: number | null;
  /** Output quality for GPT image models. */
  quality?: EditImageBodyJsonParamQuality | null;
  /** Controls fidelity to the original input image(s). */
  input_fidelity?: EditImageBodyJsonParamInputFidelity | null;
  /** Requested output image size. */
  size?: EditImageBodyJsonParamSize | null;
  /**
   * A unique identifier representing your end-user, which can help OpenAI
   * monitor and detect abuse.
   */
  user?: string;
  /** Output image format. Supported for GPT image models. */
  output_format?: EditImageBodyJsonParamOutputFormat | null;
  /** Compression level for `jpeg` or `webp` output. */
  output_compression?: number | null;
  /** Moderation level for GPT image models. */
  moderation?: EditImageBodyJsonParamModeration | null;
  /** Background behavior for generated image output. */
  background?: EditImageBodyJsonParamBackground | null;
  /** Stream partial image results as events. */
  stream?: boolean | null;
  partial_images?: PartialImages;
}

/** The model to use for image editing. */
export type EditImageBodyJsonParamModel = string | EditImageBodyJsonParamModelString;

export type EditImageBodyJsonParamModelString = "gpt-image-1.5" | "gpt-image-1" | "gpt-image-1-mini" | "chatgpt-image-latest";

/**
 * Reference an input image by either URL or uploaded file ID.
 * Provide exactly one of `image_url` or `file_id`.
 */
export type ImageRefParam = unknown | unknown;

export type EditImageBodyJsonParamQuality = "low" | "medium" | "high" | "auto";

export type EditImageBodyJsonParamInputFidelity = "high" | "low";

export type EditImageBodyJsonParamSize = "auto" | "1024x1024" | "1536x1024" | "1024x1536";

export type EditImageBodyJsonParamOutputFormat = "png" | "jpeg" | "webp";

export type EditImageBodyJsonParamModeration = "low" | "auto";

export type EditImageBodyJsonParamBackground = "transparent" | "opaque" | "auto";

export type PartialImages = number | null;

/** The response from the image generation endpoint. */
export interface ImagesResponse {
  /** The Unix timestamp (in seconds) of when the image was created. */
  created: number;
  /** The list of generated images. */
  data?: Image[];
  /** The background parameter used for the image generation. Either `transparent` or `opaque`. */
  background?: ImagesResponseBackground;
  /** The output format of the image generation. Either `png`, `webp`, or `jpeg`. */
  output_format?: ImagesResponseOutputFormat;
  /** The size of the image generated. Either `1024x1024`, `1024x1536`, or `1536x1024`. */
  size?: ImagesResponseSize;
  /** The quality of the image generated. Either `low`, `medium`, or `high`. */
  quality?: ImagesResponseQuality;
  usage?: ImageGenUsage;
}

/** Represents the content or the URL of an image generated by the OpenAI API. */
export interface Image {
  /** The base64-encoded JSON of the generated image. Returned by default for the GPT image models, and only present if `response_format` is set to `b64_json` for `dall-e-2` and `dall-e-3`. */
  b64_json?: string;
  /** When using `dall-e-2` or `dall-e-3`, the URL of the generated image if `response_format` is set to `url` (default value). Unsupported for the GPT image models. */
  url?: string;
  /** For `dall-e-3` only, the revised prompt that was used to generate the image. */
  revised_prompt?: string;
}

/** The background parameter used for the image generation. Either `transparent` or `opaque`. */
export type ImagesResponseBackground = "transparent" | "opaque";

/** The output format of the image generation. Either `png`, `webp`, or `jpeg`. */
export type ImagesResponseOutputFormat = "png" | "webp" | "jpeg";

/** The size of the image generated. Either `1024x1024`, `1024x1536`, or `1536x1024`. */
export type ImagesResponseSize = "1024x1024" | "1024x1536" | "1536x1024";

/** The quality of the image generated. Either `low`, `medium`, or `high`. */
export type ImagesResponseQuality = "low" | "medium" | "high";

/** For `gpt-image-1` only, the token usage information for the image generation. */
export interface ImageGenUsage {
  /** The number of tokens (images and text) in the input prompt. */
  input_tokens: number;
  /** The total number of tokens (images and text) used for the image generation. */
  total_tokens: number;
  /** The number of output tokens generated by the model. */
  output_tokens: number;
  output_tokens_details?: ImageGenOutputTokensDetails;
  input_tokens_details: ImageGenInputUsageDetails;
}

/** The output token details for the image generation. */
export interface ImageGenOutputTokensDetails {
  /** The number of image output tokens generated by the model. */
  image_tokens: number;
  /** The number of text output tokens generated by the model. */
  text_tokens: number;
}

/** The input tokens detailed information for the image generation. */
export interface ImageGenInputUsageDetails {
  /** The number of text tokens in the input prompt. */
  text_tokens: number;
  /** The number of image tokens in the input prompt. */
  image_tokens: number;
}

export interface CreateImageRequest {
  /** A text description of the desired image(s). The maximum length is 32000 characters for the GPT image models, 1000 characters for `dall-e-2` and 4000 characters for `dall-e-3`. */
  prompt: string;
  /** The model to use for image generation. One of `dall-e-2`, `dall-e-3`, or a GPT image model (`gpt-image-1`, `gpt-image-1-mini`, `gpt-image-1.5`). Defaults to `dall-e-2` unless a parameter specific to the GPT image models is used. */
  model?: CreateImageRequestModel | null;
  /** The number of images to generate. Must be between 1 and 10. For `dall-e-3`, only `n=1` is supported. */
  n?: number | null;
  /**
   * The quality of the image that will be generated.
   *
   * - `auto` (default value) will automatically select the best quality for the given model.
   * - `high`, `medium` and `low` are supported for the GPT image models.
   * - `hd` and `standard` are supported for `dall-e-3`.
   * - `standard` is the only option for `dall-e-2`.
   */
  quality?: CreateImageRequestQuality | null;
  /** The format in which generated images with `dall-e-2` and `dall-e-3` are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. This parameter isn't supported for the GPT image models, which always return base64-encoded images. */
  response_format?: CreateImageRequestResponseFormat | null;
  /** The format in which the generated images are returned. This parameter is only supported for the GPT image models. Must be one of `png`, `jpeg`, or `webp`. */
  output_format?: CreateImageRequestOutputFormat | null;
  /** The compression level (0-100%) for the generated images. This parameter is only supported for the GPT image models with the `webp` or `jpeg` output formats, and defaults to 100. */
  output_compression?: number | null;
  /**
   * Generate the image in streaming mode. Defaults to `false`. See the
   * [Image generation guide](/docs/guides/image-generation) for more information.
   * This parameter is only supported for the GPT image models.
   */
  stream?: boolean | null;
  partial_images?: PartialImages;
  /** The size of the generated images. For `gpt-image-2` and `gpt-image-2-2026-04-21`, arbitrary resolutions are supported as `WIDTHxHEIGHT` strings, for example `1536x864`. Width and height must both be divisible by 16 and the requested aspect ratio must be between 1:3 and 3:1. Resolutions above `2560x1440` are experimental, and the maximum supported resolution is `3840x2160`. The requested size must also satisfy the model's current pixel and edge limits. The standard sizes `1024x1024`, `1536x1024`, and `1024x1536` are supported by the GPT image models; `auto` is supported for models that allow automatic sizing. For `dall-e-2`, use one of `256x256`, `512x512`, or `1024x1024`. For `dall-e-3`, use one of `1024x1024`, `1792x1024`, or `1024x1792`. */
  size?: CreateImageRequestSize | null;
  /** Control the content-moderation level for images generated by the GPT image models. Must be either `low` for less restrictive filtering or `auto` (default value). */
  moderation?: CreateImageRequestModeration | null;
  /**
   * Allows to set transparency for the background of the generated image(s).
   * This parameter is only supported for the GPT image models. Must be one of
   * `transparent`, `opaque` or `auto` (default value). When `auto` is used, the
   * model will automatically determine the best background for the image.
   *
   * If `transparent`, the output format needs to support transparency, so it
   * should be set to either `png` (default value) or `webp`.
   */
  background?: CreateImageRequestBackground | null;
  /** The style of the generated images. This parameter is only supported for `dall-e-3`. Must be one of `vivid` or `natural`. Vivid causes the model to lean towards generating hyper-real and dramatic images. Natural causes the model to produce more natural, less hyper-real looking images. */
  style?: CreateImageRequestStyle | null;
  /** A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices#end-user-ids). */
  user?: string;
}

/** The model to use for image generation. One of `dall-e-2`, `dall-e-3`, or a GPT image model (`gpt-image-1`, `gpt-image-1-mini`, `gpt-image-1.5`). Defaults to `dall-e-2` unless a parameter specific to the GPT image models is used. */
export type CreateImageRequestModel = string | CreateImageRequestModelString;

export type CreateImageRequestModelString = "gpt-image-1.5" | "dall-e-2" | "dall-e-3" | "gpt-image-1" | "gpt-image-1-mini";

/**
 * The quality of the image that will be generated.
 *
 * - `auto` (default value) will automatically select the best quality for the given model.
 * - `high`, `medium` and `low` are supported for the GPT image models.
 * - `hd` and `standard` are supported for `dall-e-3`.
 * - `standard` is the only option for `dall-e-2`.
 */
export type CreateImageRequestQuality = "standard" | "hd" | "low" | "medium" | "high" | "auto";

/** The format in which generated images with `dall-e-2` and `dall-e-3` are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. This parameter isn't supported for the GPT image models, which always return base64-encoded images. */
export type CreateImageRequestResponseFormat = "url" | "b64_json";

/** The format in which the generated images are returned. This parameter is only supported for the GPT image models. Must be one of `png`, `jpeg`, or `webp`. */
export type CreateImageRequestOutputFormat = "png" | "jpeg" | "webp";

/** The size of the generated images. For `gpt-image-2` and `gpt-image-2-2026-04-21`, arbitrary resolutions are supported as `WIDTHxHEIGHT` strings, for example `1536x864`. Width and height must both be divisible by 16 and the requested aspect ratio must be between 1:3 and 3:1. Resolutions above `2560x1440` are experimental, and the maximum supported resolution is `3840x2160`. The requested size must also satisfy the model's current pixel and edge limits. The standard sizes `1024x1024`, `1536x1024`, and `1024x1536` are supported by the GPT image models; `auto` is supported for models that allow automatic sizing. For `dall-e-2`, use one of `256x256`, `512x512`, or `1024x1024`. For `dall-e-3`, use one of `1024x1024`, `1792x1024`, or `1024x1792`. */
export type CreateImageRequestSize = string | CreateImageRequestSizeString;

export type CreateImageRequestSizeString = "auto" | "1024x1024" | "1536x1024" | "1024x1536" | "256x256" | "512x512" | "1792x1024" | "1024x1792";

/** Control the content-moderation level for images generated by the GPT image models. Must be either `low` for less restrictive filtering or `auto` (default value). */
export type CreateImageRequestModeration = "low" | "auto";

/**
 * Allows to set transparency for the background of the generated image(s).
 * This parameter is only supported for the GPT image models. Must be one of
 * `transparent`, `opaque` or `auto` (default value). When `auto` is used, the
 * model will automatically determine the best background for the image.
 *
 * If `transparent`, the output format needs to support transparency, so it
 * should be set to either `png` (default value) or `webp`.
 */
export type CreateImageRequestBackground = "transparent" | "opaque" | "auto";

/** The style of the generated images. This parameter is only supported for `dall-e-3`. Must be one of `vivid` or `natural`. Vivid causes the model to lean towards generating hyper-real and dramatic images. Natural causes the model to produce more natural, less hyper-real looking images. */
export type CreateImageRequestStyle = "vivid" | "natural";

export interface CreateImageVariationRequest {
  /** The image to use as the basis for the variation(s). Must be a valid PNG file, less than 4MB, and square. */
  image: Uint8Array | Blob;
  /** The model to use for image generation. Only `dall-e-2` is supported at this time. */
  model?: CreateImageVariationRequestModel | null;
  /** The number of images to generate. Must be between 1 and 10. */
  n?: number | null;
  /** The format in which the generated images are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. */
  response_format?: CreateImageVariationRequestResponseFormat | null;
  /** The size of the generated images. Must be one of `256x256`, `512x512`, or `1024x1024`. */
  size?: CreateImageVariationRequestSize | null;
  /** A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices#end-user-ids). */
  user?: string;
}

/** The model to use for image generation. Only `dall-e-2` is supported at this time. */
export type CreateImageVariationRequestModel = string | "dall-e-2";

/** The format in which the generated images are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. */
export type CreateImageVariationRequestResponseFormat = "url" | "b64_json";

/** The size of the generated images. Must be one of `256x256`, `512x512`, or `1024x1024`. */
export type CreateImageVariationRequestSize = "256x256" | "512x512" | "1024x1024";

export interface DeleteModelResponse {
  id: string;
  deleted: boolean;
  object: string;
}

export interface ListModelsResponse {
  object: "list";
  data: Model[];
}

/** Describes an OpenAI model offering that can be used with the API. */
export interface Model {
  /** The model identifier, which can be referenced in the API endpoints. */
  id: string;
  /** The Unix timestamp (in seconds) when the model was created. */
  created: number;
  /** The object type, which is always "model". */
  object: "model";
  /** The organization that owns the model. */
  owned_by: string;
}

export interface CreateModerationRequest {
  /**
   * Input (or inputs) to classify. Can be a single string, an array of strings, or
   * an array of multi-modal input objects similar to other models.
   */
  input: CreateModerationRequestInput;
  /**
   * The content moderation model you would like to use. Learn more in
   * [the moderation guide](/docs/guides/moderation), and learn about
   * available models [here](/docs/models#moderation).
   */
  model?: CreateModerationRequestModel;
}

/**
 * Input (or inputs) to classify. Can be a single string, an array of strings, or
 * an array of multi-modal input objects similar to other models.
 */
export type CreateModerationRequestInput = string | string[] | CreateModerationRequestInputArrayItem[];

export type CreateModerationRequestInputArrayItem = CreateModerationRequestInputArrayItemImageUrl | CreateModerationRequestInputArrayItemText;

/** An object describing an image to classify. */
export interface CreateModerationRequestInputArrayItemImageUrl {
  /** Always `image_url`. */
  type: "image_url";
  /** Contains either an image URL or a data URL for a base64 encoded image. */
  image_url: CreateModerationRequestInputArrayItemImageUrlImageUrl;
}

/** Contains either an image URL or a data URL for a base64 encoded image. */
export interface CreateModerationRequestInputArrayItemImageUrlImageUrl {
  /** Either a URL of the image or the base64 encoded image data. */
  url: string;
}

/** An object describing text to classify. */
export interface CreateModerationRequestInputArrayItemText {
  /** Always `text`. */
  type: "text";
  /** A string of text to classify. */
  text: string;
}

/**
 * The content moderation model you would like to use. Learn more in
 * [the moderation guide](/docs/guides/moderation), and learn about
 * available models [here](/docs/models#moderation).
 */
export type CreateModerationRequestModel = string | CreateModerationRequestModelString;

export type CreateModerationRequestModelString = "omni-moderation-latest" | "omni-moderation-2024-09-26" | "text-moderation-latest" | "text-moderation-stable";

/** Represents if a given text input is potentially harmful. */
export interface CreateModerationResponse {
  /** The unique identifier for the moderation request. */
  id: string;
  /** The model used to generate the moderation results. */
  model: string;
  /** A list of moderation objects. */
  results: CreateModerationResponseResultsItem[];
}

export interface CreateModerationResponseResultsItem {
  /** Whether any of the below categories are flagged. */
  flagged: boolean;
  /** A list of the categories, and whether they are flagged or not. */
  categories: CreateModerationResponseResultsItemCategories;
  /** A list of the categories along with their scores as predicted by model. */
  category_scores: CreateModerationResponseResultsItemCategoryScores;
  /** A list of the categories along with the input type(s) that the score applies to. */
  category_applied_input_types: CreateModerationResponseResultsItemCategoryAppliedInputTypes;
}

/** A list of the categories, and whether they are flagged or not. */
export interface CreateModerationResponseResultsItemCategories {
  /** Content that expresses, incites, or promotes hate based on race, gender, ethnicity, religion, nationality, sexual orientation, disability status, or caste. Hateful content aimed at non-protected groups (e.g., chess players) is harassment. */
  hate: boolean;
  /** Hateful content that also includes violence or serious harm towards the targeted group based on race, gender, ethnicity, religion, nationality, sexual orientation, disability status, or caste. */
  "hate/threatening": boolean;
  /** Content that expresses, incites, or promotes harassing language towards any target. */
  harassment: boolean;
  /** Harassment content that also includes violence or serious harm towards any target. */
  "harassment/threatening": boolean;
  illicit: boolean | null;
  "illicit/violent": boolean | null;
  /** Content that promotes, encourages, or depicts acts of self-harm, such as suicide, cutting, and eating disorders. */
  "self-harm": boolean;
  /** Content where the speaker expresses that they are engaging or intend to engage in acts of self-harm, such as suicide, cutting, and eating disorders. */
  "self-harm/intent": boolean;
  /** Content that encourages performing acts of self-harm, such as suicide, cutting, and eating disorders, or that gives instructions or advice on how to commit such acts. */
  "self-harm/instructions": boolean;
  /** Content meant to arouse sexual excitement, such as the description of sexual activity, or that promotes sexual services (excluding sex education and wellness). */
  sexual: boolean;
  /** Sexual content that includes an individual who is under 18 years old. */
  "sexual/minors": boolean;
  /** Content that depicts death, violence, or physical injury. */
  violence: boolean;
  /** Content that depicts death, violence, or physical injury in graphic detail. */
  "violence/graphic": boolean;
}

/** A list of the categories along with their scores as predicted by model. */
export interface CreateModerationResponseResultsItemCategoryScores {
  /** The score for the category 'hate'. */
  hate: number;
  /** The score for the category 'hate/threatening'. */
  "hate/threatening": number;
  /** The score for the category 'harassment'. */
  harassment: number;
  /** The score for the category 'harassment/threatening'. */
  "harassment/threatening": number;
  /** The score for the category 'illicit'. */
  illicit: number;
  /** The score for the category 'illicit/violent'. */
  "illicit/violent": number;
  /** The score for the category 'self-harm'. */
  "self-harm": number;
  /** The score for the category 'self-harm/intent'. */
  "self-harm/intent": number;
  /** The score for the category 'self-harm/instructions'. */
  "self-harm/instructions": number;
  /** The score for the category 'sexual'. */
  sexual: number;
  /** The score for the category 'sexual/minors'. */
  "sexual/minors": number;
  /** The score for the category 'violence'. */
  violence: number;
  /** The score for the category 'violence/graphic'. */
  "violence/graphic": number;
}

/** A list of the categories along with the input type(s) that the score applies to. */
export interface CreateModerationResponseResultsItemCategoryAppliedInputTypes {
  /** The applied input type(s) for the category 'hate'. */
  hate: "text"[];
  /** The applied input type(s) for the category 'hate/threatening'. */
  "hate/threatening": "text"[];
  /** The applied input type(s) for the category 'harassment'. */
  harassment: "text"[];
  /** The applied input type(s) for the category 'harassment/threatening'. */
  "harassment/threatening": "text"[];
  /** The applied input type(s) for the category 'illicit'. */
  illicit: "text"[];
  /** The applied input type(s) for the category 'illicit/violent'. */
  "illicit/violent": "text"[];
  /** The applied input type(s) for the category 'self-harm'. */
  "self-harm": CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmItem[];
  /** The applied input type(s) for the category 'self-harm/intent'. */
  "self-harm/intent": CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmItem[];
  /** The applied input type(s) for the category 'self-harm/instructions'. */
  "self-harm/instructions": CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmItem[];
  /** The applied input type(s) for the category 'sexual'. */
  sexual: CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmItem[];
  /** The applied input type(s) for the category 'sexual/minors'. */
  "sexual/minors": "text"[];
  /** The applied input type(s) for the category 'violence'. */
  violence: CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmItem[];
  /** The applied input type(s) for the category 'violence/graphic'. */
  "violence/graphic": CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmItem[];
}

export type CreateModerationResponseResultsItemCategoryAppliedInputTypesSelfHarmItem = "text" | "image";

export type ProjectsGroupsRolesListOrder = "asc" | "desc";

export type ProjectsRolesListOrder = "asc" | "desc";

export type ProjectsUsersRolesListOrder = "asc" | "desc";

/** Realtime session object configuration. */
export interface RealtimeSessionCreateRequestGa {
  /** The type of session to create. Always `realtime` for the Realtime API. */
  type: "realtime";
  /**
   * The set of modalities the model can respond with. It defaults to `["audio"]`, indicating
   * that the model will respond with audio plus a transcript. `["text"]` can be used to make
   * the model respond with text only. It is not possible to request both `text` and `audio` at the same time.
   */
  output_modalities?: RealtimeSessionCreateRequestGaOutputModalitiesItem[];
  /** The Realtime model used for this session. */
  model?: RealtimeSessionCreateRequestGaModel;
  /**
   * The default system instructions (i.e. system message) prepended to model calls. This field allows the client to guide the model on desired responses. The model can be instructed on response content and format, (e.g. "be extremely succinct", "act friendly", "here are examples of good responses") and on audio behavior (e.g. "talk quickly", "inject emotion into your voice", "laugh frequently"). The instructions are not guaranteed to be followed by the model, but they provide guidance to the model on the desired behavior.
   *
   * Note that the server sets default instructions which will be used if this field is not set and are visible in the `session.created` event at the start of the session.
   */
  instructions?: string;
  /** Configuration for input and output audio. */
  audio?: RealtimeSessionCreateRequestGaAudio;
  /**
   * Additional fields to include in server outputs.
   *
   * `item.input_audio_transcription.logprobs`: Include logprobs for input audio transcription.
   */
  include?: "item.input_audio_transcription.logprobs"[];
  /**
   * Realtime API can write session traces to the [Traces Dashboard](https://platform.openai.com/logs?api=traces). Set to null to disable tracing. Once
   * tracing is enabled for a session, the configuration cannot be modified.
   *
   * `auto` will create a trace for the session with default values for the
   * workflow name, group id, and metadata.
   */
  tracing?: RealtimeSessionCreateRequestGaTracing | null;
  /** Tools available to the model. */
  tools?: RealtimeSessionCreateRequestGaToolsItem[];
  /**
   * How the model chooses tools. Provide one of the string modes or force a specific
   * function/MCP tool.
   */
  tool_choice?: RealtimeSessionCreateRequestGaToolChoice;
  /**
   * Whether the model may call multiple tools in parallel. Only supported by
   * reasoning Realtime models such as `gpt-realtime-2`.
   */
  parallel_tool_calls?: boolean;
  reasoning?: RealtimeReasoning;
  /**
   * Maximum number of output tokens for a single assistant response,
   * inclusive of tool calls. Provide an integer between 1 and 4096 to
   * limit output tokens, or `inf` for the maximum available tokens for a
   * given model. Defaults to `inf`.
   */
  max_output_tokens?: RealtimeSessionCreateRequestGaMaxOutputTokens;
  truncation?: RealtimeTruncation;
  prompt?: Prompt;
}

export type RealtimeSessionCreateRequestGaOutputModalitiesItem = "text" | "audio";

/** The Realtime model used for this session. */
export type RealtimeSessionCreateRequestGaModel = string | RealtimeSessionCreateRequestGaModelString;

export type RealtimeSessionCreateRequestGaModelString = "gpt-realtime" | "gpt-realtime-1.5" | "gpt-realtime-2" | "gpt-realtime-2025-08-28" | "gpt-4o-realtime-preview" | "gpt-4o-realtime-preview-2024-10-01" | "gpt-4o-realtime-preview-2024-12-17" | "gpt-4o-realtime-preview-2025-06-03" | "gpt-4o-mini-realtime-preview" | "gpt-4o-mini-realtime-preview-2024-12-17" | "gpt-realtime-mini" | "gpt-realtime-mini-2025-10-06" | "gpt-realtime-mini-2025-12-15" | "gpt-audio-1.5" | "gpt-audio-mini" | "gpt-audio-mini-2025-10-06" | "gpt-audio-mini-2025-12-15";

/** Configuration for input and output audio. */
export interface RealtimeSessionCreateRequestGaAudio {
  input?: RealtimeSessionCreateRequestGaAudioInput;
  output?: RealtimeSessionCreateRequestGaAudioOutput;
}

export interface RealtimeSessionCreateRequestGaAudioInput {
  format?: RealtimeAudioFormats;
  transcription?: AudioTranscription;
  /**
   * Configuration for input audio noise reduction. This can be set to `null` to turn off.
   * Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
   * Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.
   */
  noise_reduction?: RealtimeSessionCreateRequestGaAudioInputNoiseReduction;
  turn_detection?: RealtimeTurnDetection;
}

export type RealtimeAudioFormats = RealtimeAudioFormatsAudioPcm | RealtimeAudioFormatsAudioPcmu | RealtimeAudioFormatsAudioPcma;

/** The PCM audio format. Only a 24kHz sample rate is supported. */
export interface RealtimeAudioFormatsAudioPcm {
  /** The audio format. Always `audio/pcm`. */
  type?: "audio/pcm";
  /** The sample rate of the audio. Always `24000`. */
  rate?: 24000;
}

/** The G.711 μ-law format. */
export interface RealtimeAudioFormatsAudioPcmu {
  /** The audio format. Always `audio/pcmu`. */
  type?: "audio/pcmu";
}

/** The G.711 A-law format. */
export interface RealtimeAudioFormatsAudioPcma {
  /** The audio format. Always `audio/pcma`. */
  type?: "audio/pcma";
}

export interface AudioTranscription {
  /** The model to use for transcription. Current options are `whisper-1`, `gpt-4o-mini-transcribe`, `gpt-4o-mini-transcribe-2025-12-15`, `gpt-4o-transcribe`, `gpt-4o-transcribe-diarize`, and `gpt-realtime-whisper`. Use `gpt-4o-transcribe-diarize` when you need diarization with speaker labels. */
  model?: AudioTranscriptionModel;
  /**
   * The language of the input audio. Supplying the input language in
   * [ISO-639-1](https://en.wikipedia.org/wiki/List_of_ISO_639-1_codes) (e.g. `en`) format
   * will improve accuracy and latency.
   */
  language?: string;
  /**
   * An optional text to guide the model's style or continue a previous audio
   * segment.
   * For `whisper-1`, the [prompt is a list of keywords](/docs/guides/speech-to-text#prompting).
   * For `gpt-4o-transcribe` models (excluding `gpt-4o-transcribe-diarize`), the prompt is a free text string, for example "expect words related to technology".
   * Prompt is not supported with `gpt-realtime-whisper` in GA Realtime sessions.
   */
  prompt?: string;
  /**
   * Controls how long the model waits before emitting transcription text.
   * Higher values can improve transcription accuracy at the cost of latency.
   * Only supported with `gpt-realtime-whisper` in GA Realtime sessions.
   */
  delay?: AudioTranscriptionDelay;
}

/** The model to use for transcription. Current options are `whisper-1`, `gpt-4o-mini-transcribe`, `gpt-4o-mini-transcribe-2025-12-15`, `gpt-4o-transcribe`, `gpt-4o-transcribe-diarize`, and `gpt-realtime-whisper`. Use `gpt-4o-transcribe-diarize` when you need diarization with speaker labels. */
export type AudioTranscriptionModel = string | AudioTranscriptionModelString;

export type AudioTranscriptionModelString = "whisper-1" | "gpt-4o-mini-transcribe" | "gpt-4o-mini-transcribe-2025-12-15" | "gpt-4o-transcribe" | "gpt-4o-transcribe-diarize" | "gpt-realtime-whisper";

/**
 * Controls how long the model waits before emitting transcription text.
 * Higher values can improve transcription accuracy at the cost of latency.
 * Only supported with `gpt-realtime-whisper` in GA Realtime sessions.
 */
export type AudioTranscriptionDelay = "minimal" | "low" | "medium" | "high" | "xhigh";

/**
 * Configuration for input audio noise reduction. This can be set to `null` to turn off.
 * Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
 * Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.
 */
export interface RealtimeSessionCreateRequestGaAudioInputNoiseReduction {
  type?: NoiseReductionType;
}

/** Type of noise reduction. `near_field` is for close-talking microphones such as headphones, `far_field` is for far-field microphones such as laptop or conference room microphones. */
export type NoiseReductionType = "near_field" | "far_field";

/**
 * Configuration for turn detection, ether Server VAD or Semantic VAD. This can be set to `null` to turn off, in which case the client must manually trigger model response.
 *
 * Server VAD means that the model will detect the start and end of speech based on audio volume and respond at the end of user speech.
 *
 * Semantic VAD is more advanced and uses a turn detection model (in conjunction with VAD) to semantically estimate whether the user has finished speaking, then dynamically sets a timeout based on this probability. For example, if user audio trails off with "uhhm", the model will score a low probability of turn end and wait longer for the user to continue speaking. This can be useful for more natural conversations, but may have a higher latency.
 *
 * For `gpt-realtime-whisper` transcription sessions, turn detection must be
 * set to `null`; VAD is not supported.
 */
export type RealtimeTurnDetection = RealtimeTurnDetectionServerVad | RealtimeTurnDetectionSemanticVad;

/** Decode a `RealtimeTurnDetection` by its `type` discriminator (unknown value falls through). */
export function decodeRealtimeTurnDetection(data: unknown): RealtimeTurnDetection {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "server_vad":
      return data as RealtimeTurnDetectionServerVad;
    case "semantic_vad":
      return data as RealtimeTurnDetectionSemanticVad;
    default:
      return data as RealtimeTurnDetection;
  }
}

/** Server-side voice activity detection (VAD) which flips on when user speech is detected and off after a period of silence. */
export interface RealtimeTurnDetectionServerVad {
  /** Type of turn detection, `server_vad` to turn on simple Server VAD. */
  type: "server_vad";
  /**
   * Used only for `server_vad` mode. Activation threshold for VAD (0.0 to 1.0), this defaults to 0.5. A
   * higher threshold will require louder audio to activate the model, and
   * thus might perform better in noisy environments.
   */
  threshold?: number;
  /**
   * Used only for `server_vad` mode. Amount of audio to include before the VAD detected speech (in
   * milliseconds). Defaults to 300ms.
   */
  prefix_padding_ms?: number;
  /**
   * Used only for `server_vad` mode. Duration of silence to detect speech stop (in milliseconds). Defaults
   * to 500ms. With shorter values the model will respond more quickly,
   * but may jump in on short pauses from the user.
   */
  silence_duration_ms?: number;
  /**
   * Whether or not to automatically generate a response when a VAD stop event occurs. If `interrupt_response` is set to `false` this may fail to create a response if the model is already responding.
   *
   * If both `create_response` and `interrupt_response` are set to `false`, the model will never respond automatically but VAD events will still be emitted.
   */
  create_response?: boolean;
  /**
   * Whether or not to automatically interrupt (cancel) any ongoing response with output to the default
   * conversation (i.e. `conversation` of `auto`) when a VAD start event occurs. If `true` then the response will be cancelled, otherwise it will continue until complete.
   *
   * If both `create_response` and `interrupt_response` are set to `false`, the model will never respond automatically but VAD events will still be emitted.
   */
  interrupt_response?: boolean;
  idle_timeout_ms?: number | null;
}

/** Server-side semantic turn detection which uses a model to determine when the user has finished speaking. */
export interface RealtimeTurnDetectionSemanticVad {
  /** Type of turn detection, `semantic_vad` to turn on Semantic VAD. */
  type: "semantic_vad";
  /** Used only for `semantic_vad` mode. The eagerness of the model to respond. `low` will wait longer for the user to continue speaking, `high` will respond more quickly. `auto` is the default and is equivalent to `medium`. `low`, `medium`, and `high` have max timeouts of 8s, 4s, and 2s respectively. */
  eagerness?: RealtimeTurnDetectionSemanticVadEagerness;
  /** Whether or not to automatically generate a response when a VAD stop event occurs. */
  create_response?: boolean;
  /**
   * Whether or not to automatically interrupt any ongoing response with output to the default
   * conversation (i.e. `conversation` of `auto`) when a VAD start event occurs.
   */
  interrupt_response?: boolean;
}

/** Used only for `semantic_vad` mode. The eagerness of the model to respond. `low` will wait longer for the user to continue speaking, `high` will respond more quickly. `auto` is the default and is equivalent to `medium`. `low`, `medium`, and `high` have max timeouts of 8s, 4s, and 2s respectively. */
export type RealtimeTurnDetectionSemanticVadEagerness = "low" | "medium" | "high" | "auto";

export interface RealtimeSessionCreateRequestGaAudioOutput {
  format?: RealtimeAudioFormats;
  voice?: VoiceIdsOrCustomVoice;
  /**
   * The speed of the model's spoken response as a multiple of the original speed.
   * 1.0 is the default speed. 0.25 is the minimum speed. 1.5 is the maximum speed. This value can only be changed in between model turns, not while a response is in progress.
   *
   * This parameter is a post-processing adjustment to the audio after it is generated, it's
   * also possible to prompt the model to speak faster or slower.
   */
  speed?: number;
}

/**
 * Realtime API can write session traces to the [Traces Dashboard](https://platform.openai.com/logs?api=traces). Set to null to disable tracing. Once
 * tracing is enabled for a session, the configuration cannot be modified.
 *
 * `auto` will create a trace for the session with default values for the
 * workflow name, group id, and metadata.
 */
export type RealtimeSessionCreateRequestGaTracing = "auto" | RealtimeSessionCreateRequestGaTracingTracingConfiguration;

/** Granular configuration for tracing. */
export interface RealtimeSessionCreateRequestGaTracingTracingConfiguration {
  /**
   * The name of the workflow to attach to this trace. This is used to
   * name the trace in the Traces Dashboard.
   */
  workflow_name?: string;
  /**
   * The group id to attach to this trace to enable filtering and
   * grouping in the Traces Dashboard.
   */
  group_id?: string;
  /**
   * The arbitrary metadata to attach to this trace to enable
   * filtering in the Traces Dashboard.
   */
  metadata?: Record<string, unknown>;
}

export type RealtimeSessionCreateRequestGaToolsItem = RealtimeFunctionTool | McpTool;

export interface RealtimeFunctionTool {
  /** The type of the tool, i.e. `function`. */
  type?: "function";
  /** The name of the function. */
  name?: string;
  /**
   * The description of the function, including guidance on when and how
   * to call it, and guidance about what to tell the user when calling
   * (if anything).
   */
  description?: string;
  /** Parameters of the function in JSON Schema. */
  parameters?: Record<string, unknown>;
}

/**
 * How the model chooses tools. Provide one of the string modes or force a specific
 * function/MCP tool.
 */
export type RealtimeSessionCreateRequestGaToolChoice = ToolChoiceOptions | ToolChoiceFunction | ToolChoiceMcp;

/**
 * Controls which (if any) tool is called by the model.
 *
 * `none` means the model will not call any tool and instead generates a message.
 *
 * `auto` means the model can pick between generating a message or calling one or
 * more tools.
 *
 * `required` means the model must call one or more tools.
 */
export type ToolChoiceOptions = "none" | "auto" | "required";

/** Use this option to force the model to call a specific function. */
export interface ToolChoiceFunction {
  /** For function calling, the type is always `function`. */
  type: "function";
  /** The name of the function to call. */
  name: string;
}

/** Use this option to force the model to call a specific tool on a remote MCP server. */
export interface ToolChoiceMcp {
  /** For MCP tools, the type is always `mcp`. */
  type: "mcp";
  /** The label of the MCP server to use. */
  server_label: string;
  name?: string | null;
}

/** Configuration for reasoning-capable Realtime models such as `gpt-realtime-2`. */
export interface RealtimeReasoning {
  effort?: RealtimeReasoningEffort;
}

/**
 * Constrains effort on reasoning for reasoning-capable Realtime models such as
 * `gpt-realtime-2`.
 */
export type RealtimeReasoningEffort = "minimal" | "low" | "medium" | "high" | "xhigh";

/**
 * Maximum number of output tokens for a single assistant response,
 * inclusive of tool calls. Provide an integer between 1 and 4096 to
 * limit output tokens, or `inf` for the maximum available tokens for a
 * given model. Defaults to `inf`.
 */
export type RealtimeSessionCreateRequestGaMaxOutputTokens = number | "inf";

/**
 * When the number of tokens in a conversation exceeds the model's input token limit, the conversation be truncated, meaning messages (starting from the oldest) will not be included in the model's context. A 32k context model with 4,096 max output tokens can only include 28,224 tokens in the context before truncation occurs.
 *
 * Clients can configure truncation behavior to truncate with a lower max token limit, which is an effective way to control token usage and cost.
 *
 * Truncation will reduce the number of cached tokens on the next turn (busting the cache), since messages are dropped from the beginning of the context. However, clients can also configure truncation to retain messages up to a fraction of the maximum context size, which will reduce the need for future truncations and thus improve the cache rate.
 *
 * Truncation can be disabled entirely, which means the server will never truncate but would instead return an error if the conversation exceeds the model's input token limit.
 */
export type RealtimeTruncation = RealtimeTruncationString | RealtimeTruncationRetentionRatio;

/** The truncation strategy to use for the session. `auto` is the default truncation strategy. `disabled` will disable truncation and emit errors when the conversation exceeds the input token limit. */
export type RealtimeTruncationString = "auto" | "disabled";

/** Retain a fraction of the conversation tokens when the conversation exceeds the input token limit. This allows you to amortize truncations across multiple turns, which can help improve cached token usage. */
export interface RealtimeTruncationRetentionRatio {
  /** Use retention ratio truncation. */
  type: "retention_ratio";
  /** Fraction of post-instruction conversation tokens to retain (`0.0` - `1.0`) when the conversation exceeds the input token limit. Setting this to `0.8` means that messages will be dropped until 80% of the maximum allowed tokens are used. This helps reduce the frequency of truncations and improve cache rates. */
  retention_ratio: number;
  /** Optional custom token limits for this truncation strategy. If not provided, the model's default token limits will be used. */
  token_limits?: RealtimeTruncationRetentionRatioTokenLimits;
}

/** Optional custom token limits for this truncation strategy. If not provided, the model's default token limits will be used. */
export interface RealtimeTruncationRetentionRatioTokenLimits {
  /** Maximum tokens allowed in the conversation after instructions (which including tool definitions). For example, setting this to 5,000 would mean that truncation would occur when the conversation exceeds 5,000 tokens after instructions. This cannot be higher than the model's context window size minus the maximum output tokens. */
  post_instructions?: number;
}

/**
 * Reference to a prompt template and its variables.
 * [Learn more](/docs/guides/text?api-mode=responses#reusable-prompts).
 */
export interface Prompt {
  /** The unique identifier of the prompt template to use. */
  id: string;
  version?: string | null;
  variables?: ResponsePromptVariables;
}

export type ResponsePromptVariables = Record<string, ResponsePromptVariablesValue> | null;

export type ResponsePromptVariablesValue = string | InputTextContent | InputImageContent | InputFileContent;

/**
 * Parameters required to initiate a realtime call and receive the SDP answer
 * needed to complete a WebRTC peer connection. Provide an SDP offer generated
 * by your client and optionally configure the session that will answer the call.
 */
export interface RealtimeCallCreateRequest {
  /** WebRTC Session Description Protocol (SDP) offer generated by the caller. */
  sdp: string;
  /**
   * Optional session configuration to apply before the realtime session is
   * created. Use the same parameters you would send in a [`create client secret`](/docs/api-reference/realtime-sessions/create-realtime-client-secret)
   * request.
   */
  session?: RealtimeSessionCreateRequestGa;
}

/**
 * Parameters required to transfer a SIP call to a new destination using the
 * Realtime API.
 */
export interface RealtimeCallReferRequest {
  /**
   * URI that should appear in the SIP Refer-To header. Supports values like
   * `tel:+14155550123` or `sip:agent@example.com`.
   */
  target_uri: string;
}

/** Parameters used to decline an incoming SIP call handled by the Realtime API. */
export interface RealtimeCallRejectRequest {
  /**
   * SIP response code to send back to the caller. Defaults to `603` (Decline)
   * when omitted.
   */
  status_code?: number;
}

/**
 * Create a session and client secret for the Realtime API. The request can specify
 * either a realtime or a transcription session configuration.
 * [Learn more about the Realtime API](/docs/guides/realtime).
 */
export interface RealtimeCreateClientSecretRequest {
  /**
   * Configuration for the client secret expiration. Expiration refers to the time after which
   * a client secret will no longer be valid for creating sessions. The session itself may
   * continue after that time once started. A secret can be used to create multiple sessions
   * until it expires.
   */
  expires_after?: RealtimeCreateClientSecretRequestExpiresAfter;
  /**
   * Session configuration to use for the client secret. Choose either a realtime
   * session or a transcription session.
   */
  session?: RealtimeCreateClientSecretRequestSession;
}

/**
 * Configuration for the client secret expiration. Expiration refers to the time after which
 * a client secret will no longer be valid for creating sessions. The session itself may
 * continue after that time once started. A secret can be used to create multiple sessions
 * until it expires.
 */
export interface RealtimeCreateClientSecretRequestExpiresAfter {
  /** The anchor point for the client secret expiration, meaning that `seconds` will be added to the `created_at` time of the client secret to produce an expiration timestamp. Only `created_at` is currently supported. */
  anchor?: "created_at";
  /** The number of seconds from the anchor point to the expiration. Select a value between `10` and `7200` (2 hours). This default to 600 seconds (10 minutes) if not specified. */
  seconds?: number;
}

/**
 * Session configuration to use for the client secret. Choose either a realtime
 * session or a transcription session.
 */
export type RealtimeCreateClientSecretRequestSession = RealtimeSessionCreateRequestGa | RealtimeTranscriptionSessionCreateRequestGa;

/** Realtime transcription session object configuration. */
export interface RealtimeTranscriptionSessionCreateRequestGa {
  /** The type of session to create. Always `transcription` for transcription sessions. */
  type: "transcription";
  /** Configuration for input and output audio. */
  audio?: RealtimeTranscriptionSessionCreateRequestGaAudio;
  /**
   * Additional fields to include in server outputs.
   *
   * `item.input_audio_transcription.logprobs`: Include logprobs for input audio transcription.
   */
  include?: "item.input_audio_transcription.logprobs"[];
}

/** Configuration for input and output audio. */
export interface RealtimeTranscriptionSessionCreateRequestGaAudio {
  input?: RealtimeSessionCreateRequestGaAudioInput;
}

/** Response from creating a session and client secret for the Realtime API. */
export interface RealtimeCreateClientSecretResponse {
  /** The generated client secret value. */
  value: string;
  /** Expiration timestamp for the client secret, in seconds since epoch. */
  expires_at: number;
  /** The session configuration for either a realtime or transcription session. */
  session: RealtimeCreateClientSecretResponseSession;
}

/** The session configuration for either a realtime or transcription session. */
export type RealtimeCreateClientSecretResponseSession = RealtimeSessionCreateResponseGa | RealtimeTranscriptionSessionCreateResponseGa;

/** Decode a `RealtimeCreateClientSecretResponseSession` by its `type` discriminator (unknown value falls through). */
export function decodeRealtimeCreateClientSecretResponseSession(data: unknown): RealtimeCreateClientSecretResponseSession {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "realtime":
      return data as RealtimeSessionCreateResponseGa;
    case "transcription":
      return data as RealtimeTranscriptionSessionCreateResponseGa;
    default:
      return data as RealtimeCreateClientSecretResponseSession;
  }
}

/** A Realtime session configuration object. */
export interface RealtimeSessionCreateResponseGa {
  /** The type of session to create. Always `realtime` for the Realtime API. */
  type: "realtime";
  /** Unique identifier for the session that looks like `sess_1234567890abcdef`. */
  id: string;
  /** The object type. Always `realtime.session`. */
  object: "realtime.session";
  /** Expiration timestamp for the session, in seconds since epoch. */
  expires_at?: number;
  /**
   * The set of modalities the model can respond with. It defaults to `["audio"]`, indicating
   * that the model will respond with audio plus a transcript. `["text"]` can be used to make
   * the model respond with text only. It is not possible to request both `text` and `audio` at the same time.
   */
  output_modalities?: RealtimeSessionCreateRequestGaOutputModalitiesItem[];
  /** The Realtime model used for this session. */
  model?: RealtimeSessionCreateRequestGaModel;
  /**
   * The default system instructions (i.e. system message) prepended to model calls. This field allows the client to guide the model on desired responses. The model can be instructed on response content and format, (e.g. "be extremely succinct", "act friendly", "here are examples of good responses") and on audio behavior (e.g. "talk quickly", "inject emotion into your voice", "laugh frequently"). The instructions are not guaranteed to be followed by the model, but they provide guidance to the model on the desired behavior.
   *
   * Note that the server sets default instructions which will be used if this field is not set and are visible in the `session.created` event at the start of the session.
   */
  instructions?: string;
  /** Configuration for input and output audio. */
  audio?: RealtimeSessionCreateResponseGaAudio;
  /**
   * Additional fields to include in server outputs.
   *
   * `item.input_audio_transcription.logprobs`: Include logprobs for input audio transcription.
   */
  include?: "item.input_audio_transcription.logprobs"[];
  tracing?: RealtimeSessionCreateRequestGaTracing | null;
  /** Tools available to the model. */
  tools?: RealtimeSessionCreateRequestGaToolsItem[];
  /**
   * How the model chooses tools. Provide one of the string modes or force a specific
   * function/MCP tool.
   */
  tool_choice?: RealtimeSessionCreateRequestGaToolChoice;
  reasoning?: RealtimeReasoning;
  /**
   * Maximum number of output tokens for a single assistant response,
   * inclusive of tool calls. Provide an integer between 1 and 4096 to
   * limit output tokens, or `inf` for the maximum available tokens for a
   * given model. Defaults to `inf`.
   */
  max_output_tokens?: RealtimeSessionCreateRequestGaMaxOutputTokens;
  truncation?: RealtimeTruncation;
  prompt?: Prompt;
}

/** Configuration for input and output audio. */
export interface RealtimeSessionCreateResponseGaAudio {
  input?: RealtimeSessionCreateResponseGaAudioInput;
  output?: RealtimeSessionCreateResponseGaAudioOutput;
}

export interface RealtimeSessionCreateResponseGaAudioInput {
  format?: RealtimeAudioFormats;
  transcription?: AudioTranscriptionResponse;
  /**
   * Configuration for input audio noise reduction. This can be set to `null` to turn off.
   * Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
   * Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.
   */
  noise_reduction?: RealtimeSessionCreateRequestGaAudioInputNoiseReduction;
  turn_detection?: RealtimeTurnDetection;
}

export interface AudioTranscriptionResponse {
  /** The model used for transcription. Current options are `whisper-1`, `gpt-4o-mini-transcribe`, `gpt-4o-mini-transcribe-2025-12-15`, `gpt-4o-transcribe`, `gpt-4o-transcribe-diarize`, and `gpt-realtime-whisper`. */
  model?: AudioTranscriptionModel;
  /** The language of the input audio. */
  language?: string;
  /** The prompt configured for input audio transcription, when present. */
  prompt?: string;
}

export interface RealtimeSessionCreateResponseGaAudioOutput {
  format?: RealtimeAudioFormats;
  voice?: VoiceIdsShared;
  /**
   * The speed of the model's spoken response as a multiple of the original speed.
   * 1.0 is the default speed. 0.25 is the minimum speed. 1.5 is the maximum speed. This value can only be changed in between model turns, not while a response is in progress.
   *
   * This parameter is a post-processing adjustment to the audio after it is generated, it's
   * also possible to prompt the model to speak faster or slower.
   */
  speed?: number;
}

/** A Realtime transcription session configuration object. */
export interface RealtimeTranscriptionSessionCreateResponseGa {
  /** The type of session. Always `transcription` for transcription sessions. */
  type: "transcription";
  /** Unique identifier for the session that looks like `sess_1234567890abcdef`. */
  id: string;
  /** The object type. Always `realtime.transcription_session`. */
  object: string;
  /** Expiration timestamp for the session, in seconds since epoch. */
  expires_at?: number;
  /**
   * Additional fields to include in server outputs.
   * - `item.input_audio_transcription.logprobs`: Include logprobs for input audio transcription.
   */
  include?: "item.input_audio_transcription.logprobs"[];
  /** Configuration for input audio for the session. */
  audio?: RealtimeTranscriptionSessionCreateResponseGaAudio;
}

/** Configuration for input audio for the session. */
export interface RealtimeTranscriptionSessionCreateResponseGaAudio {
  input?: RealtimeTranscriptionSessionCreateResponseGaAudioInput;
}

export interface RealtimeTranscriptionSessionCreateResponseGaAudioInput {
  format?: RealtimeAudioFormats;
  transcription?: AudioTranscriptionResponse;
  /** Configuration for input audio noise reduction. */
  noise_reduction?: RealtimeSessionCreateRequestGaAudioInputNoiseReduction;
  /** Configuration for turn detection. For `gpt-realtime-whisper`, this must be `null`; VAD is not supported. */
  turn_detection?: RealtimeTranscriptionSessionCreateResponseGaAudioInputTurnDetection | null;
}

/**
 * Configuration for turn detection. Can be set to `null` to turn off. Server
 * VAD means that the model will detect the start and end of speech based on
 * audio volume and respond at the end of user speech. For `gpt-realtime-whisper`, this must be `null`; VAD is not supported.
 */
export interface RealtimeTranscriptionSessionCreateResponseGaAudioInputTurnDetection {
  /** Type of turn detection, only `server_vad` is currently supported. */
  type?: string;
  /**
   * Activation threshold for VAD (0.0 to 1.0), this defaults to 0.5. A
   * higher threshold will require louder audio to activate the model, and
   * thus might perform better in noisy environments.
   */
  threshold?: number;
  /**
   * Amount of audio to include before the VAD detected speech (in
   * milliseconds). Defaults to 300ms.
   */
  prefix_padding_ms?: number;
  /**
   * Duration of silence to detect speech stop (in milliseconds). Defaults
   * to 500ms. With shorter values the model will respond more quickly,
   * but may jump in on short pauses from the user.
   */
  silence_duration_ms?: number;
}

/**
 * A new Realtime session configuration, with an ephemeral key. Default TTL
 * for keys is one minute.
 */
export interface RealtimeSessionCreateRequest {
  /** Ephemeral key returned by the API. */
  client_secret: RealtimeSessionCreateRequestClientSecret;
  /**
   * The set of modalities the model can respond with. To disable audio,
   * set this to ["text"].
   */
  modalities?: unknown;
  /**
   * The default system instructions (i.e. system message) prepended to model calls. This field allows the client to guide the model on desired responses. The model can be instructed on response content and format, (e.g. "be extremely succinct", "act friendly", "here are examples of good responses") and on audio behavior (e.g. "talk quickly", "inject emotion into your voice", "laugh frequently"). The instructions are not guaranteed to be followed by the model, but they provide guidance to the model on the desired behavior.
   * Note that the server sets default instructions which will be used if this field is not set and are visible in the `session.created` event at the start of the session.
   */
  instructions?: string;
  voice?: VoiceIdsOrCustomVoice;
  /** The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`. */
  input_audio_format?: string;
  /** The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`. */
  output_audio_format?: string;
  /**
   * Configuration for input audio transcription, defaults to off and can be
   * set to `null` to turn off once on. Input audio transcription is not native
   * to the model, since the model consumes audio directly. Transcription runs
   * asynchronously and should be treated as rough guidance
   * rather than the representation understood by the model.
   */
  input_audio_transcription?: RealtimeSessionCreateRequestInputAudioTranscription;
  /**
   * The speed of the model's spoken response. 1.0 is the default speed. 0.25 is
   * the minimum speed. 1.5 is the maximum speed. This value can only be changed
   * in between model turns, not while a response is in progress.
   */
  speed?: number;
  /**
   * Configuration options for tracing. Set to null to disable tracing. Once
   * tracing is enabled for a session, the configuration cannot be modified.
   *
   * `auto` will create a trace for the session with default values for the
   * workflow name, group id, and metadata.
   */
  tracing?: RealtimeSessionCreateRequestTracing;
  /**
   * Configuration for turn detection. Can be set to `null` to turn off. Server
   * VAD means that the model will detect the start and end of speech based on
   * audio volume and respond at the end of user speech.
   */
  turn_detection?: RealtimeSessionCreateRequestTurnDetection;
  /** Tools (functions) available to the model. */
  tools?: RealtimeSessionCreateRequestToolsItem[];
  /**
   * How the model chooses tools. Options are `auto`, `none`, `required`, or
   * specify a function.
   */
  tool_choice?: string;
  /** Sampling temperature for the model, limited to [0.6, 1.2]. Defaults to 0.8. */
  temperature?: number;
  /**
   * Maximum number of output tokens for a single assistant response,
   * inclusive of tool calls. Provide an integer between 1 and 4096 to
   * limit output tokens, or `inf` for the maximum available tokens for a
   * given model. Defaults to `inf`.
   */
  max_response_output_tokens?: RealtimeSessionCreateRequestMaxResponseOutputTokens;
  truncation?: RealtimeTruncation;
  prompt?: Prompt;
}

/** Ephemeral key returned by the API. */
export interface RealtimeSessionCreateRequestClientSecret {
  /**
   * Ephemeral key usable in client environments to authenticate connections
   * to the Realtime API. Use this in client-side environments rather than
   * a standard API token, which should only be used server-side.
   */
  value: string;
  /**
   * Timestamp for when the token expires. Currently, all tokens expire
   * after one minute.
   */
  expires_at: number;
}

/**
 * Configuration for input audio transcription, defaults to off and can be
 * set to `null` to turn off once on. Input audio transcription is not native
 * to the model, since the model consumes audio directly. Transcription runs
 * asynchronously and should be treated as rough guidance
 * rather than the representation understood by the model.
 */
export interface RealtimeSessionCreateRequestInputAudioTranscription {
  /** The model to use for transcription. */
  model?: string;
}

/**
 * Configuration options for tracing. Set to null to disable tracing. Once
 * tracing is enabled for a session, the configuration cannot be modified.
 *
 * `auto` will create a trace for the session with default values for the
 * workflow name, group id, and metadata.
 */
export type RealtimeSessionCreateRequestTracing = "auto" | RealtimeSessionCreateRequestTracingTracingConfiguration;

/** Granular configuration for tracing. */
export interface RealtimeSessionCreateRequestTracingTracingConfiguration {
  /**
   * The name of the workflow to attach to this trace. This is used to
   * name the trace in the traces dashboard.
   */
  workflow_name?: string;
  /**
   * The group id to attach to this trace to enable filtering and
   * grouping in the traces dashboard.
   */
  group_id?: string;
  /**
   * The arbitrary metadata to attach to this trace to enable
   * filtering in the traces dashboard.
   */
  metadata?: Record<string, unknown>;
}

/**
 * Configuration for turn detection. Can be set to `null` to turn off. Server
 * VAD means that the model will detect the start and end of speech based on
 * audio volume and respond at the end of user speech.
 */
export interface RealtimeSessionCreateRequestTurnDetection {
  /** Type of turn detection, only `server_vad` is currently supported. */
  type?: string;
  /**
   * Activation threshold for VAD (0.0 to 1.0), this defaults to 0.5. A
   * higher threshold will require louder audio to activate the model, and
   * thus might perform better in noisy environments.
   */
  threshold?: number;
  /**
   * Amount of audio to include before the VAD detected speech (in
   * milliseconds). Defaults to 300ms.
   */
  prefix_padding_ms?: number;
  /**
   * Duration of silence to detect speech stop (in milliseconds). Defaults
   * to 500ms. With shorter values the model will respond more quickly,
   * but may jump in on short pauses from the user.
   */
  silence_duration_ms?: number;
}

export interface RealtimeSessionCreateRequestToolsItem {
  /** The type of the tool, i.e. `function`. */
  type?: "function";
  /** The name of the function. */
  name?: string;
  /**
   * The description of the function, including guidance on when and how
   * to call it, and guidance about what to tell the user when calling
   * (if anything).
   */
  description?: string;
  /** Parameters of the function in JSON Schema. */
  parameters?: Record<string, unknown>;
}

/**
 * Maximum number of output tokens for a single assistant response,
 * inclusive of tool calls. Provide an integer between 1 and 4096 to
 * limit output tokens, or `inf` for the maximum available tokens for a
 * given model. Defaults to `inf`.
 */
export type RealtimeSessionCreateRequestMaxResponseOutputTokens = number | "inf";

/** A Realtime session configuration object. */
export interface RealtimeSessionCreateResponse {
  /** Unique identifier for the session that looks like `sess_1234567890abcdef`. */
  id?: string;
  /** The object type. Always `realtime.session`. */
  object?: string;
  /** Expiration timestamp for the session, in seconds since epoch. */
  expires_at?: number;
  /**
   * Additional fields to include in server outputs.
   * - `item.input_audio_transcription.logprobs`: Include logprobs for input audio transcription.
   */
  include?: "item.input_audio_transcription.logprobs"[];
  /** The Realtime model used for this session. */
  model?: string;
  /**
   * The set of modalities the model can respond with. To disable audio,
   * set this to ["text"].
   */
  output_modalities?: unknown;
  /**
   * The default system instructions (i.e. system message) prepended to model
   * calls. This field allows the client to guide the model on desired
   * responses. The model can be instructed on response content and format,
   * (e.g. "be extremely succinct", "act friendly", "here are examples of good
   * responses") and on audio behavior (e.g. "talk quickly", "inject emotion
   * into your voice", "laugh frequently"). The instructions are not guaranteed
   * to be followed by the model, but they provide guidance to the model on the
   * desired behavior.
   *
   * Note that the server sets default instructions which will be used if this
   * field is not set and are visible in the `session.created` event at the
   * start of the session.
   */
  instructions?: string;
  /** Configuration for input and output audio for the session. */
  audio?: RealtimeSessionCreateResponseAudio;
  /**
   * Configuration options for tracing. Set to null to disable tracing. Once
   * tracing is enabled for a session, the configuration cannot be modified.
   *
   * `auto` will create a trace for the session with default values for the
   * workflow name, group id, and metadata.
   */
  tracing?: RealtimeSessionCreateRequestTracing;
  /**
   * Configuration for turn detection. Can be set to `null` to turn off. Server
   * VAD means that the model will detect the start and end of speech based on
   * audio volume and respond at the end of user speech.
   */
  turn_detection?: RealtimeSessionCreateRequestTurnDetection;
  /** Tools (functions) available to the model. */
  tools?: RealtimeFunctionTool[];
  /**
   * How the model chooses tools. Options are `auto`, `none`, `required`, or
   * specify a function.
   */
  tool_choice?: string;
  /**
   * Maximum number of output tokens for a single assistant response,
   * inclusive of tool calls. Provide an integer between 1 and 4096 to
   * limit output tokens, or `inf` for the maximum available tokens for a
   * given model. Defaults to `inf`.
   */
  max_output_tokens?: RealtimeSessionCreateRequestMaxResponseOutputTokens;
}

/** Configuration for input and output audio for the session. */
export interface RealtimeSessionCreateResponseAudio {
  input?: RealtimeSessionCreateResponseAudioInput;
  output?: RealtimeSessionCreateResponseAudioOutput;
}

export interface RealtimeSessionCreateResponseAudioInput {
  format?: RealtimeAudioFormats;
  transcription?: AudioTranscriptionResponse;
  /** Configuration for input audio noise reduction. */
  noise_reduction?: RealtimeSessionCreateResponseAudioInputNoiseReduction;
  /** Configuration for turn detection. */
  turn_detection?: RealtimeSessionCreateResponseAudioInputTurnDetection;
}

/** The model used for transcription. Current options are `whisper-1`, `gpt-4o-mini-transcribe`, `gpt-4o-mini-transcribe-2025-12-15`, `gpt-4o-transcribe`, `gpt-4o-transcribe-diarize`, and `gpt-realtime-whisper`. */
export type AudioTranscriptionResponseModel = string | AudioTranscriptionResponseModelString;

export type AudioTranscriptionResponseModelString = "whisper-1" | "gpt-4o-mini-transcribe" | "gpt-4o-mini-transcribe-2025-12-15" | "gpt-4o-transcribe" | "gpt-4o-transcribe-diarize" | "gpt-realtime-whisper";

/** Configuration for input audio noise reduction. */
export interface RealtimeSessionCreateResponseAudioInputNoiseReduction {
  type?: NoiseReductionType;
}

/** Configuration for turn detection. */
export interface RealtimeSessionCreateResponseAudioInputTurnDetection {
  /** Type of turn detection, only `server_vad` is currently supported. */
  type?: string;
  threshold?: number;
  prefix_padding_ms?: number;
  silence_duration_ms?: number;
}

export interface RealtimeSessionCreateResponseAudioOutput {
  format?: RealtimeAudioFormats;
  voice?: VoiceIdsShared;
  speed?: number;
}

/** Realtime transcription session object configuration. */
export interface RealtimeTranscriptionSessionCreateRequest {
  /** Configuration for turn detection. Can be set to `null` to turn off. Server VAD means that the model will detect the start and end of speech based on audio volume and respond at the end of user speech. */
  turn_detection?: RealtimeTranscriptionSessionCreateRequestTurnDetection;
  /**
   * Configuration for input audio noise reduction. This can be set to `null` to turn off.
   * Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
   * Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.
   */
  input_audio_noise_reduction?: RealtimeTranscriptionSessionCreateRequestInputAudioNoiseReduction;
  /**
   * The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
   * For `pcm16`, input audio must be 16-bit PCM at a 24kHz sample rate,
   * single channel (mono), and little-endian byte order.
   */
  input_audio_format?: RealtimeTranscriptionSessionCreateRequestInputAudioFormat;
  input_audio_transcription?: AudioTranscription;
  /**
   * The set of items to include in the transcription. Current available items are:
   * `item.input_audio_transcription.logprobs`
   */
  include?: "item.input_audio_transcription.logprobs"[];
}

/** Configuration for turn detection. Can be set to `null` to turn off. Server VAD means that the model will detect the start and end of speech based on audio volume and respond at the end of user speech. */
export interface RealtimeTranscriptionSessionCreateRequestTurnDetection {
  /** Type of turn detection. Only `server_vad` is currently supported for transcription sessions. */
  type?: "server_vad";
  /**
   * Activation threshold for VAD (0.0 to 1.0), this defaults to 0.5. A
   * higher threshold will require louder audio to activate the model, and
   * thus might perform better in noisy environments.
   */
  threshold?: number;
  /**
   * Amount of audio to include before the VAD detected speech (in
   * milliseconds). Defaults to 300ms.
   */
  prefix_padding_ms?: number;
  /**
   * Duration of silence to detect speech stop (in milliseconds). Defaults
   * to 500ms. With shorter values the model will respond more quickly,
   * but may jump in on short pauses from the user.
   */
  silence_duration_ms?: number;
}

/**
 * Configuration for input audio noise reduction. This can be set to `null` to turn off.
 * Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
 * Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.
 */
export interface RealtimeTranscriptionSessionCreateRequestInputAudioNoiseReduction {
  type?: NoiseReductionType;
}

/**
 * The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
 * For `pcm16`, input audio must be 16-bit PCM at a 24kHz sample rate,
 * single channel (mono), and little-endian byte order.
 */
export type RealtimeTranscriptionSessionCreateRequestInputAudioFormat = "pcm16" | "g711_ulaw" | "g711_alaw";

/**
 * A new Realtime transcription session configuration.
 *
 * When a session is created on the server via REST API, the session object
 * also contains an ephemeral key. Default TTL for keys is 10 minutes. This
 * property is not present when a session is updated via the WebSocket API.
 */
export interface RealtimeTranscriptionSessionCreateResponse {
  /**
   * Ephemeral key returned by the API. Only present when the session is
   * created on the server via REST API.
   */
  client_secret: RealtimeTranscriptionSessionCreateResponseClientSecret;
  /**
   * The set of modalities the model can respond with. To disable audio,
   * set this to ["text"].
   */
  modalities?: unknown;
  /** The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`. */
  input_audio_format?: string;
  input_audio_transcription?: AudioTranscriptionResponse;
  /**
   * Configuration for turn detection. Can be set to `null` to turn off. Server
   * VAD means that the model will detect the start and end of speech based on
   * audio volume and respond at the end of user speech.
   */
  turn_detection?: RealtimeTranscriptionSessionCreateResponseTurnDetection;
}

/**
 * Ephemeral key returned by the API. Only present when the session is
 * created on the server via REST API.
 */
export interface RealtimeTranscriptionSessionCreateResponseClientSecret {
  /**
   * Ephemeral key usable in client environments to authenticate connections
   * to the Realtime API. Use this in client-side environments rather than
   * a standard API token, which should only be used server-side.
   */
  value: string;
  /**
   * Timestamp for when the token expires. Currently, all tokens expire
   * after one minute.
   */
  expires_at: number;
}

/**
 * Configuration for turn detection. Can be set to `null` to turn off. Server
 * VAD means that the model will detect the start and end of speech based on
 * audio volume and respond at the end of user speech.
 */
export interface RealtimeTranscriptionSessionCreateResponseTurnDetection {
  /** Type of turn detection, only `server_vad` is currently supported. */
  type?: string;
  /**
   * Activation threshold for VAD (0.0 to 1.0), this defaults to 0.5. A
   * higher threshold will require louder audio to activate the model, and
   * thus might perform better in noisy environments.
   */
  threshold?: number;
  /**
   * Amount of audio to include before the VAD detected speech (in
   * milliseconds). Defaults to 300ms.
   */
  prefix_padding_ms?: number;
  /**
   * Duration of silence to detect speech stop (in milliseconds). Defaults
   * to 500ms. With shorter values the model will respond more quickly,
   * but may jump in on short pauses from the user.
   */
  silence_duration_ms?: number;
}

/** Create a translation session and client secret for the Realtime API. */
export interface RealtimeTranslationClientSecretCreateRequest {
  /**
   * Configuration for the client secret expiration. Expiration refers to the time after which
   * a client secret will no longer be valid for creating sessions. The session itself may
   * continue after that time once started. A secret can be used to create multiple sessions
   * until it expires.
   */
  expires_after?: RealtimeTranslationClientSecretCreateRequestExpiresAfter;
  session: RealtimeTranslationSessionCreateRequest;
}

/**
 * Configuration for the client secret expiration. Expiration refers to the time after which
 * a client secret will no longer be valid for creating sessions. The session itself may
 * continue after that time once started. A secret can be used to create multiple sessions
 * until it expires.
 */
export interface RealtimeTranslationClientSecretCreateRequestExpiresAfter {
  /** The anchor point for the client secret expiration, meaning that `seconds` will be added to the `created_at` time of the client secret to produce an expiration timestamp. Only `created_at` is currently supported. */
  anchor?: "created_at";
  /** The number of seconds from the anchor point to the expiration. Select a value between `10` and `7200` (2 hours). This default to 600 seconds (10 minutes) if not specified. */
  seconds?: number;
}

/**
 * Realtime translation session configuration. Translation sessions stream source
 * audio in and translated audio plus transcript deltas out continuously.
 */
export interface RealtimeTranslationSessionCreateRequest {
  /** The Realtime translation model used for this session. */
  model: string;
  /** Configuration for translation input and output audio. */
  audio?: RealtimeTranslationSessionCreateRequestAudio;
}

/** Configuration for translation input and output audio. */
export interface RealtimeTranslationSessionCreateRequestAudio {
  input?: RealtimeTranslationSessionCreateRequestAudioInput;
  output?: RealtimeTranslationSessionCreateRequestAudioOutput;
}

export interface RealtimeTranslationSessionCreateRequestAudioInput {
  transcription?: RealtimeTranslationSessionCreateRequestAudioInputTranscription | null;
  noise_reduction?: RealtimeTranslationSessionCreateRequestAudioInputNoiseReduction | null;
}

/**
 * Optional source-language transcription. When configured, the server emits
 * `session.input_transcript.delta` events. Translation itself still runs from
 * the input audio stream.
 */
export interface RealtimeTranslationSessionCreateRequestAudioInputTranscription {
  /** The transcription model to use for source transcript deltas. */
  model: string;
}

/** Optional input noise reduction. Set to `null` to disable it. */
export interface RealtimeTranslationSessionCreateRequestAudioInputNoiseReduction {
  type: NoiseReductionType;
}

export interface RealtimeTranslationSessionCreateRequestAudioOutput {
  /** Target language for translated output audio and transcript deltas. */
  language?: string;
}

/** Response from creating a translation session and client secret for the Realtime API. */
export interface RealtimeTranslationClientSecretCreateResponse {
  /** The generated client secret value. */
  value: string;
  /** Expiration timestamp for the client secret, in seconds since epoch. */
  expires_at: number;
  session: RealtimeTranslationSession;
}

/**
 * A Realtime translation session. Translation sessions continuously translate input
 * audio into the configured output language.
 */
export interface RealtimeTranslationSession {
  /** Unique identifier for the session that looks like `sess_1234567890abcdef`. */
  id: string;
  /** The session type. Always `translation` for Realtime translation sessions. */
  type: "translation";
  /** Expiration timestamp for the session, in seconds since epoch. */
  expires_at: number;
  /**
   * The Realtime translation model used for this session. This field is set at
   * session creation and cannot be changed with `session.update`.
   */
  model: string;
  /** Configuration for translation input and output audio. */
  audio: RealtimeTranslationSessionAudio;
}

/** Configuration for translation input and output audio. */
export interface RealtimeTranslationSessionAudio {
  input?: RealtimeTranslationSessionAudioInput;
  output?: RealtimeTranslationSessionCreateRequestAudioOutput;
}

export interface RealtimeTranslationSessionAudioInput {
  transcription?: RealtimeTranslationSessionAudioInputTranscription | null;
  noise_reduction?: RealtimeTranslationSessionCreateRequestAudioInputNoiseReduction | null;
}

/**
 * Optional source-language transcription. When configured, the server emits
 * `session.input_transcript.delta` events. Translation itself still runs from
 * the input audio stream.
 */
export interface RealtimeTranslationSessionAudioInputTranscription {
  /** The transcription model used for source transcript deltas. */
  model: string;
}

export interface Response {
  metadata: Metadata;
  top_logprobs?: number | null;
  temperature: number | null;
  top_p: number | null;
  /**
   * This field is being replaced by `safety_identifier` and `prompt_cache_key`. Use `prompt_cache_key` instead to maintain caching optimizations.
   * A stable identifier for your end-users.
   * Used to boost cache hit rates by better bucketing similar requests and  to help OpenAI detect and prevent abuse. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).
   */
  user?: string;
  /**
   * A stable identifier used to help detect users of your application that may be violating OpenAI's usage policies.
   * The IDs should be a string that uniquely identifies each user, with a maximum length of 64 characters. We recommend hashing their username or email address, in order to avoid sending us any identifying information. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).
   */
  safety_identifier?: string;
  /** Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the `user` field. [Learn more](/docs/guides/prompt-caching). */
  prompt_cache_key?: string;
  service_tier?: ServiceTier;
  prompt_cache_retention?: ResponsePromptCacheRetention | null;
  previous_response_id?: string | null;
  model: ModelIdsResponses;
  reasoning?: Reasoning | null;
  background?: boolean | null;
  max_tool_calls?: number | null;
  text?: ResponseTextParam;
  tools: ToolsArray;
  tool_choice: ToolChoiceParam;
  prompt?: Prompt;
  truncation?: ResponseTruncation | null;
  /** Unique identifier for this Response. */
  id: string;
  /** The object type of this resource - always set to `response`. */
  object: "response";
  /**
   * The status of the response generation. One of `completed`, `failed`,
   * `in_progress`, `cancelled`, `queued`, or `incomplete`.
   */
  status?: ResponseStatus;
  /** Unix timestamp (in seconds) of when this Response was created. */
  created_at: number;
  completed_at?: number | null;
  error: ResponseError;
  incomplete_details: ResponseIncompleteDetails | null;
  /**
   * An array of content items generated by the model.
   *
   * - The length and order of items in the `output` array is dependent
   * on the model's response.
   * - Rather than accessing the first item in the `output` array and
   * assuming it's an `assistant` message with the content generated by
   * the model, you might consider using the `output_text` property where
   * supported in SDKs.
   */
  output: OutputItem[];
  instructions: ResponseInstructions | null;
  output_text?: string | null;
  usage?: ResponseUsage;
  /** Whether to allow the model to run tool calls in parallel. */
  parallel_tool_calls: boolean;
  conversation?: Conversation2 | null;
  max_output_tokens?: number | null;
}

/** The retention policy for the prompt cache. Set to `24h` to enable extended prompt caching, which keeps cached prefixes active for longer, up to a maximum of 24 hours. [Learn more](/docs/guides/prompt-caching#prompt-cache-retention). */
export type ResponsePromptCacheRetention = "in_memory" | "24h";

export type ModelIdsResponses = ModelIdsShared | ModelIdsResponsesResponsesOnlyModel;

export type ModelIdsResponsesResponsesOnlyModel = "o1-pro" | "o1-pro-2025-03-19" | "o3-pro" | "o3-pro-2025-06-10" | "o3-deep-research" | "o3-deep-research-2025-06-26" | "o4-mini-deep-research" | "o4-mini-deep-research-2025-06-26" | "computer-use-preview" | "computer-use-preview-2025-03-11" | "gpt-5-codex" | "gpt-5-pro" | "gpt-5-pro-2025-10-06" | "gpt-5.1-codex-max";

/**
 * **gpt-5 and o-series models only**
 *
 * Configuration options for
 * [reasoning models](https://platform.openai.com/docs/guides/reasoning).
 */
export interface Reasoning {
  effort?: ReasoningEffort;
  summary?: ReasoningSummary | null;
  generate_summary?: ReasoningSummary | null;
}

/**
 * A summary of the reasoning performed by the model. This can be
 * useful for debugging and understanding the model's reasoning process.
 * One of `auto`, `concise`, or `detailed`.
 *
 * `concise` is supported for `computer-use-preview` models and all reasoning models after `gpt-5`.
 */
export type ReasoningSummary = "auto" | "concise" | "detailed";

/**
 * Configuration options for a text response from the model. Can be plain
 * text or structured JSON data. Learn more:
 * - [Text inputs and outputs](/docs/guides/text)
 * - [Structured Outputs](/docs/guides/structured-outputs)
 */
export interface ResponseTextParam {
  format?: TextResponseFormatConfiguration;
  verbosity?: Verbosity;
}

export type ToolsArray = Tool[];

/**
 * How the model should select which tool (or tools) to use when generating
 * a response. See the `tools` parameter to see how to specify which tools
 * the model can call.
 */
export type ToolChoiceParam = ToolChoiceOptions | ToolChoiceAllowed | ToolChoiceTypes | ToolChoiceFunction | ToolChoiceMcp | ToolChoiceCustom | SpecificApplyPatchParam | SpecificFunctionShellParam;

/** Constrains the tools available to the model to a pre-defined set. */
export interface ToolChoiceAllowed {
  /** Allowed tool configuration type. Always `allowed_tools`. */
  type: "allowed_tools";
  /**
   * Constrains the tools available to the model to a pre-defined set.
   *
   * `auto` allows the model to pick from among the allowed tools and generate a
   * message.
   *
   * `required` requires the model to call one or more of the allowed tools.
   */
  mode: ToolChoiceAllowedMode;
  /**
   * A list of tool definitions that the model should be allowed to call.
   *
   * For the Responses API, the list of tool definitions might look like:
   * ```json
   * [
   * { "type": "function", "name": "get_weather" },
   * { "type": "mcp", "server_label": "deepwiki" },
   * { "type": "image_generation" }
   * ]
   * ```
   */
  tools: (Record<string, unknown>)[];
}

/**
 * Constrains the tools available to the model to a pre-defined set.
 *
 * `auto` allows the model to pick from among the allowed tools and generate a
 * message.
 *
 * `required` requires the model to call one or more of the allowed tools.
 */
export type ToolChoiceAllowedMode = "auto" | "required";

/**
 * Indicates that the model should use a built-in tool to generate a response.
 * [Learn more about built-in tools](/docs/guides/tools).
 */
export interface ToolChoiceTypes {
  /**
   * The type of hosted tool the model should to use. Learn more about
   * [built-in tools](/docs/guides/tools).
   *
   * Allowed values are:
   * - `file_search`
   * - `web_search_preview`
   * - `computer`
   * - `computer_use_preview`
   * - `computer_use`
   * - `code_interpreter`
   * - `image_generation`
   */
  type: ToolChoiceTypesType;
}

/**
 * The type of hosted tool the model should to use. Learn more about
 * [built-in tools](/docs/guides/tools).
 *
 * Allowed values are:
 * - `file_search`
 * - `web_search_preview`
 * - `computer`
 * - `computer_use_preview`
 * - `computer_use`
 * - `code_interpreter`
 * - `image_generation`
 */
export type ToolChoiceTypesType = "file_search" | "web_search_preview" | "computer" | "computer_use_preview" | "computer_use" | "web_search_preview_2025_03_11" | "image_generation" | "code_interpreter";

/** Use this option to force the model to call a specific custom tool. */
export interface ToolChoiceCustom {
  /** For custom tool calling, the type is always `custom`. */
  type: "custom";
  /** The name of the custom tool to call. */
  name: string;
}

/** Forces the model to call the apply_patch tool when executing a tool call. */
export interface SpecificApplyPatchParam {
  /** The tool to call. Always `apply_patch`. */
  type: "apply_patch";
}

/** Forces the model to call the shell tool when a tool call is required. */
export interface SpecificFunctionShellParam {
  /** The tool to call. Always `shell`. */
  type: "shell";
}

/**
 * The truncation strategy to use for the model response.
 * - `auto`: If the input to this Response exceeds
 * the model's context window size, the model will truncate the
 * response to fit the context window by dropping items from the beginning of the conversation.
 * - `disabled` (default): If the input size will exceed the context window
 * size for a model, the request will fail with a 400 error.
 */
export type ResponseTruncation = "auto" | "disabled";

/**
 * The status of the response generation. One of `completed`, `failed`,
 * `in_progress`, `cancelled`, `queued`, or `incomplete`.
 */
export type ResponseStatus = "completed" | "failed" | "in_progress" | "cancelled" | "queued" | "incomplete";

/** An error object returned when the model fails to generate a Response. */
export interface ResponseError {
  code: ResponseErrorCode;
  /** A human-readable description of the error. */
  message: string;
}

/** The error code for the response. */
export type ResponseErrorCode = "server_error" | "rate_limit_exceeded" | "invalid_prompt" | "vector_store_timeout" | "invalid_image" | "invalid_image_format" | "invalid_base64_image" | "invalid_image_url" | "image_too_large" | "image_too_small" | "image_parse_error" | "image_content_policy_violation" | "invalid_image_mode" | "image_file_too_large" | "unsupported_image_media_type" | "empty_image_file" | "failed_to_download_image" | "image_file_not_found";

/** Details about why the response is incomplete. */
export interface ResponseIncompleteDetails {
  /** The reason why the response is incomplete. */
  reason?: ResponseIncompleteDetailsReason;
}

/** The reason why the response is incomplete. */
export type ResponseIncompleteDetailsReason = "max_output_tokens" | "content_filter";

export type OutputItem = OutputMessage | FileSearchToolCall | FunctionToolCall | FunctionToolCallOutputResource | WebSearchToolCall | ComputerToolCall | ComputerToolCallOutputResource | ReasoningItem | ToolSearchCall | ToolSearchOutput | CompactionBody | ImageGenToolCall | CodeInterpreterToolCall | LocalShellToolCall | LocalShellToolCallOutput | FunctionShellCall | FunctionShellCallOutput | ApplyPatchToolCall | ApplyPatchToolCallOutput | McpToolCall | McpListTools | McpApprovalRequest | McpApprovalResponseResource | CustomToolCall | CustomToolCallOutputResource;

/** Decode a `OutputItem` by its `type` discriminator (unknown value falls through). */
export function decodeOutputItem(data: unknown): OutputItem {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "message":
      return data as OutputMessage;
    case "file_search_call":
      return data as FileSearchToolCall;
    case "function_call":
      return data as FunctionToolCall;
    case "function_call_output":
      return data as FunctionToolCallOutputResource;
    case "web_search_call":
      return data as WebSearchToolCall;
    case "computer_call":
      return data as ComputerToolCall;
    case "computer_call_output":
      return data as ComputerToolCallOutputResource;
    case "reasoning":
      return data as ReasoningItem;
    case "tool_search_call":
      return data as ToolSearchCall;
    case "tool_search_output":
      return data as ToolSearchOutput;
    case "compaction":
      return data as CompactionBody;
    case "image_generation_call":
      return data as ImageGenToolCall;
    case "code_interpreter_call":
      return data as CodeInterpreterToolCall;
    case "local_shell_call":
      return data as LocalShellToolCall;
    case "local_shell_call_output":
      return data as LocalShellToolCallOutput;
    case "shell_call":
      return data as FunctionShellCall;
    case "shell_call_output":
      return data as FunctionShellCallOutput;
    case "apply_patch_call":
      return data as ApplyPatchToolCall;
    case "apply_patch_call_output":
      return data as ApplyPatchToolCallOutput;
    case "mcp_call":
      return data as McpToolCall;
    case "mcp_list_tools":
      return data as McpListTools;
    case "mcp_approval_request":
      return data as McpApprovalRequest;
    case "mcp_approval_response":
      return data as McpApprovalResponseResource;
    case "custom_tool_call":
      return data as CustomToolCall;
    case "custom_tool_call_output":
      return data as CustomToolCallOutputResource;
    default:
      return data as OutputItem;
  }
}

/**
 * The status of the message input. One of `in_progress`, `completed`, or
 * `incomplete`. Populated when input items are returned via API.
 */
export type OutputMessageStatus = "in_progress" | "completed" | "incomplete";

export interface CustomToolCallOutputResource {
  /** The type of the custom tool call output. Always `custom_tool_call_output`. */
  type: "custom_tool_call_output";
  /** The unique ID of the custom tool call output item. */
  id: string;
  /** The call ID, used to map this custom tool call output to a custom tool call. */
  call_id: string;
  /**
   * The output from the custom tool call generated by your code.
   * Can be a string or an list of output content.
   */
  output: FunctionToolCallOutputResourceOutput;
  status: FunctionCallOutputStatusEnum;
  /** The identifier of the actor that created the item. */
  created_by?: string;
}

/**
 * A system (or developer) message inserted into the model's context.
 *
 * When using along with `previous_response_id`, the instructions from a previous
 * response will not be carried over to the next response. This makes it simple
 * to swap out system (or developer) messages in new responses.
 */
export type ResponseInstructions = string | InputItem[];

/**
 * Represents token usage details including input tokens, output tokens,
 * a breakdown of output tokens, and the total tokens used.
 */
export interface ResponseUsage {
  /** The number of input tokens. */
  input_tokens: number;
  /** A detailed breakdown of the input tokens. */
  input_tokens_details: ResponseUsageInputTokensDetails;
  /** The number of output tokens. */
  output_tokens: number;
  /** A detailed breakdown of the output tokens. */
  output_tokens_details: ResponseUsageOutputTokensDetails;
  /** The total number of tokens used. */
  total_tokens: number;
}

/** A detailed breakdown of the input tokens. */
export interface ResponseUsageInputTokensDetails {
  /**
   * The number of tokens that were retrieved from the cache.
   * [More on prompt caching](/docs/guides/prompt-caching).
   */
  cached_tokens: number;
}

/** A detailed breakdown of the output tokens. */
export interface ResponseUsageOutputTokensDetails {
  /** The number of reasoning tokens. */
  reasoning_tokens: number;
}

/** The conversation that this response belonged to. Input items and output items from this response were automatically added to this conversation. */
export interface Conversation2 {
  /** The unique ID of the conversation that this response was associated with. */
  id: string;
}

export interface CompactResponseMethodPublicBody {
  model: ModelIdsCompaction;
  input?: CompactResponseMethodPublicBodyInput | null;
  previous_response_id?: string | null;
  instructions?: string | null;
  prompt_cache_key?: string | null;
  prompt_cache_retention?: PromptCacheRetentionEnum | null;
  service_tier?: ServiceTierEnum | null;
}

/** Model ID used to generate the response, like `gpt-5` or `o3`. OpenAI offers a wide range of models with different capabilities, performance characteristics, and price points. Refer to the [model guide](/docs/models) to browse and compare available models. */
export type ModelIdsCompaction = ModelIdsResponses | string;

/** Text, image, or file inputs to the model, used to generate a response */
export type CompactResponseMethodPublicBodyInput = string | InputItem[];

export type PromptCacheRetentionEnum = "in_memory" | "24h";

export type ServiceTierEnum = "auto" | "default" | "flex" | "priority";

export interface CompactResource {
  /** The unique identifier for the compacted response. */
  id: string;
  /** The object type. Always `response.compaction`. */
  object: "response.compaction";
  /** The compacted list of output items. */
  output: ItemField[];
  /** Unix timestamp (in seconds) when the compacted conversation was created. */
  created_at: number;
  usage: ResponseUsage;
}

/** An item representing a message, tool call, tool output, reasoning, or other response element. */
export type ItemField = Message | FunctionToolCall | ToolSearchCall | ToolSearchOutput | FunctionToolCallOutput | FileSearchToolCall | WebSearchToolCall | ImageGenToolCall | ComputerToolCall | ComputerToolCallOutputResource | ReasoningItem | CompactionBody | CodeInterpreterToolCall | LocalShellToolCall | LocalShellToolCallOutput | FunctionShellCall | FunctionShellCallOutput | ApplyPatchToolCall | ApplyPatchToolCallOutput | McpListTools | McpApprovalRequest | McpApprovalResponseResource | McpToolCall | CustomToolCall | CustomToolCallOutput;

/** Decode a `ItemField` by its `type` discriminator (unknown value falls through). */
export function decodeItemField(data: unknown): ItemField {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "message":
      return data as Message;
    case "function_call":
      return data as FunctionToolCall;
    case "tool_search_call":
      return data as ToolSearchCall;
    case "tool_search_output":
      return data as ToolSearchOutput;
    case "function_call_output":
      return data as FunctionToolCallOutput;
    case "file_search_call":
      return data as FileSearchToolCall;
    case "web_search_call":
      return data as WebSearchToolCall;
    case "image_generation_call":
      return data as ImageGenToolCall;
    case "computer_call":
      return data as ComputerToolCall;
    case "computer_call_output":
      return data as ComputerToolCallOutputResource;
    case "reasoning":
      return data as ReasoningItem;
    case "compaction":
      return data as CompactionBody;
    case "code_interpreter_call":
      return data as CodeInterpreterToolCall;
    case "local_shell_call":
      return data as LocalShellToolCall;
    case "local_shell_call_output":
      return data as LocalShellToolCallOutput;
    case "shell_call":
      return data as FunctionShellCall;
    case "shell_call_output":
      return data as FunctionShellCallOutput;
    case "apply_patch_call":
      return data as ApplyPatchToolCall;
    case "apply_patch_call_output":
      return data as ApplyPatchToolCallOutput;
    case "mcp_list_tools":
      return data as McpListTools;
    case "mcp_approval_request":
      return data as McpApprovalRequest;
    case "mcp_approval_response":
      return data as McpApprovalResponseResource;
    case "mcp_call":
      return data as McpToolCall;
    case "custom_tool_call":
      return data as CustomToolCall;
    case "custom_tool_call_output":
      return data as CustomToolCallOutput;
    default:
      return data as ItemField;
  }
}

/** The output of a function tool call. */
export interface FunctionToolCallOutput {
  /**
   * The unique ID of the function tool call output. Populated when this item
   * is returned via API.
   */
  id?: string;
  /** The type of the function tool call output. Always `function_call_output`. */
  type: "function_call_output";
  /** The unique ID of the function tool call generated by the model. */
  call_id: string;
  /**
   * The output from the function call generated by your code.
   * Can be a string or an list of output content.
   */
  output: CustomToolCallOutputOutput;
  /**
   * The status of the item. One of `in_progress`, `completed`, or
   * `incomplete`. Populated when items are returned via API.
   */
  status?: InputMessageStatus;
}

export interface CreateResponse {
  metadata?: Metadata;
  /**
   * An integer between 0 and 20 specifying the maximum number of most likely
   * tokens to return at each token position, each with an associated log
   * probability. In some cases, the number of returned tokens may be fewer than
   * requested.
   */
  top_logprobs?: number;
  temperature?: number | null;
  top_p?: number | null;
  /**
   * This field is being replaced by `safety_identifier` and `prompt_cache_key`. Use `prompt_cache_key` instead to maintain caching optimizations.
   * A stable identifier for your end-users.
   * Used to boost cache hit rates by better bucketing similar requests and  to help OpenAI detect and prevent abuse. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).
   */
  user?: string;
  /**
   * A stable identifier used to help detect users of your application that may be violating OpenAI's usage policies.
   * The IDs should be a string that uniquely identifies each user, with a maximum length of 64 characters. We recommend hashing their username or email address, in order to avoid sending us any identifying information. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).
   */
  safety_identifier?: string;
  /** Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the `user` field. [Learn more](/docs/guides/prompt-caching). */
  prompt_cache_key?: string;
  service_tier?: ServiceTier;
  prompt_cache_retention?: CreateResponsePromptCacheRetention | null;
  previous_response_id?: string | null;
  model?: ModelIdsResponses;
  reasoning?: Reasoning | null;
  background?: boolean | null;
  max_tool_calls?: number | null;
  text?: ResponseTextParam;
  tools?: ToolsArray;
  tool_choice?: ToolChoiceParam;
  prompt?: Prompt;
  truncation?: CreateResponseTruncation | null;
  input?: InputParam;
  include?: IncludeEnum[] | null;
  parallel_tool_calls?: boolean | null;
  store?: boolean | null;
  instructions?: string | null;
  stream?: boolean | null;
  stream_options?: ResponseStreamOptions;
  conversation?: ConversationParam | null;
  context_management?: ContextManagementParam[] | null;
  max_output_tokens?: number | null;
}

/** The retention policy for the prompt cache. Set to `24h` to enable extended prompt caching, which keeps cached prefixes active for longer, up to a maximum of 24 hours. [Learn more](/docs/guides/prompt-caching#prompt-cache-retention). */
export type CreateResponsePromptCacheRetention = "in_memory" | "24h";

/**
 * The truncation strategy to use for the model response.
 * - `auto`: If the input to this Response exceeds
 * the model's context window size, the model will truncate the
 * response to fit the context window by dropping items from the beginning of the conversation.
 * - `disabled` (default): If the input size will exceed the context window
 * size for a model, the request will fail with a 400 error.
 */
export type CreateResponseTruncation = "auto" | "disabled";

/**
 * Text, image, or file inputs to the model, used to generate a response.
 *
 * Learn more:
 * - [Text inputs and outputs](/docs/guides/text)
 * - [Image inputs](/docs/guides/images)
 * - [File inputs](/docs/guides/pdf-files)
 * - [Conversation state](/docs/guides/conversation-state)
 * - [Function calling](/docs/guides/function-calling)
 */
export type InputParam = string | InputItem[];

/** Options for streaming responses. Only set this when you set `stream: true`. */
export interface ResponseStreamOptions {
  /**
   * When true, stream obfuscation will be enabled. Stream obfuscation adds
   * random characters to an `obfuscation` field on streaming delta events to
   * normalize payload sizes as a mitigation to certain side-channel attacks.
   * These obfuscation fields are included by default, but add a small amount
   * of overhead to the data stream. You can set `include_obfuscation` to
   * false to optimize for bandwidth if you trust the network links between
   * your application and the OpenAI API.
   */
  include_obfuscation?: boolean;
}

/**
 * The conversation that this response belongs to. Items from this conversation are prepended to `input_items` for this response request.
 * Input items and output items from this response are automatically added to this conversation after this response completes.
 */
export type ConversationParam = string | ConversationParam2;

/** The conversation that this response belongs to. */
export interface ConversationParam2 {
  /** The unique ID of the conversation. */
  id: string;
}

export interface ContextManagementParam {
  /** The context management entry type. Currently only 'compaction' is supported. */
  type: string;
  compact_threshold?: number | null;
}

export type ResponsesInputItemsListOrder = "asc" | "desc";

/** A list of Response items. */
export interface ResponseItemList {
  /** The type of object returned, must be `list`. */
  object: "list";
  /** A list of items used to generate this response. */
  data: ItemResource[];
  /** Whether there are more items available. */
  has_more: boolean;
  /** The ID of the first item in the list. */
  first_id: string;
  /** The ID of the last item in the list. */
  last_id: string;
}

/** Content item used to generate a response. */
export type ItemResource = InputMessageResource | OutputMessage | FileSearchToolCall | ComputerToolCall | ComputerToolCallOutputResource | WebSearchToolCall | FunctionToolCallResource | FunctionToolCallOutputResource | ToolSearchCall | ToolSearchOutput | ReasoningItem | CompactionBody | ImageGenToolCall | CodeInterpreterToolCall | LocalShellToolCall | LocalShellToolCallOutput | FunctionShellCall | FunctionShellCallOutput | ApplyPatchToolCall | ApplyPatchToolCallOutput | McpListTools | McpApprovalRequest | McpApprovalResponseResource | McpToolCall | CustomToolCallResource | CustomToolCallOutputResource;

/** Decode a `ItemResource` by its `type` discriminator (unknown value falls through). */
export function decodeItemResource(data: unknown): ItemResource {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "message":
      return data as OutputMessage;
    case "file_search_call":
      return data as FileSearchToolCall;
    case "computer_call":
      return data as ComputerToolCall;
    case "computer_call_output":
      return data as ComputerToolCallOutputResource;
    case "web_search_call":
      return data as WebSearchToolCall;
    case "function_call":
      return data as FunctionToolCallResource;
    case "function_call_output":
      return data as FunctionToolCallOutputResource;
    case "tool_search_call":
      return data as ToolSearchCall;
    case "tool_search_output":
      return data as ToolSearchOutput;
    case "reasoning":
      return data as ReasoningItem;
    case "compaction":
      return data as CompactionBody;
    case "image_generation_call":
      return data as ImageGenToolCall;
    case "code_interpreter_call":
      return data as CodeInterpreterToolCall;
    case "local_shell_call":
      return data as LocalShellToolCall;
    case "local_shell_call_output":
      return data as LocalShellToolCallOutput;
    case "shell_call":
      return data as FunctionShellCall;
    case "shell_call_output":
      return data as FunctionShellCallOutput;
    case "apply_patch_call":
      return data as ApplyPatchToolCall;
    case "apply_patch_call_output":
      return data as ApplyPatchToolCallOutput;
    case "mcp_list_tools":
      return data as McpListTools;
    case "mcp_approval_request":
      return data as McpApprovalRequest;
    case "mcp_approval_response":
      return data as McpApprovalResponseResource;
    case "mcp_call":
      return data as McpToolCall;
    case "custom_tool_call":
      return data as CustomToolCallResource;
    case "custom_tool_call_output":
      return data as CustomToolCallOutputResource;
    default:
      return data as ItemResource;
  }
}

export interface InputMessageResource {
  /** The type of the message input. Always set to `message`. */
  type: "message";
  /** The role of the message input. One of `user`, `system`, or `developer`. */
  role: InputMessageResourceRole;
  /**
   * The status of item. One of `in_progress`, `completed`, or
   * `incomplete`. Populated when items are returned via API.
   */
  status?: InputMessageResourceStatus;
  content: InputMessageContentList;
  /** The unique ID of the message input. */
  id: string;
}

/** The role of the message input. One of `user`, `system`, or `developer`. */
export type InputMessageResourceRole = "user" | "system" | "developer";

/**
 * The status of item. One of `in_progress`, `completed`, or
 * `incomplete`. Populated when items are returned via API.
 */
export type InputMessageResourceStatus = "in_progress" | "completed" | "incomplete";

export interface CustomToolCallResource {
  /** The type of the custom tool call. Always `custom_tool_call`. */
  type: "custom_tool_call";
  /** The unique ID of the custom tool call item. */
  id: string;
  /** An identifier used to map this custom tool call to a tool call output. */
  call_id: string;
  /** The namespace of the custom tool being called. */
  namespace?: string;
  /** The name of the custom tool being called. */
  name: string;
  /** The input for the custom tool call generated by the model. */
  input: string;
  status: FunctionCallStatus;
  /** The identifier of the actor that created the item. */
  created_by?: string;
}

export interface TokenCountsBody {
  model?: string | null;
  input?: TokenCountsBodyInput | null;
  previous_response_id?: string | null;
  tools?: Tool[] | null;
  text?: ResponseTextParam | null;
  reasoning?: Reasoning | null;
  truncation?: TruncationEnum;
  instructions?: string | null;
  conversation?: ConversationParam | null;
  tool_choice?: ToolChoiceParam | null;
  parallel_tool_calls?: boolean | null;
}

/** Text, image, or file inputs to the model, used to generate a response */
export type TokenCountsBodyInput = string | InputItem[];

export type TruncationEnum = "auto" | "disabled";

export interface TokenCountsResource {
  object: "response.input_tokens";
  input_tokens: number;
}

/** Uploads a skill either as a directory (multipart `files[]`) or as a single zip file. */
export interface CreateSkillBody {
  files: CreateSkillBodyFiles;
}

export type CreateSkillBodyFiles = (Uint8Array | Blob)[] | Uint8Array | Blob;

export interface SkillResource {
  /** Unique identifier for the skill. */
  id: string;
  /** The object type, which is `skill`. */
  object: "skill";
  /** Name of the skill. */
  name: string;
  /** Description of the skill. */
  description: string;
  /** Unix timestamp (seconds) for when the skill was created. */
  created_at: number;
  /** Default version for the skill. */
  default_version: string;
  /** Latest version for the skill. */
  latest_version: string;
}

export interface DeletedSkillResource {
  object: "skill.deleted";
  deleted: boolean;
  id: string;
}

export interface SkillListResource {
  /** The type of object returned, must be `list`. */
  object: "list";
  /** A list of items */
  data: SkillResource[];
  first_id: string | null;
  last_id: string | null;
  /** Whether there are more items available. */
  has_more: boolean;
}

/** Updates the default version pointer for a skill. */
export interface SetDefaultSkillVersionBody {
  /** The skill version number to set as default. */
  default_version: string;
}

/** Uploads a new immutable version of a skill. */
export interface CreateSkillVersionBody {
  files: CreateSkillVersionBodyFiles;
  /** Whether to set this version as the default. */
  default?: boolean;
}

export type CreateSkillVersionBodyFiles = (Uint8Array | Blob)[] | Uint8Array | Blob;

export interface SkillVersionResource {
  /** The object type, which is `skill.version`. */
  object: "skill.version";
  /** Unique identifier for the skill version. */
  id: string;
  /** Identifier of the skill for this version. */
  skill_id: string;
  /** Version number for this skill. */
  version: string;
  /** Unix timestamp (seconds) for when the version was created. */
  created_at: number;
  /** Name of the skill version. */
  name: string;
  /** Description of the skill version. */
  description: string;
}

export interface DeletedSkillVersionResource {
  object: "skill.version.deleted";
  deleted: boolean;
  id: string;
  /** The deleted skill version. */
  version: string;
}

export interface SkillVersionListResource {
  /** The type of object returned, must be `list`. */
  object: "list";
  /** A list of items */
  data: SkillVersionResource[];
  first_id: string | null;
  last_id: string | null;
  /** Whether there are more items available. */
  has_more: boolean;
}

/** The Upload object can accept byte chunks in the form of Parts. */
export interface Upload {
  /** The Upload unique identifier, which can be referenced in API endpoints. */
  id: string;
  /** The Unix timestamp (in seconds) for when the Upload was created. */
  created_at: number;
  /** The name of the file to be uploaded. */
  filename: string;
  /** The intended number of bytes to be uploaded. */
  bytes: number;
  /** The intended purpose of the file. [Please refer here](/docs/api-reference/files/object#files/object-purpose) for acceptable values. */
  purpose: string;
  /** The status of the Upload. */
  status: UploadStatus;
  /** The Unix timestamp (in seconds) for when the Upload will expire. */
  expires_at: number;
  /** The object type, which is always "upload". */
  object?: "upload";
  file?: OpenAiFile | null;
}

/** The status of the Upload. */
export type UploadStatus = "pending" | "completed" | "cancelled" | "expired";

export interface CompleteUploadRequest {
  /** The ordered list of Part IDs. */
  part_ids: string[];
  /** The optional md5 checksum for the file contents to verify if the bytes uploaded matches what you expect. */
  md5?: string;
}

export interface CreateUploadRequest {
  /** The name of the file to upload. */
  filename: string;
  /**
   * The intended purpose of the uploaded file.
   *
   * See the [documentation on File
   * purposes](/docs/api-reference/files/create#files-create-purpose).
   */
  purpose: CreateUploadRequestPurpose;
  /** The number of bytes in the file you are uploading. */
  bytes: number;
  /**
   * The MIME type of the file.
   *
   *
   * This must fall within the supported MIME types for your file purpose. See
   * the supported MIME types for assistants and vision.
   */
  mime_type: string;
  expires_after?: FileExpirationAfter;
}

/**
 * The intended purpose of the uploaded file.
 *
 * See the [documentation on File
 * purposes](/docs/api-reference/files/create#files-create-purpose).
 */
export type CreateUploadRequestPurpose = "assistants" | "batch" | "fine-tune" | "vision";

export interface AddUploadPartRequest {
  /** The chunk of bytes for this Part. */
  data: Uint8Array | Blob;
}

/** The upload Part represents a chunk of bytes we can add to an Upload object. */
export interface UploadPart {
  /** The upload Part unique identifier, which can be referenced in API endpoints. */
  id: string;
  /** The Unix timestamp (in seconds) for when the Part was created. */
  created_at: number;
  /** The ID of the Upload object that this Part was added to. */
  upload_id: string;
  /** The object type, which is always `upload.part`. */
  object: "upload.part";
}

export interface CreateVectorStoreRequest {
  /** A list of [File](/docs/api-reference/files) IDs that the vector store should use. Useful for tools like `file_search` that can access files. */
  file_ids?: string[];
  /** The name of the vector store. */
  name?: string;
  /** A description for the vector store. Can be used to describe the vector store's purpose. */
  description?: string;
  expires_after?: VectorStoreExpirationAfter;
  /** The chunking strategy used to chunk the file(s). If not set, will use the `auto` strategy. Only applicable if `file_ids` is non-empty. */
  chunking_strategy?: CreateVectorStoreRequestChunkingStrategy;
  metadata?: Metadata;
}

/** The expiration policy for a vector store. */
export interface VectorStoreExpirationAfter {
  /** Anchor timestamp after which the expiration policy applies. Supported anchors: `last_active_at`. */
  anchor: "last_active_at";
  /** The number of days after the anchor time that the vector store will expire. */
  days: number;
}

/** The chunking strategy used to chunk the file(s). If not set, will use the `auto` strategy. Only applicable if `file_ids` is non-empty. */
export type CreateVectorStoreRequestChunkingStrategy = AutoChunkingStrategyRequestParam | StaticChunkingStrategyRequestParam;

/** The default strategy. This strategy currently uses a `max_chunk_size_tokens` of `800` and `chunk_overlap_tokens` of `400`. */
export interface AutoChunkingStrategyRequestParam {
  /** Always `auto`. */
  type: "auto";
}

/** Customize your own chunking strategy by setting chunk size and chunk overlap. */
export interface StaticChunkingStrategyRequestParam {
  /** Always `static`. */
  type: "static";
  static: StaticChunkingStrategy;
}

export interface StaticChunkingStrategy {
  /** The maximum number of tokens in each chunk. The default value is `800`. The minimum value is `100` and the maximum value is `4096`. */
  max_chunk_size_tokens: number;
  /**
   * The number of tokens that overlap between chunks. The default value is `400`.
   *
   * Note that the overlap must not exceed half of `max_chunk_size_tokens`.
   */
  chunk_overlap_tokens: number;
}

/** A vector store is a collection of processed files can be used by the `file_search` tool. */
export interface VectorStoreObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `vector_store`. */
  object: "vector_store";
  /** The Unix timestamp (in seconds) for when the vector store was created. */
  created_at: number;
  /** The name of the vector store. */
  name: string;
  /** The total number of bytes used by the files in the vector store. */
  usage_bytes: number;
  file_counts: VectorStoreObjectFileCounts;
  /** The status of the vector store, which can be either `expired`, `in_progress`, or `completed`. A status of `completed` indicates that the vector store is ready for use. */
  status: VectorStoreObjectStatus;
  expires_after?: VectorStoreExpirationAfter;
  expires_at?: number | null;
  last_active_at: number | null;
  metadata: Metadata;
}

export interface VectorStoreObjectFileCounts {
  /** The number of files that are currently being processed. */
  in_progress: number;
  /** The number of files that have been successfully processed. */
  completed: number;
  /** The number of files that have failed to process. */
  failed: number;
  /** The number of files that were cancelled. */
  cancelled: number;
  /** The total number of files. */
  total: number;
}

/** The status of the vector store, which can be either `expired`, `in_progress`, or `completed`. A status of `completed` indicates that the vector store is ready for use. */
export type VectorStoreObjectStatus = "expired" | "in_progress" | "completed";

export interface DeleteVectorStoreResponse {
  id: string;
  deleted: boolean;
  object: "vector_store.deleted";
}

/** A batch of files attached to a vector store. */
export interface VectorStoreFileBatchObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `vector_store.file_batch`. */
  object: "vector_store.files_batch";
  /** The Unix timestamp (in seconds) for when the vector store files batch was created. */
  created_at: number;
  /** The ID of the [vector store](/docs/api-reference/vector-stores/object) that the [File](/docs/api-reference/files) is attached to. */
  vector_store_id: string;
  /** The status of the vector store files batch, which can be either `in_progress`, `completed`, `cancelled` or `failed`. */
  status: VectorStoreFileBatchObjectStatus;
  file_counts: VectorStoreFileBatchObjectFileCounts;
}

/** The status of the vector store files batch, which can be either `in_progress`, `completed`, `cancelled` or `failed`. */
export type VectorStoreFileBatchObjectStatus = "in_progress" | "completed" | "cancelled" | "failed";

export interface VectorStoreFileBatchObjectFileCounts {
  /** The number of files that are currently being processed. */
  in_progress: number;
  /** The number of files that have been processed. */
  completed: number;
  /** The number of files that have failed to process. */
  failed: number;
  /** The number of files that where cancelled. */
  cancelled: number;
  /** The total number of files. */
  total: number;
}

export type CreateVectorStoreFileBatchRequest = unknown | unknown;

export type VectorStoresFileBatchesListFilesOrder = "asc" | "desc";

export type VectorStoresFileBatchesListFilesFilter = "in_progress" | "completed" | "failed" | "cancelled";

export interface ListVectorStoreFilesResponse {
  object: string;
  data: VectorStoreFileObject[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

/** A list of files attached to a vector store. */
export interface VectorStoreFileObject {
  /** The identifier, which can be referenced in API endpoints. */
  id: string;
  /** The object type, which is always `vector_store.file`. */
  object: "vector_store.file";
  /** The total vector store usage in bytes. Note that this may be different from the original file size. */
  usage_bytes: number;
  /** The Unix timestamp (in seconds) for when the vector store file was created. */
  created_at: number;
  /** The ID of the [vector store](/docs/api-reference/vector-stores/object) that the [File](/docs/api-reference/files) is attached to. */
  vector_store_id: string;
  /** The status of the vector store file, which can be either `in_progress`, `completed`, `cancelled`, or `failed`. The status `completed` indicates that the vector store file is ready for use. */
  status: VectorStoreFileObjectStatus;
  last_error: VectorStoreFileObjectLastError | null;
  /** The strategy used to chunk the file. */
  chunking_strategy?: VectorStoreFileObjectChunkingStrategy;
  attributes?: VectorStoreFileAttributes;
}

/** The status of the vector store file, which can be either `in_progress`, `completed`, `cancelled`, or `failed`. The status `completed` indicates that the vector store file is ready for use. */
export type VectorStoreFileObjectStatus = "in_progress" | "completed" | "cancelled" | "failed";

/** The last error associated with this vector store file. Will be `null` if there are no errors. */
export interface VectorStoreFileObjectLastError {
  /** One of `server_error`, `unsupported_file`, or `invalid_file`. */
  code: VectorStoreFileObjectLastErrorCode;
  /** A human-readable description of the error. */
  message: string;
}

/** One of `server_error`, `unsupported_file`, or `invalid_file`. */
export type VectorStoreFileObjectLastErrorCode = "server_error" | "unsupported_file" | "invalid_file";

/** The strategy used to chunk the file. */
export type VectorStoreFileObjectChunkingStrategy = StaticChunkingStrategyResponseParam | OtherChunkingStrategyResponseParam;

export interface StaticChunkingStrategyResponseParam {
  /** Always `static`. */
  type: "static";
  static: StaticChunkingStrategy;
}

/** This is returned when the chunking strategy is unknown. Typically, this is because the file was indexed before the `chunking_strategy` concept was introduced in the API. */
export interface OtherChunkingStrategyResponseParam {
  /** Always `other`. */
  type: "other";
}

/** Represents the parsed content of a vector store file. */
export interface VectorStoreFileContentResponse {
  /** The object type, which is always `vector_store.file_content.page` */
  object: "vector_store.file_content.page";
  /** Parsed content of the file. */
  data: VectorStoreFileContentResponseDataItem[];
  /** Indicates if there are more content pages to fetch. */
  has_more: boolean;
  next_page: string | null;
}

export interface VectorStoreFileContentResponseDataItem {
  /** The content type (currently only `"text"`) */
  type?: string;
  /** The text content */
  text?: string;
}

export interface CreateVectorStoreFileRequest {
  /** A [File](/docs/api-reference/files) ID that the vector store should use. Useful for tools like `file_search` that can access files. For multi-file ingestion, we recommend [`file_batches`](/docs/api-reference/vector-stores-file-batches/createBatch) to minimize per-vector-store write requests. */
  file_id: string;
  chunking_strategy?: ChunkingStrategyRequestParam;
  attributes?: VectorStoreFileAttributes;
}

/** The chunking strategy used to chunk the file(s). If not set, will use the `auto` strategy. */
export type ChunkingStrategyRequestParam = AutoChunkingStrategyRequestParam | StaticChunkingStrategyRequestParam;

/** Decode a `ChunkingStrategyRequestParam` by its `type` discriminator (unknown value falls through). */
export function decodeChunkingStrategyRequestParam(data: unknown): ChunkingStrategyRequestParam {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "auto":
      return data as AutoChunkingStrategyRequestParam;
    case "static":
      return data as StaticChunkingStrategyRequestParam;
    default:
      return data as ChunkingStrategyRequestParam;
  }
}

export interface DeleteVectorStoreFileResponse {
  id: string;
  deleted: boolean;
  object: "vector_store.file.deleted";
}

export type VectorStoresFilesListOrder = "asc" | "desc";

export type VectorStoresFilesListFilter = "in_progress" | "completed" | "failed" | "cancelled";

export interface UpdateVectorStoreFileAttributesRequest {
  attributes: VectorStoreFileAttributes;
}

export type VectorStoresListOrder = "asc" | "desc";

export interface ListVectorStoresResponse {
  object: string;
  data: VectorStoreObject[];
  first_id: string;
  last_id: string;
  has_more: boolean;
}

export interface VectorStoreSearchRequest {
  /** A query string for a search */
  query: VectorStoreSearchRequestQuery;
  /** Whether to rewrite the natural language query for vector search. */
  rewrite_query?: boolean;
  /** The maximum number of results to return. This number should be between 1 and 50 inclusive. */
  max_num_results?: number;
  /** A filter to apply based on file attributes. */
  filters?: VectorStoreSearchRequestFilters;
  /** Ranking options for search. */
  ranking_options?: VectorStoreSearchRequestRankingOptions;
}

/** A query string for a search */
export type VectorStoreSearchRequestQuery = string | string[];

/** A filter to apply based on file attributes. */
export type VectorStoreSearchRequestFilters = ComparisonFilter | CompoundFilter;

/** Ranking options for search. */
export interface VectorStoreSearchRequestRankingOptions {
  /** Enable re-ranking; set to `none` to disable, which can help reduce latency. */
  ranker?: VectorStoreSearchRequestRankingOptionsRanker;
  score_threshold?: number;
}

/** Enable re-ranking; set to `none` to disable, which can help reduce latency. */
export type VectorStoreSearchRequestRankingOptionsRanker = "none" | "auto" | "default-2024-11-15";

export interface VectorStoreSearchResultsPage {
  /** The object type, which is always `vector_store.search_results.page` */
  object: "vector_store.search_results.page";
  search_query: string[];
  /** The list of search result items. */
  data: VectorStoreSearchResultItem[];
  /** Indicates if there are more results to fetch. */
  has_more: boolean;
  next_page: string | null;
}

export interface VectorStoreSearchResultItem {
  /** The ID of the vector store file. */
  file_id: string;
  /** The name of the vector store file. */
  filename: string;
  /** The similarity score for the result. */
  score: number;
  attributes: VectorStoreFileAttributes;
  /** Content chunks from the file. */
  content: VectorStoreSearchResultContentObject[];
}

export interface VectorStoreSearchResultContentObject {
  /** The type of content. */
  type: "text";
  /** The text content returned from search. */
  text: string;
}

export interface UpdateVectorStoreRequest {
  /** The name of the vector store. */
  name?: string | null;
  expires_after?: VectorStoreExpirationAfter | null;
  metadata?: Metadata;
}

/** Parameters for creating a character from an uploaded video. */
export interface CreateVideoCharacterBody {
  /** Video file used to create a character. */
  video: Uint8Array | Blob;
  /** Display name for this API character. */
  name: string;
}

export interface VideoCharacterResource {
  id: string | null;
  name: string | null;
  /** Unix timestamp (in seconds) when the character was created. */
  created_at: number;
}

/** JSON parameters for creating a new video generation job. */
export interface CreateVideoJsonBody {
  model?: VideoModel;
  /** Text prompt that describes the video to generate. */
  prompt: string;
  input_reference?: ImageRefParam2;
  seconds?: VideoSeconds;
  size?: VideoSize;
}

export type VideoModel = string | VideoModelString;

export type VideoModelString = "sora-2" | "sora-2-pro" | "sora-2-2025-10-06" | "sora-2-pro-2025-10-06" | "sora-2-2025-12-08";

export interface ImageRefParam2 {
  /** A fully qualified URL or base64-encoded data URL. */
  image_url?: string;
  file_id?: string;
}

export type VideoSeconds = "4" | "8" | "12";

export type VideoSize = "720x1280" | "1280x720" | "1024x1792" | "1792x1024";

/** Structured information describing a generated video job. */
export interface VideoResource {
  /** Unique identifier for the video job. */
  id: string;
  /** The object type, which is always `video`. */
  object: "video";
  model: VideoModel;
  status: VideoStatus;
  /** Approximate completion percentage for the generation task. */
  progress: number;
  /** Unix timestamp (seconds) for when the job was created. */
  created_at: number;
  completed_at: number | null;
  expires_at: number | null;
  prompt: string | null;
  size: VideoSize;
  /** Duration of the generated clip in seconds. For extensions, this is the stitched total duration. */
  seconds: string;
  remixed_from_video_id: string | null;
  error: Error2 | null;
}

export type VideoStatus = "queued" | "in_progress" | "completed" | "failed";

/** An error that occurred while generating the response. */
export interface Error2 {
  /** A machine-readable error code that was returned. */
  code: string;
  /** A human-readable description of the error that was returned. */
  message: string;
}

/** Confirmation payload returned after deleting a video. */
export interface DeletedVideoResource {
  /** The object type that signals the deletion response. */
  object: "video.deleted";
  /** Indicates that the video resource was deleted. */
  deleted: boolean;
  /** Identifier of the deleted video. */
  id: string;
}

export type VideoContentVariant = "video" | "thumbnail" | "spritesheet";

/** JSON parameters for editing an existing generated video. */
export interface CreateVideoEditJsonBody {
  video: VideoReferenceInputParam;
  /** Text prompt that describes how to edit the source video. */
  prompt: string;
}

/** Reference to the completed video. */
export interface VideoReferenceInputParam {
  /** The identifier of the completed video. */
  id: string;
}

/** JSON parameters for extending an existing generated video. */
export interface CreateVideoExtendJsonBody {
  video: VideoReferenceInputParam;
  /** Updated text prompt that directs the extension generation. */
  prompt: string;
  seconds: VideoSeconds;
}

export interface VideoListResource {
  /** The type of object returned, must be `list`. */
  object: "list";
  /** A list of items */
  data: VideoResource[];
  first_id: string | null;
  last_id: string | null;
  /** Whether there are more items available. */
  has_more: boolean;
}

/** Parameters for remixing an existing generated video. */
export interface CreateVideoRemixBody {
  /** Updated text prompt that directs the remix generation. */
  prompt: string;
}

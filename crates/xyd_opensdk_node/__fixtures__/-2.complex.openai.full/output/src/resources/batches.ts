// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { RequestOptions } from '../core/request';
import { CursorPage } from '../core/pagination';
import type { Batch, BatchFileExpirationAfter, BatchesCreateRequestEndpoint, Metadata } from '../models';

export class Batches extends APIResource {
  /** Cancels an in-progress batch. The batch will be in status `cancelling` for up to 10 minutes, before changing to `cancelled`, where it will have partial results (if any) available in the output file. */
  cancel(batchId: string, options?: RequestOptions): Promise<Batch> {
    if (!batchId) {
      throw new Error("missing required batch_id parameter");
    }
    return this._client.request<Batch>({ method: "POST", path: `/batches/${batchId}/cancel` }, options);
  }

  /** Creates and executes a batch from an uploaded file of requests */
  create(body: BatchCreateParams, options?: RequestOptions): Promise<Batch> {
    return this._client.request<Batch>({ method: "POST", path: "/batches", body }, options);
  }

  /** List your organization's batches. */
  list(query?: BatchListParams, options?: RequestOptions): Promise<CursorPage<Batch>> {
    return this._client
      .request<unknown>({ method: "GET", path: "/batches", query }, options)
      .then((raw) => CursorPage.fromResponse<Batch>(raw));
  }

  /** Retrieves a batch. */
  retrieve(batchId: string, options?: RequestOptions): Promise<Batch> {
    if (!batchId) {
      throw new Error("missing required batch_id parameter");
    }
    return this._client.request<Batch>({ method: "GET", path: `/batches/${batchId}` }, options);
  }
}

export interface BatchCreateParams {
  input_file_id: string;
  endpoint: BatchesCreateRequestEndpoint;
  completion_window: "24h";
  metadata?: Metadata;
  output_expires_after?: BatchFileExpirationAfter;
}

export interface BatchListParams {
  after?: string;
  limit?: number;
}

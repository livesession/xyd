// Code generated by opensdk. DO NOT EDIT.

import { test } from 'node:test';
import assert from 'node:assert';

import { assertMatchesType, checkTestServer, testClient } from './setup';

test("cancel (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.responses.cancel("response_id");
  assertMatchesType(result);
});

test("cancel (path params)", () => {
  const client = testClient();
  assert.throws(() => client.responses.cancel(""), /missing required response_id parameter/);
});

test("create (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.responses.create({});
  assertMatchesType(result);
});

test("create (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.responses.create({ metadata: { key: "x" }, top_logprobs: 0, temperature: 0, top_p: 0, user: "x", safety_identifier: "x", prompt_cache_key: "x", service_tier: "auto", prompt_cache_retention: "in_memory", previous_response_id: "x", model: "x", reasoning: { effort: "none", summary: "auto", generate_summary: "auto" }, background: true, max_tool_calls: 0, text: { format: { type: "text" }, verbosity: "low" }, tools: [{ type: "function", name: "x", parameters: { key: {} as any }, strict: true, description: "x", defer_loading: true }], tool_choice: "none", prompt: { id: "x", version: "x", variables: { key: "x" } }, truncation: "auto", input: "x", include: ["file_search_call.results"], parallel_tool_calls: true, store: true, instructions: "x", stream: true, stream_options: { include_obfuscation: true }, conversation: "x", context_management: [{ type: "x", compact_threshold: 0 }], max_output_tokens: 0 });
  assertMatchesType(result);
});

test("delete (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  await client.responses.delete("response_id");
});

test("delete (path params)", () => {
  const client = testClient();
  assert.throws(() => client.responses.delete(""), /missing required response_id parameter/);
});

test("retrieve (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.responses.retrieve("response_id");
  assertMatchesType(result);
});

test("retrieve (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.responses.retrieve("response_id", { include: ["file_search_call.results"], stream: true, starting_after: 0, include_obfuscation: true });
  assertMatchesType(result);
});

test("retrieve (path params)", () => {
  const client = testClient();
  assert.throws(() => client.responses.retrieve(""), /missing required response_id parameter/);
});

test("compact.create (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.responses.compact.create({ model: "x" });
  assertMatchesType(result);
});

test("compact.create (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.responses.compact.create({ model: "x", input: "x", previous_response_id: "x", instructions: "x", prompt_cache_key: "x", prompt_cache_retention: "in_memory", service_tier: "auto" });
  assertMatchesType(result);
});

test("inputItems.list (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.responses.inputItems.list("response_id");
  assertMatchesType(result);
});

test("inputItems.list (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.responses.inputItems.list("response_id", { limit: 0, order: "asc", after: "after", include: ["file_search_call.results"] });
  assertMatchesType(result);
});

test("inputItems.list (path params)", () => {
  const client = testClient();
  assert.throws(() => client.responses.inputItems.list(""), /missing required response_id parameter/);
});

test("inputTokens.count (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.responses.inputTokens.count();
  assertMatchesType(result);
});

test("inputTokens.count (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.responses.inputTokens.count({ model: "x", input: "x", previous_response_id: "x", tools: [{ type: "function", name: "x", parameters: { key: {} as any }, strict: true, description: "x", defer_loading: true }], text: { format: { type: "text" }, verbosity: "low" }, reasoning: { effort: "none", summary: "auto", generate_summary: "auto" }, truncation: "auto", instructions: "x", conversation: "x", tool_choice: "none", parallel_tool_calls: true });
  assertMatchesType(result);
});

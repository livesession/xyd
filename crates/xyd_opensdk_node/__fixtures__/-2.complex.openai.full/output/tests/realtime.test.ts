// Code generated by opensdk. DO NOT EDIT.

import { test } from 'node:test';
import assert from 'node:assert';

import { assertMatchesType, checkTestServer, testClient } from './setup';

test("calls.accept (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  await client.realtime.calls.accept("call_id", { type: "realtime" });
});

test("calls.accept (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  await client.realtime.calls.accept("call_id", { type: "realtime", output_modalities: ["text"], model: "x", instructions: "x", audio: { input: { format: { type: "audio/pcm", rate: 24000 }, transcription: { model: "x", language: "x", prompt: "x", delay: "minimal" }, noise_reduction: { type: "near_field" }, turn_detection: { type: "server_vad", threshold: 0, prefix_padding_ms: 0, silence_duration_ms: 0, create_response: true, interrupt_response: true, idle_timeout_ms: 0 } }, output: { format: { type: "audio/pcm", rate: 24000 }, voice: "x", speed: 0 } }, include: ["item.input_audio_transcription.logprobs"], tracing: "auto", tools: [{ type: "function", name: "x", description: "x", parameters: { key: {} as any } }], tool_choice: "none", parallel_tool_calls: true, reasoning: { effort: "minimal" }, max_output_tokens: 0, truncation: "auto", prompt: { id: "x", version: "x", variables: { key: "x" } } });
});

test("calls.accept (path params)", () => {
  const client = testClient();
  assert.throws(() => client.realtime.calls.accept("", { type: "realtime" }), /missing required call_id parameter/);
});

test("calls.create (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.realtime.calls.create({ sdp: "x" });
  assertMatchesType(result);
});

test("calls.create (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.realtime.calls.create({ sdp: "x", session: { type: "realtime", output_modalities: ["text"], model: "x", instructions: "x", audio: { input: { format: { type: "audio/pcm", rate: 24000 }, transcription: { model: "x", language: "x", prompt: "x", delay: "minimal" }, noise_reduction: { type: "near_field" }, turn_detection: { type: "server_vad", threshold: 0, prefix_padding_ms: 0, silence_duration_ms: 0, create_response: true, interrupt_response: true, idle_timeout_ms: 0 } }, output: { format: { type: "audio/pcm", rate: 24000 }, voice: "x", speed: 0 } }, include: ["item.input_audio_transcription.logprobs"], tracing: "auto", tools: [{ type: "function", name: "x", description: "x", parameters: { key: {} as any } }], tool_choice: "none", parallel_tool_calls: true, reasoning: { effort: "minimal" }, max_output_tokens: 0, truncation: "auto", prompt: { id: "x", version: "x", variables: { key: "x" } } } });
  assertMatchesType(result);
});

test("calls.hangup (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  await client.realtime.calls.hangup("call_id");
});

test("calls.hangup (path params)", () => {
  const client = testClient();
  assert.throws(() => client.realtime.calls.hangup(""), /missing required call_id parameter/);
});

test("calls.refer (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  await client.realtime.calls.refer("call_id", { target_uri: "x" });
});

test("calls.refer (path params)", () => {
  const client = testClient();
  assert.throws(() => client.realtime.calls.refer("", { target_uri: "x" }), /missing required call_id parameter/);
});

test("calls.reject (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  await client.realtime.calls.reject("call_id");
});

test("calls.reject (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  await client.realtime.calls.reject("call_id", { status_code: 0 });
});

test("calls.reject (path params)", () => {
  const client = testClient();
  assert.throws(() => client.realtime.calls.reject(""), /missing required call_id parameter/);
});

test("clientSecrets.create (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.realtime.clientSecrets.create({});
  assertMatchesType(result);
});

test("clientSecrets.create (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.realtime.clientSecrets.create({ expires_after: { anchor: "created_at", seconds: 0 }, session: { type: "realtime", output_modalities: ["text"], model: "x", instructions: "x", audio: { input: { format: { type: "audio/pcm", rate: 24000 }, transcription: { model: "x", language: "x", prompt: "x", delay: "minimal" }, noise_reduction: { type: "near_field" }, turn_detection: { type: "server_vad", threshold: 0, prefix_padding_ms: 0, silence_duration_ms: 0, create_response: true, interrupt_response: true, idle_timeout_ms: 0 } }, output: { format: { type: "audio/pcm", rate: 24000 }, voice: "x", speed: 0 } }, include: ["item.input_audio_transcription.logprobs"], tracing: "auto", tools: [{ type: "function", name: "x", description: "x", parameters: { key: {} as any } }], tool_choice: "none", parallel_tool_calls: true, reasoning: { effort: "minimal" }, max_output_tokens: 0, truncation: "auto", prompt: { id: "x", version: "x", variables: { key: "x" } } } });
  assertMatchesType(result);
});

test("sessions.create (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.realtime.sessions.create({ client_secret: { value: "x", expires_at: 0 } });
  assertMatchesType(result);
});

test("sessions.create (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.realtime.sessions.create({ client_secret: { value: "x", expires_at: 0 }, modalities: {} as any, instructions: "x", voice: "x", input_audio_format: "x", output_audio_format: "x", input_audio_transcription: { model: "x" }, speed: 0, tracing: "auto", turn_detection: { type: "x", threshold: 0, prefix_padding_ms: 0, silence_duration_ms: 0 }, tools: [{ type: "function", name: "x", description: "x", parameters: { key: {} as any } }], tool_choice: "x", temperature: 0, max_response_output_tokens: 0, truncation: "auto", prompt: { id: "x", version: "x", variables: { key: "x" } } });
  assertMatchesType(result);
});

test("transcriptionSessions.create (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.realtime.transcriptionSessions.create({});
  assertMatchesType(result);
});

test("transcriptionSessions.create (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.realtime.transcriptionSessions.create({ turn_detection: { type: "server_vad", threshold: 0, prefix_padding_ms: 0, silence_duration_ms: 0 }, input_audio_noise_reduction: { type: "near_field" }, input_audio_format: "pcm16", input_audio_transcription: { model: "x", language: "x", prompt: "x", delay: "minimal" }, include: ["item.input_audio_transcription.logprobs"] });
  assertMatchesType(result);
});

test("translations.clientSecrets.create (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.realtime.translations.clientSecrets.create({ session: { model: "x" } });
  assertMatchesType(result);
});

test("translations.clientSecrets.create (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.realtime.translations.clientSecrets.create({ session: { model: "x", audio: { input: { transcription: { model: "x" }, noise_reduction: { type: "near_field" } }, output: { language: "x" } } }, expires_after: { anchor: "created_at", seconds: 0 } });
  assertMatchesType(result);
});

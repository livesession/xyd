// Code generated by opensdk. DO NOT EDIT.

import { test } from 'node:test';

import { assertMatchesType, checkTestServer, testClient } from './setup';

test("edits.create (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.images.edits.create({ images: [{} as any], prompt: "x" });
  assertMatchesType(result);
});

test("edits.create (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.images.edits.create({ images: [{} as any], prompt: "x", model: "x", mask: {} as any, n: 0, quality: "low", input_fidelity: "high", size: "auto", user: "x", output_format: "png", output_compression: 0, moderation: "low", background: "transparent", stream: true, partial_images: 0 });
  assertMatchesType(result);
});

test("generations.create (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.images.generations.create({ prompt: "x" });
  assertMatchesType(result);
});

test("generations.create (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.images.generations.create({ prompt: "x", model: "x", n: 0, quality: "standard", response_format: "url", output_format: "png", output_compression: 0, stream: true, partial_images: 0, size: "x", moderation: "low", background: "transparent", style: "vivid", user: "x" });
  assertMatchesType(result);
});

test("variations.create (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.images.variations.create({ image: new Uint8Array([1, 2, 3]) });
  assertMatchesType(result);
});

test("variations.create (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.images.variations.create({ image: new Uint8Array([1, 2, 3]), model: "x", n: 0, response_format: "url", size: "256x256", user: "x" });
  assertMatchesType(result);
});

// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;

namespace Example.Acme;

public sealed class ChatService
{
    private readonly Transport _transport;
    public ChatCompletionsService Completions { get; }

    internal ChatService(Transport transport)
    {
        _transport = transport;
        Completions = new ChatCompletionsService(transport);
    }
}

public sealed class ChatCompletionsService
{
    private readonly Transport _transport;

    internal ChatCompletionsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>List completions</summary>
    public async Task<CommandResult> List(CancellationToken cancellationToken = default)
    {
        var argv = new List<string> { "chat", "completions", "list" };
        return await _transport.RunAsync(argv, cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Create a completion</summary>
    public async Task<CommandResult> Create(string prompt, string model, double? temperature = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(prompt))
        {
            throw new ArgumentException("Expected a non-empty value for prompt.", nameof(prompt));
        }
        var argv = new List<string> { "chat", "completions", "create" };
        argv.Add(prompt);
        argv.Add("--model");
        argv.Add(model);
        if (temperature != null)
        {
            argv.Add("--temperature");
            argv.Add(Transport.FormatValue(temperature.Value));
        }
        return await _transport.RunAsync(argv, cancellationToken).ConfigureAwait(false);
    }
}

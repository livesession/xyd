// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>
#nullable enable annotations

using System;
using System.Collections.Generic;
using System.Threading;
using System.Threading.Tasks;

namespace Example.Packer;

/// <summary>packer CLI client (drives the `packer` binary).</summary>
public sealed class PackerClient
{
    private readonly Transport _transport;

    /// <summary>Creates a client that runs `packer`. When binPath is null the binary is resolved from the PACKER_BIN environment variable, then PATH.</summary>
    public PackerClient(string? binPath = null, IReadOnlyDictionary<string, string>? env = null, string? cwd = null, int? timeoutMs = null)
    {
        _transport = new Transport(binPath, env, cwd, timeoutMs);
    }

    /// <summary>Pack files</summary>
    public async Task<CommandResult> Pack(string dest, List<string> files, List<string>? tag = null, object? meta = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(dest))
        {
            throw new ArgumentException("Expected a non-empty value for dest.", nameof(dest));
        }
        var argv = new List<string> { "pack" };
        argv.Add(dest);
        foreach (var value in files)
        {
            argv.Add(value);
        }
        if (tag != null)
        {
            foreach (var value in tag)
            {
                argv.Add("--tag");
                argv.Add(value);
            }
        }
        if (meta != null)
        {
            argv.Add("--meta");
            argv.Add(Transport.JsonArg(meta));
        }
        return await _transport.RunAsync(argv, cancellationToken).ConfigureAwait(false);
    }
}

// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct System {
    transport: Arc<Transport>,
}

impl System {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        System { transport }
    }

    pub fn health(&self) -> SystemHealth {
        SystemHealth::new(self.transport.clone())
    }
}

pub struct SystemHealth {
    transport: Arc<Transport>,
}

impl SystemHealth {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        SystemHealth { transport }
    }

    /// Health check
    pub async fn check(&self) -> Result<SystemHealthCheckResponse, Error> {
        let req = self.transport.request(reqwest::Method::GET, "/misc/ping");
        req.send().await
    }
}

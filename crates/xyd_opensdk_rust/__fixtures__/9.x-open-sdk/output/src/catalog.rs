// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Catalog {
    transport: Arc<Transport>,
}

impl Catalog {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Catalog { transport }
    }

    /// List things
    pub async fn browse(&self) -> Result<Vec<Thing>, Error> {
        let req = self.transport.request(reqwest::Method::GET, "/things");
        req.send().await
    }
}

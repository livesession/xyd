// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Realtime {
    transport: Arc<Transport>,
}

impl Realtime {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Realtime { transport }
    }

    pub fn translations(&self) -> RealtimeTranslations {
        RealtimeTranslations::new(self.transport.clone())
    }
}

pub struct RealtimeTranslations {
    transport: Arc<Transport>,
}

impl RealtimeTranslations {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        RealtimeTranslations { transport }
    }

    pub fn client_secrets(&self) -> RealtimeTranslationsClientSecrets {
        RealtimeTranslationsClientSecrets::new(self.transport.clone())
    }
}

pub struct RealtimeTranslationsClientSecrets {
    transport: Arc<Transport>,
}

impl RealtimeTranslationsClientSecrets {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        RealtimeTranslationsClientSecrets { transport }
    }

    /// Create a Realtime translation client secret with an associated translation session configuration.
    ///
    /// Client secrets are short-lived tokens that can be passed to a client app,
    /// such as a web frontend or mobile client, which grants access to the Realtime
    /// Translation API without leaking your main API key. You can configure a custom
    /// TTL for each client secret.
    ///
    /// Returns the created client secret and the effective translation session object.
    /// The client secret is a string that looks like `ek_1234`.
    pub async fn create(&self, params: RealtimeTranslationsClientSecretsCreateParams) -> Result<RealtimeTranslationClientSecretCreateResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/realtime/translations/client_secrets");
        if let Some(value) = &params.expires_after {
            req = req.json_field("expires_after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.session {
            req = req.json_field("session", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct RealtimeTranslationsClientSecretsCreateParams {
    /// Configuration for the client secret expiration. Expiration refers to the time after which
    /// a client secret will no longer be valid for creating sessions. The session itself may
    /// continue after that time once started. A secret can be used to create multiple sessions
    /// until it expires.
    pub expires_after: Option<RealtimeTranslationClientSecretCreateRequestExpiresAfter>,
    pub session: Option<RealtimeTranslationSessionCreateRequest>,
}

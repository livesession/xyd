// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Images {
    transport: Arc<Transport>,
}

impl Images {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Images { transport }
    }

    pub fn variations(&self) -> ImagesVariations {
        ImagesVariations::new(self.transport.clone())
    }
}

pub struct ImagesVariations {
    transport: Arc<Transport>,
}

impl ImagesVariations {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ImagesVariations { transport }
    }

    /// Creates a variation of a given image. This endpoint only supports `dall-e-2`.
    pub async fn create(&self, params: ImagesVariationsCreateParams) -> Result<ImagesResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/images/variations");
        if let Some(value) = &params.image {
            req = req.multipart_file("image", value.clone());
        }
        if let Some(value) = &params.model {
            req = req.multipart_json("model", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.n {
            req = req.multipart_json("n", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.response_format {
            req = req.multipart_json("response_format", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.size {
            req = req.multipart_json("size", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.user {
            req = req.multipart_json("user", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct ImagesVariationsCreateParams {
    /// The image to use as the basis for the variation(s). Must be a valid PNG file, less than 4MB, and square.
    pub image: Option<Vec<u8>>,
    /// The model to use for image generation. Only `dall-e-2` is supported at this time.
    pub model: Option<CreateImageVariationRequestModel>,
    /// The number of images to generate. Must be between 1 and 10.
    pub n: Option<i64>,
    /// The format in which the generated images are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated.
    pub response_format: Option<CreateImageVariationRequestResponseFormat>,
    /// The size of the generated images. Must be one of `256x256`, `512x512`, or `1024x1024`.
    pub size: Option<CreateImageVariationRequestSize>,
    /// A unique identifier representing your end-user, which can help OpenAI to monitor and detect abuse. [Learn more](/docs/guides/safety-best-practices#end-user-ids).
    pub user: Option<String>,
}

// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Conversations {
    transport: Arc<Transport>,
}

impl Conversations {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Conversations { transport }
    }

    /// Delete a conversation. Items in the conversation will not be deleted.
    pub async fn delete(&self, conversation_id: &str) -> Result<DeletedConversationResource, Error> {
        if conversation_id.is_empty() {
            return Err(Error::InvalidArgument("conversation_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/conversations/{}", conversation_id));
        req.send().await
    }
}

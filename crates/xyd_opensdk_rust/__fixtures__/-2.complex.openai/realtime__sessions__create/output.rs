// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Realtime {
    transport: Arc<Transport>,
}

impl Realtime {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Realtime { transport }
    }

    pub fn sessions(&self) -> RealtimeSessions {
        RealtimeSessions::new(self.transport.clone())
    }
}

pub struct RealtimeSessions {
    transport: Arc<Transport>,
}

impl RealtimeSessions {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        RealtimeSessions { transport }
    }

    /// Create an ephemeral API token for use in client-side applications with the
    /// Realtime API. Can be configured with the same session parameters as the
    /// `session.update` client event.
    ///
    /// It responds with a session object, plus a `client_secret` key which contains
    /// a usable ephemeral API token that can be used to authenticate browser clients
    /// for the Realtime API.
    ///
    /// Returns the created Realtime session object, plus an ephemeral key.
    pub async fn create(&self, params: RealtimeSessionsCreateParams) -> Result<RealtimeSessionCreateResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/realtime/sessions");
        if let Some(value) = &params.client_secret {
            req = req.json_field("client_secret", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.modalities {
            req = req.json_field("modalities", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.instructions {
            req = req.json_field("instructions", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.voice {
            req = req.json_field("voice", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.input_audio_format {
            req = req.json_field("input_audio_format", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.output_audio_format {
            req = req.json_field("output_audio_format", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.input_audio_transcription {
            req = req.json_field("input_audio_transcription", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.speed {
            req = req.json_field("speed", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tracing {
            req = req.json_field("tracing", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.turn_detection {
            req = req.json_field("turn_detection", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tools {
            req = req.json_field("tools", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tool_choice {
            req = req.json_field("tool_choice", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.temperature {
            req = req.json_field("temperature", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.max_response_output_tokens {
            req = req.json_field("max_response_output_tokens", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.truncation {
            req = req.json_field("truncation", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.prompt {
            req = req.json_field("prompt", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct RealtimeSessionsCreateParams {
    /// Ephemeral key returned by the API.
    pub client_secret: Option<RealtimeSessionCreateRequestClientSecret>,
    /// The set of modalities the model can respond with. To disable audio,
    /// set this to ["text"].
    pub modalities: Option<serde_json::Value>,
    /// The default system instructions (i.e. system message) prepended to model calls. This field allows the client to guide the model on desired responses. The model can be instructed on response content and format, (e.g. "be extremely succinct", "act friendly", "here are examples of good responses") and on audio behavior (e.g. "talk quickly", "inject emotion into your voice", "laugh frequently"). The instructions are not guaranteed to be followed by the model, but they provide guidance to the model on the desired behavior.
    /// Note that the server sets default instructions which will be used if this field is not set and are visible in the `session.created` event at the start of the session.
    pub instructions: Option<String>,
    pub voice: Option<VoiceIdsOrCustomVoice>,
    /// The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
    pub input_audio_format: Option<String>,
    /// The format of output audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
    pub output_audio_format: Option<String>,
    /// Configuration for input audio transcription, defaults to off and can be
    /// set to `null` to turn off once on. Input audio transcription is not native
    /// to the model, since the model consumes audio directly. Transcription runs
    /// asynchronously and should be treated as rough guidance
    /// rather than the representation understood by the model.
    pub input_audio_transcription: Option<RealtimeSessionCreateRequestInputAudioTranscription>,
    /// The speed of the model's spoken response. 1.0 is the default speed. 0.25 is
    /// the minimum speed. 1.5 is the maximum speed. This value can only be changed
    /// in between model turns, not while a response is in progress.
    pub speed: Option<f64>,
    /// Configuration options for tracing. Set to null to disable tracing. Once
    /// tracing is enabled for a session, the configuration cannot be modified.
    ///
    /// `auto` will create a trace for the session with default values for the
    /// workflow name, group id, and metadata.
    pub tracing: Option<RealtimeSessionCreateRequestTracing>,
    /// Configuration for turn detection. Can be set to `null` to turn off. Server
    /// VAD means that the model will detect the start and end of speech based on
    /// audio volume and respond at the end of user speech.
    pub turn_detection: Option<RealtimeSessionCreateRequestTurnDetection>,
    /// Tools (functions) available to the model.
    pub tools: Option<Vec<RealtimeSessionCreateRequestToolsItem>>,
    /// How the model chooses tools. Options are `auto`, `none`, `required`, or
    /// specify a function.
    pub tool_choice: Option<String>,
    /// Sampling temperature for the model, limited to [0.6, 1.2]. Defaults to 0.8.
    pub temperature: Option<f64>,
    /// Maximum number of output tokens for a single assistant response,
    /// inclusive of tool calls. Provide an integer between 1 and 4096 to
    /// limit output tokens, or `inf` for the maximum available tokens for a
    /// given model. Defaults to `inf`.
    pub max_response_output_tokens: Option<RealtimeSessionCreateRequestMaxResponseOutputTokens>,
    pub truncation: Option<RealtimeTruncation>,
    pub prompt: Option<Prompt>,
}

// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Responses {
    transport: Arc<Transport>,
}

impl Responses {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Responses { transport }
    }

    /// Creates a model response. Provide [text](/docs/guides/text) or
    /// [image](/docs/guides/images) inputs to generate [text](/docs/guides/text)
    /// or [JSON](/docs/guides/structured-outputs) outputs. Have the model call
    /// your own [custom code](/docs/guides/function-calling) or use built-in
    /// [tools](/docs/guides/tools) like [web search](/docs/guides/tools-web-search)
    /// or [file search](/docs/guides/tools-file-search) to use your own data
    /// as input for the model's response.
    pub async fn create(&self, params: ResponsesCreateParams) -> Result<Response, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/responses");
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.top_logprobs {
            req = req.json_field("top_logprobs", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.temperature {
            req = req.json_field("temperature", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.top_p {
            req = req.json_field("top_p", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.user {
            req = req.json_field("user", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.safety_identifier {
            req = req.json_field("safety_identifier", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.prompt_cache_key {
            req = req.json_field("prompt_cache_key", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.service_tier {
            req = req.json_field("service_tier", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.prompt_cache_retention {
            req = req.json_field("prompt_cache_retention", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.previous_response_id {
            req = req.json_field("previous_response_id", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.model {
            req = req.json_field("model", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.reasoning {
            req = req.json_field("reasoning", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.background {
            req = req.json_field("background", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.max_tool_calls {
            req = req.json_field("max_tool_calls", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.text {
            req = req.json_field("text", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tools {
            req = req.json_field("tools", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tool_choice {
            req = req.json_field("tool_choice", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.prompt {
            req = req.json_field("prompt", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.truncation {
            req = req.json_field("truncation", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.input {
            req = req.json_field("input", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.include {
            req = req.json_field("include", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.parallel_tool_calls {
            req = req.json_field("parallel_tool_calls", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.store {
            req = req.json_field("store", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.instructions {
            req = req.json_field("instructions", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.stream {
            req = req.json_field("stream", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.stream_options {
            req = req.json_field("stream_options", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.conversation {
            req = req.json_field("conversation", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.context_management {
            req = req.json_field("context_management", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.max_output_tokens {
            req = req.json_field("max_output_tokens", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct ResponsesCreateParams {
    pub metadata: Option<Metadata>,
    /// An integer between 0 and 20 specifying the maximum number of most likely
    /// tokens to return at each token position, each with an associated log
    /// probability. In some cases, the number of returned tokens may be fewer than
    /// requested.
    pub top_logprobs: Option<i64>,
    pub temperature: Option<f64>,
    pub top_p: Option<f64>,
    /// This field is being replaced by `safety_identifier` and `prompt_cache_key`. Use `prompt_cache_key` instead to maintain caching optimizations.
    /// A stable identifier for your end-users.
    /// Used to boost cache hit rates by better bucketing similar requests and  to help OpenAI detect and prevent abuse. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).
    pub user: Option<String>,
    /// A stable identifier used to help detect users of your application that may be violating OpenAI's usage policies.
    /// The IDs should be a string that uniquely identifies each user, with a maximum length of 64 characters. We recommend hashing their username or email address, in order to avoid sending us any identifying information. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).
    pub safety_identifier: Option<String>,
    /// Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the `user` field. [Learn more](/docs/guides/prompt-caching).
    pub prompt_cache_key: Option<String>,
    pub service_tier: Option<ServiceTier>,
    pub prompt_cache_retention: Option<CreateResponsePromptCacheRetention>,
    pub previous_response_id: Option<String>,
    pub model: Option<ModelIdsResponses>,
    pub reasoning: Option<Reasoning>,
    pub background: Option<bool>,
    pub max_tool_calls: Option<i64>,
    pub text: Option<ResponseTextParam>,
    pub tools: Option<ToolsArray>,
    pub tool_choice: Option<ToolChoiceParam>,
    pub prompt: Option<Prompt>,
    pub truncation: Option<CreateResponseTruncation>,
    pub input: Option<InputParam>,
    pub include: Option<Vec<IncludeEnum>>,
    pub parallel_tool_calls: Option<bool>,
    pub store: Option<bool>,
    pub instructions: Option<String>,
    pub stream: Option<bool>,
    pub stream_options: Option<ResponseStreamOptions>,
    pub conversation: Option<ConversationParam>,
    pub context_management: Option<Vec<ContextManagementParam>>,
    pub max_output_tokens: Option<i64>,
}

// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Chat {
    transport: Arc<Transport>,
}

impl Chat {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Chat { transport }
    }

    pub fn completions(&self) -> ChatCompletions {
        ChatCompletions::new(self.transport.clone())
    }
}

pub struct ChatCompletions {
    transport: Arc<Transport>,
}

impl ChatCompletions {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ChatCompletions { transport }
    }

    /// **Starting a new project?** We recommend trying [Responses](/docs/api-reference/responses)
    /// to take advantage of the latest OpenAI platform features. Compare
    /// [Chat Completions with Responses](/docs/guides/responses-vs-chat-completions?api-mode=responses).
    ///
    /// ---
    ///
    /// Creates a model response for the given chat conversation. Learn more in the
    /// [text generation](/docs/guides/text-generation), [vision](/docs/guides/vision),
    /// and [audio](/docs/guides/audio) guides.
    ///
    /// Parameter support can differ depending on the model used to generate the
    /// response, particularly for newer reasoning models. Parameters that are only
    /// supported for reasoning models are noted below. For the current state of
    /// unsupported parameters in reasoning models,
    /// [refer to the reasoning guide](/docs/guides/reasoning).
    ///
    /// Returns a chat completion object, or a streamed sequence of chat completion
    /// chunk objects if the request is streamed.
    pub async fn create(&self, params: ChatCompletionsCreateParams) -> Result<CreateChatCompletionResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/chat/completions");
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.top_logprobs {
            req = req.json_field("top_logprobs", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.temperature {
            req = req.json_field("temperature", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.top_p {
            req = req.json_field("top_p", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.user {
            req = req.json_field("user", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.safety_identifier {
            req = req.json_field("safety_identifier", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.prompt_cache_key {
            req = req.json_field("prompt_cache_key", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.service_tier {
            req = req.json_field("service_tier", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.prompt_cache_retention {
            req = req.json_field("prompt_cache_retention", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.messages {
            req = req.json_field("messages", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.model {
            req = req.json_field("model", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.modalities {
            req = req.json_field("modalities", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.verbosity {
            req = req.json_field("verbosity", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.reasoning_effort {
            req = req.json_field("reasoning_effort", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.max_completion_tokens {
            req = req.json_field("max_completion_tokens", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.frequency_penalty {
            req = req.json_field("frequency_penalty", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.presence_penalty {
            req = req.json_field("presence_penalty", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.web_search_options {
            req = req.json_field("web_search_options", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.response_format {
            req = req.json_field("response_format", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.audio {
            req = req.json_field("audio", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.store {
            req = req.json_field("store", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.stream {
            req = req.json_field("stream", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.stop {
            req = req.json_field("stop", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.logit_bias {
            req = req.json_field("logit_bias", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.logprobs {
            req = req.json_field("logprobs", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.max_tokens {
            req = req.json_field("max_tokens", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.n {
            req = req.json_field("n", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.prediction {
            req = req.json_field("prediction", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.seed {
            req = req.json_field("seed", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.stream_options {
            req = req.json_field("stream_options", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tools {
            req = req.json_field("tools", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tool_choice {
            req = req.json_field("tool_choice", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.parallel_tool_calls {
            req = req.json_field("parallel_tool_calls", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.function_call {
            req = req.json_field("function_call", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.functions {
            req = req.json_field("functions", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct ChatCompletionsCreateParams {
    pub metadata: Option<Metadata>,
    /// An integer between 0 and 20 specifying the maximum number of most likely
    /// tokens to return at each token position, each with an associated log
    /// probability. In some cases, the number of returned tokens may be fewer than
    /// requested.
    /// `logprobs` must be set to `true` if this parameter is used.
    pub top_logprobs: Option<i64>,
    pub temperature: Option<f64>,
    pub top_p: Option<f64>,
    /// This field is being replaced by `safety_identifier` and `prompt_cache_key`. Use `prompt_cache_key` instead to maintain caching optimizations.
    /// A stable identifier for your end-users.
    /// Used to boost cache hit rates by better bucketing similar requests and  to help OpenAI detect and prevent abuse. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).
    pub user: Option<String>,
    /// A stable identifier used to help detect users of your application that may be violating OpenAI's usage policies.
    /// The IDs should be a string that uniquely identifies each user, with a maximum length of 64 characters. We recommend hashing their username or email address, in order to avoid sending us any identifying information. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).
    pub safety_identifier: Option<String>,
    /// Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the `user` field. [Learn more](/docs/guides/prompt-caching).
    pub prompt_cache_key: Option<String>,
    pub service_tier: Option<ServiceTier>,
    pub prompt_cache_retention: Option<CreateChatCompletionRequestPromptCacheRetention>,
    /// A list of messages comprising the conversation so far. Depending on the
    /// [model](/docs/models) you use, different message types (modalities) are
    /// supported, like [text](/docs/guides/text-generation),
    /// [images](/docs/guides/vision), and [audio](/docs/guides/audio).
    pub messages: Option<Vec<ChatCompletionRequestMessage>>,
    pub model: Option<ModelIdsShared>,
    pub modalities: Option<ResponseModalities>,
    pub verbosity: Option<Verbosity>,
    pub reasoning_effort: Option<ReasoningEffort>,
    /// An upper bound for the number of tokens that can be generated for a completion, including visible output tokens and [reasoning tokens](/docs/guides/reasoning).
    pub max_completion_tokens: Option<i64>,
    /// Number between -2.0 and 2.0. Positive values penalize new tokens based on
    /// their existing frequency in the text so far, decreasing the model's
    /// likelihood to repeat the same line verbatim.
    pub frequency_penalty: Option<f64>,
    /// Number between -2.0 and 2.0. Positive values penalize new tokens based on
    /// whether they appear in the text so far, increasing the model's likelihood
    /// to talk about new topics.
    pub presence_penalty: Option<f64>,
    /// This tool searches the web for relevant results to use in a response.
    /// Learn more about the [web search tool](/docs/guides/tools-web-search?api-mode=chat).
    pub web_search_options: Option<CreateChatCompletionRequestWebSearchOptions>,
    /// An object specifying the format that the model must output.
    ///
    /// Setting to `{ "type": "json_schema", "json_schema": {...} }` enables
    /// Structured Outputs which ensures the model will match your supplied JSON
    /// schema. Learn more in the [Structured Outputs
    /// guide](/docs/guides/structured-outputs).
    ///
    /// Setting to `{ "type": "json_object" }` enables the older JSON mode, which
    /// ensures the message the model generates is valid JSON. Using `json_schema`
    /// is preferred for models that support it.
    pub response_format: Option<CreateChatCompletionRequestResponseFormat>,
    /// Parameters for audio output. Required when audio output is requested with
    /// `modalities: ["audio"]`. [Learn more](/docs/guides/audio).
    pub audio: Option<CreateChatCompletionRequestAudio>,
    /// Whether or not to store the output of this chat completion request for
    /// use in our [model distillation](/docs/guides/distillation) or
    /// [evals](/docs/guides/evals) products.
    ///
    /// Supports text and image inputs. Note: image inputs over 8MB will be dropped.
    pub store: Option<bool>,
    /// If set to true, the model response data will be streamed to the client
    /// as it is generated using [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format).
    /// See the [Streaming section below](/docs/api-reference/chat/streaming)
    /// for more information, along with the [streaming responses](/docs/guides/streaming-responses)
    /// guide for more information on how to handle the streaming events.
    pub stream: Option<bool>,
    pub stop: Option<StopConfiguration>,
    /// Modify the likelihood of specified tokens appearing in the completion.
    ///
    /// Accepts a JSON object that maps tokens (specified by their token ID in the
    /// tokenizer) to an associated bias value from -100 to 100. Mathematically,
    /// the bias is added to the logits generated by the model prior to sampling.
    /// The exact effect will vary per model, but values between -1 and 1 should
    /// decrease or increase likelihood of selection; values like -100 or 100
    /// should result in a ban or exclusive selection of the relevant token.
    pub logit_bias: Option<std::collections::HashMap<String, i64>>,
    /// Whether to return log probabilities of the output tokens or not. If true,
    /// returns the log probabilities of each output token returned in the
    /// `content` of `message`.
    pub logprobs: Option<bool>,
    /// The maximum number of [tokens](/tokenizer) that can be generated in the
    /// chat completion. This value can be used to control
    /// [costs](https://openai.com/api/pricing/) for text generated via API.
    ///
    /// This value is now deprecated in favor of `max_completion_tokens`, and is
    /// not compatible with [o-series models](/docs/guides/reasoning).
    pub max_tokens: Option<i64>,
    /// How many chat completion choices to generate for each input message. Note that you will be charged based on the number of generated tokens across all of the choices. Keep `n` as `1` to minimize costs.
    pub n: Option<i64>,
    /// Configuration for a [Predicted Output](/docs/guides/predicted-outputs),
    /// which can greatly improve response times when large parts of the model
    /// response are known ahead of time. This is most common when you are
    /// regenerating a file with only minor changes to most of the content.
    pub prediction: Option<PredictionContent>,
    /// This feature is in Beta.
    /// If specified, our system will make a best effort to sample deterministically, such that repeated requests with the same `seed` and parameters should return the same result.
    /// Determinism is not guaranteed, and you should refer to the `system_fingerprint` response parameter to monitor changes in the backend.
    pub seed: Option<i64>,
    pub stream_options: Option<ChatCompletionStreamOptions>,
    /// A list of tools the model may call. You can provide either
    /// [custom tools](/docs/guides/function-calling#custom-tools) or
    /// [function tools](/docs/guides/function-calling).
    pub tools: Option<Vec<CreateChatCompletionRequestToolsItem>>,
    pub tool_choice: Option<ChatCompletionToolChoiceOption>,
    pub parallel_tool_calls: Option<ParallelToolCalls>,
    /// Deprecated in favor of `tool_choice`.
    ///
    /// Controls which (if any) function is called by the model.
    ///
    /// `none` means the model will not call a function and instead generates a
    /// message.
    ///
    /// `auto` means the model can pick between generating a message or calling a
    /// function.
    ///
    /// Specifying a particular function via `{"name": "my_function"}` forces the
    /// model to call that function.
    ///
    /// `none` is the default when no functions are present. `auto` is the default
    /// if functions are present.
    pub function_call: Option<CreateChatCompletionRequestFunctionCall>,
    /// Deprecated in favor of `tools`.
    ///
    /// A list of functions the model may generate JSON inputs for.
    pub functions: Option<Vec<ChatCompletionFunctions>>,
}

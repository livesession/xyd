// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Chat {
    transport: Arc<Transport>,
}

impl Chat {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Chat { transport }
    }

    pub fn completions(&self) -> ChatCompletions {
        ChatCompletions::new(self.transport.clone())
    }
}

pub struct ChatCompletions {
    transport: Arc<Transport>,
}

impl ChatCompletions {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ChatCompletions { transport }
    }

    /// List stored Chat Completions. Only Chat Completions that have been stored
    /// with the `store` parameter set to `true` will be returned.
    pub async fn list(&self, params: ChatCompletionsListParams) -> Result<Page<CreateChatCompletionResponse>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/chat/completions");
        if let Some(value) = &params.model {
            req = req.query("model", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.metadata {
            req = req.query("metadata", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct ChatCompletionsListParams {
    /// The model used to generate the Chat Completions.
    pub model: Option<String>,
    /// A list of metadata keys to filter the Chat Completions by. Example:
    ///
    /// `metadata[key1]=value1&metadata[key2]=value2`
    pub metadata: Option<Metadata>,
    /// Identifier for the last chat completion from the previous pagination request.
    pub after: Option<String>,
    /// Number of Chat Completions to retrieve.
    pub limit: Option<i64>,
    /// Sort order for Chat Completions by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `asc`.
    pub order: Option<ChatCompletionsListOrder>,
}

// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Admin {
    transport: Arc<Transport>,
}

impl Admin {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Admin { transport }
    }

    pub fn organization(&self) -> AdminOrganization {
        AdminOrganization::new(self.transport.clone())
    }
}

pub struct AdminOrganization {
    transport: Arc<Transport>,
}

impl AdminOrganization {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganization { transport }
    }

    pub fn admin_api_keys(&self) -> AdminOrganizationAdminApiKeys {
        AdminOrganizationAdminApiKeys::new(self.transport.clone())
    }
}

pub struct AdminOrganizationAdminApiKeys {
    transport: Arc<Transport>,
}

impl AdminOrganizationAdminApiKeys {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationAdminApiKeys { transport }
    }

    /// Retrieve a paginated list of organization admin API keys.
    pub async fn list(&self, params: AdminOrganizationAdminApiKeysListParams) -> Result<Page<AdminApiKey>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/admin_api_keys");
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationAdminApiKeysListParams {
    pub after: Option<String>,
    pub order: Option<AdminOrganizationAdminApiKeysListOrder>,
    pub limit: Option<i64>,
}

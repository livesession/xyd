// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Projects {
    transport: Arc<Transport>,
}

impl Projects {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Projects { transport }
    }

    pub fn users(&self) -> ProjectsUsers {
        ProjectsUsers::new(self.transport.clone())
    }
}

pub struct ProjectsUsers {
    transport: Arc<Transport>,
}

impl ProjectsUsers {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ProjectsUsers { transport }
    }

    pub fn roles(&self) -> ProjectsUsersRoles {
        ProjectsUsersRoles::new(self.transport.clone())
    }
}

pub struct ProjectsUsersRoles {
    transport: Arc<Transport>,
}

impl ProjectsUsersRoles {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ProjectsUsersRoles { transport }
    }

    /// Unassigns a project role from a user within a project.
    pub async fn delete(&self, project_id: &str, user_id: &str, role_id: &str) -> Result<DeletedRoleAssignmentResource, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }
        if role_id.is_empty() {
            return Err(Error::InvalidArgument("role_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/projects/{}/users/{}/roles/{}", project_id, user_id, role_id));
        req.send().await
    }
}

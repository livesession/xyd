// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Beta {
    transport: Arc<Transport>,
}

impl Beta {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Beta { transport }
    }

    pub fn threads(&self) -> BetaThreads {
        BetaThreads::new(self.transport.clone())
    }
}

pub struct BetaThreads {
    transport: Arc<Transport>,
}

impl BetaThreads {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaThreads { transport }
    }

    pub fn messages(&self) -> BetaThreadsMessages {
        BetaThreadsMessages::new(self.transport.clone())
    }
}

pub struct BetaThreadsMessages {
    transport: Arc<Transport>,
}

impl BetaThreadsMessages {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaThreadsMessages { transport }
    }

    /// Create a message.
    pub async fn create(&self, thread_id: &str, params: BetaThreadsMessagesCreateParams) -> Result<MessageObject, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/threads/{}/messages", thread_id));
        if let Some(value) = &params.role {
            req = req.json_field("role", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.content {
            req = req.json_field("content", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.attachments {
            req = req.json_field("attachments", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct BetaThreadsMessagesCreateParams {
    /// The role of the entity that is creating the message. Allowed values include:
    /// - `user`: Indicates the message is sent by an actual user and should be used in most cases to represent user-generated messages.
    /// - `assistant`: Indicates the message is generated by the assistant. Use this value to insert messages from the assistant into the conversation.
    pub role: Option<CreateMessageRequestRole>,
    pub content: Option<CreateMessageRequestContent>,
    pub attachments: Option<Vec<CreateMessageRequestAttachmentsItem>>,
    pub metadata: Option<Metadata>,
}

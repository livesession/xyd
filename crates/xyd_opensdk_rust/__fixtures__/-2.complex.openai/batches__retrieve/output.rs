// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Batches {
    transport: Arc<Transport>,
}

impl Batches {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Batches { transport }
    }

    /// Retrieves a batch.
    pub async fn retrieve(&self, batch_id: &str) -> Result<Batch, Error> {
        if batch_id.is_empty() {
            return Err(Error::InvalidArgument("batch_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/batches/{}", batch_id));
        req.send().await
    }
}

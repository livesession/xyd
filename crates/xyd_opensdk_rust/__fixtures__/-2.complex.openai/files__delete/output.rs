// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Files {
    transport: Arc<Transport>,
}

impl Files {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Files { transport }
    }

    /// Delete a file and remove it from all vector stores.
    pub async fn delete(&self, file_id: &str) -> Result<DeleteFileResponse, Error> {
        if file_id.is_empty() {
            return Err(Error::InvalidArgument("file_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/files/{}", file_id));
        req.send().await
    }
}

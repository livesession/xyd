// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Projects {
    transport: Arc<Transport>,
}

impl Projects {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Projects { transport }
    }

    pub fn groups(&self) -> ProjectsGroups {
        ProjectsGroups::new(self.transport.clone())
    }
}

pub struct ProjectsGroups {
    transport: Arc<Transport>,
}

impl ProjectsGroups {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ProjectsGroups { transport }
    }

    pub fn roles(&self) -> ProjectsGroupsRoles {
        ProjectsGroupsRoles::new(self.transport.clone())
    }
}

pub struct ProjectsGroupsRoles {
    transport: Arc<Transport>,
}

impl ProjectsGroupsRoles {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ProjectsGroupsRoles { transport }
    }

    /// Assigns a project role to a group within a project.
    pub async fn create(&self, project_id: &str, group_id: &str, params: ProjectsGroupsRolesCreateParams) -> Result<GroupRoleAssignment, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if group_id.is_empty() {
            return Err(Error::InvalidArgument("group_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/projects/{}/groups/{}/roles", project_id, group_id));
        if let Some(value) = &params.role_id {
            req = req.json_field("role_id", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct ProjectsGroupsRolesCreateParams {
    /// Identifier of the role to assign.
    pub role_id: Option<String>,
}

// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Realtime {
    transport: Arc<Transport>,
}

impl Realtime {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Realtime { transport }
    }

    pub fn transcription_sessions(&self) -> RealtimeTranscriptionSessions {
        RealtimeTranscriptionSessions::new(self.transport.clone())
    }
}

pub struct RealtimeTranscriptionSessions {
    transport: Arc<Transport>,
}

impl RealtimeTranscriptionSessions {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        RealtimeTranscriptionSessions { transport }
    }

    /// Create an ephemeral API token for use in client-side applications with the
    /// Realtime API specifically for realtime transcriptions.
    /// Can be configured with the same session parameters as the `transcription_session.update` client event.
    ///
    /// It responds with a session object, plus a `client_secret` key which contains
    /// a usable ephemeral API token that can be used to authenticate browser clients
    /// for the Realtime API.
    ///
    /// Returns the created Realtime transcription session object, plus an ephemeral key.
    pub async fn create(&self, params: RealtimeTranscriptionSessionsCreateParams) -> Result<RealtimeTranscriptionSessionCreateResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/realtime/transcription_sessions");
        if let Some(value) = &params.turn_detection {
            req = req.json_field("turn_detection", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.input_audio_noise_reduction {
            req = req.json_field("input_audio_noise_reduction", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.input_audio_format {
            req = req.json_field("input_audio_format", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.input_audio_transcription {
            req = req.json_field("input_audio_transcription", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.include {
            req = req.json_field("include", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct RealtimeTranscriptionSessionsCreateParams {
    /// Configuration for turn detection. Can be set to `null` to turn off. Server VAD means that the model will detect the start and end of speech based on audio volume and respond at the end of user speech.
    pub turn_detection: Option<RealtimeTranscriptionSessionCreateRequestTurnDetection>,
    /// Configuration for input audio noise reduction. This can be set to `null` to turn off.
    /// Noise reduction filters audio added to the input audio buffer before it is sent to VAD and the model.
    /// Filtering the audio can improve VAD and turn detection accuracy (reducing false positives) and model performance by improving perception of the input audio.
    pub input_audio_noise_reduction: Option<RealtimeTranscriptionSessionCreateRequestInputAudioNoiseReduction>,
    /// The format of input audio. Options are `pcm16`, `g711_ulaw`, or `g711_alaw`.
    /// For `pcm16`, input audio must be 16-bit PCM at a 24kHz sample rate,
    /// single channel (mono), and little-endian byte order.
    pub input_audio_format: Option<RealtimeTranscriptionSessionCreateRequestInputAudioFormat>,
    pub input_audio_transcription: Option<AudioTranscription>,
    /// The set of items to include in the transcription. Current available items are:
    /// `item.input_audio_transcription.logprobs`
    pub include: Option<Vec<String>>,
}

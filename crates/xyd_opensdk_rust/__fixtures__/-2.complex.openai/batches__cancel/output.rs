// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Batches {
    transport: Arc<Transport>,
}

impl Batches {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Batches { transport }
    }

    /// Cancels an in-progress batch. The batch will be in status `cancelling` for up to 10 minutes, before changing to `cancelled`, where it will have partial results (if any) available in the output file.
    pub async fn cancel(&self, batch_id: &str) -> Result<Batch, Error> {
        if batch_id.is_empty() {
            return Err(Error::InvalidArgument("batch_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::POST, format!("/batches/{}/cancel", batch_id));
        req.send().await
    }
}

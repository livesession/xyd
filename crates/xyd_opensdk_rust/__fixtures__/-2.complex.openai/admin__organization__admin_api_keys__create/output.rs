// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Admin {
    transport: Arc<Transport>,
}

impl Admin {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Admin { transport }
    }

    pub fn organization(&self) -> AdminOrganization {
        AdminOrganization::new(self.transport.clone())
    }
}

pub struct AdminOrganization {
    transport: Arc<Transport>,
}

impl AdminOrganization {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganization { transport }
    }

    pub fn admin_api_keys(&self) -> AdminOrganizationAdminApiKeys {
        AdminOrganizationAdminApiKeys::new(self.transport.clone())
    }
}

pub struct AdminOrganizationAdminApiKeys {
    transport: Arc<Transport>,
}

impl AdminOrganizationAdminApiKeys {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationAdminApiKeys { transport }
    }

    /// Create a new admin-level API key for the organization.
    pub async fn create(&self, params: AdminOrganizationAdminApiKeysCreateParams) -> Result<AdminApiKeyCreateResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/organization/admin_api_keys");
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationAdminApiKeysCreateParams {
    pub name: Option<String>,
}

// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Skills {
    transport: Arc<Transport>,
}

impl Skills {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Skills { transport }
    }

    pub fn versions(&self) -> SkillsVersions {
        SkillsVersions::new(self.transport.clone())
    }

    /// Download a skill zip bundle by its ID.
    pub async fn content(&self, skill_id: &str) -> Result<String, Error> {
        if skill_id.is_empty() {
            return Err(Error::InvalidArgument("skill_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/skills/{}/content", skill_id));
        req.send().await
    }

    /// Create a new skill.
    pub async fn create(&self, params: SkillsCreateParams) -> Result<SkillResource, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/skills");
        if let Some(value) = &params.files {
            req = req.json_field("files", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Delete a skill by its ID.
    pub async fn delete(&self, skill_id: &str) -> Result<DeletedSkillResource, Error> {
        if skill_id.is_empty() {
            return Err(Error::InvalidArgument("skill_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/skills/{}", skill_id));
        req.send().await
    }

    /// List all skills for the current project.
    pub async fn list(&self, params: SkillsListParams) -> Result<Page<SkillResource>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/skills");
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Get a skill by its ID.
    pub async fn retrieve(&self, skill_id: &str) -> Result<SkillResource, Error> {
        if skill_id.is_empty() {
            return Err(Error::InvalidArgument("skill_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/skills/{}", skill_id));
        req.send().await
    }

    /// Update the default version pointer for a skill.
    pub async fn update(&self, skill_id: &str, params: SkillsUpdateParams) -> Result<SkillResource, Error> {
        if skill_id.is_empty() {
            return Err(Error::InvalidArgument("skill_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/skills/{}", skill_id));
        if let Some(value) = &params.default_version {
            req = req.json_field("default_version", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct SkillsCreateParams {
    pub files: Option<CreateSkillBodyFiles>,
}

#[derive(Debug, Clone, Default)]
pub struct SkillsListParams {
    /// Number of items to retrieve
    pub limit: Option<i64>,
    /// Sort order of results by timestamp. Use `asc` for ascending order or `desc` for descending order.
    pub order: Option<OrderEnum>,
    /// Identifier for the last item from the previous pagination request
    pub after: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct SkillsUpdateParams {
    /// The skill version number to set as default.
    pub default_version: Option<String>,
}

pub struct SkillsVersions {
    transport: Arc<Transport>,
}

impl SkillsVersions {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        SkillsVersions { transport }
    }

    /// Download a skill version zip bundle.
    pub async fn content(&self, skill_id: &str, version: &str) -> Result<String, Error> {
        if skill_id.is_empty() {
            return Err(Error::InvalidArgument("skill_id".to_string()));
        }
        if version.is_empty() {
            return Err(Error::InvalidArgument("version".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/skills/{}/versions/{}/content", skill_id, version));
        req.send().await
    }

    /// Create a new immutable skill version.
    pub async fn create(&self, skill_id: &str, params: SkillsVersionsCreateParams) -> Result<SkillVersionResource, Error> {
        if skill_id.is_empty() {
            return Err(Error::InvalidArgument("skill_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/skills/{}/versions", skill_id));
        if let Some(value) = &params.files {
            req = req.json_field("files", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.default {
            req = req.json_field("default", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Delete a skill version.
    pub async fn delete(&self, skill_id: &str, version: &str) -> Result<DeletedSkillVersionResource, Error> {
        if skill_id.is_empty() {
            return Err(Error::InvalidArgument("skill_id".to_string()));
        }
        if version.is_empty() {
            return Err(Error::InvalidArgument("version".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/skills/{}/versions/{}", skill_id, version));
        req.send().await
    }

    /// List skill versions for a skill.
    pub async fn list(&self, skill_id: &str, params: SkillsVersionsListParams) -> Result<Page<SkillVersionResource>, Error> {
        if skill_id.is_empty() {
            return Err(Error::InvalidArgument("skill_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/skills/{}/versions", skill_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Get a specific skill version.
    pub async fn retrieve(&self, skill_id: &str, version: &str) -> Result<SkillVersionResource, Error> {
        if skill_id.is_empty() {
            return Err(Error::InvalidArgument("skill_id".to_string()));
        }
        if version.is_empty() {
            return Err(Error::InvalidArgument("version".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/skills/{}/versions/{}", skill_id, version));
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct SkillsVersionsCreateParams {
    pub files: Option<CreateSkillVersionBodyFiles>,
    /// Whether to set this version as the default.
    pub default: Option<bool>,
}

#[derive(Debug, Clone, Default)]
pub struct SkillsVersionsListParams {
    /// Number of versions to retrieve.
    pub limit: Option<i64>,
    /// Sort order of results by version number.
    pub order: Option<OrderEnum>,
    /// The skill version ID to start after.
    pub after: Option<String>,
}

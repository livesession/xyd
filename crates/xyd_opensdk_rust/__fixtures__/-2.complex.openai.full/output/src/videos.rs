// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Videos {
    transport: Arc<Transport>,
}

impl Videos {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Videos { transport }
    }

    pub fn characters(&self) -> VideosCharacters {
        VideosCharacters::new(self.transport.clone())
    }

    pub fn edits(&self) -> VideosEdits {
        VideosEdits::new(self.transport.clone())
    }

    pub fn extensions(&self) -> VideosExtensions {
        VideosExtensions::new(self.transport.clone())
    }

    /// Create a new video generation job from a prompt and optional reference assets.
    pub async fn create(&self, params: VideosCreateParams) -> Result<VideoResource, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/videos");
        if let Some(value) = &params.model {
            req = req.json_field("model", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.prompt {
            req = req.json_field("prompt", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.input_reference {
            req = req.json_field("input_reference", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.seconds {
            req = req.json_field("seconds", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.size {
            req = req.json_field("size", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Permanently delete a completed or failed video and its stored assets.
    pub async fn delete(&self, video_id: &str) -> Result<DeletedVideoResource, Error> {
        if video_id.is_empty() {
            return Err(Error::InvalidArgument("video_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/videos/{}", video_id));
        req.send().await
    }

    /// Download the generated video bytes or a derived preview asset.
    ///
    /// Streams the rendered video content for the specified video job.
    pub async fn download_content(&self, video_id: &str, params: VideosDownloadContentParams) -> Result<String, Error> {
        if video_id.is_empty() {
            return Err(Error::InvalidArgument("video_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/videos/{}/content", video_id));
        if let Some(value) = &params.variant {
            req = req.query("variant", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// List recently generated videos for the current project.
    pub async fn list(&self, params: VideosListParams) -> Result<Page<VideoResource>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/videos");
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Create a remix of a completed video using a refreshed prompt.
    pub async fn remix(&self, video_id: &str, params: VideosRemixParams) -> Result<VideoResource, Error> {
        if video_id.is_empty() {
            return Err(Error::InvalidArgument("video_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/videos/{}/remix", video_id));
        if let Some(value) = &params.prompt {
            req = req.json_field("prompt", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Fetch the latest metadata for a generated video.
    pub async fn retrieve(&self, video_id: &str) -> Result<VideoResource, Error> {
        if video_id.is_empty() {
            return Err(Error::InvalidArgument("video_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/videos/{}", video_id));
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct VideosCreateParams {
    pub model: Option<VideoModel>,
    /// Text prompt that describes the video to generate.
    pub prompt: Option<String>,
    pub input_reference: Option<ImageRefParam2>,
    pub seconds: Option<VideoSeconds>,
    pub size: Option<VideoSize>,
}

#[derive(Debug, Clone, Default)]
pub struct VideosDownloadContentParams {
    /// Which downloadable asset to return. Defaults to the MP4 video.
    pub variant: Option<VideoContentVariant>,
}

#[derive(Debug, Clone, Default)]
pub struct VideosListParams {
    /// Number of items to retrieve
    pub limit: Option<i64>,
    /// Sort order of results by timestamp. Use `asc` for ascending order or `desc` for descending order.
    pub order: Option<OrderEnum>,
    /// Identifier for the last item from the previous pagination request
    pub after: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct VideosRemixParams {
    /// Updated text prompt that directs the remix generation.
    pub prompt: Option<String>,
}

pub struct VideosCharacters {
    transport: Arc<Transport>,
}

impl VideosCharacters {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        VideosCharacters { transport }
    }

    /// Create a character from an uploaded video.
    pub async fn create(&self, params: VideosCharactersCreateParams) -> Result<VideoCharacterResource, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/videos/characters");
        if let Some(value) = &params.video {
            req = req.multipart_file("video", value.clone());
        }
        if let Some(value) = &params.name {
            req = req.multipart_json("name", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Fetch a character.
    pub async fn retrieve(&self, character_id: &str) -> Result<VideoCharacterResource, Error> {
        if character_id.is_empty() {
            return Err(Error::InvalidArgument("character_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/videos/characters/{}", character_id));
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct VideosCharactersCreateParams {
    /// Video file used to create a character.
    pub video: Option<Vec<u8>>,
    /// Display name for this API character.
    pub name: Option<String>,
}

pub struct VideosEdits {
    transport: Arc<Transport>,
}

impl VideosEdits {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        VideosEdits { transport }
    }

    /// Create a new video generation job by editing a source video or existing generated video.
    pub async fn create(&self, params: VideosEditsCreateParams) -> Result<VideoResource, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/videos/edits");
        if let Some(value) = &params.video {
            req = req.json_field("video", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.prompt {
            req = req.json_field("prompt", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct VideosEditsCreateParams {
    pub video: Option<VideoReferenceInputParam>,
    /// Text prompt that describes how to edit the source video.
    pub prompt: Option<String>,
}

pub struct VideosExtensions {
    transport: Arc<Transport>,
}

impl VideosExtensions {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        VideosExtensions { transport }
    }

    /// Create an extension of a completed video.
    pub async fn create(&self, params: VideosExtensionsCreateParams) -> Result<VideoResource, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/videos/extensions");
        if let Some(value) = &params.video {
            req = req.json_field("video", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.prompt {
            req = req.json_field("prompt", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.seconds {
            req = req.json_field("seconds", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct VideosExtensionsCreateParams {
    pub video: Option<VideoReferenceInputParam>,
    /// Updated text prompt that directs the extension generation.
    pub prompt: Option<String>,
    pub seconds: Option<VideoSeconds>,
}

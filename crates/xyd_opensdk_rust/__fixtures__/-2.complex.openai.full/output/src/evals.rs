// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Evals {
    transport: Arc<Transport>,
}

impl Evals {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Evals { transport }
    }

    pub fn runs(&self) -> EvalsRuns {
        EvalsRuns::new(self.transport.clone())
    }

    /// Create the structure of an evaluation that can be used to test a model's performance.
    /// An evaluation is a set of testing criteria and the config for a data source, which dictates the schema of the data used in the evaluation. After creating an evaluation, you can run it on different models and model parameters. We support several types of graders and datasources.
    /// For more information, see the [Evals guide](/docs/guides/evals).
    pub async fn create(&self, params: EvalsCreateParams) -> Result<Eval, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/evals");
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.data_source_config {
            req = req.json_field("data_source_config", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.testing_criteria {
            req = req.json_field("testing_criteria", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Delete an evaluation.
    pub async fn delete(&self, eval_id: &str) -> Result<EvalsDeleteResponse, Error> {
        if eval_id.is_empty() {
            return Err(Error::InvalidArgument("eval_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/evals/{}", eval_id));
        req.send().await
    }

    /// List evaluations for a project.
    pub async fn list(&self, params: EvalsListParams) -> Result<Page<Eval>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/evals");
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order_by {
            req = req.query("order_by", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Get an evaluation by ID.
    pub async fn retrieve(&self, eval_id: &str) -> Result<Eval, Error> {
        if eval_id.is_empty() {
            return Err(Error::InvalidArgument("eval_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/evals/{}", eval_id));
        req.send().await
    }

    /// Update certain properties of an evaluation.
    pub async fn update(&self, eval_id: &str, params: EvalsUpdateParams) -> Result<Eval, Error> {
        if eval_id.is_empty() {
            return Err(Error::InvalidArgument("eval_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/evals/{}", eval_id));
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct EvalsCreateParams {
    /// The name of the evaluation.
    pub name: Option<String>,
    pub metadata: Option<Metadata>,
    /// The configuration for the data source used for the evaluation runs. Dictates the schema of the data used in the evaluation.
    pub data_source_config: Option<CreateEvalRequestDataSourceConfig>,
    /// A list of graders for all eval runs in this group. Graders can reference variables in the data source using double curly braces notation, like `{{item.variable_name}}`. To reference the model's output, use the `sample` namespace (ie, `{{sample.output_text}}`).
    pub testing_criteria: Option<Vec<CreateEvalRequestTestingCriteriaItem>>,
}

#[derive(Debug, Clone, Default)]
pub struct EvalsListParams {
    /// Identifier for the last eval from the previous pagination request.
    pub after: Option<String>,
    /// Number of evals to retrieve.
    pub limit: Option<i64>,
    /// Sort order for evals by timestamp. Use `asc` for ascending order or `desc` for descending order.
    pub order: Option<EvalsListOrder>,
    /// Evals can be ordered by creation time or last updated time. Use
    /// `created_at` for creation time or `updated_at` for last updated time.
    pub order_by: Option<EvalsListOrderBy>,
}

#[derive(Debug, Clone, Default)]
pub struct EvalsUpdateParams {
    /// Rename the evaluation.
    pub name: Option<String>,
    pub metadata: Option<Metadata>,
}

pub struct EvalsRuns {
    transport: Arc<Transport>,
}

impl EvalsRuns {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        EvalsRuns { transport }
    }

    pub fn output_items(&self) -> EvalsRunsOutputItems {
        EvalsRunsOutputItems::new(self.transport.clone())
    }

    /// Cancel an ongoing evaluation run.
    pub async fn cancel(&self, eval_id: &str, run_id: &str) -> Result<EvalRun, Error> {
        if eval_id.is_empty() {
            return Err(Error::InvalidArgument("eval_id".to_string()));
        }
        if run_id.is_empty() {
            return Err(Error::InvalidArgument("run_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::POST, format!("/evals/{}/runs/{}", eval_id, run_id));
        req.send().await
    }

    /// Kicks off a new run for a given evaluation, specifying the data source, and what model configuration to use to test. The datasource will be validated against the schema specified in the config of the evaluation.
    pub async fn create(&self, eval_id: &str, params: EvalsRunsCreateParams) -> Result<EvalRun, Error> {
        if eval_id.is_empty() {
            return Err(Error::InvalidArgument("eval_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/evals/{}/runs", eval_id));
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.data_source {
            req = req.json_field("data_source", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Delete an eval run.
    pub async fn delete(&self, eval_id: &str, run_id: &str) -> Result<EvalsRunsDeleteResponse, Error> {
        if eval_id.is_empty() {
            return Err(Error::InvalidArgument("eval_id".to_string()));
        }
        if run_id.is_empty() {
            return Err(Error::InvalidArgument("run_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/evals/{}/runs/{}", eval_id, run_id));
        req.send().await
    }

    /// Get a list of runs for an evaluation.
    pub async fn list(&self, eval_id: &str, params: EvalsRunsListParams) -> Result<Page<EvalRun>, Error> {
        if eval_id.is_empty() {
            return Err(Error::InvalidArgument("eval_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/evals/{}/runs", eval_id));
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.status {
            req = req.query("status", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Get an evaluation run by ID.
    pub async fn retrieve(&self, eval_id: &str, run_id: &str) -> Result<EvalRun, Error> {
        if eval_id.is_empty() {
            return Err(Error::InvalidArgument("eval_id".to_string()));
        }
        if run_id.is_empty() {
            return Err(Error::InvalidArgument("run_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/evals/{}/runs/{}", eval_id, run_id));
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct EvalsRunsCreateParams {
    /// The name of the run.
    pub name: Option<String>,
    pub metadata: Option<Metadata>,
    /// Details about the run's data source.
    pub data_source: Option<CreateEvalRunRequestDataSource>,
}

#[derive(Debug, Clone, Default)]
pub struct EvalsRunsListParams {
    /// Identifier for the last run from the previous pagination request.
    pub after: Option<String>,
    /// Number of runs to retrieve.
    pub limit: Option<i64>,
    /// Sort order for runs by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `asc`.
    pub order: Option<EvalsRunsListOrder>,
    /// Filter runs by status. One of `queued` | `in_progress` | `failed` | `completed` | `canceled`.
    pub status: Option<EvalsRunsListStatus>,
}

pub struct EvalsRunsOutputItems {
    transport: Arc<Transport>,
}

impl EvalsRunsOutputItems {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        EvalsRunsOutputItems { transport }
    }

    /// Get a list of output items for an evaluation run.
    pub async fn list(&self, eval_id: &str, run_id: &str, params: EvalsRunsOutputItemsListParams) -> Result<Page<EvalRunOutputItem>, Error> {
        if eval_id.is_empty() {
            return Err(Error::InvalidArgument("eval_id".to_string()));
        }
        if run_id.is_empty() {
            return Err(Error::InvalidArgument("run_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/evals/{}/runs/{}/output_items", eval_id, run_id));
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.status {
            req = req.query("status", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Get an evaluation run output item by ID.
    pub async fn retrieve(&self, eval_id: &str, run_id: &str, output_item_id: &str) -> Result<EvalRunOutputItem, Error> {
        if eval_id.is_empty() {
            return Err(Error::InvalidArgument("eval_id".to_string()));
        }
        if run_id.is_empty() {
            return Err(Error::InvalidArgument("run_id".to_string()));
        }
        if output_item_id.is_empty() {
            return Err(Error::InvalidArgument("output_item_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/evals/{}/runs/{}/output_items/{}", eval_id, run_id, output_item_id));
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct EvalsRunsOutputItemsListParams {
    /// Identifier for the last output item from the previous pagination request.
    pub after: Option<String>,
    /// Number of output items to retrieve.
    pub limit: Option<i64>,
    /// Filter output items by status. Use `failed` to filter by failed output
    /// items or `pass` to filter by passed output items.
    pub status: Option<EvalsRunsOutputItemsListStatus>,
    /// Sort order for output items by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `asc`.
    pub order: Option<EvalsRunsOutputItemsListOrder>,
}

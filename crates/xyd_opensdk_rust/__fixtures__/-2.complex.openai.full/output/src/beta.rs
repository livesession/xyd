// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Beta {
    transport: Arc<Transport>,
}

impl Beta {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Beta { transport }
    }

    pub fn assistants(&self) -> BetaAssistants {
        BetaAssistants::new(self.transport.clone())
    }

    pub fn chat_kit(&self) -> BetaChatKit {
        BetaChatKit::new(self.transport.clone())
    }

    pub fn threads(&self) -> BetaThreads {
        BetaThreads::new(self.transport.clone())
    }
}

pub struct BetaAssistants {
    transport: Arc<Transport>,
}

impl BetaAssistants {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaAssistants { transport }
    }

    /// Create an assistant with a model and instructions.
    pub async fn create(&self, params: BetaAssistantsCreateParams) -> Result<AssistantObject, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/assistants");
        if let Some(value) = &params.model {
            req = req.json_field("model", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.description {
            req = req.json_field("description", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.instructions {
            req = req.json_field("instructions", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.reasoning_effort {
            req = req.json_field("reasoning_effort", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tools {
            req = req.json_field("tools", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tool_resources {
            req = req.json_field("tool_resources", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.temperature {
            req = req.json_field("temperature", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.top_p {
            req = req.json_field("top_p", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.response_format {
            req = req.json_field("response_format", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Delete an assistant.
    pub async fn delete(&self, assistant_id: &str) -> Result<DeleteAssistantResponse, Error> {
        if assistant_id.is_empty() {
            return Err(Error::InvalidArgument("assistant_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/assistants/{}", assistant_id));
        req.send().await
    }

    /// Returns a list of assistants.
    pub async fn list(&self, params: BetaAssistantsListParams) -> Result<Page<AssistantObject>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/assistants");
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.before {
            req = req.query("before", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Retrieves an assistant.
    pub async fn retrieve(&self, assistant_id: &str) -> Result<AssistantObject, Error> {
        if assistant_id.is_empty() {
            return Err(Error::InvalidArgument("assistant_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/assistants/{}", assistant_id));
        req.send().await
    }

    /// Modifies an assistant.
    pub async fn update(&self, assistant_id: &str, params: BetaAssistantsUpdateParams) -> Result<AssistantObject, Error> {
        if assistant_id.is_empty() {
            return Err(Error::InvalidArgument("assistant_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/assistants/{}", assistant_id));
        if let Some(value) = &params.model {
            req = req.json_field("model", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.reasoning_effort {
            req = req.json_field("reasoning_effort", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.description {
            req = req.json_field("description", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.instructions {
            req = req.json_field("instructions", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tools {
            req = req.json_field("tools", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tool_resources {
            req = req.json_field("tool_resources", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.temperature {
            req = req.json_field("temperature", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.top_p {
            req = req.json_field("top_p", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.response_format {
            req = req.json_field("response_format", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct BetaAssistantsCreateParams {
    /// ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](/docs/models) for descriptions of them.
    pub model: Option<CreateAssistantRequestModel>,
    pub name: Option<String>,
    pub description: Option<String>,
    pub instructions: Option<String>,
    pub reasoning_effort: Option<ReasoningEffort>,
    /// A list of tool enabled on the assistant. There can be a maximum of 128 tools per assistant. Tools can be of types `code_interpreter`, `file_search`, or `function`.
    pub tools: Option<Vec<CreateAssistantRequestToolsItem>>,
    pub tool_resources: Option<CreateAssistantRequestToolResources>,
    pub metadata: Option<Metadata>,
    pub temperature: Option<f64>,
    pub top_p: Option<f64>,
    pub response_format: Option<AssistantsApiResponseFormatOption>,
}

#[derive(Debug, Clone, Default)]
pub struct BetaAssistantsListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.
    pub order: Option<BetaAssistantsListOrder>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
    /// A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.
    pub before: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct BetaAssistantsUpdateParams {
    /// ID of the model to use. You can use the [List models](/docs/api-reference/models/list) API to see all of your available models, or see our [Model overview](/docs/models) for descriptions of them.
    pub model: Option<ModifyAssistantRequestModel>,
    pub reasoning_effort: Option<ReasoningEffort>,
    pub name: Option<String>,
    pub description: Option<String>,
    pub instructions: Option<String>,
    /// A list of tool enabled on the assistant. There can be a maximum of 128 tools per assistant. Tools can be of types `code_interpreter`, `file_search`, or `function`.
    pub tools: Option<Vec<ModifyAssistantRequestToolsItem>>,
    pub tool_resources: Option<ModifyAssistantRequestToolResources>,
    pub metadata: Option<Metadata>,
    pub temperature: Option<f64>,
    pub top_p: Option<f64>,
    pub response_format: Option<AssistantsApiResponseFormatOption>,
}

pub struct BetaChatKit {
    transport: Arc<Transport>,
}

impl BetaChatKit {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaChatKit { transport }
    }

    pub fn sessions(&self) -> BetaChatKitSessions {
        BetaChatKitSessions::new(self.transport.clone())
    }

    pub fn threads(&self) -> BetaChatKitThreads {
        BetaChatKitThreads::new(self.transport.clone())
    }
}

pub struct BetaChatKitSessions {
    transport: Arc<Transport>,
}

impl BetaChatKitSessions {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaChatKitSessions { transport }
    }

    /// Cancel an active ChatKit session and return its most recent metadata.
    ///
    /// Cancelling prevents new requests from using the issued client secret.
    pub async fn cancel(&self, session_id: &str) -> Result<ChatSessionResource, Error> {
        if session_id.is_empty() {
            return Err(Error::InvalidArgument("session_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::POST, format!("/chatkit/sessions/{}/cancel", session_id));
        req.send().await
    }

    /// Create a ChatKit session.
    pub async fn create(&self, params: BetaChatKitSessionsCreateParams) -> Result<ChatSessionResource, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/chatkit/sessions");
        if let Some(value) = &params.workflow {
            req = req.json_field("workflow", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.user {
            req = req.json_field("user", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.expires_after {
            req = req.json_field("expires_after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.rate_limits {
            req = req.json_field("rate_limits", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.chatkit_configuration {
            req = req.json_field("chatkit_configuration", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct BetaChatKitSessionsCreateParams {
    pub workflow: Option<WorkflowParam>,
    /// A free-form string that identifies your end user; ensures this Session can access other objects that have the same `user` scope.
    pub user: Option<String>,
    pub expires_after: Option<ExpiresAfterParam>,
    pub rate_limits: Option<RateLimitsParam>,
    pub chatkit_configuration: Option<ChatkitConfigurationParam>,
}

pub struct BetaChatKitThreads {
    transport: Arc<Transport>,
}

impl BetaChatKitThreads {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaChatKitThreads { transport }
    }

    pub fn items(&self) -> BetaChatKitThreadsItems {
        BetaChatKitThreadsItems::new(self.transport.clone())
    }

    /// Delete a ChatKit thread along with its items and stored attachments.
    pub async fn delete(&self, thread_id: &str) -> Result<DeletedThreadResource, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/chatkit/threads/{}", thread_id));
        req.send().await
    }

    /// List ChatKit threads with optional pagination and user filters.
    pub async fn list(&self, params: BetaChatKitThreadsListParams) -> Result<Page<ThreadResource>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/chatkit/threads");
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.before {
            req = req.query("before", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.user {
            req = req.query("user", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Retrieve a ChatKit thread by its identifier.
    pub async fn retrieve(&self, thread_id: &str) -> Result<ThreadResource, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/chatkit/threads/{}", thread_id));
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct BetaChatKitThreadsListParams {
    /// Maximum number of thread items to return. Defaults to 20.
    pub limit: Option<i64>,
    /// Sort order for results by creation time. Defaults to `desc`.
    pub order: Option<OrderEnum>,
    /// List items created after this thread item ID. Defaults to null for the first page.
    pub after: Option<String>,
    /// List items created before this thread item ID. Defaults to null for the newest results.
    pub before: Option<String>,
    /// Filter threads that belong to this user identifier. Defaults to null to return all users.
    pub user: Option<String>,
}

pub struct BetaChatKitThreadsItems {
    transport: Arc<Transport>,
}

impl BetaChatKitThreadsItems {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaChatKitThreadsItems { transport }
    }

    /// List items that belong to a ChatKit thread.
    pub async fn list(&self, thread_id: &str, params: BetaChatKitThreadsItemsListParams) -> Result<Page<ThreadItem>, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/chatkit/threads/{}/items", thread_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.before {
            req = req.query("before", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct BetaChatKitThreadsItemsListParams {
    /// Maximum number of thread items to return. Defaults to 20.
    pub limit: Option<i64>,
    /// Sort order for results by creation time. Defaults to `desc`.
    pub order: Option<OrderEnum>,
    /// List items created after this thread item ID. Defaults to null for the first page.
    pub after: Option<String>,
    /// List items created before this thread item ID. Defaults to null for the newest results.
    pub before: Option<String>,
}

pub struct BetaThreads {
    transport: Arc<Transport>,
}

impl BetaThreads {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaThreads { transport }
    }

    pub fn messages(&self) -> BetaThreadsMessages {
        BetaThreadsMessages::new(self.transport.clone())
    }

    pub fn runs(&self) -> BetaThreadsRuns {
        BetaThreadsRuns::new(self.transport.clone())
    }

    /// Create a thread.
    pub async fn create(&self, params: BetaThreadsCreateParams) -> Result<ThreadObject, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/threads");
        if let Some(value) = &params.messages {
            req = req.json_field("messages", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tool_resources {
            req = req.json_field("tool_resources", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Delete a thread.
    pub async fn delete(&self, thread_id: &str) -> Result<DeleteThreadResponse, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/threads/{}", thread_id));
        req.send().await
    }

    /// Create a thread and run it in one request.
    pub async fn new_and_run(&self, params: BetaThreadsNewAndRunParams) -> Result<RunObject, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/threads/runs");
        if let Some(value) = &params.assistant_id {
            req = req.json_field("assistant_id", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.thread {
            req = req.json_field("thread", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.model {
            req = req.json_field("model", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.instructions {
            req = req.json_field("instructions", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tools {
            req = req.json_field("tools", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tool_resources {
            req = req.json_field("tool_resources", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.temperature {
            req = req.json_field("temperature", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.top_p {
            req = req.json_field("top_p", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.stream {
            req = req.json_field("stream", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.max_prompt_tokens {
            req = req.json_field("max_prompt_tokens", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.max_completion_tokens {
            req = req.json_field("max_completion_tokens", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.truncation_strategy {
            req = req.json_field("truncation_strategy", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tool_choice {
            req = req.json_field("tool_choice", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.parallel_tool_calls {
            req = req.json_field("parallel_tool_calls", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.response_format {
            req = req.json_field("response_format", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Retrieves a thread.
    pub async fn retrieve(&self, thread_id: &str) -> Result<ThreadObject, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/threads/{}", thread_id));
        req.send().await
    }

    /// Modifies a thread.
    pub async fn update(&self, thread_id: &str, params: BetaThreadsUpdateParams) -> Result<ThreadObject, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/threads/{}", thread_id));
        if let Some(value) = &params.tool_resources {
            req = req.json_field("tool_resources", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct BetaThreadsCreateParams {
    /// A list of [messages](/docs/api-reference/messages) to start the thread with.
    pub messages: Option<Vec<CreateMessageRequest>>,
    pub tool_resources: Option<CreateThreadRequestToolResources>,
    pub metadata: Option<Metadata>,
}

#[derive(Debug, Clone, Default)]
pub struct BetaThreadsNewAndRunParams {
    /// The ID of the [assistant](/docs/api-reference/assistants) to use to execute this run.
    pub assistant_id: Option<String>,
    pub thread: Option<CreateThreadRequest>,
    /// The ID of the [Model](/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used.
    pub model: Option<CreateThreadAndRunRequestModel>,
    /// Override the default system message of the assistant. This is useful for modifying the behavior on a per-run basis.
    pub instructions: Option<String>,
    /// Override the tools the assistant can use for this run. This is useful for modifying the behavior on a per-run basis.
    pub tools: Option<Vec<CreateThreadAndRunRequestToolsItem>>,
    /// A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.
    pub tool_resources: Option<CreateThreadAndRunRequestToolResources>,
    pub metadata: Option<Metadata>,
    /// What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.
    pub temperature: Option<f64>,
    /// An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.
    ///
    /// We generally recommend altering this or temperature but not both.
    pub top_p: Option<f64>,
    /// If `true`, returns a stream of events that happen during the Run as server-sent events, terminating when the Run enters a terminal state with a `data: [DONE]` message.
    pub stream: Option<bool>,
    /// The maximum number of prompt tokens that may be used over the course of the run. The run will make a best effort to use only the number of prompt tokens specified, across multiple turns of the run. If the run exceeds the number of prompt tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.
    pub max_prompt_tokens: Option<i64>,
    /// The maximum number of completion tokens that may be used over the course of the run. The run will make a best effort to use only the number of completion tokens specified, across multiple turns of the run. If the run exceeds the number of completion tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.
    pub max_completion_tokens: Option<i64>,
    pub truncation_strategy: Option<TruncationObject>,
    pub tool_choice: Option<AssistantsApiToolChoiceOption>,
    pub parallel_tool_calls: Option<ParallelToolCalls>,
    pub response_format: Option<AssistantsApiResponseFormatOption>,
}

#[derive(Debug, Clone, Default)]
pub struct BetaThreadsUpdateParams {
    pub tool_resources: Option<ModifyThreadRequestToolResources>,
    pub metadata: Option<Metadata>,
}

pub struct BetaThreadsMessages {
    transport: Arc<Transport>,
}

impl BetaThreadsMessages {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaThreadsMessages { transport }
    }

    /// Create a message.
    pub async fn create(&self, thread_id: &str, params: BetaThreadsMessagesCreateParams) -> Result<MessageObject, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/threads/{}/messages", thread_id));
        if let Some(value) = &params.role {
            req = req.json_field("role", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.content {
            req = req.json_field("content", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.attachments {
            req = req.json_field("attachments", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Deletes a message.
    pub async fn delete(&self, thread_id: &str, message_id: &str) -> Result<DeleteMessageResponse, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }
        if message_id.is_empty() {
            return Err(Error::InvalidArgument("message_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/threads/{}/messages/{}", thread_id, message_id));
        req.send().await
    }

    /// Returns a list of messages for a given thread.
    pub async fn list(&self, thread_id: &str, params: BetaThreadsMessagesListParams) -> Result<Page<MessageObject>, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/threads/{}/messages", thread_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.before {
            req = req.query("before", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.run_id {
            req = req.query("run_id", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Retrieve a message.
    pub async fn retrieve(&self, thread_id: &str, message_id: &str) -> Result<MessageObject, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }
        if message_id.is_empty() {
            return Err(Error::InvalidArgument("message_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/threads/{}/messages/{}", thread_id, message_id));
        req.send().await
    }

    /// Modifies a message.
    pub async fn update(&self, thread_id: &str, message_id: &str, params: BetaThreadsMessagesUpdateParams) -> Result<MessageObject, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }
        if message_id.is_empty() {
            return Err(Error::InvalidArgument("message_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/threads/{}/messages/{}", thread_id, message_id));
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct BetaThreadsMessagesCreateParams {
    /// The role of the entity that is creating the message. Allowed values include:
    /// - `user`: Indicates the message is sent by an actual user and should be used in most cases to represent user-generated messages.
    /// - `assistant`: Indicates the message is generated by the assistant. Use this value to insert messages from the assistant into the conversation.
    pub role: Option<CreateMessageRequestRole>,
    pub content: Option<CreateMessageRequestContent>,
    pub attachments: Option<Vec<CreateMessageRequestAttachmentsItem>>,
    pub metadata: Option<Metadata>,
}

#[derive(Debug, Clone, Default)]
pub struct BetaThreadsMessagesListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.
    pub order: Option<BetaThreadsMessagesListOrder>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
    /// A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.
    pub before: Option<String>,
    /// Filter messages by the run ID that generated them.
    pub run_id: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct BetaThreadsMessagesUpdateParams {
    pub metadata: Option<Metadata>,
}

pub struct BetaThreadsRuns {
    transport: Arc<Transport>,
}

impl BetaThreadsRuns {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaThreadsRuns { transport }
    }

    pub fn steps(&self) -> BetaThreadsRunsSteps {
        BetaThreadsRunsSteps::new(self.transport.clone())
    }

    pub fn submit_tool_outputs(&self) -> BetaThreadsRunsSubmitToolOutputs {
        BetaThreadsRunsSubmitToolOutputs::new(self.transport.clone())
    }

    /// Cancels a run that is `in_progress`.
    pub async fn cancel(&self, thread_id: &str, run_id: &str) -> Result<RunObject, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }
        if run_id.is_empty() {
            return Err(Error::InvalidArgument("run_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::POST, format!("/threads/{}/runs/{}/cancel", thread_id, run_id));
        req.send().await
    }

    /// Create a run.
    pub async fn create(&self, thread_id: &str, params: BetaThreadsRunsCreateParams) -> Result<RunObject, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/threads/{}/runs", thread_id));
        if let Some(value) = &params.include {
            req = req.query("include[]", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.assistant_id {
            req = req.json_field("assistant_id", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.model {
            req = req.json_field("model", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.reasoning_effort {
            req = req.json_field("reasoning_effort", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.instructions {
            req = req.json_field("instructions", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.additional_instructions {
            req = req.json_field("additional_instructions", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.additional_messages {
            req = req.json_field("additional_messages", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tools {
            req = req.json_field("tools", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.temperature {
            req = req.json_field("temperature", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.top_p {
            req = req.json_field("top_p", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.stream {
            req = req.json_field("stream", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.max_prompt_tokens {
            req = req.json_field("max_prompt_tokens", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.max_completion_tokens {
            req = req.json_field("max_completion_tokens", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.truncation_strategy {
            req = req.json_field("truncation_strategy", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.tool_choice {
            req = req.json_field("tool_choice", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.parallel_tool_calls {
            req = req.json_field("parallel_tool_calls", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.response_format {
            req = req.json_field("response_format", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Returns a list of runs belonging to a thread.
    pub async fn list(&self, thread_id: &str, params: BetaThreadsRunsListParams) -> Result<Page<RunObject>, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/threads/{}/runs", thread_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.before {
            req = req.query("before", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Retrieves a run.
    pub async fn retrieve(&self, thread_id: &str, run_id: &str) -> Result<RunObject, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }
        if run_id.is_empty() {
            return Err(Error::InvalidArgument("run_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/threads/{}/runs/{}", thread_id, run_id));
        req.send().await
    }

    /// Modifies a run.
    pub async fn update(&self, thread_id: &str, run_id: &str, params: BetaThreadsRunsUpdateParams) -> Result<RunObject, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }
        if run_id.is_empty() {
            return Err(Error::InvalidArgument("run_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/threads/{}/runs/{}", thread_id, run_id));
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct BetaThreadsRunsCreateParams {
    /// A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.
    ///
    /// See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.
    pub include: Option<Vec<String>>,
    /// The ID of the [assistant](/docs/api-reference/assistants) to use to execute this run.
    pub assistant_id: Option<String>,
    /// The ID of the [Model](/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used.
    pub model: Option<CreateRunRequestModel>,
    pub reasoning_effort: Option<ReasoningEffort>,
    /// Overrides the [instructions](/docs/api-reference/assistants/createAssistant) of the assistant. This is useful for modifying the behavior on a per-run basis.
    pub instructions: Option<String>,
    /// Appends additional instructions at the end of the instructions for the run. This is useful for modifying the behavior on a per-run basis without overriding other instructions.
    pub additional_instructions: Option<String>,
    /// Adds additional messages to the thread before creating the run.
    pub additional_messages: Option<Vec<CreateMessageRequest>>,
    /// Override the tools the assistant can use for this run. This is useful for modifying the behavior on a per-run basis.
    pub tools: Option<Vec<CreateRunRequestToolsItem>>,
    pub metadata: Option<Metadata>,
    /// What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.
    pub temperature: Option<f64>,
    /// An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.
    ///
    /// We generally recommend altering this or temperature but not both.
    pub top_p: Option<f64>,
    /// If `true`, returns a stream of events that happen during the Run as server-sent events, terminating when the Run enters a terminal state with a `data: [DONE]` message.
    pub stream: Option<bool>,
    /// The maximum number of prompt tokens that may be used over the course of the run. The run will make a best effort to use only the number of prompt tokens specified, across multiple turns of the run. If the run exceeds the number of prompt tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.
    pub max_prompt_tokens: Option<i64>,
    /// The maximum number of completion tokens that may be used over the course of the run. The run will make a best effort to use only the number of completion tokens specified, across multiple turns of the run. If the run exceeds the number of completion tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.
    pub max_completion_tokens: Option<i64>,
    pub truncation_strategy: Option<TruncationObject>,
    pub tool_choice: Option<AssistantsApiToolChoiceOption>,
    pub parallel_tool_calls: Option<ParallelToolCalls>,
    pub response_format: Option<AssistantsApiResponseFormatOption>,
}

#[derive(Debug, Clone, Default)]
pub struct BetaThreadsRunsListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.
    pub order: Option<BetaThreadsRunsListOrder>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
    /// A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.
    pub before: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct BetaThreadsRunsUpdateParams {
    pub metadata: Option<Metadata>,
}

pub struct BetaThreadsRunsSteps {
    transport: Arc<Transport>,
}

impl BetaThreadsRunsSteps {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaThreadsRunsSteps { transport }
    }

    /// Returns a list of run steps belonging to a run.
    pub async fn list(&self, thread_id: &str, run_id: &str, params: BetaThreadsRunsStepsListParams) -> Result<Page<RunStepObject>, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }
        if run_id.is_empty() {
            return Err(Error::InvalidArgument("run_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/threads/{}/runs/{}/steps", thread_id, run_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.before {
            req = req.query("before", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.include {
            req = req.query("include[]", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Retrieves a run step.
    pub async fn retrieve(&self, thread_id: &str, run_id: &str, step_id: &str, params: BetaThreadsRunsStepsRetrieveParams) -> Result<RunStepObject, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }
        if run_id.is_empty() {
            return Err(Error::InvalidArgument("run_id".to_string()));
        }
        if step_id.is_empty() {
            return Err(Error::InvalidArgument("step_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/threads/{}/runs/{}/steps/{}", thread_id, run_id, step_id));
        if let Some(value) = &params.include {
            req = req.query("include[]", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct BetaThreadsRunsStepsListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.
    pub order: Option<BetaThreadsRunsStepsListOrder>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
    /// A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.
    pub before: Option<String>,
    /// A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.
    ///
    /// See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.
    pub include: Option<Vec<String>>,
}

#[derive(Debug, Clone, Default)]
pub struct BetaThreadsRunsStepsRetrieveParams {
    /// A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.
    ///
    /// See the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.
    pub include: Option<Vec<String>>,
}

pub struct BetaThreadsRunsSubmitToolOutputs {
    transport: Arc<Transport>,
}

impl BetaThreadsRunsSubmitToolOutputs {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaThreadsRunsSubmitToolOutputs { transport }
    }

    /// When a run has the `status: "requires_action"` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request.
    pub async fn create(&self, thread_id: &str, run_id: &str, params: BetaThreadsRunsSubmitToolOutputsCreateParams) -> Result<RunObject, Error> {
        if thread_id.is_empty() {
            return Err(Error::InvalidArgument("thread_id".to_string()));
        }
        if run_id.is_empty() {
            return Err(Error::InvalidArgument("run_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/threads/{}/runs/{}/submit_tool_outputs", thread_id, run_id));
        if let Some(value) = &params.tool_outputs {
            req = req.json_field("tool_outputs", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.stream {
            req = req.json_field("stream", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct BetaThreadsRunsSubmitToolOutputsCreateParams {
    /// A list of tools for which the outputs are being submitted.
    pub tool_outputs: Option<Vec<SubmitToolOutputsRunRequestToolOutputsItem>>,
    pub stream: Option<bool>,
}

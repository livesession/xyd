// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Conversations {
    transport: Arc<Transport>,
}

impl Conversations {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Conversations { transport }
    }

    pub fn items(&self) -> ConversationsItems {
        ConversationsItems::new(self.transport.clone())
    }

    /// Create a conversation.
    pub async fn create(&self, params: ConversationsCreateParams) -> Result<ConversationResource, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/conversations");
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.items {
            req = req.json_field("items", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Delete a conversation. Items in the conversation will not be deleted.
    pub async fn delete(&self, conversation_id: &str) -> Result<DeletedConversationResource, Error> {
        if conversation_id.is_empty() {
            return Err(Error::InvalidArgument("conversation_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/conversations/{}", conversation_id));
        req.send().await
    }

    /// Get a conversation
    pub async fn retrieve(&self, conversation_id: &str) -> Result<ConversationResource, Error> {
        if conversation_id.is_empty() {
            return Err(Error::InvalidArgument("conversation_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/conversations/{}", conversation_id));
        req.send().await
    }

    /// Update a conversation
    pub async fn update(&self, conversation_id: &str, params: ConversationsUpdateParams) -> Result<ConversationResource, Error> {
        if conversation_id.is_empty() {
            return Err(Error::InvalidArgument("conversation_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/conversations/{}", conversation_id));
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct ConversationsCreateParams {
    pub metadata: Option<Metadata>,
    pub items: Option<Vec<InputItem>>,
}

#[derive(Debug, Clone, Default)]
pub struct ConversationsUpdateParams {
    pub metadata: Option<Metadata>,
}

pub struct ConversationsItems {
    transport: Arc<Transport>,
}

impl ConversationsItems {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ConversationsItems { transport }
    }

    /// Create items in a conversation with the given ID.
    pub async fn create(&self, conversation_id: &str, params: ConversationsItemsCreateParams) -> Result<Page<ConversationItem>, Error> {
        if conversation_id.is_empty() {
            return Err(Error::InvalidArgument("conversation_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/conversations/{}/items", conversation_id));
        if let Some(value) = &params.include {
            req = req.query("include", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.items {
            req = req.json_field("items", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Delete an item from a conversation with the given IDs.
    pub async fn delete(&self, conversation_id: &str, item_id: &str) -> Result<ConversationResource, Error> {
        if conversation_id.is_empty() {
            return Err(Error::InvalidArgument("conversation_id".to_string()));
        }
        if item_id.is_empty() {
            return Err(Error::InvalidArgument("item_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/conversations/{}/items/{}", conversation_id, item_id));
        req.send().await
    }

    /// List all items for a conversation with the given ID.
    pub async fn list(&self, conversation_id: &str, params: ConversationsItemsListParams) -> Result<Page<ConversationItem>, Error> {
        if conversation_id.is_empty() {
            return Err(Error::InvalidArgument("conversation_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/conversations/{}/items", conversation_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.include {
            req = req.query("include", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Get a single item from a conversation with the given IDs.
    pub async fn retrieve(&self, conversation_id: &str, item_id: &str, params: ConversationsItemsRetrieveParams) -> Result<ConversationItem, Error> {
        if conversation_id.is_empty() {
            return Err(Error::InvalidArgument("conversation_id".to_string()));
        }
        if item_id.is_empty() {
            return Err(Error::InvalidArgument("item_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/conversations/{}/items/{}", conversation_id, item_id));
        if let Some(value) = &params.include {
            req = req.query("include", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct ConversationsItemsCreateParams {
    /// Additional fields to include in the response. See the `include`
    /// parameter for [listing Conversation items above](/docs/api-reference/conversations/list-items#conversations_list_items-include) for more information.
    pub include: Option<Vec<IncludeEnum>>,
    /// The items to add to the conversation. You may add up to 20 items at a time.
    pub items: Option<Vec<InputItem>>,
}

#[derive(Debug, Clone, Default)]
pub struct ConversationsItemsListParams {
    /// A limit on the number of objects to be returned. Limit can range between
    /// 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// The order to return the input items in. Default is `desc`.
    /// - `asc`: Return the input items in ascending order.
    /// - `desc`: Return the input items in descending order.
    pub order: Option<ConversationsItemsListOrder>,
    /// An item ID to list items after, used in pagination.
    pub after: Option<String>,
    /// Specify additional output data to include in the model response. Currently supported values are:
    /// - `web_search_call.action.sources`: Include the sources of the web search tool call.
    /// - `code_interpreter_call.outputs`: Includes the outputs of python code execution in code interpreter tool call items.
    /// - `computer_call_output.output.image_url`: Include image urls from the computer call output.
    /// - `file_search_call.results`: Include the search results of the file search tool call.
    /// - `message.input_image.image_url`: Include image urls from the input message.
    /// - `message.output_text.logprobs`: Include logprobs with assistant messages.
    /// - `reasoning.encrypted_content`: Includes an encrypted version of reasoning tokens in reasoning item outputs. This enables reasoning items to be used in multi-turn conversations when using the Responses API statelessly (like when the `store` parameter is set to `false`, or when an organization is enrolled in the zero data retention program).
    pub include: Option<Vec<IncludeEnum>>,
}

#[derive(Debug, Clone, Default)]
pub struct ConversationsItemsRetrieveParams {
    /// Additional fields to include in the response. See the `include`
    /// parameter for [listing Conversation items above](/docs/api-reference/conversations/list-items#conversations_list_items-include) for more information.
    pub include: Option<Vec<IncludeEnum>>,
}

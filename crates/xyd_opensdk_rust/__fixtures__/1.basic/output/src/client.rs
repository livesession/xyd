// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::transport::Transport;

/// Client is the Petstore API client.
pub struct Client {
    transport: Arc<Transport>,
}

impl Client {
    pub fn new(api_key: impl Into<String>) -> Self {
        Client {
            transport: Arc::new(Transport::new(Some(api_key.into()), None, None)),
        }
    }

    pub fn from_env() -> Self {
        Client {
            transport: Arc::new(Transport::new(std::env::var("PETSTORE_API_KEY").ok(), None, None)),
        }
    }

    pub fn builder() -> ClientBuilder {
        ClientBuilder::default()
    }

    pub fn pets(&self) -> crate::pets::Pets {
        crate::pets::Pets::new(self.transport.clone())
    }
}

/// A builder for base URL / timeout overrides.
#[derive(Default)]
pub struct ClientBuilder {
    api_key: Option<String>,
    base_url: Option<String>,
    timeout_ms: Option<u64>,
}

impl ClientBuilder {
    pub fn api_key(mut self, api_key: impl Into<String>) -> Self {
        self.api_key = Some(api_key.into());
        self
    }

    pub fn base_url(mut self, base_url: impl Into<String>) -> Self {
        self.base_url = Some(base_url.into());
        self
    }

    pub fn timeout_ms(mut self, timeout_ms: u64) -> Self {
        self.timeout_ms = Some(timeout_ms);
        self
    }

    pub fn build(self) -> Client {
        Client {
            transport: Arc::new(Transport::new(self.api_key, self.base_url, self.timeout_ms)),
        }
    }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ModerationService {
  private final Transport transport;

  ModerationService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Classifies if text and/or image inputs are potentially harmful. Learn
   * more in the [moderation guide](/docs/guides/moderation).
   */
  public CreateModerationResponse create(ModerationCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/moderations");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("input", params.input());
    params.model().ifPresent(value -> body.put("model", value));
    request.body(body);
    return CreateModerationResponse.fromJson(transport.execute(request));
  }
}

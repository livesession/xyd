// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class EvalRunOutputItemService {
  private final Transport transport;

  EvalRunOutputItemService(Transport transport) {
    this.transport = transport;
  }

  /** Get an evaluation run output item by ID. */
  public EvalRunOutputItem retrieve(String evalId, String runId, String outputItemId) {
    if (evalId == null || evalId.isEmpty()) {
      throw new IllegalArgumentException("missing required eval_id parameter");
    }
    if (runId == null || runId.isEmpty()) {
      throw new IllegalArgumentException("missing required run_id parameter");
    }
    if (outputItemId == null || outputItemId.isEmpty()) {
      throw new IllegalArgumentException("missing required output_item_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/evals/" + Transport.encodePath(evalId) + "/runs/" + Transport.encodePath(runId) + "/output_items/" + Transport.encodePath(outputItemId));
    return EvalRunOutputItem.fromJson(transport.execute(request));
  }
}

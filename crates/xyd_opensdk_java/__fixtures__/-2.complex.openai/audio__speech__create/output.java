// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AudioSpeechService {
  private final Transport transport;

  AudioSpeechService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Generates audio from the input text.
   * 
   * Returns the audio file content, or a stream of audio events.
   */
  public byte[] create(AudioSpeechCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/audio/speech");
    request.accept("application/octet-stream");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("model", params.model());
    body.put("input", params.input());
    params.instructions().ifPresent(value -> body.put("instructions", value));
    body.put("voice", params.voice());
    params.responseFormat().ifPresent(value -> body.put("response_format", value));
    params.speed().ifPresent(value -> body.put("speed", value));
    params.streamFormat().ifPresent(value -> body.put("stream_format", value));
    request.body(body);
    return transport.executeRaw(request);
  }
}

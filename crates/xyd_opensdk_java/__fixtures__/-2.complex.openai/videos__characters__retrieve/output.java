// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class VideoCharacterService {
  private final Transport transport;

  VideoCharacterService(Transport transport) {
    this.transport = transport;
  }

  /** Fetch a character. */
  public VideoCharacterResource retrieve(String characterId) {
    if (characterId == null || characterId.isEmpty()) {
      throw new IllegalArgumentException("missing required character_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/videos/characters/" + Transport.encodePath(characterId));
    return VideoCharacterResource.fromJson(transport.execute(request));
  }
}

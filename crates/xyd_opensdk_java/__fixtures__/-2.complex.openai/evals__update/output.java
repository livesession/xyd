// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class EvalService {
  private final Transport transport;

  EvalService(Transport transport) {
    this.transport = transport;
  }

  /** Update certain properties of an evaluation. */
  public Eval update(String evalId, EvalUpdateParams params) {
    if (evalId == null || evalId.isEmpty()) {
      throw new IllegalArgumentException("missing required eval_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/evals/" + Transport.encodePath(evalId));
    Map<String, Object> body = new LinkedHashMap<>();
    params.name().ifPresent(value -> body.put("name", value));
    params.metadata().ifPresent(value -> body.put("metadata", value));
    request.body(body);
    return Eval.fromJson(transport.execute(request));
  }
}

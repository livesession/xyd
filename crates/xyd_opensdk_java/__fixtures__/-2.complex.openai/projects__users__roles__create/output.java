// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ProjectUserRoleService {
  private final Transport transport;

  ProjectUserRoleService(Transport transport) {
    this.transport = transport;
  }

  /** Assigns a project role to a user within a project. */
  public UserRoleAssignment create(String projectId, String userId, ProjectUserRoleCreateParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/projects/" + Transport.encodePath(projectId) + "/users/" + Transport.encodePath(userId) + "/roles");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("role_id", params.roleId());
    request.body(body);
    return UserRoleAssignment.fromJson(transport.execute(request));
  }
}

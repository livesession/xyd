// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class VectorStoreFileBatchService {
  private final Transport transport;

  VectorStoreFileBatchService(Transport transport) {
    this.transport = transport;
  }

  /**
   * The maximum number of files in a single batch request is 2000.
   * Vector store file attach requests are rate limited per vector store (300 requests per minute across both this endpoint and `/vector_stores/{vector_store_id}/files`).
   * For ingesting multiple files into the same vector store, this batch endpoint is recommended.
   */
  public VectorStoreFileBatchObject create(String vectorStoreId) {
    if (vectorStoreId == null || vectorStoreId.isEmpty()) {
      throw new IllegalArgumentException("missing required vector_store_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/vector_stores/" + Transport.encodePath(vectorStoreId) + "/file_batches");
    return VectorStoreFileBatchObject.fromJson(transport.execute(request));
  }
}

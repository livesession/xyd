// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class BetaChatKitSessionService {
  private final Transport transport;

  BetaChatKitSessionService(Transport transport) {
    this.transport = transport;
  }

  /** Create a ChatKit session. */
  public ChatSessionResource create(BetaChatKitSessionCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/chatkit/sessions");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("workflow", params.workflow());
    body.put("user", params.user());
    params.expiresAfter().ifPresent(value -> body.put("expires_after", value));
    params.rateLimits().ifPresent(value -> body.put("rate_limits", value));
    params.chatkitConfiguration().ifPresent(value -> body.put("chatkit_configuration", value));
    request.body(body);
    return ChatSessionResource.fromJson(transport.execute(request));
  }
}

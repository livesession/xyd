// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class AdminOrganizationProjectUserService {
  private final Transport transport;

  AdminOrganizationProjectUserService(Transport transport) {
    this.transport = transport;
  }

  /** Retrieves a user in the project. */
  public ProjectUser retrieve(String projectId, String userId) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/organization/projects/" + Transport.encodePath(projectId) + "/users/" + Transport.encodePath(userId));
    return ProjectUser.fromJson(transport.execute(request));
  }
}

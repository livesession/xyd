// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ConversationService {
  private final Transport transport;

  ConversationService(Transport transport) {
    this.transport = transport;
  }

  /** Update a conversation */
  public ConversationResource update(String conversationId, ConversationUpdateParams params) {
    if (conversationId == null || conversationId.isEmpty()) {
      throw new IllegalArgumentException("missing required conversation_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/conversations/" + Transport.encodePath(conversationId));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("metadata", params.metadata());
    request.body(body);
    return ConversationResource.fromJson(transport.execute(request));
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AdminOrganizationProjectGroupService {
  private final Transport transport;

  AdminOrganizationProjectGroupService(Transport transport) {
    this.transport = transport;
  }

  /** Grants a group access to a project. */
  public ProjectGroup create(String projectId, AdminOrganizationProjectGroupCreateParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/organization/projects/" + Transport.encodePath(projectId) + "/groups");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("group_id", params.groupId());
    body.put("role", params.role());
    request.body(body);
    return ProjectGroup.fromJson(transport.execute(request));
  }
}

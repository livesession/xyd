// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class BetaThreadMessageService {
  private final Transport transport;

  BetaThreadMessageService(Transport transport) {
    this.transport = transport;
  }

  /** Create a message. */
  public MessageObject create(String threadId, BetaThreadMessageCreateParams params) {
    if (threadId == null || threadId.isEmpty()) {
      throw new IllegalArgumentException("missing required thread_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/threads/" + Transport.encodePath(threadId) + "/messages");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("role", params.role());
    body.put("content", params.content());
    params.attachments().ifPresent(value -> body.put("attachments", value));
    params.metadata().ifPresent(value -> body.put("metadata", value));
    request.body(body);
    return MessageObject.fromJson(transport.execute(request));
  }
}

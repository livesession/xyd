// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class AdminOrganizationProjectApiKeyService {
  private final Transport transport;

  AdminOrganizationProjectApiKeyService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Deletes an API key from the project.
   * 
   * Returns confirmation of the key deletion, or an error if the key belonged to
   * a service account.
   */
  public ProjectApiKeyDeleteResponse delete(String projectId, String apiKeyId) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (apiKeyId == null || apiKeyId.isEmpty()) {
      throw new IllegalArgumentException("missing required api_key_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/organization/projects/" + Transport.encodePath(projectId) + "/api_keys/" + Transport.encodePath(apiKeyId));
    return ProjectApiKeyDeleteResponse.fromJson(transport.execute(request));
  }
}

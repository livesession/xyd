// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class ModelService {
  private final Transport transport;

  ModelService(Transport transport) {
    this.transport = transport;
  }

  /** Delete a fine-tuned model. You must have the Owner role in your organization to delete a model. */
  public DeleteModelResponse delete(String model) {
    if (model == null || model.isEmpty()) {
      throw new IllegalArgumentException("missing required model parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/models/" + Transport.encodePath(model));
    return DeleteModelResponse.fromJson(transport.execute(request));
  }
}

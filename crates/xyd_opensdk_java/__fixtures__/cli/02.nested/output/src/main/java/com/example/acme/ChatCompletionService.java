// Code generated by opensdk. DO NOT EDIT.

package com.example.acme;

import java.util.ArrayList;
import java.util.List;

public final class ChatCompletionService {
  private final CliTransport transport;

  ChatCompletionService(CliTransport transport) {
    this.transport = transport;
  }

  /** List completions */
  public CommandResult list() {
    List<String> argv = new ArrayList<>();
    argv.add("chat");
    argv.add("completions");
    argv.add("list");
    return transport.run(argv);
  }

  /** Create a completion */
  public CommandResult create(String prompt, ChatCompletionCreateParams params) {
    if (prompt == null || prompt.isEmpty()) {
      throw new IllegalArgumentException("missing required prompt parameter");
    }
    List<String> argv = new ArrayList<>();
    argv.add("chat");
    argv.add("completions");
    argv.add("create");
    argv.add(prompt);
    argv.add("--model");
    argv.add(params.model());
    params.temperature().ifPresent(value -> {
      argv.add("--temperature");
      argv.add(String.valueOf(value));
    });
    return transport.run(argv);
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.petstore;

/**
 * A dependency-free assertion test for PetService — run with plain `java PetServiceTest`.
 */
public final class PetServiceTest {
  private static final String BASE_URL =
      System.getenv().getOrDefault("TEST_API_BASE_URL", "http://localhost:4010");

  public static void main(String[] args) {
    Client client = Client.builder().baseUrl(BASE_URL).apiKey("My API Key").build();
    testPetListWithOptionalParams(client);
    testPetCreateWithOptionalParams(client);
    testPetRetrieve(client);
    testPetRetrievePathParams(client);
    testPetDownloadPhoto(client);
    testPetDownloadPhotoPathParams(client);
    System.out.println("PetServiceTest OK");
  }

  private static void testPetListWithOptionalParams(Client client) {
    try {
      client.pets().list(PetListParams.builder()
          .after("after")
          .limit(0L)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testPetCreateWithOptionalParams(Client client) {
    try {
      client.pets().create(PetCreateParams.builder()
          .name("x")
          .status(PetStatus.AVAILABLE)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testPetRetrieve(Client client) {
    try {
      client.pets().retrieve("pet_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testPetRetrievePathParams(Client client) {
    try {
      client.pets().retrieve("");
      throw new AssertionError("expected a missing required path param error for retrieve");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testPetDownloadPhoto(Client client) {
    try {
      client.pets().downloadPhoto("pet_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testPetDownloadPhotoPathParams(Client client) {
    try {
      client.pets().downloadPhoto("");
      throw new AssertionError("expected a missing required path param error for downloadPhoto");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void requireContains(String actual, String needle) {
    if (actual == null || !actual.contains(needle)) {
      throw new AssertionError("missing substring [" + needle + "] in: " + actual);
    }
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.Map;

/** Decode helper for the FunctionShellCallEnvironment union, selecting the concrete variant by its "type" discriminator. */
public final class FunctionShellCallEnvironment {
  private FunctionShellCallEnvironment() {
  }

  /**
   * Decode JSON into the concrete FunctionShellCallEnvironment variant selected by the "type"
   * discriminator. An unknown or absent discriminator keeps the raw parsed value.
   */
  public static Object fromJson(Object json) {
    if (!(json instanceof Map)) {
      return json;
    }
    Object discriminator = ((Map<?, ?>) json).get("type");
    if ("container_reference".equals(discriminator)) {
      return ContainerReferenceResource.fromJson(json);
    }
    if ("local".equals(discriminator)) {
      return LocalEnvironmentResource.fromJson(json);
    }
    return json;
  }
}

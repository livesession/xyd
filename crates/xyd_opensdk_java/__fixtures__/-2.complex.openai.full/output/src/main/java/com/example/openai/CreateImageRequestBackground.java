// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * Allows to set transparency for the background of the generated image(s).
 * This parameter is only supported for the GPT image models. Must be one of
 * `transparent`, `opaque` or `auto` (default value). When `auto` is used, the
 * model will automatically determine the best background for the image.
 * 
 * If `transparent`, the output format needs to support transparency, so it
 * should be set to either `png` (default value) or `webp`.
 */
public enum CreateImageRequestBackground implements Json.JsonEnum {
  TRANSPARENT("transparent"),
  OPAQUE("opaque"),
  AUTO("auto");

  private final String value;

  CreateImageRequestBackground(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static CreateImageRequestBackground fromValue(String value) {
    for (CreateImageRequestBackground item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown CreateImageRequestBackground value: " + value);
  }

  public static CreateImageRequestBackground fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

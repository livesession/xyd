// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * A dependency-free assertion test for ResponseService — run with plain `java ResponseServiceTest`.
 */
public final class ResponseServiceTest {
  private static final String BASE_URL =
      System.getenv().getOrDefault("TEST_API_BASE_URL", "http://localhost:4010");

  public static void main(String[] args) {
    Client client = Client.builder().baseUrl(BASE_URL).apiKey("My API Key").build();
    testResponseCancel(client);
    testResponseCancelPathParams(client);
    testResponseCreateWithOptionalParams(client);
    testResponseDelete(client);
    testResponseDeletePathParams(client);
    testResponseRetrieveWithOptionalParams(client);
    testResponseRetrievePathParams(client);
    testResponseCompactCreateWithOptionalParams(client);
    testResponseInputItemListWithOptionalParams(client);
    testResponseInputItemListPathParams(client);
    testResponseInputTokenCountWithOptionalParams(client);
    System.out.println("ResponseServiceTest OK");
  }

  private static void testResponseCancel(Client client) {
    try {
      client.responses().cancel("response_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testResponseCancelPathParams(Client client) {
    try {
      client.responses().cancel("");
      throw new AssertionError("expected a missing required path param error for cancel");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testResponseCreateWithOptionalParams(Client client) {
    try {
      client.responses().create(ResponseCreateParams.builder()
          .metadata(java.util.Map.of("key", "x"))
          .topLogprobs(0L)
          .temperature(0.0)
          .topP(0.0)
          .user("x")
          .safetyIdentifier("x")
          .promptCacheKey("x")
          .serviceTier(ServiceTier.AUTO)
          .promptCacheRetention(CreateResponsePromptCacheRetention.IN_MEMORY)
          .previousResponseId("x")
          .model("x")
          .reasoning(new Reasoning())
          .background(true)
          .maxToolCalls(0L)
          .text(new ResponseTextParam())
          .tools(java.util.List.of())
          .toolChoice(ToolChoiceOptions.NONE)
          .prompt(new Prompt())
          .truncation(CreateResponseTruncation.AUTO)
          .input("x")
          .include(java.util.List.of(IncludeEnum.FILE_SEARCH_CALL_RESULTS))
          .parallelToolCalls(true)
          .store(true)
          .instructions("x")
          .stream(true)
          .streamOptions(new ResponseStreamOptions())
          .conversation("x")
          .contextManagement(java.util.List.of())
          .maxOutputTokens(0L)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testResponseDelete(Client client) {
    try {
      client.responses().delete("response_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testResponseDeletePathParams(Client client) {
    try {
      client.responses().delete("");
      throw new AssertionError("expected a missing required path param error for delete");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testResponseRetrieveWithOptionalParams(Client client) {
    try {
      client.responses().retrieve("response_id", ResponseRetrieveParams.builder()
          .include(java.util.List.of(IncludeEnum.FILE_SEARCH_CALL_RESULTS))
          .stream(true)
          .startingAfter(0L)
          .includeObfuscation(true)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testResponseRetrievePathParams(Client client) {
    try {
      client.responses().retrieve("", ResponseRetrieveParams.builder().build());
      throw new AssertionError("expected a missing required path param error for retrieve");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testResponseCompactCreateWithOptionalParams(Client client) {
    try {
      client.responses().compact().create(ResponseCompactCreateParams.builder()
          .model("x")
          .input("x")
          .previousResponseId("x")
          .instructions("x")
          .promptCacheKey("x")
          .promptCacheRetention(PromptCacheRetentionEnum.IN_MEMORY)
          .serviceTier(ServiceTierEnum.AUTO)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testResponseInputItemListWithOptionalParams(Client client) {
    try {
      client.responses().inputItems().list("response_id", ResponseInputItemListParams.builder()
          .limit(0L)
          .order(ResponsesInputItemsListOrder.ASC)
          .after("after")
          .include(java.util.List.of(IncludeEnum.FILE_SEARCH_CALL_RESULTS))
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testResponseInputItemListPathParams(Client client) {
    try {
      client.responses().inputItems().list("", ResponseInputItemListParams.builder().build());
      throw new AssertionError("expected a missing required path param error for list");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testResponseInputTokenCountWithOptionalParams(Client client) {
    try {
      client.responses().inputTokens().count(ResponseInputTokenCountParams.builder()
          .model("x")
          .input("x")
          .previousResponseId("x")
          .tools(java.util.List.of())
          .text(new ResponseTextParam())
          .reasoning(new Reasoning())
          .truncation(TruncationEnum.AUTO)
          .instructions("x")
          .conversation("x")
          .toolChoice(ToolChoiceOptions.NONE)
          .parallelToolCalls(true)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void requireContains(String actual, String needle) {
    if (actual == null || !actual.contains(needle)) {
      throw new AssertionError("missing substring [" + needle + "] in: " + actual);
    }
  }
}

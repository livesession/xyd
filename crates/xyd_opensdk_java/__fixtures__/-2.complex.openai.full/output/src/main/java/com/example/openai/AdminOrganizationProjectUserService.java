// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AdminOrganizationProjectUserService {
  private final Transport transport;

  AdminOrganizationProjectUserService(Transport transport) {
    this.transport = transport;
  }

  /** Adds a user to the project. Users must already be members of the organization to be added to a project. */
  public ProjectUser create(String projectId, AdminOrganizationProjectUserCreateParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/organization/projects/" + Transport.encodePath(projectId) + "/users");
    Map<String, Object> body = new LinkedHashMap<>();
    params.userId().ifPresent(value -> body.put("user_id", value));
    params.email().ifPresent(value -> body.put("email", value));
    body.put("role", params.role());
    request.body(body);
    return ProjectUser.fromJson(transport.execute(request));
  }

  /**
   * Deletes a user from the project.
   * 
   * Returns confirmation of project user deletion, or an error if the project is
   * archived (archived projects have no users).
   */
  public ProjectUserDeleteResponse delete(String projectId, String userId) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/organization/projects/" + Transport.encodePath(projectId) + "/users/" + Transport.encodePath(userId));
    return ProjectUserDeleteResponse.fromJson(transport.execute(request));
  }

  /** Returns a list of users in the project. */
  public CursorPage<ProjectUser> list(String projectId, AdminOrganizationProjectUserListParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/organization/projects/" + Transport.encodePath(projectId) + "/users");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> ProjectUser.fromJson(__e));
  }

  /** Retrieves a user in the project. */
  public ProjectUser retrieve(String projectId, String userId) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/organization/projects/" + Transport.encodePath(projectId) + "/users/" + Transport.encodePath(userId));
    return ProjectUser.fromJson(transport.execute(request));
  }

  /** Modifies a user's role in the project. */
  public ProjectUser update(String projectId, String userId, AdminOrganizationProjectUserUpdateParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/organization/projects/" + Transport.encodePath(projectId) + "/users/" + Transport.encodePath(userId));
    Map<String, Object> body = new LinkedHashMap<>();
    params.role().ifPresent(value -> body.put("role", value));
    request.body(body);
    return ProjectUser.fromJson(transport.execute(request));
  }
}

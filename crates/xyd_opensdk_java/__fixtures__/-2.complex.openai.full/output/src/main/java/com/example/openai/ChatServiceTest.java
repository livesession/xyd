// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * A dependency-free assertion test for ChatService — run with plain `java ChatServiceTest`.
 */
public final class ChatServiceTest {
  private static final String BASE_URL =
      System.getenv().getOrDefault("TEST_API_BASE_URL", "http://localhost:4010");

  public static void main(String[] args) {
    Client client = Client.builder().baseUrl(BASE_URL).apiKey("My API Key").build();
    testChatCompletionCreateWithOptionalParams(client);
    testChatCompletionDelete(client);
    testChatCompletionDeletePathParams(client);
    testChatCompletionListWithOptionalParams(client);
    testChatCompletionRetrieve(client);
    testChatCompletionRetrievePathParams(client);
    testChatCompletionUpdate(client);
    testChatCompletionUpdatePathParams(client);
    testChatCompletionMessageListWithOptionalParams(client);
    testChatCompletionMessageListPathParams(client);
    System.out.println("ChatServiceTest OK");
  }

  private static void testChatCompletionCreateWithOptionalParams(Client client) {
    try {
      client.chat().completions().create(ChatCompletionCreateParams.builder()
          .messages(java.util.List.of())
          .model("x")
          .metadata(java.util.Map.of("key", "x"))
          .topLogprobs(0L)
          .temperature(0.0)
          .topP(0.0)
          .user("x")
          .safetyIdentifier("x")
          .promptCacheKey("x")
          .serviceTier(ServiceTier.AUTO)
          .promptCacheRetention(CreateChatCompletionRequestPromptCacheRetention.IN_MEMORY)
          .modalities(java.util.List.of(ResponseModalitiesItem.TEXT))
          .verbosity(Verbosity.LOW)
          .reasoningEffort(ReasoningEffort.NONE)
          .maxCompletionTokens(0L)
          .frequencyPenalty(0.0)
          .presencePenalty(0.0)
          .webSearchOptions(new CreateChatCompletionRequestWebSearchOptions())
          .responseFormat(new ResponseFormatText())
          .audio(new CreateChatCompletionRequestAudio())
          .store(true)
          .stream(true)
          .stop("x")
          .logitBias(java.util.Map.of("key", 0L))
          .logprobs(true)
          .maxTokens(0L)
          .n(0L)
          .prediction(new PredictionContent())
          .seed(0L)
          .streamOptions(new ChatCompletionStreamOptions())
          .tools(java.util.List.of())
          .toolChoice(ChatCompletionToolChoiceOptionToolChoiceMode.NONE)
          .parallelToolCalls(true)
          .functionCall(CreateChatCompletionRequestFunctionCallString.NONE)
          .functions(java.util.List.of())
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testChatCompletionDelete(Client client) {
    try {
      client.chat().completions().delete("completion_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testChatCompletionDeletePathParams(Client client) {
    try {
      client.chat().completions().delete("");
      throw new AssertionError("expected a missing required path param error for delete");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testChatCompletionListWithOptionalParams(Client client) {
    try {
      client.chat().completions().list(ChatCompletionListParams.builder()
          .model("model")
          .metadata(java.util.Map.of("key", "x"))
          .after("after")
          .limit(0L)
          .order(ChatCompletionsListOrder.ASC)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testChatCompletionRetrieve(Client client) {
    try {
      client.chat().completions().retrieve("completion_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testChatCompletionRetrievePathParams(Client client) {
    try {
      client.chat().completions().retrieve("");
      throw new AssertionError("expected a missing required path param error for retrieve");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testChatCompletionUpdate(Client client) {
    try {
      client.chat().completions().update("completion_id", ChatCompletionUpdateParams.builder()
          .metadata(java.util.Map.of("key", "x"))
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testChatCompletionUpdatePathParams(Client client) {
    try {
      client.chat().completions().update("", ChatCompletionUpdateParams.builder()
          .metadata(java.util.Map.of("key", "x"))
          .build());
      throw new AssertionError("expected a missing required path param error for update");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testChatCompletionMessageListWithOptionalParams(Client client) {
    try {
      client.chat().completions().messages().list("completion_id", ChatCompletionMessageListParams.builder()
          .after("after")
          .limit(0L)
          .order(ChatCompletionsMessagesListOrder.ASC)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testChatCompletionMessageListPathParams(Client client) {
    try {
      client.chat().completions().messages().list("", ChatCompletionMessageListParams.builder().build());
      throw new AssertionError("expected a missing required path param error for list");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void requireContains(String actual, String needle) {
    if (actual == null || !actual.contains(needle)) {
      throw new AssertionError("missing substring [" + needle + "] in: " + actual);
    }
  }
}

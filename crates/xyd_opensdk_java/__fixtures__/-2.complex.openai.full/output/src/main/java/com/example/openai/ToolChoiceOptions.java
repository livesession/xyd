// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * Controls which (if any) tool is called by the model.
 * 
 * `none` means the model will not call any tool and instead generates a message.
 * 
 * `auto` means the model can pick between generating a message or calling one or
 * more tools.
 * 
 * `required` means the model must call one or more tools.
 */
public enum ToolChoiceOptions implements Json.JsonEnum {
  NONE("none"),
  AUTO("auto"),
  REQUIRED("required");

  private final String value;

  ToolChoiceOptions(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static ToolChoiceOptions fromValue(String value) {
    for (ToolChoiceOptions item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown ToolChoiceOptions value: " + value);
  }

  public static ToolChoiceOptions fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

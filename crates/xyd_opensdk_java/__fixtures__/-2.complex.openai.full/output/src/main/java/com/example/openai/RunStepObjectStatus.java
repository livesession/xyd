// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/** The status of the run step, which can be either `in_progress`, `cancelled`, `failed`, `completed`, or `expired`. */
public enum RunStepObjectStatus implements Json.JsonEnum {
  IN_PROGRESS("in_progress"),
  CANCELLED("cancelled"),
  FAILED("failed"),
  COMPLETED("completed"),
  EXPIRED("expired");

  private final String value;

  RunStepObjectStatus(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static RunStepObjectStatus fromValue(String value) {
    for (RunStepObjectStatus item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown RunStepObjectStatus value: " + value);
  }

  public static RunStepObjectStatus fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

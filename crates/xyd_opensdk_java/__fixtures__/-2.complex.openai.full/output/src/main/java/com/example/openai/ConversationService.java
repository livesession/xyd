// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ConversationService {
  private final Transport transport;
  private final ConversationItemService items;

  ConversationService(Transport transport) {
    this.transport = transport;
    this.items = new ConversationItemService(transport);
  }

  public ConversationItemService items() {
    return items;
  }

  /** Create a conversation. */
  public ConversationResource create(ConversationCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/conversations");
    Map<String, Object> body = new LinkedHashMap<>();
    params.metadata().ifPresent(value -> body.put("metadata", value));
    params.items().ifPresent(value -> body.put("items", value));
    request.body(body);
    return ConversationResource.fromJson(transport.execute(request));
  }

  /** Delete a conversation. Items in the conversation will not be deleted. */
  public DeletedConversationResource delete(String conversationId) {
    if (conversationId == null || conversationId.isEmpty()) {
      throw new IllegalArgumentException("missing required conversation_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/conversations/" + Transport.encodePath(conversationId));
    return DeletedConversationResource.fromJson(transport.execute(request));
  }

  /** Get a conversation */
  public ConversationResource retrieve(String conversationId) {
    if (conversationId == null || conversationId.isEmpty()) {
      throw new IllegalArgumentException("missing required conversation_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/conversations/" + Transport.encodePath(conversationId));
    return ConversationResource.fromJson(transport.execute(request));
  }

  /** Update a conversation */
  public ConversationResource update(String conversationId, ConversationUpdateParams params) {
    if (conversationId == null || conversationId.isEmpty()) {
      throw new IllegalArgumentException("missing required conversation_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/conversations/" + Transport.encodePath(conversationId));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("metadata", params.metadata());
    request.body(body);
    return ConversationResource.fromJson(transport.execute(request));
  }
}

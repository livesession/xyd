// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AdminOrganizationInviteService {
  private final Transport transport;

  AdminOrganizationInviteService(Transport transport) {
    this.transport = transport;
  }

  /** Create an invite for a user to the organization. The invite must be accepted by the user before they have access to the organization. */
  public Invite create(AdminOrganizationInviteCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/organization/invites");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("email", params.email());
    body.put("role", params.role());
    params.projects().ifPresent(value -> body.put("projects", value));
    request.body(body);
    return Invite.fromJson(transport.execute(request));
  }

  /** Delete an invite. If the invite has already been accepted, it cannot be deleted. */
  public InviteDeleteResponse delete(String inviteId) {
    if (inviteId == null || inviteId.isEmpty()) {
      throw new IllegalArgumentException("missing required invite_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/organization/invites/" + Transport.encodePath(inviteId));
    return InviteDeleteResponse.fromJson(transport.execute(request));
  }

  /** Returns a list of invites in the organization. */
  public CursorPage<Invite> list(AdminOrganizationInviteListParams params) {
    Transport.Request request = new Transport.Request("GET", "/organization/invites");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> Invite.fromJson(__e));
  }

  /** Retrieves an invite. */
  public Invite retrieve(String inviteId) {
    if (inviteId == null || inviteId.isEmpty()) {
      throw new IllegalArgumentException("missing required invite_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/organization/invites/" + Transport.encodePath(inviteId));
    return Invite.fromJson(transport.execute(request));
  }
}

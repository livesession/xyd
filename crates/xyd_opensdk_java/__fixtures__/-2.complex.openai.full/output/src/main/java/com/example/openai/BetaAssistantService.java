// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class BetaAssistantService {
  private final Transport transport;

  BetaAssistantService(Transport transport) {
    this.transport = transport;
  }

  /** Create an assistant with a model and instructions. */
  public AssistantObject create(BetaAssistantCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/assistants");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("model", params.model());
    params.name().ifPresent(value -> body.put("name", value));
    params.description().ifPresent(value -> body.put("description", value));
    params.instructions().ifPresent(value -> body.put("instructions", value));
    params.reasoningEffort().ifPresent(value -> body.put("reasoning_effort", value));
    params.tools().ifPresent(value -> body.put("tools", value));
    params.toolResources().ifPresent(value -> body.put("tool_resources", value));
    params.metadata().ifPresent(value -> body.put("metadata", value));
    params.temperature().ifPresent(value -> body.put("temperature", value));
    params.topP().ifPresent(value -> body.put("top_p", value));
    params.responseFormat().ifPresent(value -> body.put("response_format", value));
    request.body(body);
    return AssistantObject.fromJson(transport.execute(request));
  }

  /** Delete an assistant. */
  public DeleteAssistantResponse delete(String assistantId) {
    if (assistantId == null || assistantId.isEmpty()) {
      throw new IllegalArgumentException("missing required assistant_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/assistants/" + Transport.encodePath(assistantId));
    return DeleteAssistantResponse.fromJson(transport.execute(request));
  }

  /** Returns a list of assistants. */
  public CursorPage<AssistantObject> list(BetaAssistantListParams params) {
    Transport.Request request = new Transport.Request("GET", "/assistants");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.before().ifPresent(value -> request.query("before", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> AssistantObject.fromJson(__e));
  }

  /** Retrieves an assistant. */
  public AssistantObject retrieve(String assistantId) {
    if (assistantId == null || assistantId.isEmpty()) {
      throw new IllegalArgumentException("missing required assistant_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/assistants/" + Transport.encodePath(assistantId));
    return AssistantObject.fromJson(transport.execute(request));
  }

  /** Modifies an assistant. */
  public AssistantObject update(String assistantId, BetaAssistantUpdateParams params) {
    if (assistantId == null || assistantId.isEmpty()) {
      throw new IllegalArgumentException("missing required assistant_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/assistants/" + Transport.encodePath(assistantId));
    Map<String, Object> body = new LinkedHashMap<>();
    params.model().ifPresent(value -> body.put("model", value));
    params.reasoningEffort().ifPresent(value -> body.put("reasoning_effort", value));
    params.name().ifPresent(value -> body.put("name", value));
    params.description().ifPresent(value -> body.put("description", value));
    params.instructions().ifPresent(value -> body.put("instructions", value));
    params.tools().ifPresent(value -> body.put("tools", value));
    params.toolResources().ifPresent(value -> body.put("tool_resources", value));
    params.metadata().ifPresent(value -> body.put("metadata", value));
    params.temperature().ifPresent(value -> body.put("temperature", value));
    params.topP().ifPresent(value -> body.put("top_p", value));
    params.responseFormat().ifPresent(value -> body.put("response_format", value));
    request.body(body);
    return AssistantObject.fromJson(transport.execute(request));
  }
}

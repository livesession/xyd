// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class EvalService {
  private final Transport transport;
  private final EvalRunService runs;

  EvalService(Transport transport) {
    this.transport = transport;
    this.runs = new EvalRunService(transport);
  }

  public EvalRunService runs() {
    return runs;
  }

  /**
   * Create the structure of an evaluation that can be used to test a model's performance.
   * An evaluation is a set of testing criteria and the config for a data source, which dictates the schema of the data used in the evaluation. After creating an evaluation, you can run it on different models and model parameters. We support several types of graders and datasources.
   * For more information, see the [Evals guide](/docs/guides/evals).
   */
  public Eval create(EvalCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/evals");
    Map<String, Object> body = new LinkedHashMap<>();
    params.name().ifPresent(value -> body.put("name", value));
    params.metadata().ifPresent(value -> body.put("metadata", value));
    body.put("data_source_config", params.dataSourceConfig());
    body.put("testing_criteria", params.testingCriteria());
    request.body(body);
    return Eval.fromJson(transport.execute(request));
  }

  /** Delete an evaluation. */
  public EvalsDeleteResponse delete(String evalId) {
    if (evalId == null || evalId.isEmpty()) {
      throw new IllegalArgumentException("missing required eval_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/evals/" + Transport.encodePath(evalId));
    return EvalsDeleteResponse.fromJson(transport.execute(request));
  }

  /** List evaluations for a project. */
  public CursorPage<Eval> list(EvalListParams params) {
    Transport.Request request = new Transport.Request("GET", "/evals");
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    params.orderBy().ifPresent(value -> request.query("order_by", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> Eval.fromJson(__e));
  }

  /** Get an evaluation by ID. */
  public Eval retrieve(String evalId) {
    if (evalId == null || evalId.isEmpty()) {
      throw new IllegalArgumentException("missing required eval_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/evals/" + Transport.encodePath(evalId));
    return Eval.fromJson(transport.execute(request));
  }

  /** Update certain properties of an evaluation. */
  public Eval update(String evalId, EvalUpdateParams params) {
    if (evalId == null || evalId.isEmpty()) {
      throw new IllegalArgumentException("missing required eval_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/evals/" + Transport.encodePath(evalId));
    Map<String, Object> body = new LinkedHashMap<>();
    params.name().ifPresent(value -> body.put("name", value));
    params.metadata().ifPresent(value -> body.put("metadata", value));
    request.body(body);
    return Eval.fromJson(transport.execute(request));
  }
}

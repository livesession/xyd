// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class VectorStoreFileService {
  private final Transport transport;

  VectorStoreFileService(Transport transport) {
    this.transport = transport;
  }

  /** Retrieve the parsed contents of a vector store file. */
  public CursorPage<VectorStoreFileContentResponseDataItem> content(String vectorStoreId, String fileId) {
    if (vectorStoreId == null || vectorStoreId.isEmpty()) {
      throw new IllegalArgumentException("missing required vector_store_id parameter");
    }
    if (fileId == null || fileId.isEmpty()) {
      throw new IllegalArgumentException("missing required file_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/vector_stores/" + Transport.encodePath(vectorStoreId) + "/files/" + Transport.encodePath(fileId) + "/content");
    return CursorPage.fromJson(transport.execute(request), __e -> VectorStoreFileContentResponseDataItem.fromJson(__e));
  }

  /**
   * This endpoint is subject to a per-vector-store write rate limit of 300 requests per minute, shared with `/vector_stores/{vector_store_id}/file_batches`.
   * For uploading multiple files to the same vector store, use the file batches endpoint to reduce request volume.
   */
  public VectorStoreFileObject create(String vectorStoreId, VectorStoreFileCreateParams params) {
    if (vectorStoreId == null || vectorStoreId.isEmpty()) {
      throw new IllegalArgumentException("missing required vector_store_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/vector_stores/" + Transport.encodePath(vectorStoreId) + "/files");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("file_id", params.fileId());
    params.chunkingStrategy().ifPresent(value -> body.put("chunking_strategy", value));
    params.attributes().ifPresent(value -> body.put("attributes", value));
    request.body(body);
    return VectorStoreFileObject.fromJson(transport.execute(request));
  }

  /** Delete a vector store file. This will remove the file from the vector store but the file itself will not be deleted. To delete the file, use the [delete file](/docs/api-reference/files/delete) endpoint. */
  public DeleteVectorStoreFileResponse delete(String vectorStoreId, String fileId) {
    if (vectorStoreId == null || vectorStoreId.isEmpty()) {
      throw new IllegalArgumentException("missing required vector_store_id parameter");
    }
    if (fileId == null || fileId.isEmpty()) {
      throw new IllegalArgumentException("missing required file_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/vector_stores/" + Transport.encodePath(vectorStoreId) + "/files/" + Transport.encodePath(fileId));
    return DeleteVectorStoreFileResponse.fromJson(transport.execute(request));
  }

  /** Returns a list of vector store files. */
  public CursorPage<VectorStoreFileObject> list(String vectorStoreId, VectorStoreFileListParams params) {
    if (vectorStoreId == null || vectorStoreId.isEmpty()) {
      throw new IllegalArgumentException("missing required vector_store_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/vector_stores/" + Transport.encodePath(vectorStoreId) + "/files");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.before().ifPresent(value -> request.query("before", String.valueOf(value)));
    params.filter().ifPresent(value -> request.query("filter", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> VectorStoreFileObject.fromJson(__e));
  }

  /** Retrieves a vector store file. */
  public VectorStoreFileObject retrieve(String vectorStoreId, String fileId) {
    if (vectorStoreId == null || vectorStoreId.isEmpty()) {
      throw new IllegalArgumentException("missing required vector_store_id parameter");
    }
    if (fileId == null || fileId.isEmpty()) {
      throw new IllegalArgumentException("missing required file_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/vector_stores/" + Transport.encodePath(vectorStoreId) + "/files/" + Transport.encodePath(fileId));
    return VectorStoreFileObject.fromJson(transport.execute(request));
  }

  /** Update attributes on a vector store file. */
  public VectorStoreFileObject update(String vectorStoreId, String fileId, VectorStoreFileUpdateParams params) {
    if (vectorStoreId == null || vectorStoreId.isEmpty()) {
      throw new IllegalArgumentException("missing required vector_store_id parameter");
    }
    if (fileId == null || fileId.isEmpty()) {
      throw new IllegalArgumentException("missing required file_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/vector_stores/" + Transport.encodePath(vectorStoreId) + "/files/" + Transport.encodePath(fileId));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("attributes", params.attributes());
    request.body(body);
    return VectorStoreFileObject.fromJson(transport.execute(request));
  }
}

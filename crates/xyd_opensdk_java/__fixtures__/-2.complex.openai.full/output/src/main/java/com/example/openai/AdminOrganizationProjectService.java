// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AdminOrganizationProjectService {
  private final Transport transport;
  private final AdminOrganizationProjectApiKeyService apiKeys;
  private final AdminOrganizationProjectCertificateService certificates;
  private final AdminOrganizationProjectGroupService groups;
  private final AdminOrganizationProjectRateLimitService rateLimits;
  private final AdminOrganizationProjectServiceAccountService serviceAccounts;
  private final AdminOrganizationProjectUserService users;

  AdminOrganizationProjectService(Transport transport) {
    this.transport = transport;
    this.apiKeys = new AdminOrganizationProjectApiKeyService(transport);
    this.certificates = new AdminOrganizationProjectCertificateService(transport);
    this.groups = new AdminOrganizationProjectGroupService(transport);
    this.rateLimits = new AdminOrganizationProjectRateLimitService(transport);
    this.serviceAccounts = new AdminOrganizationProjectServiceAccountService(transport);
    this.users = new AdminOrganizationProjectUserService(transport);
  }

  public AdminOrganizationProjectApiKeyService apiKeys() {
    return apiKeys;
  }

  public AdminOrganizationProjectCertificateService certificates() {
    return certificates;
  }

  public AdminOrganizationProjectGroupService groups() {
    return groups;
  }

  public AdminOrganizationProjectRateLimitService rateLimits() {
    return rateLimits;
  }

  public AdminOrganizationProjectServiceAccountService serviceAccounts() {
    return serviceAccounts;
  }

  public AdminOrganizationProjectUserService users() {
    return users;
  }

  /** Archives a project in the organization. Archived projects cannot be used or updated. */
  public Project archive(String projectId) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/organization/projects/" + Transport.encodePath(projectId) + "/archive");
    return Project.fromJson(transport.execute(request));
  }

  /** Create a new project in the organization. Projects can be created and archived, but cannot be deleted. */
  public Project create(AdminOrganizationProjectCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/organization/projects");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("name", params.name());
    params.geography().ifPresent(value -> body.put("geography", value));
    params.externalKeyId().ifPresent(value -> body.put("external_key_id", value));
    request.body(body);
    return Project.fromJson(transport.execute(request));
  }

  /** Returns a list of projects. */
  public CursorPage<Project> list(AdminOrganizationProjectListParams params) {
    Transport.Request request = new Transport.Request("GET", "/organization/projects");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.includeArchived().ifPresent(value -> request.query("include_archived", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> Project.fromJson(__e));
  }

  /** Retrieves a project. */
  public Project retrieve(String projectId) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/organization/projects/" + Transport.encodePath(projectId));
    return Project.fromJson(transport.execute(request));
  }

  /** Modifies a project in the organization. */
  public Project update(String projectId, AdminOrganizationProjectUpdateParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/organization/projects/" + Transport.encodePath(projectId));
    Map<String, Object> body = new LinkedHashMap<>();
    params.name().ifPresent(value -> body.put("name", value));
    params.externalKeyId().ifPresent(value -> body.put("external_key_id", value));
    params.geography().ifPresent(value -> body.put("geography", value));
    request.body(body);
    return Project.fromJson(transport.execute(request));
  }
}

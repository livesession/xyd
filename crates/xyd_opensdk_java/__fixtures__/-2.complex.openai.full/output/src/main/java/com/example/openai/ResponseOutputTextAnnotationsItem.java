// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.Map;

/** Annotation object describing a cited source. */
public final class ResponseOutputTextAnnotationsItem {
  private ResponseOutputTextAnnotationsItem() {
  }

  /**
   * Decode JSON into the concrete ResponseOutputTextAnnotationsItem variant selected by the "type"
   * discriminator. An unknown or absent discriminator keeps the raw parsed value.
   */
  public static Object fromJson(Object json) {
    if (!(json instanceof Map)) {
      return json;
    }
    Object discriminator = ((Map<?, ?>) json).get("type");
    if ("file".equals(discriminator)) {
      return FileAnnotation.fromJson(json);
    }
    if ("url".equals(discriminator)) {
      return UrlAnnotation.fromJson(json);
    }
    return json;
  }
}

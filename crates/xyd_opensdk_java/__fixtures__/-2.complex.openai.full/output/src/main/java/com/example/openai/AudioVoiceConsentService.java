// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AudioVoiceConsentService {
  private final Transport transport;

  AudioVoiceConsentService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Upload a consent recording that authorizes creation of a custom voice.
   * 
   * See the [custom voices guide](/docs/guides/text-to-speech#custom-voices) for requirements and best practices. Custom voices are limited to eligible customers.
   */
  public VoiceConsentResource create(AudioVoiceConsentCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/audio/voice_consents");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("name", params.name());
    body.put("recording", params.recording());
    body.put("language", params.language());
    request.body(body);
    request.encoding("multipart");
    return VoiceConsentResource.fromJson(transport.execute(request));
  }

  /**
   * Delete a consent recording that was uploaded for creating custom voices.
   * 
   * See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
   */
  public VoiceConsentDeletedResource delete(String consentId) {
    if (consentId == null || consentId.isEmpty()) {
      throw new IllegalArgumentException("missing required consent_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/audio/voice_consents/" + Transport.encodePath(consentId));
    return VoiceConsentDeletedResource.fromJson(transport.execute(request));
  }

  /**
   * List consent recordings available to your organization for creating custom voices.
   * 
   * See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
   */
  public CursorPage<VoiceConsentResource> list(AudioVoiceConsentListParams params) {
    Transport.Request request = new Transport.Request("GET", "/audio/voice_consents");
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> VoiceConsentResource.fromJson(__e));
  }

  /**
   * Retrieve consent recording metadata used for creating custom voices.
   * 
   * See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
   */
  public VoiceConsentResource retrieve(String consentId) {
    if (consentId == null || consentId.isEmpty()) {
      throw new IllegalArgumentException("missing required consent_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/audio/voice_consents/" + Transport.encodePath(consentId));
    return VoiceConsentResource.fromJson(transport.execute(request));
  }

  /**
   * Update consent recording metadata used for creating custom voices. This endpoint updates metadata only and does not replace the underlying audio.
   * 
   * See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
   */
  public VoiceConsentResource update(String consentId, AudioVoiceConsentUpdateParams params) {
    if (consentId == null || consentId.isEmpty()) {
      throw new IllegalArgumentException("missing required consent_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/audio/voice_consents/" + Transport.encodePath(consentId));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("name", params.name());
    request.body(body);
    return VoiceConsentResource.fromJson(transport.execute(request));
  }
}

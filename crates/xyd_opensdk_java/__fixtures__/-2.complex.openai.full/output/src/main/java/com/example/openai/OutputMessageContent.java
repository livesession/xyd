// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.Map;

/** Decode helper for the OutputMessageContent union, selecting the concrete variant by its "type" discriminator. */
public final class OutputMessageContent {
  private OutputMessageContent() {
  }

  /**
   * Decode JSON into the concrete OutputMessageContent variant selected by the "type"
   * discriminator. An unknown or absent discriminator keeps the raw parsed value.
   */
  public static Object fromJson(Object json) {
    if (!(json instanceof Map)) {
      return json;
    }
    Object discriminator = ((Map<?, ?>) json).get("type");
    if ("output_text".equals(discriminator)) {
      return OutputTextContent.fromJson(json);
    }
    if ("refusal".equals(discriminator)) {
      return RefusalContent.fromJson(json);
    }
    return json;
  }
}

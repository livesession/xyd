// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/** The format of the output, in one of these options: `json`, `text`, `srt`, `verbose_json`, `vtt`, or `diarized_json`. For `gpt-4o-transcribe` and `gpt-4o-mini-transcribe`, the only supported format is `json`. For `gpt-4o-transcribe-diarize`, the supported formats are `json`, `text`, and `diarized_json`, with `diarized_json` required to receive speaker annotations. */
public enum AudioResponseFormat implements Json.JsonEnum {
  JSON("json"),
  TEXT("text"),
  SRT("srt"),
  VERBOSE_JSON("verbose_json"),
  VTT("vtt"),
  DIARIZED_JSON("diarized_json");

  private final String value;

  AudioResponseFormat(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static AudioResponseFormat fromValue(String value) {
    for (AudioResponseFormat item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown AudioResponseFormat value: " + value);
  }

  public static AudioResponseFormat fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

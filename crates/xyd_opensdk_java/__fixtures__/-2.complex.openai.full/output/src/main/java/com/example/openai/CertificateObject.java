// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * The object type.
 * 
 * - If creating, updating, or getting a specific certificate, the object type is `certificate`.
 * - If listing, activating, or deactivating certificates for the organization, the object type is `organization.certificate`.
 * - If listing, activating, or deactivating certificates for a project, the object type is `organization.project.certificate`.
 */
public enum CertificateObject implements Json.JsonEnum {
  CERTIFICATE("certificate"),
  ORGANIZATION_CERTIFICATE("organization.certificate"),
  ORGANIZATION_PROJECT_CERTIFICATE("organization.project.certificate");

  private final String value;

  CertificateObject(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static CertificateObject fromValue(String value) {
    for (CertificateObject item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown CertificateObject value: " + value);
  }

  public static CertificateObject fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

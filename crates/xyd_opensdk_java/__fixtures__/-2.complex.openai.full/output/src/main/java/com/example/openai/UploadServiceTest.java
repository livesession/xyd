// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * A dependency-free assertion test for UploadService — run with plain `java UploadServiceTest`.
 */
public final class UploadServiceTest {
  private static final String BASE_URL =
      System.getenv().getOrDefault("TEST_API_BASE_URL", "http://localhost:4010");

  public static void main(String[] args) {
    Client client = Client.builder().baseUrl(BASE_URL).apiKey("My API Key").build();
    testUploadCancel(client);
    testUploadCancelPathParams(client);
    testUploadCompleteWithOptionalParams(client);
    testUploadCompletePathParams(client);
    testUploadCreateWithOptionalParams(client);
    testUploadPartCreate(client);
    testUploadPartCreatePathParams(client);
    System.out.println("UploadServiceTest OK");
  }

  private static void testUploadCancel(Client client) {
    try {
      client.uploads().cancel("upload_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testUploadCancelPathParams(Client client) {
    try {
      client.uploads().cancel("");
      throw new AssertionError("expected a missing required path param error for cancel");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testUploadCompleteWithOptionalParams(Client client) {
    try {
      client.uploads().complete("upload_id", UploadCompleteParams.builder()
          .partIds(java.util.List.of("x"))
          .md5("x")
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testUploadCompletePathParams(Client client) {
    try {
      client.uploads().complete("", UploadCompleteParams.builder()
          .partIds(java.util.List.of("x"))
          .build());
      throw new AssertionError("expected a missing required path param error for complete");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testUploadCreateWithOptionalParams(Client client) {
    try {
      client.uploads().create(UploadCreateParams.builder()
          .filename("x")
          .purpose(CreateUploadRequestPurpose.ASSISTANTS)
          .bytes(0L)
          .mimeType("x")
          .expiresAfter(new FileExpirationAfter())
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testUploadPartCreate(Client client) {
    try {
      client.uploads().parts().create("upload_id", UploadPartCreateParams.builder()
          .data("Example data".getBytes(java.nio.charset.StandardCharsets.UTF_8))
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testUploadPartCreatePathParams(Client client) {
    try {
      client.uploads().parts().create("", UploadPartCreateParams.builder()
          .data("Example data".getBytes(java.nio.charset.StandardCharsets.UTF_8))
          .build());
      throw new AssertionError("expected a missing required path param error for create");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void requireContains(String actual, String needle) {
    if (actual == null || !actual.contains(needle)) {
      throw new AssertionError("missing substring [" + needle + "] in: " + actual);
    }
  }
}

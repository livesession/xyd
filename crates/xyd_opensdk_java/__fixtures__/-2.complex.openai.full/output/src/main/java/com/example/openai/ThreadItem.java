// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.Map;

/** Decode helper for the ThreadItem union, selecting the concrete variant by its "type" discriminator. */
public final class ThreadItem {
  private ThreadItem() {
  }

  /**
   * Decode JSON into the concrete ThreadItem variant selected by the "type"
   * discriminator. An unknown or absent discriminator keeps the raw parsed value.
   */
  public static Object fromJson(Object json) {
    if (!(json instanceof Map)) {
      return json;
    }
    Object discriminator = ((Map<?, ?>) json).get("type");
    if ("chatkit.assistant_message".equals(discriminator)) {
      return AssistantMessageItem.fromJson(json);
    }
    if ("chatkit.client_tool_call".equals(discriminator)) {
      return ClientToolCallItem.fromJson(json);
    }
    if ("chatkit.task".equals(discriminator)) {
      return TaskItem.fromJson(json);
    }
    if ("chatkit.task_group".equals(discriminator)) {
      return TaskGroupItem.fromJson(json);
    }
    if ("chatkit.user_message".equals(discriminator)) {
      return UserMessageItem.fromJson(json);
    }
    if ("chatkit.widget".equals(discriminator)) {
      return WidgetMessageItem.fromJson(json);
    }
    return json;
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ImageVariationService {
  private final Transport transport;

  ImageVariationService(Transport transport) {
    this.transport = transport;
  }

  /** Creates a variation of a given image. This endpoint only supports `dall-e-2`. */
  public ImagesResponse create(ImageVariationCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/images/variations");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("image", params.image());
    params.model().ifPresent(value -> body.put("model", value));
    params.n().ifPresent(value -> body.put("n", value));
    params.responseFormat().ifPresent(value -> body.put("response_format", value));
    params.size().ifPresent(value -> body.put("size", value));
    params.user().ifPresent(value -> body.put("user", value));
    request.body(body);
    request.encoding("multipart");
    return ImagesResponse.fromJson(transport.execute(request));
  }
}

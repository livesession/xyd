// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ResponseService {
  private final Transport transport;
  private final ResponseCompactService compact;
  private final ResponseInputItemService inputItems;
  private final ResponseInputTokenService inputTokens;

  ResponseService(Transport transport) {
    this.transport = transport;
    this.compact = new ResponseCompactService(transport);
    this.inputItems = new ResponseInputItemService(transport);
    this.inputTokens = new ResponseInputTokenService(transport);
  }

  public ResponseCompactService compact() {
    return compact;
  }

  public ResponseInputItemService inputItems() {
    return inputItems;
  }

  public ResponseInputTokenService inputTokens() {
    return inputTokens;
  }

  /**
   * Cancels a model response with the given ID. Only responses created with
   * the `background` parameter set to `true` can be cancelled.
   * [Learn more](/docs/guides/background).
   */
  public Response cancel(String responseId) {
    if (responseId == null || responseId.isEmpty()) {
      throw new IllegalArgumentException("missing required response_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/responses/" + Transport.encodePath(responseId) + "/cancel");
    return Response.fromJson(transport.execute(request));
  }

  /**
   * Creates a model response. Provide [text](/docs/guides/text) or
   * [image](/docs/guides/images) inputs to generate [text](/docs/guides/text)
   * or [JSON](/docs/guides/structured-outputs) outputs. Have the model call
   * your own [custom code](/docs/guides/function-calling) or use built-in
   * [tools](/docs/guides/tools) like [web search](/docs/guides/tools-web-search)
   * or [file search](/docs/guides/tools-file-search) to use your own data
   * as input for the model's response.
   */
  public Response create(ResponseCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/responses");
    Map<String, Object> body = new LinkedHashMap<>();
    params.metadata().ifPresent(value -> body.put("metadata", value));
    params.topLogprobs().ifPresent(value -> body.put("top_logprobs", value));
    params.temperature().ifPresent(value -> body.put("temperature", value));
    params.topP().ifPresent(value -> body.put("top_p", value));
    params.user().ifPresent(value -> body.put("user", value));
    params.safetyIdentifier().ifPresent(value -> body.put("safety_identifier", value));
    params.promptCacheKey().ifPresent(value -> body.put("prompt_cache_key", value));
    params.serviceTier().ifPresent(value -> body.put("service_tier", value));
    params.promptCacheRetention().ifPresent(value -> body.put("prompt_cache_retention", value));
    params.previousResponseId().ifPresent(value -> body.put("previous_response_id", value));
    params.model().ifPresent(value -> body.put("model", value));
    params.reasoning().ifPresent(value -> body.put("reasoning", value));
    params.background().ifPresent(value -> body.put("background", value));
    params.maxToolCalls().ifPresent(value -> body.put("max_tool_calls", value));
    params.text().ifPresent(value -> body.put("text", value));
    params.tools().ifPresent(value -> body.put("tools", value));
    params.toolChoice().ifPresent(value -> body.put("tool_choice", value));
    params.prompt().ifPresent(value -> body.put("prompt", value));
    params.truncation().ifPresent(value -> body.put("truncation", value));
    params.input().ifPresent(value -> body.put("input", value));
    params.include().ifPresent(value -> body.put("include", value));
    params.parallelToolCalls().ifPresent(value -> body.put("parallel_tool_calls", value));
    params.store().ifPresent(value -> body.put("store", value));
    params.instructions().ifPresent(value -> body.put("instructions", value));
    params.stream().ifPresent(value -> body.put("stream", value));
    params.streamOptions().ifPresent(value -> body.put("stream_options", value));
    params.conversation().ifPresent(value -> body.put("conversation", value));
    params.contextManagement().ifPresent(value -> body.put("context_management", value));
    params.maxOutputTokens().ifPresent(value -> body.put("max_output_tokens", value));
    request.body(body);
    return Response.fromJson(transport.execute(request));
  }

  /** Deletes a model response with the given ID. */
  public void delete(String responseId) {
    if (responseId == null || responseId.isEmpty()) {
      throw new IllegalArgumentException("missing required response_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/responses/" + Transport.encodePath(responseId));
    transport.execute(request);
  }

  /** Retrieves a model response with the given ID. */
  public Response retrieve(String responseId, ResponseRetrieveParams params) {
    if (responseId == null || responseId.isEmpty()) {
      throw new IllegalArgumentException("missing required response_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/responses/" + Transport.encodePath(responseId));
    params.include().ifPresent(values -> {
      for (Object value : values) {
        request.query("include", String.valueOf(value));
      }
    });
    params.stream().ifPresent(value -> request.query("stream", String.valueOf(value)));
    params.startingAfter().ifPresent(value -> request.query("starting_after", String.valueOf(value)));
    params.includeObfuscation().ifPresent(value -> request.query("include_obfuscation", String.valueOf(value)));
    return Response.fromJson(transport.execute(request));
  }
}

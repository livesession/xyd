// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ContainerFileService {
  private final Transport transport;

  ContainerFileService(Transport transport) {
    this.transport = transport;
  }

  /** Retrieves a container file content. */
  public void content(String containerId, String fileId) {
    if (containerId == null || containerId.isEmpty()) {
      throw new IllegalArgumentException("missing required container_id parameter");
    }
    if (fileId == null || fileId.isEmpty()) {
      throw new IllegalArgumentException("missing required file_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/containers/" + Transport.encodePath(containerId) + "/files/" + Transport.encodePath(fileId) + "/content");
    transport.execute(request);
  }

  /** Creates a container file. */
  public ContainerFileResource create(String containerId, ContainerFileCreateParams params) {
    if (containerId == null || containerId.isEmpty()) {
      throw new IllegalArgumentException("missing required container_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/containers/" + Transport.encodePath(containerId) + "/files");
    Map<String, Object> body = new LinkedHashMap<>();
    params.fileId().ifPresent(value -> body.put("file_id", value));
    params.file().ifPresent(value -> body.put("file", value));
    request.body(body);
    request.encoding("multipart");
    return ContainerFileResource.fromJson(transport.execute(request));
  }

  /** Delete a container file. */
  public void delete(String containerId, String fileId) {
    if (containerId == null || containerId.isEmpty()) {
      throw new IllegalArgumentException("missing required container_id parameter");
    }
    if (fileId == null || fileId.isEmpty()) {
      throw new IllegalArgumentException("missing required file_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/containers/" + Transport.encodePath(containerId) + "/files/" + Transport.encodePath(fileId));
    transport.execute(request);
  }

  /** Lists container files. */
  public CursorPage<ContainerFileResource> list(String containerId, ContainerFileListParams params) {
    if (containerId == null || containerId.isEmpty()) {
      throw new IllegalArgumentException("missing required container_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/containers/" + Transport.encodePath(containerId) + "/files");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> ContainerFileResource.fromJson(__e));
  }

  /** Retrieves a container file. */
  public ContainerFileResource retrieve(String containerId, String fileId) {
    if (containerId == null || containerId.isEmpty()) {
      throw new IllegalArgumentException("missing required container_id parameter");
    }
    if (fileId == null || fileId.isEmpty()) {
      throw new IllegalArgumentException("missing required file_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/containers/" + Transport.encodePath(containerId) + "/files/" + Transport.encodePath(fileId));
    return ContainerFileResource.fromJson(transport.execute(request));
  }
}

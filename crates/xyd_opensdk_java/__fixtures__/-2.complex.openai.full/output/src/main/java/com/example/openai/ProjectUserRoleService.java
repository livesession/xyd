// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ProjectUserRoleService {
  private final Transport transport;

  ProjectUserRoleService(Transport transport) {
    this.transport = transport;
  }

  /** Assigns a project role to a user within a project. */
  public UserRoleAssignment create(String projectId, String userId, ProjectUserRoleCreateParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/projects/" + Transport.encodePath(projectId) + "/users/" + Transport.encodePath(userId) + "/roles");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("role_id", params.roleId());
    request.body(body);
    return UserRoleAssignment.fromJson(transport.execute(request));
  }

  /** Unassigns a project role from a user within a project. */
  public DeletedRoleAssignmentResource delete(String projectId, String userId, String roleId) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    if (roleId == null || roleId.isEmpty()) {
      throw new IllegalArgumentException("missing required role_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/projects/" + Transport.encodePath(projectId) + "/users/" + Transport.encodePath(userId) + "/roles/" + Transport.encodePath(roleId));
    return DeletedRoleAssignmentResource.fromJson(transport.execute(request));
  }

  /** Lists the project roles assigned to a user within a project. */
  public CursorPage<AssignedRoleDetails> list(String projectId, String userId, ProjectUserRoleListParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/projects/" + Transport.encodePath(projectId) + "/users/" + Transport.encodePath(userId) + "/roles");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> AssignedRoleDetails.fromJson(__e));
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class BetaChatKitThreadService {
  private final Transport transport;
  private final BetaChatKitThreadItemService items;

  BetaChatKitThreadService(Transport transport) {
    this.transport = transport;
    this.items = new BetaChatKitThreadItemService(transport);
  }

  public BetaChatKitThreadItemService items() {
    return items;
  }

  /** Delete a ChatKit thread along with its items and stored attachments. */
  public DeletedThreadResource delete(String threadId) {
    if (threadId == null || threadId.isEmpty()) {
      throw new IllegalArgumentException("missing required thread_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/chatkit/threads/" + Transport.encodePath(threadId));
    return DeletedThreadResource.fromJson(transport.execute(request));
  }

  /** List ChatKit threads with optional pagination and user filters. */
  public CursorPage<ThreadResource> list(BetaChatKitThreadListParams params) {
    Transport.Request request = new Transport.Request("GET", "/chatkit/threads");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.before().ifPresent(value -> request.query("before", String.valueOf(value)));
    params.user().ifPresent(value -> request.query("user", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> ThreadResource.fromJson(__e));
  }

  /** Retrieve a ChatKit thread by its identifier. */
  public ThreadResource retrieve(String threadId) {
    if (threadId == null || threadId.isEmpty()) {
      throw new IllegalArgumentException("missing required thread_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/chatkit/threads/" + Transport.encodePath(threadId));
    return ThreadResource.fromJson(transport.execute(request));
  }
}

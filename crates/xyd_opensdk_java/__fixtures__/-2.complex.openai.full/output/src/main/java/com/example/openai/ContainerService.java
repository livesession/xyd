// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ContainerService {
  private final Transport transport;
  private final ContainerFileService files;

  ContainerService(Transport transport) {
    this.transport = transport;
    this.files = new ContainerFileService(transport);
  }

  public ContainerFileService files() {
    return files;
  }

  /** Creates a container. */
  public ContainerResource create(ContainerCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/containers");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("name", params.name());
    params.fileIds().ifPresent(value -> body.put("file_ids", value));
    params.expiresAfter().ifPresent(value -> body.put("expires_after", value));
    params.skills().ifPresent(value -> body.put("skills", value));
    params.memoryLimit().ifPresent(value -> body.put("memory_limit", value));
    params.networkPolicy().ifPresent(value -> body.put("network_policy", value));
    request.body(body);
    return ContainerResource.fromJson(transport.execute(request));
  }

  /** Delete a container. */
  public void delete(String containerId) {
    if (containerId == null || containerId.isEmpty()) {
      throw new IllegalArgumentException("missing required container_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/containers/" + Transport.encodePath(containerId));
    transport.execute(request);
  }

  /** Lists containers. */
  public CursorPage<ContainerResource> list(ContainerListParams params) {
    Transport.Request request = new Transport.Request("GET", "/containers");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.name().ifPresent(value -> request.query("name", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> ContainerResource.fromJson(__e));
  }

  /** Retrieves a container. */
  public ContainerResource retrieve(String containerId) {
    if (containerId == null || containerId.isEmpty()) {
      throw new IllegalArgumentException("missing required container_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/containers/" + Transport.encodePath(containerId));
    return ContainerResource.fromJson(transport.execute(request));
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * The truncation strategy to use for the model response.
 * - `auto`: If the input to this Response exceeds
 * the model's context window size, the model will truncate the
 * response to fit the context window by dropping items from the beginning of the conversation.
 * - `disabled` (default): If the input size will exceed the context window
 * size for a model, the request will fail with a 400 error.
 */
public enum CreateResponseTruncation implements Json.JsonEnum {
  AUTO("auto"),
  DISABLED("disabled");

  private final String value;

  CreateResponseTruncation(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static CreateResponseTruncation fromValue(String value) {
    for (CreateResponseTruncation item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown CreateResponseTruncation value: " + value);
  }

  public static CreateResponseTruncation fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

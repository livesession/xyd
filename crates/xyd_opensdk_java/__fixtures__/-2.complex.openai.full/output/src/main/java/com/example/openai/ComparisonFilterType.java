// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * Specifies the comparison operator: `eq`, `ne`, `gt`, `gte`, `lt`, `lte`, `in`, `nin`.
 * - `eq`: equals
 * - `ne`: not equal
 * - `gt`: greater than
 * - `gte`: greater than or equal
 * - `lt`: less than
 * - `lte`: less than or equal
 * - `in`: in
 * - `nin`: not in
 */
public enum ComparisonFilterType implements Json.JsonEnum {
  EQ("eq"),
  NE("ne"),
  GT("gt"),
  GTE("gte"),
  LT("lt"),
  LTE("lte"),
  IN("in"),
  NIN("nin");

  private final String value;

  ComparisonFilterType(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static ComparisonFilterType fromValue(String value) {
    for (ComparisonFilterType item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown ComparisonFilterType value: " + value);
  }

  public static ComparisonFilterType fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

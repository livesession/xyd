// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * The quality of the image that will be generated.
 * 
 * - `auto` (default value) will automatically select the best quality for the given model.
 * - `high`, `medium` and `low` are supported for the GPT image models.
 * - `hd` and `standard` are supported for `dall-e-3`.
 * - `standard` is the only option for `dall-e-2`.
 */
public enum CreateImageRequestQuality implements Json.JsonEnum {
  STANDARD("standard"),
  HD("hd"),
  LOW("low"),
  MEDIUM("medium"),
  HIGH("high"),
  AUTO("auto");

  private final String value;

  CreateImageRequestQuality(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static CreateImageRequestQuality fromValue(String value) {
    for (CreateImageRequestQuality item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown CreateImageRequestQuality value: " + value);
  }

  public static CreateImageRequestQuality fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

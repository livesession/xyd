// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.Optional;

/**
 * Parameters for create: Upload a file that can be used across various endpoints. Individual files
 * can be up to 512 MB, and each project can store up to 2.5 TB of files in
 * total. There is no organization-wide storage limit. Uploads to this
 * endpoint are rate-limited to 1,000 requests per minute per authenticated
 * user.
 * 
 * - The Assistants API supports files up to 2 million tokens and of specific
 * file types. See the [Assistants Tools guide](/docs/assistants/tools) for
 * details.
 * - The Fine-tuning API only supports `.jsonl` files. The input also has
 * certain required formats for fine-tuning
 * [chat](/docs/api-reference/fine-tuning/chat-input) or
 * [completions](/docs/api-reference/fine-tuning/completions-input) models.
 * - The Batch API only supports `.jsonl` files up to 200 MB in size. The input
 * also has a specific required
 * [format](/docs/api-reference/batch/request-input).
 * - For Retrieval or `file_search` ingestion, upload files here first. If
 * you need to attach multiple uploaded files to the same vector store, use
 * [`/vector_stores/{vector_store_id}/file_batches`](/docs/api-reference/vector-stores-file-batches/createBatch)
 * instead of attaching them one by one. Vector store attachment has separate
 * limits from file upload, including 2,000 attached files per minute per
 * organization.
 * 
 * Please [contact us](https://help.openai.com/) if you need to increase these
 * storage limits.
 */
public final class FileCreateParams {
  private final byte[] file;
  private final CreateFileRequestPurpose purpose;
  private final FileExpirationAfter expiresAfter;

  private FileCreateParams(Builder builder) {
    this.file = builder.file;
    this.purpose = builder.purpose;
    this.expiresAfter = builder.expiresAfter;
  }

  public static Builder builder() {
    return new Builder();
  }

  public byte[] file() {
    return file;
  }

  public CreateFileRequestPurpose purpose() {
    return purpose;
  }

  public Optional<FileExpirationAfter> expiresAfter() {
    return Optional.ofNullable(expiresAfter);
  }

  public static final class Builder {
    private byte[] file;
    private CreateFileRequestPurpose purpose;
    private FileExpirationAfter expiresAfter;

    public Builder file(byte[] file) {
      this.file = file;
      return this;
    }

    public Builder purpose(CreateFileRequestPurpose purpose) {
      this.purpose = purpose;
      return this;
    }

    public Builder expiresAfter(FileExpirationAfter expiresAfter) {
      this.expiresAfter = expiresAfter;
      return this;
    }

    public FileCreateParams build() {
      if (file == null) {
        throw new IllegalStateException("file is required");
      }
      if (purpose == null) {
        throw new IllegalStateException("purpose is required");
      }
      return new FileCreateParams(this);
    }
  }
}

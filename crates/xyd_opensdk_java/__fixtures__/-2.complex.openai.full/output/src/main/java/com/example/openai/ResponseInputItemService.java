// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class ResponseInputItemService {
  private final Transport transport;

  ResponseInputItemService(Transport transport) {
    this.transport = transport;
  }

  /** Returns a list of input items for a given response. */
  public CursorPage<Object> list(String responseId, ResponseInputItemListParams params) {
    if (responseId == null || responseId.isEmpty()) {
      throw new IllegalArgumentException("missing required response_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/responses/" + Transport.encodePath(responseId) + "/input_items");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.include().ifPresent(values -> {
      for (Object value : values) {
        request.query("include", String.valueOf(value));
      }
    });
    return CursorPage.fromJson(transport.execute(request), __e -> ItemResource.fromJson(__e));
  }
}

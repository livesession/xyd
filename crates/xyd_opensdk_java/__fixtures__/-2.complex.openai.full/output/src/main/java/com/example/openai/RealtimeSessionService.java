// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class RealtimeSessionService {
  private final Transport transport;

  RealtimeSessionService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Create an ephemeral API token for use in client-side applications with the
   * Realtime API. Can be configured with the same session parameters as the
   * `session.update` client event.
   * 
   * It responds with a session object, plus a `client_secret` key which contains
   * a usable ephemeral API token that can be used to authenticate browser clients
   * for the Realtime API.
   * 
   * Returns the created Realtime session object, plus an ephemeral key.
   */
  public RealtimeSessionCreateResponse create(RealtimeSessionCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/realtime/sessions");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("client_secret", params.clientSecret());
    params.modalities().ifPresent(value -> body.put("modalities", value));
    params.instructions().ifPresent(value -> body.put("instructions", value));
    params.voice().ifPresent(value -> body.put("voice", value));
    params.inputAudioFormat().ifPresent(value -> body.put("input_audio_format", value));
    params.outputAudioFormat().ifPresent(value -> body.put("output_audio_format", value));
    params.inputAudioTranscription().ifPresent(value -> body.put("input_audio_transcription", value));
    params.speed().ifPresent(value -> body.put("speed", value));
    params.tracing().ifPresent(value -> body.put("tracing", value));
    params.turnDetection().ifPresent(value -> body.put("turn_detection", value));
    params.tools().ifPresent(value -> body.put("tools", value));
    params.toolChoice().ifPresent(value -> body.put("tool_choice", value));
    params.temperature().ifPresent(value -> body.put("temperature", value));
    params.maxResponseOutputTokens().ifPresent(value -> body.put("max_response_output_tokens", value));
    params.truncation().ifPresent(value -> body.put("truncation", value));
    params.prompt().ifPresent(value -> body.put("prompt", value));
    request.body(body);
    return RealtimeSessionCreateResponse.fromJson(transport.execute(request));
  }
}

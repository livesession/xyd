// Code generated by opensdk. DO NOT EDIT.

package com.example.uniondepot;

import java.util.Map;

/** A geometric shape, discriminated by its type property. */
public final class Shape {
  private Shape() {
  }

  /**
   * Decode JSON into the concrete Shape variant selected by the "type"
   * discriminator. An unknown or absent discriminator keeps the raw parsed value.
   */
  public static Object fromJson(Object json) {
    if (!(json instanceof Map)) {
      return json;
    }
    Object discriminator = ((Map<?, ?>) json).get("type");
    if ("circle".equals(discriminator)) {
      return Circle.fromJson(json);
    }
    if ("square".equals(discriminator)) {
      return Square.fromJson(json);
    }
    return json;
  }
}

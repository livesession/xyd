# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2018"
rust-version = "1.56"
name = "mdxjs"
version = "1.0.4"
authors = ["Titus Wormer <tituswormer@gmail.com>"]
build = false
include = [
    "license",
    "src/",
]
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Compile MDX to JavaScript in Rust."
homepage = "https://github.com/wooorm/mdxjs-rs"
readme = "README.md"
keywords = [
    "compile",
    "markdown",
    "mdx",
]
categories = [
    "compilers",
    "text-processing",
]
license = "MIT"
repository = "https://github.com/wooorm/mdxjs-rs"

[features]
serializable = ["serde"]

[lib]
name = "mdxjs"
path = "src/lib.rs"

# xyd C-S2: use the vendored forked markdown directly (parse-time directive
# constructs) instead of the crates.io release. A sibling path dep — no
# `[patch.crates-io]` needed, so nothing outside crates/xyd_mdx/ is touched and
# the napi crate's separate build (packages/xyd-native, its own lockfile) picks
# up the fork transitively through xyd_mdx with no manifest change there.
[dependencies.markdown]
version = "1"
path = "../markdown-fork"

[dependencies.rustc-hash]
version = "2"

[dependencies.serde]
version = "1"
optional = true

[dependencies.swc_core]
version = "27"
features = [
    "common",
    "ecma_ast",
    "ecma_codegen",
    "ecma_parser",
    "ecma_visit",
]

[dev-dependencies.pretty_assertions]
version = "1"

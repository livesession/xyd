---
title: Code Blocks
---

# Code Blocks

Inline code: `simple span`.

Inline code that contains a backtick: ``a ` b``.

Fenced code with a language:

```python
def hello(name):
    return "hello " + name
```

Fenced code without a language:

```
plain text block
no highlighting at all
```

Fenced code that itself contains a triple backtick fence:

````markdown
```js
const nested = "fence";
```
````

Fenced code with blank lines inside:

```go
package main

func main() {
}
```

Indented code block below this line:

indented line one
indented line two

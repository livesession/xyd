---
title: Callouts With Code
---

# Callouts With Code

:::callout{kind="warning"}
Never run this command in production:```bash
rm -rf /var/data
```
:::

:::callout
Run `npm install` first, then build with:```js
const build = require("./build");
build();
```
:::

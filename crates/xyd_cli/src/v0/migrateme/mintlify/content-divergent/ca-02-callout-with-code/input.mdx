---
title: Callouts With Code
---

<Warning>
Never run this command in production:

```bash
rm -rf /var/data
```
</Warning>

<Info>
Run `npm install` first, then build with:

```js
const build = require("./build");
build();
```
</Info>

// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::opencli::runtime::{CliOverrides, Context};

pub fn command() -> Command {
    Command::new("completion")
        .about("Generate shell completions or the CLI OpenCLI document. Run `xyd completion <zsh|fish>` to print a script, `xyd completion install [shell]` to install it, or `xyd completion opencli` to print the OpenCLI document.")
        .arg(
            Arg::new("shell")
                .help("Shell to generate completions for")
                .value_parser(["zsh", "fish", "install", "opencli"])
                .required(true),
        )
}

pub async fn run<O: CliOverrides>(
    _ctx: &Context,
    _o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

{
  "chain": [
    "admin",
    "organization",
    "certificates"
  ],
  "action": "list",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.certificates.list(limit=20, after=\"after\", order=\"asc\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.certificates.list(limit=..., after=..., order=...) -> CursorPage[OrganizationCertificate]",
    "request": {
      "fields": [
        {
          "name": "limit",
          "langType": "Optional[int]",
          "required": false,
          "description": "A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.\n"
        },
        {
          "name": "after",
          "langType": "Optional[str]",
          "required": false,
          "description": "A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n"
        },
        {
          "name": "order",
          "langType": "Optional[AdminOrganizationCertificatesListOrder]",
          "required": false,
          "description": "Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.\n",
          "refTypeName": "AdminOrganizationCertificatesListOrder"
        }
      ]
    },
    "response": {
      "typeName": "CursorPage[OrganizationCertificate]",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is always `organization.certificate`."
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "name",
          "langType": "Optional[str]",
          "required": true,
          "description": "The name of the certificate."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the certificate was uploaded."
        },
        {
          "name": "certificate_details",
          "langType": "OrganizationCertificateCertificateDetails",
          "required": true,
          "refTypeName": "OrganizationCertificateCertificateDetails"
        },
        {
          "name": "active",
          "langType": "bool",
          "required": true,
          "description": "Whether the certificate is currently active at the organization level."
        }
      ]
    }
  }
}

{
  "chain": [
    "chat",
    "completions",
    "messages"
  ],
  "action": "list",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.chat.completions.messages.list(\"completion_id\", after=\"after\", limit=20, order=\"asc\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.chat.completions.messages.list(completion_id, after=..., limit=..., order=...) -> CursorPage[ChatCompletionMessageListDataItem]",
    "request": {
      "fields": [
        {
          "name": "after",
          "langType": "Optional[str]",
          "required": false,
          "description": "Identifier for the last message from the previous pagination request."
        },
        {
          "name": "limit",
          "langType": "Optional[int]",
          "required": false,
          "description": "Number of messages to retrieve."
        },
        {
          "name": "order",
          "langType": "Optional[ChatCompletionsMessagesListOrder]",
          "required": false,
          "description": "Sort order for messages by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `asc`.",
          "refTypeName": "ChatCompletionsMessagesListOrder"
        }
      ]
    },
    "response": {
      "typeName": "CursorPage[ChatCompletionMessageListDataItem]",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "content",
          "langType": "Optional[str]",
          "required": true
        },
        {
          "name": "refusal",
          "langType": "Optional[str]",
          "required": true
        },
        {
          "name": "tool_calls",
          "langType": "ChatCompletionMessageToolCalls",
          "required": false,
          "refTypeName": "ChatCompletionMessageToolCalls"
        },
        {
          "name": "annotations",
          "langType": "list[ChatCompletionMessageListDataItemAnnotationsItem]",
          "required": false,
          "description": "Annotations for the message, when applicable, as when using the\n[web search tool](/docs/guides/tools-web-search?api-mode=chat).\n"
        },
        {
          "name": "role",
          "langType": "str",
          "required": true,
          "description": "The role of the author of this message."
        },
        {
          "name": "function_call",
          "langType": "ChatCompletionMessageListDataItemFunctionCall",
          "required": false,
          "description": "Deprecated and replaced by `tool_calls`. The name and arguments of a function that should be called, as generated by the model.",
          "deprecated": true,
          "refTypeName": "ChatCompletionMessageListDataItemFunctionCall"
        },
        {
          "name": "audio",
          "langType": "Optional[ChatCompletionMessageListDataItemAudio]",
          "required": false,
          "refTypeName": "ChatCompletionMessageListDataItemAudio"
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The identifier of the chat message."
        },
        {
          "name": "content_parts",
          "langType": "Optional[list[ChatCompletionMessageListDataItemContentPartsItem]]",
          "required": false
        }
      ]
    }
  }
}

{
  "chain": [
    "videos"
  ],
  "action": "delete",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.videos.delete(\"video_123\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.videos.delete(video_id) -> DeletedVideoResource",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "DeletedVideoResource",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type that signals the deletion response."
        },
        {
          "name": "deleted",
          "langType": "bool",
          "required": true,
          "description": "Indicates that the video resource was deleted."
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "Identifier of the deleted video."
        }
      ]
    }
  }
}

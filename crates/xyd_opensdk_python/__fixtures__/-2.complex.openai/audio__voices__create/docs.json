{
  "chain": [
    "audio",
    "voices"
  ],
  "action": "create",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.audio.voices.create(name=\"string\", audio_sample=b\"Example data\", consent=\"string\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.audio.voices.create(name, audio_sample, consent) -> VoiceResource",
    "request": {
      "fields": [
        {
          "name": "name",
          "langType": "str",
          "required": true,
          "description": "The name of the new voice."
        },
        {
          "name": "audio_sample",
          "langType": "Union[bytes, BinaryIO]",
          "required": true,
          "description": "The sample audio recording file. Maximum size is 10 MiB.\n\nSupported MIME types:\n`audio/mpeg`, `audio/wav`, `audio/x-wav`, `audio/ogg`, `audio/aac`, `audio/flac`, `audio/webm`, `audio/mp4`.\n"
        },
        {
          "name": "consent",
          "langType": "str",
          "required": true,
          "description": "The consent recording ID (for example, `cons_1234`)."
        }
      ]
    },
    "response": {
      "typeName": "VoiceResource",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is always `audio.voice`."
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The voice identifier, which can be referenced in API endpoints."
        },
        {
          "name": "name",
          "langType": "str",
          "required": true,
          "description": "The name of the voice."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the voice was created."
        }
      ]
    }
  }
}

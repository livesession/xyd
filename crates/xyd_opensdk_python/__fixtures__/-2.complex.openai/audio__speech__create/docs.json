{
  "chain": [
    "audio",
    "speech"
  ],
  "action": "create",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.audio.speech.create(model=\"string\", input=\"string\", voice=\"string\", instructions=\"string\", response_format=\"mp3\", speed=1, stream_format=\"sse\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.audio.speech.create(model, input, instructions=..., voice, response_format=..., speed=..., stream_format=...) -> bytes",
    "request": {
      "fields": [
        {
          "name": "model",
          "langType": "CreateSpeechRequestModel",
          "required": true,
          "description": "One of the available [TTS models](/docs/models#tts): `tts-1`, `tts-1-hd`, `gpt-4o-mini-tts`, or `gpt-4o-mini-tts-2025-12-15`.\n",
          "refTypeName": "CreateSpeechRequestModel"
        },
        {
          "name": "input",
          "langType": "str",
          "required": true,
          "description": "The text to generate audio for. The maximum length is 4096 characters."
        },
        {
          "name": "instructions",
          "langType": "Optional[str]",
          "required": false,
          "description": "Control the voice of your generated audio with additional instructions. Does not work with `tts-1` or `tts-1-hd`."
        },
        {
          "name": "voice",
          "langType": "VoiceIdsOrCustomVoice",
          "required": true,
          "refTypeName": "VoiceIdsOrCustomVoice"
        },
        {
          "name": "response_format",
          "langType": "Optional[CreateSpeechRequestResponseFormat]",
          "required": false,
          "description": "The format to audio in. Supported formats are `mp3`, `opus`, `aac`, `flac`, `wav`, and `pcm`.",
          "refTypeName": "CreateSpeechRequestResponseFormat"
        },
        {
          "name": "speed",
          "langType": "Optional[float]",
          "required": false,
          "description": "The speed of the generated audio. Select a value from `0.25` to `4.0`. `1.0` is the default."
        },
        {
          "name": "stream_format",
          "langType": "Optional[CreateSpeechRequestStreamFormat]",
          "required": false,
          "description": "The format to stream the audio in. Supported formats are `sse` and `audio`. `sse` is not supported for `tts-1` or `tts-1-hd`.",
          "refTypeName": "CreateSpeechRequestStreamFormat"
        }
      ]
    },
    "response": {
      "langType": "bytes",
      "note": "binary download (application/octet-stream)"
    }
  }
}

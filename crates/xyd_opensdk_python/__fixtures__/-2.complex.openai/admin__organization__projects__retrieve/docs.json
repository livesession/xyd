{
  "chain": [
    "admin",
    "organization",
    "projects"
  ],
  "action": "retrieve",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.projects.retrieve(\"project_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.retrieve(project_id) -> Project",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "Project",
      "fields": [
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is always `organization.project`"
        },
        {
          "name": "name",
          "langType": "Optional[str]",
          "required": false,
          "description": "The name of the project. This appears in reporting."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the project was created."
        },
        {
          "name": "archived_at",
          "langType": "Optional[int]",
          "required": false
        },
        {
          "name": "status",
          "langType": "Optional[str]",
          "required": false,
          "description": "`active` or `archived`"
        },
        {
          "name": "external_key_id",
          "langType": "Optional[str]",
          "required": false,
          "description": "The external key associated with the project."
        }
      ]
    }
  }
}

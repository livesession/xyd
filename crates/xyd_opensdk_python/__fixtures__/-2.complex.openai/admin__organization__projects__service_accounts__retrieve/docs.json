{
  "chain": [
    "admin",
    "organization",
    "projects",
    "service-accounts"
  ],
  "action": "retrieve",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.projects.service_accounts.retrieve(\"project_id\", \"service_account_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.service_accounts.retrieve(project_id, service_account_id) -> ProjectServiceAccount",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "ProjectServiceAccount",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is always `organization.project.service_account`"
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "name",
          "langType": "str",
          "required": true,
          "description": "The name of the service account"
        },
        {
          "name": "role",
          "langType": "ProjectServiceAccountRole",
          "required": true,
          "description": "`owner` or `member`",
          "refTypeName": "ProjectServiceAccountRole"
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the service account was created"
        }
      ]
    }
  }
}

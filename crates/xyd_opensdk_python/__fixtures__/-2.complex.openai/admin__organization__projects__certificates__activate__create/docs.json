{
  "chain": [
    "admin",
    "organization",
    "projects",
    "certificates",
    "activate"
  ],
  "action": "create",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.projects.certificates.activate.create(\"project_id\", certificate_ids=[\"string\"])\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.certificates.activate.create(project_id, certificate_ids) -> OrganizationProjectCertificateActivationResponse",
    "request": {
      "fields": [
        {
          "name": "certificate_ids",
          "langType": "list[str]",
          "required": true
        }
      ]
    },
    "response": {
      "typeName": "OrganizationProjectCertificateActivationResponse",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The project certificate activation result type."
        },
        {
          "name": "data",
          "langType": "list[OrganizationProjectCertificate]",
          "required": true
        }
      ]
    }
  }
}

{
  "chain": [
    "vector-stores",
    "file-batches"
  ],
  "action": "list-files",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.vector_stores.file_batches.list_files(\"vector_store_id\", \"batch_id\", limit=20, order=\"asc\", after=\"after\", before=\"before\", filter=\"in_progress\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.vector_stores.file_batches.list_files(vector_store_id, batch_id, limit=..., order=..., after=..., before=..., filter=...) -> CursorPage[VectorStoreFileObject]",
    "request": {
      "fields": [
        {
          "name": "limit",
          "langType": "Optional[int]",
          "required": false,
          "description": "A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.\n"
        },
        {
          "name": "order",
          "langType": "Optional[VectorStoresFileBatchesListFilesOrder]",
          "required": false,
          "description": "Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.\n",
          "refTypeName": "VectorStoresFileBatchesListFilesOrder"
        },
        {
          "name": "after",
          "langType": "Optional[str]",
          "required": false,
          "description": "A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n"
        },
        {
          "name": "before",
          "langType": "Optional[str]",
          "required": false,
          "description": "A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.\n"
        },
        {
          "name": "filter",
          "langType": "Optional[VectorStoresFileBatchesListFilesFilter]",
          "required": false,
          "description": "Filter by file status. One of `in_progress`, `completed`, `failed`, `cancelled`.",
          "refTypeName": "VectorStoresFileBatchesListFilesFilter"
        }
      ]
    },
    "response": {
      "typeName": "CursorPage[VectorStoreFileObject]",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints."
        },
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is always `vector_store.file`."
        },
        {
          "name": "usage_bytes",
          "langType": "int",
          "required": true,
          "description": "The total vector store usage in bytes. Note that this may be different from the original file size."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the vector store file was created."
        },
        {
          "name": "vector_store_id",
          "langType": "str",
          "required": true,
          "description": "The ID of the [vector store](/docs/api-reference/vector-stores/object) that the [File](/docs/api-reference/files) is attached to."
        },
        {
          "name": "status",
          "langType": "VectorStoreFileObjectStatus",
          "required": true,
          "description": "The status of the vector store file, which can be either `in_progress`, `completed`, `cancelled`, or `failed`. The status `completed` indicates that the vector store file is ready for use.",
          "refTypeName": "VectorStoreFileObjectStatus"
        },
        {
          "name": "last_error",
          "langType": "Optional[VectorStoreFileObjectLastError]",
          "required": true,
          "refTypeName": "VectorStoreFileObjectLastError"
        },
        {
          "name": "chunking_strategy",
          "langType": "VectorStoreFileObjectChunkingStrategy",
          "required": false,
          "description": "The strategy used to chunk the file.",
          "refTypeName": "VectorStoreFileObjectChunkingStrategy"
        },
        {
          "name": "attributes",
          "langType": "VectorStoreFileAttributes",
          "required": false,
          "refTypeName": "VectorStoreFileAttributes"
        }
      ]
    }
  }
}

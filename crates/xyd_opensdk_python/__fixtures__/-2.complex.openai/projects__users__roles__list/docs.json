{
  "chain": [
    "projects",
    "users",
    "roles"
  ],
  "action": "list",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.projects.users.roles.list(\"project_id\", \"user_id\", limit=1, after=\"after\", order=\"asc\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.projects.users.roles.list(project_id, user_id, limit=..., after=..., order=...) -> CursorPage[AssignedRoleDetails]",
    "request": {
      "fields": [
        {
          "name": "limit",
          "langType": "Optional[int]",
          "required": false,
          "description": "A limit on the number of project role assignments to return."
        },
        {
          "name": "after",
          "langType": "Optional[str]",
          "required": false,
          "description": "Cursor for pagination. Provide the value from the previous response's `next` field to continue listing project roles."
        },
        {
          "name": "order",
          "langType": "Optional[ProjectsUsersRolesListOrder]",
          "required": false,
          "description": "Sort order for the returned project roles.",
          "refTypeName": "ProjectsUsersRolesListOrder"
        }
      ]
    },
    "response": {
      "typeName": "CursorPage[AssignedRoleDetails]",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "Identifier for the role."
        },
        {
          "name": "name",
          "langType": "str",
          "required": true,
          "description": "Name of the role."
        },
        {
          "name": "permissions",
          "langType": "list[str]",
          "required": true,
          "description": "Permissions associated with the role."
        },
        {
          "name": "resource_type",
          "langType": "str",
          "required": true,
          "description": "Resource type the role applies to."
        },
        {
          "name": "predefined_role",
          "langType": "bool",
          "required": true,
          "description": "Whether the role is predefined by OpenAI."
        },
        {
          "name": "description",
          "langType": "Optional[str]",
          "required": true,
          "description": "Description of the role."
        },
        {
          "name": "created_at",
          "langType": "Optional[int]",
          "required": true,
          "description": "When the role was created."
        },
        {
          "name": "updated_at",
          "langType": "Optional[int]",
          "required": true,
          "description": "When the role was last updated."
        },
        {
          "name": "created_by",
          "langType": "Optional[str]",
          "required": true,
          "description": "Identifier of the actor who created the role."
        },
        {
          "name": "created_by_user_obj",
          "langType": "Optional[dict[str, Any]]",
          "required": true,
          "description": "User details for the actor that created the role, when available."
        },
        {
          "name": "metadata",
          "langType": "Optional[dict[str, Any]]",
          "required": true,
          "description": "Arbitrary metadata stored on the role."
        }
      ]
    }
  }
}

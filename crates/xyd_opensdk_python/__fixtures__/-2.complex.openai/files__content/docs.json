{
  "chain": [
    "files"
  ],
  "action": "content",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.files.content(\"file_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.files.content(file_id) -> str",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "str",
      "langType": "str"
    }
  }
}

{
  "chain": [
    "admin",
    "organization",
    "certificates"
  ],
  "action": "delete",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.certificates.delete(\"certificate_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.certificates.delete(certificate_id) -> DeleteCertificateResponse",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "DeleteCertificateResponse",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, must be `certificate.deleted`."
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The ID of the certificate that was deleted."
        }
      ]
    }
  }
}

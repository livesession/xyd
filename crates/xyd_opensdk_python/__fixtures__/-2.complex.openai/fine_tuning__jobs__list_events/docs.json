{
  "chain": [
    "fine-tuning",
    "jobs"
  ],
  "action": "list-events",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.fine_tuning.jobs.list_events(\"ft-AF1WoRqd3aJAHsqc9NY7iL8F\", after=\"after\", limit=20)\nprint(result)\n",
  "typeReference": {
    "signature": "client.fine_tuning.jobs.list_events(fine_tuning_job_id, after=..., limit=...) -> CursorPage[FineTuningJobEvent]",
    "request": {
      "fields": [
        {
          "name": "after",
          "langType": "Optional[str]",
          "required": false,
          "description": "Identifier for the last event from the previous pagination request."
        },
        {
          "name": "limit",
          "langType": "Optional[int]",
          "required": false,
          "description": "Number of events to retrieve."
        }
      ]
    },
    "response": {
      "typeName": "CursorPage[FineTuningJobEvent]",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is always \"fine_tuning.job.event\"."
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The object identifier."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the fine-tuning job was created."
        },
        {
          "name": "level",
          "langType": "FineTuningJobEventLevel",
          "required": true,
          "description": "The log level of the event.",
          "refTypeName": "FineTuningJobEventLevel"
        },
        {
          "name": "message",
          "langType": "str",
          "required": true,
          "description": "The message of the event."
        },
        {
          "name": "type",
          "langType": "FineTuningJobEventType",
          "required": false,
          "description": "The type of event.",
          "refTypeName": "FineTuningJobEventType"
        },
        {
          "name": "data",
          "langType": "dict[str, Any]",
          "required": false,
          "description": "The data associated with the event."
        }
      ]
    }
  }
}

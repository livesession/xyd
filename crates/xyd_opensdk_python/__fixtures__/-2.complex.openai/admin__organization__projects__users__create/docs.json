{
  "chain": [
    "admin",
    "organization",
    "projects",
    "users"
  ],
  "action": "create",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.projects.users.create(\"project_id\", role=\"string\", user_id=\"string\", email=\"string\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.users.create(project_id, user_id=..., email=..., role) -> ProjectUser",
    "request": {
      "fields": [
        {
          "name": "user_id",
          "langType": "Optional[str]",
          "required": false,
          "description": "The ID of the user."
        },
        {
          "name": "email",
          "langType": "Optional[str]",
          "required": false,
          "description": "Email of the user to add."
        },
        {
          "name": "role",
          "langType": "str",
          "required": true,
          "description": "`owner` or `member`"
        }
      ]
    },
    "response": {
      "typeName": "ProjectUser",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is always `organization.project.user`"
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "name",
          "langType": "Optional[str]",
          "required": false,
          "description": "The name of the user"
        },
        {
          "name": "email",
          "langType": "Optional[str]",
          "required": false,
          "description": "The email address of the user"
        },
        {
          "name": "role",
          "langType": "str",
          "required": true,
          "description": "`owner` or `member`"
        },
        {
          "name": "added_at",
          "langType": "int",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the project was added."
        }
      ]
    }
  }
}

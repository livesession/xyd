{
  "chain": [
    "admin",
    "organization",
    "projects",
    "groups"
  ],
  "action": "delete",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.projects.groups.delete(\"project_id\", \"group_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.groups.delete(project_id, group_id) -> ProjectGroupDeletedResource",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "ProjectGroupDeletedResource",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "Always `project.group.deleted`."
        },
        {
          "name": "deleted",
          "langType": "bool",
          "required": true,
          "description": "Whether the group membership in the project was removed."
        }
      ]
    }
  }
}

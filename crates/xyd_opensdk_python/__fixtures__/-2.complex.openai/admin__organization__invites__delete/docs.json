{
  "chain": [
    "admin",
    "organization",
    "invites"
  ],
  "action": "delete",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.invites.delete(\"invite_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.invites.delete(invite_id) -> InviteDeleteResponse",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "InviteDeleteResponse",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is always `organization.invite.deleted`"
        },
        {
          "name": "id",
          "langType": "str",
          "required": true
        },
        {
          "name": "deleted",
          "langType": "bool",
          "required": true
        }
      ]
    }
  }
}

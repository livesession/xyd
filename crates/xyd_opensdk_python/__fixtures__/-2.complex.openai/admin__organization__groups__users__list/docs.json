{
  "chain": [
    "admin",
    "organization",
    "groups",
    "users"
  ],
  "action": "list",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.groups.users.list(\"group_id\", limit=100, after=\"after\", order=\"asc\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.groups.users.list(group_id, limit=..., after=..., order=...) -> CursorPage[GroupUser]",
    "request": {
      "fields": [
        {
          "name": "limit",
          "langType": "Optional[int]",
          "required": false,
          "description": "A limit on the number of users to be returned. Limit can range between 0 and 1000, and the default is 100.\n"
        },
        {
          "name": "after",
          "langType": "Optional[str]",
          "required": false,
          "description": "A cursor for use in pagination. Provide the ID of the last user from the previous list response to retrieve the next page.\n"
        },
        {
          "name": "order",
          "langType": "Optional[AdminOrganizationGroupsUsersListOrder]",
          "required": false,
          "description": "Specifies the sort order of users in the list.",
          "refTypeName": "AdminOrganizationGroupsUsersListOrder"
        }
      ]
    },
    "response": {
      "typeName": "CursorPage[GroupUser]",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "name",
          "langType": "str",
          "required": true,
          "description": "The name of the user."
        },
        {
          "name": "email",
          "langType": "Optional[str]",
          "required": true,
          "description": "The email address of the user."
        }
      ]
    }
  }
}

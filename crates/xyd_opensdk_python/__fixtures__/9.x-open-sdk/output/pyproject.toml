[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "extensions-demo"
version = "1.0.0"
description = "Exercises x-open-sdk-* naming extensions."
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["extensions_demo"]

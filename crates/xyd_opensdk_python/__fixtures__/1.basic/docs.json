{
  "chain": [
    "pets"
  ],
  "action": "list",
  "usage": "import os\n\nfrom petstore import Client\n\nclient = Client(\n    api_key=os.environ.get(\"PETSTORE_API_KEY\"),\n)\n\nresult = client.pets.list(after=\"after\", limit=1)\nprint(result)\n",
  "typeReference": {
    "signature": "client.pets.list(after=..., limit=...) -> CursorPage[Pet]",
    "request": {
      "fields": [
        {
          "name": "after",
          "langType": "Optional[str]",
          "required": false
        },
        {
          "name": "limit",
          "langType": "Optional[int]",
          "required": false
        }
      ]
    },
    "response": {
      "typeName": "CursorPage[Pet]",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "id",
          "langType": "str",
          "required": true
        },
        {
          "name": "name",
          "langType": "str",
          "required": true
        },
        {
          "name": "status",
          "langType": "PetStatus",
          "required": false,
          "refTypeName": "PetStatus"
        },
        {
          "name": "tags",
          "langType": "list[str]",
          "required": false
        }
      ]
    }
  }
}

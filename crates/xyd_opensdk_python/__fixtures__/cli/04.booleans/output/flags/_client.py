# Code generated by opensdk. DO NOT EDIT.

from __future__ import annotations

from typing import Optional

from ._transport import CommandResult, Transport, cli_text


class Client:
    """Drives the flags CLI (the `flags` binary)."""

    def __init__(
        self,
        bin_path: Optional[str] = None,
        env: Optional[dict[str, str]] = None,
        cwd: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> None:
        """Create a client that runs `flags` (override with the FLAGS_BIN env var or bin_path); timeout is in milliseconds."""
        self._transport = Transport(bin_path=bin_path, env=env, cwd=cwd, timeout=timeout)

    def run(self, *, verbose: Optional[bool] = None, q: Optional[bool] = None, count: Optional[int] = None, ratio: Optional[float] = None) -> CommandResult:
        """Run the thing (runs `flags run`)."""
        argv: list[str] = ["run"]
        if verbose:
            argv.append("--verbose")
        if q:
            argv.append("-q")
        if count is not None:
            argv.extend(["--count", cli_text(count)])
        if ratio is not None:
            argv.extend(["--ratio", cli_text(ratio)])
        return self._transport.run(argv)

# Code generated by opensdk. DO NOT EDIT.

from __future__ import annotations

from typing import Optional

from ._transport import CommandResult, Transport, cli_text


class ChatResource:
    def __init__(self, transport: Transport) -> None:
        self._transport = transport
        self.completions = ChatCompletionsResource(transport)


class ChatCompletionsResource:
    def __init__(self, transport: Transport) -> None:
        self._transport = transport

    def list(self) -> CommandResult:
        """List completions (runs `acme chat completions list`)."""
        argv: list[str] = ["chat", "completions", "list"]
        return self._transport.run(argv)

    def create(self, prompt: str, *, model: str, temperature: Optional[float] = None) -> CommandResult:
        """Create a completion (runs `acme chat completions create`)."""
        if not prompt:
            raise ValueError(f"Expected a non-empty value for `prompt` but received {prompt!r}")
        argv: list[str] = ["chat", "completions", "create"]
        argv.append(prompt)
        argv.extend(["--model", model])
        if temperature is not None:
            argv.extend(["--temperature", cli_text(temperature)])
        return self._transport.run(argv)

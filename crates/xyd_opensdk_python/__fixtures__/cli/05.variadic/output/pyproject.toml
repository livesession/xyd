[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "packer"
version = "1.0.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["packer"]

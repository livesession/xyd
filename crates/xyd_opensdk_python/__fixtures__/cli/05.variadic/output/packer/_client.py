# Code generated by opensdk. DO NOT EDIT.

from __future__ import annotations

from typing import Any, Optional

from ._transport import CommandResult, Transport, cli_json


class Client:
    """Drives the packer CLI (the `packer` binary)."""

    def __init__(
        self,
        bin_path: Optional[str] = None,
        env: Optional[dict[str, str]] = None,
        cwd: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> None:
        """Create a client that runs `packer` (override with the PACKER_BIN env var or bin_path); timeout is in milliseconds."""
        self._transport = Transport(bin_path=bin_path, env=env, cwd=cwd, timeout=timeout)

    def pack(self, dest: str, files: list[str], *, tag: Optional[list[str]] = None, meta: Optional[Any] = None) -> CommandResult:
        """Pack files (runs `packer pack`)."""
        if not dest:
            raise ValueError(f"Expected a non-empty value for `dest` but received {dest!r}")
        argv: list[str] = ["pack"]
        argv.append(dest)
        for value in files:
            argv.append(value)
        for value in tag or []:
            argv.extend(["--tag", value])
        if meta is not None:
            argv.extend(["--meta", cli_json(meta)])
        return self._transport.run(argv)

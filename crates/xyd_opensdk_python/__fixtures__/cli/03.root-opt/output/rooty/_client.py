# Code generated by opensdk. DO NOT EDIT.

from __future__ import annotations

from typing import Optional

from ._transport import CommandResult, Transport


class Client:
    """Drives the rooty CLI (the `rooty` binary)."""

    def __init__(
        self,
        bin_path: Optional[str] = None,
        env: Optional[dict[str, str]] = None,
        cwd: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> None:
        """Create a client that runs `rooty` (override with the ROOTY_BIN env var or bin_path); timeout is in milliseconds."""
        self._transport = Transport(bin_path=bin_path, env=env, cwd=cwd, timeout=timeout)

    def init(self) -> CommandResult:
        """Initialize a project (runs `rooty init`)."""
        argv: list[str] = ["init"]
        return self._transport.run(argv)

    def opt_help(self) -> CommandResult:
        """Print help (runs `rooty`)."""
        argv: list[str] = []
        argv.append("--help")
        return self._transport.run(argv)

    def opt_version(self) -> CommandResult:
        """Print version (runs `rooty`)."""
        argv: list[str] = []
        argv.append("--version")
        return self._transport.run(argv)

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rooty"
version = "0.2.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["rooty"]

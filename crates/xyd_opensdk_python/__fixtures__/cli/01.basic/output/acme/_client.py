# Code generated by opensdk. DO NOT EDIT.

from __future__ import annotations

from typing import Optional

from ._transport import CommandResult, Transport
from .resources import ProjectsResource


class Client:
    """Drives the acme CLI (the `acme` binary)."""

    def __init__(
        self,
        bin_path: Optional[str] = None,
        env: Optional[dict[str, str]] = None,
        cwd: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> None:
        """Create a client that runs `acme` (override with the ACME_BIN env var or bin_path); timeout is in milliseconds."""
        self._transport = Transport(bin_path=bin_path, env=env, cwd=cwd, timeout=timeout)
        self.projects = ProjectsResource(self._transport)

    def opt_version(self) -> CommandResult:
        """Print the CLI version and exit (runs `acme`)."""
        argv: list[str] = []
        argv.append("--version")
        return self._transport.run(argv)

    def status(self, *, format: Optional[str] = None) -> CommandResult:
        """Show status (runs `acme status`)."""
        argv: list[str] = ["status"]
        if format is not None:
            argv.extend(["--format", format])
        return self._transport.run(argv)

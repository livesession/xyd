# Code generated by opensdk. DO NOT EDIT.

from __future__ import annotations

from typing import Optional

from ._transport import CommandResult, Transport, cli_text


class Client:
    """Drives the acme CLI (the `acme` binary)."""

    def __init__(
        self,
        bin_path: Optional[str] = None,
        env: Optional[dict[str, str]] = None,
        cwd: Optional[str] = None,
        timeout: Optional[float] = None,
    ) -> None:
        """Create a client that runs `acme` (override with the ACME_BIN env var or bin_path); timeout is in milliseconds."""
        self._transport = Transport(bin_path=bin_path, env=env, cwd=cwd, timeout=timeout)

    def convert(self, target: ConvertTarget, *, level: Optional[ConvertLevel] = None) -> CommandResult:
        """Convert a document (runs `acme convert`)."""
        argv: list[str] = ["convert"]
        argv.append(cli_text(target))
        if level is not None:
            argv.extend(["--level", cli_text(level)])
        return self._transport.run(argv)

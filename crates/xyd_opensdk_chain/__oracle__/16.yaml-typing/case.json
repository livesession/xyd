{
  "description": "readRawDoc yaml typing: js-yaml default (core) schema scalar resolution, anchors/aliases, block scalars. Pins what the Rust yaml loader must reproduce.",
  "ops": [
    {
      "id": "types",
      "op": "readRawDoc",
      "location": "./types.yaml"
    }
  ]
}

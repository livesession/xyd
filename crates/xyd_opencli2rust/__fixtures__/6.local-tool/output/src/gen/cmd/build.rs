// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{CliOverrides, Context};

pub fn command() -> Command {
    Command::new("build")
        .about("Build your docs")
        .arg(
            Arg::new("strict")
                .long("strict")
                .num_args(0..=1)
                .require_equals(true)
                .default_missing_value("true")
                .value_parser(clap::value_parser!(bool))
                .help("Fail the build on warnings"),
        )
        .arg(
            Arg::new("mode")
                .long("mode")
                .value_parser(["static", "ssr"])
                .help("Build mode"),
        )
        .arg(
            Arg::new("define")
                .long("define")
                .action(clap::ArgAction::Append)
                .value_delimiter(',')
                .num_args(1..)
                .help("Inject build-time defines (repeatable)"),
        )
}

pub async fn run<O: CliOverrides>(_ctx: &Context, _o: &O, cmd_path: &[String], _m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

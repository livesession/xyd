// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("widgets")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("list")
                .about("List all widgets.")
                .arg(
                    Arg::new("limit")
                        .long("limit")
                        .value_parser(clap::value_parser!(i64))
                        .help("Maximum number of widgets to return."),
                )
                .arg(
                    Arg::new("status")
                        .long("status")
                        .help("Filter by status."),
                ),
        )
        .subcommand(
            Command::new("create")
                .about("Create a widget.")
                .arg(
                    Arg::new("name")
                        .long("name")
                        .help("The widget name.")
                        .required(true),
                )
                .arg(
                    Arg::new("color")
                        .long("color"),
                )
                .arg(
                    Arg::new("tags")
                        .long("tags")
                        .action(clap::ArgAction::Append)
                        .value_delimiter(','),
                )
                .arg(
                    Arg::new("enabled")
                        .long("enabled")
                        .num_args(0..=1)
                        .require_equals(true)
                        .default_missing_value("true")
                        .value_parser(clap::value_parser!(bool)),
                )
                .arg(
                    Arg::new("config")
                        .long("config"),
                ),
        )
        .subcommand(
            Command::new("retrieve")
                .visible_alias("get")
                .about("Retrieve a widget.")
                .arg(
                    Arg::new("widget-id")
                        .required(true),
                ),
        )
        .subcommand(
            Command::new("update")
                .about("Update a widget.")
                .arg(
                    Arg::new("widget-id")
                        .required(true),
                )
                .arg(
                    Arg::new("name")
                        .long("name"),
                )
                .arg(
                    Arg::new("enabled")
                        .long("enabled")
                        .num_args(0..=1)
                        .require_equals(true)
                        .default_missing_value("true")
                        .value_parser(clap::value_parser!(bool)),
                ),
        )
        .subcommand(
            Command::new("delete")
                .about("Delete a widget.")
                .arg(
                    Arg::new("widget-id")
                        .required(true),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["widgets", "list"] => handle_widgets_list(ctx, o, cmd_path, m).await,
        ["widgets", "create"] => handle_widgets_create(ctx, o, cmd_path, m).await,
        ["widgets", "retrieve"] => handle_widgets_retrieve(ctx, o, cmd_path, m).await,
        ["widgets", "update"] => handle_widgets_update(ctx, o, cmd_path, m).await,
        ["widgets", "delete"] => handle_widgets_delete(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_widgets_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/widgets".to_string();
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<i64>("limit") {
        query.push(("limit", v.to_string()));
    }
    if let Some(v) = m.get_one::<String>("status") {
        query.push(("status", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_widgets_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/widgets".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("color") {
        body.insert("color".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(vs) = m.get_many::<String>("tags") {
        body.insert("tags".to_string(), serde_json::Value::Array(vs.cloned().map(serde_json::Value::String).collect()));
    }
    if let Some(v) = m.get_one::<bool>("enabled") {
        body.insert("enabled".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<String>("config") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("config".to_string(), value);
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_widgets_retrieve<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let widget_id = m.get_one::<String>("widget-id").map(String::as_str).unwrap_or("");
    let path = format!("/widgets/{}", runtime::path_escape(widget_id));
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_widgets_update<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let widget_id = m.get_one::<String>("widget-id").map(String::as_str).unwrap_or("");
    let path = format!("/widgets/{}", runtime::path_escape(widget_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<bool>("enabled") {
        body.insert("enabled".to_string(), serde_json::Value::Bool(*v));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "PATCH",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_widgets_delete<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let widget_id = m.get_one::<String>("widget-id").map(String::as_str).unwrap_or("");
    let path = format!("/widgets/{}", runtime::path_escape(widget_id));
    let req = runtime::Request {
        method: "DELETE",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

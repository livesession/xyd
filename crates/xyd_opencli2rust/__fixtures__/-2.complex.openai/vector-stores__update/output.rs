// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("vector-stores")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("create")
                .about("Create a vector store.")
                .arg(
                    Arg::new("file-ids")
                        .long("file-ids")
                        .action(clap::ArgAction::Append)
                        .value_delimiter(',')
                        .help("A list of [File](/docs/api-reference/files) IDs that the vector store should use. Useful for tools like `file_search` that can access files."),
                )
                .arg(
                    Arg::new("name")
                        .long("name")
                        .help("The name of the vector store."),
                )
                .arg(
                    Arg::new("description")
                        .long("description")
                        .help("A description for the vector store. Can be used to describe the vector store's purpose."),
                )
                .arg(
                    Arg::new("expires-after")
                        .long("expires-after")
                        .help("The expiration policy for a vector store."),
                )
                .arg(
                    Arg::new("chunking-strategy")
                        .long("chunking-strategy")
                        .help("The chunking strategy used to chunk the file(s). If not set, will use the `auto` strategy. Only applicable if `file_ids` is non-empty."),
                )
                .arg(
                    Arg::new("metadata")
                        .long("metadata"),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["vector-stores", "create"] => handle_vector_stores_create(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_vector_stores_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/vector_stores".to_string();
    let mut body = serde_json::Map::new();
    if let Some(vs) = m.get_many::<String>("file-ids") {
        body.insert("file_ids".to_string(), serde_json::Value::Array(vs.cloned().map(serde_json::Value::String).collect()));
    }
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("description") {
        body.insert("description".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("expires-after") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("expires_after".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("chunking-strategy") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("chunking_strategy".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("metadata") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("metadata".to_string(), value);
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("responses")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("retrieve")
                .visible_alias("get")
                .about("Retrieves a model response with the given ID.\n")
                .arg(
                    Arg::new("response-id")
                        .help("The ID of the response to retrieve.")
                        .required(true),
                )
                .arg(
                    Arg::new("include")
                        .long("include")
                        .action(clap::ArgAction::Append)
                        .value_delimiter(',')
                        .help("Additional fields to include in the response. See the `include`\nparameter for Response creation above for more information.\n"),
                )
                .arg(
                    Arg::new("stream")
                        .long("stream")
                        .num_args(0..=1)
                        .require_equals(true)
                        .default_missing_value("true")
                        .value_parser(clap::value_parser!(bool))
                        .help("If set to true, the model response data will be streamed to the client\nas it is generated using [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format).\nSee the [Streaming section below](/docs/api-reference/responses-streaming)\nfor more information.\n"),
                )
                .arg(
                    Arg::new("starting-after")
                        .long("starting-after")
                        .value_parser(clap::value_parser!(i64))
                        .help("The sequence number of the event after which to start streaming.\n"),
                )
                .arg(
                    Arg::new("include-obfuscation")
                        .long("include-obfuscation")
                        .num_args(0..=1)
                        .require_equals(true)
                        .default_missing_value("true")
                        .value_parser(clap::value_parser!(bool))
                        .help("When true, stream obfuscation will be enabled. Stream obfuscation adds\nrandom characters to an `obfuscation` field on streaming delta events\nto normalize payload sizes as a mitigation to certain side-channel\nattacks. These obfuscation fields are included by default, but add a\nsmall amount of overhead to the data stream. You can set\n`include_obfuscation` to false to optimize for bandwidth if you trust\nthe network links between your application and the OpenAI API.\n"),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["responses", "retrieve"] => handle_responses_retrieve(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_responses_retrieve<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let response_id = m.get_one::<String>("response-id").map(String::as_str).unwrap_or("");
    let path = format!("/responses/{}", runtime::path_escape(response_id));
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(vs) = m.get_many::<String>("include") {
        query.push(("include", vs.cloned().collect::<Vec<_>>().join(",")));
    }
    if let Some(v) = m.get_one::<bool>("stream") {
        query.push(("stream", v.to_string()));
    }
    if let Some(v) = m.get_one::<i64>("starting-after") {
        query.push(("starting_after", v.to_string()));
    }
    if let Some(v) = m.get_one::<bool>("include-obfuscation") {
        query.push(("include_obfuscation", v.to_string()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("vector-stores")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("files")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("delete")
                        .about("Delete a vector store file. This will remove the file from the vector store but the file itself will not be deleted. To delete the file, use the [delete file](/docs/api-reference/files/delete) endpoint.")
                        .arg(
                            Arg::new("vector-store-id")
                                .help("The ID of the vector store that the file belongs to.")
                                .required(true),
                        )
                        .arg(
                            Arg::new("file-id")
                                .help("The ID of the file to delete.")
                                .required(true),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["vector-stores", "files", "delete"] => handle_vector_stores_files_delete(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_vector_stores_files_delete<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let vector_store_id = m.get_one::<String>("vector-store-id").map(String::as_str).unwrap_or("");
    let file_id = m.get_one::<String>("file-id").map(String::as_str).unwrap_or("");
    let path = format!("/vector_stores/{}/files/{}", runtime::path_escape(vector_store_id), runtime::path_escape(file_id));
    let req = runtime::Request {
        method: "DELETE",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

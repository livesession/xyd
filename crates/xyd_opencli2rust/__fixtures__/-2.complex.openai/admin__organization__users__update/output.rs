// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("organization")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("users")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("modify")
                        .about("Modifies a user's role in the organization.")
                        .arg(
                            Arg::new("user-id")
                                .help("The ID of the user.")
                                .required(true),
                        )
                        .arg(
                            Arg::new("role")
                                .long("role")
                                .help("`owner` or `reader`"),
                        )
                        .arg(
                            Arg::new("role-id")
                                .long("role-id")
                                .help("Role ID to assign to the user."),
                        )
                        .arg(
                            Arg::new("technical-level")
                                .long("technical-level")
                                .help("Technical level metadata."),
                        )
                        .arg(
                            Arg::new("developer-persona")
                                .long("developer-persona")
                                .help("Developer persona metadata."),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["organization", "users", "modify"] => handle_organization_users_modify(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_organization_users_modify<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let user_id = m.get_one::<String>("user-id").map(String::as_str).unwrap_or("");
    let path = format!("/organization/users/{}", runtime::path_escape(user_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("role") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("role".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("role-id") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("role_id".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("technical-level") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("technical_level".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("developer-persona") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("developer_persona".to_string(), value);
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

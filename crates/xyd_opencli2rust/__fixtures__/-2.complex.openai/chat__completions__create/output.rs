// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("chat")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("completions")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("create")
                        .about("**Starting a new project?** We recommend trying [Responses](/docs/api-reference/responses)\nto take advantage of the latest OpenAI platform features. Compare\n[Chat Completions with Responses](/docs/guides/responses-vs-chat-completions?api-mode=responses).\n\n---\n\nCreates a model response for the given chat conversation. Learn more in the\n[text generation](/docs/guides/text-generation), [vision](/docs/guides/vision),\nand [audio](/docs/guides/audio) guides.\n\nParameter support can differ depending on the model used to generate the\nresponse, particularly for newer reasoning models. Parameters that are only\nsupported for reasoning models are noted below. For the current state of\nunsupported parameters in reasoning models,\n[refer to the reasoning guide](/docs/guides/reasoning).\n\nReturns a chat completion object, or a streamed sequence of chat completion\nchunk objects if the request is streamed.\n")
                        .arg(
                            Arg::new("metadata")
                                .long("metadata"),
                        )
                        .arg(
                            Arg::new("top-logprobs")
                                .long("top-logprobs")
                                .value_parser(clap::value_parser!(i64))
                                .help("An integer between 0 and 20 specifying the maximum number of most likely\ntokens to return at each token position, each with an associated log\nprobability. In some cases, the number of returned tokens may be fewer than\nrequested.\n`logprobs` must be set to `true` if this parameter is used.\n"),
                        )
                        .arg(
                            Arg::new("temperature")
                                .long("temperature"),
                        )
                        .arg(
                            Arg::new("top-p")
                                .long("top-p"),
                        )
                        .arg(
                            Arg::new("user")
                                .long("user")
                                .help("This field is being replaced by `safety_identifier` and `prompt_cache_key`. Use `prompt_cache_key` instead to maintain caching optimizations.\nA stable identifier for your end-users.\nUsed to boost cache hit rates by better bucketing similar requests and  to help OpenAI detect and prevent abuse. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).\n"),
                        )
                        .arg(
                            Arg::new("safety-identifier")
                                .long("safety-identifier")
                                .help("A stable identifier used to help detect users of your application that may be violating OpenAI's usage policies.\nThe IDs should be a string that uniquely identifies each user, with a maximum length of 64 characters. We recommend hashing their username or email address, in order to avoid sending us any identifying information. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).\n"),
                        )
                        .arg(
                            Arg::new("prompt-cache-key")
                                .long("prompt-cache-key")
                                .help("Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the `user` field. [Learn more](/docs/guides/prompt-caching).\n"),
                        )
                        .arg(
                            Arg::new("service-tier")
                                .long("service-tier"),
                        )
                        .arg(
                            Arg::new("prompt-cache-retention")
                                .long("prompt-cache-retention"),
                        )
                        .arg(
                            Arg::new("messages")
                                .long("messages")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(',')
                                .help("A list of messages comprising the conversation so far. Depending on the\n[model](/docs/models) you use, different message types (modalities) are\nsupported, like [text](/docs/guides/text-generation),\n[images](/docs/guides/vision), and [audio](/docs/guides/audio).\n")
                                .required(true),
                        )
                        .arg(
                            Arg::new("model")
                                .long("model")
                                .help("Model ID used to generate the response, like `gpt-4o` or `o3`. OpenAI\noffers a wide range of models with different capabilities, performance\ncharacteristics, and price points. Refer to the [model guide](/docs/models)\nto browse and compare available models.\n")
                                .required(true),
                        )
                        .arg(
                            Arg::new("modalities")
                                .long("modalities"),
                        )
                        .arg(
                            Arg::new("verbosity")
                                .long("verbosity"),
                        )
                        .arg(
                            Arg::new("reasoning-effort")
                                .long("reasoning-effort"),
                        )
                        .arg(
                            Arg::new("max-completion-tokens")
                                .long("max-completion-tokens")
                                .value_parser(clap::value_parser!(i64))
                                .help("An upper bound for the number of tokens that can be generated for a completion, including visible output tokens and [reasoning tokens](/docs/guides/reasoning).\n"),
                        )
                        .arg(
                            Arg::new("frequency-penalty")
                                .long("frequency-penalty")
                                .value_parser(clap::value_parser!(f64))
                                .help("Number between -2.0 and 2.0. Positive values penalize new tokens based on\ntheir existing frequency in the text so far, decreasing the model's\nlikelihood to repeat the same line verbatim.\n"),
                        )
                        .arg(
                            Arg::new("presence-penalty")
                                .long("presence-penalty")
                                .value_parser(clap::value_parser!(f64))
                                .help("Number between -2.0 and 2.0. Positive values penalize new tokens based on\nwhether they appear in the text so far, increasing the model's likelihood\nto talk about new topics.\n"),
                        )
                        .arg(
                            Arg::new("web-search-options")
                                .long("web-search-options")
                                .help("This tool searches the web for relevant results to use in a response.\nLearn more about the [web search tool](/docs/guides/tools-web-search?api-mode=chat).\n"),
                        )
                        .arg(
                            Arg::new("response-format")
                                .long("response-format")
                                .help("An object specifying the format that the model must output.\n\nSetting to `{ \"type\": \"json_schema\", \"json_schema\": {...} }` enables\nStructured Outputs which ensures the model will match your supplied JSON\nschema. Learn more in the [Structured Outputs\nguide](/docs/guides/structured-outputs).\n\nSetting to `{ \"type\": \"json_object\" }` enables the older JSON mode, which\nensures the message the model generates is valid JSON. Using `json_schema`\nis preferred for models that support it.\n"),
                        )
                        .arg(
                            Arg::new("audio")
                                .long("audio")
                                .help("Parameters for audio output. Required when audio output is requested with\n`modalities: [\"audio\"]`. [Learn more](/docs/guides/audio).\n"),
                        )
                        .arg(
                            Arg::new("store")
                                .long("store")
                                .num_args(0..=1)
                                .require_equals(true)
                                .default_missing_value("true")
                                .value_parser(clap::value_parser!(bool))
                                .help("Whether or not to store the output of this chat completion request for\nuse in our [model distillation](/docs/guides/distillation) or\n[evals](/docs/guides/evals) products.\n\nSupports text and image inputs. Note: image inputs over 8MB will be dropped.\n"),
                        )
                        .arg(
                            Arg::new("stream")
                                .long("stream")
                                .num_args(0..=1)
                                .require_equals(true)
                                .default_missing_value("true")
                                .value_parser(clap::value_parser!(bool))
                                .help("If set to true, the model response data will be streamed to the client\nas it is generated using [server-sent events](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events#Event_stream_format).\nSee the [Streaming section below](/docs/api-reference/chat/streaming)\nfor more information, along with the [streaming responses](/docs/guides/streaming-responses)\nguide for more information on how to handle the streaming events.\n"),
                        )
                        .arg(
                            Arg::new("stop")
                                .long("stop")
                                .help("Not supported with latest reasoning models `o3` and `o4-mini`.\n\nUp to 4 sequences where the API will stop generating further tokens. The\nreturned text will not contain the stop sequence.\n"),
                        )
                        .arg(
                            Arg::new("logit-bias")
                                .long("logit-bias")
                                .help("Modify the likelihood of specified tokens appearing in the completion.\n\nAccepts a JSON object that maps tokens (specified by their token ID in the\ntokenizer) to an associated bias value from -100 to 100. Mathematically,\nthe bias is added to the logits generated by the model prior to sampling.\nThe exact effect will vary per model, but values between -1 and 1 should\ndecrease or increase likelihood of selection; values like -100 or 100\nshould result in a ban or exclusive selection of the relevant token.\n"),
                        )
                        .arg(
                            Arg::new("logprobs")
                                .long("logprobs")
                                .num_args(0..=1)
                                .require_equals(true)
                                .default_missing_value("true")
                                .value_parser(clap::value_parser!(bool))
                                .help("Whether to return log probabilities of the output tokens or not. If true,\nreturns the log probabilities of each output token returned in the\n`content` of `message`.\n"),
                        )
                        .arg(
                            Arg::new("max-tokens")
                                .long("max-tokens")
                                .value_parser(clap::value_parser!(i64))
                                .help("The maximum number of [tokens](/tokenizer) that can be generated in the\nchat completion. This value can be used to control\n[costs](https://openai.com/api/pricing/) for text generated via API.\n\nThis value is now deprecated in favor of `max_completion_tokens`, and is\nnot compatible with [o-series models](/docs/guides/reasoning).\n"),
                        )
                        .arg(
                            Arg::new("n")
                                .long("n")
                                .value_parser(clap::value_parser!(i64))
                                .help("How many chat completion choices to generate for each input message. Note that you will be charged based on the number of generated tokens across all of the choices. Keep `n` as `1` to minimize costs."),
                        )
                        .arg(
                            Arg::new("prediction")
                                .long("prediction")
                                .help("Configuration for a [Predicted Output](/docs/guides/predicted-outputs),\nwhich can greatly improve response times when large parts of the model\nresponse are known ahead of time. This is most common when you are\nregenerating a file with only minor changes to most of the content.\n"),
                        )
                        .arg(
                            Arg::new("seed")
                                .long("seed")
                                .value_parser(clap::value_parser!(i64))
                                .help("This feature is in Beta.\nIf specified, our system will make a best effort to sample deterministically, such that repeated requests with the same `seed` and parameters should return the same result.\nDeterminism is not guaranteed, and you should refer to the `system_fingerprint` response parameter to monitor changes in the backend.\n"),
                        )
                        .arg(
                            Arg::new("stream-options")
                                .long("stream-options"),
                        )
                        .arg(
                            Arg::new("tools")
                                .long("tools")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(',')
                                .help("A list of tools the model may call. You can provide either\n[custom tools](/docs/guides/function-calling#custom-tools) or\n[function tools](/docs/guides/function-calling).\n"),
                        )
                        .arg(
                            Arg::new("tool-choice")
                                .long("tool-choice")
                                .help("Controls which (if any) tool is called by the model.\n`none` means the model will not call any tool and instead generates a message.\n`auto` means the model can pick between generating a message or calling one or more tools.\n`required` means the model must call one or more tools.\nSpecifying a particular tool via `{\"type\": \"function\", \"function\": {\"name\": \"my_function\"}}` forces the model to call that tool.\n\n`none` is the default when no tools are present. `auto` is the default if tools are present.\n"),
                        )
                        .arg(
                            Arg::new("parallel-tool-calls")
                                .long("parallel-tool-calls")
                                .num_args(0..=1)
                                .require_equals(true)
                                .default_missing_value("true")
                                .value_parser(clap::value_parser!(bool))
                                .help("Whether to enable [parallel function calling](/docs/guides/function-calling#configuring-parallel-function-calling) during tool use."),
                        )
                        .arg(
                            Arg::new("function-call")
                                .long("function-call")
                                .help("Deprecated in favor of `tool_choice`.\n\nControls which (if any) function is called by the model.\n\n`none` means the model will not call a function and instead generates a\nmessage.\n\n`auto` means the model can pick between generating a message or calling a\nfunction.\n\nSpecifying a particular function via `{\"name\": \"my_function\"}` forces the\nmodel to call that function.\n\n`none` is the default when no functions are present. `auto` is the default\nif functions are present.\n"),
                        )
                        .arg(
                            Arg::new("functions")
                                .long("functions")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(',')
                                .help("Deprecated in favor of `tools`.\n\nA list of functions the model may generate JSON inputs for.\n"),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["chat", "completions", "create"] => handle_chat_completions_create(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_chat_completions_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/chat/completions".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("metadata") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("metadata".to_string(), value);
    }
    if let Some(v) = m.get_one::<i64>("top-logprobs") {
        body.insert("top_logprobs".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<String>("temperature") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("temperature".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("top-p") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("top_p".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("user") {
        body.insert("user".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("safety-identifier") {
        body.insert("safety_identifier".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("prompt-cache-key") {
        body.insert("prompt_cache_key".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("service-tier") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("service_tier".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("prompt-cache-retention") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("prompt_cache_retention".to_string(), value);
    }
    if let Some(vs) = m.get_many::<String>("messages") {
        body.insert("messages".to_string(), serde_json::Value::Array(vs.cloned().map(serde_json::Value::String).collect()));
    }
    if let Some(v) = m.get_one::<String>("model") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("model".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("modalities") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("modalities".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("verbosity") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("verbosity".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("reasoning-effort") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("reasoning_effort".to_string(), value);
    }
    if let Some(v) = m.get_one::<i64>("max-completion-tokens") {
        body.insert("max_completion_tokens".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<f64>("frequency-penalty") {
        body.insert("frequency_penalty".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<f64>("presence-penalty") {
        body.insert("presence_penalty".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<String>("web-search-options") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("web_search_options".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("response-format") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("response_format".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("audio") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("audio".to_string(), value);
    }
    if let Some(v) = m.get_one::<bool>("store") {
        body.insert("store".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<bool>("stream") {
        body.insert("stream".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<String>("stop") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("stop".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("logit-bias") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("logit_bias".to_string(), value);
    }
    if let Some(v) = m.get_one::<bool>("logprobs") {
        body.insert("logprobs".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<i64>("max-tokens") {
        body.insert("max_tokens".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<i64>("n") {
        body.insert("n".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<String>("prediction") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("prediction".to_string(), value);
    }
    if let Some(v) = m.get_one::<i64>("seed") {
        body.insert("seed".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<String>("stream-options") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("stream_options".to_string(), value);
    }
    if let Some(vs) = m.get_many::<String>("tools") {
        body.insert("tools".to_string(), serde_json::Value::Array(vs.cloned().map(serde_json::Value::String).collect()));
    }
    if let Some(v) = m.get_one::<String>("tool-choice") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("tool_choice".to_string(), value);
    }
    if let Some(v) = m.get_one::<bool>("parallel-tool-calls") {
        body.insert("parallel_tool_calls".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<String>("function-call") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("function_call".to_string(), value);
    }
    if let Some(vs) = m.get_many::<String>("functions") {
        body.insert("functions".to_string(), serde_json::Value::Array(vs.cloned().map(serde_json::Value::String).collect()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

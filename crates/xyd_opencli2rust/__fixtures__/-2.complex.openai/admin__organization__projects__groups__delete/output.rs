// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("organization")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("projects")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("groups")
                        .subcommand_required(true)
                        .arg_required_else_help(true)
                        .subcommand(
                            Command::new("delete")
                                .about("Revokes a group's access to a project.")
                                .arg(
                                    Arg::new("project-id")
                                        .help("The ID of the project to update.")
                                        .required(true),
                                )
                                .arg(
                                    Arg::new("group-id")
                                        .help("The ID of the group to remove from the project.")
                                        .required(true),
                                ),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["organization", "projects", "groups", "delete"] => handle_organization_projects_groups_delete(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_organization_projects_groups_delete<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let project_id = m.get_one::<String>("project-id").map(String::as_str).unwrap_or("");
    let group_id = m.get_one::<String>("group-id").map(String::as_str).unwrap_or("");
    let path = format!("/organization/projects/{}/groups/{}", runtime::path_escape(project_id), runtime::path_escape(group_id));
    let req = runtime::Request {
        method: "DELETE",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

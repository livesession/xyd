// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("chatkit")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("sessions")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("create")
                        .about("Create a ChatKit session.")
                        .arg(
                            Arg::new("workflow")
                                .long("workflow")
                                .help("Workflow that powers the session.")
                                .required(true),
                        )
                        .arg(
                            Arg::new("user")
                                .long("user")
                                .help("A free-form string that identifies your end user; ensures this Session can access other objects that have the same `user` scope.")
                                .required(true),
                        )
                        .arg(
                            Arg::new("expires-after")
                                .long("expires-after")
                                .help("Optional override for session expiration timing in seconds from creation. Defaults to 10 minutes."),
                        )
                        .arg(
                            Arg::new("rate-limits")
                                .long("rate-limits")
                                .help("Optional override for per-minute request limits. When omitted, defaults to 10."),
                        )
                        .arg(
                            Arg::new("chatkit-configuration")
                                .long("chatkit-configuration")
                                .help("Optional overrides for ChatKit runtime configuration features"),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["chatkit", "sessions", "create"] => handle_chatkit_sessions_create(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_chatkit_sessions_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/chatkit/sessions".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("workflow") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("workflow".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("user") {
        body.insert("user".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("expires-after") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("expires_after".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("rate-limits") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("rate_limits".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("chatkit-configuration") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("chatkit_configuration".to_string(), value);
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("organization")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("audit-logs")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("list")
                        .about("List user actions and configuration changes within this organization.")
                        .arg(
                            Arg::new("effective-at")
                                .long("effective-at")
                                .help("Return only events whose `effective_at` (Unix seconds) is in this range."),
                        )
                        .arg(
                            Arg::new("project-ids[]")
                                .long("project-ids[]")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(',')
                                .help("Return only events for these projects."),
                        )
                        .arg(
                            Arg::new("event-types[]")
                                .long("event-types[]")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(',')
                                .help("Return only events with a `type` in one of these values. For example, `project.created`. For all options, see the documentation for the [audit log object](/docs/api-reference/audit-logs/object)."),
                        )
                        .arg(
                            Arg::new("actor-ids[]")
                                .long("actor-ids[]")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(',')
                                .help("Return only events performed by these actors. Can be a user ID, a service account ID, or an api key tracking ID."),
                        )
                        .arg(
                            Arg::new("actor-emails[]")
                                .long("actor-emails[]")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(',')
                                .help("Return only events performed by users with these emails."),
                        )
                        .arg(
                            Arg::new("resource-ids[]")
                                .long("resource-ids[]")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(',')
                                .help("Return only events performed on these targets. For example, a project ID updated."),
                        )
                        .arg(
                            Arg::new("limit")
                                .long("limit")
                                .value_parser(clap::value_parser!(i64))
                                .help("A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.\n"),
                        )
                        .arg(
                            Arg::new("after")
                                .long("after")
                                .help("A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n"),
                        )
                        .arg(
                            Arg::new("before")
                                .long("before")
                                .help("A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.\n"),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["organization", "audit-logs", "list"] => handle_organization_audit_logs_list(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_organization_audit_logs_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/organization/audit_logs".to_string();
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("effective-at") {
        query.push(("effective_at", v.clone()));
    }
    if let Some(vs) = m.get_many::<String>("project-ids[]") {
        query.push(("project_ids[]", vs.cloned().collect::<Vec<_>>().join(",")));
    }
    if let Some(vs) = m.get_many::<String>("event-types[]") {
        query.push(("event_types[]", vs.cloned().collect::<Vec<_>>().join(",")));
    }
    if let Some(vs) = m.get_many::<String>("actor-ids[]") {
        query.push(("actor_ids[]", vs.cloned().collect::<Vec<_>>().join(",")));
    }
    if let Some(vs) = m.get_many::<String>("actor-emails[]") {
        query.push(("actor_emails[]", vs.cloned().collect::<Vec<_>>().join(",")));
    }
    if let Some(vs) = m.get_many::<String>("resource-ids[]") {
        query.push(("resource_ids[]", vs.cloned().collect::<Vec<_>>().join(",")));
    }
    if let Some(v) = m.get_one::<i64>("limit") {
        query.push(("limit", v.to_string()));
    }
    if let Some(v) = m.get_one::<String>("after") {
        query.push(("after", v.clone()));
    }
    if let Some(v) = m.get_one::<String>("before") {
        query.push(("before", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

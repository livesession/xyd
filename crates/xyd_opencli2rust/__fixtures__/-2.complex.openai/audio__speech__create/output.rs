// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("audio")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("speech")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("create")
                        .about("Generates audio from the input text.\n\nReturns the audio file content, or a stream of audio events.\n")
                        .arg(
                            Arg::new("model")
                                .long("model")
                                .help("One of the available [TTS models](/docs/models#tts): `tts-1`, `tts-1-hd`, `gpt-4o-mini-tts`, or `gpt-4o-mini-tts-2025-12-15`.\n")
                                .required(true),
                        )
                        .arg(
                            Arg::new("input")
                                .long("input")
                                .help("The text to generate audio for. The maximum length is 4096 characters.")
                                .required(true),
                        )
                        .arg(
                            Arg::new("instructions")
                                .long("instructions")
                                .help("Control the voice of your generated audio with additional instructions. Does not work with `tts-1` or `tts-1-hd`."),
                        )
                        .arg(
                            Arg::new("voice")
                                .long("voice")
                                .help("The voice to use when generating the audio. Supported built-in voices are `alloy`, `ash`, `ballad`, `coral`, `echo`, `fable`, `onyx`, `nova`, `sage`, `shimmer`, `verse`, `marin`, and `cedar`. You may also provide a custom voice object with an `id`, for example `{ \"id\": \"voice_1234\" }`. Previews of the voices are available in the [Text to speech guide](/docs/guides/text-to-speech#voice-options).")
                                .required(true),
                        )
                        .arg(
                            Arg::new("response-format")
                                .long("response-format")
                                .help("The format to audio in. Supported formats are `mp3`, `opus`, `aac`, `flac`, `wav`, and `pcm`."),
                        )
                        .arg(
                            Arg::new("speed")
                                .long("speed")
                                .value_parser(clap::value_parser!(f64))
                                .help("The speed of the generated audio. Select a value from `0.25` to `4.0`. `1.0` is the default."),
                        )
                        .arg(
                            Arg::new("stream-format")
                                .long("stream-format")
                                .help("The format to stream the audio in. Supported formats are `sse` and `audio`. `sse` is not supported for `tts-1` or `tts-1-hd`."),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["audio", "speech", "create"] => handle_audio_speech_create(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_audio_speech_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/audio/speech".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("model") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("model".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("input") {
        body.insert("input".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("instructions") {
        body.insert("instructions".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("voice") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("voice".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("response-format") {
        body.insert("response_format".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<f64>("speed") {
        body.insert("speed".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<String>("stream-format") {
        body.insert("stream_format".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

{
  "opencli": "1.0.0",
  "info": {
    "title": "sample-api",
    "version": "1.2.0",
    "description": "A mixed CLI: an x-openapi resource plus a non-API runnable leaf."
  },
  "x-openapi": {
    "servers": [
      "https://api.example.com/v1"
    ],
    "security": [
      {
        "type": "http",
        "kind": "bearer",
        "envVar": "SAMPLE_API_API_KEY",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    ]
  },
  "commands": [
    {
      "name": "models",
      "commands": [
        {
          "name": "list",
          "description": "Lists the currently available models.",
          "x-openapi": {
            "method": "get",
            "path": "/models",
            "operationId": "listModels"
          }
        },
        {
          "name": "retrieve",
          "aliases": [
            "get"
          ],
          "description": "Retrieves a model instance.",
          "arguments": [
            {
              "name": "model",
              "required": true,
              "description": "The ID of the model to use for this request."
            }
          ],
          "x-openapi": {
            "method": "get",
            "path": "/models/{model}",
            "operationId": "retrieveModel",
            "params": [
              {
                "in": "path",
                "name": "model",
                "from": "argument:model",
                "required": true
              }
            ]
          }
        }
      ]
    },
    {
      "name": "dev",
      "description": "Run your docs locally in development mode"
    }
  ]
}

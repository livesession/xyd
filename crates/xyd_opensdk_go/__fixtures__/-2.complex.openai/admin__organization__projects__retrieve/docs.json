{
  "chain": [
    "admin",
    "organization",
    "projects"
  ],
  "action": "retrieve",
  "usage": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\n\t\"github.com/example/openai\"\n\t\"github.com/example/openai/option\"\n)\n\nfunc main() {\n\tclient := openai.NewClient(\n\t\toption.WithAPIKey(\"My API Key\"),\n\t)\n\tresult, err := client.Admin.Organization.Projects.Get(context.TODO(), \"project_id\")\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf(\"%+v\\n\", result)\n}\n",
  "typeReference": {
    "signature": "client.Admin.Organization.Projects.Get(ctx, projectID) (*Project, error)",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "Project",
      "fields": [
        {
          "name": "ID",
          "langType": "string",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "Object",
          "langType": "string",
          "required": true,
          "description": "The object type, which is always `organization.project`"
        },
        {
          "name": "Name",
          "langType": "string",
          "required": false,
          "description": "The name of the project. This appears in reporting."
        },
        {
          "name": "CreatedAt",
          "langType": "int64",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the project was created."
        },
        {
          "name": "ArchivedAt",
          "langType": "int64",
          "required": false
        },
        {
          "name": "Status",
          "langType": "string",
          "required": false,
          "description": "`active` or `archived`"
        },
        {
          "name": "ExternalKeyID",
          "langType": "string",
          "required": false,
          "description": "The external key associated with the project."
        }
      ]
    }
  }
}

{
  "chain": [
    "beta",
    "chat-kit",
    "threads"
  ],
  "action": "list",
  "usage": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\n\t\"github.com/example/openai\"\n\t\"github.com/example/openai/option\"\n\t\"github.com/example/openai/packages/param\"\n)\n\nfunc main() {\n\tclient := openai.NewClient(\n\t\toption.WithAPIKey(\"My API Key\"),\n\t)\n\tresult, err := client.Beta.ChatKit.Threads.List(\n\t\tcontext.TODO(),\n\t\topenai.BetaChatKitThreadListParams{\n\t\t\tLimit: param.NewOpt[int64](1),\n\t\t\tOrder: param.NewOpt[openai.OrderEnum](openai.OrderEnumAsc),\n\t\t\tAfter: param.NewOpt[string](\"after\"),\n\t\t\tBefore: param.NewOpt[string](\"before\"),\n\t\t\tUser: param.NewOpt[string](\"user\"),\n\t\t},\n\t)\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf(\"%+v\\n\", result)\n}\n",
  "typeReference": {
    "signature": "client.Beta.ChatKit.Threads.List(ctx, query) (*pagination.CursorPage[ThreadResource], error)",
    "request": {
      "typeName": "BetaChatKitThreadListParams",
      "argName": "query",
      "fields": [
        {
          "name": "Limit",
          "langType": "param.Opt[int64]",
          "required": false,
          "description": "Maximum number of thread items to return. Defaults to 20."
        },
        {
          "name": "Order",
          "langType": "param.Opt[OrderEnum]",
          "required": false,
          "description": "Sort order for results by creation time. Defaults to `desc`.",
          "refTypeName": "OrderEnum"
        },
        {
          "name": "After",
          "langType": "param.Opt[string]",
          "required": false,
          "description": "List items created after this thread item ID. Defaults to null for the first page."
        },
        {
          "name": "Before",
          "langType": "param.Opt[string]",
          "required": false,
          "description": "List items created before this thread item ID. Defaults to null for the newest results."
        },
        {
          "name": "User",
          "langType": "param.Opt[string]",
          "required": false,
          "description": "Filter threads that belong to this user identifier. Defaults to null to return all users."
        }
      ]
    },
    "response": {
      "typeName": "ThreadResource",
      "note": "paginated (pagination.CursorPage)",
      "fields": [
        {
          "name": "ID",
          "langType": "string",
          "required": true,
          "description": "Identifier of the thread."
        },
        {
          "name": "Object",
          "langType": "string",
          "required": true,
          "description": "Type discriminator that is always `chatkit.thread`."
        },
        {
          "name": "CreatedAt",
          "langType": "int64",
          "required": true,
          "description": "Unix timestamp (in seconds) for when the thread was created."
        },
        {
          "name": "Title",
          "langType": "string",
          "required": true
        },
        {
          "name": "Status",
          "langType": "ThreadResourceStatus",
          "required": true,
          "description": "Current status for the thread. Defaults to `active` for newly created threads.",
          "refTypeName": "ThreadResourceStatus"
        },
        {
          "name": "User",
          "langType": "string",
          "required": true,
          "description": "Free-form string that identifies your end user who owns the thread."
        }
      ]
    }
  }
}

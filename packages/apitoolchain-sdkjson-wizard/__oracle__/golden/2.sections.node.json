{
  "files": [
    {
      "path": "src/client.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\nimport { APIClient, readEnv } from './core/request';\nimport type { ClientOptions } from './core/request';\nimport { Pets } from './resources/pets';\nimport { Store } from './resources/store';\n\n/** The Acme API client. */\nexport class AcmeSdk extends APIClient {\n  readonly pets: Pets;\n  readonly store: Store;\n\n  constructor(options: ClientOptions = {}) {\n    super({ ...options, apiKey: options.apiKey ?? readEnv(\"ACME_API_KEY\") });\n    this.pets = new Pets(this);\n    this.store = new Store(this);\n  }\n}\n",
      "language": "typescript"
    },
    {
      "path": "src/index.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\nexport { AcmeSdk as default } from './client';\nexport { APIError, BadRequestError, ConflictError, InternalError, NotFoundError, PermissionDeniedError, RateLimitedError, UnauthorizedError, UnprocessableEntityError } from './core/error';\nexport type { ClientOptions, RequestOptions } from './core/request';\nexport * from './models';\nexport * from './resources/index';\nexport * as busybox from './busybox';\n",
      "language": "typescript"
    },
    {
      "path": "src/resources/index.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\nexport * from './pets';\nexport * from './store';\n",
      "language": "typescript"
    },
    {
      "path": "src/busybox.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\nimport { APIError } from './core/error';\n\n/** True when `err` is an APIError (a non-2xx API response), narrowing the type. */\nexport function isAPIError(err: unknown): err is APIError {\n  return err instanceof APIError;\n}\n\n/** True when `err` is an APIError carrying the given HTTP status. */\nexport function isStatus(err: unknown, status: number): boolean {\n  return err instanceof APIError && err.status === status;\n}\n\n/** True when `err` is a 404 Not Found API response. */\nexport function isNotFound(err: unknown): boolean {\n  return isStatus(err, 404);\n}\n\n/** True when `err` is a 401 Unauthorized API response. */\nexport function isUnauthorized(err: unknown): boolean {\n  return isStatus(err, 401);\n}\n\n/** True when `err` is a 403 Forbidden API response. */\nexport function isForbidden(err: unknown): boolean {\n  return isStatus(err, 403);\n}\n\n/** True when `err` is a 409 Conflict API response. */\nexport function isConflict(err: unknown): boolean {\n  return isStatus(err, 409);\n}\n\n/** True when `err` is a 429 Too Many Requests API response. */\nexport function isRateLimited(err: unknown): boolean {\n  return isStatus(err, 429);\n}\n\n/** True when `err` is a 5xx server-side API response. */\nexport function isServerError(err: unknown): boolean {\n  return err instanceof APIError && err.status >= 500;\n}\n\n/** A human message for ANY thrown value: an Error's `message`, else `String(err)`. */\nexport function errMessage(err: unknown): string {\n  return err instanceof Error ? err.message : String(err);\n}\n\n/** Prefer the API's real `message` (carried in the JSON body of an APIError) over\n * the generic \"failed with status N\"; falls back to {@link errMessage} otherwise. */\nexport function apiErrMessage(err: unknown): string {\n  if (err instanceof APIError) {\n    try {\n      const body = JSON.parse(err.body) as { message?: string };\n      if (body?.message) return body.message;\n    } catch {\n      // body wasn't JSON — fall through to the SDK's message\n    }\n    return err.message;\n  }\n  return errMessage(err);\n}\n",
      "language": "typescript"
    },
    {
      "path": "package.json",
      "code": "{\n  \"name\": \"@acme/sdk\",\n  \"version\": \"2.3.0\",\n  \"description\": \"A tiny sample API used by the sdk.json wizard preview.\",\n  \"author\": \"Acme Inc\",\n  \"license\": \"MIT\",\n  \"type\": \"module\",\n  \"main\": \"./dist/index.js\",\n  \"types\": \"./dist/index.d.ts\",\n  \"exports\": {\n    \".\": {\n      \"types\": \"./dist/index.d.ts\",\n      \"import\": \"./dist/index.js\"\n    }\n  },\n  \"files\": [\n    \"dist\",\n    \"src\"\n  ],\n  \"scripts\": {\n    \"build\": \"tsc\",\n    \"prepare\": \"tsc\"\n  },\n  \"engines\": {\n    \"node\": \">=18\"\n  },\n  \"dependencies\": {},\n  \"devDependencies\": {\n    \"typescript\": \"^5.6.2\"\n  }\n}\n",
      "language": "json"
    },
    {
      "path": "README.md",
      "code": "# @acme/sdk\n\nA tiny sample API used by the sdk.json wizard preview.\n\n## Usage\n\n```ts\nimport AcmeSdk from '@acme/sdk';\n\nconst client = new AcmeSdk({ apiKey: process.env.API_KEY });\n```\n",
      "language": "markdown"
    },
    {
      "path": "src/core/error.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\n/** The telemetry header the request id is read from (sdk.telemetry.requestIdHeader). */\nexport const REQUEST_ID_HEADER = \"X-Request-ID\";\n\n/** A constructor shared by APIError and its policy subclasses. */\ntype APIErrorConstructor = new (status: number, message: string, headers: Headers, body: string) => APIError;\n\n/** The error thrown for a non-2xx API response: status, headers, raw body and request id. */\nexport class APIError extends Error {\n  readonly status: number;\n  readonly headers: Headers;\n  readonly body: string;\n  /** The captured request id (from the telemetry header), when present. */\n  readonly requestId: string | undefined;\n  /** The policy error kind (overridden by subclasses). */\n  readonly kind: string = 'API';\n\n  constructor(status: number, message: string, headers: Headers, body: string) {\n    super(message);\n    this.name = new.target.name;\n    this.status = status;\n    this.headers = headers;\n    this.body = body;\n    this.requestId = headers.get(REQUEST_ID_HEADER) ?? undefined;\n  }\n}\n\n/** The mapped error for HTTP 400 responses. */\nexport class BadRequestError extends APIError {\n  override readonly kind = \"BadRequest\";\n}\n\n/** The mapped error for HTTP 401 responses. */\nexport class UnauthorizedError extends APIError {\n  override readonly kind = \"Unauthorized\";\n}\n\n/** The mapped error for HTTP 403 responses. */\nexport class PermissionDeniedError extends APIError {\n  override readonly kind = \"PermissionDenied\";\n}\n\n/** The mapped error for HTTP 404 responses. */\nexport class NotFoundError extends APIError {\n  override readonly kind = \"NotFound\";\n}\n\n/** The mapped error for HTTP 409 responses. */\nexport class ConflictError extends APIError {\n  override readonly kind = \"Conflict\";\n}\n\n/** The mapped error for HTTP 422 responses. */\nexport class UnprocessableEntityError extends APIError {\n  override readonly kind = \"UnprocessableEntity\";\n}\n\n/** The mapped error for HTTP 429 responses. */\nexport class RateLimitedError extends APIError {\n  override readonly kind = \"RateLimited\";\n}\n\n/** Catch-all for unmapped 5xx responses. */\nexport class InternalError extends APIError {\n  override readonly kind = \"Internal\";\n}\n\nconst STATUS_TO_ERROR: Record<number, APIErrorConstructor> = {\n  400: BadRequestError,\n  401: UnauthorizedError,\n  403: PermissionDeniedError,\n  404: NotFoundError,\n  409: ConflictError,\n  422: UnprocessableEntityError,\n  429: RateLimitedError,\n};\n\n/** The policy-mapped error: exact status map first, then the 5xx catch-all, then the client catch-all. */\nexport function errorForStatus(status: number, message: string, headers: Headers, body: string): APIError {\n  const Ctor = STATUS_TO_ERROR[status] ?? (status >= 500 ? InternalError : APIError);\n  return new Ctor(status, message, headers, body);\n}\n",
      "language": "typescript"
    },
    {
      "path": "src/core/request.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\nimport { errorForStatus } from './error';\n\ndeclare const process: { env: Record<string, string | undefined>; versions?: { node?: string } } | undefined;\n\nexport const DEFAULT_BASE_URL = \"https://api.acme.com/v1\";\nconst USER_AGENT = \"@acme/sdk-node/2.3.0\";\nconst AI_AGENT_ENV_VARS: Record<string, string> = { \"CLAUDE_CODE\": \"claude-code\", \"CURSOR_AGENT\": \"cursor\", \"CLINE_ACTIVE\": \"cline\", \"WINDSURF_ACTIVE\": \"windsurf\", \"COPILOT_AGENT\": \"copilot\" };\nconst DEFAULT_TIMEOUT_MS = 60000;\nconst MAX_RETRIES = 2;\nconst RETRYABLE_STATUS_CODES = new Set<number>([408, 429, 500, 502, 503, 504]);\nconst RETRY_CONNECTION_ERRORS = true;\nconst HONOR_RETRY_AFTER_HEADER = true;\nconst BACKOFF_INITIAL_DELAY_MS = 500;\nconst BACKOFF_MAX_DELAY_MS = 8000;\nconst BACKOFF_MULTIPLIER = 2;\nconst BACKOFF_JITTER = 0.25;\nconst IDEMPOTENCY_HEADER = \"Idempotency-Key\";\n\n/** Read an environment variable without a hard dependency on `@types/node`. */\nexport function readEnv(name: string): string | undefined {\n  return typeof process !== 'undefined' && process ? process.env[name] : undefined;\n}\n\n/** The policy default request timeout in milliseconds (0 = no deadline). */\nfunction defaultTimeout(): number {\n  return DEFAULT_TIMEOUT_MS;\n}\n\n/** The policy User-Agent: SDK identifier plus an AI-agent slug when a known agent env var is set. */\nfunction userAgent(): string {\n  let ua = USER_AGENT;\n  for (const [envVar, slug] of Object.entries(AI_AGENT_ENV_VARS)) {\n    if (readEnv(envVar)) {\n      ua += ' agent/' + slug;\n      break;\n    }\n  }\n  return ua;\n}\n\nconst sleep = (ms: number): Promise<void> => new Promise((resolve) => setTimeout(resolve, ms));\n\n/** Milliseconds to wait before a retry attempt (0-based): Retry-After wins when honored,\n * else min(initial * multiplier**attempt, max) plus proportional random jitter. */\nfunction retryDelay(attempt: number, headers: Headers | null): number {\n  if (HONOR_RETRY_AFTER_HEADER && headers) {\n    const after = retryAfterMs(headers);\n    if (after !== null) return after;\n  }\n  const delay = Math.min(BACKOFF_INITIAL_DELAY_MS * BACKOFF_MULTIPLIER ** attempt, BACKOFF_MAX_DELAY_MS);\n  return delay + delay * BACKOFF_JITTER * Math.random();\n}\n\n/** A Retry-After header as milliseconds: the integer-seconds and the HTTP-date forms. */\nfunction retryAfterMs(headers: Headers): number | null {\n  const value = headers.get('Retry-After');\n  if (!value) return null;\n  const trimmed = value.trim();\n  if (/^\\d+$/.test(trimmed)) return Number(trimmed) * 1000;\n  const when = Date.parse(trimmed);\n  if (Number.isNaN(when)) return null;\n  return Math.max(0, when - Date.now());\n}\n\n/** Coerce a scalar to its wire string form (booleans lower-cased). */\nfunction textValue(value: unknown): string {\n  if (typeof value === 'boolean') return value ? 'true' : 'false';\n  if (value !== null && typeof value === 'object') return JSON.stringify(value);\n  return String(value);\n}\n\n/** Serialize an explode=false array query parameter as one comma-joined value. */\nexport function joinCsv(value: unknown): string | undefined {\n  if (value === undefined || value === null) return undefined;\n  if (!Array.isArray(value)) return textValue(value);\n  return value.map((item) => textValue(item)).join(',');\n}\n\n/** Encode a multipart/form-data body (built-in FormData): binary values become file parts. */\nfunction toFormData(payload: Record<string, unknown>): FormData {\n  const form = new FormData();\n  for (const [key, value] of Object.entries(payload)) {\n    if (value === undefined || value === null) continue;\n    for (const item of Array.isArray(value) ? value : [value]) {\n      if (item === undefined || item === null) continue;\n      if (item instanceof Blob) form.append(key, item);\n      else if (item instanceof Uint8Array) form.append(key, new Blob([item as BlobPart]), key);\n      else form.append(key, textValue(item));\n    }\n  }\n  return form;\n}\n\n/** Encode an application/x-www-form-urlencoded body (built-in URLSearchParams). */\nfunction toURLSearchParams(payload: Record<string, unknown>): URLSearchParams {\n  const params = new URLSearchParams();\n  for (const [key, value] of Object.entries(payload)) {\n    if (value === undefined || value === null) continue;\n    for (const item of Array.isArray(value) ? value : [value]) {\n      if (item === undefined || item === null) continue;\n      params.append(key, textValue(item));\n    }\n  }\n  return params;\n}\n\n/** Options for constructing a client. */\nexport interface ClientOptions {\n  /** API key; defaults to the configured environment variable. */\n  apiKey?: string;\n  /** Override the API base URL. */\n  baseURL?: string;\n  /** Per-request timeout in milliseconds (0 disables). Default: the sdk.timeout policy. */\n  timeout?: number;\n  /** A custom `fetch` implementation (defaults to the global `fetch`). */\n  fetch?: typeof fetch;\n}\n\n/** Per-call request overrides. */\nexport interface RequestOptions {\n  /** Extra headers merged onto (and overriding) the request's headers. */\n  headers?: Record<string, string | undefined>;\n  /** An AbortSignal to cancel the request. */\n  signal?: AbortSignal;\n}\n\n/** The wire encoding of a request body. */\nexport type BodyEncoding = 'json' | 'multipart' | 'form';\n\n/** The assembled arguments of a single API call (built by the resource methods). */\nexport interface RequestArgs {\n  method: string;\n  path: string;\n  /** `object` (not `Record<string, unknown>`) so a named params interface — which\n   * lacks an index signature — is assignable as the whole-object query shorthand. */\n  query?: object;\n  body?: unknown;\n  headers?: Record<string, unknown>;\n  /** The body wire encoding (default json). */\n  encoding?: BodyEncoding;\n  /** Return the raw `Response` without JSON-decoding (binary/stream downloads). */\n  raw?: boolean;\n  /** Inject a generated idempotency key (one per logical call, stable across retries). */\n  idempotency?: boolean;\n}\n\n/** Copy header entries onto `target`, coercing values and dropping undefined/null. */\nfunction assignHeaders(target: Record<string, string>, source: Record<string, unknown> | undefined): void {\n  for (const [key, value] of Object.entries(source ?? {})) {\n    if (value === undefined || value === null) continue;\n    target[key] = textValue(value);\n  }\n}\n\n/** The fetch-based transport shared by every resource: auth, retry/timeout/UA, JSON/form/multipart. */\nexport class APIClient {\n  protected readonly apiKey?: string;\n  protected readonly baseURL: string;\n  protected readonly fetchImpl: typeof fetch;\n  protected readonly timeoutMs: number;\n  protected readonly userAgent: string;\n\n  constructor(options: ClientOptions = {}) {\n    this.apiKey = options.apiKey;\n    this.baseURL = (options.baseURL ?? DEFAULT_BASE_URL).replace(/\\/+$/, '');\n    this.fetchImpl = options.fetch ?? fetch;\n    this.timeoutMs = options.timeout ?? defaultTimeout();\n    this.userAgent = userAgent();\n  }\n\n  /** A per-attempt timeout signal composed with the caller's signal (no AbortSignal.any dependency). */\n  private startTimeout(userSignal?: AbortSignal): { signal: AbortSignal | undefined; clear: () => void } {\n    if (!this.timeoutMs) return { signal: userSignal, clear: () => {} };\n    const controller = new AbortController();\n    const onAbort = () => controller.abort();\n    const timer = setTimeout(onAbort, this.timeoutMs);\n    if (userSignal) {\n      if (userSignal.aborted) controller.abort();\n      else userSignal.addEventListener('abort', onAbort, { once: true });\n    }\n    return {\n      signal: controller.signal,\n      clear: () => {\n        clearTimeout(timer);\n        if (userSignal) userSignal.removeEventListener('abort', onAbort);\n      },\n    };\n  }\n\n  /** Execute a request: build the URL + headers, attach auth, encode the body,\n   * run the retry loop, and decode the JSON response (or return the raw `Response`). */\n  async request<T>(args: RequestArgs, options?: RequestOptions): Promise<T> {\n    const url = new URL(this.baseURL + args.path);\n    if (args.query) {\n      for (const [key, value] of Object.entries(args.query as Record<string, unknown>)) {\n        if (value === undefined || value === null) continue;\n        if (Array.isArray(value)) {\n          for (const item of value) url.searchParams.append(key, textValue(item));\n        } else {\n          url.searchParams.set(key, textValue(value));\n        }\n      }\n    }\n\n    const headers: Record<string, string> = { Accept: 'application/json', 'User-Agent': this.userAgent };\n    assignHeaders(headers, args.headers);\n    assignHeaders(headers, options?.headers);\n    if (this.apiKey) headers['Authorization'] = 'Bearer ' + this.apiKey;\n\n    let body: BodyInit | undefined;\n    if (args.body !== undefined && args.body !== null) {\n      const encoding: BodyEncoding = args.encoding ?? 'json';\n      const payload = args.body as Record<string, unknown>;\n      if (encoding === 'multipart') {\n        // fetch sets the multipart Content-Type (with boundary) for a FormData body.\n        body = toFormData(payload);\n      } else if (encoding === 'form') {\n        headers['Content-Type'] = 'application/x-www-form-urlencoded';\n        body = toURLSearchParams(payload);\n      } else {\n        headers['Content-Type'] = 'application/json';\n        body = JSON.stringify(args.body);\n      }\n    }\n\n    if (args.idempotency && headers[IDEMPOTENCY_HEADER] === undefined) {\n      // Generated before the loop so every retry carries the SAME idempotency key.\n      headers[IDEMPOTENCY_HEADER] = crypto.randomUUID();\n    }\n\n    let attempt = 0;\n    for (;;) {\n      const timeout = this.startTimeout(options?.signal);\n      let response: Response;\n      try {\n        response = await this.fetchImpl(url.toString(), {\n          method: args.method,\n          headers,\n          body,\n          signal: timeout.signal,\n        });\n      } catch (error) {\n        timeout.clear();\n        if (RETRY_CONNECTION_ERRORS && attempt < MAX_RETRIES && !options?.signal?.aborted) {\n          await sleep(retryDelay(attempt, null));\n          attempt++;\n          continue;\n        }\n        throw error;\n      }\n      timeout.clear();\n\n      if (!response.ok) {\n        if (attempt < MAX_RETRIES && RETRYABLE_STATUS_CODES.has(response.status)) {\n          await sleep(retryDelay(attempt, response.headers));\n          attempt++;\n          continue;\n        }\n        const errorBody = await response.text();\n        throw errorForStatus(\n          response.status,\n          args.method + ' ' + args.path + ' failed with status ' + response.status,\n          response.headers,\n          errorBody,\n        );\n      }\n\n      if (args.raw) return response as unknown as T;\n      if (response.status === 204) return undefined as T;\n      const text = await response.text();\n      return (text ? JSON.parse(text) : undefined) as T;\n    }\n  }\n}\n",
      "language": "typescript"
    },
    {
      "path": "src/core/resource.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\nimport type { APIClient } from './request';\n\n/** The base class every resource (`client.pets`, ...) extends: it holds the client. */\nexport abstract class APIResource {\n  protected _client: APIClient;\n\n  constructor(client: APIClient) {\n    this._client = client;\n  }\n}\n",
      "language": "typescript"
    },
    {
      "path": "src/models.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\nexport type PetsListStatus = \"available\" | \"pending\" | \"sold\";\n\nexport interface Pet {\n  id: string;\n  name: string;\n  status?: PetsListStatus;\n  tags?: string[];\n}\n\nexport interface Error {\n  code?: string;\n  message: string;\n}\n\nexport interface NewPet {\n  name: string;\n  status?: PetsListStatus;\n}\n\nexport interface Order {\n  id: string;\n  petId: string;\n  quantity?: number;\n}\n\nexport interface NewOrder {\n  petId: string;\n  quantity?: number;\n}\n",
      "language": "typescript"
    },
    {
      "path": "src/resources/pets.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\nimport { APIResource } from '../core/resource';\nimport type { RequestOptions } from '../core/request';\nimport type { Pet, PetsListStatus } from '../models';\n\nexport class Pets extends APIResource {\n  /** List pets */\n  list(query?: PetListParams, options?: RequestOptions): Promise<Pet[]> {\n    return this._client.request<Pet[]>({ method: \"GET\", path: \"/pets\", query }, options);\n  }\n\n  /** Create a pet */\n  create(body: PetCreateParams, options?: RequestOptions): Promise<Pet> {\n    return this._client.request<Pet>({ method: \"POST\", path: \"/pets\", body }, options);\n  }\n\n  /** Retrieve a pet */\n  retrieve(petId: string, options?: RequestOptions): Promise<Pet> {\n    if (!petId) {\n      throw new Error(\"missing required petId parameter\");\n    }\n    return this._client.request<Pet>({ method: \"GET\", path: `/pets/${petId}` }, options);\n  }\n\n  /** Delete a pet */\n  delete(petId: string, options?: RequestOptions): Promise<void> {\n    if (!petId) {\n      throw new Error(\"missing required petId parameter\");\n    }\n    return this._client.request<void>({ method: \"DELETE\", path: `/pets/${petId}` }, options);\n  }\n}\n\nexport interface PetListParams {\n  limit?: number;\n  status?: PetsListStatus;\n}\n\nexport interface PetCreateParams {\n  name: string;\n  status?: PetsListStatus;\n}\n",
      "language": "typescript"
    },
    {
      "path": "src/resources/store.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\nimport { APIResource } from '../core/resource';\nimport type { APIClient, RequestOptions } from '../core/request';\nimport type { Order } from '../models';\n\nexport class Store extends APIResource {\n  readonly orders: StoreOrders;\n\n  constructor(client: APIClient) {\n    super(client);\n    this.orders = new StoreOrders(client);\n  }\n}\n\nexport class StoreOrders extends APIResource {\n  /** List store orders */\n  list(options?: RequestOptions): Promise<Order[]> {\n    return this._client.request<Order[]>({ method: \"GET\", path: \"/store/orders\" }, options);\n  }\n\n  /** Place a store order */\n  create(body: StoreOrderCreateParams, options?: RequestOptions): Promise<Order> {\n    return this._client.request<Order>({ method: \"POST\", path: \"/store/orders\", body }, options);\n  }\n}\n\nexport interface StoreOrderCreateParams {\n  petId: string;\n  quantity?: number;\n}\n",
      "language": "typescript"
    },
    {
      "path": "tests/_shims.d.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\ndeclare module 'node:test' {\n  interface TestContext {\n    skip(message?: string): void;\n  }\n  export function test(name: string, fn: (t: TestContext) => void | Promise<void>): void;\n  export function describe(name: string, fn: () => void): void;\n  export function it(name: string, fn: (t: TestContext) => void | Promise<void>): void;\n}\n\ndeclare module 'node:assert' {\n  interface AssertFn {\n    (value: unknown, message?: string): void;\n    ok(value: unknown, message?: string): void;\n    equal(actual: unknown, expected: unknown, message?: string): void;\n    deepEqual(actual: unknown, expected: unknown, message?: string): void;\n    rejects(\n      block: (() => Promise<unknown>) | Promise<unknown>,\n      error?: RegExp | ((err: unknown) => boolean),\n      message?: string,\n    ): Promise<void>;\n    throws(block: () => unknown, error?: RegExp | ((err: unknown) => boolean), message?: string): void;\n  }\n  const assert: AssertFn;\n  export default assert;\n}\n",
      "language": "typescript"
    },
    {
      "path": "tests/pets.test.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\nimport { test } from 'node:test';\nimport assert from 'node:assert';\n\nimport { assertMatchesType, checkTestServer, testClient } from './setup';\n\ntest(\"list (method)\", async (t) => {\n  if (!(await checkTestServer())) return t.skip();\n  const client = testClient();\n  const result = await client.pets.list();\n  assertMatchesType(result);\n});\n\ntest(\"list (method, all params)\", async (t) => {\n  if (!(await checkTestServer())) return t.skip();\n  const client = testClient();\n  const result = await client.pets.list({ limit: 0, status: \"available\" });\n  assertMatchesType(result);\n});\n\ntest(\"create (method)\", async (t) => {\n  if (!(await checkTestServer())) return t.skip();\n  const client = testClient();\n  const result = await client.pets.create({ name: \"x\" });\n  assertMatchesType(result);\n});\n\ntest(\"create (method, all params)\", async (t) => {\n  if (!(await checkTestServer())) return t.skip();\n  const client = testClient();\n  const result = await client.pets.create({ name: \"x\", status: \"available\" });\n  assertMatchesType(result);\n});\n\ntest(\"retrieve (method)\", async (t) => {\n  if (!(await checkTestServer())) return t.skip();\n  const client = testClient();\n  const result = await client.pets.retrieve(\"petId\");\n  assertMatchesType(result);\n});\n\ntest(\"retrieve (path params)\", () => {\n  const client = testClient();\n  assert.throws(() => client.pets.retrieve(\"\"), /missing required petId parameter/);\n});\n\ntest(\"delete (method)\", async (t) => {\n  if (!(await checkTestServer())) return t.skip();\n  const client = testClient();\n  await client.pets.delete(\"petId\");\n});\n\ntest(\"delete (path params)\", () => {\n  const client = testClient();\n  assert.throws(() => client.pets.delete(\"\"), /missing required petId parameter/);\n});\n",
      "language": "typescript"
    },
    {
      "path": "tests/setup.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\nimport assert from 'node:assert';\n\nimport Client from '../src/index';\n\n/** The spec-shaped mock base URL the generated suite runs against (openai-node's TEST_API_BASE_URL). */\nconst baseURL = readEnv('TEST_API_BASE_URL') ?? 'http://127.0.0.1:4010';\n\n/** Read an env var without a hard `@types/node` dependency (DOM lib only). */\nfunction readEnv(name: string): string | undefined {\n  const proc = (globalThis as { process?: { env?: Record<string, string | undefined> } }).process;\n  return proc?.env?.[name];\n}\n\n/** A Client pointed at the mock base URL, using the platform `fetch`. */\nexport function testClient(): Client {\n  return new Client({ apiKey: 'My API Key', baseURL });\n}\n\n/** Probe the mock server; a `false` skips the test (offline / no mock server). */\nexport async function checkTestServer(): Promise<boolean> {\n  try {\n    await fetch(baseURL);\n    return true;\n  } catch {\n    return false;\n  }\n}\n\n/** Pragmatic structural check: a method result (model, page container or raw Response) is present. */\nexport function assertMatchesType(value: unknown): void {\n  assert.ok(value !== undefined && value !== null, 'expected a response');\n}\n",
      "language": "typescript"
    },
    {
      "path": "tests/store.test.ts",
      "code": "// Code generated by opensdk. DO NOT EDIT.\n\nimport { test } from 'node:test';\n\nimport { assertMatchesType, checkTestServer, testClient } from './setup';\n\ntest(\"orders.list (method)\", async (t) => {\n  if (!(await checkTestServer())) return t.skip();\n  const client = testClient();\n  const result = await client.store.orders.list();\n  assertMatchesType(result);\n});\n\ntest(\"orders.create (method)\", async (t) => {\n  if (!(await checkTestServer())) return t.skip();\n  const client = testClient();\n  const result = await client.store.orders.create({ petId: \"x\" });\n  assertMatchesType(result);\n});\n\ntest(\"orders.create (method, all params)\", async (t) => {\n  if (!(await checkTestServer())) return t.skip();\n  const client = testClient();\n  const result = await client.store.orders.create({ petId: \"x\", quantity: 0 });\n  assertMatchesType(result);\n});\n",
      "language": "typescript"
    },
    {
      "path": "tsconfig.json",
      "code": "{\n  \"compilerOptions\": {\n    \"target\": \"ES2022\",\n    \"module\": \"ESNext\",\n    \"moduleResolution\": \"bundler\",\n    \"lib\": [\n      \"ES2022\",\n      \"DOM\"\n    ],\n    \"strict\": true,\n    \"esModuleInterop\": true,\n    \"skipLibCheck\": true,\n    \"forceConsistentCasingInFileNames\": true,\n    \"declaration\": true,\n    \"outDir\": \"./dist\"\n  },\n  \"include\": [\n    \"src\"\n  ]\n}\n",
      "language": "json"
    },
    {
      "path": "tsconfig.test.json",
      "code": "{\n  \"extends\": \"./tsconfig.json\",\n  \"compilerOptions\": {\n    \"noEmit\": true\n  },\n  \"include\": [\n    \"src\",\n    \"tests\"\n  ]\n}\n",
      "language": "json"
    }
  ],
  "usage": "import AcmeSdk from \"@acme/sdk\";\n\nconst client = new AcmeSdk({\n\tapiKey: process.env[\"ACME_API_KEY\"],\n});\n\nconst result = await client.pets.list({ limit: 1, status: \"available\" });\nconsole.log(result);\n",
  "operations": [
    {
      "id": "pets.list",
      "action": "list",
      "httpMethod": "GET",
      "path": "/pets",
      "code": "import AcmeSdk from \"@acme/sdk\";\n\nconst client = new AcmeSdk({\n\tapiKey: process.env[\"ACME_API_KEY\"],\n});\n\nconst result = await client.pets.list({ limit: 1, status: \"available\" });\nconsole.log(result);\n"
    },
    {
      "id": "pets.create",
      "action": "create",
      "httpMethod": "POST",
      "path": "/pets",
      "code": "import AcmeSdk from \"@acme/sdk\";\n\nconst client = new AcmeSdk({\n\tapiKey: process.env[\"ACME_API_KEY\"],\n});\n\nconst result = await client.pets.create({\n\tname: \"string\",\n\tstatus: \"available\",\n});\nconsole.log(result);\n"
    },
    {
      "id": "pets.retrieve",
      "action": "retrieve",
      "httpMethod": "GET",
      "path": "/pets/{petId}",
      "code": "import AcmeSdk from \"@acme/sdk\";\n\nconst client = new AcmeSdk({\n\tapiKey: process.env[\"ACME_API_KEY\"],\n});\n\nconst result = await client.pets.retrieve(\"petId\");\nconsole.log(result);\n"
    },
    {
      "id": "pets.delete",
      "action": "delete",
      "httpMethod": "DELETE",
      "path": "/pets/{petId}",
      "code": "import AcmeSdk from \"@acme/sdk\";\n\nconst client = new AcmeSdk({\n\tapiKey: process.env[\"ACME_API_KEY\"],\n});\n\nawait client.pets.delete(\"petId\");\n"
    },
    {
      "id": "store.orders.list",
      "action": "list",
      "httpMethod": "GET",
      "path": "/store/orders",
      "code": "import AcmeSdk from \"@acme/sdk\";\n\nconst client = new AcmeSdk({\n\tapiKey: process.env[\"ACME_API_KEY\"],\n});\n\nconst result = await client.store.orders.list();\nconsole.log(result);\n"
    },
    {
      "id": "store.orders.create",
      "action": "create",
      "httpMethod": "POST",
      "path": "/store/orders",
      "code": "import AcmeSdk from \"@acme/sdk\";\n\nconst client = new AcmeSdk({\n\tapiKey: process.env[\"ACME_API_KEY\"],\n});\n\nconst result = await client.store.orders.create({\n\tpetId: \"string\",\n\tquantity: 1,\n});\nconsole.log(result);\n"
    }
  ],
  "defaultOperationId": "pets.list"
}

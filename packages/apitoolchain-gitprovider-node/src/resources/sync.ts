// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { RequestOptions } from '../core/request';
import type { FileEntry, ProviderKind, Signature, SyncResult } from '../models';

export class Sync extends APIResource {
  create(body: SyncCreateParams, options?: RequestOptions): Promise<SyncResult> {
    return this._client.request<SyncResult>({ method: "POST", path: "/sync", body }, options);
  }
}

export interface SyncCreateParams {
  kind: ProviderKind;
  baseUrl: string;
  token: string;
  login?: string;
  repo: string;
  branch: string;
  prefix: string;
  message: string;
  author: Signature;
  files: FileEntry[];
}

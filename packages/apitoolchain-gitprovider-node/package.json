{
  "name": "@apitoolchain/gitprovider-node",
  "version": "v1",
  "description": "apitoolchain gitproviderd — the HTTP wire contract for the Go git-provider\nservice. Provider credentials are inlined into every request body (kind,\nbaseUrl, token, login); there is no shared auth scheme.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.2"
  }
}

// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("members")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("role")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("update")
                        .arg(
                            Arg::new("user-id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("role")
                                .long("role")
                                .required(true),
                        ),
                ),
        )
        .subcommand(Command::new("list"))
        .subcommand(
            Command::new("create")
                .arg(
                    Arg::new("email")
                        .long("email")
                        .required(true),
                )
                .arg(
                    Arg::new("role")
                        .long("role"),
                ),
        )
        .subcommand(
            Command::new("delete")
                .arg(
                    Arg::new("user-id")
                        .required(true),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["members", "role", "update"] => handle_members_role_update(ctx, o, cmd_path, m).await,
        ["members", "list"] => handle_members_list(ctx, o, cmd_path, m).await,
        ["members", "create"] => handle_members_create(ctx, o, cmd_path, m).await,
        ["members", "delete"] => handle_members_delete(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_members_role_update<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let user_id = m.get_one::<String>("user-id").map(String::as_str).unwrap_or("");
    let path = format!("/members/{}/role", runtime::path_escape(user_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("role") {
        body.insert("role".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "PATCH",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_members_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/members".to_string();
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_members_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/members".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("email") {
        body.insert("email".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("role") {
        body.insert("role".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_members_delete<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let user_id = m.get_one::<String>("user-id").map(String::as_str).unwrap_or("");
    let path = format!("/members/{}", runtime::path_escape(user_id));
    let req = runtime::Request {
        method: "DELETE",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("sdks")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("build")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("create")
                        .arg(
                            Arg::new("sdk-id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("api-version")
                                .long("api-version")
                                .help("API version to build every target from; defaults to the API's current."),
                        ),
                ),
        )
        .subcommand(
            Command::new("targets")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("list")
                        .arg(
                            Arg::new("sdk-id")
                                .required(true),
                        ),
                )
                .subcommand(
                    Command::new("create")
                        .arg(
                            Arg::new("sdk-id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("language")
                                .long("language")
                                .required(true),
                        )
                        .arg(
                            Arg::new("package-name")
                                .long("package-name"),
                        )
                        .arg(
                            Arg::new("version")
                                .long("version")
                                .help("API version to generate from; defaults to the API's current version."),
                        )
                        .arg(
                            Arg::new("sdk-json")
                                .long("sdk-json")
                                .help("A full custom sdk.json (behavior + per-language emitter options), serialized.\nWhen set it's applied over the derived defaults during generation (the\n\"wizard\" flow); empty → the auto-derived config."),
                        ),
                ),
        )
        .subcommand(
            Command::new("versions")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("list")
                        .about("SDK version/build history (newest first).")
                        .arg(
                            Arg::new("sdk-id")
                                .required(true),
                        ),
                )
                .subcommand(
                    Command::new("create")
                        .about("Create a new SDK version: bump + rebuild every target, recorded as a build.")
                        .arg(
                            Arg::new("sdk-id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("version")
                                .long("version")
                                .help("The new SDK version (e.g. \"0.2.0\").")
                                .required(true),
                        )
                        .arg(
                            Arg::new("api-version")
                                .long("api-version")
                                .help("API version to build from; defaults to the API's current."),
                        ),
                ),
        )
        .subcommand(
            Command::new("list")
                .arg(
                    Arg::new("api-id")
                        .long("api-id"),
                ),
        )
        .subcommand(
            Command::new("create")
                .arg(
                    Arg::new("api-id")
                        .long("api-id")
                        .required(true),
                )
                .arg(
                    Arg::new("name")
                        .long("name"),
                )
                .arg(
                    Arg::new("id")
                        .long("id")
                        .help("Explicit id/slug for the SDK. Slugified server-side; defaults to a slug of\n`name`. Lets the display title (`name`) be decoupled from the stable id."),
                )
                .arg(
                    Arg::new("description")
                        .long("description"),
                )
                .arg(
                    Arg::new("ns")
                        .long("ns"),
                ),
        )
        .subcommand(
            Command::new("retrieve")
                .visible_alias("get")
                .arg(
                    Arg::new("id")
                        .required(true),
                ),
        )
        .subcommand(
            Command::new("delete")
                .arg(
                    Arg::new("id")
                        .required(true),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["sdks", "build", "create"] => handle_sdks_build_create(ctx, o, cmd_path, m).await,
        ["sdks", "targets", "list"] => handle_sdks_targets_list(ctx, o, cmd_path, m).await,
        ["sdks", "targets", "create"] => handle_sdks_targets_create(ctx, o, cmd_path, m).await,
        ["sdks", "versions", "list"] => handle_sdks_versions_list(ctx, o, cmd_path, m).await,
        ["sdks", "versions", "create"] => handle_sdks_versions_create(ctx, o, cmd_path, m).await,
        ["sdks", "list"] => handle_sdks_list(ctx, o, cmd_path, m).await,
        ["sdks", "create"] => handle_sdks_create(ctx, o, cmd_path, m).await,
        ["sdks", "retrieve"] => handle_sdks_retrieve(ctx, o, cmd_path, m).await,
        ["sdks", "delete"] => handle_sdks_delete(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_sdks_build_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let sdk_id = m.get_one::<String>("sdk-id").map(String::as_str).unwrap_or("");
    let path = format!("/sdks/{}/build", runtime::path_escape(sdk_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("api-version") {
        body.insert("apiVersion".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_sdks_targets_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let sdk_id = m.get_one::<String>("sdk-id").map(String::as_str).unwrap_or("");
    let path = format!("/sdks/{}/targets", runtime::path_escape(sdk_id));
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_sdks_targets_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let sdk_id = m.get_one::<String>("sdk-id").map(String::as_str).unwrap_or("");
    let path = format!("/sdks/{}/targets", runtime::path_escape(sdk_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("language") {
        body.insert("language".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("package-name") {
        body.insert("packageName".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("version") {
        body.insert("version".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("sdk-json") {
        body.insert("sdkJson".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_sdks_versions_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let sdk_id = m.get_one::<String>("sdk-id").map(String::as_str).unwrap_or("");
    let path = format!("/sdks/{}/versions", runtime::path_escape(sdk_id));
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_sdks_versions_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let sdk_id = m.get_one::<String>("sdk-id").map(String::as_str).unwrap_or("");
    let path = format!("/sdks/{}/versions", runtime::path_escape(sdk_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("version") {
        body.insert("version".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("api-version") {
        body.insert("apiVersion".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_sdks_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/sdks".to_string();
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("api-id") {
        query.push(("apiId", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_sdks_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/sdks".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("api-id") {
        body.insert("apiId".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("id") {
        body.insert("id".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("description") {
        body.insert("description".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("ns") {
        body.insert("ns".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_sdks_retrieve<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/sdks/{}", runtime::path_escape(id));
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_sdks_delete<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/sdks/{}", runtime::path_escape(id));
    let req = runtime::Request {
        method: "DELETE",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

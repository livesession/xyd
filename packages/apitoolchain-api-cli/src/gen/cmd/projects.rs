// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("projects")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(Command::new("list"))
        .subcommand(
            Command::new("create")
                .arg(
                    Arg::new("name")
                        .long("name")
                        .required(true),
                ),
        )
        .subcommand(
            Command::new("update")
                .arg(
                    Arg::new("id")
                        .required(true),
                )
                .arg(
                    Arg::new("name")
                        .long("name")
                        .required(true),
                ),
        )
        .subcommand(
            Command::new("delete")
                .arg(
                    Arg::new("id")
                        .required(true),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["projects", "list"] => handle_projects_list(ctx, o, cmd_path, m).await,
        ["projects", "create"] => handle_projects_create(ctx, o, cmd_path, m).await,
        ["projects", "update"] => handle_projects_update(ctx, o, cmd_path, m).await,
        ["projects", "delete"] => handle_projects_delete(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_projects_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/projects".to_string();
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_projects_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/projects".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_projects_update<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/projects/{}", runtime::path_escape(id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "PATCH",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_projects_delete<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/projects/{}", runtime::path_escape(id));
    let req = runtime::Request {
        method: "DELETE",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("git-providers")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("branches")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("list")
                        .about("Branch names of one repo (for the branch picker after a repo is chosen).")
                        .arg(
                            Arg::new("id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("repo")
                                .long("repo")
                                .required(true),
                        ),
                ),
        )
        .subcommand(
            Command::new("repos")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("list")
                        .about("Repos the provider's token can access (for the connect picker).")
                        .arg(
                            Arg::new("id")
                                .required(true),
                        ),
                ),
        )
        .subcommand(Command::new("list"))
        .subcommand(
            Command::new("create")
                .arg(
                    Arg::new("kind")
                        .long("kind")
                        .help("The git platforms the gitprovider service can connect to.")
                        .required(true),
                )
                .arg(
                    Arg::new("name")
                        .long("name"),
                )
                .arg(
                    Arg::new("base-url")
                        .long("base-url")
                        .help("Provider base URL; optional for github.com/gitlab.com, required for self-hosted."),
                )
                .arg(
                    Arg::new("token")
                        .long("token")
                        .required(true),
                ),
        )
        .subcommand(
            Command::new("delete")
                .arg(
                    Arg::new("id")
                        .required(true),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["git-providers", "branches", "list"] => handle_git_providers_branches_list(ctx, o, cmd_path, m).await,
        ["git-providers", "repos", "list"] => handle_git_providers_repos_list(ctx, o, cmd_path, m).await,
        ["git-providers", "list"] => handle_git_providers_list(ctx, o, cmd_path, m).await,
        ["git-providers", "create"] => handle_git_providers_create(ctx, o, cmd_path, m).await,
        ["git-providers", "delete"] => handle_git_providers_delete(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_git_providers_branches_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/git-providers/{}/branches", runtime::path_escape(id));
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("repo") {
        query.push(("repo", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_git_providers_repos_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/git-providers/{}/repos", runtime::path_escape(id));
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_git_providers_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/git-providers".to_string();
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_git_providers_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/git-providers".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("kind") {
        body.insert("kind".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("name") {
        body.insert("name".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("base-url") {
        body.insert("baseUrl".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("token") {
        body.insert("token".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_git_providers_delete<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/git-providers/{}", runtime::path_escape(id));
    let req = runtime::Request {
        method: "DELETE",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

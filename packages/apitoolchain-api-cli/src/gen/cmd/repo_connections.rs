// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("repo-connections")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("release-config")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("create")
                        .about("Switch a connection between direct-push and PR-based release mode.")
                        .arg(
                            Arg::new("id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("release-mode")
                                .long("release-mode")
                                .required(true),
                        )
                        .arg(
                            Arg::new("auto-release")
                                .long("auto-release")
                                .num_args(0..=1)
                                .require_equals(true)
                                .default_missing_value("true")
                                .value_parser(clap::value_parser!(bool))
                                .required(true),
                        )
                        .arg(
                            Arg::new("base-branch")
                                .long("base-branch"),
                        )
                        .arg(
                            Arg::new("prerelease")
                                .long("prerelease")
                                .num_args(0..=1)
                                .require_equals(true)
                                .default_missing_value("true")
                                .value_parser(clap::value_parser!(bool)),
                        )
                        .arg(
                            Arg::new("dist-tags")
                                .long("dist-tags")
                                .help("Comma-separated dist-tags this connection reacts to (default `latest`);\n`*` = all. Only a version published under a matching tag triggers a\nrelease."),
                        ),
                ),
        )
        .subcommand(
            Command::new("sync")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("create")
                        .arg(
                            Arg::new("id")
                                .required(true),
                        ),
                ),
        )
        .subcommand(
            Command::new("list")
                .arg(
                    Arg::new("target-kind")
                        .long("target-kind"),
                )
                .arg(
                    Arg::new("target-id")
                        .long("target-id"),
                ),
        )
        .subcommand(
            Command::new("create")
                .arg(
                    Arg::new("provider-id")
                        .long("provider-id")
                        .required(true),
                )
                .arg(
                    Arg::new("target-kind")
                        .long("target-kind")
                        .help("What a repo connection syncs into a repo.")
                        .required(true),
                )
                .arg(
                    Arg::new("target-id")
                        .long("target-id")
                        .required(true),
                )
                .arg(
                    Arg::new("ref")
                        .long("ref"),
                )
                .arg(
                    Arg::new("repo")
                        .long("repo")
                        .help("For an existing repo: \"owner/name\". For a new repo: just the name.")
                        .required(true),
                )
                .arg(
                    Arg::new("create-repo")
                        .long("create-repo")
                        .num_args(0..=1)
                        .require_equals(true)
                        .default_missing_value("true")
                        .value_parser(clap::value_parser!(bool))
                        .help("When true, create `repo` (a name) under the account before connecting."),
                )
                .arg(
                    Arg::new("make-private")
                        .long("make-private")
                        .num_args(0..=1)
                        .require_equals(true)
                        .default_missing_value("true")
                        .value_parser(clap::value_parser!(bool))
                        .help("Visibility for a newly-created repo."),
                )
                .arg(
                    Arg::new("branch")
                        .long("branch"),
                )
                .arg(
                    Arg::new("prefix")
                        .long("prefix"),
                )
                .arg(
                    Arg::new("release-mode")
                        .long("release-mode"),
                )
                .arg(
                    Arg::new("auto-release")
                        .long("auto-release")
                        .num_args(0..=1)
                        .require_equals(true)
                        .default_missing_value("true")
                        .value_parser(clap::value_parser!(bool)),
                )
                .arg(
                    Arg::new("base-branch")
                        .long("base-branch"),
                )
                .arg(
                    Arg::new("dist-tags")
                        .long("dist-tags")
                        .help("Comma-separated dist-tags to react to (default `latest`; `*` = all)."),
                ),
        )
        .subcommand(
            Command::new("delete")
                .arg(
                    Arg::new("id")
                        .required(true),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["repo-connections", "release-config", "create"] => handle_repo_connections_release_config_create(ctx, o, cmd_path, m).await,
        ["repo-connections", "sync", "create"] => handle_repo_connections_sync_create(ctx, o, cmd_path, m).await,
        ["repo-connections", "list"] => handle_repo_connections_list(ctx, o, cmd_path, m).await,
        ["repo-connections", "create"] => handle_repo_connections_create(ctx, o, cmd_path, m).await,
        ["repo-connections", "delete"] => handle_repo_connections_delete(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_repo_connections_release_config_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/repo-connections/{}/release-config", runtime::path_escape(id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("release-mode") {
        body.insert("releaseMode".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<bool>("auto-release") {
        body.insert("autoRelease".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<String>("base-branch") {
        body.insert("baseBranch".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<bool>("prerelease") {
        body.insert("prerelease".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<String>("dist-tags") {
        body.insert("distTags".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_repo_connections_sync_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/repo-connections/{}/sync", runtime::path_escape(id));
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_repo_connections_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/repo-connections".to_string();
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("target-kind") {
        query.push(("targetKind", v.clone()));
    }
    if let Some(v) = m.get_one::<String>("target-id") {
        query.push(("targetId", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_repo_connections_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/repo-connections".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("provider-id") {
        body.insert("providerId".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("target-kind") {
        body.insert("targetKind".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("target-id") {
        body.insert("targetId".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("ref") {
        body.insert("ref".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("repo") {
        body.insert("repo".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<bool>("create-repo") {
        body.insert("createRepo".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<bool>("make-private") {
        body.insert("makePrivate".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<String>("branch") {
        body.insert("branch".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("prefix") {
        body.insert("prefix".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("release-mode") {
        body.insert("releaseMode".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<bool>("auto-release") {
        body.insert("autoRelease".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<String>("base-branch") {
        body.insert("baseBranch".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("dist-tags") {
        body.insert("distTags".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_repo_connections_delete<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let id = m.get_one::<String>("id").map(String::as_str).unwrap_or("");
    let path = format!("/repo-connections/{}", runtime::path_escape(id));
    let req = runtime::Request {
        method: "DELETE",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("context")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("select")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("create")
                        .about("Switch the caller's current project (persisted per-user).")
                        .arg(
                            Arg::new("project-id")
                                .long("project-id")
                                .required(true),
                        ),
                ),
        )
        .subcommand(Command::new("list"))
        .subcommand(
            Command::new("update")
                .arg(
                    Arg::new("org-name")
                        .long("org-name"),
                )
                .arg(
                    Arg::new("project-name")
                        .long("project-name"),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["context", "select", "create"] => handle_context_select_create(ctx, o, cmd_path, m).await,
        ["context", "list"] => handle_context_list(ctx, o, cmd_path, m).await,
        ["context", "update"] => handle_context_update(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_context_select_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/context/select".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("project-id") {
        body.insert("projectId".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_context_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    _m: &ArgMatches,
) -> ExitCode {
    let path = "/context".to_string();
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_context_update<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/context".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("org-name") {
        body.insert("orgName".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("project-name") {
        body.insert("projectName".to_string(), serde_json::Value::String(v.clone()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "PATCH",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.uniondepot;

/**
 * A dependency-free assertion test for EventService — run with plain `java EventServiceTest`.
 */
public final class EventServiceTest {
  private static final String BASE_URL =
      System.getenv().getOrDefault("TEST_API_BASE_URL", "http://localhost:4010");

  public static void main(String[] args) {
    Client client = Client.builder().baseUrl(BASE_URL).apiKey("My API Key").build();
    testEventListWithOptionalParams(client);
    testEventCreateWithOptionalParams(client);
    System.out.println("EventServiceTest OK");
  }

  private static void testEventListWithOptionalParams(Client client) {
    try {
      client.events().list(EventListParams.builder()
          .after("after")
          .limit(0L)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testEventCreateWithOptionalParams(Client client) {
    try {
      client.events().create(EventCreateParams.builder()
          .name("x")
          .priority(0L)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }
}

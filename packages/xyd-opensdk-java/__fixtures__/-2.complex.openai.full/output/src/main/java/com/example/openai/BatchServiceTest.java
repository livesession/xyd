// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * A dependency-free assertion test for BatchService — run with plain `java BatchServiceTest`.
 */
public final class BatchServiceTest {
  private static final String BASE_URL =
      System.getenv().getOrDefault("TEST_API_BASE_URL", "http://localhost:4010");

  public static void main(String[] args) {
    Client client = Client.builder().baseUrl(BASE_URL).apiKey("My API Key").build();
    testBatchCancel(client);
    testBatchCancelPathParams(client);
    testBatchCreateWithOptionalParams(client);
    testBatchListWithOptionalParams(client);
    testBatchRetrieve(client);
    testBatchRetrievePathParams(client);
    System.out.println("BatchServiceTest OK");
  }

  private static void testBatchCancel(Client client) {
    try {
      client.batches().cancel("batch_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testBatchCancelPathParams(Client client) {
    try {
      client.batches().cancel("");
      throw new AssertionError("expected a missing required path param error for cancel");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testBatchCreateWithOptionalParams(Client client) {
    try {
      client.batches().create(BatchCreateParams.builder()
          .inputFileId("x")
          .endpoint(BatchesCreateRequestEndpoint.V1_RESPONSES)
          .metadata(java.util.Map.of("key", "x"))
          .outputExpiresAfter(new BatchFileExpirationAfter())
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testBatchListWithOptionalParams(Client client) {
    try {
      client.batches().list(BatchListParams.builder()
          .after("after")
          .limit(0L)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testBatchRetrieve(Client client) {
    try {
      client.batches().retrieve("batch_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testBatchRetrievePathParams(Client client) {
    try {
      client.batches().retrieve("");
      throw new AssertionError("expected a missing required path param error for retrieve");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void requireContains(String actual, String needle) {
    if (actual == null || !actual.contains(needle)) {
      throw new AssertionError("missing substring [" + needle + "] in: " + actual);
    }
  }
}

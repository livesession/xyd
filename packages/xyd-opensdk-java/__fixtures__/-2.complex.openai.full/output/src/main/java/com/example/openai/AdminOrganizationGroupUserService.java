// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AdminOrganizationGroupUserService {
  private final Transport transport;

  AdminOrganizationGroupUserService(Transport transport) {
    this.transport = transport;
  }

  /** Adds a user to a group. */
  public GroupUserAssignment create(String groupId, AdminOrganizationGroupUserCreateParams params) {
    if (groupId == null || groupId.isEmpty()) {
      throw new IllegalArgumentException("missing required group_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/organization/groups/" + Transport.encodePath(groupId) + "/users");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("user_id", params.userId());
    request.body(body);
    return GroupUserAssignment.fromJson(transport.execute(request));
  }

  /** Removes a user from a group. */
  public GroupUserDeletedResource delete(String groupId, String userId) {
    if (groupId == null || groupId.isEmpty()) {
      throw new IllegalArgumentException("missing required group_id parameter");
    }
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/organization/groups/" + Transport.encodePath(groupId) + "/users/" + Transport.encodePath(userId));
    return GroupUserDeletedResource.fromJson(transport.execute(request));
  }

  /** Lists the users assigned to a group. */
  public CursorPage<GroupUser> list(String groupId, AdminOrganizationGroupUserListParams params) {
    if (groupId == null || groupId.isEmpty()) {
      throw new IllegalArgumentException("missing required group_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/organization/groups/" + Transport.encodePath(groupId) + "/users");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> GroupUser.fromJson(__e));
  }
}

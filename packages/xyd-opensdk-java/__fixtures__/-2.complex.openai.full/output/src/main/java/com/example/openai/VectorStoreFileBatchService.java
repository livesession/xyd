// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class VectorStoreFileBatchService {
  private final Transport transport;

  VectorStoreFileBatchService(Transport transport) {
    this.transport = transport;
  }

  /** Cancel a vector store file batch. This attempts to cancel the processing of files in this batch as soon as possible. */
  public VectorStoreFileBatchObject cancel(String vectorStoreId, String batchId) {
    if (vectorStoreId == null || vectorStoreId.isEmpty()) {
      throw new IllegalArgumentException("missing required vector_store_id parameter");
    }
    if (batchId == null || batchId.isEmpty()) {
      throw new IllegalArgumentException("missing required batch_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/vector_stores/" + Transport.encodePath(vectorStoreId) + "/file_batches/" + Transport.encodePath(batchId) + "/cancel");
    return VectorStoreFileBatchObject.fromJson(transport.execute(request));
  }

  /**
   * The maximum number of files in a single batch request is 2000.
   * Vector store file attach requests are rate limited per vector store (300 requests per minute across both this endpoint and `/vector_stores/{vector_store_id}/files`).
   * For ingesting multiple files into the same vector store, this batch endpoint is recommended.
   */
  public VectorStoreFileBatchObject create(String vectorStoreId) {
    if (vectorStoreId == null || vectorStoreId.isEmpty()) {
      throw new IllegalArgumentException("missing required vector_store_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/vector_stores/" + Transport.encodePath(vectorStoreId) + "/file_batches");
    return VectorStoreFileBatchObject.fromJson(transport.execute(request));
  }

  /** Returns a list of vector store files in a batch. */
  public CursorPage<VectorStoreFileObject> listFiles(String vectorStoreId, String batchId, VectorStoreFileBatchListFilesParams params) {
    if (vectorStoreId == null || vectorStoreId.isEmpty()) {
      throw new IllegalArgumentException("missing required vector_store_id parameter");
    }
    if (batchId == null || batchId.isEmpty()) {
      throw new IllegalArgumentException("missing required batch_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/vector_stores/" + Transport.encodePath(vectorStoreId) + "/file_batches/" + Transport.encodePath(batchId) + "/files");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.before().ifPresent(value -> request.query("before", String.valueOf(value)));
    params.filter().ifPresent(value -> request.query("filter", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> VectorStoreFileObject.fromJson(__e));
  }

  /** Retrieves a vector store file batch. */
  public VectorStoreFileBatchObject retrieve(String vectorStoreId, String batchId) {
    if (vectorStoreId == null || vectorStoreId.isEmpty()) {
      throw new IllegalArgumentException("missing required vector_store_id parameter");
    }
    if (batchId == null || batchId.isEmpty()) {
      throw new IllegalArgumentException("missing required batch_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/vector_stores/" + Transport.encodePath(vectorStoreId) + "/file_batches/" + Transport.encodePath(batchId));
    return VectorStoreFileBatchObject.fromJson(transport.execute(request));
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.Map;

/** Decode helper for the InputItem union, selecting the concrete variant by its "type" discriminator. */
public final class InputItem {
  private InputItem() {
  }

  /**
   * Decode JSON into the concrete InputItem variant selected by the "type"
   * discriminator. An unknown or absent discriminator keeps the raw parsed value.
   */
  public static Object fromJson(Object json) {
    if (!(json instanceof Map)) {
      return json;
    }
    Object discriminator = ((Map<?, ?>) json).get("type");
    if ("message".equals(discriminator)) {
      return EasyInputMessage.fromJson(json);
    }
    return json;
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * Labels an `assistant` message as intermediate commentary (`commentary`) or the final answer (`final_answer`).
 * For models like `gpt-5.3-codex` and beyond, when sending follow-up requests, preserve and resend
 * phase on all assistant messages — dropping it can degrade performance. Not used for user messages.
 */
public enum MessagePhase implements Json.JsonEnum {
  COMMENTARY("commentary"),
  FINAL_ANSWER("final_answer");

  private final String value;

  MessagePhase(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static MessagePhase fromValue(String value) {
    for (MessagePhase item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown MessagePhase value: " + value);
  }

  public static MessagePhase fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AudioVoiceService {
  private final Transport transport;

  AudioVoiceService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Create a custom voice you can use for audio output (for example, in Text-to-Speech and the Realtime API). This requires an audio sample and a previously uploaded consent recording.
   * 
   * See the [custom voices guide](/docs/guides/text-to-speech#custom-voices) for requirements and best practices. Custom voices are limited to eligible customers.
   */
  public VoiceResource create(AudioVoiceCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/audio/voices");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("name", params.name());
    body.put("audio_sample", params.audioSample());
    body.put("consent", params.consent());
    request.body(body);
    request.encoding("multipart");
    return VoiceResource.fromJson(transport.execute(request));
  }
}

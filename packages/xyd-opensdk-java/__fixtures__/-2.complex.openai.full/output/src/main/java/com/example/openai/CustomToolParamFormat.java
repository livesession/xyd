// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.Map;

/** The input format for the custom tool. Default is unconstrained text. */
public final class CustomToolParamFormat {
  private CustomToolParamFormat() {
  }

  /**
   * Decode JSON into the concrete CustomToolParamFormat variant selected by the "type"
   * discriminator. An unknown or absent discriminator keeps the raw parsed value.
   */
  public static Object fromJson(Object json) {
    if (!(json instanceof Map)) {
      return json;
    }
    Object discriminator = ((Map<?, ?>) json).get("type");
    if ("grammar".equals(discriminator)) {
      return CustomGrammarFormatParam.fromJson(json);
    }
    if ("text".equals(discriminator)) {
      return CustomTextFormatParam.fromJson(json);
    }
    return json;
  }
}

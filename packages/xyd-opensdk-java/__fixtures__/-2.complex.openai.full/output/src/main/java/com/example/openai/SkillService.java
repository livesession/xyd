// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class SkillService {
  private final Transport transport;
  private final SkillVersionService versions;

  SkillService(Transport transport) {
    this.transport = transport;
    this.versions = new SkillVersionService(transport);
  }

  public SkillVersionService versions() {
    return versions;
  }

  /** Download a skill zip bundle by its ID. */
  public String content(String skillId) {
    if (skillId == null || skillId.isEmpty()) {
      throw new IllegalArgumentException("missing required skill_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/skills/" + Transport.encodePath(skillId) + "/content");
    return Json.asString(transport.execute(request));
  }

  /** Create a new skill. */
  public SkillResource create(SkillCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/skills");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("files", params.files());
    request.body(body);
    request.encoding("multipart");
    return SkillResource.fromJson(transport.execute(request));
  }

  /** Delete a skill by its ID. */
  public DeletedSkillResource delete(String skillId) {
    if (skillId == null || skillId.isEmpty()) {
      throw new IllegalArgumentException("missing required skill_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/skills/" + Transport.encodePath(skillId));
    return DeletedSkillResource.fromJson(transport.execute(request));
  }

  /** List all skills for the current project. */
  public CursorPage<SkillResource> list(SkillListParams params) {
    Transport.Request request = new Transport.Request("GET", "/skills");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> SkillResource.fromJson(__e));
  }

  /** Get a skill by its ID. */
  public SkillResource retrieve(String skillId) {
    if (skillId == null || skillId.isEmpty()) {
      throw new IllegalArgumentException("missing required skill_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/skills/" + Transport.encodePath(skillId));
    return SkillResource.fromJson(transport.execute(request));
  }

  /** Update the default version pointer for a skill. */
  public SkillResource update(String skillId, SkillUpdateParams params) {
    if (skillId == null || skillId.isEmpty()) {
      throw new IllegalArgumentException("missing required skill_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/skills/" + Transport.encodePath(skillId));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("default_version", params.defaultVersion());
    request.body(body);
    return SkillResource.fromJson(transport.execute(request));
  }
}

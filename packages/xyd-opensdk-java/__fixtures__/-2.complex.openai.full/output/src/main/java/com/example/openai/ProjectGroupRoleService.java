// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ProjectGroupRoleService {
  private final Transport transport;

  ProjectGroupRoleService(Transport transport) {
    this.transport = transport;
  }

  /** Assigns a project role to a group within a project. */
  public GroupRoleAssignment create(String projectId, String groupId, ProjectGroupRoleCreateParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (groupId == null || groupId.isEmpty()) {
      throw new IllegalArgumentException("missing required group_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/projects/" + Transport.encodePath(projectId) + "/groups/" + Transport.encodePath(groupId) + "/roles");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("role_id", params.roleId());
    request.body(body);
    return GroupRoleAssignment.fromJson(transport.execute(request));
  }

  /** Unassigns a project role from a group within a project. */
  public DeletedRoleAssignmentResource delete(String projectId, String groupId, String roleId) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (groupId == null || groupId.isEmpty()) {
      throw new IllegalArgumentException("missing required group_id parameter");
    }
    if (roleId == null || roleId.isEmpty()) {
      throw new IllegalArgumentException("missing required role_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/projects/" + Transport.encodePath(projectId) + "/groups/" + Transport.encodePath(groupId) + "/roles/" + Transport.encodePath(roleId));
    return DeletedRoleAssignmentResource.fromJson(transport.execute(request));
  }

  /** Lists the project roles assigned to a group within a project. */
  public CursorPage<AssignedRoleDetails> list(String projectId, String groupId, ProjectGroupRoleListParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (groupId == null || groupId.isEmpty()) {
      throw new IllegalArgumentException("missing required group_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/projects/" + Transport.encodePath(projectId) + "/groups/" + Transport.encodePath(groupId) + "/roles");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> AssignedRoleDetails.fromJson(__e));
  }
}

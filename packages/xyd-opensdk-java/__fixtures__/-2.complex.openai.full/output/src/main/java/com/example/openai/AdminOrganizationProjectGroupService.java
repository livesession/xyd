// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AdminOrganizationProjectGroupService {
  private final Transport transport;

  AdminOrganizationProjectGroupService(Transport transport) {
    this.transport = transport;
  }

  /** Grants a group access to a project. */
  public ProjectGroup create(String projectId, AdminOrganizationProjectGroupCreateParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/organization/projects/" + Transport.encodePath(projectId) + "/groups");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("group_id", params.groupId());
    body.put("role", params.role());
    request.body(body);
    return ProjectGroup.fromJson(transport.execute(request));
  }

  /** Revokes a group's access to a project. */
  public ProjectGroupDeletedResource delete(String projectId, String groupId) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (groupId == null || groupId.isEmpty()) {
      throw new IllegalArgumentException("missing required group_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/organization/projects/" + Transport.encodePath(projectId) + "/groups/" + Transport.encodePath(groupId));
    return ProjectGroupDeletedResource.fromJson(transport.execute(request));
  }

  /** Lists the groups that have access to a project. */
  public CursorPage<ProjectGroup> list(String projectId, AdminOrganizationProjectGroupListParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/organization/projects/" + Transport.encodePath(projectId) + "/groups");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> ProjectGroup.fromJson(__e));
  }
}

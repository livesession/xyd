// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.Map;

/**
 * An object specifying the format that the model must output.
 * 
 * Setting to `{ "type": "json_schema", "json_schema": {...} }` enables
 * Structured Outputs which ensures the model will match your supplied JSON
 * schema. Learn more in the [Structured Outputs
 * guide](/docs/guides/structured-outputs).
 * 
 * Setting to `{ "type": "json_object" }` enables the older JSON mode, which
 * ensures the message the model generates is valid JSON. Using `json_schema`
 * is preferred for models that support it.
 */
public final class CreateChatCompletionRequestResponseFormat {
  private CreateChatCompletionRequestResponseFormat() {
  }

  /**
   * Decode JSON into the concrete CreateChatCompletionRequestResponseFormat variant selected by the "type"
   * discriminator. An unknown or absent discriminator keeps the raw parsed value.
   */
  public static Object fromJson(Object json) {
    if (!(json instanceof Map)) {
      return json;
    }
    Object discriminator = ((Map<?, ?>) json).get("type");
    if ("json_object".equals(discriminator)) {
      return ResponseFormatJsonObject.fromJson(json);
    }
    if ("json_schema".equals(discriminator)) {
      return ResponseFormatJsonSchema.fromJson(json);
    }
    if ("text".equals(discriminator)) {
      return ResponseFormatText.fromJson(json);
    }
    return json;
  }
}

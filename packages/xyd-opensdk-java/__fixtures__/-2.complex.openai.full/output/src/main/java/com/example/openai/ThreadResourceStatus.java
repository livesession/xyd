// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.Map;

/** Current status for the thread. Defaults to `active` for newly created threads. */
public final class ThreadResourceStatus {
  private ThreadResourceStatus() {
  }

  /**
   * Decode JSON into the concrete ThreadResourceStatus variant selected by the "type"
   * discriminator. An unknown or absent discriminator keeps the raw parsed value.
   */
  public static Object fromJson(Object json) {
    if (!(json instanceof Map)) {
      return json;
    }
    Object discriminator = ((Map<?, ?>) json).get("type");
    if ("active".equals(discriminator)) {
      return ActiveStatus.fromJson(json);
    }
    if ("closed".equals(discriminator)) {
      return ClosedStatus.fromJson(json);
    }
    if ("locked".equals(discriminator)) {
      return LockedStatus.fromJson(json);
    }
    return json;
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.Map;

/** Decode helper for the CreateContainerBodySkillsItem union, selecting the concrete variant by its "type" discriminator. */
public final class CreateContainerBodySkillsItem {
  private CreateContainerBodySkillsItem() {
  }

  /**
   * Decode JSON into the concrete CreateContainerBodySkillsItem variant selected by the "type"
   * discriminator. An unknown or absent discriminator keeps the raw parsed value.
   */
  public static Object fromJson(Object json) {
    if (!(json instanceof Map)) {
      return json;
    }
    Object discriminator = ((Map<?, ?>) json).get("type");
    if ("inline".equals(discriminator)) {
      return InlineSkillParam.fromJson(json);
    }
    if ("skill_reference".equals(discriminator)) {
      return SkillReferenceParam.fromJson(json);
    }
    return json;
  }
}

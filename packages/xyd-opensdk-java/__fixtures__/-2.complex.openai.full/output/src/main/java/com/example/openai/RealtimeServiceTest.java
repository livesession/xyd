// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * A dependency-free assertion test for RealtimeService — run with plain `java RealtimeServiceTest`.
 */
public final class RealtimeServiceTest {
  private static final String BASE_URL =
      System.getenv().getOrDefault("TEST_API_BASE_URL", "http://localhost:4010");

  public static void main(String[] args) {
    Client client = Client.builder().baseUrl(BASE_URL).apiKey("My API Key").build();
    testRealtimeCallAcceptWithOptionalParams(client);
    testRealtimeCallAcceptPathParams(client);
    testRealtimeCallCreateWithOptionalParams(client);
    testRealtimeCallHangup(client);
    testRealtimeCallHangupPathParams(client);
    testRealtimeCallRefer(client);
    testRealtimeCallReferPathParams(client);
    testRealtimeCallRejectWithOptionalParams(client);
    testRealtimeCallRejectPathParams(client);
    testRealtimeClientSecretCreateWithOptionalParams(client);
    testRealtimeSessionCreateWithOptionalParams(client);
    testRealtimeTranscriptionSessionCreateWithOptionalParams(client);
    testRealtimeTranslationClientSecretCreateWithOptionalParams(client);
    System.out.println("RealtimeServiceTest OK");
  }

  private static void testRealtimeCallAcceptWithOptionalParams(Client client) {
    try {
      client.realtime().calls().accept("call_id", RealtimeCallAcceptParams.builder()
          .outputModalities(java.util.List.of(RealtimeSessionCreateRequestGaOutputModalitiesItem.TEXT))
          .model("x")
          .instructions("x")
          .audio(new RealtimeSessionCreateRequestGaAudio())
          .include(java.util.List.of("item.input_audio_transcription.logprobs"))
          .tracing("auto")
          .tools(java.util.List.of())
          .toolChoice(ToolChoiceOptions.NONE)
          .parallelToolCalls(true)
          .reasoning(new RealtimeReasoning())
          .maxOutputTokens(0L)
          .truncation(RealtimeTruncationString.AUTO)
          .prompt(new Prompt())
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testRealtimeCallAcceptPathParams(Client client) {
    try {
      client.realtime().calls().accept("", RealtimeCallAcceptParams.builder().build());
      throw new AssertionError("expected a missing required path param error for accept");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testRealtimeCallCreateWithOptionalParams(Client client) {
    try {
      client.realtime().calls().create(RealtimeCallCreateParams.builder()
          .sdp("x")
          .session(new RealtimeSessionCreateRequestGa())
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testRealtimeCallHangup(Client client) {
    try {
      client.realtime().calls().hangup("call_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testRealtimeCallHangupPathParams(Client client) {
    try {
      client.realtime().calls().hangup("");
      throw new AssertionError("expected a missing required path param error for hangup");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testRealtimeCallRefer(Client client) {
    try {
      client.realtime().calls().refer("call_id", RealtimeCallReferParams.builder()
          .targetUri("x")
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testRealtimeCallReferPathParams(Client client) {
    try {
      client.realtime().calls().refer("", RealtimeCallReferParams.builder()
          .targetUri("x")
          .build());
      throw new AssertionError("expected a missing required path param error for refer");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testRealtimeCallRejectWithOptionalParams(Client client) {
    try {
      client.realtime().calls().reject("call_id", RealtimeCallRejectParams.builder()
          .statusCode(0L)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testRealtimeCallRejectPathParams(Client client) {
    try {
      client.realtime().calls().reject("", RealtimeCallRejectParams.builder().build());
      throw new AssertionError("expected a missing required path param error for reject");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testRealtimeClientSecretCreateWithOptionalParams(Client client) {
    try {
      client.realtime().clientSecrets().create(RealtimeClientSecretCreateParams.builder()
          .expiresAfter(new RealtimeCreateClientSecretRequestExpiresAfter())
          .session(new RealtimeSessionCreateRequestGa())
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testRealtimeSessionCreateWithOptionalParams(Client client) {
    try {
      client.realtime().sessions().create(RealtimeSessionCreateParams.builder()
          .clientSecret(new RealtimeSessionCreateRequestClientSecret())
          .modalities(null)
          .instructions("x")
          .voice("x")
          .inputAudioFormat("x")
          .outputAudioFormat("x")
          .inputAudioTranscription(new RealtimeSessionCreateRequestInputAudioTranscription())
          .speed(0.0)
          .tracing("auto")
          .turnDetection(new RealtimeSessionCreateRequestTurnDetection())
          .tools(java.util.List.of())
          .toolChoice("x")
          .temperature(0.0)
          .maxResponseOutputTokens(0L)
          .truncation(RealtimeTruncationString.AUTO)
          .prompt(new Prompt())
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testRealtimeTranscriptionSessionCreateWithOptionalParams(Client client) {
    try {
      client.realtime().transcriptionSessions().create(RealtimeTranscriptionSessionCreateParams.builder()
          .turnDetection(new RealtimeTranscriptionSessionCreateRequestTurnDetection())
          .inputAudioNoiseReduction(new RealtimeTranscriptionSessionCreateRequestInputAudioNoiseReduction())
          .inputAudioFormat(RealtimeTranscriptionSessionCreateRequestInputAudioFormat.PCM16)
          .inputAudioTranscription(new AudioTranscription())
          .include(java.util.List.of("item.input_audio_transcription.logprobs"))
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testRealtimeTranslationClientSecretCreateWithOptionalParams(Client client) {
    try {
      client.realtime().translations().clientSecrets().create(RealtimeTranslationClientSecretCreateParams.builder()
          .session(new RealtimeTranslationSessionCreateRequest())
          .expiresAfter(new RealtimeTranslationClientSecretCreateRequestExpiresAfter())
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void requireContains(String actual, String needle) {
    if (actual == null || !actual.contains(needle)) {
      throw new AssertionError("missing substring [" + needle + "] in: " + actual);
    }
  }
}

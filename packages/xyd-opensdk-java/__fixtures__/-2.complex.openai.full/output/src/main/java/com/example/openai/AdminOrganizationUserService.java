// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AdminOrganizationUserService {
  private final Transport transport;
  private final AdminOrganizationUserRoleService roles;

  AdminOrganizationUserService(Transport transport) {
    this.transport = transport;
    this.roles = new AdminOrganizationUserRoleService(transport);
  }

  public AdminOrganizationUserRoleService roles() {
    return roles;
  }

  /** Deletes a user from the organization. */
  public UserDeleteResponse delete(String userId) {
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/organization/users/" + Transport.encodePath(userId));
    return UserDeleteResponse.fromJson(transport.execute(request));
  }

  /** Lists all of the users in the organization. */
  public CursorPage<User> list(AdminOrganizationUserListParams params) {
    Transport.Request request = new Transport.Request("GET", "/organization/users");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.emails().ifPresent(values -> {
      for (Object value : values) {
        request.query("emails", String.valueOf(value));
      }
    });
    return CursorPage.fromJson(transport.execute(request), __e -> User.fromJson(__e));
  }

  /** Retrieves a user by their identifier. */
  public User retrieve(String userId) {
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/organization/users/" + Transport.encodePath(userId));
    return User.fromJson(transport.execute(request));
  }

  /** Modifies a user's role in the organization. */
  public User update(String userId, AdminOrganizationUserUpdateParams params) {
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/organization/users/" + Transport.encodePath(userId));
    Map<String, Object> body = new LinkedHashMap<>();
    params.role().ifPresent(value -> body.put("role", value));
    params.roleId().ifPresent(value -> body.put("role_id", value));
    params.technicalLevel().ifPresent(value -> body.put("technical_level", value));
    params.developerPersona().ifPresent(value -> body.put("developer_persona", value));
    request.body(body);
    return User.fromJson(transport.execute(request));
  }
}

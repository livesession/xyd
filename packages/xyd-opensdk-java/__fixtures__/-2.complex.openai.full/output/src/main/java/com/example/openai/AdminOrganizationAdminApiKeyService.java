// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AdminOrganizationAdminApiKeyService {
  private final Transport transport;

  AdminOrganizationAdminApiKeyService(Transport transport) {
    this.transport = transport;
  }

  /** Create a new admin-level API key for the organization. */
  public AdminApiKeyCreateResponse create(AdminOrganizationAdminApiKeyCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/organization/admin_api_keys");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("name", params.name());
    request.body(body);
    return AdminApiKeyCreateResponse.fromJson(transport.execute(request));
  }

  /** Delete the specified admin API key. */
  public AdminOrganizationAdminApiKeysDeleteResponse delete(String keyId) {
    if (keyId == null || keyId.isEmpty()) {
      throw new IllegalArgumentException("missing required key_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/organization/admin_api_keys/" + Transport.encodePath(keyId));
    return AdminOrganizationAdminApiKeysDeleteResponse.fromJson(transport.execute(request));
  }

  /** Retrieve a paginated list of organization admin API keys. */
  public CursorPage<AdminApiKey> list(AdminOrganizationAdminApiKeyListParams params) {
    Transport.Request request = new Transport.Request("GET", "/organization/admin_api_keys");
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> AdminApiKey.fromJson(__e));
  }

  /** Get details for a specific organization API key by its ID. */
  public AdminApiKey retrieve(String keyId) {
    if (keyId == null || keyId.isEmpty()) {
      throw new IllegalArgumentException("missing required key_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/organization/admin_api_keys/" + Transport.encodePath(keyId));
    return AdminApiKey.fromJson(transport.execute(request));
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class AdminOrganizationAuditLogService {
  private final Transport transport;

  AdminOrganizationAuditLogService(Transport transport) {
    this.transport = transport;
  }

  /** List user actions and configuration changes within this organization. */
  public CursorPage<AuditLog> list(AdminOrganizationAuditLogListParams params) {
    Transport.Request request = new Transport.Request("GET", "/organization/audit_logs");
    params.effectiveAt().ifPresent(value -> request.query("effective_at", Json.encode(value)));
    params.projectIds().ifPresent(values -> {
      for (Object value : values) {
        request.query("project_ids[]", String.valueOf(value));
      }
    });
    params.eventTypes().ifPresent(values -> {
      for (Object value : values) {
        request.query("event_types[]", String.valueOf(value));
      }
    });
    params.actorIds().ifPresent(values -> {
      for (Object value : values) {
        request.query("actor_ids[]", String.valueOf(value));
      }
    });
    params.actorEmails().ifPresent(values -> {
      for (Object value : values) {
        request.query("actor_emails[]", String.valueOf(value));
      }
    });
    params.resourceIds().ifPresent(values -> {
      for (Object value : values) {
        request.query("resource_ids[]", String.valueOf(value));
      }
    });
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.before().ifPresent(value -> request.query("before", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> AuditLog.fromJson(__e));
  }
}

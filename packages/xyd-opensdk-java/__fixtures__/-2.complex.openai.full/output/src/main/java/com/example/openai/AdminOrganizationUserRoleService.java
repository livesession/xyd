// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AdminOrganizationUserRoleService {
  private final Transport transport;

  AdminOrganizationUserRoleService(Transport transport) {
    this.transport = transport;
  }

  /** Assigns an organization role to a user within the organization. */
  public UserRoleAssignment create(String userId, AdminOrganizationUserRoleCreateParams params) {
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/organization/users/" + Transport.encodePath(userId) + "/roles");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("role_id", params.roleId());
    request.body(body);
    return UserRoleAssignment.fromJson(transport.execute(request));
  }

  /** Unassigns an organization role from a user within the organization. */
  public DeletedRoleAssignmentResource delete(String userId, String roleId) {
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    if (roleId == null || roleId.isEmpty()) {
      throw new IllegalArgumentException("missing required role_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/organization/users/" + Transport.encodePath(userId) + "/roles/" + Transport.encodePath(roleId));
    return DeletedRoleAssignmentResource.fromJson(transport.execute(request));
  }

  /** Lists the organization roles assigned to a user within the organization. */
  public CursorPage<AssignedRoleDetails> list(String userId, AdminOrganizationUserRoleListParams params) {
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/organization/users/" + Transport.encodePath(userId) + "/roles");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> AssignedRoleDetails.fromJson(__e));
  }
}

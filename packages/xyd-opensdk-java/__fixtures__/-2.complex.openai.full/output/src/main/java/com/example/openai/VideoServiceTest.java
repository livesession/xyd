// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * A dependency-free assertion test for VideoService — run with plain `java VideoServiceTest`.
 */
public final class VideoServiceTest {
  private static final String BASE_URL =
      System.getenv().getOrDefault("TEST_API_BASE_URL", "http://localhost:4010");

  public static void main(String[] args) {
    Client client = Client.builder().baseUrl(BASE_URL).apiKey("My API Key").build();
    testVideoCreateWithOptionalParams(client);
    testVideoDelete(client);
    testVideoDeletePathParams(client);
    testVideoDownloadContentWithOptionalParams(client);
    testVideoDownloadContentPathParams(client);
    testVideoListWithOptionalParams(client);
    testVideoRemix(client);
    testVideoRemixPathParams(client);
    testVideoRetrieve(client);
    testVideoRetrievePathParams(client);
    testVideoCharacterCreate(client);
    testVideoCharacterRetrieve(client);
    testVideoCharacterRetrievePathParams(client);
    testVideoEditCreate(client);
    testVideoExtensionCreate(client);
    System.out.println("VideoServiceTest OK");
  }

  private static void testVideoCreateWithOptionalParams(Client client) {
    try {
      client.videos().create(VideoCreateParams.builder()
          .prompt("x")
          .model("x")
          .inputReference(new ImageRefParam2())
          .seconds(VideoSeconds._4)
          .size(VideoSize._720X1280)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testVideoDelete(Client client) {
    try {
      client.videos().delete("video_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testVideoDeletePathParams(Client client) {
    try {
      client.videos().delete("");
      throw new AssertionError("expected a missing required path param error for delete");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testVideoDownloadContentWithOptionalParams(Client client) {
    try {
      client.videos().downloadContent("video_id", VideoDownloadContentParams.builder()
          .variant(VideoContentVariant.VIDEO)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testVideoDownloadContentPathParams(Client client) {
    try {
      client.videos().downloadContent("", VideoDownloadContentParams.builder().build());
      throw new AssertionError("expected a missing required path param error for downloadContent");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testVideoListWithOptionalParams(Client client) {
    try {
      client.videos().list(VideoListParams.builder()
          .limit(0L)
          .order(OrderEnum.ASC)
          .after("after")
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testVideoRemix(Client client) {
    try {
      client.videos().remix("video_id", VideoRemixParams.builder()
          .prompt("x")
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testVideoRemixPathParams(Client client) {
    try {
      client.videos().remix("", VideoRemixParams.builder()
          .prompt("x")
          .build());
      throw new AssertionError("expected a missing required path param error for remix");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testVideoRetrieve(Client client) {
    try {
      client.videos().retrieve("video_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testVideoRetrievePathParams(Client client) {
    try {
      client.videos().retrieve("");
      throw new AssertionError("expected a missing required path param error for retrieve");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testVideoCharacterCreate(Client client) {
    try {
      client.videos().characters().create(VideoCharacterCreateParams.builder()
          .video("Example data".getBytes(java.nio.charset.StandardCharsets.UTF_8))
          .name("x")
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testVideoCharacterRetrieve(Client client) {
    try {
      client.videos().characters().retrieve("character_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testVideoCharacterRetrievePathParams(Client client) {
    try {
      client.videos().characters().retrieve("");
      throw new AssertionError("expected a missing required path param error for retrieve");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void testVideoEditCreate(Client client) {
    try {
      client.videos().edits().create(VideoEditCreateParams.builder()
          .video(new VideoReferenceInputParam())
          .prompt("x")
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testVideoExtensionCreate(Client client) {
    try {
      client.videos().extensions().create(VideoExtensionCreateParams.builder()
          .video(new VideoReferenceInputParam())
          .prompt("x")
          .seconds(VideoSeconds._4)
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void requireContains(String actual, String needle) {
    if (actual == null || !actual.contains(needle)) {
      throw new AssertionError("missing substring [" + needle + "] in: " + actual);
    }
  }
}

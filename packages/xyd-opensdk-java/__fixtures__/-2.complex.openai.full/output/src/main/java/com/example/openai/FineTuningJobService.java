// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class FineTuningJobService {
  private final Transport transport;
  private final FineTuningJobCheckpointService checkpoints;

  FineTuningJobService(Transport transport) {
    this.transport = transport;
    this.checkpoints = new FineTuningJobCheckpointService(transport);
  }

  public FineTuningJobCheckpointService checkpoints() {
    return checkpoints;
  }

  /** Immediately cancel a fine-tune job. */
  public FineTuningJob cancel(String fineTuningJobId) {
    if (fineTuningJobId == null || fineTuningJobId.isEmpty()) {
      throw new IllegalArgumentException("missing required fine_tuning_job_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/fine_tuning/jobs/" + Transport.encodePath(fineTuningJobId) + "/cancel");
    return FineTuningJob.fromJson(transport.execute(request));
  }

  /**
   * Creates a fine-tuning job which begins the process of creating a new model from a given dataset.
   * 
   * Response includes details of the enqueued job including job status and the name of the fine-tuned models once complete.
   * 
   * [Learn more about fine-tuning](/docs/guides/model-optimization)
   */
  public FineTuningJob create(FineTuningJobCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/fine_tuning/jobs");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("model", params.model());
    body.put("training_file", params.trainingFile());
    params.hyperparameters().ifPresent(value -> body.put("hyperparameters", value));
    params.suffix().ifPresent(value -> body.put("suffix", value));
    params.validationFile().ifPresent(value -> body.put("validation_file", value));
    params.integrations().ifPresent(value -> body.put("integrations", value));
    params.seed().ifPresent(value -> body.put("seed", value));
    params.method().ifPresent(value -> body.put("method", value));
    params.metadata().ifPresent(value -> body.put("metadata", value));
    request.body(body);
    return FineTuningJob.fromJson(transport.execute(request));
  }

  /** List your organization's fine-tuning jobs */
  public CursorPage<FineTuningJob> list(FineTuningJobListParams params) {
    Transport.Request request = new Transport.Request("GET", "/fine_tuning/jobs");
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.metadata().ifPresent(entries -> entries.forEach((mapKey, mapValue) -> request.query("metadata[" + mapKey + "]", String.valueOf(mapValue))));
    return CursorPage.fromJson(transport.execute(request), __e -> FineTuningJob.fromJson(__e));
  }

  /** Get status updates for a fine-tuning job. */
  public CursorPage<FineTuningJobEvent> listEvents(String fineTuningJobId, FineTuningJobListEventsParams params) {
    if (fineTuningJobId == null || fineTuningJobId.isEmpty()) {
      throw new IllegalArgumentException("missing required fine_tuning_job_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/fine_tuning/jobs/" + Transport.encodePath(fineTuningJobId) + "/events");
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> FineTuningJobEvent.fromJson(__e));
  }

  /** Pause a fine-tune job. */
  public FineTuningJob pause(String fineTuningJobId) {
    if (fineTuningJobId == null || fineTuningJobId.isEmpty()) {
      throw new IllegalArgumentException("missing required fine_tuning_job_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/fine_tuning/jobs/" + Transport.encodePath(fineTuningJobId) + "/pause");
    return FineTuningJob.fromJson(transport.execute(request));
  }

  /** Resume a fine-tune job. */
  public FineTuningJob resume(String fineTuningJobId) {
    if (fineTuningJobId == null || fineTuningJobId.isEmpty()) {
      throw new IllegalArgumentException("missing required fine_tuning_job_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/fine_tuning/jobs/" + Transport.encodePath(fineTuningJobId) + "/resume");
    return FineTuningJob.fromJson(transport.execute(request));
  }

  /**
   * Get info about a fine-tuning job.
   * 
   * [Learn more about fine-tuning](/docs/guides/model-optimization)
   */
  public FineTuningJob retrieve(String fineTuningJobId) {
    if (fineTuningJobId == null || fineTuningJobId.isEmpty()) {
      throw new IllegalArgumentException("missing required fine_tuning_job_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/fine_tuning/jobs/" + Transport.encodePath(fineTuningJobId));
    return FineTuningJob.fromJson(transport.execute(request));
  }
}

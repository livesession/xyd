// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AdminOrganizationGroupService {
  private final Transport transport;
  private final AdminOrganizationGroupRoleService roles;
  private final AdminOrganizationGroupUserService users;

  AdminOrganizationGroupService(Transport transport) {
    this.transport = transport;
    this.roles = new AdminOrganizationGroupRoleService(transport);
    this.users = new AdminOrganizationGroupUserService(transport);
  }

  public AdminOrganizationGroupRoleService roles() {
    return roles;
  }

  public AdminOrganizationGroupUserService users() {
    return users;
  }

  /** Creates a new group in the organization. */
  public GroupResponse create(AdminOrganizationGroupCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/organization/groups");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("name", params.name());
    request.body(body);
    return GroupResponse.fromJson(transport.execute(request));
  }

  /** Deletes a group from the organization. */
  public GroupDeletedResource delete(String groupId) {
    if (groupId == null || groupId.isEmpty()) {
      throw new IllegalArgumentException("missing required group_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/organization/groups/" + Transport.encodePath(groupId));
    return GroupDeletedResource.fromJson(transport.execute(request));
  }

  /** Lists all groups in the organization. */
  public CursorPage<GroupResponse> list(AdminOrganizationGroupListParams params) {
    Transport.Request request = new Transport.Request("GET", "/organization/groups");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> GroupResponse.fromJson(__e));
  }

  /** Updates a group's information. */
  public GroupResourceWithSuccess update(String groupId, AdminOrganizationGroupUpdateParams params) {
    if (groupId == null || groupId.isEmpty()) {
      throw new IllegalArgumentException("missing required group_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/organization/groups/" + Transport.encodePath(groupId));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("name", params.name());
    request.body(body);
    return GroupResourceWithSuccess.fromJson(transport.execute(request));
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class SkillVersionService {
  private final Transport transport;

  SkillVersionService(Transport transport) {
    this.transport = transport;
  }

  /** Download a skill version zip bundle. */
  public String content(String skillId, String version) {
    if (skillId == null || skillId.isEmpty()) {
      throw new IllegalArgumentException("missing required skill_id parameter");
    }
    if (version == null || version.isEmpty()) {
      throw new IllegalArgumentException("missing required version parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/skills/" + Transport.encodePath(skillId) + "/versions/" + Transport.encodePath(version) + "/content");
    return Json.asString(transport.execute(request));
  }

  /** Create a new immutable skill version. */
  public SkillVersionResource create(String skillId, SkillVersionCreateParams params) {
    if (skillId == null || skillId.isEmpty()) {
      throw new IllegalArgumentException("missing required skill_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/skills/" + Transport.encodePath(skillId) + "/versions");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("files", params.files());
    params.default_().ifPresent(value -> body.put("default", value));
    request.body(body);
    request.encoding("multipart");
    return SkillVersionResource.fromJson(transport.execute(request));
  }

  /** Delete a skill version. */
  public DeletedSkillVersionResource delete(String skillId, String version) {
    if (skillId == null || skillId.isEmpty()) {
      throw new IllegalArgumentException("missing required skill_id parameter");
    }
    if (version == null || version.isEmpty()) {
      throw new IllegalArgumentException("missing required version parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/skills/" + Transport.encodePath(skillId) + "/versions/" + Transport.encodePath(version));
    return DeletedSkillVersionResource.fromJson(transport.execute(request));
  }

  /** List skill versions for a skill. */
  public CursorPage<SkillVersionResource> list(String skillId, SkillVersionListParams params) {
    if (skillId == null || skillId.isEmpty()) {
      throw new IllegalArgumentException("missing required skill_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/skills/" + Transport.encodePath(skillId) + "/versions");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> SkillVersionResource.fromJson(__e));
  }

  /** Get a specific skill version. */
  public SkillVersionResource retrieve(String skillId, String version) {
    if (skillId == null || skillId.isEmpty()) {
      throw new IllegalArgumentException("missing required skill_id parameter");
    }
    if (version == null || version.isEmpty()) {
      throw new IllegalArgumentException("missing required version parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/skills/" + Transport.encodePath(skillId) + "/versions/" + Transport.encodePath(version));
    return SkillVersionResource.fromJson(transport.execute(request));
  }
}

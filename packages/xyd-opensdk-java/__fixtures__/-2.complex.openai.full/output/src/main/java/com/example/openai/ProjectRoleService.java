// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ProjectRoleService {
  private final Transport transport;

  ProjectRoleService(Transport transport) {
    this.transport = transport;
  }

  /** Creates a custom role for a project. */
  public Role create(String projectId, ProjectRoleCreateParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/projects/" + Transport.encodePath(projectId) + "/roles");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("role_name", params.roleName());
    body.put("permissions", params.permissions());
    params.description().ifPresent(value -> body.put("description", value));
    request.body(body);
    return Role.fromJson(transport.execute(request));
  }

  /** Deletes a custom role from a project. */
  public RoleDeletedResource delete(String projectId, String roleId) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (roleId == null || roleId.isEmpty()) {
      throw new IllegalArgumentException("missing required role_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/projects/" + Transport.encodePath(projectId) + "/roles/" + Transport.encodePath(roleId));
    return RoleDeletedResource.fromJson(transport.execute(request));
  }

  /** Lists the roles configured for a project. */
  public CursorPage<Role> list(String projectId, ProjectRoleListParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/projects/" + Transport.encodePath(projectId) + "/roles");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> Role.fromJson(__e));
  }

  /** Updates an existing project role. */
  public Role update(String projectId, String roleId, ProjectRoleUpdateParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (roleId == null || roleId.isEmpty()) {
      throw new IllegalArgumentException("missing required role_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/projects/" + Transport.encodePath(projectId) + "/roles/" + Transport.encodePath(roleId));
    Map<String, Object> body = new LinkedHashMap<>();
    params.permissions().ifPresent(value -> body.put("permissions", value));
    params.description().ifPresent(value -> body.put("description", value));
    params.roleName().ifPresent(value -> body.put("role_name", value));
    request.body(body);
    return Role.fromJson(transport.execute(request));
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/** Control how much effort the model will exert to match the style and features, especially facial features, of input images. This parameter is only supported for `gpt-image-1` and `gpt-image-1.5` and later models, unsupported for `gpt-image-1-mini`. Supports `high` and `low`. Defaults to `low`. */
public enum InputFidelity implements Json.JsonEnum {
  HIGH("high"),
  LOW("low");

  private final String value;

  InputFidelity(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static InputFidelity fromValue(String value) {
    for (InputFidelity item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown InputFidelity value: " + value);
  }

  public static InputFidelity fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

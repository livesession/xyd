// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ResponseCompactService {
  private final Transport transport;

  ResponseCompactService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Compact a conversation. Returns a compacted response object.
   * 
   * Learn when and how to compact long-running conversations in the [conversation state guide](/docs/guides/conversation-state#managing-the-context-window). For ZDR-compatible compaction details, see [Compaction (advanced)](/docs/guides/conversation-state#compaction-advanced).
   */
  public CompactResource create(ResponseCompactCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/responses/compact");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("model", params.model());
    params.input().ifPresent(value -> body.put("input", value));
    params.previousResponseId().ifPresent(value -> body.put("previous_response_id", value));
    params.instructions().ifPresent(value -> body.put("instructions", value));
    params.promptCacheKey().ifPresent(value -> body.put("prompt_cache_key", value));
    params.promptCacheRetention().ifPresent(value -> body.put("prompt_cache_retention", value));
    params.serviceTier().ifPresent(value -> body.put("service_tier", value));
    request.body(body);
    return CompactResource.fromJson(transport.execute(request));
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/** The status of the vector store, which can be either `expired`, `in_progress`, or `completed`. A status of `completed` indicates that the vector store is ready for use. */
public enum VectorStoreObjectStatus implements Json.JsonEnum {
  EXPIRED("expired"),
  IN_PROGRESS("in_progress"),
  COMPLETED("completed");

  private final String value;

  VectorStoreObjectStatus(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static VectorStoreObjectStatus fromValue(String value) {
    for (VectorStoreObjectStatus item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown VectorStoreObjectStatus value: " + value);
  }

  public static VectorStoreObjectStatus fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

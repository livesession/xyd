// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * Constrains the verbosity of the model's response. Lower values will result in
 * more concise responses, while higher values will result in more verbose responses.
 * Currently supported values are `low`, `medium`, and `high`.
 */
public enum Verbosity implements Json.JsonEnum {
  LOW("low"),
  MEDIUM("medium"),
  HIGH("high");

  private final String value;

  Verbosity(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static Verbosity fromValue(String value) {
    for (Verbosity item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown Verbosity value: " + value);
  }

  public static Verbosity fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

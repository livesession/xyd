// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.Map;

/** An annotation that applies to a span of output text. */
public final class Annotation {
  private Annotation() {
  }

  /**
   * Decode JSON into the concrete Annotation variant selected by the "type"
   * discriminator. An unknown or absent discriminator keeps the raw parsed value.
   */
  public static Object fromJson(Object json) {
    if (!(json instanceof Map)) {
      return json;
    }
    Object discriminator = ((Map<?, ?>) json).get("type");
    if ("container_file_citation".equals(discriminator)) {
      return ContainerFileCitationBody.fromJson(json);
    }
    if ("file_citation".equals(discriminator)) {
      return FileCitationBody.fromJson(json);
    }
    if ("file_path".equals(discriminator)) {
      return FilePath.fromJson(json);
    }
    if ("url_citation".equals(discriminator)) {
      return UrlCitationBody.fromJson(json);
    }
    return json;
  }
}

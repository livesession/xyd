// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/** The format in which generated images with `dall-e-2` and `dall-e-3` are returned. Must be one of `url` or `b64_json`. URLs are only valid for 60 minutes after the image has been generated. This parameter isn't supported for the GPT image models, which always return base64-encoded images. */
public enum CreateImageRequestResponseFormat implements Json.JsonEnum {
  URL("url"),
  B64_JSON("b64_json");

  private final String value;

  CreateImageRequestResponseFormat(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static CreateImageRequestResponseFormat fromValue(String value) {
    for (CreateImageRequestResponseFormat item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown CreateImageRequestResponseFormat value: " + value);
  }

  public static CreateImageRequestResponseFormat fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

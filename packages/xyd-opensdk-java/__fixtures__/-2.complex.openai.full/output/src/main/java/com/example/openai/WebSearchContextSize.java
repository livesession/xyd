// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

/**
 * High level guidance for the amount of context window space to use for the
 * search. One of `low`, `medium`, or `high`. `medium` is the default.
 */
public enum WebSearchContextSize implements Json.JsonEnum {
  LOW("low"),
  MEDIUM("medium"),
  HIGH("high");

  private final String value;

  WebSearchContextSize(String value) {
    this.value = value;
  }

  public String value() {
    return value;
  }

  @Override
  public Object jsonValue() {
    return value;
  }

  public static WebSearchContextSize fromValue(String value) {
    for (WebSearchContextSize item : values()) {
      if (item.value.equals(value)) {
        return item;
      }
    }
    throw new IllegalArgumentException("unknown WebSearchContextSize value: " + value);
  }

  public static WebSearchContextSize fromJson(Object json) {
    return json == null ? null : fromValue(Json.asString(json));
  }
}

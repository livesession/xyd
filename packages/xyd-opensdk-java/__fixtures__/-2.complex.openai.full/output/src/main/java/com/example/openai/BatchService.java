// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class BatchService {
  private final Transport transport;

  BatchService(Transport transport) {
    this.transport = transport;
  }

  /** Cancels an in-progress batch. The batch will be in status `cancelling` for up to 10 minutes, before changing to `cancelled`, where it will have partial results (if any) available in the output file. */
  public Batch cancel(String batchId) {
    if (batchId == null || batchId.isEmpty()) {
      throw new IllegalArgumentException("missing required batch_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/batches/" + Transport.encodePath(batchId) + "/cancel");
    return Batch.fromJson(transport.execute(request));
  }

  /** Creates and executes a batch from an uploaded file of requests */
  public Batch create(BatchCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/batches");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("input_file_id", params.inputFileId());
    body.put("endpoint", params.endpoint());
    body.put("completion_window", "24h");
    params.metadata().ifPresent(value -> body.put("metadata", value));
    params.outputExpiresAfter().ifPresent(value -> body.put("output_expires_after", value));
    request.body(body);
    return Batch.fromJson(transport.execute(request));
  }

  /** List your organization's batches. */
  public CursorPage<Batch> list(BatchListParams params) {
    Transport.Request request = new Transport.Request("GET", "/batches");
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> Batch.fromJson(__e));
  }

  /** Retrieves a batch. */
  public Batch retrieve(String batchId) {
    if (batchId == null || batchId.isEmpty()) {
      throw new IllegalArgumentException("missing required batch_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/batches/" + Transport.encodePath(batchId));
    return Batch.fromJson(transport.execute(request));
  }
}

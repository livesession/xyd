// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AdminOrganizationRoleService {
  private final Transport transport;

  AdminOrganizationRoleService(Transport transport) {
    this.transport = transport;
  }

  /** Creates a custom role for the organization. */
  public Role create(AdminOrganizationRoleCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/organization/roles");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("role_name", params.roleName());
    body.put("permissions", params.permissions());
    params.description().ifPresent(value -> body.put("description", value));
    request.body(body);
    return Role.fromJson(transport.execute(request));
  }

  /** Deletes a custom role from the organization. */
  public RoleDeletedResource delete(String roleId) {
    if (roleId == null || roleId.isEmpty()) {
      throw new IllegalArgumentException("missing required role_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/organization/roles/" + Transport.encodePath(roleId));
    return RoleDeletedResource.fromJson(transport.execute(request));
  }

  /** Lists the roles configured for the organization. */
  public CursorPage<Role> list(AdminOrganizationRoleListParams params) {
    Transport.Request request = new Transport.Request("GET", "/organization/roles");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> Role.fromJson(__e));
  }

  /** Updates an existing organization role. */
  public Role update(String roleId, AdminOrganizationRoleUpdateParams params) {
    if (roleId == null || roleId.isEmpty()) {
      throw new IllegalArgumentException("missing required role_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/organization/roles/" + Transport.encodePath(roleId));
    Map<String, Object> body = new LinkedHashMap<>();
    params.permissions().ifPresent(value -> body.put("permissions", value));
    params.description().ifPresent(value -> body.put("description", value));
    params.roleName().ifPresent(value -> body.put("role_name", value));
    request.body(body);
    return Role.fromJson(transport.execute(request));
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.List;
import java.util.Map;

/**
 * A description of the chain of thought used by a reasoning model while generating
 * a response. Be sure to include these items in your `input` to the Responses API
 * for subsequent turns of a conversation if you are manually
 * [managing context](/docs/guides/conversation-state).
 */
public final class ReasoningItem implements Json.JsonSerializable {
  private String type;
  private String id;
  private String encryptedContent;
  private List<SummaryTextContent> summary;
  private List<ReasoningTextContent> content;
  private InputMessageStatus status;

  public String type() {
    return type;
  }

  public String id() {
    return id;
  }

  public String encryptedContent() {
    return encryptedContent;
  }

  public List<SummaryTextContent> summary() {
    return summary;
  }

  public List<ReasoningTextContent> content() {
    return content;
  }

  public InputMessageStatus status() {
    return status;
  }

  public static ReasoningItem fromJson(Object json) {
    if (json == null) {
      return null;
    }
    @SuppressWarnings("unchecked")
    Map<String, Object> map = (Map<String, Object>) json;
    ReasoningItem out = new ReasoningItem();
    out.type = Json.asString(map.get("type"));
    out.id = Json.asString(map.get("id"));
    out.encryptedContent = Json.asString(map.get("encrypted_content"));
    out.summary = Json.mapList(map.get("summary"), __e -> SummaryTextContent.fromJson(__e));
    out.content = Json.mapList(map.get("content"), __e -> ReasoningTextContent.fromJson(__e));
    out.status = InputMessageStatus.fromJson(map.get("status"));
    return out;
  }

  @Override
  public Map<String, Object> toJsonMap() {
    Map<String, Object> map = new LinkedHashMap<>();
    map.put("type", type != null ? type : "reasoning");
    map.put("id", id);
    if (encryptedContent != null) {
      map.put("encrypted_content", encryptedContent);
    }
    map.put("summary", summary);
    if (content != null) {
      map.put("content", content);
    }
    if (status != null) {
      map.put("status", status);
    }
    return map;
  }
}

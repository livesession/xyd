// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class ProjectUserRoleService {
  private final Transport transport;

  ProjectUserRoleService(Transport transport) {
    this.transport = transport;
  }

  /** Unassigns a project role from a user within a project. */
  public DeletedRoleAssignmentResource delete(String projectId, String userId, String roleId) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (userId == null || userId.isEmpty()) {
      throw new IllegalArgumentException("missing required user_id parameter");
    }
    if (roleId == null || roleId.isEmpty()) {
      throw new IllegalArgumentException("missing required role_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/projects/" + Transport.encodePath(projectId) + "/users/" + Transport.encodePath(userId) + "/roles/" + Transport.encodePath(roleId));
    return DeletedRoleAssignmentResource.fromJson(transport.execute(request));
  }
}

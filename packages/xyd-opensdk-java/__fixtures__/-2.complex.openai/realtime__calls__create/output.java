// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class RealtimeCallService {
  private final Transport transport;

  RealtimeCallService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Create a new Realtime API call over WebRTC and receive the SDP answer needed
   * to complete the peer connection.
   */
  public byte[] create(RealtimeCallCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/realtime/calls");
    request.accept("application/sdp");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("sdp", params.sdp());
    params.session().ifPresent(value -> body.put("session", value));
    request.body(body);
    request.encoding("multipart");
    return transport.executeRaw(request);
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class EvalService {
  private final Transport transport;

  EvalService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Create the structure of an evaluation that can be used to test a model's performance.
   * An evaluation is a set of testing criteria and the config for a data source, which dictates the schema of the data used in the evaluation. After creating an evaluation, you can run it on different models and model parameters. We support several types of graders and datasources.
   * For more information, see the [Evals guide](/docs/guides/evals).
   */
  public Eval create(EvalCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/evals");
    Map<String, Object> body = new LinkedHashMap<>();
    params.name().ifPresent(value -> body.put("name", value));
    params.metadata().ifPresent(value -> body.put("metadata", value));
    body.put("data_source_config", params.dataSourceConfig());
    body.put("testing_criteria", params.testingCriteria());
    request.body(body);
    return Eval.fromJson(transport.execute(request));
  }
}

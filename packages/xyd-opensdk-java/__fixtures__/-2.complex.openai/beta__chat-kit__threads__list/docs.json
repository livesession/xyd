{
  "chain": [
    "beta",
    "chat-kit",
    "threads"
  ],
  "action": "list",
  "usage": "// Code generated by opensdk. DO NOT EDIT.\n\npackage com.example.openai;\n\npublic final class Example {\n  public static void main(String[] args) {\n    Client client = Client.builder().apiKey(System.getenv(\"OPENAI_API_KEY\")).build();\n    var result = client.beta().chatKit().threads().list(BetaChatKitThreadListParams.builder()\n          .limit(1L)\n          .order(OrderEnum.ASC)\n          .after(\"after\")\n          .before(\"before\")\n          .user(\"user\")\n          .build());\n    System.out.println(result);\n  }\n}\n",
  "typeReference": {
    "signature": "client.beta().chatKit().threads().list(params) -> CursorPage<ThreadResource>",
    "request": {
      "typeName": "BetaChatKitThreadListParams",
      "argName": "params",
      "fields": [
        {
          "name": "limit",
          "langType": "Optional<Long>",
          "required": false,
          "description": "Maximum number of thread items to return. Defaults to 20."
        },
        {
          "name": "order",
          "langType": "Optional<OrderEnum>",
          "required": false,
          "description": "Sort order for results by creation time. Defaults to `desc`.",
          "refTypeName": "OrderEnum"
        },
        {
          "name": "after",
          "langType": "Optional<String>",
          "required": false,
          "description": "List items created after this thread item ID. Defaults to null for the first page."
        },
        {
          "name": "before",
          "langType": "Optional<String>",
          "required": false,
          "description": "List items created before this thread item ID. Defaults to null for the newest results."
        },
        {
          "name": "user",
          "langType": "Optional<String>",
          "required": false,
          "description": "Filter threads that belong to this user identifier. Defaults to null to return all users."
        }
      ]
    },
    "response": {
      "typeName": "ThreadResource",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "Identifier of the thread."
        },
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "Type discriminator that is always `chatkit.thread`."
        },
        {
          "name": "createdAt",
          "langType": "Long",
          "required": true,
          "description": "Unix timestamp (in seconds) for when the thread was created."
        },
        {
          "name": "title",
          "langType": "String",
          "required": true
        },
        {
          "name": "status",
          "langType": "Object",
          "required": true,
          "description": "Current status for the thread. Defaults to `active` for newly created threads.",
          "refTypeName": "ThreadResourceStatus"
        },
        {
          "name": "user",
          "langType": "String",
          "required": true,
          "description": "Free-form string that identifies your end user who owns the thread."
        }
      ]
    }
  }
}

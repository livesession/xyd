// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class BetaChatKitSessionService {
  private final Transport transport;

  BetaChatKitSessionService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Cancel an active ChatKit session and return its most recent metadata.
   * 
   * Cancelling prevents new requests from using the issued client secret.
   */
  public ChatSessionResource cancel(String sessionId) {
    if (sessionId == null || sessionId.isEmpty()) {
      throw new IllegalArgumentException("missing required session_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/chatkit/sessions/" + Transport.encodePath(sessionId) + "/cancel");
    return ChatSessionResource.fromJson(transport.execute(request));
  }
}

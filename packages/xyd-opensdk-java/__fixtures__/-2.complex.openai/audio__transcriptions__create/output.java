// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AudioTranscriptionService {
  private final Transport transport;

  AudioTranscriptionService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Transcribes audio into the input language.
   * 
   * Returns a transcription object in `json`, `diarized_json`, or `verbose_json`
   * format, or a stream of transcript events.
   */
  public Object create(AudioTranscriptionCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/audio/transcriptions");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("file", params.file());
    body.put("model", params.model());
    params.language().ifPresent(value -> body.put("language", value));
    params.prompt().ifPresent(value -> body.put("prompt", value));
    params.responseFormat().ifPresent(value -> body.put("response_format", value));
    params.temperature().ifPresent(value -> body.put("temperature", value));
    params.include().ifPresent(value -> body.put("include", value));
    params.timestampGranularities().ifPresent(value -> body.put("timestamp_granularities", value));
    params.stream().ifPresent(value -> body.put("stream", value));
    params.chunkingStrategy().ifPresent(value -> body.put("chunking_strategy", value));
    params.knownSpeakerNames().ifPresent(value -> body.put("known_speaker_names", value));
    params.knownSpeakerReferences().ifPresent(value -> body.put("known_speaker_references", value));
    request.body(body);
    request.encoding("multipart");
    return transport.execute(request);
  }
}

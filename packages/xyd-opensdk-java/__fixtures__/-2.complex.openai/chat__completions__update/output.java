// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ChatCompletionService {
  private final Transport transport;

  ChatCompletionService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Modify a stored chat completion. Only Chat Completions that have been
   * created with the `store` parameter set to `true` can be modified. Currently,
   * the only supported modification is to update the `metadata` field.
   */
  public CreateChatCompletionResponse update(String completionId, ChatCompletionUpdateParams params) {
    if (completionId == null || completionId.isEmpty()) {
      throw new IllegalArgumentException("missing required completion_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/chat/completions/" + Transport.encodePath(completionId));
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("metadata", params.metadata());
    request.body(body);
    return CreateChatCompletionResponse.fromJson(transport.execute(request));
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class BetaChatKitThreadItemService {
  private final Transport transport;

  BetaChatKitThreadItemService(Transport transport) {
    this.transport = transport;
  }

  /** List items that belong to a ChatKit thread. */
  public CursorPage<Object> list(String threadId, BetaChatKitThreadItemListParams params) {
    if (threadId == null || threadId.isEmpty()) {
      throw new IllegalArgumentException("missing required thread_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/chatkit/threads/" + Transport.encodePath(threadId) + "/items");
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.before().ifPresent(value -> request.query("before", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> ThreadItem.fromJson(__e));
  }
}

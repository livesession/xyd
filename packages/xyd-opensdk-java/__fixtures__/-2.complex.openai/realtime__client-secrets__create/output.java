// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class RealtimeClientSecretService {
  private final Transport transport;

  RealtimeClientSecretService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Create a Realtime client secret with an associated session configuration.
   * 
   * Client secrets are short-lived tokens that can be passed to a client app,
   * such as a web frontend or mobile client, which grants access to the Realtime API without
   * leaking your main API key. You can configure a custom TTL for each client secret.
   * 
   * You can also attach session configuration options to the client secret, which will be
   * applied to any sessions created using that client secret, but these can also be overridden
   * by the client connection.
   * 
   * [Learn more about authentication with client secrets over WebRTC](/docs/guides/realtime-webrtc).
   * 
   * Returns the created client secret and the effective session object. The client secret is a string that looks like `ek_1234`.
   */
  public RealtimeCreateClientSecretResponse create(RealtimeClientSecretCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/realtime/client_secrets");
    Map<String, Object> body = new LinkedHashMap<>();
    params.expiresAfter().ifPresent(value -> body.put("expires_after", value));
    params.session().ifPresent(value -> body.put("session", value));
    request.body(body);
    return RealtimeCreateClientSecretResponse.fromJson(transport.execute(request));
  }
}

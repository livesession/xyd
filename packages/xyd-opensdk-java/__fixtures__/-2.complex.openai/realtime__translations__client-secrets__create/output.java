// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class RealtimeTranslationClientSecretService {
  private final Transport transport;

  RealtimeTranslationClientSecretService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Create a Realtime translation client secret with an associated translation session configuration.
   * 
   * Client secrets are short-lived tokens that can be passed to a client app,
   * such as a web frontend or mobile client, which grants access to the Realtime
   * Translation API without leaking your main API key. You can configure a custom
   * TTL for each client secret.
   * 
   * Returns the created client secret and the effective translation session object.
   * The client secret is a string that looks like `ek_1234`.
   */
  public RealtimeTranslationClientSecretCreateResponse create(RealtimeTranslationClientSecretCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/realtime/translations/client_secrets");
    Map<String, Object> body = new LinkedHashMap<>();
    params.expiresAfter().ifPresent(value -> body.put("expires_after", value));
    body.put("session", params.session());
    request.body(body);
    return RealtimeTranslationClientSecretCreateResponse.fromJson(transport.execute(request));
  }
}

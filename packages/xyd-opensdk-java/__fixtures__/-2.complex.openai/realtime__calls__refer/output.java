// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class RealtimeCallService {
  private final Transport transport;

  RealtimeCallService(Transport transport) {
    this.transport = transport;
  }

  /** Transfer an active SIP call to a new destination using the SIP REFER verb. */
  public void refer(String callId, RealtimeCallReferParams params) {
    if (callId == null || callId.isEmpty()) {
      throw new IllegalArgumentException("missing required call_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/realtime/calls/" + Transport.encodePath(callId) + "/refer");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("target_uri", params.targetUri());
    request.body(body);
    transport.execute(request);
  }
}

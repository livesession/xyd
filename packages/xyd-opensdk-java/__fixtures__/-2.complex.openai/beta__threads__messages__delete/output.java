// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class BetaThreadMessageService {
  private final Transport transport;

  BetaThreadMessageService(Transport transport) {
    this.transport = transport;
  }

  /** Deletes a message. */
  public DeleteMessageResponse delete(String threadId, String messageId) {
    if (threadId == null || threadId.isEmpty()) {
      throw new IllegalArgumentException("missing required thread_id parameter");
    }
    if (messageId == null || messageId.isEmpty()) {
      throw new IllegalArgumentException("missing required message_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/threads/" + Transport.encodePath(threadId) + "/messages/" + Transport.encodePath(messageId));
    return DeleteMessageResponse.fromJson(transport.execute(request));
  }
}

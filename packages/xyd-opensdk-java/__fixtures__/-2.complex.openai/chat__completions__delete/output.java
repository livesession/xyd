// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class ChatCompletionService {
  private final Transport transport;

  ChatCompletionService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Delete a stored chat completion. Only Chat Completions that have been
   * created with the `store` parameter set to `true` can be deleted.
   */
  public ChatCompletionDeleted delete(String completionId) {
    if (completionId == null || completionId.isEmpty()) {
      throw new IllegalArgumentException("missing required completion_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/chat/completions/" + Transport.encodePath(completionId));
    return ChatCompletionDeleted.fromJson(transport.execute(request));
  }
}

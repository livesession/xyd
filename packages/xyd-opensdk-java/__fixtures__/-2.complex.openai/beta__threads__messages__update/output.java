// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class BetaThreadMessageService {
  private final Transport transport;

  BetaThreadMessageService(Transport transport) {
    this.transport = transport;
  }

  /** Modifies a message. */
  public MessageObject update(String threadId, String messageId, BetaThreadMessageUpdateParams params) {
    if (threadId == null || threadId.isEmpty()) {
      throw new IllegalArgumentException("missing required thread_id parameter");
    }
    if (messageId == null || messageId.isEmpty()) {
      throw new IllegalArgumentException("missing required message_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/threads/" + Transport.encodePath(threadId) + "/messages/" + Transport.encodePath(messageId));
    Map<String, Object> body = new LinkedHashMap<>();
    params.metadata().ifPresent(value -> body.put("metadata", value));
    request.body(body);
    return MessageObject.fromJson(transport.execute(request));
  }
}

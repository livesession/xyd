// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class RealtimeCallService {
  private final Transport transport;

  RealtimeCallService(Transport transport) {
    this.transport = transport;
  }

  /** Decline an incoming SIP call by returning a SIP status code to the caller. */
  public void reject(String callId, RealtimeCallRejectParams params) {
    if (callId == null || callId.isEmpty()) {
      throw new IllegalArgumentException("missing required call_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/realtime/calls/" + Transport.encodePath(callId) + "/reject");
    Map<String, Object> body = new LinkedHashMap<>();
    params.statusCode().ifPresent(value -> body.put("status_code", value));
    request.body(body);
    transport.execute(request);
  }
}

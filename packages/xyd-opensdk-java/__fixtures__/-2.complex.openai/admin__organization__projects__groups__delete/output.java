// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class AdminOrganizationProjectGroupService {
  private final Transport transport;

  AdminOrganizationProjectGroupService(Transport transport) {
    this.transport = transport;
  }

  /** Revokes a group's access to a project. */
  public ProjectGroupDeletedResource delete(String projectId, String groupId) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (groupId == null || groupId.isEmpty()) {
      throw new IllegalArgumentException("missing required group_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/organization/projects/" + Transport.encodePath(projectId) + "/groups/" + Transport.encodePath(groupId));
    return ProjectGroupDeletedResource.fromJson(transport.execute(request));
  }
}

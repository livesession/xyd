// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class BetaChatKitThreadService {
  private final Transport transport;

  BetaChatKitThreadService(Transport transport) {
    this.transport = transport;
  }

  /** Delete a ChatKit thread along with its items and stored attachments. */
  public DeletedThreadResource delete(String threadId) {
    if (threadId == null || threadId.isEmpty()) {
      throw new IllegalArgumentException("missing required thread_id parameter");
    }
    Transport.Request request = new Transport.Request("DELETE", "/chatkit/threads/" + Transport.encodePath(threadId));
    return DeletedThreadResource.fromJson(transport.execute(request));
  }
}

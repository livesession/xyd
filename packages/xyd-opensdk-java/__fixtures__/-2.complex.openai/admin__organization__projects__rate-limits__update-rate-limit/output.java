// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class AdminOrganizationProjectRateLimitService {
  private final Transport transport;

  AdminOrganizationProjectRateLimitService(Transport transport) {
    this.transport = transport;
  }

  /** Updates a project rate limit. */
  public ProjectRateLimit updateRateLimit(String projectId, String rateLimitId, AdminOrganizationProjectRateLimitUpdateRateLimitParams params) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (rateLimitId == null || rateLimitId.isEmpty()) {
      throw new IllegalArgumentException("missing required rate_limit_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/organization/projects/" + Transport.encodePath(projectId) + "/rate_limits/" + Transport.encodePath(rateLimitId));
    Map<String, Object> body = new LinkedHashMap<>();
    params.maxRequestsPer1Minute().ifPresent(value -> body.put("max_requests_per_1_minute", value));
    params.maxTokensPer1Minute().ifPresent(value -> body.put("max_tokens_per_1_minute", value));
    params.maxImagesPer1Minute().ifPresent(value -> body.put("max_images_per_1_minute", value));
    params.maxAudioMegabytesPer1Minute().ifPresent(value -> body.put("max_audio_megabytes_per_1_minute", value));
    params.maxRequestsPer1Day().ifPresent(value -> body.put("max_requests_per_1_day", value));
    params.batch1DayMaxInputTokens().ifPresent(value -> body.put("batch_1_day_max_input_tokens", value));
    request.body(body);
    return ProjectRateLimit.fromJson(transport.execute(request));
  }
}

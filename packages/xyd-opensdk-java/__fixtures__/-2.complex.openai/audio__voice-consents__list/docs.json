{
  "chain": [
    "audio",
    "voice-consents"
  ],
  "action": "list",
  "usage": "// Code generated by opensdk. DO NOT EDIT.\n\npackage com.example.openai;\n\npublic final class Example {\n  public static void main(String[] args) {\n    Client client = Client.builder().apiKey(System.getenv(\"OPENAI_API_KEY\")).build();\n    var result = client.audio().voiceConsents().list(AudioVoiceConsentListParams.builder()\n          .after(\"after\")\n          .limit(20L)\n          .build());\n    System.out.println(result);\n  }\n}\n",
  "typeReference": {
    "signature": "client.audio().voiceConsents().list(params) -> CursorPage<VoiceConsentResource>",
    "request": {
      "typeName": "AudioVoiceConsentListParams",
      "argName": "params",
      "fields": [
        {
          "name": "after",
          "langType": "Optional<String>",
          "required": false,
          "description": "A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n"
        },
        {
          "name": "limit",
          "langType": "Optional<Long>",
          "required": false,
          "description": "A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.\n"
        }
      ]
    },
    "response": {
      "typeName": "VoiceConsentResource",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "The object type, which is always `audio.voice_consent`."
        },
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "The consent recording identifier."
        },
        {
          "name": "name",
          "langType": "String",
          "required": true,
          "description": "The label provided when the consent recording was uploaded."
        },
        {
          "name": "language",
          "langType": "String",
          "required": true,
          "description": "The BCP 47 language tag for the consent phrase (for example, `en-US`)."
        },
        {
          "name": "createdAt",
          "langType": "Long",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the consent recording was created."
        }
      ]
    }
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class BetaThreadRunSubmitToolOutputService {
  private final Transport transport;

  BetaThreadRunSubmitToolOutputService(Transport transport) {
    this.transport = transport;
  }

  /** When a run has the `status: "requires_action"` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request. */
  public RunObject create(String threadId, String runId, BetaThreadRunSubmitToolOutputCreateParams params) {
    if (threadId == null || threadId.isEmpty()) {
      throw new IllegalArgumentException("missing required thread_id parameter");
    }
    if (runId == null || runId.isEmpty()) {
      throw new IllegalArgumentException("missing required run_id parameter");
    }
    Transport.Request request = new Transport.Request("POST", "/threads/" + Transport.encodePath(threadId) + "/runs/" + Transport.encodePath(runId) + "/submit_tool_outputs");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("tool_outputs", params.toolOutputs());
    params.stream().ifPresent(value -> body.put("stream", value));
    request.body(body);
    return RunObject.fromJson(transport.execute(request));
  }
}

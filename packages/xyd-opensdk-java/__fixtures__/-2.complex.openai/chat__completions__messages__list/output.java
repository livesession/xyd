// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class ChatCompletionMessageService {
  private final Transport transport;

  ChatCompletionMessageService(Transport transport) {
    this.transport = transport;
  }

  /**
   * Get the messages in a stored chat completion. Only Chat Completions that
   * have been created with the `store` parameter set to `true` will be
   * returned.
   */
  public CursorPage<ChatCompletionMessageListDataItem> list(String completionId, ChatCompletionMessageListParams params) {
    if (completionId == null || completionId.isEmpty()) {
      throw new IllegalArgumentException("missing required completion_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/chat/completions/" + Transport.encodePath(completionId) + "/messages");
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    params.order().ifPresent(value -> request.query("order", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> ChatCompletionMessageListDataItem.fromJson(__e));
  }
}

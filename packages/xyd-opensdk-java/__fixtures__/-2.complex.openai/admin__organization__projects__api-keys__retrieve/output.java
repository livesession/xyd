// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

public final class AdminOrganizationProjectApiKeyService {
  private final Transport transport;

  AdminOrganizationProjectApiKeyService(Transport transport) {
    this.transport = transport;
  }

  /** Retrieves an API key in the project. */
  public ProjectApiKey retrieve(String projectId, String apiKeyId) {
    if (projectId == null || projectId.isEmpty()) {
      throw new IllegalArgumentException("missing required project_id parameter");
    }
    if (apiKeyId == null || apiKeyId.isEmpty()) {
      throw new IllegalArgumentException("missing required api_key_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/organization/projects/" + Transport.encodePath(projectId) + "/api_keys/" + Transport.encodePath(apiKeyId));
    return ProjectApiKey.fromJson(transport.execute(request));
  }
}

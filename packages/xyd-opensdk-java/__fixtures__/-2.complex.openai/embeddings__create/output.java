// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class EmbeddingService {
  private final Transport transport;

  EmbeddingService(Transport transport) {
    this.transport = transport;
  }

  /** Creates an embedding vector representing the input text. */
  public CreateEmbeddingResponse create(EmbeddingCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/embeddings");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("input", params.input());
    body.put("model", params.model());
    params.encodingFormat().ifPresent(value -> body.put("encoding_format", value));
    params.dimensions().ifPresent(value -> body.put("dimensions", value));
    params.user().ifPresent(value -> body.put("user", value));
    request.body(body);
    return CreateEmbeddingResponse.fromJson(transport.execute(request));
  }
}

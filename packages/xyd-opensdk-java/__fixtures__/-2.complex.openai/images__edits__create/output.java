// Code generated by opensdk. DO NOT EDIT.

package com.example.openai;

import java.util.LinkedHashMap;
import java.util.Map;

public final class ImageEditService {
  private final Transport transport;

  ImageEditService(Transport transport) {
    this.transport = transport;
  }

  /**
   * You can call this endpoint with either:
   * 
   * - `multipart/form-data`: use binary uploads via `image` (and optional `mask`).
   * - `application/json`: use `images` (and optional `mask`) as references with either `image_url` or `file_id`.
   * 
   * Note that JSON requests use `images` (array) instead of the multipart `image` field.
   */
  public ImagesResponse create(ImageEditCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/images/edits");
    Map<String, Object> body = new LinkedHashMap<>();
    params.model().ifPresent(value -> body.put("model", value));
    body.put("images", params.images());
    params.mask().ifPresent(value -> body.put("mask", value));
    body.put("prompt", params.prompt());
    params.n().ifPresent(value -> body.put("n", value));
    params.quality().ifPresent(value -> body.put("quality", value));
    params.inputFidelity().ifPresent(value -> body.put("input_fidelity", value));
    params.size().ifPresent(value -> body.put("size", value));
    params.user().ifPresent(value -> body.put("user", value));
    params.outputFormat().ifPresent(value -> body.put("output_format", value));
    params.outputCompression().ifPresent(value -> body.put("output_compression", value));
    params.moderation().ifPresent(value -> body.put("moderation", value));
    params.background().ifPresent(value -> body.put("background", value));
    params.stream().ifPresent(value -> body.put("stream", value));
    params.partialImages().ifPresent(value -> body.put("partial_images", value));
    request.body(body);
    return ImagesResponse.fromJson(transport.execute(request));
  }
}

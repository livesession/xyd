// Code generated by opensdk. DO NOT EDIT.

package com.example.uniondepot;

import java.util.LinkedHashMap;
import java.util.Map;

public final class EventService {
  private final Transport transport;

  EventService(Transport transport) {
    this.transport = transport;
  }

  /** List events (cursor-paginated) */
  public CursorPage<Event> list(EventListParams params) {
    Transport.Request request = new Transport.Request("GET", "/events");
    params.after().ifPresent(value -> request.query("after", String.valueOf(value)));
    params.limit().ifPresent(value -> request.query("limit", String.valueOf(value)));
    return CursorPage.fromJson(transport.execute(request), __e -> Event.fromJson(__e));
  }

  /** Publish an event (const-filled body field) */
  public Event create(EventCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/events");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("object", "event");
    body.put("name", params.name());
    params.priority().ifPresent(value -> body.put("priority", value));
    request.body(body);
    return Event.fromJson(transport.execute(request));
  }
}

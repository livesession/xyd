// Code generated by opensdk. DO NOT EDIT.

package com.example.extensionsdemo;

public final class ThingService {
  private final Transport transport;

  ThingService(Transport transport) {
    this.transport = transport;
  }

  /** Retrieve a thing */
  public Thing fetch(String thingId) {
    if (thingId == null || thingId.isEmpty()) {
      throw new IllegalArgumentException("missing required thing_id parameter");
    }
    Transport.Request request = new Transport.Request("GET", "/things/" + Transport.encodePath(thingId));
    return Thing.fromJson(transport.execute(request));
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.wirekitchen;

import java.util.LinkedHashMap;
import java.util.Map;

public final class TokenService {
  private final Transport transport;

  TokenService(Transport transport) {
    this.transport = transport;
  }

  /** Create a token (application/x-www-form-urlencoded body). */
  public Token create(TokenCreateParams params) {
    Transport.Request request = new Transport.Request("POST", "/tokens");
    Map<String, Object> body = new LinkedHashMap<>();
    body.put("grant_type", params.grantType());
    params.scope().ifPresent(value -> body.put("scope", value));
    request.body(body);
    request.encoding("form");
    return Token.fromJson(transport.execute(request));
  }
}

// Code generated by opensdk. DO NOT EDIT.

package com.example.wirekitchen;

/**
 * A dependency-free assertion test for FileService — run with plain `java FileServiceTest`.
 */
public final class FileServiceTest {
  private static final String BASE_URL =
      System.getenv().getOrDefault("TEST_API_BASE_URL", "http://localhost:4010");

  public static void main(String[] args) {
    Client client = Client.builder().baseUrl(BASE_URL).apiKey("My API Key").build();
    testFileUploadWithOptionalParams(client);
    testFileListWithOptionalParams(client);
    testFileRetrieve(client);
    testFileRetrievePathParams(client);
    System.out.println("FileServiceTest OK");
  }

  private static void testFileUploadWithOptionalParams(Client client) {
    try {
      client.files().upload(FileUploadParams.builder()
          .idempotencyKey("idempotency_key")
          .file("Example data".getBytes(java.nio.charset.StandardCharsets.UTF_8))
          .purpose("x")
          .requestPriority("request_priority")
          .description("x")
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testFileListWithOptionalParams(Client client) {
    try {
      client.files().list(FileListParams.builder()
          .tags(java.util.List.of("x"))
          .statuses(java.util.List.of("x"))
          .metadata(java.util.Map.of("key", "x"))
          .purpose("purpose")
          .build());
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testFileRetrieve(Client client) {
    try {
      client.files().retrieve("file_id");
    } catch (ApiException expected) {
      // Offline: no mock server answering — a transport/HTTP error is acceptable here.
    }
  }

  private static void testFileRetrievePathParams(Client client) {
    try {
      client.files().retrieve("");
      throw new AssertionError("expected a missing required path param error for retrieve");
    } catch (IllegalArgumentException expected) {
      requireContains(expected.getMessage(), "missing required");
    }
  }

  private static void requireContains(String actual, String needle) {
    if (actual == null || !actual.contains(needle)) {
      throw new AssertionError("missing substring [" + needle + "] in: " + actual);
    }
  }
}

// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Events {
    transport: Arc<Transport>,
}

impl Events {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Events { transport }
    }

    /// List events (cursor-paginated)
    pub async fn list(&self, params: EventsListParams) -> Result<Page<Event>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/events");
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Publish an event (const-filled body field)
    pub async fn create(&self, params: EventsCreateParams) -> Result<Event, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/events");
        if let Some(value) = &params.object {
            req = req.json_field("object", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.priority {
            req = req.json_field("priority", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct EventsListParams {
    pub after: Option<String>,
    pub limit: Option<i64>,
}

#[derive(Debug, Clone, Default)]
pub struct EventsCreateParams {
    /// Always "event" — auto-filled by the SDK when left zero.
    pub object: Option<String>,
    pub name: Option<String>,
    pub priority: Option<i64>,
}

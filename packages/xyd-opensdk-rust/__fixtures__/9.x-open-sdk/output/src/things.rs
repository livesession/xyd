// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Things {
    transport: Arc<Transport>,
}

impl Things {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Things { transport }
    }

    /// Retrieve a thing
    pub async fn fetch(&self, thing_id: &str) -> Result<Thing, Error> {
        if thing_id.is_empty() {
            return Err(Error::InvalidArgument("thing_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/things/{}", thing_id));
        req.send().await
    }
}

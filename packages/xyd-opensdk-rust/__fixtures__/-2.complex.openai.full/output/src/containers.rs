// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Containers {
    transport: Arc<Transport>,
}

impl Containers {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Containers { transport }
    }

    pub fn files(&self) -> ContainersFiles {
        ContainersFiles::new(self.transport.clone())
    }

    /// Creates a container.
    pub async fn create(&self, params: ContainersCreateParams) -> Result<ContainerResource, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/containers");
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.file_ids {
            req = req.json_field("file_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.expires_after {
            req = req.json_field("expires_after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.skills {
            req = req.json_field("skills", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.memory_limit {
            req = req.json_field("memory_limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.network_policy {
            req = req.json_field("network_policy", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Delete a container.
    pub async fn delete(&self, container_id: &str) -> Result<(), Error> {
        if container_id.is_empty() {
            return Err(Error::InvalidArgument("container_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/containers/{}", container_id));
        req.send_empty().await
    }

    /// Lists containers.
    pub async fn list(&self, params: ContainersListParams) -> Result<Page<ContainerResource>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/containers");
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.name {
            req = req.query("name", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Retrieves a container.
    pub async fn retrieve(&self, container_id: &str) -> Result<ContainerResource, Error> {
        if container_id.is_empty() {
            return Err(Error::InvalidArgument("container_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/containers/{}", container_id));
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct ContainersCreateParams {
    /// Name of the container to create.
    pub name: Option<String>,
    /// IDs of files to copy to the container.
    pub file_ids: Option<Vec<String>>,
    /// Container expiration time in seconds relative to the 'anchor' time.
    pub expires_after: Option<CreateContainerBodyExpiresAfter>,
    /// An optional list of skills referenced by id or inline data.
    pub skills: Option<Vec<CreateContainerBodySkillsItem>>,
    /// Optional memory limit for the container. Defaults to "1g".
    pub memory_limit: Option<CreateContainerBodyMemoryLimit>,
    /// Network access policy for the container.
    pub network_policy: Option<CreateContainerBodyNetworkPolicy>,
}

#[derive(Debug, Clone, Default)]
pub struct ContainersListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.
    pub order: Option<ContainersListOrder>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
    /// Filter results by container name.
    pub name: Option<String>,
}

pub struct ContainersFiles {
    transport: Arc<Transport>,
}

impl ContainersFiles {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ContainersFiles { transport }
    }

    /// Retrieves a container file content.
    pub async fn content(&self, container_id: &str, file_id: &str) -> Result<(), Error> {
        if container_id.is_empty() {
            return Err(Error::InvalidArgument("container_id".to_string()));
        }
        if file_id.is_empty() {
            return Err(Error::InvalidArgument("file_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/containers/{}/files/{}/content", container_id, file_id));
        req.send_empty().await
    }

    /// Creates a container file.
    pub async fn create(&self, container_id: &str, params: ContainersFilesCreateParams) -> Result<ContainerFileResource, Error> {
        if container_id.is_empty() {
            return Err(Error::InvalidArgument("container_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/containers/{}/files", container_id));
        if let Some(value) = &params.file_id {
            req = req.json_field("file_id", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.file {
            req = req.json_field("file", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Delete a container file.
    pub async fn delete(&self, container_id: &str, file_id: &str) -> Result<(), Error> {
        if container_id.is_empty() {
            return Err(Error::InvalidArgument("container_id".to_string()));
        }
        if file_id.is_empty() {
            return Err(Error::InvalidArgument("file_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/containers/{}/files/{}", container_id, file_id));
        req.send_empty().await
    }

    /// Lists container files.
    pub async fn list(&self, container_id: &str, params: ContainersFilesListParams) -> Result<Page<ContainerFileResource>, Error> {
        if container_id.is_empty() {
            return Err(Error::InvalidArgument("container_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/containers/{}/files", container_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Retrieves a container file.
    pub async fn retrieve(&self, container_id: &str, file_id: &str) -> Result<ContainerFileResource, Error> {
        if container_id.is_empty() {
            return Err(Error::InvalidArgument("container_id".to_string()));
        }
        if file_id.is_empty() {
            return Err(Error::InvalidArgument("file_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/containers/{}/files/{}", container_id, file_id));
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct ContainersFilesCreateParams {
    /// Name of the file to create.
    pub file_id: Option<String>,
    /// The File object (not file name) to be uploaded.
    pub file: Option<Vec<u8>>,
}

#[derive(Debug, Clone, Default)]
pub struct ContainersFilesListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.
    pub order: Option<ContainersFilesListOrder>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
}

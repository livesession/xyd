// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Admin {
    transport: Arc<Transport>,
}

impl Admin {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Admin { transport }
    }

    pub fn organization(&self) -> AdminOrganization {
        AdminOrganization::new(self.transport.clone())
    }
}

pub struct AdminOrganization {
    transport: Arc<Transport>,
}

impl AdminOrganization {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganization { transport }
    }

    pub fn admin_api_keys(&self) -> AdminOrganizationAdminApiKeys {
        AdminOrganizationAdminApiKeys::new(self.transport.clone())
    }

    pub fn audit_logs(&self) -> AdminOrganizationAuditLogs {
        AdminOrganizationAuditLogs::new(self.transport.clone())
    }

    pub fn certificates(&self) -> AdminOrganizationCertificates {
        AdminOrganizationCertificates::new(self.transport.clone())
    }

    pub fn costs(&self) -> AdminOrganizationCosts {
        AdminOrganizationCosts::new(self.transport.clone())
    }

    pub fn groups(&self) -> AdminOrganizationGroups {
        AdminOrganizationGroups::new(self.transport.clone())
    }

    pub fn invites(&self) -> AdminOrganizationInvites {
        AdminOrganizationInvites::new(self.transport.clone())
    }

    pub fn projects(&self) -> AdminOrganizationProjects {
        AdminOrganizationProjects::new(self.transport.clone())
    }

    pub fn roles(&self) -> AdminOrganizationRoles {
        AdminOrganizationRoles::new(self.transport.clone())
    }

    pub fn usage(&self) -> AdminOrganizationUsage {
        AdminOrganizationUsage::new(self.transport.clone())
    }

    pub fn users(&self) -> AdminOrganizationUsers {
        AdminOrganizationUsers::new(self.transport.clone())
    }
}

pub struct AdminOrganizationAdminApiKeys {
    transport: Arc<Transport>,
}

impl AdminOrganizationAdminApiKeys {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationAdminApiKeys { transport }
    }

    /// Create a new admin-level API key for the organization.
    pub async fn create(&self, params: AdminOrganizationAdminApiKeysCreateParams) -> Result<AdminApiKeyCreateResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/organization/admin_api_keys");
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Delete the specified admin API key.
    pub async fn delete(&self, key_id: &str) -> Result<AdminOrganizationAdminApiKeysDeleteResponse, Error> {
        if key_id.is_empty() {
            return Err(Error::InvalidArgument("key_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/organization/admin_api_keys/{}", key_id));
        req.send().await
    }

    /// Retrieve a paginated list of organization admin API keys.
    pub async fn list(&self, params: AdminOrganizationAdminApiKeysListParams) -> Result<Page<AdminApiKey>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/admin_api_keys");
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Get details for a specific organization API key by its ID.
    pub async fn retrieve(&self, key_id: &str) -> Result<AdminApiKey, Error> {
        if key_id.is_empty() {
            return Err(Error::InvalidArgument("key_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/organization/admin_api_keys/{}", key_id));
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationAdminApiKeysCreateParams {
    pub name: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationAdminApiKeysListParams {
    pub after: Option<String>,
    pub order: Option<AdminOrganizationAdminApiKeysListOrder>,
    pub limit: Option<i64>,
}

pub struct AdminOrganizationAuditLogs {
    transport: Arc<Transport>,
}

impl AdminOrganizationAuditLogs {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationAuditLogs { transport }
    }

    /// List user actions and configuration changes within this organization.
    pub async fn list(&self, params: AdminOrganizationAuditLogsListParams) -> Result<Page<AuditLog>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/audit_logs");
        if let Some(value) = &params.effective_at {
            req = req.query("effective_at", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.project_ids {
            req = req.query("project_ids[]", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.event_types {
            req = req.query("event_types[]", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.actor_ids {
            req = req.query("actor_ids[]", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.actor_emails {
            req = req.query("actor_emails[]", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.resource_ids {
            req = req.query("resource_ids[]", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.before {
            req = req.query("before", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationAuditLogsListParams {
    /// Return only events whose `effective_at` (Unix seconds) is in this range.
    pub effective_at: Option<AdminOrganizationAuditLogsListEffectiveAt>,
    /// Return only events for these projects.
    pub project_ids: Option<Vec<String>>,
    /// Return only events with a `type` in one of these values. For example, `project.created`. For all options, see the documentation for the [audit log object](/docs/api-reference/audit-logs/object).
    pub event_types: Option<Vec<AuditLogEventType>>,
    /// Return only events performed by these actors. Can be a user ID, a service account ID, or an api key tracking ID.
    pub actor_ids: Option<Vec<String>>,
    /// Return only events performed by users with these emails.
    pub actor_emails: Option<Vec<String>>,
    /// Return only events performed on these targets. For example, a project ID updated.
    pub resource_ids: Option<Vec<String>>,
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
    /// A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.
    pub before: Option<String>,
}

pub struct AdminOrganizationCertificates {
    transport: Arc<Transport>,
}

impl AdminOrganizationCertificates {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationCertificates { transport }
    }

    pub fn activate(&self) -> AdminOrganizationCertificatesActivate {
        AdminOrganizationCertificatesActivate::new(self.transport.clone())
    }

    pub fn deactivate(&self) -> AdminOrganizationCertificatesDeactivate {
        AdminOrganizationCertificatesDeactivate::new(self.transport.clone())
    }

    /// Upload a certificate to the organization. This does **not** automatically activate the certificate.
    ///
    /// Organizations can upload up to 50 certificates.
    pub async fn create(&self, params: AdminOrganizationCertificatesCreateParams) -> Result<Certificate, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/organization/certificates");
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.certificate {
            req = req.json_field("certificate", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Delete a certificate from the organization.
    ///
    /// The certificate must be inactive for the organization and all projects.
    pub async fn delete(&self, certificate_id: &str) -> Result<DeleteCertificateResponse, Error> {
        if certificate_id.is_empty() {
            return Err(Error::InvalidArgument("certificate_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/organization/certificates/{}", certificate_id));
        req.send().await
    }

    /// List uploaded certificates for this organization.
    pub async fn list(&self, params: AdminOrganizationCertificatesListParams) -> Result<Page<OrganizationCertificate>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/certificates");
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Get a certificate that has been uploaded to the organization.
    ///
    /// You can get a certificate regardless of whether it is active or not.
    pub async fn retrieve(&self, certificate_id: &str, params: AdminOrganizationCertificatesRetrieveParams) -> Result<Certificate, Error> {
        if certificate_id.is_empty() {
            return Err(Error::InvalidArgument("certificate_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/organization/certificates/{}", certificate_id));
        if let Some(value) = &params.include {
            req = req.query("include", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Modify a certificate. Note that only the name can be modified.
    pub async fn update(&self, certificate_id: &str, params: AdminOrganizationCertificatesUpdateParams) -> Result<Certificate, Error> {
        if certificate_id.is_empty() {
            return Err(Error::InvalidArgument("certificate_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/certificates/{}", certificate_id));
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationCertificatesCreateParams {
    /// An optional name for the certificate
    pub name: Option<String>,
    /// The certificate content in PEM format
    pub certificate: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationCertificatesListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
    /// Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.
    pub order: Option<AdminOrganizationCertificatesListOrder>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationCertificatesRetrieveParams {
    /// A list of additional fields to include in the response. Currently the only supported value is `content` to fetch the PEM content of the certificate.
    pub include: Option<Vec<String>>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationCertificatesUpdateParams {
    /// The updated name for the certificate
    pub name: Option<String>,
}

pub struct AdminOrganizationCertificatesActivate {
    transport: Arc<Transport>,
}

impl AdminOrganizationCertificatesActivate {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationCertificatesActivate { transport }
    }

    /// Activate certificates at the organization level.
    ///
    /// You can atomically and idempotently activate up to 10 certificates at a time.
    pub async fn create(&self, params: AdminOrganizationCertificatesActivateCreateParams) -> Result<OrganizationCertificateActivationResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/organization/certificates/activate");
        if let Some(value) = &params.certificate_ids {
            req = req.json_field("certificate_ids", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationCertificatesActivateCreateParams {
    pub certificate_ids: Option<Vec<String>>,
}

pub struct AdminOrganizationCertificatesDeactivate {
    transport: Arc<Transport>,
}

impl AdminOrganizationCertificatesDeactivate {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationCertificatesDeactivate { transport }
    }

    /// Deactivate certificates at the organization level.
    ///
    /// You can atomically and idempotently deactivate up to 10 certificates at a time.
    pub async fn create(&self, params: AdminOrganizationCertificatesDeactivateCreateParams) -> Result<OrganizationCertificateDeactivationResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/organization/certificates/deactivate");
        if let Some(value) = &params.certificate_ids {
            req = req.json_field("certificate_ids", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationCertificatesDeactivateCreateParams {
    pub certificate_ids: Option<Vec<String>>,
}

pub struct AdminOrganizationCosts {
    transport: Arc<Transport>,
}

impl AdminOrganizationCosts {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationCosts { transport }
    }

    /// Get costs details for the organization.
    pub async fn list(&self, params: AdminOrganizationCostsListParams) -> Result<Page<UsageTimeBucket>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/costs");
        if let Some(value) = &params.start_time {
            req = req.query("start_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.end_time {
            req = req.query("end_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.bucket_width {
            req = req.query("bucket_width", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.project_ids {
            req = req.query("project_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.api_key_ids {
            req = req.query("api_key_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.group_by {
            req = req.query("group_by", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.page {
            req = req.query("page", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationCostsListParams {
    /// Start time (Unix seconds) of the query time range, inclusive.
    pub start_time: Option<i64>,
    /// End time (Unix seconds) of the query time range, exclusive.
    pub end_time: Option<i64>,
    /// Width of each time bucket in response. Currently only `1d` is supported, default to `1d`.
    pub bucket_width: Option<String>,
    /// Return only costs for these projects.
    pub project_ids: Option<Vec<String>>,
    /// Return only costs for these API keys.
    pub api_key_ids: Option<Vec<String>>,
    /// Group the costs by the specified fields. Support fields include `project_id`, `line_item`, `api_key_id` and any combination of them.
    pub group_by: Option<Vec<AdminOrganizationCostsListGroupByItem>>,
    /// A limit on the number of buckets to be returned. Limit can range between 1 and 180, and the default is 7.
    pub limit: Option<i64>,
    /// A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
    pub page: Option<String>,
}

pub struct AdminOrganizationGroups {
    transport: Arc<Transport>,
}

impl AdminOrganizationGroups {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationGroups { transport }
    }

    pub fn roles(&self) -> AdminOrganizationGroupsRoles {
        AdminOrganizationGroupsRoles::new(self.transport.clone())
    }

    pub fn users(&self) -> AdminOrganizationGroupsUsers {
        AdminOrganizationGroupsUsers::new(self.transport.clone())
    }

    /// Creates a new group in the organization.
    pub async fn create(&self, params: AdminOrganizationGroupsCreateParams) -> Result<GroupResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/organization/groups");
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Deletes a group from the organization.
    pub async fn delete(&self, group_id: &str) -> Result<GroupDeletedResource, Error> {
        if group_id.is_empty() {
            return Err(Error::InvalidArgument("group_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/organization/groups/{}", group_id));
        req.send().await
    }

    /// Lists all groups in the organization.
    pub async fn list(&self, params: AdminOrganizationGroupsListParams) -> Result<Page<GroupResponse>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/groups");
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Updates a group's information.
    pub async fn update(&self, group_id: &str, params: AdminOrganizationGroupsUpdateParams) -> Result<GroupResourceWithSuccess, Error> {
        if group_id.is_empty() {
            return Err(Error::InvalidArgument("group_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/groups/{}", group_id));
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationGroupsCreateParams {
    /// Human readable name for the group.
    pub name: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationGroupsListParams {
    /// A limit on the number of groups to be returned. Limit can range between 0 and 1000, and the default is 100.
    pub limit: Option<i64>,
    /// A cursor for use in pagination. `after` is a group ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with group_abc, your subsequent call can include `after=group_abc` in order to fetch the next page of the list.
    pub after: Option<String>,
    /// Specifies the sort order of the returned groups.
    pub order: Option<AdminOrganizationGroupsListOrder>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationGroupsUpdateParams {
    /// New display name for the group.
    pub name: Option<String>,
}

pub struct AdminOrganizationGroupsRoles {
    transport: Arc<Transport>,
}

impl AdminOrganizationGroupsRoles {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationGroupsRoles { transport }
    }

    /// Assigns an organization role to a group within the organization.
    pub async fn create(&self, group_id: &str, params: AdminOrganizationGroupsRolesCreateParams) -> Result<GroupRoleAssignment, Error> {
        if group_id.is_empty() {
            return Err(Error::InvalidArgument("group_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/groups/{}/roles", group_id));
        if let Some(value) = &params.role_id {
            req = req.json_field("role_id", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Unassigns an organization role from a group within the organization.
    pub async fn delete(&self, group_id: &str, role_id: &str) -> Result<DeletedRoleAssignmentResource, Error> {
        if group_id.is_empty() {
            return Err(Error::InvalidArgument("group_id".to_string()));
        }
        if role_id.is_empty() {
            return Err(Error::InvalidArgument("role_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/organization/groups/{}/roles/{}", group_id, role_id));
        req.send().await
    }

    /// Lists the organization roles assigned to a group within the organization.
    pub async fn list(&self, group_id: &str, params: AdminOrganizationGroupsRolesListParams) -> Result<Page<AssignedRoleDetails>, Error> {
        if group_id.is_empty() {
            return Err(Error::InvalidArgument("group_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/organization/groups/{}/roles", group_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationGroupsRolesCreateParams {
    /// Identifier of the role to assign.
    pub role_id: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationGroupsRolesListParams {
    /// A limit on the number of organization role assignments to return.
    pub limit: Option<i64>,
    /// Cursor for pagination. Provide the value from the previous response's `next` field to continue listing organization roles.
    pub after: Option<String>,
    /// Sort order for the returned organization roles.
    pub order: Option<AdminOrganizationGroupsRolesListOrder>,
}

pub struct AdminOrganizationGroupsUsers {
    transport: Arc<Transport>,
}

impl AdminOrganizationGroupsUsers {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationGroupsUsers { transport }
    }

    /// Adds a user to a group.
    pub async fn create(&self, group_id: &str, params: AdminOrganizationGroupsUsersCreateParams) -> Result<GroupUserAssignment, Error> {
        if group_id.is_empty() {
            return Err(Error::InvalidArgument("group_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/groups/{}/users", group_id));
        if let Some(value) = &params.user_id {
            req = req.json_field("user_id", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Removes a user from a group.
    pub async fn delete(&self, group_id: &str, user_id: &str) -> Result<GroupUserDeletedResource, Error> {
        if group_id.is_empty() {
            return Err(Error::InvalidArgument("group_id".to_string()));
        }
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/organization/groups/{}/users/{}", group_id, user_id));
        req.send().await
    }

    /// Lists the users assigned to a group.
    pub async fn list(&self, group_id: &str, params: AdminOrganizationGroupsUsersListParams) -> Result<Page<GroupUser>, Error> {
        if group_id.is_empty() {
            return Err(Error::InvalidArgument("group_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/organization/groups/{}/users", group_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationGroupsUsersCreateParams {
    /// Identifier of the user to add to the group.
    pub user_id: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationGroupsUsersListParams {
    /// A limit on the number of users to be returned. Limit can range between 0 and 1000, and the default is 100.
    pub limit: Option<i64>,
    /// A cursor for use in pagination. Provide the ID of the last user from the previous list response to retrieve the next page.
    pub after: Option<String>,
    /// Specifies the sort order of users in the list.
    pub order: Option<AdminOrganizationGroupsUsersListOrder>,
}

pub struct AdminOrganizationInvites {
    transport: Arc<Transport>,
}

impl AdminOrganizationInvites {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationInvites { transport }
    }

    /// Create an invite for a user to the organization. The invite must be accepted by the user before they have access to the organization.
    pub async fn create(&self, params: AdminOrganizationInvitesCreateParams) -> Result<Invite, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/organization/invites");
        if let Some(value) = &params.email {
            req = req.json_field("email", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.role {
            req = req.json_field("role", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.projects {
            req = req.json_field("projects", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Delete an invite. If the invite has already been accepted, it cannot be deleted.
    pub async fn delete(&self, invite_id: &str) -> Result<InviteDeleteResponse, Error> {
        if invite_id.is_empty() {
            return Err(Error::InvalidArgument("invite_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/organization/invites/{}", invite_id));
        req.send().await
    }

    /// Returns a list of invites in the organization.
    pub async fn list(&self, params: AdminOrganizationInvitesListParams) -> Result<Page<Invite>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/invites");
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Retrieves an invite.
    pub async fn retrieve(&self, invite_id: &str) -> Result<Invite, Error> {
        if invite_id.is_empty() {
            return Err(Error::InvalidArgument("invite_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/organization/invites/{}", invite_id));
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationInvitesCreateParams {
    /// Send an email to this address
    pub email: Option<String>,
    /// `owner` or `reader`
    pub role: Option<InviteRequestRole>,
    /// An array of projects to which membership is granted at the same time the org invite is accepted. If omitted, the user will be invited to the default project for compatibility with legacy behavior.
    pub projects: Option<Vec<InviteRequestProjectsItem>>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationInvitesListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
}

pub struct AdminOrganizationProjects {
    transport: Arc<Transport>,
}

impl AdminOrganizationProjects {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationProjects { transport }
    }

    pub fn api_keys(&self) -> AdminOrganizationProjectsApiKeys {
        AdminOrganizationProjectsApiKeys::new(self.transport.clone())
    }

    pub fn certificates(&self) -> AdminOrganizationProjectsCertificates {
        AdminOrganizationProjectsCertificates::new(self.transport.clone())
    }

    pub fn groups(&self) -> AdminOrganizationProjectsGroups {
        AdminOrganizationProjectsGroups::new(self.transport.clone())
    }

    pub fn rate_limits(&self) -> AdminOrganizationProjectsRateLimits {
        AdminOrganizationProjectsRateLimits::new(self.transport.clone())
    }

    pub fn service_accounts(&self) -> AdminOrganizationProjectsServiceAccounts {
        AdminOrganizationProjectsServiceAccounts::new(self.transport.clone())
    }

    pub fn users(&self) -> AdminOrganizationProjectsUsers {
        AdminOrganizationProjectsUsers::new(self.transport.clone())
    }

    /// Archives a project in the organization. Archived projects cannot be used or updated.
    pub async fn archive(&self, project_id: &str) -> Result<Project, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::POST, format!("/organization/projects/{}/archive", project_id));
        req.send().await
    }

    /// Create a new project in the organization. Projects can be created and archived, but cannot be deleted.
    pub async fn create(&self, params: AdminOrganizationProjectsCreateParams) -> Result<Project, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/organization/projects");
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.geography {
            req = req.json_field("geography", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.external_key_id {
            req = req.json_field("external_key_id", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Returns a list of projects.
    pub async fn list(&self, params: AdminOrganizationProjectsListParams) -> Result<Page<Project>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/projects");
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.include_archived {
            req = req.query("include_archived", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Retrieves a project.
    pub async fn retrieve(&self, project_id: &str) -> Result<Project, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/organization/projects/{}", project_id));
        req.send().await
    }

    /// Modifies a project in the organization.
    pub async fn update(&self, project_id: &str, params: AdminOrganizationProjectsUpdateParams) -> Result<Project, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/projects/{}", project_id));
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.external_key_id {
            req = req.json_field("external_key_id", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.geography {
            req = req.json_field("geography", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsCreateParams {
    /// The friendly name of the project, this name appears in reports.
    pub name: Option<String>,
    /// Create the project with the specified data residency region. Your organization must have access to Data residency functionality in order to use. See [data residency controls](/docs/guides/your-data#data-residency-controls) to review the functionality and limitations of setting this field.
    pub geography: Option<String>,
    /// External key ID to associate with the project.
    pub external_key_id: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
    /// If `true` returns all projects including those that have been `archived`. Archived projects are not included by default.
    pub include_archived: Option<bool>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsUpdateParams {
    /// The updated name of the project, this name appears in reports.
    pub name: Option<String>,
    /// External key ID to associate with the project.
    pub external_key_id: Option<String>,
    /// Geography for the project.
    pub geography: Option<String>,
}

pub struct AdminOrganizationProjectsApiKeys {
    transport: Arc<Transport>,
}

impl AdminOrganizationProjectsApiKeys {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationProjectsApiKeys { transport }
    }

    /// Deletes an API key from the project.
    ///
    /// Returns confirmation of the key deletion, or an error if the key belonged to
    /// a service account.
    pub async fn delete(&self, project_id: &str, api_key_id: &str) -> Result<ProjectApiKeyDeleteResponse, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if api_key_id.is_empty() {
            return Err(Error::InvalidArgument("api_key_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/organization/projects/{}/api_keys/{}", project_id, api_key_id));
        req.send().await
    }

    /// Returns a list of API keys in the project.
    pub async fn list(&self, project_id: &str, params: AdminOrganizationProjectsApiKeysListParams) -> Result<Page<ProjectApiKey>, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/organization/projects/{}/api_keys", project_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Retrieves an API key in the project.
    pub async fn retrieve(&self, project_id: &str, api_key_id: &str) -> Result<ProjectApiKey, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if api_key_id.is_empty() {
            return Err(Error::InvalidArgument("api_key_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/organization/projects/{}/api_keys/{}", project_id, api_key_id));
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsApiKeysListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
}

pub struct AdminOrganizationProjectsCertificates {
    transport: Arc<Transport>,
}

impl AdminOrganizationProjectsCertificates {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationProjectsCertificates { transport }
    }

    pub fn activate(&self) -> AdminOrganizationProjectsCertificatesActivate {
        AdminOrganizationProjectsCertificatesActivate::new(self.transport.clone())
    }

    pub fn deactivate(&self) -> AdminOrganizationProjectsCertificatesDeactivate {
        AdminOrganizationProjectsCertificatesDeactivate::new(self.transport.clone())
    }

    /// List certificates for this project.
    pub async fn list(&self, project_id: &str, params: AdminOrganizationProjectsCertificatesListParams) -> Result<Page<OrganizationProjectCertificate>, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/organization/projects/{}/certificates", project_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsCertificatesListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
    /// Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.
    pub order: Option<AdminOrganizationProjectsCertificatesListOrder>,
}

pub struct AdminOrganizationProjectsCertificatesActivate {
    transport: Arc<Transport>,
}

impl AdminOrganizationProjectsCertificatesActivate {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationProjectsCertificatesActivate { transport }
    }

    /// Activate certificates at the project level.
    ///
    /// You can atomically and idempotently activate up to 10 certificates at a time.
    pub async fn create(&self, project_id: &str, params: AdminOrganizationProjectsCertificatesActivateCreateParams) -> Result<OrganizationProjectCertificateActivationResponse, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/projects/{}/certificates/activate", project_id));
        if let Some(value) = &params.certificate_ids {
            req = req.json_field("certificate_ids", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsCertificatesActivateCreateParams {
    pub certificate_ids: Option<Vec<String>>,
}

pub struct AdminOrganizationProjectsCertificatesDeactivate {
    transport: Arc<Transport>,
}

impl AdminOrganizationProjectsCertificatesDeactivate {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationProjectsCertificatesDeactivate { transport }
    }

    /// Deactivate certificates at the project level. You can atomically and
    /// idempotently deactivate up to 10 certificates at a time.
    pub async fn create(&self, project_id: &str, params: AdminOrganizationProjectsCertificatesDeactivateCreateParams) -> Result<OrganizationProjectCertificateDeactivationResponse, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/projects/{}/certificates/deactivate", project_id));
        if let Some(value) = &params.certificate_ids {
            req = req.json_field("certificate_ids", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsCertificatesDeactivateCreateParams {
    pub certificate_ids: Option<Vec<String>>,
}

pub struct AdminOrganizationProjectsGroups {
    transport: Arc<Transport>,
}

impl AdminOrganizationProjectsGroups {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationProjectsGroups { transport }
    }

    /// Grants a group access to a project.
    pub async fn create(&self, project_id: &str, params: AdminOrganizationProjectsGroupsCreateParams) -> Result<ProjectGroup, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/projects/{}/groups", project_id));
        if let Some(value) = &params.group_id {
            req = req.json_field("group_id", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.role {
            req = req.json_field("role", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Revokes a group's access to a project.
    pub async fn delete(&self, project_id: &str, group_id: &str) -> Result<ProjectGroupDeletedResource, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if group_id.is_empty() {
            return Err(Error::InvalidArgument("group_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/organization/projects/{}/groups/{}", project_id, group_id));
        req.send().await
    }

    /// Lists the groups that have access to a project.
    pub async fn list(&self, project_id: &str, params: AdminOrganizationProjectsGroupsListParams) -> Result<Page<ProjectGroup>, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/organization/projects/{}/groups", project_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsGroupsCreateParams {
    /// Identifier of the group to add to the project.
    pub group_id: Option<String>,
    /// Identifier of the project role to grant to the group.
    pub role: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsGroupsListParams {
    /// A limit on the number of project groups to return. Defaults to 20.
    pub limit: Option<i64>,
    /// Cursor for pagination. Provide the ID of the last group from the previous response to fetch the next page.
    pub after: Option<String>,
    /// Sort order for the returned groups.
    pub order: Option<AdminOrganizationProjectsGroupsListOrder>,
}

pub struct AdminOrganizationProjectsRateLimits {
    transport: Arc<Transport>,
}

impl AdminOrganizationProjectsRateLimits {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationProjectsRateLimits { transport }
    }

    /// Returns the rate limits per model for a project.
    pub async fn list_rate_limits(&self, project_id: &str, params: AdminOrganizationProjectsRateLimitsListRateLimitsParams) -> Result<Page<ProjectRateLimit>, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/organization/projects/{}/rate_limits", project_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.before {
            req = req.query("before", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Updates a project rate limit.
    pub async fn update_rate_limit(&self, project_id: &str, rate_limit_id: &str, params: AdminOrganizationProjectsRateLimitsUpdateRateLimitParams) -> Result<ProjectRateLimit, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if rate_limit_id.is_empty() {
            return Err(Error::InvalidArgument("rate_limit_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/projects/{}/rate_limits/{}", project_id, rate_limit_id));
        if let Some(value) = &params.max_requests_per_1_minute {
            req = req.json_field("max_requests_per_1_minute", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.max_tokens_per_1_minute {
            req = req.json_field("max_tokens_per_1_minute", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.max_images_per_1_minute {
            req = req.json_field("max_images_per_1_minute", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.max_audio_megabytes_per_1_minute {
            req = req.json_field("max_audio_megabytes_per_1_minute", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.max_requests_per_1_day {
            req = req.json_field("max_requests_per_1_day", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.batch_1_day_max_input_tokens {
            req = req.json_field("batch_1_day_max_input_tokens", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsRateLimitsListRateLimitsParams {
    /// A limit on the number of objects to be returned. The default is 100.
    pub limit: Option<i64>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
    /// A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, beginning with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.
    pub before: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsRateLimitsUpdateRateLimitParams {
    /// The maximum requests per minute.
    pub max_requests_per_1_minute: Option<i64>,
    /// The maximum tokens per minute.
    pub max_tokens_per_1_minute: Option<i64>,
    /// The maximum images per minute. Only relevant for certain models.
    pub max_images_per_1_minute: Option<i64>,
    /// The maximum audio megabytes per minute. Only relevant for certain models.
    pub max_audio_megabytes_per_1_minute: Option<i64>,
    /// The maximum requests per day. Only relevant for certain models.
    pub max_requests_per_1_day: Option<i64>,
    /// The maximum batch input tokens per day. Only relevant for certain models.
    pub batch_1_day_max_input_tokens: Option<i64>,
}

pub struct AdminOrganizationProjectsServiceAccounts {
    transport: Arc<Transport>,
}

impl AdminOrganizationProjectsServiceAccounts {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationProjectsServiceAccounts { transport }
    }

    /// Creates a new service account in the project. This also returns an unredacted API key for the service account.
    pub async fn create(&self, project_id: &str, params: AdminOrganizationProjectsServiceAccountsCreateParams) -> Result<ProjectServiceAccountCreateResponse, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/projects/{}/service_accounts", project_id));
        if let Some(value) = &params.name {
            req = req.json_field("name", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Deletes a service account from the project.
    ///
    /// Returns confirmation of service account deletion, or an error if the project
    /// is archived (archived projects have no service accounts).
    pub async fn delete(&self, project_id: &str, service_account_id: &str) -> Result<ProjectServiceAccountDeleteResponse, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if service_account_id.is_empty() {
            return Err(Error::InvalidArgument("service_account_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/organization/projects/{}/service_accounts/{}", project_id, service_account_id));
        req.send().await
    }

    /// Returns a list of service accounts in the project.
    pub async fn list(&self, project_id: &str, params: AdminOrganizationProjectsServiceAccountsListParams) -> Result<Page<ProjectServiceAccount>, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/organization/projects/{}/service_accounts", project_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Retrieves a service account in the project.
    pub async fn retrieve(&self, project_id: &str, service_account_id: &str) -> Result<ProjectServiceAccount, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if service_account_id.is_empty() {
            return Err(Error::InvalidArgument("service_account_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/organization/projects/{}/service_accounts/{}", project_id, service_account_id));
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsServiceAccountsCreateParams {
    /// The name of the service account being created.
    pub name: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsServiceAccountsListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
}

pub struct AdminOrganizationProjectsUsers {
    transport: Arc<Transport>,
}

impl AdminOrganizationProjectsUsers {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationProjectsUsers { transport }
    }

    /// Adds a user to the project. Users must already be members of the organization to be added to a project.
    pub async fn create(&self, project_id: &str, params: AdminOrganizationProjectsUsersCreateParams) -> Result<ProjectUser, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/projects/{}/users", project_id));
        if let Some(value) = &params.user_id {
            req = req.json_field("user_id", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.email {
            req = req.json_field("email", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.role {
            req = req.json_field("role", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Deletes a user from the project.
    ///
    /// Returns confirmation of project user deletion, or an error if the project is
    /// archived (archived projects have no users).
    pub async fn delete(&self, project_id: &str, user_id: &str) -> Result<ProjectUserDeleteResponse, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/organization/projects/{}/users/{}", project_id, user_id));
        req.send().await
    }

    /// Returns a list of users in the project.
    pub async fn list(&self, project_id: &str, params: AdminOrganizationProjectsUsersListParams) -> Result<Page<ProjectUser>, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/organization/projects/{}/users", project_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Retrieves a user in the project.
    pub async fn retrieve(&self, project_id: &str, user_id: &str) -> Result<ProjectUser, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/organization/projects/{}/users/{}", project_id, user_id));
        req.send().await
    }

    /// Modifies a user's role in the project.
    pub async fn update(&self, project_id: &str, user_id: &str, params: AdminOrganizationProjectsUsersUpdateParams) -> Result<ProjectUser, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/projects/{}/users/{}", project_id, user_id));
        if let Some(value) = &params.role {
            req = req.json_field("role", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsUsersCreateParams {
    /// The ID of the user.
    pub user_id: Option<String>,
    /// Email of the user to add.
    pub email: Option<String>,
    /// `owner` or `member`
    pub role: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsUsersListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationProjectsUsersUpdateParams {
    /// `owner` or `member`
    pub role: Option<String>,
}

pub struct AdminOrganizationRoles {
    transport: Arc<Transport>,
}

impl AdminOrganizationRoles {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationRoles { transport }
    }

    /// Creates a custom role for the organization.
    pub async fn create(&self, params: AdminOrganizationRolesCreateParams) -> Result<Role, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/organization/roles");
        if let Some(value) = &params.role_name {
            req = req.json_field("role_name", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.permissions {
            req = req.json_field("permissions", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.description {
            req = req.json_field("description", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Deletes a custom role from the organization.
    pub async fn delete(&self, role_id: &str) -> Result<RoleDeletedResource, Error> {
        if role_id.is_empty() {
            return Err(Error::InvalidArgument("role_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/organization/roles/{}", role_id));
        req.send().await
    }

    /// Lists the roles configured for the organization.
    pub async fn list(&self, params: AdminOrganizationRolesListParams) -> Result<Page<Role>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/roles");
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Updates an existing organization role.
    pub async fn update(&self, role_id: &str, params: AdminOrganizationRolesUpdateParams) -> Result<Role, Error> {
        if role_id.is_empty() {
            return Err(Error::InvalidArgument("role_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/roles/{}", role_id));
        if let Some(value) = &params.permissions {
            req = req.json_field("permissions", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.description {
            req = req.json_field("description", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.role_name {
            req = req.json_field("role_name", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationRolesCreateParams {
    /// Unique name for the role.
    pub role_name: Option<String>,
    /// Permissions to grant to the role.
    pub permissions: Option<Vec<String>>,
    /// Optional description of the role.
    pub description: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationRolesListParams {
    /// A limit on the number of roles to return. Defaults to 1000.
    pub limit: Option<i64>,
    /// Cursor for pagination. Provide the value from the previous response's `next` field to continue listing roles.
    pub after: Option<String>,
    /// Sort order for the returned roles.
    pub order: Option<AdminOrganizationRolesListOrder>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationRolesUpdateParams {
    /// Updated set of permissions for the role.
    pub permissions: Option<Vec<String>>,
    /// New description for the role.
    pub description: Option<String>,
    /// New name for the role.
    pub role_name: Option<String>,
}

pub struct AdminOrganizationUsage {
    transport: Arc<Transport>,
}

impl AdminOrganizationUsage {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationUsage { transport }
    }

    pub fn audio_speeches(&self) -> AdminOrganizationUsageAudioSpeeches {
        AdminOrganizationUsageAudioSpeeches::new(self.transport.clone())
    }

    pub fn audio_transcriptions(&self) -> AdminOrganizationUsageAudioTranscriptions {
        AdminOrganizationUsageAudioTranscriptions::new(self.transport.clone())
    }

    pub fn code_interpreter_sessions(&self) -> AdminOrganizationUsageCodeInterpreterSessions {
        AdminOrganizationUsageCodeInterpreterSessions::new(self.transport.clone())
    }

    pub fn completions(&self) -> AdminOrganizationUsageCompletions {
        AdminOrganizationUsageCompletions::new(self.transport.clone())
    }

    pub fn embeddings(&self) -> AdminOrganizationUsageEmbeddings {
        AdminOrganizationUsageEmbeddings::new(self.transport.clone())
    }

    pub fn images(&self) -> AdminOrganizationUsageImages {
        AdminOrganizationUsageImages::new(self.transport.clone())
    }

    pub fn moderations(&self) -> AdminOrganizationUsageModerations {
        AdminOrganizationUsageModerations::new(self.transport.clone())
    }

    pub fn vector_stores(&self) -> AdminOrganizationUsageVectorStores {
        AdminOrganizationUsageVectorStores::new(self.transport.clone())
    }
}

pub struct AdminOrganizationUsageAudioSpeeches {
    transport: Arc<Transport>,
}

impl AdminOrganizationUsageAudioSpeeches {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationUsageAudioSpeeches { transport }
    }

    /// Get audio speeches usage details for the organization.
    pub async fn list(&self, params: AdminOrganizationUsageAudioSpeechesListParams) -> Result<Page<UsageTimeBucket>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/usage/audio_speeches");
        if let Some(value) = &params.start_time {
            req = req.query("start_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.end_time {
            req = req.query("end_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.bucket_width {
            req = req.query("bucket_width", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.project_ids {
            req = req.query("project_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.user_ids {
            req = req.query("user_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.api_key_ids {
            req = req.query("api_key_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.models {
            req = req.query("models", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.group_by {
            req = req.query("group_by", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.page {
            req = req.query("page", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationUsageAudioSpeechesListParams {
    /// Start time (Unix seconds) of the query time range, inclusive.
    pub start_time: Option<i64>,
    /// End time (Unix seconds) of the query time range, exclusive.
    pub end_time: Option<i64>,
    /// Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
    pub bucket_width: Option<AdminOrganizationUsageAudioSpeechesListBucketWidth>,
    /// Return only usage for these projects.
    pub project_ids: Option<Vec<String>>,
    /// Return only usage for these users.
    pub user_ids: Option<Vec<String>>,
    /// Return only usage for these API keys.
    pub api_key_ids: Option<Vec<String>>,
    /// Return only usage for these models.
    pub models: Option<Vec<String>>,
    /// Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model` or any combination of them.
    pub group_by: Option<Vec<AdminOrganizationUsageAudioSpeechesListGroupByItem>>,
    /// Specifies the number of buckets to return.
    /// - `bucket_width=1d`: default: 7, max: 31
    /// - `bucket_width=1h`: default: 24, max: 168
    /// - `bucket_width=1m`: default: 60, max: 1440
    pub limit: Option<i64>,
    /// A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
    pub page: Option<String>,
}

pub struct AdminOrganizationUsageAudioTranscriptions {
    transport: Arc<Transport>,
}

impl AdminOrganizationUsageAudioTranscriptions {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationUsageAudioTranscriptions { transport }
    }

    /// Get audio transcriptions usage details for the organization.
    pub async fn list(&self, params: AdminOrganizationUsageAudioTranscriptionsListParams) -> Result<Page<UsageTimeBucket>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/usage/audio_transcriptions");
        if let Some(value) = &params.start_time {
            req = req.query("start_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.end_time {
            req = req.query("end_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.bucket_width {
            req = req.query("bucket_width", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.project_ids {
            req = req.query("project_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.user_ids {
            req = req.query("user_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.api_key_ids {
            req = req.query("api_key_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.models {
            req = req.query("models", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.group_by {
            req = req.query("group_by", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.page {
            req = req.query("page", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationUsageAudioTranscriptionsListParams {
    /// Start time (Unix seconds) of the query time range, inclusive.
    pub start_time: Option<i64>,
    /// End time (Unix seconds) of the query time range, exclusive.
    pub end_time: Option<i64>,
    /// Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
    pub bucket_width: Option<AdminOrganizationUsageAudioTranscriptionsListBucketWidth>,
    /// Return only usage for these projects.
    pub project_ids: Option<Vec<String>>,
    /// Return only usage for these users.
    pub user_ids: Option<Vec<String>>,
    /// Return only usage for these API keys.
    pub api_key_ids: Option<Vec<String>>,
    /// Return only usage for these models.
    pub models: Option<Vec<String>>,
    /// Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model` or any combination of them.
    pub group_by: Option<Vec<AdminOrganizationUsageAudioTranscriptionsListGroupByItem>>,
    /// Specifies the number of buckets to return.
    /// - `bucket_width=1d`: default: 7, max: 31
    /// - `bucket_width=1h`: default: 24, max: 168
    /// - `bucket_width=1m`: default: 60, max: 1440
    pub limit: Option<i64>,
    /// A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
    pub page: Option<String>,
}

pub struct AdminOrganizationUsageCodeInterpreterSessions {
    transport: Arc<Transport>,
}

impl AdminOrganizationUsageCodeInterpreterSessions {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationUsageCodeInterpreterSessions { transport }
    }

    /// Get code interpreter sessions usage details for the organization.
    pub async fn list(&self, params: AdminOrganizationUsageCodeInterpreterSessionsListParams) -> Result<Page<UsageTimeBucket>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/usage/code_interpreter_sessions");
        if let Some(value) = &params.start_time {
            req = req.query("start_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.end_time {
            req = req.query("end_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.bucket_width {
            req = req.query("bucket_width", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.project_ids {
            req = req.query("project_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.group_by {
            req = req.query("group_by", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.page {
            req = req.query("page", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationUsageCodeInterpreterSessionsListParams {
    /// Start time (Unix seconds) of the query time range, inclusive.
    pub start_time: Option<i64>,
    /// End time (Unix seconds) of the query time range, exclusive.
    pub end_time: Option<i64>,
    /// Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
    pub bucket_width: Option<AdminOrganizationUsageCodeInterpreterSessionsListBucketWidth>,
    /// Return only usage for these projects.
    pub project_ids: Option<Vec<String>>,
    /// Group the usage data by the specified fields. Support fields include `project_id`.
    pub group_by: Option<Vec<String>>,
    /// Specifies the number of buckets to return.
    /// - `bucket_width=1d`: default: 7, max: 31
    /// - `bucket_width=1h`: default: 24, max: 168
    /// - `bucket_width=1m`: default: 60, max: 1440
    pub limit: Option<i64>,
    /// A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
    pub page: Option<String>,
}

pub struct AdminOrganizationUsageCompletions {
    transport: Arc<Transport>,
}

impl AdminOrganizationUsageCompletions {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationUsageCompletions { transport }
    }

    /// Get completions usage details for the organization.
    pub async fn list(&self, params: AdminOrganizationUsageCompletionsListParams) -> Result<Page<UsageTimeBucket>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/usage/completions");
        if let Some(value) = &params.start_time {
            req = req.query("start_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.end_time {
            req = req.query("end_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.bucket_width {
            req = req.query("bucket_width", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.project_ids {
            req = req.query("project_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.user_ids {
            req = req.query("user_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.api_key_ids {
            req = req.query("api_key_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.models {
            req = req.query("models", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.batch {
            req = req.query("batch", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.group_by {
            req = req.query("group_by", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.page {
            req = req.query("page", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationUsageCompletionsListParams {
    /// Start time (Unix seconds) of the query time range, inclusive.
    pub start_time: Option<i64>,
    /// End time (Unix seconds) of the query time range, exclusive.
    pub end_time: Option<i64>,
    /// Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
    pub bucket_width: Option<AdminOrganizationUsageCompletionsListBucketWidth>,
    /// Return only usage for these projects.
    pub project_ids: Option<Vec<String>>,
    /// Return only usage for these users.
    pub user_ids: Option<Vec<String>>,
    /// Return only usage for these API keys.
    pub api_key_ids: Option<Vec<String>>,
    /// Return only usage for these models.
    pub models: Option<Vec<String>>,
    /// If `true`, return batch jobs only. If `false`, return non-batch jobs only. By default, return both.
    pub batch: Option<bool>,
    /// Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model`, `batch`, `service_tier` or any combination of them.
    pub group_by: Option<Vec<AdminOrganizationUsageCompletionsListGroupByItem>>,
    /// Specifies the number of buckets to return.
    /// - `bucket_width=1d`: default: 7, max: 31
    /// - `bucket_width=1h`: default: 24, max: 168
    /// - `bucket_width=1m`: default: 60, max: 1440
    pub limit: Option<i64>,
    /// A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
    pub page: Option<String>,
}

pub struct AdminOrganizationUsageEmbeddings {
    transport: Arc<Transport>,
}

impl AdminOrganizationUsageEmbeddings {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationUsageEmbeddings { transport }
    }

    /// Get embeddings usage details for the organization.
    pub async fn list(&self, params: AdminOrganizationUsageEmbeddingsListParams) -> Result<Page<UsageTimeBucket>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/usage/embeddings");
        if let Some(value) = &params.start_time {
            req = req.query("start_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.end_time {
            req = req.query("end_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.bucket_width {
            req = req.query("bucket_width", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.project_ids {
            req = req.query("project_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.user_ids {
            req = req.query("user_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.api_key_ids {
            req = req.query("api_key_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.models {
            req = req.query("models", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.group_by {
            req = req.query("group_by", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.page {
            req = req.query("page", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationUsageEmbeddingsListParams {
    /// Start time (Unix seconds) of the query time range, inclusive.
    pub start_time: Option<i64>,
    /// End time (Unix seconds) of the query time range, exclusive.
    pub end_time: Option<i64>,
    /// Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
    pub bucket_width: Option<AdminOrganizationUsageEmbeddingsListBucketWidth>,
    /// Return only usage for these projects.
    pub project_ids: Option<Vec<String>>,
    /// Return only usage for these users.
    pub user_ids: Option<Vec<String>>,
    /// Return only usage for these API keys.
    pub api_key_ids: Option<Vec<String>>,
    /// Return only usage for these models.
    pub models: Option<Vec<String>>,
    /// Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model` or any combination of them.
    pub group_by: Option<Vec<AdminOrganizationUsageEmbeddingsListGroupByItem>>,
    /// Specifies the number of buckets to return.
    /// - `bucket_width=1d`: default: 7, max: 31
    /// - `bucket_width=1h`: default: 24, max: 168
    /// - `bucket_width=1m`: default: 60, max: 1440
    pub limit: Option<i64>,
    /// A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
    pub page: Option<String>,
}

pub struct AdminOrganizationUsageImages {
    transport: Arc<Transport>,
}

impl AdminOrganizationUsageImages {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationUsageImages { transport }
    }

    /// Get images usage details for the organization.
    pub async fn list(&self, params: AdminOrganizationUsageImagesListParams) -> Result<Page<UsageTimeBucket>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/usage/images");
        if let Some(value) = &params.start_time {
            req = req.query("start_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.end_time {
            req = req.query("end_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.bucket_width {
            req = req.query("bucket_width", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.sources {
            req = req.query("sources", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.sizes {
            req = req.query("sizes", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.project_ids {
            req = req.query("project_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.user_ids {
            req = req.query("user_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.api_key_ids {
            req = req.query("api_key_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.models {
            req = req.query("models", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.group_by {
            req = req.query("group_by", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.page {
            req = req.query("page", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationUsageImagesListParams {
    /// Start time (Unix seconds) of the query time range, inclusive.
    pub start_time: Option<i64>,
    /// End time (Unix seconds) of the query time range, exclusive.
    pub end_time: Option<i64>,
    /// Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
    pub bucket_width: Option<AdminOrganizationUsageImagesListBucketWidth>,
    /// Return only usages for these sources. Possible values are `image.generation`, `image.edit`, `image.variation` or any combination of them.
    pub sources: Option<Vec<AdminOrganizationUsageImagesListSourcesItem>>,
    /// Return only usages for these image sizes. Possible values are `256x256`, `512x512`, `1024x1024`, `1792x1792`, `1024x1792` or any combination of them.
    pub sizes: Option<Vec<AdminOrganizationUsageImagesListSizesItem>>,
    /// Return only usage for these projects.
    pub project_ids: Option<Vec<String>>,
    /// Return only usage for these users.
    pub user_ids: Option<Vec<String>>,
    /// Return only usage for these API keys.
    pub api_key_ids: Option<Vec<String>>,
    /// Return only usage for these models.
    pub models: Option<Vec<String>>,
    /// Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model`, `size`, `source` or any combination of them.
    pub group_by: Option<Vec<AdminOrganizationUsageImagesListGroupByItem>>,
    /// Specifies the number of buckets to return.
    /// - `bucket_width=1d`: default: 7, max: 31
    /// - `bucket_width=1h`: default: 24, max: 168
    /// - `bucket_width=1m`: default: 60, max: 1440
    pub limit: Option<i64>,
    /// A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
    pub page: Option<String>,
}

pub struct AdminOrganizationUsageModerations {
    transport: Arc<Transport>,
}

impl AdminOrganizationUsageModerations {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationUsageModerations { transport }
    }

    /// Get moderations usage details for the organization.
    pub async fn list(&self, params: AdminOrganizationUsageModerationsListParams) -> Result<Page<UsageTimeBucket>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/usage/moderations");
        if let Some(value) = &params.start_time {
            req = req.query("start_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.end_time {
            req = req.query("end_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.bucket_width {
            req = req.query("bucket_width", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.project_ids {
            req = req.query("project_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.user_ids {
            req = req.query("user_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.api_key_ids {
            req = req.query("api_key_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.models {
            req = req.query("models", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.group_by {
            req = req.query("group_by", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.page {
            req = req.query("page", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationUsageModerationsListParams {
    /// Start time (Unix seconds) of the query time range, inclusive.
    pub start_time: Option<i64>,
    /// End time (Unix seconds) of the query time range, exclusive.
    pub end_time: Option<i64>,
    /// Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
    pub bucket_width: Option<AdminOrganizationUsageModerationsListBucketWidth>,
    /// Return only usage for these projects.
    pub project_ids: Option<Vec<String>>,
    /// Return only usage for these users.
    pub user_ids: Option<Vec<String>>,
    /// Return only usage for these API keys.
    pub api_key_ids: Option<Vec<String>>,
    /// Return only usage for these models.
    pub models: Option<Vec<String>>,
    /// Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model` or any combination of them.
    pub group_by: Option<Vec<AdminOrganizationUsageModerationsListGroupByItem>>,
    /// Specifies the number of buckets to return.
    /// - `bucket_width=1d`: default: 7, max: 31
    /// - `bucket_width=1h`: default: 24, max: 168
    /// - `bucket_width=1m`: default: 60, max: 1440
    pub limit: Option<i64>,
    /// A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
    pub page: Option<String>,
}

pub struct AdminOrganizationUsageVectorStores {
    transport: Arc<Transport>,
}

impl AdminOrganizationUsageVectorStores {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationUsageVectorStores { transport }
    }

    /// Get vector stores usage details for the organization.
    pub async fn list(&self, params: AdminOrganizationUsageVectorStoresListParams) -> Result<Page<UsageTimeBucket>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/usage/vector_stores");
        if let Some(value) = &params.start_time {
            req = req.query("start_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.end_time {
            req = req.query("end_time", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.bucket_width {
            req = req.query("bucket_width", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.project_ids {
            req = req.query("project_ids", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.group_by {
            req = req.query("group_by", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.page {
            req = req.query("page", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationUsageVectorStoresListParams {
    /// Start time (Unix seconds) of the query time range, inclusive.
    pub start_time: Option<i64>,
    /// End time (Unix seconds) of the query time range, exclusive.
    pub end_time: Option<i64>,
    /// Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.
    pub bucket_width: Option<AdminOrganizationUsageVectorStoresListBucketWidth>,
    /// Return only usage for these projects.
    pub project_ids: Option<Vec<String>>,
    /// Group the usage data by the specified fields. Support fields include `project_id`.
    pub group_by: Option<Vec<String>>,
    /// Specifies the number of buckets to return.
    /// - `bucket_width=1d`: default: 7, max: 31
    /// - `bucket_width=1h`: default: 24, max: 168
    /// - `bucket_width=1m`: default: 60, max: 1440
    pub limit: Option<i64>,
    /// A cursor for use in pagination. Corresponding to the `next_page` field from the previous response.
    pub page: Option<String>,
}

pub struct AdminOrganizationUsers {
    transport: Arc<Transport>,
}

impl AdminOrganizationUsers {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationUsers { transport }
    }

    pub fn roles(&self) -> AdminOrganizationUsersRoles {
        AdminOrganizationUsersRoles::new(self.transport.clone())
    }

    /// Deletes a user from the organization.
    pub async fn delete(&self, user_id: &str) -> Result<UserDeleteResponse, Error> {
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/organization/users/{}", user_id));
        req.send().await
    }

    /// Lists all of the users in the organization.
    pub async fn list(&self, params: AdminOrganizationUsersListParams) -> Result<Page<User>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/organization/users");
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.emails {
            req = req.query("emails", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Retrieves a user by their identifier.
    pub async fn retrieve(&self, user_id: &str) -> Result<User, Error> {
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/organization/users/{}", user_id));
        req.send().await
    }

    /// Modifies a user's role in the organization.
    pub async fn update(&self, user_id: &str, params: AdminOrganizationUsersUpdateParams) -> Result<User, Error> {
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/users/{}", user_id));
        if let Some(value) = &params.role {
            req = req.json_field("role", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.role_id {
            req = req.json_field("role_id", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.technical_level {
            req = req.json_field("technical_level", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.developer_persona {
            req = req.json_field("developer_persona", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationUsersListParams {
    /// A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.
    pub limit: Option<i64>,
    /// A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.
    pub after: Option<String>,
    /// Filter by the email address of users.
    pub emails: Option<Vec<String>>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationUsersUpdateParams {
    /// `owner` or `reader`
    pub role: Option<String>,
    /// Role ID to assign to the user.
    pub role_id: Option<String>,
    /// Technical level metadata.
    pub technical_level: Option<String>,
    /// Developer persona metadata.
    pub developer_persona: Option<String>,
}

pub struct AdminOrganizationUsersRoles {
    transport: Arc<Transport>,
}

impl AdminOrganizationUsersRoles {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        AdminOrganizationUsersRoles { transport }
    }

    /// Assigns an organization role to a user within the organization.
    pub async fn create(&self, user_id: &str, params: AdminOrganizationUsersRolesCreateParams) -> Result<UserRoleAssignment, Error> {
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/organization/users/{}/roles", user_id));
        if let Some(value) = &params.role_id {
            req = req.json_field("role_id", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Unassigns an organization role from a user within the organization.
    pub async fn delete(&self, user_id: &str, role_id: &str) -> Result<DeletedRoleAssignmentResource, Error> {
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }
        if role_id.is_empty() {
            return Err(Error::InvalidArgument("role_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/organization/users/{}/roles/{}", user_id, role_id));
        req.send().await
    }

    /// Lists the organization roles assigned to a user within the organization.
    pub async fn list(&self, user_id: &str, params: AdminOrganizationUsersRolesListParams) -> Result<Page<AssignedRoleDetails>, Error> {
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/organization/users/{}/roles", user_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationUsersRolesCreateParams {
    /// Identifier of the role to assign.
    pub role_id: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct AdminOrganizationUsersRolesListParams {
    /// A limit on the number of organization role assignments to return.
    pub limit: Option<i64>,
    /// Cursor for pagination. Provide the value from the previous response's `next` field to continue listing organization roles.
    pub after: Option<String>,
    /// Sort order for the returned organization roles.
    pub order: Option<AdminOrganizationUsersRolesListOrder>,
}

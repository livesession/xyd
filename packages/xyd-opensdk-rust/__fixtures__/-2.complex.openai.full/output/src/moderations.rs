// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Moderations {
    transport: Arc<Transport>,
}

impl Moderations {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Moderations { transport }
    }

    /// Classifies if text and/or image inputs are potentially harmful. Learn
    /// more in the [moderation guide](/docs/guides/moderation).
    pub async fn create(&self, params: ModerationsCreateParams) -> Result<CreateModerationResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/moderations");
        if let Some(value) = &params.input {
            req = req.json_field("input", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.model {
            req = req.json_field("model", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct ModerationsCreateParams {
    /// Input (or inputs) to classify. Can be a single string, an array of strings, or
    /// an array of multi-modal input objects similar to other models.
    pub input: Option<CreateModerationRequestInput>,
    /// The content moderation model you would like to use. Learn more in
    /// [the moderation guide](/docs/guides/moderation), and learn about
    /// available models [here](/docs/models#moderation).
    pub model: Option<CreateModerationRequestModel>,
}

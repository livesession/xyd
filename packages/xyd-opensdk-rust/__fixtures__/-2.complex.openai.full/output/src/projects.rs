// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Projects {
    transport: Arc<Transport>,
}

impl Projects {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Projects { transport }
    }

    pub fn groups(&self) -> ProjectsGroups {
        ProjectsGroups::new(self.transport.clone())
    }

    pub fn roles(&self) -> ProjectsRoles {
        ProjectsRoles::new(self.transport.clone())
    }

    pub fn users(&self) -> ProjectsUsers {
        ProjectsUsers::new(self.transport.clone())
    }
}

pub struct ProjectsGroups {
    transport: Arc<Transport>,
}

impl ProjectsGroups {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ProjectsGroups { transport }
    }

    pub fn roles(&self) -> ProjectsGroupsRoles {
        ProjectsGroupsRoles::new(self.transport.clone())
    }
}

pub struct ProjectsGroupsRoles {
    transport: Arc<Transport>,
}

impl ProjectsGroupsRoles {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ProjectsGroupsRoles { transport }
    }

    /// Assigns a project role to a group within a project.
    pub async fn create(&self, project_id: &str, group_id: &str, params: ProjectsGroupsRolesCreateParams) -> Result<GroupRoleAssignment, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if group_id.is_empty() {
            return Err(Error::InvalidArgument("group_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/projects/{}/groups/{}/roles", project_id, group_id));
        if let Some(value) = &params.role_id {
            req = req.json_field("role_id", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Unassigns a project role from a group within a project.
    pub async fn delete(&self, project_id: &str, group_id: &str, role_id: &str) -> Result<DeletedRoleAssignmentResource, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if group_id.is_empty() {
            return Err(Error::InvalidArgument("group_id".to_string()));
        }
        if role_id.is_empty() {
            return Err(Error::InvalidArgument("role_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/projects/{}/groups/{}/roles/{}", project_id, group_id, role_id));
        req.send().await
    }

    /// Lists the project roles assigned to a group within a project.
    pub async fn list(&self, project_id: &str, group_id: &str, params: ProjectsGroupsRolesListParams) -> Result<Page<AssignedRoleDetails>, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if group_id.is_empty() {
            return Err(Error::InvalidArgument("group_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/projects/{}/groups/{}/roles", project_id, group_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct ProjectsGroupsRolesCreateParams {
    /// Identifier of the role to assign.
    pub role_id: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct ProjectsGroupsRolesListParams {
    /// A limit on the number of project role assignments to return.
    pub limit: Option<i64>,
    /// Cursor for pagination. Provide the value from the previous response's `next` field to continue listing project roles.
    pub after: Option<String>,
    /// Sort order for the returned project roles.
    pub order: Option<ProjectsGroupsRolesListOrder>,
}

pub struct ProjectsRoles {
    transport: Arc<Transport>,
}

impl ProjectsRoles {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ProjectsRoles { transport }
    }

    /// Creates a custom role for a project.
    pub async fn create(&self, project_id: &str, params: ProjectsRolesCreateParams) -> Result<Role, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/projects/{}/roles", project_id));
        if let Some(value) = &params.role_name {
            req = req.json_field("role_name", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.permissions {
            req = req.json_field("permissions", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.description {
            req = req.json_field("description", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Deletes a custom role from a project.
    pub async fn delete(&self, project_id: &str, role_id: &str) -> Result<RoleDeletedResource, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if role_id.is_empty() {
            return Err(Error::InvalidArgument("role_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/projects/{}/roles/{}", project_id, role_id));
        req.send().await
    }

    /// Lists the roles configured for a project.
    pub async fn list(&self, project_id: &str, params: ProjectsRolesListParams) -> Result<Page<Role>, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/projects/{}/roles", project_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }

    /// Updates an existing project role.
    pub async fn update(&self, project_id: &str, role_id: &str, params: ProjectsRolesUpdateParams) -> Result<Role, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if role_id.is_empty() {
            return Err(Error::InvalidArgument("role_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/projects/{}/roles/{}", project_id, role_id));
        if let Some(value) = &params.permissions {
            req = req.json_field("permissions", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.description {
            req = req.json_field("description", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.role_name {
            req = req.json_field("role_name", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct ProjectsRolesCreateParams {
    /// Unique name for the role.
    pub role_name: Option<String>,
    /// Permissions to grant to the role.
    pub permissions: Option<Vec<String>>,
    /// Optional description of the role.
    pub description: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct ProjectsRolesListParams {
    /// A limit on the number of roles to return. Defaults to 1000.
    pub limit: Option<i64>,
    /// Cursor for pagination. Provide the value from the previous response's `next` field to continue listing roles.
    pub after: Option<String>,
    /// Sort order for the returned roles.
    pub order: Option<ProjectsRolesListOrder>,
}

#[derive(Debug, Clone, Default)]
pub struct ProjectsRolesUpdateParams {
    /// Updated set of permissions for the role.
    pub permissions: Option<Vec<String>>,
    /// New description for the role.
    pub description: Option<String>,
    /// New name for the role.
    pub role_name: Option<String>,
}

pub struct ProjectsUsers {
    transport: Arc<Transport>,
}

impl ProjectsUsers {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ProjectsUsers { transport }
    }

    pub fn roles(&self) -> ProjectsUsersRoles {
        ProjectsUsersRoles::new(self.transport.clone())
    }
}

pub struct ProjectsUsersRoles {
    transport: Arc<Transport>,
}

impl ProjectsUsersRoles {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ProjectsUsersRoles { transport }
    }

    /// Assigns a project role to a user within a project.
    pub async fn create(&self, project_id: &str, user_id: &str, params: ProjectsUsersRolesCreateParams) -> Result<UserRoleAssignment, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/projects/{}/users/{}/roles", project_id, user_id));
        if let Some(value) = &params.role_id {
            req = req.json_field("role_id", serde_json::to_value(value)?);
        }
        req.send().await
    }

    /// Unassigns a project role from a user within a project.
    pub async fn delete(&self, project_id: &str, user_id: &str, role_id: &str) -> Result<DeletedRoleAssignmentResource, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }
        if role_id.is_empty() {
            return Err(Error::InvalidArgument("role_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/projects/{}/users/{}/roles/{}", project_id, user_id, role_id));
        req.send().await
    }

    /// Lists the project roles assigned to a user within a project.
    pub async fn list(&self, project_id: &str, user_id: &str, params: ProjectsUsersRolesListParams) -> Result<Page<AssignedRoleDetails>, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::GET, format!("/projects/{}/users/{}/roles", project_id, user_id));
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct ProjectsUsersRolesCreateParams {
    /// Identifier of the role to assign.
    pub role_id: Option<String>,
}

#[derive(Debug, Clone, Default)]
pub struct ProjectsUsersRolesListParams {
    /// A limit on the number of project role assignments to return.
    pub limit: Option<i64>,
    /// Cursor for pagination. Provide the value from the previous response's `next` field to continue listing project roles.
    pub after: Option<String>,
    /// Sort order for the returned project roles.
    pub order: Option<ProjectsUsersRolesListOrder>,
}

// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Realtime {
    transport: Arc<Transport>,
}

impl Realtime {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Realtime { transport }
    }

    pub fn client_secrets(&self) -> RealtimeClientSecrets {
        RealtimeClientSecrets::new(self.transport.clone())
    }
}

pub struct RealtimeClientSecrets {
    transport: Arc<Transport>,
}

impl RealtimeClientSecrets {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        RealtimeClientSecrets { transport }
    }

    /// Create a Realtime client secret with an associated session configuration.
    ///
    /// Client secrets are short-lived tokens that can be passed to a client app,
    /// such as a web frontend or mobile client, which grants access to the Realtime API without
    /// leaking your main API key. You can configure a custom TTL for each client secret.
    ///
    /// You can also attach session configuration options to the client secret, which will be
    /// applied to any sessions created using that client secret, but these can also be overridden
    /// by the client connection.
    ///
    /// [Learn more about authentication with client secrets over WebRTC](/docs/guides/realtime-webrtc).
    ///
    /// Returns the created client secret and the effective session object. The client secret is a string that looks like `ek_1234`.
    pub async fn create(&self, params: RealtimeClientSecretsCreateParams) -> Result<RealtimeCreateClientSecretResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/realtime/client_secrets");
        if let Some(value) = &params.expires_after {
            req = req.json_field("expires_after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.session {
            req = req.json_field("session", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct RealtimeClientSecretsCreateParams {
    /// Configuration for the client secret expiration. Expiration refers to the time after which
    /// a client secret will no longer be valid for creating sessions. The session itself may
    /// continue after that time once started. A secret can be used to create multiple sessions
    /// until it expires.
    pub expires_after: Option<RealtimeCreateClientSecretRequestExpiresAfter>,
    /// Session configuration to use for the client secret. Choose either a realtime
    /// session or a transcription session.
    pub session: Option<RealtimeCreateClientSecretRequestSession>,
}

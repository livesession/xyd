// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Images {
    transport: Arc<Transport>,
}

impl Images {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Images { transport }
    }

    pub fn edits(&self) -> ImagesEdits {
        ImagesEdits::new(self.transport.clone())
    }
}

pub struct ImagesEdits {
    transport: Arc<Transport>,
}

impl ImagesEdits {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ImagesEdits { transport }
    }

    /// You can call this endpoint with either:
    ///
    /// - `multipart/form-data`: use binary uploads via `image` (and optional `mask`).
    /// - `application/json`: use `images` (and optional `mask`) as references with either `image_url` or `file_id`.
    ///
    /// Note that JSON requests use `images` (array) instead of the multipart `image` field.
    pub async fn create(&self, params: ImagesEditsCreateParams) -> Result<ImagesResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/images/edits");
        if let Some(value) = &params.model {
            req = req.json_field("model", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.images {
            req = req.json_field("images", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.mask {
            req = req.json_field("mask", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.prompt {
            req = req.json_field("prompt", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.n {
            req = req.json_field("n", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.quality {
            req = req.json_field("quality", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.input_fidelity {
            req = req.json_field("input_fidelity", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.size {
            req = req.json_field("size", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.user {
            req = req.json_field("user", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.output_format {
            req = req.json_field("output_format", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.output_compression {
            req = req.json_field("output_compression", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.moderation {
            req = req.json_field("moderation", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.background {
            req = req.json_field("background", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.stream {
            req = req.json_field("stream", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.partial_images {
            req = req.json_field("partial_images", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct ImagesEditsCreateParams {
    /// The model to use for image editing.
    pub model: Option<EditImageBodyJsonParamModel>,
    /// Input image references to edit.
    /// For GPT image models, you can provide up to 16 images.
    pub images: Option<Vec<ImageRefParam>>,
    pub mask: Option<ImageRefParam>,
    /// A text description of the desired image edit.
    pub prompt: Option<String>,
    /// The number of edited images to generate.
    pub n: Option<i64>,
    /// Output quality for GPT image models.
    pub quality: Option<EditImageBodyJsonParamQuality>,
    /// Controls fidelity to the original input image(s).
    pub input_fidelity: Option<EditImageBodyJsonParamInputFidelity>,
    /// Requested output image size.
    pub size: Option<EditImageBodyJsonParamSize>,
    /// A unique identifier representing your end-user, which can help OpenAI
    /// monitor and detect abuse.
    pub user: Option<String>,
    /// Output image format. Supported for GPT image models.
    pub output_format: Option<EditImageBodyJsonParamOutputFormat>,
    /// Compression level for `jpeg` or `webp` output.
    pub output_compression: Option<i64>,
    /// Moderation level for GPT image models.
    pub moderation: Option<EditImageBodyJsonParamModeration>,
    /// Background behavior for generated image output.
    pub background: Option<EditImageBodyJsonParamBackground>,
    /// Stream partial image results as events.
    pub stream: Option<bool>,
    pub partial_images: Option<PartialImages>,
}

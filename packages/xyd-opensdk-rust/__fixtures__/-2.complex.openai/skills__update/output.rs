// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Skills {
    transport: Arc<Transport>,
}

impl Skills {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Skills { transport }
    }

    /// Update the default version pointer for a skill.
    pub async fn update(&self, skill_id: &str, params: SkillsUpdateParams) -> Result<SkillResource, Error> {
        if skill_id.is_empty() {
            return Err(Error::InvalidArgument("skill_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/skills/{}", skill_id));
        if let Some(value) = &params.default_version {
            req = req.json_field("default_version", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct SkillsUpdateParams {
    /// The skill version number to set as default.
    pub default_version: Option<String>,
}

// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Batches {
    transport: Arc<Transport>,
}

impl Batches {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Batches { transport }
    }

    /// Creates and executes a batch from an uploaded file of requests
    pub async fn create(&self, params: BatchesCreateParams) -> Result<Batch, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/batches");
        if let Some(value) = &params.input_file_id {
            req = req.json_field("input_file_id", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.endpoint {
            req = req.json_field("endpoint", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.completion_window {
            req = req.json_field("completion_window", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.output_expires_after {
            req = req.json_field("output_expires_after", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct BatchesCreateParams {
    /// The ID of an uploaded file that contains requests for the new batch.
    ///
    /// See [upload file](/docs/api-reference/files/create) for how to upload a file.
    ///
    /// Your input file must be formatted as a [JSONL file](/docs/api-reference/batch/request-input), and must be uploaded with the purpose `batch`. The file can contain up to 50,000 requests, and can be up to 200 MB in size.
    pub input_file_id: Option<String>,
    /// The endpoint to be used for all requests in the batch. Currently `/v1/responses`, `/v1/chat/completions`, `/v1/embeddings`, `/v1/completions`, `/v1/moderations`, `/v1/images/generations`, `/v1/images/edits`, and `/v1/videos` are supported. Note that `/v1/embeddings` batches are also restricted to a maximum of 50,000 embedding inputs across all requests in the batch.
    pub endpoint: Option<BatchesCreateRequestEndpoint>,
    /// The time frame within which the batch should be processed. Currently only `24h` is supported.
    pub completion_window: Option<String>,
    pub metadata: Option<Metadata>,
    pub output_expires_after: Option<BatchFileExpirationAfter>,
}

// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Chat {
    transport: Arc<Transport>,
}

impl Chat {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Chat { transport }
    }

    pub fn completions(&self) -> ChatCompletions {
        ChatCompletions::new(self.transport.clone())
    }
}

pub struct ChatCompletions {
    transport: Arc<Transport>,
}

impl ChatCompletions {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ChatCompletions { transport }
    }

    /// Delete a stored chat completion. Only Chat Completions that have been
    /// created with the `store` parameter set to `true` can be deleted.
    pub async fn delete(&self, completion_id: &str) -> Result<ChatCompletionDeleted, Error> {
        if completion_id.is_empty() {
            return Err(Error::InvalidArgument("completion_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/chat/completions/{}", completion_id));
        req.send().await
    }
}

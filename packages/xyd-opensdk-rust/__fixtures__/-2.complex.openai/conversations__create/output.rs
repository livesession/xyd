// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Conversations {
    transport: Arc<Transport>,
}

impl Conversations {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Conversations { transport }
    }

    /// Create a conversation.
    pub async fn create(&self, params: ConversationsCreateParams) -> Result<ConversationResource, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/conversations");
        if let Some(value) = &params.metadata {
            req = req.json_field("metadata", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.items {
            req = req.json_field("items", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct ConversationsCreateParams {
    pub metadata: Option<Metadata>,
    pub items: Option<Vec<InputItem>>,
}

// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct FineTuning {
    transport: Arc<Transport>,
}

impl FineTuning {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        FineTuning { transport }
    }

    pub fn checkpoints(&self) -> FineTuningCheckpoints {
        FineTuningCheckpoints::new(self.transport.clone())
    }
}

pub struct FineTuningCheckpoints {
    transport: Arc<Transport>,
}

impl FineTuningCheckpoints {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        FineTuningCheckpoints { transport }
    }

    pub fn permissions(&self) -> FineTuningCheckpointsPermissions {
        FineTuningCheckpointsPermissions::new(self.transport.clone())
    }
}

pub struct FineTuningCheckpointsPermissions {
    transport: Arc<Transport>,
}

impl FineTuningCheckpointsPermissions {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        FineTuningCheckpointsPermissions { transport }
    }

    /// **NOTE:** This endpoint requires an [admin API key](../admin-api-keys).
    ///
    /// Organization owners can use this endpoint to delete a permission for a fine-tuned model checkpoint.
    pub async fn delete(&self, fine_tuned_model_checkpoint: &str, permission_id: &str) -> Result<DeleteFineTuningCheckpointPermissionResponse, Error> {
        if fine_tuned_model_checkpoint.is_empty() {
            return Err(Error::InvalidArgument("fine_tuned_model_checkpoint".to_string()));
        }
        if permission_id.is_empty() {
            return Err(Error::InvalidArgument("permission_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/fine_tuning/checkpoints/{}/permissions/{}", fine_tuned_model_checkpoint, permission_id));
        req.send().await
    }
}

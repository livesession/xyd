// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Projects {
    transport: Arc<Transport>,
}

impl Projects {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Projects { transport }
    }

    pub fn users(&self) -> ProjectsUsers {
        ProjectsUsers::new(self.transport.clone())
    }
}

pub struct ProjectsUsers {
    transport: Arc<Transport>,
}

impl ProjectsUsers {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ProjectsUsers { transport }
    }

    pub fn roles(&self) -> ProjectsUsersRoles {
        ProjectsUsersRoles::new(self.transport.clone())
    }
}

pub struct ProjectsUsersRoles {
    transport: Arc<Transport>,
}

impl ProjectsUsersRoles {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        ProjectsUsersRoles { transport }
    }

    /// Assigns a project role to a user within a project.
    pub async fn create(&self, project_id: &str, user_id: &str, params: ProjectsUsersRolesCreateParams) -> Result<UserRoleAssignment, Error> {
        if project_id.is_empty() {
            return Err(Error::InvalidArgument("project_id".to_string()));
        }
        if user_id.is_empty() {
            return Err(Error::InvalidArgument("user_id".to_string()));
        }

        let mut req = self.transport.request(reqwest::Method::POST, format!("/projects/{}/users/{}/roles", project_id, user_id));
        if let Some(value) = &params.role_id {
            req = req.json_field("role_id", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct ProjectsUsersRolesCreateParams {
    /// Identifier of the role to assign.
    pub role_id: Option<String>,
}

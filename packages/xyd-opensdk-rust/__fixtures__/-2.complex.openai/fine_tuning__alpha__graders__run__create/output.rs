// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct FineTuning {
    transport: Arc<Transport>,
}

impl FineTuning {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        FineTuning { transport }
    }

    pub fn alpha(&self) -> FineTuningAlpha {
        FineTuningAlpha::new(self.transport.clone())
    }
}

pub struct FineTuningAlpha {
    transport: Arc<Transport>,
}

impl FineTuningAlpha {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        FineTuningAlpha { transport }
    }

    pub fn graders(&self) -> FineTuningAlphaGraders {
        FineTuningAlphaGraders::new(self.transport.clone())
    }
}

pub struct FineTuningAlphaGraders {
    transport: Arc<Transport>,
}

impl FineTuningAlphaGraders {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        FineTuningAlphaGraders { transport }
    }

    pub fn run(&self) -> FineTuningAlphaGradersRun {
        FineTuningAlphaGradersRun::new(self.transport.clone())
    }
}

pub struct FineTuningAlphaGradersRun {
    transport: Arc<Transport>,
}

impl FineTuningAlphaGradersRun {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        FineTuningAlphaGradersRun { transport }
    }

    /// Run a grader.
    pub async fn create(&self, params: FineTuningAlphaGradersRunCreateParams) -> Result<RunGraderResponse, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/fine_tuning/alpha/graders/run");
        if let Some(value) = &params.grader {
            req = req.json_field("grader", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.item {
            req = req.json_field("item", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.model_sample {
            req = req.json_field("model_sample", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct FineTuningAlphaGradersRunCreateParams {
    /// The grader used for the fine-tuning job.
    pub grader: Option<RunGraderRequestGrader>,
    /// The dataset item provided to the grader. This will be used to populate
    /// the `item` namespace. See [the guide](/docs/guides/graders) for more details.
    pub item: Option<std::collections::HashMap<String, serde_json::Value>>,
    /// The model sample to be evaluated. This value will be used to populate
    /// the `sample` namespace. See [the guide](/docs/guides/graders) for more details.
    /// The `output_json` variable will be populated if the model sample is a
    /// valid JSON string.
    pub model_sample: Option<String>,
}

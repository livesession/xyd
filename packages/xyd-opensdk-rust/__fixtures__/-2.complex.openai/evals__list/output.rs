// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Evals {
    transport: Arc<Transport>,
}

impl Evals {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Evals { transport }
    }

    /// List evaluations for a project.
    pub async fn list(&self, params: EvalsListParams) -> Result<Page<Eval>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/evals");
        if let Some(value) = &params.after {
            req = req.query("after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order {
            req = req.query("order", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.order_by {
            req = req.query("order_by", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", Some("has_more"))
    }
}

#[derive(Debug, Clone, Default)]
pub struct EvalsListParams {
    /// Identifier for the last eval from the previous pagination request.
    pub after: Option<String>,
    /// Number of evals to retrieve.
    pub limit: Option<i64>,
    /// Sort order for evals by timestamp. Use `asc` for ascending order or `desc` for descending order.
    pub order: Option<EvalsListOrder>,
    /// Evals can be ordered by creation time or last updated time. Use
    /// `created_at` for creation time or `updated_at` for last updated time.
    pub order_by: Option<EvalsListOrderBy>,
}

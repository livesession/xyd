// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Beta {
    transport: Arc<Transport>,
}

impl Beta {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Beta { transport }
    }

    pub fn chat_kit(&self) -> BetaChatKit {
        BetaChatKit::new(self.transport.clone())
    }
}

pub struct BetaChatKit {
    transport: Arc<Transport>,
}

impl BetaChatKit {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaChatKit { transport }
    }

    pub fn sessions(&self) -> BetaChatKitSessions {
        BetaChatKitSessions::new(self.transport.clone())
    }
}

pub struct BetaChatKitSessions {
    transport: Arc<Transport>,
}

impl BetaChatKitSessions {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        BetaChatKitSessions { transport }
    }

    /// Create a ChatKit session.
    pub async fn create(&self, params: BetaChatKitSessionsCreateParams) -> Result<ChatSessionResource, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/chatkit/sessions");
        if let Some(value) = &params.workflow {
            req = req.json_field("workflow", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.user {
            req = req.json_field("user", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.expires_after {
            req = req.json_field("expires_after", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.rate_limits {
            req = req.json_field("rate_limits", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.chatkit_configuration {
            req = req.json_field("chatkit_configuration", serde_json::to_value(value)?);
        }
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct BetaChatKitSessionsCreateParams {
    pub workflow: Option<WorkflowParam>,
    /// A free-form string that identifies your end user; ensures this Session can access other objects that have the same `user` scope.
    pub user: Option<String>,
    pub expires_after: Option<ExpiresAfterParam>,
    pub rate_limits: Option<RateLimitsParam>,
    pub chatkit_configuration: Option<ChatkitConfigurationParam>,
}

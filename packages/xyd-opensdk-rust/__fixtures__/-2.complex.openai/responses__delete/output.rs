// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Responses {
    transport: Arc<Transport>,
}

impl Responses {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Responses { transport }
    }

    /// Deletes a model response with the given ID.
    pub async fn delete(&self, response_id: &str) -> Result<(), Error> {
        if response_id.is_empty() {
            return Err(Error::InvalidArgument("response_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::DELETE, format!("/responses/{}", response_id));
        req.send_empty().await
    }
}

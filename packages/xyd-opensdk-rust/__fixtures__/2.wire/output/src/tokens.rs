// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Tokens {
    transport: Arc<Transport>,
}

impl Tokens {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Tokens { transport }
    }

    /// Create a token (application/x-www-form-urlencoded body).
    pub async fn create(&self, params: TokensCreateParams) -> Result<Token, Error> {
        let mut req = self.transport.request(reqwest::Method::POST, "/tokens");
        if let Some(value) = &params.grant_type {
            req = req.form_json("grant_type", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.scope {
            req = req.form_json("scope", serde_json::to_value(value)?);
        }
        req = req.idempotency();
        req.send().await
    }
}

#[derive(Debug, Clone, Default)]
pub struct TokensCreateParams {
    pub grant_type: Option<String>,
    pub scope: Option<String>,
}

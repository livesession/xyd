// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Shapes {
    transport: Arc<Transport>,
}

impl Shapes {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Shapes { transport }
    }

    /// Retrieve a shape (discriminated-union response)
    pub async fn retrieve(&self, shape_id: &str) -> Result<Shape, Error> {
        if shape_id.is_empty() {
            return Err(Error::InvalidArgument("shape_id".to_string()));
        }

        let req = self.transport.request(reqwest::Method::GET, format!("/shapes/{}", shape_id));
        req.send().await
    }
}

// Code generated by opensdk. DO NOT EDIT.

use std::sync::Arc;

use crate::error::Error;
use crate::models::*;
use crate::transport::{Page, Transport};

pub struct Logs {
    transport: Arc<Transport>,
}

impl Logs {
    pub(crate) fn new(transport: Arc<Transport>) -> Self {
        Logs { transport }
    }

    /// List logs (offset-paginated)
    pub async fn list(&self, params: LogsListParams) -> Result<Page<Log>, Error> {
        let mut req = self.transport.request(reqwest::Method::GET, "/logs");
        if let Some(value) = &params.offset {
            req = req.query("offset", serde_json::to_value(value)?);
        }
        if let Some(value) = &params.limit {
            req = req.query("limit", serde_json::to_value(value)?);
        }
        let value = req.send_value().await?;
        Page::from_value(value, "data", None)
    }
}

#[derive(Debug, Clone, Default)]
pub struct LogsListParams {
    pub offset: Option<i64>,
    pub limit: Option<i64>,
}

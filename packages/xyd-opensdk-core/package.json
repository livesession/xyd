{
  "name": "@xyd-js/opensdk-core",
  "version": "0.1.0-build.0",
  "description": "OpenSDK core model and helpers (the language-agnostic SDK IR shared by the openapi2opensdk converter and the opensdk-* code generators)",
  "main": "./dist/index.js",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "pnpm clean",
    "build": "tsup",
    "test": "vitest",
    "ci:test": "vitest run",
    "generate:types": "json2ts -i opensdk-spec.json -o src/types.ts",
    "format": "bunx biome check --write src"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.9.0",
    "json-schema-to-typescript": "^14.0.1",
    "rimraf": "^3.0.2",
    "tsup": "^8.3.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  }
}

/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * The OpenSDK description: a language-agnostic intermediate representation (IR) of an API SDK — a nominal type system (named structs/enums/unions) plus a resource tree of typed methods carrying their HTTP request binding. Emitted by @xyd-js/openapi2opensdk and consumed by the opensdk-* code generators (Go first).
 */
export interface OpensdkSpecJson {
  /**
   * The OpenSDK version number
   */
  opensdk: string;
  info: SdkInfo;
  /**
   * Available API base URLs (from the OpenAPI servers list)
   */
  servers?: string[];
  /**
   * Default security requirements for the SDK
   */
  security?: SdkSecurity[];
  /**
   * The symbol table: all named types (structs, enums, unions, aliases) referenced by resources/methods, resolved from OpenAPI component schemas and hoisted inline types
   */
  types?: NamedType[];
  /**
   * The client resource tree (nested services, each with typed methods)
   */
  resources?: Resource[];
  sdk?: SdkBehavior;
  /**
   * Custom metadata
   */
  metadata?: Metadata[];
  [k: string]: unknown;
}
/**
 * Information about the SDK / API
 */
export interface SdkInfo {
  /**
   * The API title
   */
  title: string;
  /**
   * A short summary of the API
   */
  summary?: string;
  /**
   * A description of the API
   */
  description?: string;
  contact?: Contact;
  license?: License;
  /**
   * The API version
   */
  version: string;
  [k: string]: unknown;
}
/**
 * The contact information
 */
export interface Contact {
  /**
   * The identifying name of the contact person/organization
   */
  name?: string;
  /**
   * The URI for the contact information
   */
  url?: string;
  /**
   * The email address of the contact person/organization
   */
  email?: string;
  [k: string]: unknown;
}
/**
 * The API license
 */
export interface License {
  /**
   * The license name
   */
  name?: string;
  /**
   * The SPDX license identifier
   */
  identifier?: string;
  /**
   * The URI for the license
   */
  url?: string;
  [k: string]: unknown;
}
/**
 * A single security scheme mapped from the OpenAPI securitySchemes.
 */
export interface SdkSecurity {
  /**
   * The source securityScheme key (e.g. AdminApiKeyAuth) — distinguishes per-operation overrides whose scheme shape equals the default
   */
  schemeName?: string;
  /**
   * Normalized scheme a generator branches on: bearer | apiKey-header | apiKey-query | apiKey-cookie | basic | other
   */
  kind?: string;
  /**
   * The OpenAPI security scheme type (e.g. http, apiKey, oauth2)
   */
  type: string;
  /**
   * The HTTP auth scheme (e.g. bearer, basic) when type is http
   */
  scheme?: string;
  /**
   * Location for an apiKey scheme (header, query or cookie)
   */
  in?: string;
  /**
   * Header/query/cookie name for an apiKey scheme
   */
  name?: string;
  /**
   * The bearer token format hint (e.g. JWT)
   */
  bearerFormat?: string;
  /**
   * Environment variable a generated SDK reads the credential from (e.g. OPENAI_API_KEY)
   */
  envVar?: string;
  [k: string]: unknown;
}
/**
 * A named type in the symbol table. `kind` discriminates which fields apply.
 */
export interface NamedType {
  /**
   * The type name (source component key or a deterministic synthetic name for a hoisted inline type)
   */
  name: string;
  /**
   * The type kind a generator branches on: struct | enum | union | alias
   */
  kind: string;
  /**
   * The type description
   */
  description?: string;
  /**
   * Whether the type is deprecated
   */
  deprecated?: boolean;
  /**
   * kind=struct: the struct fields
   */
  fields?: Field[];
  /**
   * kind=enum: the base scalar (string | integer)
   */
  base?: string;
  /**
   * kind=enum: the enumerated values
   */
  values?: EnumValue[];
  /**
   * kind=union: oneOf (exactly one) | anyOf (one or more)
   */
  semantics?: string;
  /**
   * kind=union: the union member types
   */
  variants?: TypeRef3[];
  discriminator?: Discriminator;
  of?: TypeRef4;
  /**
   * Custom metadata
   */
  metadata?: Metadata[];
  [k: string]: unknown;
}
/**
 * A struct field.
 */
export interface Field {
  /**
   * The field's wire name (a generator derives the language-specific field name)
   */
  name: string;
  type: TypeRef;
  /**
   * Whether the field is required
   */
  required?: boolean;
  /**
   * Whether the field may be null
   */
  nullable?: boolean;
  /**
   * Whether the field is response-only
   */
  readOnly?: boolean;
  /**
   * Whether the field is request-only
   */
  writeOnly?: boolean;
  /**
   * Whether the field is deprecated
   */
  deprecated?: boolean;
  /**
   * The field's default value, if any
   */
  default?: unknown;
  /**
   * The field description
   */
  description?: string;
  [k: string]: unknown;
}
/**
 * The field type
 */
export interface TypeRef {
  /**
   * scalar | ref | array | map | any
   */
  kind: string;
  /**
   * kind=scalar: string | integer | number | boolean
   */
  scalar?: string;
  /**
   * kind=scalar: the OpenAPI format hint (e.g. date-time, binary, int64)
   */
  format?: string;
  /**
   * kind=ref: the referenced NamedType name (symbol-table lookup)
   */
  name?: string;
  items?: TypeRef1;
  values?: TypeRef2;
  /**
   * kind=scalar: a fixed literal value (JSON Schema const / single-value enum); emitters may auto-fill it
   */
  const?: unknown;
  /**
   * Whether the referenced value may be null
   */
  nullable?: boolean;
  [k: string]: unknown;
}
/**
 * kind=array: the element type
 */
export interface TypeRef1 {
  /**
   * scalar | ref | array | map | any
   */
  kind: string;
  /**
   * kind=scalar: string | integer | number | boolean
   */
  scalar?: string;
  /**
   * kind=scalar: the OpenAPI format hint (e.g. date-time, binary, int64)
   */
  format?: string;
  /**
   * kind=ref: the referenced NamedType name (symbol-table lookup)
   */
  name?: string;
  items?: TypeRef1;
  values?: TypeRef2;
  /**
   * kind=scalar: a fixed literal value (JSON Schema const / single-value enum); emitters may auto-fill it
   */
  const?: unknown;
  /**
   * Whether the referenced value may be null
   */
  nullable?: boolean;
  [k: string]: unknown;
}
/**
 * kind=map: the additionalProperties value type
 */
export interface TypeRef2 {
  /**
   * scalar | ref | array | map | any
   */
  kind: string;
  /**
   * kind=scalar: string | integer | number | boolean
   */
  scalar?: string;
  /**
   * kind=scalar: the OpenAPI format hint (e.g. date-time, binary, int64)
   */
  format?: string;
  /**
   * kind=ref: the referenced NamedType name (symbol-table lookup)
   */
  name?: string;
  items?: TypeRef1;
  values?: TypeRef2;
  /**
   * kind=scalar: a fixed literal value (JSON Schema const / single-value enum); emitters may auto-fill it
   */
  const?: unknown;
  /**
   * Whether the referenced value may be null
   */
  nullable?: boolean;
  [k: string]: unknown;
}
/**
 * One value of a named enum.
 */
export interface EnumValue {
  /**
   * The wire value (string or integer)
   */
  value: unknown;
  /**
   * An explicit constant name, if the wire value isn't a clean identifier
   */
  name?: string;
  /**
   * The value description
   */
  description?: string;
  [k: string]: unknown;
}
/**
 * A structural reference to a type. `kind` discriminates which fields apply.
 */
export interface TypeRef3 {
  /**
   * scalar | ref | array | map | any
   */
  kind: string;
  /**
   * kind=scalar: string | integer | number | boolean
   */
  scalar?: string;
  /**
   * kind=scalar: the OpenAPI format hint (e.g. date-time, binary, int64)
   */
  format?: string;
  /**
   * kind=ref: the referenced NamedType name (symbol-table lookup)
   */
  name?: string;
  items?: TypeRef1;
  values?: TypeRef2;
  /**
   * kind=scalar: a fixed literal value (JSON Schema const / single-value enum); emitters may auto-fill it
   */
  const?: unknown;
  /**
   * Whether the referenced value may be null
   */
  nullable?: boolean;
  [k: string]: unknown;
}
/**
 * kind=union: the OpenAPI discriminator, when present
 */
export interface Discriminator {
  /**
   * The property whose value selects the variant
   */
  propertyName: string;
  /**
   * Map of discriminator value -> variant type name
   */
  mapping?: Record<string, string>;
  [k: string]: unknown;
}
/**
 * A structural reference to a type. `kind` discriminates which fields apply.
 */
export interface TypeRef4 {
  /**
   * scalar | ref | array | map | any
   */
  kind: string;
  /**
   * kind=scalar: string | integer | number | boolean
   */
  scalar?: string;
  /**
   * kind=scalar: the OpenAPI format hint (e.g. date-time, binary, int64)
   */
  format?: string;
  /**
   * kind=ref: the referenced NamedType name (symbol-table lookup)
   */
  name?: string;
  items?: TypeRef1;
  values?: TypeRef2;
  /**
   * kind=scalar: a fixed literal value (JSON Schema const / single-value enum); emitters may auto-fill it
   */
  const?: unknown;
  /**
   * Whether the referenced value may be null
   */
  nullable?: boolean;
  [k: string]: unknown;
}
export interface Metadata {
  name: string;
  value?: unknown;
  [k: string]: unknown;
}
/**
 * A resource / service node in the client tree.
 */
export interface Resource {
  /**
   * The resource name (a generator derives the service/field name)
   */
  name: string;
  /**
   * The resource description
   */
  description?: string;
  /**
   * Nested sub-resources
   */
  resources?: Resource[];
  /**
   * The resource's methods
   */
  methods?: Method[];
  [k: string]: unknown;
}
/**
 * A typed SDK method plus the HTTP request binding needed to call the API.
 */
export interface Method {
  /**
   * The method verb: list | create | retrieve | update | delete | <custom>
   */
  action: string;
  /**
   * The source OpenAPI operationId, when present
   */
  operationId?: string;
  /**
   * HTTP method (lowercase)
   */
  httpMethod: string;
  /**
   * HTTP path template (e.g. /chat/completions/{completion_id})
   */
  path: string;
  /**
   * The method description
   */
  description?: string;
  /**
   * Whether the method is deprecated
   */
  deprecated?: boolean;
  /**
   * The spec declared an idempotency-key header on this operation; the converter strips it from params and the runtime injects a generated key per sdk.idempotency
   */
  injectIdempotencyKey?: boolean;
  /**
   * Path parameters, in path order (positional method arguments)
   */
  pathParams?: Param[];
  /**
   * Query parameters (fields of the params struct)
   */
  queryParams?: Param[];
  /**
   * Header parameters (excluding well-known auth headers)
   */
  headerParams?: Param[];
  requestBody?: RequestBody;
  /**
   * The declared responses
   */
  responses?: Response[];
  primaryResponse?: TypeRef8;
  pagination?: Pagination;
  /**
   * Per-operation security requirements (overrides root)
   */
  security?: SdkSecurity[];
  [k: string]: unknown;
}
/**
 * A path/query/header parameter with its wire name (request binding) and type.
 */
export interface Param {
  /**
   * The identifier-safe parameter name (generators derive SDK identifiers from it)
   */
  name: string;
  /**
   * The raw HTTP wire name when it differs from `name` (e.g. `ids[]`); emitters must send this on the wire
   */
  wireName?: string;
  type: TypeRef5;
  /**
   * Whether the parameter is required
   */
  required?: boolean;
  /**
   * The parameter description
   */
  description?: string;
  /**
   * The parameter's default value, if any
   */
  default?: unknown;
  /**
   * An example value, if any
   */
  example?: unknown;
  /**
   * Whether the parameter is deprecated
   */
  deprecated?: boolean;
  /**
   * OpenAPI explode flag for array/object serialization
   */
  explode?: boolean;
  /**
   * OpenAPI serialization style
   */
  style?: string;
  [k: string]: unknown;
}
/**
 * A structural reference to a type. `kind` discriminates which fields apply.
 */
export interface TypeRef5 {
  /**
   * scalar | ref | array | map | any
   */
  kind: string;
  /**
   * kind=scalar: string | integer | number | boolean
   */
  scalar?: string;
  /**
   * kind=scalar: the OpenAPI format hint (e.g. date-time, binary, int64)
   */
  format?: string;
  /**
   * kind=ref: the referenced NamedType name (symbol-table lookup)
   */
  name?: string;
  items?: TypeRef1;
  values?: TypeRef2;
  /**
   * kind=scalar: a fixed literal value (JSON Schema const / single-value enum); emitters may auto-fill it
   */
  const?: unknown;
  /**
   * Whether the referenced value may be null
   */
  nullable?: boolean;
  [k: string]: unknown;
}
/**
 * The request body, when present
 */
export interface RequestBody {
  /**
   * Body content type (e.g. application/json, multipart/form-data)
   */
  contentType?: string;
  type: TypeRef6;
  /**
   * Whether the body is required
   */
  required?: boolean;
  /**
   * How the body is encoded: json | multipart | form
   */
  encoding?: string;
  /**
   * The body description
   */
  description?: string;
  [k: string]: unknown;
}
/**
 * A structural reference to a type. `kind` discriminates which fields apply.
 */
export interface TypeRef6 {
  /**
   * scalar | ref | array | map | any
   */
  kind: string;
  /**
   * kind=scalar: string | integer | number | boolean
   */
  scalar?: string;
  /**
   * kind=scalar: the OpenAPI format hint (e.g. date-time, binary, int64)
   */
  format?: string;
  /**
   * kind=ref: the referenced NamedType name (symbol-table lookup)
   */
  name?: string;
  items?: TypeRef1;
  values?: TypeRef2;
  /**
   * kind=scalar: a fixed literal value (JSON Schema const / single-value enum); emitters may auto-fill it
   */
  const?: unknown;
  /**
   * Whether the referenced value may be null
   */
  nullable?: boolean;
  [k: string]: unknown;
}
/**
 * A declared HTTP response.
 */
export interface Response {
  /**
   * HTTP status code (e.g. "200") or "default"
   */
  status?: string;
  /**
   * Response content type (e.g. application/json)
   */
  contentType?: string;
  type?: TypeRef7;
  /**
   * The response description
   */
  description?: string;
  [k: string]: unknown;
}
/**
 * A structural reference to a type. `kind` discriminates which fields apply.
 */
export interface TypeRef7 {
  /**
   * scalar | ref | array | map | any
   */
  kind: string;
  /**
   * kind=scalar: string | integer | number | boolean
   */
  scalar?: string;
  /**
   * kind=scalar: the OpenAPI format hint (e.g. date-time, binary, int64)
   */
  format?: string;
  /**
   * kind=ref: the referenced NamedType name (symbol-table lookup)
   */
  name?: string;
  items?: TypeRef1;
  values?: TypeRef2;
  /**
   * kind=scalar: a fixed literal value (JSON Schema const / single-value enum); emitters may auto-fill it
   */
  const?: unknown;
  /**
   * Whether the referenced value may be null
   */
  nullable?: boolean;
  [k: string]: unknown;
}
/**
 * A structural reference to a type. `kind` discriminates which fields apply.
 */
export interface TypeRef8 {
  /**
   * scalar | ref | array | map | any
   */
  kind: string;
  /**
   * kind=scalar: string | integer | number | boolean
   */
  scalar?: string;
  /**
   * kind=scalar: the OpenAPI format hint (e.g. date-time, binary, int64)
   */
  format?: string;
  /**
   * kind=ref: the referenced NamedType name (symbol-table lookup)
   */
  name?: string;
  items?: TypeRef1;
  values?: TypeRef2;
  /**
   * kind=scalar: a fixed literal value (JSON Schema const / single-value enum); emitters may auto-fill it
   */
  const?: unknown;
  /**
   * Whether the referenced value may be null
   */
  nullable?: boolean;
  [k: string]: unknown;
}
/**
 * Pagination shape, when the method is a pageable list
 */
export interface Pagination {
  /**
   * The pagination style (e.g. cursor, page, offset)
   */
  style: string;
  /**
   * The response field holding the page items (e.g. data)
   */
  itemsField: string;
  itemType?: TypeRef9;
  /**
   * The request parameter carrying the cursor (e.g. after)
   */
  cursorParam?: string;
  /**
   * style=offset: the request parameter carrying the item offset (e.g. offset, skip)
   */
  offsetParam?: string;
  /**
   * The request parameter carrying the page size (e.g. limit, page_size, per_page)
   */
  limitParam?: string;
  /**
   * The response field indicating more pages (e.g. has_more, next)
   */
  nextField?: string;
  [k: string]: unknown;
}
/**
 * A structural reference to a type. `kind` discriminates which fields apply.
 */
export interface TypeRef9 {
  /**
   * scalar | ref | array | map | any
   */
  kind: string;
  /**
   * kind=scalar: string | integer | number | boolean
   */
  scalar?: string;
  /**
   * kind=scalar: the OpenAPI format hint (e.g. date-time, binary, int64)
   */
  format?: string;
  /**
   * kind=ref: the referenced NamedType name (symbol-table lookup)
   */
  name?: string;
  items?: TypeRef1;
  values?: TypeRef2;
  /**
   * kind=scalar: a fixed literal value (JSON Schema const / single-value enum); emitters may auto-fill it
   */
  const?: unknown;
  /**
   * Whether the referenced value may be null
   */
  nullable?: boolean;
  [k: string]: unknown;
}
/**
 * Declarative runtime behavior of the generated SDK (retry, timeout, errors, user-agent, ...). The converter always stamps defaults so emitters can rely on it; hand-written IR may omit it and use opensdk-core's sdkBehavior() accessor for defaults.
 */
export interface SdkBehavior {
  retry?: RetryPolicy;
  timeout?: TimeoutPolicy;
  errors?: ErrorPolicy;
  userAgent?: UserAgentPolicy;
  telemetry?: TelemetryPolicy;
  logging?: LoggingPolicy;
  idempotency?: IdempotencyPolicy;
  pagination?: PaginationPolicy;
  requestGuard?: RequestGuardPolicy;
  [k: string]: unknown;
}
/**
 * Automatic retry policy
 */
export interface RetryPolicy {
  /**
   * Maximum retry attempts after the first request
   */
  maxRetries?: number;
  /**
   * HTTP status codes that trigger a retry
   */
  retryableStatusCodes?: number[];
  /**
   * Whether transport/connection errors are retried
   */
  retryConnectionErrors?: boolean;
  /**
   * Whether a Retry-After response header overrides the computed backoff
   */
  honorRetryAfterHeader?: boolean;
  backoff?: BackoffStrategy;
  [k: string]: unknown;
}
/**
 * Exponential backoff parameters
 */
export interface BackoffStrategy {
  /**
   * Delay before the first retry
   */
  initialDelayMs?: number;
  /**
   * Upper bound on the computed delay
   */
  maxDelayMs?: number;
  /**
   * Exponential growth factor per attempt
   */
  multiplier?: number;
  /**
   * Random jitter fraction (0..1) applied to each delay
   */
  jitter?: number;
  [k: string]: unknown;
}
/**
 * Request timeout policy
 */
export interface TimeoutPolicy {
  /**
   * Default per-request timeout; 0 means no deadline
   */
  defaultTimeoutMs?: number;
  /**
   * Environment variable that overrides the default timeout (milliseconds)
   */
  timeoutEnvVar?: string;
  [k: string]: unknown;
}
/**
 * Status-code to error-kind mapping
 */
export interface ErrorPolicy {
  /**
   * Exact status -> error kind (e.g. "404": "NotFound")
   */
  statusCodeMap?: Record<string, string>;
  /**
   * Kind for unmapped 4xx statuses
   */
  clientErrorKind?: string;
  /**
   * Kind for 5xx statuses
   */
  serverErrorKind?: string;
  /**
   * URL template for error docs (e.g. https://api.example.com/errors/{kind})
   */
  errorDocUrlTemplate?: string;
  [k: string]: unknown;
}
/**
 * User-Agent assembly policy
 */
export interface UserAgentPolicy {
  /**
   * UA template; emitters substitute {package}, {language}, {version}
   */
  sdkIdentifierTemplate?: string;
  /**
   * Append the language runtime version (go1.22, python/3.12)
   */
  includeRuntimeVersion?: boolean;
  /**
   * Env var -> agent slug; when set at runtime the slug is appended to the UA for agent-traffic attribution
   */
  aiAgentEnvVars?: Record<string, string>;
  [k: string]: unknown;
}
/**
 * Request-ID / client-telemetry headers
 */
export interface TelemetryPolicy {
  /**
   * Response header carrying the request id, surfaced on errors
   */
  requestIdHeader?: string;
  /**
   * Request header carrying client telemetry (previous request id + latency)
   */
  headerName?: string;
  /**
   * Whether the client-telemetry loop is on by default
   */
  enabledByDefault?: boolean;
  [k: string]: unknown;
}
/**
 * Lifecycle log-event vocabulary
 */
export interface LoggingPolicy {
  /**
   * Closed lifecycle-event vocabulary every runtime must emit (request.start, request.success, request.retry, request.rate_limited, request.error, request.connection_error)
   */
  events?: string[];
  [k: string]: unknown;
}
/**
 * Idempotency-key injection policy
 */
export interface IdempotencyPolicy {
  /**
   * The idempotency header wire name
   */
  headerName?: string;
  /**
   * Auto-generate a key for retried POSTs on methods flagged injectIdempotencyKey
   */
  autoGenerateForPost?: boolean;
  [k: string]: unknown;
}
/**
 * Auto-pagination behavior
 */
export interface PaginationPolicy {
  /**
   * Politeness delay between auto-fetched pages
   */
  autoPageDelayMs?: number;
  [k: string]: unknown;
}
/**
 * Misplaced request-option detection
 */
export interface RequestGuardPolicy {
  /**
   * Request-option keys that must never appear inside request params (fail fast if seen)
   */
  optionKeys?: string[];
  [k: string]: unknown;
}

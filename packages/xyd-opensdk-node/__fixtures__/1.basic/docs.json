{
  "chain": [
    "pets"
  ],
  "action": "list",
  "usage": "import Petstore from \"petstore\";\n\nconst client = new Petstore({\n  apiKey: process.env[\"PETSTORE_API_KEY\"],\n});\n\nconst result = await client.pets.list({ after: \"after\", limit: 1 });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.pets.list(query?): Promise<CursorPage<Pet>>",
    "request": {
      "typeName": "PetListParams",
      "argName": "query",
      "fields": [
        {
          "name": "after",
          "langType": "string",
          "required": false
        },
        {
          "name": "limit",
          "langType": "number",
          "required": false
        }
      ]
    },
    "response": {
      "typeName": "Pet",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true
        },
        {
          "name": "name",
          "langType": "string",
          "required": true
        },
        {
          "name": "status",
          "langType": "PetStatus",
          "required": false,
          "refTypeName": "PetStatus"
        },
        {
          "name": "tags",
          "langType": "string[]",
          "required": false
        }
      ]
    }
  }
}

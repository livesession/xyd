// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { RequestOptions } from '../core/request';
import { CursorPage } from '../core/pagination';
import type { Pet, PetStatus } from '../models';

export class Pets extends APIResource {
  /** List pets */
  list(query?: PetListParams, options?: RequestOptions): Promise<CursorPage<Pet>> {
    return this._client
      .request<unknown>({ method: "GET", path: "/pets", query }, options)
      .then((raw) => CursorPage.fromResponse<Pet>(raw));
  }

  /** Create a pet */
  create(body: PetCreateParams, options?: RequestOptions): Promise<Pet> {
    return this._client.request<Pet>({ method: "POST", path: "/pets", body }, options);
  }

  /** Retrieve a pet */
  retrieve(petId: string, options?: RequestOptions): Promise<Pet> {
    if (!petId) {
      throw new Error("missing required pet_id parameter");
    }
    return this._client.request<Pet>({ method: "GET", path: `/pets/${petId}` }, options);
  }

  /** Download a pet's photo */
  downloadPhoto(petId: string, options?: RequestOptions): Promise<Response> {
    if (!petId) {
      throw new Error("missing required pet_id parameter");
    }
    return this._client.request<Response>({ method: "GET", path: `/pets/${petId}/photo`, headers: { Accept: "application/binary" }, raw: true }, options);
  }
}

export interface PetListParams {
  after?: string;
  limit?: number;
}

export interface PetCreateParams {
  name: string;
  status?: PetStatus;
}

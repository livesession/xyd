// Code generated by opensdk. DO NOT EDIT.

export interface Circle {
  type: "circle";
  radius: number;
}

export interface Square {
  type: "square";
  side: number;
}

/** A geometric shape, discriminated by its type property. */
export type Shape = Circle | Square;

/** Decode a `Shape` by its `type` discriminator (unknown value falls through). */
export function decodeShape(data: unknown): Shape {
  switch ((data as { type?: string } | null | undefined)?.type) {
    case "circle":
      return data as Circle;
    case "square":
      return data as Square;
    default:
      return data as Shape;
  }
}

/** A mapping-less union — stays an open interface with no decode helper. */
export type Payload = string | Record<string, unknown>;

export interface Event {
  id: string;
  kind?: string;
}

export interface EventList {
  data?: Event[];
  has_more?: boolean;
  next_cursor?: string;
}

export interface PublishEventRequest {
  /** Always "event" — auto-filled by the SDK when left zero. */
  object: "event";
  name: string;
  priority?: number;
}

export interface Log {
  id: string;
  message?: string;
}

export interface LogList {
  data?: Log[];
}

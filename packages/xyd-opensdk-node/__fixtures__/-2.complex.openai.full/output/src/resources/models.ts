// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { RequestOptions } from '../core/request';
import { Page } from '../core/pagination';
import type { DeleteModelResponse, Model } from '../models';

export class Models extends APIResource {
  /** Delete a fine-tuned model. You must have the Owner role in your organization to delete a model. */
  delete(model: string, options?: RequestOptions): Promise<DeleteModelResponse> {
    if (!model) {
      throw new Error("missing required model parameter");
    }
    return this._client.request<DeleteModelResponse>({ method: "DELETE", path: `/models/${model}` }, options);
  }

  /** Lists the currently available models, and provides basic information about each one such as the owner and availability. */
  list(options?: RequestOptions): Promise<Page<Model>> {
    return this._client
      .request<unknown>({ method: "GET", path: "/models" }, options)
      .then((raw) => Page.fromResponse<Model>(raw));
  }

  /** Retrieves a model instance, providing basic information about the model such as the owner and permissioning. */
  retrieve(model: string, options?: RequestOptions): Promise<Model> {
    if (!model) {
      throw new Error("missing required model parameter");
    }
    return this._client.request<Model>({ method: "GET", path: `/models/${model}` }, options);
  }
}

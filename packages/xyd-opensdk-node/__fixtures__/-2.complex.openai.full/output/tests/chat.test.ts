// Code generated by opensdk. DO NOT EDIT.

import { test } from 'node:test';
import assert from 'node:assert';

import { assertMatchesType, checkTestServer, testClient } from './setup';

test("completions.create (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.chat.completions.create({ messages: [{ content: "x", role: "developer" }], model: "x" });
  assertMatchesType(result);
});

test("completions.create (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.chat.completions.create({ messages: [{ content: "x", role: "developer", name: "x" }], model: "x", metadata: { key: "x" }, top_logprobs: 0, temperature: 0, top_p: 0, user: "x", safety_identifier: "x", prompt_cache_key: "x", service_tier: "auto", prompt_cache_retention: "in_memory", modalities: ["text"], verbosity: "low", reasoning_effort: "none", max_completion_tokens: 0, frequency_penalty: 0, presence_penalty: 0, web_search_options: { user_location: { type: "approximate", approximate: { country: "x", region: "x", city: "x", timezone: "x" } }, search_context_size: "low" }, response_format: { type: "text" }, audio: { voice: "x", format: "wav" }, store: true, stream: true, stop: "x", logit_bias: { key: 0 }, logprobs: true, max_tokens: 0, n: 0, prediction: { type: "content", content: "x" }, seed: 0, stream_options: { include_usage: true, include_obfuscation: true }, tools: [{ type: "function", function: { name: "x", description: "x", parameters: { key: {} as any }, strict: true } }], tool_choice: "none", parallel_tool_calls: true, function_call: "none", functions: [{ name: "x", description: "x", parameters: { key: {} as any } }] });
  assertMatchesType(result);
});

test("completions.delete (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.chat.completions.delete("completion_id");
  assertMatchesType(result);
});

test("completions.delete (path params)", () => {
  const client = testClient();
  assert.throws(() => client.chat.completions.delete(""), /missing required completion_id parameter/);
});

test("completions.list (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.chat.completions.list();
  assertMatchesType(result);
});

test("completions.list (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.chat.completions.list({ model: "model", metadata: { key: "x" }, after: "after", limit: 0, order: "asc" });
  assertMatchesType(result);
});

test("completions.retrieve (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.chat.completions.retrieve("completion_id");
  assertMatchesType(result);
});

test("completions.retrieve (path params)", () => {
  const client = testClient();
  assert.throws(() => client.chat.completions.retrieve(""), /missing required completion_id parameter/);
});

test("completions.update (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.chat.completions.update("completion_id", { metadata: { key: "x" } });
  assertMatchesType(result);
});

test("completions.update (path params)", () => {
  const client = testClient();
  assert.throws(() => client.chat.completions.update("", { metadata: { key: "x" } }), /missing required completion_id parameter/);
});

test("completions.messages.list (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.chat.completions.messages.list("completion_id");
  assertMatchesType(result);
});

test("completions.messages.list (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.chat.completions.messages.list("completion_id", { after: "after", limit: 0, order: "asc" });
  assertMatchesType(result);
});

test("completions.messages.list (path params)", () => {
  const client = testClient();
  assert.throws(() => client.chat.completions.messages.list(""), /missing required completion_id parameter/);
});

// Code generated by opensdk. DO NOT EDIT.

import { test } from 'node:test';
import assert from 'node:assert';

import { assertMatchesType, checkTestServer, testClient } from './setup';

test("cancel (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.uploads.cancel("upload_id");
  assertMatchesType(result);
});

test("cancel (path params)", () => {
  const client = testClient();
  assert.throws(() => client.uploads.cancel(""), /missing required upload_id parameter/);
});

test("complete (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.uploads.complete("upload_id", { part_ids: ["x"] });
  assertMatchesType(result);
});

test("complete (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.uploads.complete("upload_id", { part_ids: ["x"], md5: "x" });
  assertMatchesType(result);
});

test("complete (path params)", () => {
  const client = testClient();
  assert.throws(() => client.uploads.complete("", { part_ids: ["x"] }), /missing required upload_id parameter/);
});

test("create (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.uploads.create({ filename: "x", purpose: "assistants", bytes: 0, mime_type: "x" });
  assertMatchesType(result);
});

test("create (method, all params)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.uploads.create({ filename: "x", purpose: "assistants", bytes: 0, mime_type: "x", expires_after: { anchor: "created_at", seconds: 0 } });
  assertMatchesType(result);
});

test("parts.create (method)", async (t) => {
  if (!(await checkTestServer())) return t.skip();
  const client = testClient();
  const result = await client.uploads.parts.create("upload_id", { data: new Uint8Array([1, 2, 3]) });
  assertMatchesType(result);
});

test("parts.create (path params)", () => {
  const client = testClient();
  assert.throws(() => client.uploads.parts.create("", { data: new Uint8Array([1, 2, 3]) }), /missing required upload_id parameter/);
});

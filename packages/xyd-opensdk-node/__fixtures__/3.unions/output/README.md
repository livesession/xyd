# union-depot

Handcrafted IR exercising discriminated-union decode, const auto-fill and auto-pagers.

## Usage

```ts
import UnionDepot from 'union-depot';

const client = new UnionDepot({ apiKey: process.env.API_KEY });
```

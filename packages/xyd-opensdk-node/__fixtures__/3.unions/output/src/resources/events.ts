// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { RequestOptions } from '../core/request';
import { CursorPage } from '../core/pagination';
import type { Event } from '../models';

export class Events extends APIResource {
  /** List events (cursor-paginated) */
  list(query?: EventListParams, options?: RequestOptions): Promise<CursorPage<Event>> {
    return this._client
      .request<unknown>({ method: "GET", path: "/events", query }, options)
      .then((raw) => CursorPage.fromResponse<Event>(raw));
  }

  /** Publish an event (const-filled body field) */
  create(body: EventCreateParams, options?: RequestOptions): Promise<Event> {
    return this._client.request<Event>({ method: "POST", path: "/events", body }, options);
  }
}

export interface EventListParams {
  after?: string;
  limit?: number;
}

export interface EventCreateParams {
  object: "event";
  name: string;
  priority?: number;
}

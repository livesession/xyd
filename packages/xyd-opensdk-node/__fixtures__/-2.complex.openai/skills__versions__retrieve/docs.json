{
  "chain": [
    "skills",
    "versions"
  ],
  "action": "retrieve",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.skills.versions.retrieve(\"skill_123\", \"version\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.skills.versions.retrieve(skillId, version): Promise<SkillVersionResource>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "SkillVersionResource",
      "fields": [
        {
          "name": "object",
          "langType": "\"skill.version\"",
          "required": true,
          "description": "The object type, which is `skill.version`."
        },
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "Unique identifier for the skill version."
        },
        {
          "name": "skill_id",
          "langType": "string",
          "required": true,
          "description": "Identifier of the skill for this version."
        },
        {
          "name": "version",
          "langType": "string",
          "required": true,
          "description": "Version number for this skill."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "Unix timestamp (seconds) for when the version was created."
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "Name of the skill version."
        },
        {
          "name": "description",
          "langType": "string",
          "required": true,
          "description": "Description of the skill version."
        }
      ]
    }
  }
}

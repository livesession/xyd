{
  "chain": [
    "admin",
    "organization",
    "admin-api-keys"
  ],
  "action": "retrieve",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.admin.organization.adminApiKeys.retrieve(\"key_id\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.admin.organization.adminApiKeys.retrieve(keyId): Promise<AdminApiKey>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "AdminApiKey",
      "fields": [
        {
          "name": "object",
          "langType": "\"organization.admin_api_key\"",
          "required": true,
          "description": "The object type, which is always `organization.admin_api_key`"
        },
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "name",
          "langType": "string | null",
          "required": false,
          "description": "The name of the API key"
        },
        {
          "name": "redacted_value",
          "langType": "string",
          "required": true,
          "description": "The redacted value of the API key"
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the API key was created"
        },
        {
          "name": "last_used_at",
          "langType": "number | null",
          "required": false
        },
        {
          "name": "owner",
          "langType": "AdminApiKeyOwner",
          "required": true,
          "refTypeName": "AdminApiKeyOwner"
        }
      ]
    }
  }
}

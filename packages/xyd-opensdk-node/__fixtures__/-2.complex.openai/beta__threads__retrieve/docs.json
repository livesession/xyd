{
  "chain": [
    "beta",
    "threads"
  ],
  "action": "retrieve",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.beta.threads.retrieve(\"thread_id\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.beta.threads.retrieve(threadId): Promise<ThreadObject>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "ThreadObject",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints."
        },
        {
          "name": "object",
          "langType": "\"thread\"",
          "required": true,
          "description": "The object type, which is always `thread`."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the thread was created."
        },
        {
          "name": "tool_resources",
          "langType": "ThreadObjectToolResources | null",
          "required": true,
          "refTypeName": "ThreadObjectToolResources"
        },
        {
          "name": "metadata",
          "langType": "Metadata",
          "required": true,
          "refTypeName": "Metadata"
        }
      ]
    }
  }
}

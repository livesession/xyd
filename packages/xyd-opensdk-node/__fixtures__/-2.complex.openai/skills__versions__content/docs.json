{
  "chain": [
    "skills",
    "versions"
  ],
  "action": "content",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.skills.versions.content(\"skill_123\", \"version\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.skills.versions.content(skillId, version): Promise<string>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "string",
      "langType": "string"
    }
  }
}

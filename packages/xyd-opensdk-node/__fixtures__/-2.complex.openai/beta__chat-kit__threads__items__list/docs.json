{
  "chain": [
    "beta",
    "chat-kit",
    "threads",
    "items"
  ],
  "action": "list",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.beta.chatKit.threads.items.list(\"cthr_123\", { limit: 1, order: \"asc\", after: \"after\", before: \"before\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.beta.chatKit.threads.items.list(threadId, query?): Promise<CursorPage<ThreadItem>>",
    "request": {
      "typeName": "BetaChatKitThreadItemListParams",
      "argName": "query",
      "fields": [
        {
          "name": "limit",
          "langType": "number",
          "required": false,
          "description": "Maximum number of thread items to return. Defaults to 20."
        },
        {
          "name": "order",
          "langType": "OrderEnum",
          "required": false,
          "description": "Sort order for results by creation time. Defaults to `desc`.",
          "refTypeName": "OrderEnum"
        },
        {
          "name": "after",
          "langType": "string",
          "required": false,
          "description": "List items created after this thread item ID. Defaults to null for the first page."
        },
        {
          "name": "before",
          "langType": "string",
          "required": false,
          "description": "List items created before this thread item ID. Defaults to null for the newest results."
        }
      ]
    },
    "response": {
      "typeName": "ThreadItem",
      "langType": "ThreadItem",
      "note": "paginated (CursorPage)"
    }
  }
}

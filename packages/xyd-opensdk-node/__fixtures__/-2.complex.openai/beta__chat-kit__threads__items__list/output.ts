// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { APIClient, RequestOptions } from '../core/request';
import { CursorPage } from '../core/pagination';
import type { OrderEnum, ThreadItem } from '../models';

export class Beta extends APIResource {
  readonly chatKit: BetaChatKit;

  constructor(client: APIClient) {
    super(client);
    this.chatKit = new BetaChatKit(client);
  }
}

export class BetaChatKit extends APIResource {
  readonly threads: BetaChatKitThreads;

  constructor(client: APIClient) {
    super(client);
    this.threads = new BetaChatKitThreads(client);
  }
}

export class BetaChatKitThreads extends APIResource {
  readonly items: BetaChatKitThreadsItems;

  constructor(client: APIClient) {
    super(client);
    this.items = new BetaChatKitThreadsItems(client);
  }
}

export class BetaChatKitThreadsItems extends APIResource {
  /** List items that belong to a ChatKit thread. */
  list(threadId: string, query?: BetaChatKitThreadItemListParams, options?: RequestOptions): Promise<CursorPage<ThreadItem>> {
    if (!threadId) {
      throw new Error("missing required thread_id parameter");
    }
    return this._client
      .request<unknown>({ method: "GET", path: `/chatkit/threads/${threadId}/items`, query }, options)
      .then((raw) => CursorPage.fromResponse<ThreadItem>(raw));
  }
}

export interface BetaChatKitThreadItemListParams {
  limit?: number;
  order?: OrderEnum;
  after?: string;
  before?: string;
}

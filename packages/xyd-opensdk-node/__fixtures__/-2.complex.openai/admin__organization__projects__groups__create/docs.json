{
  "chain": [
    "admin",
    "organization",
    "projects",
    "groups"
  ],
  "action": "create",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.admin.organization.projects.groups.create(\"project_id\", { group_id: \"string\", role: \"string\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.groups.create(projectId, body): Promise<ProjectGroup>",
    "request": {
      "typeName": "AdminOrganizationProjectGroupCreateParams",
      "argName": "body",
      "fields": [
        {
          "name": "group_id",
          "langType": "string",
          "required": true,
          "description": "Identifier of the group to add to the project."
        },
        {
          "name": "role",
          "langType": "string",
          "required": true,
          "description": "Identifier of the project role to grant to the group."
        }
      ]
    },
    "response": {
      "typeName": "ProjectGroup",
      "fields": [
        {
          "name": "object",
          "langType": "\"project.group\"",
          "required": true,
          "description": "Always `project.group`."
        },
        {
          "name": "project_id",
          "langType": "string",
          "required": true,
          "description": "Identifier of the project."
        },
        {
          "name": "group_id",
          "langType": "string",
          "required": true,
          "description": "Identifier of the group that has access to the project."
        },
        {
          "name": "group_name",
          "langType": "string",
          "required": true,
          "description": "Display name of the group."
        },
        {
          "name": "group_type",
          "langType": "string",
          "required": true,
          "description": "The type of the group."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "Unix timestamp (in seconds) when the group was granted project access."
        }
      ]
    }
  }
}

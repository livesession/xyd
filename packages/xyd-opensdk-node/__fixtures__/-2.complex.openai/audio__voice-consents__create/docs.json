{
  "chain": [
    "audio",
    "voice-consents"
  ],
  "action": "create",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.audio.voiceConsents.create({ name: \"string\", recording: new Uint8Array([1, 2, 3]), language: \"string\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.audio.voiceConsents.create(body): Promise<VoiceConsentResource>",
    "request": {
      "typeName": "AudioVoiceConsentCreateParams",
      "argName": "body",
      "fields": [
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "The label to use for this consent recording."
        },
        {
          "name": "recording",
          "langType": "Uint8Array | Blob",
          "required": true,
          "description": "The consent audio recording file. Maximum size is 10 MiB.\n\nSupported MIME types:\n`audio/mpeg`, `audio/wav`, `audio/x-wav`, `audio/ogg`, `audio/aac`, `audio/flac`, `audio/webm`, `audio/mp4`.\n"
        },
        {
          "name": "language",
          "langType": "string",
          "required": true,
          "description": "The BCP 47 language tag for the consent phrase (for example, `en-US`)."
        }
      ]
    },
    "response": {
      "typeName": "VoiceConsentResource",
      "fields": [
        {
          "name": "object",
          "langType": "\"audio.voice_consent\"",
          "required": true,
          "description": "The object type, which is always `audio.voice_consent`."
        },
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The consent recording identifier."
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "The label provided when the consent recording was uploaded."
        },
        {
          "name": "language",
          "langType": "string",
          "required": true,
          "description": "The BCP 47 language tag for the consent phrase (for example, `en-US`)."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the consent recording was created."
        }
      ]
    }
  }
}

// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { APIClient, RequestOptions } from '../core/request';
import { CursorPage } from '../core/pagination';
import type { EvalRunOutputItem, EvalsRunsOutputItemsListOrder, EvalsRunsOutputItemsListStatus } from '../models';

export class Evals extends APIResource {
  readonly runs: EvalsRuns;

  constructor(client: APIClient) {
    super(client);
    this.runs = new EvalsRuns(client);
  }
}

export class EvalsRuns extends APIResource {
  readonly outputItems: EvalsRunsOutputItems;

  constructor(client: APIClient) {
    super(client);
    this.outputItems = new EvalsRunsOutputItems(client);
  }
}

export class EvalsRunsOutputItems extends APIResource {
  /** Get a list of output items for an evaluation run. */
  list(evalId: string, runId: string, query?: EvalRunOutputItemListParams, options?: RequestOptions): Promise<CursorPage<EvalRunOutputItem>> {
    if (!evalId) {
      throw new Error("missing required eval_id parameter");
    }
    if (!runId) {
      throw new Error("missing required run_id parameter");
    }
    return this._client
      .request<unknown>({ method: "GET", path: `/evals/${evalId}/runs/${runId}/output_items`, query }, options)
      .then((raw) => CursorPage.fromResponse<EvalRunOutputItem>(raw));
  }
}

export interface EvalRunOutputItemListParams {
  after?: string;
  limit?: number;
  status?: EvalsRunsOutputItemsListStatus;
  order?: EvalsRunsOutputItemsListOrder;
}

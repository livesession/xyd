{
  "chain": [
    "audio",
    "voices"
  ],
  "action": "create",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.audio.voices.create({ name: \"string\", audio_sample: new Uint8Array([1, 2, 3]), consent: \"string\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.audio.voices.create(body): Promise<VoiceResource>",
    "request": {
      "typeName": "AudioVoiceCreateParams",
      "argName": "body",
      "fields": [
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "The name of the new voice."
        },
        {
          "name": "audio_sample",
          "langType": "Uint8Array | Blob",
          "required": true,
          "description": "The sample audio recording file. Maximum size is 10 MiB.\n\nSupported MIME types:\n`audio/mpeg`, `audio/wav`, `audio/x-wav`, `audio/ogg`, `audio/aac`, `audio/flac`, `audio/webm`, `audio/mp4`.\n"
        },
        {
          "name": "consent",
          "langType": "string",
          "required": true,
          "description": "The consent recording ID (for example, `cons_1234`)."
        }
      ]
    },
    "response": {
      "typeName": "VoiceResource",
      "fields": [
        {
          "name": "object",
          "langType": "\"audio.voice\"",
          "required": true,
          "description": "The object type, which is always `audio.voice`."
        },
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The voice identifier, which can be referenced in API endpoints."
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "The name of the voice."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the voice was created."
        }
      ]
    }
  }
}

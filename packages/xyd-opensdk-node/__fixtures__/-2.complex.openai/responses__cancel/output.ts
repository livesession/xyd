// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { RequestOptions } from '../core/request';
import type { Response } from '../models';

export class Responses extends APIResource {
  /**
   * Cancels a model response with the given ID. Only responses created with
   * the `background` parameter set to `true` can be cancelled.
   * [Learn more](/docs/guides/background).
   */
  cancel(responseId: string, options?: RequestOptions): Promise<Response> {
    if (!responseId) {
      throw new Error("missing required response_id parameter");
    }
    return this._client.request<Response>({ method: "POST", path: `/responses/${responseId}/cancel` }, options);
  }
}

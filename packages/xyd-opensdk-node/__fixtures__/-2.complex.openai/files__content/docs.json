{
  "chain": [
    "files"
  ],
  "action": "content",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.files.content(\"file_id\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.files.content(fileId): Promise<string>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "string",
      "langType": "string"
    }
  }
}

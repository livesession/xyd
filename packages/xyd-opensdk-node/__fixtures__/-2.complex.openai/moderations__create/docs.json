{
  "chain": [
    "moderations"
  ],
  "action": "create",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.moderations.create({ input: \"string\", model: \"string\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.moderations.create(body): Promise<CreateModerationResponse>",
    "request": {
      "typeName": "ModerationCreateParams",
      "argName": "body",
      "fields": [
        {
          "name": "input",
          "langType": "CreateModerationRequestInput",
          "required": true,
          "description": "Input (or inputs) to classify. Can be a single string, an array of strings, or\nan array of multi-modal input objects similar to other models.\n",
          "refTypeName": "CreateModerationRequestInput"
        },
        {
          "name": "model",
          "langType": "CreateModerationRequestModel",
          "required": false,
          "description": "The content moderation model you would like to use. Learn more in\n[the moderation guide](/docs/guides/moderation), and learn about\navailable models [here](/docs/models#moderation).\n",
          "refTypeName": "CreateModerationRequestModel"
        }
      ]
    },
    "response": {
      "typeName": "CreateModerationResponse",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The unique identifier for the moderation request."
        },
        {
          "name": "model",
          "langType": "string",
          "required": true,
          "description": "The model used to generate the moderation results."
        },
        {
          "name": "results",
          "langType": "CreateModerationResponseResultsItem[]",
          "required": true,
          "description": "A list of moderation objects."
        }
      ]
    }
  }
}

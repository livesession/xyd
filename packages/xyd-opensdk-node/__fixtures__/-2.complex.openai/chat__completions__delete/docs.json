{
  "chain": [
    "chat",
    "completions"
  ],
  "action": "delete",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.chat.completions.delete(\"completion_id\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.chat.completions.delete(completionId): Promise<ChatCompletionDeleted>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "ChatCompletionDeleted",
      "fields": [
        {
          "name": "object",
          "langType": "\"chat.completion.deleted\"",
          "required": true,
          "description": "The type of object being deleted."
        },
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The ID of the chat completion that was deleted."
        },
        {
          "name": "deleted",
          "langType": "boolean",
          "required": true,
          "description": "Whether the chat completion was deleted."
        }
      ]
    }
  }
}

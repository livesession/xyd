// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { APIClient, RequestOptions } from '../core/request';
import type { CreateEvalRunRequestDataSource, EvalRun, Metadata } from '../models';

export class Evals extends APIResource {
  readonly runs: EvalsRuns;

  constructor(client: APIClient) {
    super(client);
    this.runs = new EvalsRuns(client);
  }
}

export class EvalsRuns extends APIResource {
  /** Kicks off a new run for a given evaluation, specifying the data source, and what model configuration to use to test. The datasource will be validated against the schema specified in the config of the evaluation. */
  create(evalId: string, body: EvalRunCreateParams, options?: RequestOptions): Promise<EvalRun> {
    if (!evalId) {
      throw new Error("missing required eval_id parameter");
    }
    return this._client.request<EvalRun>({ method: "POST", path: `/evals/${evalId}/runs`, body }, options);
  }
}

export interface EvalRunCreateParams {
  name?: string;
  metadata?: Metadata;
  data_source: CreateEvalRunRequestDataSource;
}

{
  "chain": [
    "admin",
    "organization",
    "projects",
    "api-keys"
  ],
  "action": "retrieve",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.admin.organization.projects.apiKeys.retrieve(\"project_id\", \"api_key_id\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.apiKeys.retrieve(projectId, apiKeyId): Promise<ProjectApiKey>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "ProjectApiKey",
      "fields": [
        {
          "name": "object",
          "langType": "\"organization.project.api_key\"",
          "required": true,
          "description": "The object type, which is always `organization.project.api_key`"
        },
        {
          "name": "redacted_value",
          "langType": "string",
          "required": true,
          "description": "The redacted value of the API key"
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "The name of the API key"
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the API key was created"
        },
        {
          "name": "last_used_at",
          "langType": "number | null",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the API key was last used."
        },
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "owner",
          "langType": "ProjectApiKeyOwner",
          "required": true,
          "refTypeName": "ProjectApiKeyOwner"
        }
      ]
    }
  }
}

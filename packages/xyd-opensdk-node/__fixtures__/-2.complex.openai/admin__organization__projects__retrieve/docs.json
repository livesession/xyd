{
  "chain": [
    "admin",
    "organization",
    "projects"
  ],
  "action": "retrieve",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.admin.organization.projects.retrieve(\"project_id\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.retrieve(projectId): Promise<Project>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "Project",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "object",
          "langType": "\"organization.project\"",
          "required": true,
          "description": "The object type, which is always `organization.project`"
        },
        {
          "name": "name",
          "langType": "string | null",
          "required": false,
          "description": "The name of the project. This appears in reporting."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the project was created."
        },
        {
          "name": "archived_at",
          "langType": "number | null",
          "required": false
        },
        {
          "name": "status",
          "langType": "string | null",
          "required": false,
          "description": "`active` or `archived`"
        },
        {
          "name": "external_key_id",
          "langType": "string | null",
          "required": false,
          "description": "The external key associated with the project."
        }
      ]
    }
  }
}

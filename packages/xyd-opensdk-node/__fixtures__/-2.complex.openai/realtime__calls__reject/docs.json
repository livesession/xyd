{
  "chain": [
    "realtime",
    "calls"
  ],
  "action": "reject",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nawait client.realtime.calls.reject(\"call_id\", { status_code: 1 });\n",
  "typeReference": {
    "signature": "client.realtime.calls.reject(callId, body?): Promise<void>",
    "request": {
      "typeName": "RealtimeCallRejectParams",
      "argName": "body",
      "fields": [
        {
          "name": "status_code",
          "langType": "number",
          "required": false,
          "description": "SIP response code to send back to the caller. Defaults to `603` (Decline)\nwhen omitted."
        }
      ]
    },
    "response": {
      "langType": "void",
      "note": "no response body"
    }
  }
}

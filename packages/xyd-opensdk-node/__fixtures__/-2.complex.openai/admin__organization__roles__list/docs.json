{
  "chain": [
    "admin",
    "organization",
    "roles"
  ],
  "action": "list",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.admin.organization.roles.list({ limit: 1000, after: \"after\", order: \"asc\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.admin.organization.roles.list(query?): Promise<CursorPage<Role>>",
    "request": {
      "typeName": "AdminOrganizationRoleListParams",
      "argName": "query",
      "fields": [
        {
          "name": "limit",
          "langType": "number",
          "required": false,
          "description": "A limit on the number of roles to return. Defaults to 1000."
        },
        {
          "name": "after",
          "langType": "string",
          "required": false,
          "description": "Cursor for pagination. Provide the value from the previous response's `next` field to continue listing roles."
        },
        {
          "name": "order",
          "langType": "AdminOrganizationRolesListOrder",
          "required": false,
          "description": "Sort order for the returned roles.",
          "refTypeName": "AdminOrganizationRolesListOrder"
        }
      ]
    },
    "response": {
      "typeName": "Role",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "object",
          "langType": "\"role\"",
          "required": true,
          "description": "Always `role`."
        },
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "Identifier for the role."
        },
        {
          "name": "name",
          "langType": "string",
          "required": true,
          "description": "Unique name for the role."
        },
        {
          "name": "description",
          "langType": "string | null",
          "required": true,
          "description": "Optional description of the role."
        },
        {
          "name": "permissions",
          "langType": "string[]",
          "required": true,
          "description": "Permissions granted by the role."
        },
        {
          "name": "resource_type",
          "langType": "string",
          "required": true,
          "description": "Resource type the role is bound to (for example `api.organization` or `api.project`)."
        },
        {
          "name": "predefined_role",
          "langType": "boolean",
          "required": true,
          "description": "Whether the role is predefined and managed by OpenAI."
        }
      ]
    }
  }
}

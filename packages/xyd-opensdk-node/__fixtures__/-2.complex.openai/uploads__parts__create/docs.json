{
  "chain": [
    "uploads",
    "parts"
  ],
  "action": "create",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.uploads.parts.create(\"upload_abc123\", { data: new Uint8Array([1, 2, 3]) });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.uploads.parts.create(uploadId, body): Promise<UploadPart>",
    "request": {
      "typeName": "UploadPartCreateParams",
      "argName": "body",
      "fields": [
        {
          "name": "data",
          "langType": "Uint8Array | Blob",
          "required": true,
          "description": "The chunk of bytes for this Part.\n"
        }
      ]
    },
    "response": {
      "typeName": "UploadPart",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The upload Part unique identifier, which can be referenced in API endpoints."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the Part was created."
        },
        {
          "name": "upload_id",
          "langType": "string",
          "required": true,
          "description": "The ID of the Upload object that this Part was added to."
        },
        {
          "name": "object",
          "langType": "\"upload.part\"",
          "required": true,
          "description": "The object type, which is always `upload.part`."
        }
      ]
    }
  }
}

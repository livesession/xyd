// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { APIClient, RequestOptions } from '../core/request';
import type { ProjectGroupDeletedResource } from '../models';

export class Admin extends APIResource {
  readonly organization: AdminOrganization;

  constructor(client: APIClient) {
    super(client);
    this.organization = new AdminOrganization(client);
  }
}

export class AdminOrganization extends APIResource {
  readonly projects: AdminOrganizationProjects;

  constructor(client: APIClient) {
    super(client);
    this.projects = new AdminOrganizationProjects(client);
  }
}

export class AdminOrganizationProjects extends APIResource {
  readonly groups: AdminOrganizationProjectsGroups;

  constructor(client: APIClient) {
    super(client);
    this.groups = new AdminOrganizationProjectsGroups(client);
  }
}

export class AdminOrganizationProjectsGroups extends APIResource {
  /** Revokes a group's access to a project. */
  delete(projectId: string, groupId: string, options?: RequestOptions): Promise<ProjectGroupDeletedResource> {
    if (!projectId) {
      throw new Error("missing required project_id parameter");
    }
    if (!groupId) {
      throw new Error("missing required group_id parameter");
    }
    return this._client.request<ProjectGroupDeletedResource>({ method: "DELETE", path: `/organization/projects/${projectId}/groups/${groupId}` }, options);
  }
}

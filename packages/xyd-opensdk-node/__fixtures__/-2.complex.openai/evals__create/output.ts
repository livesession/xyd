// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { RequestOptions } from '../core/request';
import type { CreateEvalRequestDataSourceConfig, CreateEvalRequestTestingCriteriaItem, Eval, Metadata } from '../models';

export class Evals extends APIResource {
  /**
   * Create the structure of an evaluation that can be used to test a model's performance.
   * An evaluation is a set of testing criteria and the config for a data source, which dictates the schema of the data used in the evaluation. After creating an evaluation, you can run it on different models and model parameters. We support several types of graders and datasources.
   * For more information, see the [Evals guide](/docs/guides/evals).
   */
  create(body: EvalCreateParams, options?: RequestOptions): Promise<Eval> {
    return this._client.request<Eval>({ method: "POST", path: "/evals", body }, options);
  }
}

export interface EvalCreateParams {
  name?: string;
  metadata?: Metadata;
  data_source_config: CreateEvalRequestDataSourceConfig;
  testing_criteria: CreateEvalRequestTestingCriteriaItem[];
}

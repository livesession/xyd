{
  "chain": [
    "containers",
    "files"
  ],
  "action": "create",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.containers.files.create(\"container_id\", { file_id: \"string\", file: new Uint8Array([1, 2, 3]) });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.containers.files.create(containerId, body): Promise<ContainerFileResource>",
    "request": {
      "typeName": "ContainerFileCreateParams",
      "argName": "body",
      "fields": [
        {
          "name": "file_id",
          "langType": "string",
          "required": false,
          "description": "Name of the file to create."
        },
        {
          "name": "file",
          "langType": "Uint8Array | Blob",
          "required": false,
          "description": "The File object (not file name) to be uploaded.\n"
        }
      ]
    },
    "response": {
      "typeName": "ContainerFileResource",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "Unique identifier for the file."
        },
        {
          "name": "object",
          "langType": "string",
          "required": true,
          "description": "The type of this object (`container.file`)."
        },
        {
          "name": "container_id",
          "langType": "string",
          "required": true,
          "description": "The container this file belongs to."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "Unix timestamp (in seconds) when the file was created."
        },
        {
          "name": "bytes",
          "langType": "number",
          "required": true,
          "description": "Size of the file in bytes."
        },
        {
          "name": "path",
          "langType": "string",
          "required": true,
          "description": "Path of the file in the container."
        },
        {
          "name": "source",
          "langType": "string",
          "required": true,
          "description": "Source of the file (e.g., `user`, `assistant`)."
        }
      ]
    }
  }
}

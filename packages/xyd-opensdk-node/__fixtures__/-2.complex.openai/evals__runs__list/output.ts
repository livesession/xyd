// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { APIClient, RequestOptions } from '../core/request';
import { CursorPage } from '../core/pagination';
import type { EvalRun, EvalsRunsListOrder, EvalsRunsListStatus } from '../models';

export class Evals extends APIResource {
  readonly runs: EvalsRuns;

  constructor(client: APIClient) {
    super(client);
    this.runs = new EvalsRuns(client);
  }
}

export class EvalsRuns extends APIResource {
  /** Get a list of runs for an evaluation. */
  list(evalId: string, query?: EvalRunListParams, options?: RequestOptions): Promise<CursorPage<EvalRun>> {
    if (!evalId) {
      throw new Error("missing required eval_id parameter");
    }
    return this._client
      .request<unknown>({ method: "GET", path: `/evals/${evalId}/runs`, query }, options)
      .then((raw) => CursorPage.fromResponse<EvalRun>(raw));
  }
}

export interface EvalRunListParams {
  after?: string;
  limit?: number;
  order?: EvalsRunsListOrder;
  status?: EvalsRunsListStatus;
}

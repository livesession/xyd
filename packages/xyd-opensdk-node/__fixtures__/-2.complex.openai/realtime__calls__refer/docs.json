{
  "chain": [
    "realtime",
    "calls"
  ],
  "action": "refer",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nawait client.realtime.calls.refer(\"call_id\", { target_uri: \"string\" });\n",
  "typeReference": {
    "signature": "client.realtime.calls.refer(callId, body): Promise<void>",
    "request": {
      "typeName": "RealtimeCallReferParams",
      "argName": "body",
      "fields": [
        {
          "name": "target_uri",
          "langType": "string",
          "required": true,
          "description": "URI that should appear in the SIP Refer-To header. Supports values like\n`tel:+14155550123` or `sip:agent@example.com`."
        }
      ]
    },
    "response": {
      "langType": "void",
      "note": "no response body"
    }
  }
}

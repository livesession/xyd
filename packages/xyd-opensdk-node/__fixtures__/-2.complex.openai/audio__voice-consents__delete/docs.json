{
  "chain": [
    "audio",
    "voice-consents"
  ],
  "action": "delete",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.audio.voiceConsents.delete(\"consent_id\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.audio.voiceConsents.delete(consentId): Promise<VoiceConsentDeletedResource>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "VoiceConsentDeletedResource",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "The consent recording identifier."
        },
        {
          "name": "object",
          "langType": "\"audio.voice_consent\"",
          "required": true
        },
        {
          "name": "deleted",
          "langType": "boolean",
          "required": true
        }
      ]
    }
  }
}

{
  "chain": [
    "videos"
  ],
  "action": "download-content",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.videos.downloadContent(\"video_123\", { variant: \"video\" });\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.videos.downloadContent(videoId, query?): Promise<string>",
    "request": {
      "typeName": "VideoDownloadContentParams",
      "argName": "query",
      "fields": [
        {
          "name": "variant",
          "langType": "VideoContentVariant",
          "required": false,
          "description": "Which downloadable asset to return. Defaults to the MP4 video.",
          "refTypeName": "VideoContentVariant"
        }
      ]
    },
    "response": {
      "typeName": "string",
      "langType": "string"
    }
  }
}

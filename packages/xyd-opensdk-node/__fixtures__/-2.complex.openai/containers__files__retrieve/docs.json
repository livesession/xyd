{
  "chain": [
    "containers",
    "files"
  ],
  "action": "retrieve",
  "usage": "import Openai from \"openai\";\n\nconst client = new Openai({\n  apiKey: process.env[\"OPENAI_API_KEY\"],\n});\n\nconst result = await client.containers.files.retrieve(\"container_id\", \"file_id\");\nconsole.log(result);\n",
  "typeReference": {
    "signature": "client.containers.files.retrieve(containerId, fileId): Promise<ContainerFileResource>",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "ContainerFileResource",
      "fields": [
        {
          "name": "id",
          "langType": "string",
          "required": true,
          "description": "Unique identifier for the file."
        },
        {
          "name": "object",
          "langType": "string",
          "required": true,
          "description": "The type of this object (`container.file`)."
        },
        {
          "name": "container_id",
          "langType": "string",
          "required": true,
          "description": "The container this file belongs to."
        },
        {
          "name": "created_at",
          "langType": "number",
          "required": true,
          "description": "Unix timestamp (in seconds) when the file was created."
        },
        {
          "name": "bytes",
          "langType": "number",
          "required": true,
          "description": "Size of the file in bytes."
        },
        {
          "name": "path",
          "langType": "string",
          "required": true,
          "description": "Path of the file in the container."
        },
        {
          "name": "source",
          "langType": "string",
          "required": true,
          "description": "Source of the file (e.g., `user`, `assistant`)."
        }
      ]
    }
  }
}

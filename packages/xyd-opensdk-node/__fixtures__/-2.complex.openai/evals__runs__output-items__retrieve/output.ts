// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { APIClient, RequestOptions } from '../core/request';
import type { EvalRunOutputItem } from '../models';

export class Evals extends APIResource {
  readonly runs: EvalsRuns;

  constructor(client: APIClient) {
    super(client);
    this.runs = new EvalsRuns(client);
  }
}

export class EvalsRuns extends APIResource {
  readonly outputItems: EvalsRunsOutputItems;

  constructor(client: APIClient) {
    super(client);
    this.outputItems = new EvalsRunsOutputItems(client);
  }
}

export class EvalsRunsOutputItems extends APIResource {
  /** Get an evaluation run output item by ID. */
  retrieve(evalId: string, runId: string, outputItemId: string, options?: RequestOptions): Promise<EvalRunOutputItem> {
    if (!evalId) {
      throw new Error("missing required eval_id parameter");
    }
    if (!runId) {
      throw new Error("missing required run_id parameter");
    }
    if (!outputItemId) {
      throw new Error("missing required output_item_id parameter");
    }
    return this._client.request<EvalRunOutputItem>({ method: "GET", path: `/evals/${evalId}/runs/${runId}/output_items/${outputItemId}` }, options);
  }
}

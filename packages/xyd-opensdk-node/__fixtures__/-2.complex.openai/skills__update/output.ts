// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { RequestOptions } from '../core/request';
import type { SkillResource } from '../models';

export class Skills extends APIResource {
  /** Update the default version pointer for a skill. */
  update(skillId: string, body?: SkillUpdateParams, options?: RequestOptions): Promise<SkillResource> {
    if (!skillId) {
      throw new Error("missing required skill_id parameter");
    }
    return this._client.request<SkillResource>({ method: "POST", path: `/skills/${skillId}`, body }, options);
  }
}

export interface SkillUpdateParams {
  default_version: string;
}

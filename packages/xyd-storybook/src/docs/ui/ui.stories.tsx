import React, {} from 'react';
import type {Meta} from '@storybook/react';

export default {
    title: 'UI/Example',
} as Meta;

export const Default = () => {
    return <div style={{
        padding: "100px",
        paddingTop: "0px",
        margin: "0 auto",
    }}>
        TODO: finish
    </div>
}


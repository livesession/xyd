{
  "chain": [
    "catalog"
  ],
  "action": "browse",
  "usage": "import os\n\nfrom extensions_demo import Client\n\nclient = Client(\n    api_key=os.environ.get(\"EXTENSIONS_DEMO_API_KEY\"),\n)\n\nresult = client.catalog.browse()\nprint(result)\n",
  "typeReference": {
    "signature": "client.catalog.browse() -> list[Thing]",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "list[Thing]",
      "langType": "list[Thing]"
    }
  }
}

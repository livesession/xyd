{
  "chain": [
    "audio",
    "voice-consents"
  ],
  "action": "update",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.audio.voice_consents.update(\"consent_id\", name=\"string\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.audio.voice_consents.update(consent_id, name) -> VoiceConsentResource",
    "request": {
      "fields": [
        {
          "name": "name",
          "langType": "str",
          "required": true,
          "description": "The updated label for this consent recording."
        }
      ]
    },
    "response": {
      "typeName": "VoiceConsentResource",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is always `audio.voice_consent`."
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The consent recording identifier."
        },
        {
          "name": "name",
          "langType": "str",
          "required": true,
          "description": "The label provided when the consent recording was uploaded."
        },
        {
          "name": "language",
          "langType": "str",
          "required": true,
          "description": "The BCP 47 language tag for the consent phrase (for example, `en-US`)."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the consent recording was created."
        }
      ]
    }
  }
}

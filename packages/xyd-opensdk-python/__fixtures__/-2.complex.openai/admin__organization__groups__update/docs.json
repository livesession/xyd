{
  "chain": [
    "admin",
    "organization",
    "groups"
  ],
  "action": "update",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.groups.update(\"group_id\", name=\"string\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.groups.update(group_id, name) -> GroupResourceWithSuccess",
    "request": {
      "fields": [
        {
          "name": "name",
          "langType": "str",
          "required": true,
          "description": "New display name for the group."
        }
      ]
    },
    "response": {
      "typeName": "GroupResourceWithSuccess",
      "fields": [
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "Identifier for the group."
        },
        {
          "name": "name",
          "langType": "str",
          "required": true,
          "description": "Updated display name for the group."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "Unix timestamp (in seconds) when the group was created."
        },
        {
          "name": "is_scim_managed",
          "langType": "bool",
          "required": true,
          "description": "Whether the group is managed through SCIM and controlled by your identity provider."
        }
      ]
    }
  }
}

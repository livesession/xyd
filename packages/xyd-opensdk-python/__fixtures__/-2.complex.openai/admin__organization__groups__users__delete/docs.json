{
  "chain": [
    "admin",
    "organization",
    "groups",
    "users"
  ],
  "action": "delete",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.groups.users.delete(\"group_id\", \"user_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.groups.users.delete(group_id, user_id) -> GroupUserDeletedResource",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "GroupUserDeletedResource",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "Always `group.user.deleted`."
        },
        {
          "name": "deleted",
          "langType": "bool",
          "required": true,
          "description": "Whether the group membership was removed."
        }
      ]
    }
  }
}

{
  "chain": [
    "admin",
    "organization",
    "users"
  ],
  "action": "delete",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.users.delete(\"user_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.users.delete(user_id) -> UserDeleteResponse",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "UserDeleteResponse",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true
        },
        {
          "name": "id",
          "langType": "str",
          "required": true
        },
        {
          "name": "deleted",
          "langType": "bool",
          "required": true
        }
      ]
    }
  }
}

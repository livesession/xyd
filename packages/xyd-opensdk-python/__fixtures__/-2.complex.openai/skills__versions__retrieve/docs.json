{
  "chain": [
    "skills",
    "versions"
  ],
  "action": "retrieve",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.skills.versions.retrieve(\"skill_123\", \"version\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.skills.versions.retrieve(skill_id, version) -> SkillVersionResource",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "SkillVersionResource",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is `skill.version`."
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "Unique identifier for the skill version."
        },
        {
          "name": "skill_id",
          "langType": "str",
          "required": true,
          "description": "Identifier of the skill for this version."
        },
        {
          "name": "version",
          "langType": "str",
          "required": true,
          "description": "Version number for this skill."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "Unix timestamp (seconds) for when the version was created."
        },
        {
          "name": "name",
          "langType": "str",
          "required": true,
          "description": "Name of the skill version."
        },
        {
          "name": "description",
          "langType": "str",
          "required": true,
          "description": "Description of the skill version."
        }
      ]
    }
  }
}

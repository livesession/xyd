{
  "chain": [
    "admin",
    "organization",
    "roles"
  ],
  "action": "delete",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.roles.delete(\"role_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.roles.delete(role_id) -> RoleDeletedResource",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "RoleDeletedResource",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "Always `role.deleted`."
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "Identifier of the deleted role."
        },
        {
          "name": "deleted",
          "langType": "bool",
          "required": true,
          "description": "Whether the role was deleted."
        }
      ]
    }
  }
}

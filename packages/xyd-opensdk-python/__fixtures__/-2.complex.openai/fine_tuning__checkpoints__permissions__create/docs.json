{
  "chain": [
    "fine-tuning",
    "checkpoints",
    "permissions"
  ],
  "action": "create",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.fine_tuning.checkpoints.permissions.create(\"ft:gpt-4o-mini-2024-07-18:org:weather:B7R9VjQd\", project_ids=[\"string\"])\nprint(result)\n",
  "typeReference": {
    "signature": "client.fine_tuning.checkpoints.permissions.create(fine_tuned_model_checkpoint, project_ids) -> CursorPage[FineTuningCheckpointPermission]",
    "request": {
      "fields": [
        {
          "name": "project_ids",
          "langType": "list[str]",
          "required": true,
          "description": "The project identifiers to grant access to."
        }
      ]
    },
    "response": {
      "typeName": "CursorPage[FineTuningCheckpointPermission]",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The permission identifier, which can be referenced in the API endpoints."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the permission was created."
        },
        {
          "name": "project_id",
          "langType": "str",
          "required": true,
          "description": "The project identifier that the permission is for."
        },
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is always \"checkpoint.permission\"."
        }
      ]
    }
  }
}

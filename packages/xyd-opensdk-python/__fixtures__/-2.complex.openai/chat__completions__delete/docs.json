{
  "chain": [
    "chat",
    "completions"
  ],
  "action": "delete",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.chat.completions.delete(\"completion_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.chat.completions.delete(completion_id) -> ChatCompletionDeleted",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "ChatCompletionDeleted",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The type of object being deleted."
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The ID of the chat completion that was deleted."
        },
        {
          "name": "deleted",
          "langType": "bool",
          "required": true,
          "description": "Whether the chat completion was deleted."
        }
      ]
    }
  }
}

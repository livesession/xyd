{
  "chain": [
    "audio",
    "voice-consents"
  ],
  "action": "delete",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.audio.voice_consents.delete(\"consent_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.audio.voice_consents.delete(consent_id) -> VoiceConsentDeletedResource",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "VoiceConsentDeletedResource",
      "fields": [
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The consent recording identifier."
        },
        {
          "name": "object",
          "langType": "str",
          "required": true
        },
        {
          "name": "deleted",
          "langType": "bool",
          "required": true
        }
      ]
    }
  }
}

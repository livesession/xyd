{
  "chain": [
    "fine-tuning",
    "alpha",
    "graders",
    "validate"
  ],
  "action": "create",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.fine_tuning.alpha.graders.validate.create(grader={\"type\": \"string_check\", \"name\": \"string\", \"input\": \"string\", \"reference\": \"string\", \"operation\": \"eq\"})\nprint(result)\n",
  "typeReference": {
    "signature": "client.fine_tuning.alpha.graders.validate.create(grader) -> ValidateGraderResponse",
    "request": {
      "fields": [
        {
          "name": "grader",
          "langType": "ValidateGraderRequestGrader",
          "required": true,
          "description": "The grader used for the fine-tuning job.",
          "refTypeName": "ValidateGraderRequestGrader"
        }
      ]
    },
    "response": {
      "typeName": "ValidateGraderResponse",
      "fields": [
        {
          "name": "grader",
          "langType": "ValidateGraderRequestGrader",
          "required": false,
          "description": "The grader used for the fine-tuning job.",
          "refTypeName": "ValidateGraderRequestGrader"
        }
      ]
    }
  }
}

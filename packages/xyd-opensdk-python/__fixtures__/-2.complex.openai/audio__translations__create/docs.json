{
  "chain": [
    "audio",
    "translations"
  ],
  "action": "create",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.audio.translations.create(file=b\"Example data\", model=\"string\", prompt=\"string\", response_format=\"json\", temperature=0)\nprint(result)\n",
  "typeReference": {
    "signature": "client.audio.translations.create(file, model, prompt=..., response_format=..., temperature=...) -> AudioTranslationsCreateResponse",
    "request": {
      "fields": [
        {
          "name": "file",
          "langType": "Union[bytes, BinaryIO]",
          "required": true,
          "description": "The audio file object (not file name) translate, in one of these formats: flac, mp3, mp4, mpeg, mpga, m4a, ogg, wav, or webm.\n"
        },
        {
          "name": "model",
          "langType": "CreateTranslationRequestModel",
          "required": true,
          "description": "ID of the model to use. Only `whisper-1` (which is powered by our open source Whisper V2 model) is currently available.\n",
          "refTypeName": "CreateTranslationRequestModel"
        },
        {
          "name": "prompt",
          "langType": "Optional[str]",
          "required": false,
          "description": "An optional text to guide the model's style or continue a previous audio segment. The [prompt](/docs/guides/speech-to-text#prompting) should be in English.\n"
        },
        {
          "name": "response_format",
          "langType": "Optional[CreateTranslationRequestResponseFormat]",
          "required": false,
          "description": "The format of the output, in one of these options: `json`, `text`, `srt`, `verbose_json`, or `vtt`.\n",
          "refTypeName": "CreateTranslationRequestResponseFormat"
        },
        {
          "name": "temperature",
          "langType": "Optional[float]",
          "required": false,
          "description": "The sampling temperature, between 0 and 1. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic. If set to 0, the model will use [log probability](https://en.wikipedia.org/wiki/Log_probability) to automatically increase the temperature until certain thresholds are hit.\n"
        }
      ]
    },
    "response": {
      "typeName": "AudioTranslationsCreateResponse",
      "langType": "AudioTranslationsCreateResponse"
    }
  }
}

{
  "chain": [
    "beta",
    "assistants"
  ],
  "action": "delete",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.beta.assistants.delete(\"assistant_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.beta.assistants.delete(assistant_id) -> DeleteAssistantResponse",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "DeleteAssistantResponse",
      "fields": [
        {
          "name": "id",
          "langType": "str",
          "required": true
        },
        {
          "name": "deleted",
          "langType": "bool",
          "required": true
        },
        {
          "name": "object",
          "langType": "str",
          "required": true
        }
      ]
    }
  }
}

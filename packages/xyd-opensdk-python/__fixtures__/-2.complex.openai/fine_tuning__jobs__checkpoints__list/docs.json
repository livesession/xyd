{
  "chain": [
    "fine-tuning",
    "jobs",
    "checkpoints"
  ],
  "action": "list",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.fine_tuning.jobs.checkpoints.list(\"ft-AF1WoRqd3aJAHsqc9NY7iL8F\", after=\"after\", limit=10)\nprint(result)\n",
  "typeReference": {
    "signature": "client.fine_tuning.jobs.checkpoints.list(fine_tuning_job_id, after=..., limit=...) -> CursorPage[FineTuningJobCheckpoint]",
    "request": {
      "fields": [
        {
          "name": "after",
          "langType": "Optional[str]",
          "required": false,
          "description": "Identifier for the last checkpoint ID from the previous pagination request."
        },
        {
          "name": "limit",
          "langType": "Optional[int]",
          "required": false,
          "description": "Number of checkpoints to retrieve."
        }
      ]
    },
    "response": {
      "typeName": "CursorPage[FineTuningJobCheckpoint]",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The checkpoint identifier, which can be referenced in the API endpoints."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the checkpoint was created."
        },
        {
          "name": "fine_tuned_model_checkpoint",
          "langType": "str",
          "required": true,
          "description": "The name of the fine-tuned checkpoint model that is created."
        },
        {
          "name": "step_number",
          "langType": "int",
          "required": true,
          "description": "The step number that the checkpoint was created at."
        },
        {
          "name": "metrics",
          "langType": "FineTuningJobCheckpointMetrics",
          "required": true,
          "description": "Metrics at the step number during the fine-tuning job.",
          "refTypeName": "FineTuningJobCheckpointMetrics"
        },
        {
          "name": "fine_tuning_job_id",
          "langType": "str",
          "required": true,
          "description": "The name of the fine-tuning job that this checkpoint was created from."
        },
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is always \"fine_tuning.job.checkpoint\"."
        }
      ]
    }
  }
}

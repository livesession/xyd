{
  "chain": [
    "admin",
    "organization",
    "groups"
  ],
  "action": "delete",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.groups.delete(\"group_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.groups.delete(group_id) -> GroupDeletedResource",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "GroupDeletedResource",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "Always `group.deleted`."
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "Identifier of the deleted group."
        },
        {
          "name": "deleted",
          "langType": "bool",
          "required": true,
          "description": "Whether the group was deleted."
        }
      ]
    }
  }
}

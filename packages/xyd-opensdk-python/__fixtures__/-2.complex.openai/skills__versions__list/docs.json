{
  "chain": [
    "skills",
    "versions"
  ],
  "action": "list",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.skills.versions.list(\"skill_123\", limit=1, order=\"asc\", after=\"skillver_123\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.skills.versions.list(skill_id, limit=..., order=..., after=...) -> CursorPage[SkillVersionResource]",
    "request": {
      "fields": [
        {
          "name": "limit",
          "langType": "Optional[int]",
          "required": false,
          "description": "Number of versions to retrieve."
        },
        {
          "name": "order",
          "langType": "Optional[OrderEnum]",
          "required": false,
          "description": "Sort order of results by version number.",
          "refTypeName": "OrderEnum"
        },
        {
          "name": "after",
          "langType": "Optional[str]",
          "required": false,
          "description": "The skill version ID to start after."
        }
      ]
    },
    "response": {
      "typeName": "CursorPage[SkillVersionResource]",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is `skill.version`."
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "Unique identifier for the skill version."
        },
        {
          "name": "skill_id",
          "langType": "str",
          "required": true,
          "description": "Identifier of the skill for this version."
        },
        {
          "name": "version",
          "langType": "str",
          "required": true,
          "description": "Version number for this skill."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "Unix timestamp (seconds) for when the version was created."
        },
        {
          "name": "name",
          "langType": "str",
          "required": true,
          "description": "Name of the skill version."
        },
        {
          "name": "description",
          "langType": "str",
          "required": true,
          "description": "Description of the skill version."
        }
      ]
    }
  }
}

{
  "chain": [
    "vector-stores"
  ],
  "action": "update",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.vector_stores.update(\"vector_store_id\", name=\"string\", expires_after={\"anchor\": \"last_active_at\", \"days\": 1}, metadata={\"key\": \"string\"})\nprint(result)\n",
  "typeReference": {
    "signature": "client.vector_stores.update(vector_store_id, name=..., expires_after=..., metadata=...) -> VectorStoreObject",
    "request": {
      "fields": [
        {
          "name": "name",
          "langType": "Optional[str]",
          "required": false,
          "description": "The name of the vector store."
        },
        {
          "name": "expires_after",
          "langType": "Optional[VectorStoreExpirationAfter]",
          "required": false,
          "refTypeName": "VectorStoreExpirationAfter"
        },
        {
          "name": "metadata",
          "langType": "Optional[Metadata]",
          "required": false,
          "refTypeName": "Metadata"
        }
      ]
    },
    "response": {
      "typeName": "VectorStoreObject",
      "fields": [
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints."
        },
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "The object type, which is always `vector_store`."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the vector store was created."
        },
        {
          "name": "name",
          "langType": "str",
          "required": true,
          "description": "The name of the vector store."
        },
        {
          "name": "usage_bytes",
          "langType": "int",
          "required": true,
          "description": "The total number of bytes used by the files in the vector store."
        },
        {
          "name": "file_counts",
          "langType": "VectorStoreObjectFileCounts",
          "required": true,
          "refTypeName": "VectorStoreObjectFileCounts"
        },
        {
          "name": "status",
          "langType": "VectorStoreObjectStatus",
          "required": true,
          "description": "The status of the vector store, which can be either `expired`, `in_progress`, or `completed`. A status of `completed` indicates that the vector store is ready for use.",
          "refTypeName": "VectorStoreObjectStatus"
        },
        {
          "name": "expires_after",
          "langType": "VectorStoreExpirationAfter",
          "required": false,
          "refTypeName": "VectorStoreExpirationAfter"
        },
        {
          "name": "expires_at",
          "langType": "Optional[int]",
          "required": false
        },
        {
          "name": "last_active_at",
          "langType": "Optional[int]",
          "required": true
        },
        {
          "name": "metadata",
          "langType": "Metadata",
          "required": true,
          "refTypeName": "Metadata"
        }
      ]
    }
  }
}

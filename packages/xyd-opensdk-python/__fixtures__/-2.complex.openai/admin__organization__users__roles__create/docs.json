{
  "chain": [
    "admin",
    "organization",
    "users",
    "roles"
  ],
  "action": "create",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.users.roles.create(\"user_id\", role_id=\"string\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.users.roles.create(user_id, role_id) -> UserRoleAssignment",
    "request": {
      "fields": [
        {
          "name": "role_id",
          "langType": "str",
          "required": true,
          "description": "Identifier of the role to assign."
        }
      ]
    },
    "response": {
      "typeName": "UserRoleAssignment",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "Always `user.role`."
        },
        {
          "name": "user",
          "langType": "User",
          "required": true,
          "refTypeName": "User"
        },
        {
          "name": "role",
          "langType": "Role",
          "required": true,
          "refTypeName": "Role"
        }
      ]
    }
  }
}

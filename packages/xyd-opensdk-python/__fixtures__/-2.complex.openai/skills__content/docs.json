{
  "chain": [
    "skills"
  ],
  "action": "content",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.skills.content(\"skill_123\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.skills.content(skill_id) -> str",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "str",
      "langType": "str"
    }
  }
}

{
  "chain": [
    "videos",
    "characters"
  ],
  "action": "retrieve",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.videos.characters.retrieve(\"char_123\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.videos.characters.retrieve(character_id) -> VideoCharacterResource",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "VideoCharacterResource",
      "fields": [
        {
          "name": "id",
          "langType": "Optional[str]",
          "required": true
        },
        {
          "name": "name",
          "langType": "Optional[str]",
          "required": true
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "Unix timestamp (in seconds) when the character was created."
        }
      ]
    }
  }
}

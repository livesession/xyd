{
  "chain": [
    "admin",
    "organization",
    "projects",
    "groups"
  ],
  "action": "create",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.projects.groups.create(\"project_id\", group_id=\"string\", role=\"string\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.groups.create(project_id, group_id, role) -> ProjectGroup",
    "request": {
      "fields": [
        {
          "name": "group_id",
          "langType": "str",
          "required": true,
          "description": "Identifier of the group to add to the project."
        },
        {
          "name": "role",
          "langType": "str",
          "required": true,
          "description": "Identifier of the project role to grant to the group."
        }
      ]
    },
    "response": {
      "typeName": "ProjectGroup",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "Always `project.group`."
        },
        {
          "name": "project_id",
          "langType": "str",
          "required": true,
          "description": "Identifier of the project."
        },
        {
          "name": "group_id",
          "langType": "str",
          "required": true,
          "description": "Identifier of the group that has access to the project."
        },
        {
          "name": "group_name",
          "langType": "str",
          "required": true,
          "description": "Display name of the group."
        },
        {
          "name": "group_type",
          "langType": "str",
          "required": true,
          "description": "The type of the group."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "Unix timestamp (in seconds) when the group was granted project access."
        }
      ]
    }
  }
}

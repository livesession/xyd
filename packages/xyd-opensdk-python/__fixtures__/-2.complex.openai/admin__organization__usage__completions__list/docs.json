{
  "chain": [
    "admin",
    "organization",
    "usage",
    "completions"
  ],
  "action": "list",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.usage.completions.list(start_time=1, end_time=1, bucket_width=\"1m\", project_ids=[\"string\"], user_ids=[\"string\"], api_key_ids=[\"string\"], models=[\"string\"], batch=True, group_by=[\"project_id\"], limit=1, page=\"page\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.usage.completions.list(start_time, end_time=..., bucket_width=..., project_ids=..., user_ids=..., api_key_ids=..., models=..., batch=..., group_by=..., limit=..., page=...) -> CursorPage[UsageTimeBucket]",
    "request": {
      "fields": [
        {
          "name": "start_time",
          "langType": "int",
          "required": true,
          "description": "Start time (Unix seconds) of the query time range, inclusive."
        },
        {
          "name": "end_time",
          "langType": "Optional[int]",
          "required": false,
          "description": "End time (Unix seconds) of the query time range, exclusive."
        },
        {
          "name": "bucket_width",
          "langType": "Optional[AdminOrganizationUsageCompletionsListBucketWidth]",
          "required": false,
          "description": "Width of each time bucket in response. Currently `1m`, `1h` and `1d` are supported, default to `1d`.",
          "refTypeName": "AdminOrganizationUsageCompletionsListBucketWidth"
        },
        {
          "name": "project_ids",
          "langType": "Optional[list[str]]",
          "required": false,
          "description": "Return only usage for these projects."
        },
        {
          "name": "user_ids",
          "langType": "Optional[list[str]]",
          "required": false,
          "description": "Return only usage for these users."
        },
        {
          "name": "api_key_ids",
          "langType": "Optional[list[str]]",
          "required": false,
          "description": "Return only usage for these API keys."
        },
        {
          "name": "models",
          "langType": "Optional[list[str]]",
          "required": false,
          "description": "Return only usage for these models."
        },
        {
          "name": "batch",
          "langType": "Optional[bool]",
          "required": false,
          "description": "If `true`, return batch jobs only. If `false`, return non-batch jobs only. By default, return both.\n"
        },
        {
          "name": "group_by",
          "langType": "Optional[list[AdminOrganizationUsageCompletionsListGroupByItem]]",
          "required": false,
          "description": "Group the usage data by the specified fields. Support fields include `project_id`, `user_id`, `api_key_id`, `model`, `batch`, `service_tier` or any combination of them."
        },
        {
          "name": "limit",
          "langType": "Optional[int]",
          "required": false,
          "description": "Specifies the number of buckets to return.\n- `bucket_width=1d`: default: 7, max: 31\n- `bucket_width=1h`: default: 24, max: 168\n- `bucket_width=1m`: default: 60, max: 1440\n"
        },
        {
          "name": "page",
          "langType": "Optional[str]",
          "required": false,
          "description": "A cursor for use in pagination. Corresponding to the `next_page` field from the previous response."
        }
      ]
    },
    "response": {
      "typeName": "CursorPage[UsageTimeBucket]",
      "note": "paginated (CursorPage)",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true
        },
        {
          "name": "start_time",
          "langType": "int",
          "required": true
        },
        {
          "name": "end_time",
          "langType": "int",
          "required": true
        },
        {
          "name": "results",
          "langType": "list[UsageTimeBucketResultsItem]",
          "required": true
        }
      ]
    }
  }
}

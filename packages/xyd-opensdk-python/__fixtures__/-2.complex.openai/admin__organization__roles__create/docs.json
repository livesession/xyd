{
  "chain": [
    "admin",
    "organization",
    "roles"
  ],
  "action": "create",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.roles.create(role_name=\"string\", permissions=[\"string\"], description=\"string\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.roles.create(role_name, permissions, description=...) -> Role",
    "request": {
      "fields": [
        {
          "name": "role_name",
          "langType": "str",
          "required": true,
          "description": "Unique name for the role."
        },
        {
          "name": "permissions",
          "langType": "list[str]",
          "required": true,
          "description": "Permissions to grant to the role."
        },
        {
          "name": "description",
          "langType": "Optional[str]",
          "required": false,
          "description": "Optional description of the role."
        }
      ]
    },
    "response": {
      "typeName": "Role",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "Always `role`."
        },
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "Identifier for the role."
        },
        {
          "name": "name",
          "langType": "str",
          "required": true,
          "description": "Unique name for the role."
        },
        {
          "name": "description",
          "langType": "Optional[str]",
          "required": true,
          "description": "Optional description of the role."
        },
        {
          "name": "permissions",
          "langType": "list[str]",
          "required": true,
          "description": "Permissions granted by the role."
        },
        {
          "name": "resource_type",
          "langType": "str",
          "required": true,
          "description": "Resource type the role is bound to (for example `api.organization` or `api.project`)."
        },
        {
          "name": "predefined_role",
          "langType": "bool",
          "required": true,
          "description": "Whether the role is predefined and managed by OpenAI."
        }
      ]
    }
  }
}

{
  "chain": [
    "admin",
    "organization",
    "groups",
    "roles"
  ],
  "action": "create",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.groups.roles.create(\"group_id\", role_id=\"string\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.groups.roles.create(group_id, role_id) -> GroupRoleAssignment",
    "request": {
      "fields": [
        {
          "name": "role_id",
          "langType": "str",
          "required": true,
          "description": "Identifier of the role to assign."
        }
      ]
    },
    "response": {
      "typeName": "GroupRoleAssignment",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "Always `group.role`."
        },
        {
          "name": "group",
          "langType": "Group",
          "required": true,
          "refTypeName": "Group"
        },
        {
          "name": "role",
          "langType": "Role",
          "required": true,
          "refTypeName": "Role"
        }
      ]
    }
  }
}

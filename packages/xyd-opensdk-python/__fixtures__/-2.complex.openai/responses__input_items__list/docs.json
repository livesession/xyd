{
  "chain": [
    "responses",
    "input-items"
  ],
  "action": "list",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.responses.input_items.list(\"response_id\", limit=20, order=\"asc\", after=\"after\", include=[\"file_search_call.results\"])\nprint(result)\n",
  "typeReference": {
    "signature": "client.responses.input_items.list(response_id, limit=..., order=..., after=..., include=...) -> CursorPage[ItemResource]",
    "request": {
      "fields": [
        {
          "name": "limit",
          "langType": "Optional[int]",
          "required": false,
          "description": "A limit on the number of objects to be returned. Limit can range between\n1 and 100, and the default is 20.\n"
        },
        {
          "name": "order",
          "langType": "Optional[ResponsesInputItemsListOrder]",
          "required": false,
          "description": "The order to return the input items in. Default is `desc`.\n- `asc`: Return the input items in ascending order.\n- `desc`: Return the input items in descending order.\n",
          "refTypeName": "ResponsesInputItemsListOrder"
        },
        {
          "name": "after",
          "langType": "Optional[str]",
          "required": false,
          "description": "An item ID to list items after, used in pagination.\n"
        },
        {
          "name": "include",
          "langType": "Optional[list[IncludeEnum]]",
          "required": false,
          "description": "Additional fields to include in the response. See the `include`\nparameter for Response creation above for more information.\n"
        }
      ]
    },
    "response": {
      "typeName": "CursorPage[ItemResource]",
      "langType": "CursorPage[ItemResource]",
      "note": "paginated (CursorPage)"
    }
  }
}

{
  "chain": [
    "beta",
    "chat-kit",
    "threads"
  ],
  "action": "retrieve",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.beta.chat_kit.threads.retrieve(\"cthr_123\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.beta.chat_kit.threads.retrieve(thread_id) -> ThreadResource",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "ThreadResource",
      "fields": [
        {
          "name": "id",
          "langType": "str",
          "required": true,
          "description": "Identifier of the thread."
        },
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "Type discriminator that is always `chatkit.thread`."
        },
        {
          "name": "created_at",
          "langType": "int",
          "required": true,
          "description": "Unix timestamp (in seconds) for when the thread was created."
        },
        {
          "name": "title",
          "langType": "Optional[str]",
          "required": true
        },
        {
          "name": "status",
          "langType": "ThreadResourceStatus",
          "required": true,
          "description": "Current status for the thread. Defaults to `active` for newly created threads.",
          "refTypeName": "ThreadResourceStatus"
        },
        {
          "name": "user",
          "langType": "str",
          "required": true,
          "description": "Free-form string that identifies your end user who owns the thread."
        }
      ]
    }
  }
}

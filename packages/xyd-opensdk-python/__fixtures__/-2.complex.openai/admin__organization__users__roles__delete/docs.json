{
  "chain": [
    "admin",
    "organization",
    "users",
    "roles"
  ],
  "action": "delete",
  "usage": "import os\n\nfrom openai import Client\n\nclient = Client(\n    api_key=os.environ.get(\"OPENAI_API_KEY\"),\n)\n\nresult = client.admin.organization.users.roles.delete(\"user_id\", \"role_id\")\nprint(result)\n",
  "typeReference": {
    "signature": "client.admin.organization.users.roles.delete(user_id, role_id) -> DeletedRoleAssignmentResource",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "DeletedRoleAssignmentResource",
      "fields": [
        {
          "name": "object",
          "langType": "str",
          "required": true,
          "description": "Identifier for the deleted assignment, such as `group.role.deleted` or `user.role.deleted`."
        },
        {
          "name": "deleted",
          "langType": "bool",
          "required": true,
          "description": "Whether the assignment was removed."
        }
      ]
    }
  }
}

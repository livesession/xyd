// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("responses")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("create")
                .about("Creates a model response. Provide [text](/docs/guides/text) or\n[image](/docs/guides/images) inputs to generate [text](/docs/guides/text)\nor [JSON](/docs/guides/structured-outputs) outputs. Have the model call\nyour own [custom code](/docs/guides/function-calling) or use built-in\n[tools](/docs/guides/tools) like [web search](/docs/guides/tools-web-search)\nor [file search](/docs/guides/tools-file-search) to use your own data\nas input for the model's response.\n")
                .arg(
                    Arg::new("metadata")
                        .long("metadata"),
                )
                .arg(
                    Arg::new("top-logprobs")
                        .long("top-logprobs")
                        .value_parser(clap::value_parser!(i64))
                        .help("An integer between 0 and 20 specifying the maximum number of most likely\ntokens to return at each token position, each with an associated log\nprobability. In some cases, the number of returned tokens may be fewer than\nrequested.\n"),
                )
                .arg(
                    Arg::new("temperature")
                        .long("temperature"),
                )
                .arg(
                    Arg::new("top-p")
                        .long("top-p"),
                )
                .arg(
                    Arg::new("user")
                        .long("user")
                        .help("This field is being replaced by `safety_identifier` and `prompt_cache_key`. Use `prompt_cache_key` instead to maintain caching optimizations.\nA stable identifier for your end-users.\nUsed to boost cache hit rates by better bucketing similar requests and  to help OpenAI detect and prevent abuse. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).\n"),
                )
                .arg(
                    Arg::new("safety-identifier")
                        .long("safety-identifier")
                        .help("A stable identifier used to help detect users of your application that may be violating OpenAI's usage policies.\nThe IDs should be a string that uniquely identifies each user, with a maximum length of 64 characters. We recommend hashing their username or email address, in order to avoid sending us any identifying information. [Learn more](/docs/guides/safety-best-practices#safety-identifiers).\n"),
                )
                .arg(
                    Arg::new("prompt-cache-key")
                        .long("prompt-cache-key")
                        .help("Used by OpenAI to cache responses for similar requests to optimize your cache hit rates. Replaces the `user` field. [Learn more](/docs/guides/prompt-caching).\n"),
                )
                .arg(
                    Arg::new("service-tier")
                        .long("service-tier"),
                )
                .arg(
                    Arg::new("prompt-cache-retention")
                        .long("prompt-cache-retention"),
                )
                .arg(
                    Arg::new("previous-response-id")
                        .long("previous-response-id"),
                )
                .arg(
                    Arg::new("model")
                        .long("model")
                        .help("Model ID used to generate the response, like `gpt-4o` or `o3`. OpenAI\noffers a wide range of models with different capabilities, performance\ncharacteristics, and price points. Refer to the [model guide](/docs/models)\nto browse and compare available models.\n"),
                )
                .arg(
                    Arg::new("reasoning")
                        .long("reasoning"),
                )
                .arg(
                    Arg::new("background")
                        .long("background"),
                )
                .arg(
                    Arg::new("max-tool-calls")
                        .long("max-tool-calls"),
                )
                .arg(
                    Arg::new("text")
                        .long("text")
                        .help("Configuration options for a text response from the model. Can be plain\ntext or structured JSON data. Learn more:\n- [Text inputs and outputs](/docs/guides/text)\n- [Structured Outputs](/docs/guides/structured-outputs)\n"),
                )
                .arg(
                    Arg::new("tools")
                        .long("tools")
                        .action(clap::ArgAction::Append)
                        .value_delimiter(',')
                        .help("An array of tools the model may call while generating a response. You\ncan specify which tool to use by setting the `tool_choice` parameter.\n\nWe support the following categories of tools:\n- **Built-in tools**: Tools that are provided by OpenAI that extend the\n  model's capabilities, like [web search](/docs/guides/tools-web-search)\n  or [file search](/docs/guides/tools-file-search). Learn more about\n  [built-in tools](/docs/guides/tools).\n- **MCP Tools**: Integrations with third-party systems via custom MCP servers\n  or predefined connectors such as Google Drive and SharePoint. Learn more about\n  [MCP Tools](/docs/guides/tools-connectors-mcp).\n- **Function calls (custom tools)**: Functions that are defined by you,\n  enabling the model to call your own code with strongly typed arguments\n  and outputs. Learn more about\n  [function calling](/docs/guides/function-calling). You can also use\n  custom tools to call your own code.\n"),
                )
                .arg(
                    Arg::new("tool-choice")
                        .long("tool-choice")
                        .help("How the model should select which tool (or tools) to use when generating\na response. See the `tools` parameter to see how to specify which tools\nthe model can call.\n"),
                )
                .arg(
                    Arg::new("prompt")
                        .long("prompt"),
                )
                .arg(
                    Arg::new("truncation")
                        .long("truncation"),
                )
                .arg(
                    Arg::new("input")
                        .long("input")
                        .help("Text, image, or file inputs to the model, used to generate a response.\n\nLearn more:\n- [Text inputs and outputs](/docs/guides/text)\n- [Image inputs](/docs/guides/images)\n- [File inputs](/docs/guides/pdf-files)\n- [Conversation state](/docs/guides/conversation-state)\n- [Function calling](/docs/guides/function-calling)\n"),
                )
                .arg(
                    Arg::new("include")
                        .long("include"),
                )
                .arg(
                    Arg::new("parallel-tool-calls")
                        .long("parallel-tool-calls"),
                )
                .arg(
                    Arg::new("store")
                        .long("store"),
                )
                .arg(
                    Arg::new("instructions")
                        .long("instructions"),
                )
                .arg(
                    Arg::new("stream")
                        .long("stream"),
                )
                .arg(
                    Arg::new("stream-options")
                        .long("stream-options"),
                )
                .arg(
                    Arg::new("conversation")
                        .long("conversation"),
                )
                .arg(
                    Arg::new("context-management")
                        .long("context-management"),
                )
                .arg(
                    Arg::new("max-output-tokens")
                        .long("max-output-tokens"),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["responses", "create"] => handle_responses_create(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_responses_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/responses".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("metadata") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("metadata".to_string(), value);
    }
    if let Some(v) = m.get_one::<i64>("top-logprobs") {
        body.insert("top_logprobs".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<String>("temperature") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("temperature".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("top-p") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("top_p".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("user") {
        body.insert("user".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("safety-identifier") {
        body.insert("safety_identifier".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("prompt-cache-key") {
        body.insert("prompt_cache_key".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("service-tier") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("service_tier".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("prompt-cache-retention") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("prompt_cache_retention".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("previous-response-id") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("previous_response_id".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("model") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("model".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("reasoning") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("reasoning".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("background") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("background".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("max-tool-calls") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("max_tool_calls".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("text") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("text".to_string(), value);
    }
    if let Some(vs) = m.get_many::<String>("tools") {
        body.insert("tools".to_string(), serde_json::Value::Array(vs.cloned().map(serde_json::Value::String).collect()));
    }
    if let Some(v) = m.get_one::<String>("tool-choice") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("tool_choice".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("prompt") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("prompt".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("truncation") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("truncation".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("input") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("input".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("include") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("include".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("parallel-tool-calls") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("parallel_tool_calls".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("store") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("store".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("instructions") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("instructions".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("stream") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("stream".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("stream-options") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("stream_options".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("conversation") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("conversation".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("context-management") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("context_management".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("max-output-tokens") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("max_output_tokens".to_string(), value);
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

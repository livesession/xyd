// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("conversations")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("items")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("create")
                        .about("Create items in a conversation with the given ID.")
                        .arg(
                            Arg::new("conversation-id")
                                .help("The ID of the conversation to add the item to.")
                                .required(true),
                        )
                        .arg(
                            Arg::new("include")
                                .long("include")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(',')
                                .help("Additional fields to include in the response. See the `include`\nparameter for [listing Conversation items above](/docs/api-reference/conversations/list-items#conversations_list_items-include) for more information.\n"),
                        )
                        .arg(
                            Arg::new("items")
                                .long("items")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(',')
                                .help("The items to add to the conversation. You may add up to 20 items at a time.\n")
                                .required(true),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["conversations", "items", "create"] => handle_conversations_items_create(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_conversations_items_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let conversation_id = m.get_one::<String>("conversation-id").map(String::as_str).unwrap_or("");
    let path = format!("/conversations/{}/items", runtime::path_escape(conversation_id));
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(vs) = m.get_many::<String>("include") {
        query.push(("include", vs.cloned().collect::<Vec<_>>().join(",")));
    }
    let mut body = serde_json::Map::new();
    if let Some(vs) = m.get_many::<String>("items") {
        body.insert("items".to_string(), serde_json::Value::Array(vs.cloned().map(serde_json::Value::String).collect()));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query,
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("conversations")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("items")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("list")
                        .about("List all items for a conversation with the given ID.")
                        .arg(
                            Arg::new("conversation-id")
                                .help("The ID of the conversation to list items for.")
                                .required(true),
                        )
                        .arg(
                            Arg::new("limit")
                                .long("limit")
                                .value_parser(clap::value_parser!(i64))
                                .help("A limit on the number of objects to be returned. Limit can range between\n1 and 100, and the default is 20.\n"),
                        )
                        .arg(
                            Arg::new("order")
                                .long("order")
                                .help("The order to return the input items in. Default is `desc`.\n- `asc`: Return the input items in ascending order.\n- `desc`: Return the input items in descending order.\n"),
                        )
                        .arg(
                            Arg::new("after")
                                .long("after")
                                .help("An item ID to list items after, used in pagination.\n"),
                        )
                        .arg(
                            Arg::new("include")
                                .long("include")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(',')
                                .help("Specify additional output data to include in the model response. Currently supported values are:\n- `web_search_call.action.sources`: Include the sources of the web search tool call.\n- `code_interpreter_call.outputs`: Includes the outputs of python code execution in code interpreter tool call items.\n- `computer_call_output.output.image_url`: Include image urls from the computer call output.\n- `file_search_call.results`: Include the search results of the file search tool call.\n- `message.input_image.image_url`: Include image urls from the input message.\n- `message.output_text.logprobs`: Include logprobs with assistant messages.\n- `reasoning.encrypted_content`: Includes an encrypted version of reasoning tokens in reasoning item outputs. This enables reasoning items to be used in multi-turn conversations when using the Responses API statelessly (like when the `store` parameter is set to `false`, or when an organization is enrolled in the zero data retention program)."),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["conversations", "items", "list"] => handle_conversations_items_list(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_conversations_items_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let conversation_id = m.get_one::<String>("conversation-id").map(String::as_str).unwrap_or("");
    let path = format!("/conversations/{}/items", runtime::path_escape(conversation_id));
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<i64>("limit") {
        query.push(("limit", v.to_string()));
    }
    if let Some(v) = m.get_one::<String>("order") {
        query.push(("order", v.clone()));
    }
    if let Some(v) = m.get_one::<String>("after") {
        query.push(("after", v.clone()));
    }
    if let Some(vs) = m.get_many::<String>("include") {
        query.push(("include", vs.cloned().collect::<Vec<_>>().join(",")));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

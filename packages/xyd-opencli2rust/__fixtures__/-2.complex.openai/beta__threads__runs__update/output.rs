// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("threads")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("runs")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("create")
                        .about("Create a thread and run it in one request.")
                        .arg(
                            Arg::new("assistant-id")
                                .long("assistant-id")
                                .help("The ID of the [assistant](/docs/api-reference/assistants) to use to execute this run.")
                                .required(true),
                        )
                        .arg(
                            Arg::new("thread")
                                .long("thread")
                                .help("Options to create a new thread. If no thread is provided when running a\nrequest, an empty thread will be created.\n"),
                        )
                        .arg(
                            Arg::new("model")
                                .long("model")
                                .help("The ID of the [Model](/docs/api-reference/models) to be used to execute this run. If a value is provided here, it will override the model associated with the assistant. If not, the model associated with the assistant will be used."),
                        )
                        .arg(
                            Arg::new("instructions")
                                .long("instructions")
                                .help("Override the default system message of the assistant. This is useful for modifying the behavior on a per-run basis."),
                        )
                        .arg(
                            Arg::new("tools")
                                .long("tools")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(',')
                                .help("Override the tools the assistant can use for this run. This is useful for modifying the behavior on a per-run basis."),
                        )
                        .arg(
                            Arg::new("tool-resources")
                                .long("tool-resources")
                                .help("A set of resources that are used by the assistant's tools. The resources are specific to the type of tool. For example, the `code_interpreter` tool requires a list of file IDs, while the `file_search` tool requires a list of vector store IDs.\n"),
                        )
                        .arg(
                            Arg::new("metadata")
                                .long("metadata"),
                        )
                        .arg(
                            Arg::new("temperature")
                                .long("temperature")
                                .value_parser(clap::value_parser!(f64))
                                .help("What sampling temperature to use, between 0 and 2. Higher values like 0.8 will make the output more random, while lower values like 0.2 will make it more focused and deterministic.\n"),
                        )
                        .arg(
                            Arg::new("top-p")
                                .long("top-p")
                                .value_parser(clap::value_parser!(f64))
                                .help("An alternative to sampling with temperature, called nucleus sampling, where the model considers the results of the tokens with top_p probability mass. So 0.1 means only the tokens comprising the top 10% probability mass are considered.\n\nWe generally recommend altering this or temperature but not both.\n"),
                        )
                        .arg(
                            Arg::new("stream")
                                .long("stream")
                                .num_args(0..=1)
                                .require_equals(true)
                                .default_missing_value("true")
                                .value_parser(clap::value_parser!(bool))
                                .help("If `true`, returns a stream of events that happen during the Run as server-sent events, terminating when the Run enters a terminal state with a `data: [DONE]` message.\n"),
                        )
                        .arg(
                            Arg::new("max-prompt-tokens")
                                .long("max-prompt-tokens")
                                .value_parser(clap::value_parser!(i64))
                                .help("The maximum number of prompt tokens that may be used over the course of the run. The run will make a best effort to use only the number of prompt tokens specified, across multiple turns of the run. If the run exceeds the number of prompt tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.\n"),
                        )
                        .arg(
                            Arg::new("max-completion-tokens")
                                .long("max-completion-tokens")
                                .value_parser(clap::value_parser!(i64))
                                .help("The maximum number of completion tokens that may be used over the course of the run. The run will make a best effort to use only the number of completion tokens specified, across multiple turns of the run. If the run exceeds the number of completion tokens specified, the run will end with status `incomplete`. See `incomplete_details` for more info.\n"),
                        )
                        .arg(
                            Arg::new("truncation-strategy")
                                .long("truncation-strategy"),
                        )
                        .arg(
                            Arg::new("tool-choice")
                                .long("tool-choice"),
                        )
                        .arg(
                            Arg::new("parallel-tool-calls")
                                .long("parallel-tool-calls")
                                .num_args(0..=1)
                                .require_equals(true)
                                .default_missing_value("true")
                                .value_parser(clap::value_parser!(bool))
                                .help("Whether to enable [parallel function calling](/docs/guides/function-calling#configuring-parallel-function-calling) during tool use."),
                        )
                        .arg(
                            Arg::new("response-format")
                                .long("response-format")
                                .help("Specifies the format that the model must output. Compatible with [GPT-4o](/docs/models#gpt-4o), [GPT-4 Turbo](/docs/models#gpt-4-turbo-and-gpt-4), and all GPT-3.5 Turbo models since `gpt-3.5-turbo-1106`.\n\nSetting to `{ \"type\": \"json_schema\", \"json_schema\": {...} }` enables Structured Outputs which ensures the model will match your supplied JSON schema. Learn more in the [Structured Outputs guide](/docs/guides/structured-outputs).\n\nSetting to `{ \"type\": \"json_object\" }` enables JSON mode, which ensures the message the model generates is valid JSON.\n\n**Important:** when using JSON mode, you **must** also instruct the model to produce JSON yourself via a system or user message. Without this, the model may generate an unending stream of whitespace until the generation reaches the token limit, resulting in a long-running and seemingly \"stuck\" request. Also note that the message content may be partially cut off if `finish_reason=\"length\"`, which indicates the generation exceeded `max_tokens` or the conversation exceeded the max context length.\n"),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["threads", "runs", "create"] => handle_threads_runs_create(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_threads_runs_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let path = "/threads/runs".to_string();
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("assistant-id") {
        body.insert("assistant_id".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("thread") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("thread".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("model") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("model".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("instructions") {
        body.insert("instructions".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(vs) = m.get_many::<String>("tools") {
        body.insert("tools".to_string(), serde_json::Value::Array(vs.cloned().map(serde_json::Value::String).collect()));
    }
    if let Some(v) = m.get_one::<String>("tool-resources") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("tool_resources".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("metadata") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("metadata".to_string(), value);
    }
    if let Some(v) = m.get_one::<f64>("temperature") {
        body.insert("temperature".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<f64>("top-p") {
        body.insert("top_p".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<bool>("stream") {
        body.insert("stream".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<i64>("max-prompt-tokens") {
        body.insert("max_prompt_tokens".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<i64>("max-completion-tokens") {
        body.insert("max_completion_tokens".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<String>("truncation-strategy") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("truncation_strategy".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("tool-choice") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("tool_choice".to_string(), value);
    }
    if let Some(v) = m.get_one::<bool>("parallel-tool-calls") {
        body.insert("parallel_tool_calls".to_string(), serde_json::Value::Bool(*v));
    }
    if let Some(v) = m.get_one::<String>("response-format") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("response_format".to_string(), value);
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

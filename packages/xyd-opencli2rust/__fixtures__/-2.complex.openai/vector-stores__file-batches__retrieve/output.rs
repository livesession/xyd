// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("vector-stores")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("file-batches")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("retrieve")
                        .visible_alias("get")
                        .about("Retrieves a vector store file batch.")
                        .arg(
                            Arg::new("vector-store-id")
                                .help("The ID of the vector store that the file batch belongs to.")
                                .required(true),
                        )
                        .arg(
                            Arg::new("batch-id")
                                .help("The ID of the file batch being retrieved.")
                                .required(true),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["vector-stores", "file-batches", "retrieve"] => handle_vector_stores_file_batches_retrieve(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_vector_stores_file_batches_retrieve<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let vector_store_id = m.get_one::<String>("vector-store-id").map(String::as_str).unwrap_or("");
    let batch_id = m.get_one::<String>("batch-id").map(String::as_str).unwrap_or("");
    let path = format!("/vector_stores/{}/file_batches/{}", runtime::path_escape(vector_store_id), runtime::path_escape(batch_id));
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

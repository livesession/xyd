// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("organization")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("projects")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("rate-limits")
                        .subcommand_required(true)
                        .arg_required_else_help(true)
                        .subcommand(
                            Command::new("update")
                                .about("Updates a project rate limit.")
                                .arg(
                                    Arg::new("project-id")
                                        .help("The ID of the project.")
                                        .required(true),
                                )
                                .arg(
                                    Arg::new("rate-limit-id")
                                        .help("The ID of the rate limit.")
                                        .required(true),
                                )
                                .arg(
                                    Arg::new("max-requests-per-1-minute")
                                        .long("max-requests-per-1-minute")
                                        .value_parser(clap::value_parser!(i64))
                                        .help("The maximum requests per minute."),
                                )
                                .arg(
                                    Arg::new("max-tokens-per-1-minute")
                                        .long("max-tokens-per-1-minute")
                                        .value_parser(clap::value_parser!(i64))
                                        .help("The maximum tokens per minute."),
                                )
                                .arg(
                                    Arg::new("max-images-per-1-minute")
                                        .long("max-images-per-1-minute")
                                        .value_parser(clap::value_parser!(i64))
                                        .help("The maximum images per minute. Only relevant for certain models."),
                                )
                                .arg(
                                    Arg::new("max-audio-megabytes-per-1-minute")
                                        .long("max-audio-megabytes-per-1-minute")
                                        .value_parser(clap::value_parser!(i64))
                                        .help("The maximum audio megabytes per minute. Only relevant for certain models."),
                                )
                                .arg(
                                    Arg::new("max-requests-per-1-day")
                                        .long("max-requests-per-1-day")
                                        .value_parser(clap::value_parser!(i64))
                                        .help("The maximum requests per day. Only relevant for certain models."),
                                )
                                .arg(
                                    Arg::new("batch-1-day-max-input-tokens")
                                        .long("batch-1-day-max-input-tokens")
                                        .value_parser(clap::value_parser!(i64))
                                        .help("The maximum batch input tokens per day. Only relevant for certain models."),
                                ),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["organization", "projects", "rate-limits", "update"] => handle_organization_projects_rate_limits_update(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_organization_projects_rate_limits_update<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let project_id = m.get_one::<String>("project-id").map(String::as_str).unwrap_or("");
    let rate_limit_id = m.get_one::<String>("rate-limit-id").map(String::as_str).unwrap_or("");
    let path = format!("/organization/projects/{}/rate_limits/{}", runtime::path_escape(project_id), runtime::path_escape(rate_limit_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<i64>("max-requests-per-1-minute") {
        body.insert("max_requests_per_1_minute".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<i64>("max-tokens-per-1-minute") {
        body.insert("max_tokens_per_1_minute".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<i64>("max-images-per-1-minute") {
        body.insert("max_images_per_1_minute".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<i64>("max-audio-megabytes-per-1-minute") {
        body.insert("max_audio_megabytes_per_1_minute".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<i64>("max-requests-per-1-day") {
        body.insert("max_requests_per_1_day".to_string(), serde_json::Value::from(*v));
    }
    if let Some(v) = m.get_one::<i64>("batch-1-day-max-input-tokens") {
        body.insert("batch_1_day_max_input_tokens".to_string(), serde_json::Value::from(*v));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

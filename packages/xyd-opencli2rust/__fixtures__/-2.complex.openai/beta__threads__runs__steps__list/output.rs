// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("threads")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("runs")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("steps")
                        .subcommand_required(true)
                        .arg_required_else_help(true)
                        .subcommand(
                            Command::new("list")
                                .about("Returns a list of run steps belonging to a run.")
                                .arg(
                                    Arg::new("thread-id")
                                        .help("The ID of the thread the run and run steps belong to.")
                                        .required(true),
                                )
                                .arg(
                                    Arg::new("run-id")
                                        .help("The ID of the run the run steps belong to.")
                                        .required(true),
                                )
                                .arg(
                                    Arg::new("limit")
                                        .long("limit")
                                        .value_parser(clap::value_parser!(i64))
                                        .help("A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.\n"),
                                )
                                .arg(
                                    Arg::new("order")
                                        .long("order")
                                        .help("Sort order by the `created_at` timestamp of the objects. `asc` for ascending order and `desc` for descending order.\n"),
                                )
                                .arg(
                                    Arg::new("after")
                                        .long("after")
                                        .help("A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n"),
                                )
                                .arg(
                                    Arg::new("before")
                                        .long("before")
                                        .help("A cursor for use in pagination. `before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with obj_foo, your subsequent call can include before=obj_foo in order to fetch the previous page of the list.\n"),
                                )
                                .arg(
                                    Arg::new("include[]")
                                        .long("include[]")
                                        .action(clap::ArgAction::Append)
                                        .value_delimiter(',')
                                        .help("A list of additional fields to include in the response. Currently the only supported value is `step_details.tool_calls[*].file_search.results[*].content` to fetch the file search result content.\n\nSee the [file search tool documentation](/docs/assistants/tools/file-search#customizing-file-search-settings) for more information.\n"),
                                ),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["threads", "runs", "steps", "list"] => handle_threads_runs_steps_list(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_threads_runs_steps_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let thread_id = m.get_one::<String>("thread-id").map(String::as_str).unwrap_or("");
    let run_id = m.get_one::<String>("run-id").map(String::as_str).unwrap_or("");
    let path = format!("/threads/{}/runs/{}/steps", runtime::path_escape(thread_id), runtime::path_escape(run_id));
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<i64>("limit") {
        query.push(("limit", v.to_string()));
    }
    if let Some(v) = m.get_one::<String>("order") {
        query.push(("order", v.clone()));
    }
    if let Some(v) = m.get_one::<String>("after") {
        query.push(("after", v.clone()));
    }
    if let Some(v) = m.get_one::<String>("before") {
        query.push(("before", v.clone()));
    }
    if let Some(vs) = m.get_many::<String>("include[]") {
        query.push(("include[]", vs.cloned().collect::<Vec<_>>().join(",")));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

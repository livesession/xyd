// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("organization")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("projects")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("groups")
                        .subcommand_required(true)
                        .arg_required_else_help(true)
                        .subcommand(
                            Command::new("list")
                                .about("Lists the groups that have access to a project.")
                                .arg(
                                    Arg::new("project-id")
                                        .help("The ID of the project to inspect.")
                                        .required(true),
                                )
                                .arg(
                                    Arg::new("limit")
                                        .long("limit")
                                        .value_parser(clap::value_parser!(i64))
                                        .help("A limit on the number of project groups to return. Defaults to 20."),
                                )
                                .arg(
                                    Arg::new("after")
                                        .long("after")
                                        .help("Cursor for pagination. Provide the ID of the last group from the previous response to fetch the next page."),
                                )
                                .arg(
                                    Arg::new("order")
                                        .long("order")
                                        .help("Sort order for the returned groups."),
                                ),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["organization", "projects", "groups", "list"] => handle_organization_projects_groups_list(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_organization_projects_groups_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let project_id = m.get_one::<String>("project-id").map(String::as_str).unwrap_or("");
    let path = format!("/organization/projects/{}/groups", runtime::path_escape(project_id));
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<i64>("limit") {
        query.push(("limit", v.to_string()));
    }
    if let Some(v) = m.get_one::<String>("after") {
        query.push(("after", v.clone()));
    }
    if let Some(v) = m.get_one::<String>("order") {
        query.push(("order", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

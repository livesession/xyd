// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("vector-stores")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("files")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("create")
                        .about("Create a vector store file by attaching a [File](/docs/api-reference/files) to a [vector store](/docs/api-reference/vector-stores/object).")
                        .arg(
                            Arg::new("vector-store-id")
                                .help("The ID of the vector store for which to create a File.\n")
                                .required(true),
                        )
                        .arg(
                            Arg::new("file-id")
                                .long("file-id")
                                .help("A [File](/docs/api-reference/files) ID that the vector store should use. Useful for tools like `file_search` that can access files. For multi-file ingestion, we recommend [`file_batches`](/docs/api-reference/vector-stores-file-batches/createBatch) to minimize per-vector-store write requests.")
                                .required(true),
                        )
                        .arg(
                            Arg::new("chunking-strategy")
                                .long("chunking-strategy")
                                .help("The chunking strategy used to chunk the file(s). If not set, will use the `auto` strategy."),
                        )
                        .arg(
                            Arg::new("attributes")
                                .long("attributes"),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["vector-stores", "files", "create"] => handle_vector_stores_files_create(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_vector_stores_files_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let vector_store_id = m.get_one::<String>("vector-store-id").map(String::as_str).unwrap_or("");
    let path = format!("/vector_stores/{}/files", runtime::path_escape(vector_store_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("file-id") {
        body.insert("file_id".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<String>("chunking-strategy") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("chunking_strategy".to_string(), value);
    }
    if let Some(v) = m.get_one::<String>("attributes") {
        let value = serde_json::from_str::<serde_json::Value>(v)
            .unwrap_or_else(|_| serde_json::Value::String(v.clone()));
        body.insert("attributes".to_string(), value);
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

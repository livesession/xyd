// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("fine-tuning")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("checkpoints")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("permissions")
                        .subcommand_required(true)
                        .arg_required_else_help(true)
                        .subcommand(
                            Command::new("delete")
                                .about("**NOTE:** This endpoint requires an [admin API key](../admin-api-keys).\n\nOrganization owners can use this endpoint to delete a permission for a fine-tuned model checkpoint.\n")
                                .arg(
                                    Arg::new("fine-tuned-model-checkpoint")
                                        .help("The ID of the fine-tuned model checkpoint to delete a permission for.\n")
                                        .required(true),
                                )
                                .arg(
                                    Arg::new("permission-id")
                                        .help("The ID of the fine-tuned model checkpoint permission to delete.\n")
                                        .required(true),
                                ),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["fine-tuning", "checkpoints", "permissions", "delete"] => handle_fine_tuning_checkpoints_permissions_delete(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_fine_tuning_checkpoints_permissions_delete<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let fine_tuned_model_checkpoint = m.get_one::<String>("fine-tuned-model-checkpoint").map(String::as_str).unwrap_or("");
    let permission_id = m.get_one::<String>("permission-id").map(String::as_str).unwrap_or("");
    let path = format!("/fine_tuning/checkpoints/{}/permissions/{}", runtime::path_escape(fine_tuned_model_checkpoint), runtime::path_escape(permission_id));
    let req = runtime::Request {
        method: "DELETE",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

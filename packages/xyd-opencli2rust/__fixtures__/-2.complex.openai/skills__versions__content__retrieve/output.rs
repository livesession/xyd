// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("skills")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("versions")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("content")
                        .subcommand_required(true)
                        .arg_required_else_help(true)
                        .subcommand(
                            Command::new("list")
                                .about("Download a skill version zip bundle.")
                                .arg(
                                    Arg::new("skill-id")
                                        .help("The identifier of the skill.")
                                        .required(true),
                                )
                                .arg(
                                    Arg::new("version")
                                        .help("The skill version number.")
                                        .required(true),
                                ),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["skills", "versions", "content", "list"] => handle_skills_versions_content_list(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_skills_versions_content_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let skill_id = m.get_one::<String>("skill-id").map(String::as_str).unwrap_or("");
    let version = m.get_one::<String>("version").map(String::as_str).unwrap_or("");
    let path = format!("/skills/{}/versions/{}/content", runtime::path_escape(skill_id), runtime::path_escape(version));
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("chat")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("completions")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("messages")
                        .subcommand_required(true)
                        .arg_required_else_help(true)
                        .subcommand(
                            Command::new("list")
                                .about("Get the messages in a stored chat completion. Only Chat Completions that\nhave been created with the `store` parameter set to `true` will be\nreturned.\n")
                                .arg(
                                    Arg::new("completion-id")
                                        .help("The ID of the chat completion to retrieve messages from.")
                                        .required(true),
                                )
                                .arg(
                                    Arg::new("after")
                                        .long("after")
                                        .help("Identifier for the last message from the previous pagination request."),
                                )
                                .arg(
                                    Arg::new("limit")
                                        .long("limit")
                                        .value_parser(clap::value_parser!(i64))
                                        .help("Number of messages to retrieve."),
                                )
                                .arg(
                                    Arg::new("order")
                                        .long("order")
                                        .help("Sort order for messages by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `asc`."),
                                ),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["chat", "completions", "messages", "list"] => handle_chat_completions_messages_list(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_chat_completions_messages_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let completion_id = m.get_one::<String>("completion-id").map(String::as_str).unwrap_or("");
    let path = format!("/chat/completions/{}/messages", runtime::path_escape(completion_id));
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<String>("after") {
        query.push(("after", v.clone()));
    }
    if let Some(v) = m.get_one::<i64>("limit") {
        query.push(("limit", v.to_string()));
    }
    if let Some(v) = m.get_one::<String>("order") {
        query.push(("order", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

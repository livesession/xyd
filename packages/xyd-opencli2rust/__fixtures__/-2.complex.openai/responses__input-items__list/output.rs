// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("responses")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("input-items")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("list")
                        .about("Returns a list of input items for a given response.")
                        .arg(
                            Arg::new("response-id")
                                .help("The ID of the response to retrieve input items for.")
                                .required(true),
                        )
                        .arg(
                            Arg::new("limit")
                                .long("limit")
                                .value_parser(clap::value_parser!(i64))
                                .help("A limit on the number of objects to be returned. Limit can range between\n1 and 100, and the default is 20.\n"),
                        )
                        .arg(
                            Arg::new("order")
                                .long("order")
                                .help("The order to return the input items in. Default is `desc`.\n- `asc`: Return the input items in ascending order.\n- `desc`: Return the input items in descending order.\n"),
                        )
                        .arg(
                            Arg::new("after")
                                .long("after")
                                .help("An item ID to list items after, used in pagination.\n"),
                        )
                        .arg(
                            Arg::new("include")
                                .long("include")
                                .action(clap::ArgAction::Append)
                                .value_delimiter(',')
                                .help("Additional fields to include in the response. See the `include`\nparameter for Response creation above for more information.\n"),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["responses", "input-items", "list"] => handle_responses_input_items_list(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_responses_input_items_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let response_id = m.get_one::<String>("response-id").map(String::as_str).unwrap_or("");
    let path = format!("/responses/{}/input_items", runtime::path_escape(response_id));
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<i64>("limit") {
        query.push(("limit", v.to_string()));
    }
    if let Some(v) = m.get_one::<String>("order") {
        query.push(("order", v.clone()));
    }
    if let Some(v) = m.get_one::<String>("after") {
        query.push(("after", v.clone()));
    }
    if let Some(vs) = m.get_many::<String>("include") {
        query.push(("include", vs.cloned().collect::<Vec<_>>().join(",")));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

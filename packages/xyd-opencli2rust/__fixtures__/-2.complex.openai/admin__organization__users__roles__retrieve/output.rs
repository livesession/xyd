// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("organization")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("users")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("roles")
                        .subcommand_required(true)
                        .arg_required_else_help(true)
                        .subcommand(
                            Command::new("list")
                                .about("Lists the organization roles assigned to a user within the organization.")
                                .arg(
                                    Arg::new("user-id")
                                        .help("The ID of the user to inspect.")
                                        .required(true),
                                )
                                .arg(
                                    Arg::new("limit")
                                        .long("limit")
                                        .value_parser(clap::value_parser!(i64))
                                        .help("A limit on the number of organization role assignments to return."),
                                )
                                .arg(
                                    Arg::new("after")
                                        .long("after")
                                        .help("Cursor for pagination. Provide the value from the previous response's `next` field to continue listing organization roles."),
                                )
                                .arg(
                                    Arg::new("order")
                                        .long("order")
                                        .help("Sort order for the returned organization roles."),
                                ),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["organization", "users", "roles", "list"] => handle_organization_users_roles_list(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_organization_users_roles_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let user_id = m.get_one::<String>("user-id").map(String::as_str).unwrap_or("");
    let path = format!("/organization/users/{}/roles", runtime::path_escape(user_id));
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<i64>("limit") {
        query.push(("limit", v.to_string()));
    }
    if let Some(v) = m.get_one::<String>("after") {
        query.push(("after", v.clone()));
    }
    if let Some(v) = m.get_one::<String>("order") {
        query.push(("order", v.clone()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

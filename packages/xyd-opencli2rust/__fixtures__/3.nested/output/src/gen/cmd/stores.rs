// Code generated by opencli2rust. DO NOT EDIT.

use std::process::ExitCode;

use clap::{Arg, ArgMatches, Command};

use crate::gen::runtime::{self, CliOverrides, Context};

pub fn command() -> Command {
    Command::new("stores")
        .subcommand_required(true)
        .arg_required_else_help(true)
        .subcommand(
            Command::new("items")
                .subcommand_required(true)
                .arg_required_else_help(true)
                .subcommand(
                    Command::new("list")
                        .about("List items in a store.")
                        .arg(
                            Arg::new("store-id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("limit")
                                .long("limit")
                                .value_parser(clap::value_parser!(i64)),
                        ),
                )
                .subcommand(
                    Command::new("create")
                        .about("Create an item in a store.")
                        .arg(
                            Arg::new("store-id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("sku")
                                .long("sku")
                                .required(true),
                        )
                        .arg(
                            Arg::new("quantity")
                                .long("quantity")
                                .value_parser(clap::value_parser!(i64)),
                        ),
                )
                .subcommand(
                    Command::new("retrieve")
                        .visible_alias("get")
                        .about("Retrieve a store item.")
                        .arg(
                            Arg::new("store-id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("item-id")
                                .required(true),
                        ),
                )
                .subcommand(
                    Command::new("archive")
                        .about("Archive a store item.")
                        .arg(
                            Arg::new("store-id")
                                .required(true),
                        )
                        .arg(
                            Arg::new("item-id")
                                .required(true),
                        ),
                ),
        )
}

pub async fn run<O: CliOverrides>(ctx: &Context, o: &O, cmd_path: &[String], m: &ArgMatches) -> ExitCode {
    let parts: Vec<&str> = cmd_path.iter().map(String::as_str).collect();
    match parts.as_slice() {
        ["stores", "items", "list"] => handle_stores_items_list(ctx, o, cmd_path, m).await,
        ["stores", "items", "create"] => handle_stores_items_create(ctx, o, cmd_path, m).await,
        ["stores", "items", "retrieve"] => handle_stores_items_retrieve(ctx, o, cmd_path, m).await,
        ["stores", "items", "archive"] => handle_stores_items_archive(ctx, o, cmd_path, m).await,
        _ => {
            eprintln!("error: unknown command: {}", cmd_path.join(" "));
            ExitCode::FAILURE
        }
    }
}

async fn handle_stores_items_list<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let store_id = m.get_one::<String>("store-id").map(String::as_str).unwrap_or("");
    let path = format!("/stores/{}/items", runtime::path_escape(store_id));
    let mut query: Vec<(&'static str, String)> = Vec::new();
    if let Some(v) = m.get_one::<i64>("limit") {
        query.push(("limit", v.to_string()));
    }
    let req = runtime::Request {
        method: "GET",
        path,
        query,
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_stores_items_create<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let store_id = m.get_one::<String>("store-id").map(String::as_str).unwrap_or("");
    let path = format!("/stores/{}/items", runtime::path_escape(store_id));
    let mut body = serde_json::Map::new();
    if let Some(v) = m.get_one::<String>("sku") {
        body.insert("sku".to_string(), serde_json::Value::String(v.clone()));
    }
    if let Some(v) = m.get_one::<i64>("quantity") {
        body.insert("quantity".to_string(), serde_json::Value::from(*v));
    }
    let body_bytes = match serde_json::to_vec(&serde_json::Value::Object(body)) {
        Ok(bytes) => bytes,
        Err(err) => return o.print_error(cmd_path, &runtime::Error::Invalid(err.to_string())),
    };
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: Some(body_bytes),
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_stores_items_retrieve<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let store_id = m.get_one::<String>("store-id").map(String::as_str).unwrap_or("");
    let item_id = m.get_one::<String>("item-id").map(String::as_str).unwrap_or("");
    let path = format!("/stores/{}/items/{}", runtime::path_escape(store_id), runtime::path_escape(item_id));
    let req = runtime::Request {
        method: "GET",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

async fn handle_stores_items_archive<O: CliOverrides>(
    ctx: &Context,
    o: &O,
    cmd_path: &[String],
    m: &ArgMatches,
) -> ExitCode {
    let store_id = m.get_one::<String>("store-id").map(String::as_str).unwrap_or("");
    let item_id = m.get_one::<String>("item-id").map(String::as_str).unwrap_or("");
    let path = format!("/stores/{}/items/{}/archive", runtime::path_escape(store_id), runtime::path_escape(item_id));
    let req = runtime::Request {
        method: "POST",
        path,
        query: Vec::new(),
        headers: Vec::new(),
        body: None,
    };
    runtime::run_request(ctx, o, cmd_path, req).await
}

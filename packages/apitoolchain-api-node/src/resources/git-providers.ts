// Code generated by opensdk. DO NOT EDIT.

import { APIResource } from '../core/resource';
import type { APIClient, RequestOptions } from '../core/request';
import type { ApitoolchainGitProvider, ApitoolchainGitProviderKind, ApitoolchainGitRepoOption } from '../models';

export class GitProviders extends APIResource {
  readonly branches: GitProvidersBranches;

  readonly repos: GitProvidersRepos;

  constructor(client: APIClient) {
    super(client);
    this.branches = new GitProvidersBranches(client);
    this.repos = new GitProvidersRepos(client);
  }

  list(options?: RequestOptions): Promise<ApitoolchainGitProvider[]> {
    return this._client.request<ApitoolchainGitProvider[]>({ method: "GET", path: "/git-providers" }, options);
  }

  create(body: GitProviderCreateParams, options?: RequestOptions): Promise<ApitoolchainGitProvider> {
    return this._client.request<ApitoolchainGitProvider>({ method: "POST", path: "/git-providers", body }, options);
  }

  delete(id: string, options?: RequestOptions): Promise<void> {
    if (!id) {
      throw new Error("missing required id parameter");
    }
    return this._client.request<void>({ method: "DELETE", path: `/git-providers/${id}` }, options);
  }
}

export class GitProvidersBranches extends APIResource {
  /** Branch names of one repo (for the branch picker after a repo is chosen). */
  list(id: string, query: GitProviderBranchListParams, options?: RequestOptions): Promise<string[]> {
    if (!id) {
      throw new Error("missing required id parameter");
    }
    return this._client.request<string[]>({ method: "GET", path: `/git-providers/${id}/branches`, query }, options);
  }
}

export class GitProvidersRepos extends APIResource {
  /** Repos the provider's token can access (for the connect picker). */
  list(id: string, options?: RequestOptions): Promise<ApitoolchainGitRepoOption[]> {
    if (!id) {
      throw new Error("missing required id parameter");
    }
    return this._client.request<ApitoolchainGitRepoOption[]>({ method: "GET", path: `/git-providers/${id}/repos` }, options);
  }
}

export interface GitProviderCreateParams {
  kind: ApitoolchainGitProviderKind;
  name?: string;
  baseUrl?: string;
  token: string;
}

export interface GitProviderBranchListParams {
  repo: string;
}

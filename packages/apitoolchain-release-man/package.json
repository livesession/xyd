{
  "name": "@apitoolchain/release-man",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure release-management logic for apitoolchain: spec-diff → semver bump, changelog / PR-body / RELEASES.md rendering, and the .apitoolchain/release.json manifest. Zero DB/HTTP deps — consumed by @apitoolchain/api via a file: dep.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "format": "bunx biome check --write ."
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.2"
  }
}

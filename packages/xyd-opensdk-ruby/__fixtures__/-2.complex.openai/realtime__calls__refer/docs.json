{
  "chain": [
    "realtime",
    "calls"
  ],
  "action": "refer",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nclient.realtime.calls.refer(\"call_id\", target_uri: \"string\")\n",
  "typeReference": {
    "signature": "client.realtime.calls.refer(call_id, target_uri:) -> nil",
    "request": {
      "fields": [
        {
          "name": "target_uri",
          "langType": "String",
          "required": true,
          "description": "URI that should appear in the SIP Refer-To header. Supports values like\n`tel:+14155550123` or `sip:agent@example.com`."
        }
      ]
    },
    "response": {
      "langType": "nil",
      "note": "no response body"
    }
  }
}

{
  "chain": [
    "admin",
    "organization",
    "groups"
  ],
  "action": "list",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.admin.organization.groups.list(limit: 100, after: \"after\", order: \"asc\")\npp result\n",
  "typeReference": {
    "signature": "client.admin.organization.groups.list(limit: nil, after: nil, order: nil) -> Page<Models::GroupResponse>",
    "request": {
      "fields": [
        {
          "name": "limit",
          "langType": "Integer",
          "required": false,
          "description": "A limit on the number of groups to be returned. Limit can range between 0 and 1000, and the default is 100.\n"
        },
        {
          "name": "after",
          "langType": "String",
          "required": false,
          "description": "A cursor for use in pagination. `after` is a group ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with group_abc, your subsequent call can include `after=group_abc` in order to fetch the next page of the list.\n"
        },
        {
          "name": "order",
          "langType": "Models::AdminOrganizationGroupsListOrder",
          "required": false,
          "description": "Specifies the sort order of the returned groups.",
          "refTypeName": "AdminOrganizationGroupsListOrder"
        }
      ]
    },
    "response": {
      "typeName": "Page<Models::GroupResponse>",
      "note": "paginated (Page)",
      "fields": [
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "Identifier for the group."
        },
        {
          "name": "name",
          "langType": "String",
          "required": true,
          "description": "Display name of the group."
        },
        {
          "name": "created_at",
          "langType": "Integer",
          "required": true,
          "description": "Unix timestamp (in seconds) when the group was created."
        },
        {
          "name": "is_scim_managed",
          "langType": "Boolean",
          "required": true,
          "description": "Whether the group is managed through SCIM and controlled by your identity provider."
        },
        {
          "name": "group_type",
          "langType": "String",
          "required": true,
          "description": "The type of the group."
        }
      ]
    }
  }
}

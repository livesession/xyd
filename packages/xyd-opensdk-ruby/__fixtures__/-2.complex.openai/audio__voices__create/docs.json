{
  "chain": [
    "audio",
    "voices"
  ],
  "action": "create",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.audio.voices.create(name: \"string\", audio_sample: StringIO.new(\"Example data\"), consent: \"string\")\npp result\n",
  "typeReference": {
    "signature": "client.audio.voices.create(name:, audio_sample:, consent:) -> Models::VoiceResource",
    "request": {
      "fields": [
        {
          "name": "name",
          "langType": "String",
          "required": true,
          "description": "The name of the new voice."
        },
        {
          "name": "audio_sample",
          "langType": "IO, String",
          "required": true,
          "description": "The sample audio recording file. Maximum size is 10 MiB.\n\nSupported MIME types:\n`audio/mpeg`, `audio/wav`, `audio/x-wav`, `audio/ogg`, `audio/aac`, `audio/flac`, `audio/webm`, `audio/mp4`.\n"
        },
        {
          "name": "consent",
          "langType": "String",
          "required": true,
          "description": "The consent recording ID (for example, `cons_1234`)."
        }
      ]
    },
    "response": {
      "typeName": "Models::VoiceResource",
      "fields": [
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "The object type, which is always `audio.voice`."
        },
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "The voice identifier, which can be referenced in API endpoints."
        },
        {
          "name": "name",
          "langType": "String",
          "required": true,
          "description": "The name of the voice."
        },
        {
          "name": "created_at",
          "langType": "Integer",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the voice was created."
        }
      ]
    }
  }
}

{
  "chain": [
    "vector-stores",
    "file-batches"
  ],
  "action": "create",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.vector_stores.file_batches.create(\"vs_abc123\")\npp result\n",
  "typeReference": {
    "signature": "client.vector_stores.file_batches.create(vector_store_id) -> Models::VectorStoreFileBatchObject",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "Models::VectorStoreFileBatchObject",
      "fields": [
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints."
        },
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "The object type, which is always `vector_store.file_batch`."
        },
        {
          "name": "created_at",
          "langType": "Integer",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the vector store files batch was created."
        },
        {
          "name": "vector_store_id",
          "langType": "String",
          "required": true,
          "description": "The ID of the [vector store](/docs/api-reference/vector-stores/object) that the [File](/docs/api-reference/files) is attached to."
        },
        {
          "name": "status",
          "langType": "Models::VectorStoreFileBatchObjectStatus",
          "required": true,
          "description": "The status of the vector store files batch, which can be either `in_progress`, `completed`, `cancelled` or `failed`.",
          "refTypeName": "VectorStoreFileBatchObjectStatus"
        },
        {
          "name": "file_counts",
          "langType": "Models::VectorStoreFileBatchObjectFileCounts",
          "required": true,
          "refTypeName": "VectorStoreFileBatchObjectFileCounts"
        }
      ]
    }
  }
}

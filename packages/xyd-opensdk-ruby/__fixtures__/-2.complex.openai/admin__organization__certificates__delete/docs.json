{
  "chain": [
    "admin",
    "organization",
    "certificates"
  ],
  "action": "delete",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.admin.organization.certificates.delete(\"certificate_id\")\npp result\n",
  "typeReference": {
    "signature": "client.admin.organization.certificates.delete(certificate_id) -> Models::DeleteCertificateResponse",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "Models::DeleteCertificateResponse",
      "fields": [
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "The object type, must be `certificate.deleted`."
        },
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "The ID of the certificate that was deleted."
        }
      ]
    }
  }
}

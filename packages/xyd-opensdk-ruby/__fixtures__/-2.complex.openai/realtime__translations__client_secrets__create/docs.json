{
  "chain": [
    "realtime",
    "translations",
    "client-secrets"
  ],
  "action": "create",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.realtime.translations.client_secrets.create(session: { \"model\" => \"string\", \"audio\" => { \"input\" => { \"transcription\" => { \"model\" => \"string\" }, \"noise_reduction\" => { \"type\" => \"near_field\" } }, \"output\" => { \"language\" => \"string\" } } }, expires_after: { \"anchor\" => \"created_at\", \"seconds\" => 600 })\npp result\n",
  "typeReference": {
    "signature": "client.realtime.translations.client_secrets.create(expires_after: nil, session:) -> Models::RealtimeTranslationClientSecretCreateResponse",
    "request": {
      "fields": [
        {
          "name": "expires_after",
          "langType": "Models::RealtimeTranslationClientSecretCreateRequestExpiresAfter",
          "required": false,
          "description": "Configuration for the client secret expiration. Expiration refers to the time after which\na client secret will no longer be valid for creating sessions. The session itself may\ncontinue after that time once started. A secret can be used to create multiple sessions\nuntil it expires.\n",
          "refTypeName": "RealtimeTranslationClientSecretCreateRequestExpiresAfter"
        },
        {
          "name": "session",
          "langType": "Models::RealtimeTranslationSessionCreateRequest",
          "required": true,
          "refTypeName": "RealtimeTranslationSessionCreateRequest"
        }
      ]
    },
    "response": {
      "typeName": "Models::RealtimeTranslationClientSecretCreateResponse",
      "fields": [
        {
          "name": "value",
          "langType": "String",
          "required": true,
          "description": "The generated client secret value."
        },
        {
          "name": "expires_at",
          "langType": "Integer",
          "required": true,
          "description": "Expiration timestamp for the client secret, in seconds since epoch."
        },
        {
          "name": "session",
          "langType": "Models::RealtimeTranslationSession",
          "required": true,
          "refTypeName": "RealtimeTranslationSession"
        }
      ]
    }
  }
}

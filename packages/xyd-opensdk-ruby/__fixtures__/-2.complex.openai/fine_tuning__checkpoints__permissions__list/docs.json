{
  "chain": [
    "fine-tuning",
    "checkpoints",
    "permissions"
  ],
  "action": "list",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.fine_tuning.checkpoints.permissions.list(\"ft-AF1WoRqd3aJAHsqc9NY7iL8F\", project_id: \"project_id\", after: \"after\", limit: 10, order: \"ascending\")\npp result\n",
  "typeReference": {
    "signature": "client.fine_tuning.checkpoints.permissions.list(fine_tuned_model_checkpoint, project_id: nil, after: nil, limit: nil, order: nil) -> Page<Models::FineTuningCheckpointPermission>",
    "request": {
      "fields": [
        {
          "name": "project_id",
          "langType": "String",
          "required": false,
          "description": "The ID of the project to get permissions for."
        },
        {
          "name": "after",
          "langType": "String",
          "required": false,
          "description": "Identifier for the last permission ID from the previous pagination request."
        },
        {
          "name": "limit",
          "langType": "Integer",
          "required": false,
          "description": "Number of permissions to retrieve."
        },
        {
          "name": "order",
          "langType": "Models::FineTuningCheckpointsPermissionsListOrder",
          "required": false,
          "description": "The order in which to retrieve permissions.",
          "refTypeName": "FineTuningCheckpointsPermissionsListOrder"
        }
      ]
    },
    "response": {
      "typeName": "Page<Models::FineTuningCheckpointPermission>",
      "note": "paginated (Page)",
      "fields": [
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "The permission identifier, which can be referenced in the API endpoints."
        },
        {
          "name": "created_at",
          "langType": "Integer",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the permission was created."
        },
        {
          "name": "project_id",
          "langType": "String",
          "required": true,
          "description": "The project identifier that the permission is for."
        },
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "The object type, which is always \"checkpoint.permission\"."
        }
      ]
    }
  }
}

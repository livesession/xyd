{
  "chain": [
    "admin",
    "organization",
    "projects",
    "users"
  ],
  "action": "retrieve",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.admin.organization.projects.users.retrieve(\"project_id\", \"user_id\")\npp result\n",
  "typeReference": {
    "signature": "client.admin.organization.projects.users.retrieve(project_id, user_id) -> Models::ProjectUser",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "Models::ProjectUser",
      "fields": [
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "The object type, which is always `organization.project.user`"
        },
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "name",
          "langType": "String",
          "required": false,
          "description": "The name of the user"
        },
        {
          "name": "email",
          "langType": "String",
          "required": false,
          "description": "The email address of the user"
        },
        {
          "name": "role",
          "langType": "String",
          "required": true,
          "description": "`owner` or `member`"
        },
        {
          "name": "added_at",
          "langType": "Integer",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the project was added."
        }
      ]
    }
  }
}

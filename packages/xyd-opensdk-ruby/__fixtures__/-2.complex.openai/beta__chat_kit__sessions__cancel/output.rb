# Code generated by opensdk. DO NOT EDIT.

module Openai
  module Resources
    class Beta
      attr_reader :chat_kit

      # @api private
      def initialize(transport)
        @transport = transport
        @chat_kit = BetaChatKit.new(transport)
      end
    end

    class BetaChatKit
      attr_reader :sessions

      # @api private
      def initialize(transport)
        @transport = transport
        @sessions = BetaChatKitSessions.new(transport)
      end
    end

    class BetaChatKitSessions
      # @api private
      def initialize(transport)
        @transport = transport
      end

      # Cancel an active ChatKit session and return its most recent metadata.
      #
      # Cancelling prevents new requests from using the issued client secret.
      #
      # @param session_id [String]
      # @return [Models::ChatSessionResource]
      def cancel(session_id)
        raise ArgumentError, "Expected a non-empty value for `session_id`" if session_id.nil? || session_id.to_s.empty?

        @transport.request(
          method: :post,
          path: "/chatkit/sessions/#{session_id}/cancel"
        )
      end
    end
  end
end

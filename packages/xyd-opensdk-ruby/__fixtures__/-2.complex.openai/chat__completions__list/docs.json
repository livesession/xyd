{
  "chain": [
    "chat",
    "completions"
  ],
  "action": "list",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.chat.completions.list(model: \"model\", metadata: { \"key\" => \"string\" }, after: \"after\", limit: 20, order: \"asc\")\npp result\n",
  "typeReference": {
    "signature": "client.chat.completions.list(model: nil, metadata: nil, after: nil, limit: nil, order: nil) -> Page<Models::CreateChatCompletionResponse>",
    "request": {
      "fields": [
        {
          "name": "model",
          "langType": "String",
          "required": false,
          "description": "The model used to generate the Chat Completions."
        },
        {
          "name": "metadata",
          "langType": "Models::Metadata",
          "required": false,
          "description": "A list of metadata keys to filter the Chat Completions by. Example:\n\n`metadata[key1]=value1&metadata[key2]=value2`\n",
          "refTypeName": "Metadata"
        },
        {
          "name": "after",
          "langType": "String",
          "required": false,
          "description": "Identifier for the last chat completion from the previous pagination request."
        },
        {
          "name": "limit",
          "langType": "Integer",
          "required": false,
          "description": "Number of Chat Completions to retrieve."
        },
        {
          "name": "order",
          "langType": "Models::ChatCompletionsListOrder",
          "required": false,
          "description": "Sort order for Chat Completions by timestamp. Use `asc` for ascending order or `desc` for descending order. Defaults to `asc`.",
          "refTypeName": "ChatCompletionsListOrder"
        }
      ]
    },
    "response": {
      "typeName": "Page<Models::CreateChatCompletionResponse>",
      "note": "paginated (Page)",
      "fields": [
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "A unique identifier for the chat completion."
        },
        {
          "name": "choices",
          "langType": "Array<Models::CreateChatCompletionResponseChoicesItem>",
          "required": true,
          "description": "A list of chat completion choices. Can be more than one if `n` is greater than 1."
        },
        {
          "name": "created",
          "langType": "Integer",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the chat completion was created."
        },
        {
          "name": "model",
          "langType": "String",
          "required": true,
          "description": "The model used for the chat completion."
        },
        {
          "name": "service_tier",
          "langType": "Models::ServiceTier",
          "required": false,
          "refTypeName": "ServiceTier"
        },
        {
          "name": "system_fingerprint",
          "langType": "String",
          "required": false,
          "description": "This fingerprint represents the backend configuration that the model runs with.\n\nCan be used in conjunction with the `seed` request parameter to understand when backend changes have been made that might impact determinism.\n",
          "deprecated": true
        },
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "The object type, which is always `chat.completion`."
        },
        {
          "name": "usage",
          "langType": "Models::CompletionUsage",
          "required": false,
          "refTypeName": "CompletionUsage"
        }
      ]
    }
  }
}

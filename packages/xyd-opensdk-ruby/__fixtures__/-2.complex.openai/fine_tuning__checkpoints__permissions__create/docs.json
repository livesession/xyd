{
  "chain": [
    "fine-tuning",
    "checkpoints",
    "permissions"
  ],
  "action": "create",
  "usage": "require \"openai\"\n\nclient = Openai::Client.new(api_key: ENV[\"OPENAI_API_KEY\"])\n\nresult = client.fine_tuning.checkpoints.permissions.create(\"ft:gpt-4o-mini-2024-07-18:org:weather:B7R9VjQd\", project_ids: [\"string\"])\npp result\n",
  "typeReference": {
    "signature": "client.fine_tuning.checkpoints.permissions.create(fine_tuned_model_checkpoint, project_ids:) -> Page<Models::FineTuningCheckpointPermission>",
    "request": {
      "fields": [
        {
          "name": "project_ids",
          "langType": "Array<String>",
          "required": true,
          "description": "The project identifiers to grant access to."
        }
      ]
    },
    "response": {
      "typeName": "Page<Models::FineTuningCheckpointPermission>",
      "note": "paginated (Page)",
      "fields": [
        {
          "name": "id",
          "langType": "String",
          "required": true,
          "description": "The permission identifier, which can be referenced in the API endpoints."
        },
        {
          "name": "created_at",
          "langType": "Integer",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the permission was created."
        },
        {
          "name": "project_id",
          "langType": "String",
          "required": true,
          "description": "The project identifier that the permission is for."
        },
        {
          "name": "object",
          "langType": "String",
          "required": true,
          "description": "The object type, which is always \"checkpoint.permission\"."
        }
      ]
    }
  }
}

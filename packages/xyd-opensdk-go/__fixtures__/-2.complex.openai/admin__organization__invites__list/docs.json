{
  "chain": [
    "admin",
    "organization",
    "invites"
  ],
  "action": "list",
  "usage": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\n\t\"github.com/example/openai\"\n\t\"github.com/example/openai/option\"\n\t\"github.com/example/openai/packages/param\"\n)\n\nfunc main() {\n\tclient := openai.NewClient(\n\t\toption.WithAPIKey(\"My API Key\"),\n\t)\n\tresult, err := client.Admin.Organization.Invites.List(\n\t\tcontext.TODO(),\n\t\topenai.AdminOrganizationInviteListParams{\n\t\t\tLimit: param.NewOpt[int64](20),\n\t\t\tAfter: param.NewOpt[string](\"after\"),\n\t\t},\n\t)\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf(\"%+v\\n\", result)\n}\n",
  "typeReference": {
    "signature": "client.Admin.Organization.Invites.List(ctx, query) (*pagination.CursorPage[Invite], error)",
    "request": {
      "typeName": "AdminOrganizationInviteListParams",
      "argName": "query",
      "fields": [
        {
          "name": "Limit",
          "langType": "param.Opt[int64]",
          "required": false,
          "description": "A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 20.\n"
        },
        {
          "name": "After",
          "langType": "param.Opt[string]",
          "required": false,
          "description": "A cursor for use in pagination. `after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with obj_foo, your subsequent call can include after=obj_foo in order to fetch the next page of the list.\n"
        }
      ]
    },
    "response": {
      "typeName": "Invite",
      "note": "paginated (pagination.CursorPage)",
      "fields": [
        {
          "name": "Object",
          "langType": "string",
          "required": true,
          "description": "The object type, which is always `organization.invite`"
        },
        {
          "name": "ID",
          "langType": "string",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints"
        },
        {
          "name": "Email",
          "langType": "string",
          "required": true,
          "description": "The email address of the individual to whom the invite was sent"
        },
        {
          "name": "Role",
          "langType": "InviteRole",
          "required": true,
          "description": "`owner` or `reader`",
          "refTypeName": "InviteRole"
        },
        {
          "name": "Status",
          "langType": "InviteStatus",
          "required": true,
          "description": "`accepted`,`expired`, or `pending`",
          "refTypeName": "InviteStatus"
        },
        {
          "name": "CreatedAt",
          "langType": "int64",
          "required": true,
          "description": "The Unix timestamp (in seconds) of when the invite was sent."
        },
        {
          "name": "ExpiresAt",
          "langType": "int64",
          "required": false,
          "description": "The Unix timestamp (in seconds) of when the invite expires."
        },
        {
          "name": "AcceptedAt",
          "langType": "int64",
          "required": false,
          "description": "The Unix timestamp (in seconds) of when the invite was accepted."
        },
        {
          "name": "Projects",
          "langType": "[]InviteProjectsItem",
          "required": true,
          "description": "The projects that were granted membership upon acceptance of the invite."
        }
      ]
    }
  }
}

{
  "chain": [
    "vector-stores"
  ],
  "action": "retrieve",
  "usage": "package main\n\nimport (\n\t\"context\"\n\t\"fmt\"\n\n\t\"github.com/example/openai\"\n\t\"github.com/example/openai/option\"\n)\n\nfunc main() {\n\tclient := openai.NewClient(\n\t\toption.WithAPIKey(\"My API Key\"),\n\t)\n\tresult, err := client.VectorStores.Get(context.TODO(), \"vector_store_id\")\n\tif err != nil {\n\t\tpanic(err.Error())\n\t}\n\tfmt.Printf(\"%+v\\n\", result)\n}\n",
  "typeReference": {
    "signature": "client.VectorStores.Get(ctx, vectorStoreID) (*VectorStoreObject, error)",
    "request": {
      "fields": []
    },
    "response": {
      "typeName": "VectorStoreObject",
      "fields": [
        {
          "name": "ID",
          "langType": "string",
          "required": true,
          "description": "The identifier, which can be referenced in API endpoints."
        },
        {
          "name": "Object",
          "langType": "string",
          "required": true,
          "description": "The object type, which is always `vector_store`."
        },
        {
          "name": "CreatedAt",
          "langType": "int64",
          "required": true,
          "description": "The Unix timestamp (in seconds) for when the vector store was created."
        },
        {
          "name": "Name",
          "langType": "string",
          "required": true,
          "description": "The name of the vector store."
        },
        {
          "name": "UsageBytes",
          "langType": "int64",
          "required": true,
          "description": "The total number of bytes used by the files in the vector store."
        },
        {
          "name": "FileCounts",
          "langType": "VectorStoreObjectFileCounts",
          "required": true,
          "refTypeName": "VectorStoreObjectFileCounts"
        },
        {
          "name": "Status",
          "langType": "VectorStoreObjectStatus",
          "required": true,
          "description": "The status of the vector store, which can be either `expired`, `in_progress`, or `completed`. A status of `completed` indicates that the vector store is ready for use.",
          "refTypeName": "VectorStoreObjectStatus"
        },
        {
          "name": "ExpiresAfter",
          "langType": "VectorStoreExpirationAfter",
          "required": false,
          "refTypeName": "VectorStoreExpirationAfter"
        },
        {
          "name": "ExpiresAt",
          "langType": "int64",
          "required": false
        },
        {
          "name": "LastActiveAt",
          "langType": "int64",
          "required": true
        },
        {
          "name": "Metadata",
          "langType": "Metadata",
          "required": true,
          "refTypeName": "Metadata"
        }
      ]
    }
  }
}

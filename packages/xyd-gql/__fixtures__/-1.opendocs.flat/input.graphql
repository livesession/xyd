extend schema @docs(
    flattenTypes: true
)

type Author {
    id: ID!

    name: String!

    details: AuthorDetails!
}

type AuthorDetails {
    birthDate: String
}

type Query {
    author(id: ID!): Author
}
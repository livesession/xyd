// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Threading;
using System.Threading.Tasks;

namespace Example.Openai;

public sealed class ProjectsService
{
    private readonly Transport _transport;
    public ProjectsGroupsService Groups { get; }
    public ProjectsRolesService Roles { get; }
    public ProjectsUsersService Users { get; }

    internal ProjectsService(Transport transport)
    {
        _transport = transport;
        Groups = new ProjectsGroupsService(transport);
        Roles = new ProjectsRolesService(transport);
        Users = new ProjectsUsersService(transport);
    }
}

public sealed class ProjectsGroupsService
{
    private readonly Transport _transport;
    public ProjectsGroupsRolesService Roles { get; }

    internal ProjectsGroupsService(Transport transport)
    {
        _transport = transport;
        Roles = new ProjectsGroupsRolesService(transport);
    }
}

public sealed class ProjectsGroupsRolesService
{
    private readonly Transport _transport;

    internal ProjectsGroupsRolesService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Assigns a project role to a group within a project.</summary>
    public async Task<GroupRoleAssignment> Create(string projectId, string groupId, PublicAssignOrganizationGroupRoleBody body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(projectId))
        {
            throw new ArgumentException("Expected a non-empty value for projectId.", nameof(projectId));
        }
        if (string.IsNullOrEmpty(groupId))
        {
            throw new ArgumentException("Expected a non-empty value for groupId.", nameof(groupId));
        }
        return await _transport.RequestAsync<GroupRoleAssignment>(HttpMethod.Post, $"/projects/{projectId}/groups/{groupId}/roles", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Unassigns a project role from a group within a project.</summary>
    public async Task<DeletedRoleAssignmentResource> Delete(string projectId, string groupId, string roleId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(projectId))
        {
            throw new ArgumentException("Expected a non-empty value for projectId.", nameof(projectId));
        }
        if (string.IsNullOrEmpty(groupId))
        {
            throw new ArgumentException("Expected a non-empty value for groupId.", nameof(groupId));
        }
        if (string.IsNullOrEmpty(roleId))
        {
            throw new ArgumentException("Expected a non-empty value for roleId.", nameof(roleId));
        }
        return await _transport.RequestAsync<DeletedRoleAssignmentResource>(HttpMethod.Delete, $"/projects/{projectId}/groups/{groupId}/roles/{roleId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Lists the project roles assigned to a group within a project.</summary>
    public async Task<CursorPage<AssignedRoleDetails>> List(string projectId, string groupId, long? limit = null, string? after = null, ProjectsGroupsRolesListOrder? order = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(projectId))
        {
            throw new ArgumentException("Expected a non-empty value for projectId.", nameof(projectId));
        }
        if (string.IsNullOrEmpty(groupId))
        {
            throw new ArgumentException("Expected a non-empty value for groupId.", nameof(groupId));
        }
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["after"] = after,
            ["order"] = order,
        };
        return await _transport.RequestAsync<CursorPage<AssignedRoleDetails>>(HttpMethod.Get, $"/projects/{projectId}/groups/{groupId}/roles", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class ProjectsRolesService
{
    private readonly Transport _transport;

    internal ProjectsRolesService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Creates a custom role for a project.</summary>
    public async Task<Role> Create(string projectId, PublicCreateOrganizationRoleBody body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(projectId))
        {
            throw new ArgumentException("Expected a non-empty value for projectId.", nameof(projectId));
        }
        return await _transport.RequestAsync<Role>(HttpMethod.Post, $"/projects/{projectId}/roles", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Deletes a custom role from a project.</summary>
    public async Task<RoleDeletedResource> Delete(string projectId, string roleId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(projectId))
        {
            throw new ArgumentException("Expected a non-empty value for projectId.", nameof(projectId));
        }
        if (string.IsNullOrEmpty(roleId))
        {
            throw new ArgumentException("Expected a non-empty value for roleId.", nameof(roleId));
        }
        return await _transport.RequestAsync<RoleDeletedResource>(HttpMethod.Delete, $"/projects/{projectId}/roles/{roleId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Lists the roles configured for a project.</summary>
    public async Task<CursorPage<Role>> List(string projectId, long? limit = null, string? after = null, ProjectsRolesListOrder? order = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(projectId))
        {
            throw new ArgumentException("Expected a non-empty value for projectId.", nameof(projectId));
        }
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["after"] = after,
            ["order"] = order,
        };
        return await _transport.RequestAsync<CursorPage<Role>>(HttpMethod.Get, $"/projects/{projectId}/roles", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Updates an existing project role.</summary>
    public async Task<Role> Update(string projectId, string roleId, PublicUpdateOrganizationRoleBody body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(projectId))
        {
            throw new ArgumentException("Expected a non-empty value for projectId.", nameof(projectId));
        }
        if (string.IsNullOrEmpty(roleId))
        {
            throw new ArgumentException("Expected a non-empty value for roleId.", nameof(roleId));
        }
        return await _transport.RequestAsync<Role>(HttpMethod.Post, $"/projects/{projectId}/roles/{roleId}", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class ProjectsUsersService
{
    private readonly Transport _transport;
    public ProjectsUsersRolesService Roles { get; }

    internal ProjectsUsersService(Transport transport)
    {
        _transport = transport;
        Roles = new ProjectsUsersRolesService(transport);
    }
}

public sealed class ProjectsUsersRolesService
{
    private readonly Transport _transport;

    internal ProjectsUsersRolesService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Assigns a project role to a user within a project.</summary>
    public async Task<UserRoleAssignment> Create(string projectId, string userId, PublicAssignOrganizationGroupRoleBody body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(projectId))
        {
            throw new ArgumentException("Expected a non-empty value for projectId.", nameof(projectId));
        }
        if (string.IsNullOrEmpty(userId))
        {
            throw new ArgumentException("Expected a non-empty value for userId.", nameof(userId));
        }
        return await _transport.RequestAsync<UserRoleAssignment>(HttpMethod.Post, $"/projects/{projectId}/users/{userId}/roles", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Unassigns a project role from a user within a project.</summary>
    public async Task<DeletedRoleAssignmentResource> Delete(string projectId, string userId, string roleId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(projectId))
        {
            throw new ArgumentException("Expected a non-empty value for projectId.", nameof(projectId));
        }
        if (string.IsNullOrEmpty(userId))
        {
            throw new ArgumentException("Expected a non-empty value for userId.", nameof(userId));
        }
        if (string.IsNullOrEmpty(roleId))
        {
            throw new ArgumentException("Expected a non-empty value for roleId.", nameof(roleId));
        }
        return await _transport.RequestAsync<DeletedRoleAssignmentResource>(HttpMethod.Delete, $"/projects/{projectId}/users/{userId}/roles/{roleId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Lists the project roles assigned to a user within a project.</summary>
    public async Task<CursorPage<AssignedRoleDetails>> List(string projectId, string userId, long? limit = null, string? after = null, ProjectsUsersRolesListOrder? order = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(projectId))
        {
            throw new ArgumentException("Expected a non-empty value for projectId.", nameof(projectId));
        }
        if (string.IsNullOrEmpty(userId))
        {
            throw new ArgumentException("Expected a non-empty value for userId.", nameof(userId));
        }
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["after"] = after,
            ["order"] = order,
        };
        return await _transport.RequestAsync<CursorPage<AssignedRoleDetails>>(HttpMethod.Get, $"/projects/{projectId}/users/{userId}/roles", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

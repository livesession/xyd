// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Threading.Tasks;

namespace Example.Openai.Tests;

public class ContainersTests
{
    [Fact]
    public async Task MethodCreate()
    {
        string baseUrl = TestServer.BaseUrl();
        if (!TestServer.Check(baseUrl))
        {
            return;
        }
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: baseUrl);
        var result = await client.Containers.Create();
        Assert.NotNull(result);
    }

    [Fact]
    public async Task MethodCreateWithAllParams()
    {
        string baseUrl = TestServer.BaseUrl();
        if (!TestServer.Check(baseUrl))
        {
            return;
        }
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: baseUrl);
        var result = await client.Containers.Create(new CreateContainerBody { Name = "x", FileIds = new List<string> { "x" }, ExpiresAfter = new CreateContainerBodyExpiresAfter { Anchor = "last_active_at", Minutes = 0 }, Skills = new List<object> { new SkillReferenceParam { Type = "skill_reference", SkillId = "x", Version = "x" } }, MemoryLimit = CreateContainerBodyMemoryLimit._1g, NetworkPolicy = new ContainerNetworkPolicyDisabledParam { Type = "disabled" } });
        Assert.NotNull(result);
    }

    [Fact]
    public async Task MethodDelete()
    {
        string baseUrl = TestServer.BaseUrl();
        if (!TestServer.Check(baseUrl))
        {
            return;
        }
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: baseUrl);
        await client.Containers.Delete("container_id");
    }

    [Fact]
    public async Task PathParamsDelete()
    {
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: "http://localhost:4010");
        await Assert.ThrowsAsync<ArgumentException>(async () => await client.Containers.Delete(""));
    }

    [Fact]
    public async Task MethodList()
    {
        string baseUrl = TestServer.BaseUrl();
        if (!TestServer.Check(baseUrl))
        {
            return;
        }
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: baseUrl);
        var result = await client.Containers.List();
        Assert.NotNull(result);
    }

    [Fact]
    public async Task MethodListWithAllParams()
    {
        string baseUrl = TestServer.BaseUrl();
        if (!TestServer.Check(baseUrl))
        {
            return;
        }
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: baseUrl);
        var result = await client.Containers.List(0, ContainersListOrder.Asc, "after", "name");
        Assert.NotNull(result);
    }

    [Fact]
    public async Task MethodRetrieve()
    {
        string baseUrl = TestServer.BaseUrl();
        if (!TestServer.Check(baseUrl))
        {
            return;
        }
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: baseUrl);
        var result = await client.Containers.Retrieve("container_id");
        Assert.NotNull(result);
    }

    [Fact]
    public async Task PathParamsRetrieve()
    {
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: "http://localhost:4010");
        await Assert.ThrowsAsync<ArgumentException>(async () => await client.Containers.Retrieve(""));
    }

    [Fact]
    public async Task MethodFilesContent()
    {
        string baseUrl = TestServer.BaseUrl();
        if (!TestServer.Check(baseUrl))
        {
            return;
        }
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: baseUrl);
        await client.Containers.Files.Content("container_id", "file_id");
    }

    [Fact]
    public async Task PathParamsFilesContent()
    {
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: "http://localhost:4010");
        await Assert.ThrowsAsync<ArgumentException>(async () => await client.Containers.Files.Content("", "file_id"));
    }

    [Fact]
    public async Task MethodFilesCreate()
    {
        string baseUrl = TestServer.BaseUrl();
        if (!TestServer.Check(baseUrl))
        {
            return;
        }
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: baseUrl);
        var result = await client.Containers.Files.Create("container_id", new CreateContainerFileBody());
        Assert.NotNull(result);
    }

    [Fact]
    public async Task MethodFilesCreateWithAllParams()
    {
        string baseUrl = TestServer.BaseUrl();
        if (!TestServer.Check(baseUrl))
        {
            return;
        }
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: baseUrl);
        var result = await client.Containers.Files.Create("container_id", new CreateContainerFileBody { FileId = "x", File = System.Text.Encoding.UTF8.GetBytes("Example data") });
        Assert.NotNull(result);
    }

    [Fact]
    public async Task PathParamsFilesCreate()
    {
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: "http://localhost:4010");
        await Assert.ThrowsAsync<ArgumentException>(async () => await client.Containers.Files.Create("", new CreateContainerFileBody()));
    }

    [Fact]
    public async Task MethodFilesDelete()
    {
        string baseUrl = TestServer.BaseUrl();
        if (!TestServer.Check(baseUrl))
        {
            return;
        }
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: baseUrl);
        await client.Containers.Files.Delete("container_id", "file_id");
    }

    [Fact]
    public async Task PathParamsFilesDelete()
    {
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: "http://localhost:4010");
        await Assert.ThrowsAsync<ArgumentException>(async () => await client.Containers.Files.Delete("", "file_id"));
    }

    [Fact]
    public async Task MethodFilesList()
    {
        string baseUrl = TestServer.BaseUrl();
        if (!TestServer.Check(baseUrl))
        {
            return;
        }
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: baseUrl);
        var result = await client.Containers.Files.List("container_id");
        Assert.NotNull(result);
    }

    [Fact]
    public async Task MethodFilesListWithAllParams()
    {
        string baseUrl = TestServer.BaseUrl();
        if (!TestServer.Check(baseUrl))
        {
            return;
        }
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: baseUrl);
        var result = await client.Containers.Files.List("container_id", 0, ContainersFilesListOrder.Asc, "after");
        Assert.NotNull(result);
    }

    [Fact]
    public async Task PathParamsFilesList()
    {
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: "http://localhost:4010");
        await Assert.ThrowsAsync<ArgumentException>(async () => await client.Containers.Files.List(""));
    }

    [Fact]
    public async Task MethodFilesRetrieve()
    {
        string baseUrl = TestServer.BaseUrl();
        if (!TestServer.Check(baseUrl))
        {
            return;
        }
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: baseUrl);
        var result = await client.Containers.Files.Retrieve("container_id", "file_id");
        Assert.NotNull(result);
    }

    [Fact]
    public async Task PathParamsFilesRetrieve()
    {
        var client = new OpenaiClient(apiKey: "My API Key", baseUrl: "http://localhost:4010");
        await Assert.ThrowsAsync<ArgumentException>(async () => await client.Containers.Files.Retrieve("", "file_id"));
    }
}

// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Threading;
using System.Threading.Tasks;

namespace Example.Openai;

public sealed class VectorStoresService
{
    private readonly Transport _transport;
    public VectorStoresFileBatchesService FileBatches { get; }
    public VectorStoresFilesService Files { get; }

    internal VectorStoresService(Transport transport)
    {
        _transport = transport;
        FileBatches = new VectorStoresFileBatchesService(transport);
        Files = new VectorStoresFilesService(transport);
    }

    /// <summary>Create a vector store.</summary>
    public async Task<VectorStoreObject> Create(CreateVectorStoreRequest body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<VectorStoreObject>(HttpMethod.Post, "/vector_stores", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Delete a vector store.</summary>
    public async Task<DeleteVectorStoreResponse> Delete(string vectorStoreId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        return await _transport.RequestAsync<DeleteVectorStoreResponse>(HttpMethod.Delete, $"/vector_stores/{vectorStoreId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Returns a list of vector stores.</summary>
    public async Task<CursorPage<VectorStoreObject>> List(long? limit = null, VectorStoresListOrder? order = null, string? after = null, string? before = null, CancellationToken cancellationToken = default)
    {
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["order"] = order,
            ["after"] = after,
            ["before"] = before,
        };
        return await _transport.RequestAsync<CursorPage<VectorStoreObject>>(HttpMethod.Get, "/vector_stores", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Retrieves a vector store.</summary>
    public async Task<VectorStoreObject> Retrieve(string vectorStoreId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        return await _transport.RequestAsync<VectorStoreObject>(HttpMethod.Get, $"/vector_stores/{vectorStoreId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Search a vector store for relevant chunks based on a query and file attributes filter.</summary>
    public async Task<CursorPage<VectorStoreSearchResultItem>> Search(string vectorStoreId, VectorStoreSearchRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        return await _transport.RequestAsync<CursorPage<VectorStoreSearchResultItem>>(HttpMethod.Post, $"/vector_stores/{vectorStoreId}/search", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Modifies a vector store.</summary>
    public async Task<VectorStoreObject> Update(string vectorStoreId, UpdateVectorStoreRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        return await _transport.RequestAsync<VectorStoreObject>(HttpMethod.Post, $"/vector_stores/{vectorStoreId}", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class VectorStoresFileBatchesService
{
    private readonly Transport _transport;

    internal VectorStoresFileBatchesService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Cancel a vector store file batch. This attempts to cancel the processing of files in this batch as soon as possible.</summary>
    public async Task<VectorStoreFileBatchObject> Cancel(string vectorStoreId, string batchId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        if (string.IsNullOrEmpty(batchId))
        {
            throw new ArgumentException("Expected a non-empty value for batchId.", nameof(batchId));
        }
        return await _transport.RequestAsync<VectorStoreFileBatchObject>(HttpMethod.Post, $"/vector_stores/{vectorStoreId}/file_batches/{batchId}/cancel", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// The maximum number of files in a single batch request is 2000.
    /// Vector store file attach requests are rate limited per vector store (300 requests per minute across both this endpoint and `/vector_stores/{vector_store_id}/files`).
    /// For ingesting multiple files into the same vector store, this batch endpoint is recommended.
    /// </summary>
    public async Task<VectorStoreFileBatchObject> Create(string vectorStoreId, object body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        return await _transport.RequestAsync<VectorStoreFileBatchObject>(HttpMethod.Post, $"/vector_stores/{vectorStoreId}/file_batches", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Returns a list of vector store files in a batch.</summary>
    public async Task<CursorPage<VectorStoreFileObject>> ListFiles(string vectorStoreId, string batchId, long? limit = null, VectorStoresFileBatchesListFilesOrder? order = null, string? after = null, string? before = null, VectorStoresFileBatchesListFilesFilter? filter = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        if (string.IsNullOrEmpty(batchId))
        {
            throw new ArgumentException("Expected a non-empty value for batchId.", nameof(batchId));
        }
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["order"] = order,
            ["after"] = after,
            ["before"] = before,
            ["filter"] = filter,
        };
        return await _transport.RequestAsync<CursorPage<VectorStoreFileObject>>(HttpMethod.Get, $"/vector_stores/{vectorStoreId}/file_batches/{batchId}/files", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Retrieves a vector store file batch.</summary>
    public async Task<VectorStoreFileBatchObject> Retrieve(string vectorStoreId, string batchId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        if (string.IsNullOrEmpty(batchId))
        {
            throw new ArgumentException("Expected a non-empty value for batchId.", nameof(batchId));
        }
        return await _transport.RequestAsync<VectorStoreFileBatchObject>(HttpMethod.Get, $"/vector_stores/{vectorStoreId}/file_batches/{batchId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class VectorStoresFilesService
{
    private readonly Transport _transport;

    internal VectorStoresFilesService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Retrieve the parsed contents of a vector store file.</summary>
    public async Task<CursorPage<VectorStoreFileContentResponseDataItem>> Content(string vectorStoreId, string fileId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        if (string.IsNullOrEmpty(fileId))
        {
            throw new ArgumentException("Expected a non-empty value for fileId.", nameof(fileId));
        }
        return await _transport.RequestAsync<CursorPage<VectorStoreFileContentResponseDataItem>>(HttpMethod.Get, $"/vector_stores/{vectorStoreId}/files/{fileId}/content", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// This endpoint is subject to a per-vector-store write rate limit of 300 requests per minute, shared with `/vector_stores/{vector_store_id}/file_batches`.
    /// For uploading multiple files to the same vector store, use the file batches endpoint to reduce request volume.
    /// </summary>
    public async Task<VectorStoreFileObject> Create(string vectorStoreId, CreateVectorStoreFileRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        return await _transport.RequestAsync<VectorStoreFileObject>(HttpMethod.Post, $"/vector_stores/{vectorStoreId}/files", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Delete a vector store file. This will remove the file from the vector store but the file itself will not be deleted. To delete the file, use the [delete file](/docs/api-reference/files/delete) endpoint.</summary>
    public async Task<DeleteVectorStoreFileResponse> Delete(string vectorStoreId, string fileId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        if (string.IsNullOrEmpty(fileId))
        {
            throw new ArgumentException("Expected a non-empty value for fileId.", nameof(fileId));
        }
        return await _transport.RequestAsync<DeleteVectorStoreFileResponse>(HttpMethod.Delete, $"/vector_stores/{vectorStoreId}/files/{fileId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Returns a list of vector store files.</summary>
    public async Task<CursorPage<VectorStoreFileObject>> List(string vectorStoreId, long? limit = null, VectorStoresFilesListOrder? order = null, string? after = null, string? before = null, VectorStoresFilesListFilter? filter = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["order"] = order,
            ["after"] = after,
            ["before"] = before,
            ["filter"] = filter,
        };
        return await _transport.RequestAsync<CursorPage<VectorStoreFileObject>>(HttpMethod.Get, $"/vector_stores/{vectorStoreId}/files", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Retrieves a vector store file.</summary>
    public async Task<VectorStoreFileObject> Retrieve(string vectorStoreId, string fileId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        if (string.IsNullOrEmpty(fileId))
        {
            throw new ArgumentException("Expected a non-empty value for fileId.", nameof(fileId));
        }
        return await _transport.RequestAsync<VectorStoreFileObject>(HttpMethod.Get, $"/vector_stores/{vectorStoreId}/files/{fileId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Update attributes on a vector store file.</summary>
    public async Task<VectorStoreFileObject> Update(string vectorStoreId, string fileId, UpdateVectorStoreFileAttributesRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(vectorStoreId))
        {
            throw new ArgumentException("Expected a non-empty value for vectorStoreId.", nameof(vectorStoreId));
        }
        if (string.IsNullOrEmpty(fileId))
        {
            throw new ArgumentException("Expected a non-empty value for fileId.", nameof(fileId));
        }
        return await _transport.RequestAsync<VectorStoreFileObject>(HttpMethod.Post, $"/vector_stores/{vectorStoreId}/files/{fileId}", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Threading;
using System.Threading.Tasks;

namespace Example.Openai;

public sealed class RealtimeService
{
    private readonly Transport _transport;
    public RealtimeCallsService Calls { get; }
    public RealtimeClientSecretsService ClientSecrets { get; }
    public RealtimeSessionsService Sessions { get; }
    public RealtimeTranscriptionSessionsService TranscriptionSessions { get; }
    public RealtimeTranslationsService Translations { get; }

    internal RealtimeService(Transport transport)
    {
        _transport = transport;
        Calls = new RealtimeCallsService(transport);
        ClientSecrets = new RealtimeClientSecretsService(transport);
        Sessions = new RealtimeSessionsService(transport);
        TranscriptionSessions = new RealtimeTranscriptionSessionsService(transport);
        Translations = new RealtimeTranslationsService(transport);
    }
}

public sealed class RealtimeCallsService
{
    private readonly Transport _transport;

    internal RealtimeCallsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Accept an incoming SIP call and configure the realtime session that will
    /// handle it.
    /// </summary>
    public async Task Accept(string callId, RealtimeSessionCreateRequestGa body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(callId))
        {
            throw new ArgumentException("Expected a non-empty value for callId.", nameof(callId));
        }
        await _transport.RequestAsync(HttpMethod.Post, $"/realtime/calls/{callId}/accept", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// Create a new Realtime API call over WebRTC and receive the SDP answer needed
    /// to complete the peer connection.
    /// </summary>
    public async Task<byte[]> Create(RealtimeCallCreateRequest body, CancellationToken cancellationToken = default)
    {
        var bodyMap = new Dictionary<string, object?>
        {
            ["sdp"] = body.Sdp,
            ["session"] = body.Session,
        };
        return await _transport.RequestRawAsync(HttpMethod.Post, "/realtime/calls", body: bodyMap, encoding: "multipart", accept: "application/sdp", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// End an active Realtime API call, whether it was initiated over SIP or
    /// WebRTC.
    /// </summary>
    public async Task Hangup(string callId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(callId))
        {
            throw new ArgumentException("Expected a non-empty value for callId.", nameof(callId));
        }
        await _transport.RequestAsync(HttpMethod.Post, $"/realtime/calls/{callId}/hangup", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Transfer an active SIP call to a new destination using the SIP REFER verb.</summary>
    public async Task Refer(string callId, RealtimeCallReferRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(callId))
        {
            throw new ArgumentException("Expected a non-empty value for callId.", nameof(callId));
        }
        await _transport.RequestAsync(HttpMethod.Post, $"/realtime/calls/{callId}/refer", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Decline an incoming SIP call by returning a SIP status code to the caller.</summary>
    public async Task Reject(string callId, RealtimeCallRejectRequest? body = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(callId))
        {
            throw new ArgumentException("Expected a non-empty value for callId.", nameof(callId));
        }
        await _transport.RequestAsync(HttpMethod.Post, $"/realtime/calls/{callId}/reject", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class RealtimeClientSecretsService
{
    private readonly Transport _transport;

    internal RealtimeClientSecretsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Create a Realtime client secret with an associated session configuration.
    /// Client secrets are short-lived tokens that can be passed to a client app,
    /// such as a web frontend or mobile client, which grants access to the Realtime API without
    /// leaking your main API key. You can configure a custom TTL for each client secret.
    /// You can also attach session configuration options to the client secret, which will be
    /// applied to any sessions created using that client secret, but these can also be overridden
    /// by the client connection.
    /// [Learn more about authentication with client secrets over WebRTC](/docs/guides/realtime-webrtc).
    /// Returns the created client secret and the effective session object. The client secret is a string that looks like `ek_1234`.
    /// </summary>
    public async Task<RealtimeCreateClientSecretResponse> Create(RealtimeCreateClientSecretRequest body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<RealtimeCreateClientSecretResponse>(HttpMethod.Post, "/realtime/client_secrets", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class RealtimeSessionsService
{
    private readonly Transport _transport;

    internal RealtimeSessionsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Create an ephemeral API token for use in client-side applications with the
    /// Realtime API. Can be configured with the same session parameters as the
    /// `session.update` client event.
    /// It responds with a session object, plus a `client_secret` key which contains
    /// a usable ephemeral API token that can be used to authenticate browser clients
    /// for the Realtime API.
    /// Returns the created Realtime session object, plus an ephemeral key.
    /// </summary>
    public async Task<RealtimeSessionCreateResponse> Create(RealtimeSessionCreateRequest body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<RealtimeSessionCreateResponse>(HttpMethod.Post, "/realtime/sessions", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class RealtimeTranscriptionSessionsService
{
    private readonly Transport _transport;

    internal RealtimeTranscriptionSessionsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Create an ephemeral API token for use in client-side applications with the
    /// Realtime API specifically for realtime transcriptions.
    /// Can be configured with the same session parameters as the `transcription_session.update` client event.
    /// It responds with a session object, plus a `client_secret` key which contains
    /// a usable ephemeral API token that can be used to authenticate browser clients
    /// for the Realtime API.
    /// Returns the created Realtime transcription session object, plus an ephemeral key.
    /// </summary>
    public async Task<RealtimeTranscriptionSessionCreateResponse> Create(RealtimeTranscriptionSessionCreateRequest body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<RealtimeTranscriptionSessionCreateResponse>(HttpMethod.Post, "/realtime/transcription_sessions", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class RealtimeTranslationsService
{
    private readonly Transport _transport;
    public RealtimeTranslationsClientSecretsService ClientSecrets { get; }

    internal RealtimeTranslationsService(Transport transport)
    {
        _transport = transport;
        ClientSecrets = new RealtimeTranslationsClientSecretsService(transport);
    }
}

public sealed class RealtimeTranslationsClientSecretsService
{
    private readonly Transport _transport;

    internal RealtimeTranslationsClientSecretsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Create a Realtime translation client secret with an associated translation session configuration.
    /// Client secrets are short-lived tokens that can be passed to a client app,
    /// such as a web frontend or mobile client, which grants access to the Realtime
    /// Translation API without leaking your main API key. You can configure a custom
    /// TTL for each client secret.
    /// Returns the created client secret and the effective translation session object.
    /// The client secret is a string that looks like `ek_1234`.
    /// </summary>
    public async Task<RealtimeTranslationClientSecretCreateResponse> Create(RealtimeTranslationClientSecretCreateRequest body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<RealtimeTranslationClientSecretCreateResponse>(HttpMethod.Post, "/realtime/translations/client_secrets", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

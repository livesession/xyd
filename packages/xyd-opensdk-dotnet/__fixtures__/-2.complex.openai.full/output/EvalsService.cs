// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Threading;
using System.Threading.Tasks;

namespace Example.Openai;

public sealed class EvalsService
{
    private readonly Transport _transport;
    public EvalsRunsService Runs { get; }

    internal EvalsService(Transport transport)
    {
        _transport = transport;
        Runs = new EvalsRunsService(transport);
    }

    /// <summary>
    /// Create the structure of an evaluation that can be used to test a model's performance.
    /// An evaluation is a set of testing criteria and the config for a data source, which dictates the schema of the data used in the evaluation. After creating an evaluation, you can run it on different models and model parameters. We support several types of graders and datasources.
    /// For more information, see the [Evals guide](/docs/guides/evals).
    /// </summary>
    public async Task<Eval> Create(CreateEvalRequest body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<Eval>(HttpMethod.Post, "/evals", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Delete an evaluation.</summary>
    public async Task<EvalsDeleteResponse> Delete(string evalId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(evalId))
        {
            throw new ArgumentException("Expected a non-empty value for evalId.", nameof(evalId));
        }
        return await _transport.RequestAsync<EvalsDeleteResponse>(HttpMethod.Delete, $"/evals/{evalId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>List evaluations for a project.</summary>
    public async Task<CursorPage<Eval>> List(string? after = null, long? limit = null, EvalsListOrder? order = null, EvalsListOrderBy? orderBy = null, CancellationToken cancellationToken = default)
    {
        var query = new Dictionary<string, object?>
        {
            ["after"] = after,
            ["limit"] = limit,
            ["order"] = order,
            ["order_by"] = orderBy,
        };
        return await _transport.RequestAsync<CursorPage<Eval>>(HttpMethod.Get, "/evals", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Get an evaluation by ID.</summary>
    public async Task<Eval> Retrieve(string evalId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(evalId))
        {
            throw new ArgumentException("Expected a non-empty value for evalId.", nameof(evalId));
        }
        return await _transport.RequestAsync<Eval>(HttpMethod.Get, $"/evals/{evalId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Update certain properties of an evaluation.</summary>
    public async Task<Eval> Update(string evalId, EvalsUpdateRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(evalId))
        {
            throw new ArgumentException("Expected a non-empty value for evalId.", nameof(evalId));
        }
        return await _transport.RequestAsync<Eval>(HttpMethod.Post, $"/evals/{evalId}", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class EvalsRunsService
{
    private readonly Transport _transport;
    public EvalsRunsOutputItemsService OutputItems { get; }

    internal EvalsRunsService(Transport transport)
    {
        _transport = transport;
        OutputItems = new EvalsRunsOutputItemsService(transport);
    }

    /// <summary>Cancel an ongoing evaluation run.</summary>
    public async Task<EvalRun> Cancel(string evalId, string runId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(evalId))
        {
            throw new ArgumentException("Expected a non-empty value for evalId.", nameof(evalId));
        }
        if (string.IsNullOrEmpty(runId))
        {
            throw new ArgumentException("Expected a non-empty value for runId.", nameof(runId));
        }
        return await _transport.RequestAsync<EvalRun>(HttpMethod.Post, $"/evals/{evalId}/runs/{runId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Kicks off a new run for a given evaluation, specifying the data source, and what model configuration to use to test. The datasource will be validated against the schema specified in the config of the evaluation.</summary>
    public async Task<EvalRun> Create(string evalId, CreateEvalRunRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(evalId))
        {
            throw new ArgumentException("Expected a non-empty value for evalId.", nameof(evalId));
        }
        return await _transport.RequestAsync<EvalRun>(HttpMethod.Post, $"/evals/{evalId}/runs", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Delete an eval run.</summary>
    public async Task<EvalsRunsDeleteResponse> Delete(string evalId, string runId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(evalId))
        {
            throw new ArgumentException("Expected a non-empty value for evalId.", nameof(evalId));
        }
        if (string.IsNullOrEmpty(runId))
        {
            throw new ArgumentException("Expected a non-empty value for runId.", nameof(runId));
        }
        return await _transport.RequestAsync<EvalsRunsDeleteResponse>(HttpMethod.Delete, $"/evals/{evalId}/runs/{runId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Get a list of runs for an evaluation.</summary>
    public async Task<CursorPage<EvalRun>> List(string evalId, string? after = null, long? limit = null, EvalsRunsListOrder? order = null, EvalsRunsListStatus? status = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(evalId))
        {
            throw new ArgumentException("Expected a non-empty value for evalId.", nameof(evalId));
        }
        var query = new Dictionary<string, object?>
        {
            ["after"] = after,
            ["limit"] = limit,
            ["order"] = order,
            ["status"] = status,
        };
        return await _transport.RequestAsync<CursorPage<EvalRun>>(HttpMethod.Get, $"/evals/{evalId}/runs", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Get an evaluation run by ID.</summary>
    public async Task<EvalRun> Retrieve(string evalId, string runId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(evalId))
        {
            throw new ArgumentException("Expected a non-empty value for evalId.", nameof(evalId));
        }
        if (string.IsNullOrEmpty(runId))
        {
            throw new ArgumentException("Expected a non-empty value for runId.", nameof(runId));
        }
        return await _transport.RequestAsync<EvalRun>(HttpMethod.Get, $"/evals/{evalId}/runs/{runId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class EvalsRunsOutputItemsService
{
    private readonly Transport _transport;

    internal EvalsRunsOutputItemsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Get a list of output items for an evaluation run.</summary>
    public async Task<CursorPage<EvalRunOutputItem>> List(string evalId, string runId, string? after = null, long? limit = null, EvalsRunsOutputItemsListStatus? status = null, EvalsRunsOutputItemsListOrder? order = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(evalId))
        {
            throw new ArgumentException("Expected a non-empty value for evalId.", nameof(evalId));
        }
        if (string.IsNullOrEmpty(runId))
        {
            throw new ArgumentException("Expected a non-empty value for runId.", nameof(runId));
        }
        var query = new Dictionary<string, object?>
        {
            ["after"] = after,
            ["limit"] = limit,
            ["status"] = status,
            ["order"] = order,
        };
        return await _transport.RequestAsync<CursorPage<EvalRunOutputItem>>(HttpMethod.Get, $"/evals/{evalId}/runs/{runId}/output_items", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Get an evaluation run output item by ID.</summary>
    public async Task<EvalRunOutputItem> Retrieve(string evalId, string runId, string outputItemId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(evalId))
        {
            throw new ArgumentException("Expected a non-empty value for evalId.", nameof(evalId));
        }
        if (string.IsNullOrEmpty(runId))
        {
            throw new ArgumentException("Expected a non-empty value for runId.", nameof(runId));
        }
        if (string.IsNullOrEmpty(outputItemId))
        {
            throw new ArgumentException("Expected a non-empty value for outputItemId.", nameof(outputItemId));
        }
        return await _transport.RequestAsync<EvalRunOutputItem>(HttpMethod.Get, $"/evals/{evalId}/runs/{runId}/output_items/{outputItemId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Threading;
using System.Threading.Tasks;

namespace Example.Openai;

public sealed class AudioService
{
    private readonly Transport _transport;
    public AudioSpeechService Speech { get; }
    public AudioTranscriptionsService Transcriptions { get; }
    public AudioTranslationsService Translations { get; }
    public AudioVoiceConsentsService VoiceConsents { get; }
    public AudioVoicesService Voices { get; }

    internal AudioService(Transport transport)
    {
        _transport = transport;
        Speech = new AudioSpeechService(transport);
        Transcriptions = new AudioTranscriptionsService(transport);
        Translations = new AudioTranslationsService(transport);
        VoiceConsents = new AudioVoiceConsentsService(transport);
        Voices = new AudioVoicesService(transport);
    }
}

public sealed class AudioSpeechService
{
    private readonly Transport _transport;

    internal AudioSpeechService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Generates audio from the input text.
    /// Returns the audio file content, or a stream of audio events.
    /// </summary>
    public async Task<byte[]> Create(CreateSpeechRequest body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestRawAsync(HttpMethod.Post, "/audio/speech", body: body, accept: "application/octet-stream", cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class AudioTranscriptionsService
{
    private readonly Transport _transport;

    internal AudioTranscriptionsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Transcribes audio into the input language.
    /// Returns a transcription object in `json`, `diarized_json`, or `verbose_json`
    /// format, or a stream of transcript events.
    /// </summary>
    public async Task<object> Create(CreateTranscriptionRequest body, CancellationToken cancellationToken = default)
    {
        var bodyMap = new Dictionary<string, object?>
        {
            ["file"] = body.File,
            ["model"] = body.Model,
            ["language"] = body.Language,
            ["prompt"] = body.Prompt,
            ["response_format"] = body.ResponseFormat,
            ["temperature"] = body.Temperature,
            ["include"] = body.Include,
            ["timestamp_granularities"] = body.TimestampGranularities,
            ["stream"] = body.Stream,
            ["chunking_strategy"] = body.ChunkingStrategy,
            ["known_speaker_names"] = body.KnownSpeakerNames,
            ["known_speaker_references"] = body.KnownSpeakerReferences,
        };
        return await _transport.RequestAsync<object>(HttpMethod.Post, "/audio/transcriptions", body: bodyMap, encoding: "multipart", cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class AudioTranslationsService
{
    private readonly Transport _transport;

    internal AudioTranslationsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Translates audio into English.</summary>
    public async Task<object> Create(CreateTranslationRequest body, CancellationToken cancellationToken = default)
    {
        var bodyMap = new Dictionary<string, object?>
        {
            ["file"] = body.File,
            ["model"] = body.Model,
            ["prompt"] = body.Prompt,
            ["response_format"] = body.ResponseFormat,
            ["temperature"] = body.Temperature,
        };
        return await _transport.RequestAsync<object>(HttpMethod.Post, "/audio/translations", body: bodyMap, encoding: "multipart", cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class AudioVoiceConsentsService
{
    private readonly Transport _transport;

    internal AudioVoiceConsentsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Upload a consent recording that authorizes creation of a custom voice.
    /// See the [custom voices guide](/docs/guides/text-to-speech#custom-voices) for requirements and best practices. Custom voices are limited to eligible customers.
    /// </summary>
    public async Task<VoiceConsentResource> Create(CreateVoiceConsentRequest body, CancellationToken cancellationToken = default)
    {
        var bodyMap = new Dictionary<string, object?>
        {
            ["name"] = body.Name,
            ["recording"] = body.Recording,
            ["language"] = body.Language,
        };
        return await _transport.RequestAsync<VoiceConsentResource>(HttpMethod.Post, "/audio/voice_consents", body: bodyMap, encoding: "multipart", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// Delete a consent recording that was uploaded for creating custom voices.
    /// See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
    /// </summary>
    public async Task<VoiceConsentDeletedResource> Delete(string consentId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(consentId))
        {
            throw new ArgumentException("Expected a non-empty value for consentId.", nameof(consentId));
        }
        return await _transport.RequestAsync<VoiceConsentDeletedResource>(HttpMethod.Delete, $"/audio/voice_consents/{consentId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// List consent recordings available to your organization for creating custom voices.
    /// See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
    /// </summary>
    public async Task<CursorPage<VoiceConsentResource>> List(string? after = null, long? limit = null, CancellationToken cancellationToken = default)
    {
        var query = new Dictionary<string, object?>
        {
            ["after"] = after,
            ["limit"] = limit,
        };
        return await _transport.RequestAsync<CursorPage<VoiceConsentResource>>(HttpMethod.Get, "/audio/voice_consents", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// Retrieve consent recording metadata used for creating custom voices.
    /// See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
    /// </summary>
    public async Task<VoiceConsentResource> Retrieve(string consentId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(consentId))
        {
            throw new ArgumentException("Expected a non-empty value for consentId.", nameof(consentId));
        }
        return await _transport.RequestAsync<VoiceConsentResource>(HttpMethod.Get, $"/audio/voice_consents/{consentId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// Update consent recording metadata used for creating custom voices. This endpoint updates metadata only and does not replace the underlying audio.
    /// See the [custom voices guide](/docs/guides/text-to-speech#custom-voices). Custom voices are limited to eligible customers.
    /// </summary>
    public async Task<VoiceConsentResource> Update(string consentId, UpdateVoiceConsentRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(consentId))
        {
            throw new ArgumentException("Expected a non-empty value for consentId.", nameof(consentId));
        }
        return await _transport.RequestAsync<VoiceConsentResource>(HttpMethod.Post, $"/audio/voice_consents/{consentId}", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class AudioVoicesService
{
    private readonly Transport _transport;

    internal AudioVoicesService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Create a custom voice you can use for audio output (for example, in Text-to-Speech and the Realtime API). This requires an audio sample and a previously uploaded consent recording.
    /// See the [custom voices guide](/docs/guides/text-to-speech#custom-voices) for requirements and best practices. Custom voices are limited to eligible customers.
    /// </summary>
    public async Task<VoiceResource> Create(CreateVoiceRequest body, CancellationToken cancellationToken = default)
    {
        var bodyMap = new Dictionary<string, object?>
        {
            ["name"] = body.Name,
            ["audio_sample"] = body.AudioSample,
            ["consent"] = body.Consent,
        };
        return await _transport.RequestAsync<VoiceResource>(HttpMethod.Post, "/audio/voices", body: bodyMap, encoding: "multipart", cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

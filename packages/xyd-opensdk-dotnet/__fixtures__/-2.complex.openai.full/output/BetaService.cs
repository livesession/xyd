// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Threading;
using System.Threading.Tasks;

namespace Example.Openai;

public sealed class BetaService
{
    private readonly Transport _transport;
    public BetaAssistantsService Assistants { get; }
    public BetaChatKitService ChatKit { get; }
    public BetaThreadsService Threads { get; }

    internal BetaService(Transport transport)
    {
        _transport = transport;
        Assistants = new BetaAssistantsService(transport);
        ChatKit = new BetaChatKitService(transport);
        Threads = new BetaThreadsService(transport);
    }
}

public sealed class BetaAssistantsService
{
    private readonly Transport _transport;

    internal BetaAssistantsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Create an assistant with a model and instructions.</summary>
    public async Task<AssistantObject> Create(CreateAssistantRequest body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<AssistantObject>(HttpMethod.Post, "/assistants", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Delete an assistant.</summary>
    public async Task<DeleteAssistantResponse> Delete(string assistantId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(assistantId))
        {
            throw new ArgumentException("Expected a non-empty value for assistantId.", nameof(assistantId));
        }
        return await _transport.RequestAsync<DeleteAssistantResponse>(HttpMethod.Delete, $"/assistants/{assistantId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Returns a list of assistants.</summary>
    public async Task<CursorPage<AssistantObject>> List(long? limit = null, BetaAssistantsListOrder? order = null, string? after = null, string? before = null, CancellationToken cancellationToken = default)
    {
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["order"] = order,
            ["after"] = after,
            ["before"] = before,
        };
        return await _transport.RequestAsync<CursorPage<AssistantObject>>(HttpMethod.Get, "/assistants", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Retrieves an assistant.</summary>
    public async Task<AssistantObject> Retrieve(string assistantId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(assistantId))
        {
            throw new ArgumentException("Expected a non-empty value for assistantId.", nameof(assistantId));
        }
        return await _transport.RequestAsync<AssistantObject>(HttpMethod.Get, $"/assistants/{assistantId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Modifies an assistant.</summary>
    public async Task<AssistantObject> Update(string assistantId, ModifyAssistantRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(assistantId))
        {
            throw new ArgumentException("Expected a non-empty value for assistantId.", nameof(assistantId));
        }
        return await _transport.RequestAsync<AssistantObject>(HttpMethod.Post, $"/assistants/{assistantId}", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class BetaChatKitService
{
    private readonly Transport _transport;
    public BetaChatKitSessionsService Sessions { get; }
    public BetaChatKitThreadsService Threads { get; }

    internal BetaChatKitService(Transport transport)
    {
        _transport = transport;
        Sessions = new BetaChatKitSessionsService(transport);
        Threads = new BetaChatKitThreadsService(transport);
    }
}

public sealed class BetaChatKitSessionsService
{
    private readonly Transport _transport;

    internal BetaChatKitSessionsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Cancel an active ChatKit session and return its most recent metadata.
    /// Cancelling prevents new requests from using the issued client secret.
    /// </summary>
    public async Task<ChatSessionResource> Cancel(string sessionId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(sessionId))
        {
            throw new ArgumentException("Expected a non-empty value for sessionId.", nameof(sessionId));
        }
        return await _transport.RequestAsync<ChatSessionResource>(HttpMethod.Post, $"/chatkit/sessions/{sessionId}/cancel", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Create a ChatKit session.</summary>
    public async Task<ChatSessionResource> Create(CreateChatSessionBody? body = null, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<ChatSessionResource>(HttpMethod.Post, "/chatkit/sessions", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class BetaChatKitThreadsService
{
    private readonly Transport _transport;
    public BetaChatKitThreadsItemsService Items { get; }

    internal BetaChatKitThreadsService(Transport transport)
    {
        _transport = transport;
        Items = new BetaChatKitThreadsItemsService(transport);
    }

    /// <summary>Delete a ChatKit thread along with its items and stored attachments.</summary>
    public async Task<DeletedThreadResource> Delete(string threadId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        return await _transport.RequestAsync<DeletedThreadResource>(HttpMethod.Delete, $"/chatkit/threads/{threadId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>List ChatKit threads with optional pagination and user filters.</summary>
    public async Task<CursorPage<ThreadResource>> List(long? limit = null, OrderEnum? order = null, string? after = null, string? before = null, string? user = null, CancellationToken cancellationToken = default)
    {
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["order"] = order,
            ["after"] = after,
            ["before"] = before,
            ["user"] = user,
        };
        return await _transport.RequestAsync<CursorPage<ThreadResource>>(HttpMethod.Get, "/chatkit/threads", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Retrieve a ChatKit thread by its identifier.</summary>
    public async Task<ThreadResource> Retrieve(string threadId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        return await _transport.RequestAsync<ThreadResource>(HttpMethod.Get, $"/chatkit/threads/{threadId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class BetaChatKitThreadsItemsService
{
    private readonly Transport _transport;

    internal BetaChatKitThreadsItemsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>List items that belong to a ChatKit thread.</summary>
    public async Task<CursorPage<object>> List(string threadId, long? limit = null, OrderEnum? order = null, string? after = null, string? before = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["order"] = order,
            ["after"] = after,
            ["before"] = before,
        };
        return await _transport.RequestAsync<CursorPage<object>>(HttpMethod.Get, $"/chatkit/threads/{threadId}/items", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class BetaThreadsService
{
    private readonly Transport _transport;
    public BetaThreadsMessagesService Messages { get; }
    public BetaThreadsRunsService Runs { get; }

    internal BetaThreadsService(Transport transport)
    {
        _transport = transport;
        Messages = new BetaThreadsMessagesService(transport);
        Runs = new BetaThreadsRunsService(transport);
    }

    /// <summary>Create a thread.</summary>
    public async Task<ThreadObject> Create(CreateThreadRequest? body = null, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<ThreadObject>(HttpMethod.Post, "/threads", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Delete a thread.</summary>
    public async Task<DeleteThreadResponse> Delete(string threadId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        return await _transport.RequestAsync<DeleteThreadResponse>(HttpMethod.Delete, $"/threads/{threadId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Create a thread and run it in one request.</summary>
    public async Task<RunObject> NewAndRun(CreateThreadAndRunRequest body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<RunObject>(HttpMethod.Post, "/threads/runs", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Retrieves a thread.</summary>
    public async Task<ThreadObject> Retrieve(string threadId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        return await _transport.RequestAsync<ThreadObject>(HttpMethod.Get, $"/threads/{threadId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Modifies a thread.</summary>
    public async Task<ThreadObject> Update(string threadId, ModifyThreadRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        return await _transport.RequestAsync<ThreadObject>(HttpMethod.Post, $"/threads/{threadId}", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class BetaThreadsMessagesService
{
    private readonly Transport _transport;

    internal BetaThreadsMessagesService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Create a message.</summary>
    public async Task<MessageObject> Create(string threadId, CreateMessageRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        return await _transport.RequestAsync<MessageObject>(HttpMethod.Post, $"/threads/{threadId}/messages", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Deletes a message.</summary>
    public async Task<DeleteMessageResponse> Delete(string threadId, string messageId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        if (string.IsNullOrEmpty(messageId))
        {
            throw new ArgumentException("Expected a non-empty value for messageId.", nameof(messageId));
        }
        return await _transport.RequestAsync<DeleteMessageResponse>(HttpMethod.Delete, $"/threads/{threadId}/messages/{messageId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Returns a list of messages for a given thread.</summary>
    public async Task<CursorPage<MessageObject>> List(string threadId, long? limit = null, BetaThreadsMessagesListOrder? order = null, string? after = null, string? before = null, string? runId = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["order"] = order,
            ["after"] = after,
            ["before"] = before,
            ["run_id"] = runId,
        };
        return await _transport.RequestAsync<CursorPage<MessageObject>>(HttpMethod.Get, $"/threads/{threadId}/messages", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Retrieve a message.</summary>
    public async Task<MessageObject> Retrieve(string threadId, string messageId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        if (string.IsNullOrEmpty(messageId))
        {
            throw new ArgumentException("Expected a non-empty value for messageId.", nameof(messageId));
        }
        return await _transport.RequestAsync<MessageObject>(HttpMethod.Get, $"/threads/{threadId}/messages/{messageId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Modifies a message.</summary>
    public async Task<MessageObject> Update(string threadId, string messageId, ModifyMessageRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        if (string.IsNullOrEmpty(messageId))
        {
            throw new ArgumentException("Expected a non-empty value for messageId.", nameof(messageId));
        }
        return await _transport.RequestAsync<MessageObject>(HttpMethod.Post, $"/threads/{threadId}/messages/{messageId}", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class BetaThreadsRunsService
{
    private readonly Transport _transport;
    public BetaThreadsRunsStepsService Steps { get; }
    public BetaThreadsRunsSubmitToolOutputsService SubmitToolOutputs { get; }

    internal BetaThreadsRunsService(Transport transport)
    {
        _transport = transport;
        Steps = new BetaThreadsRunsStepsService(transport);
        SubmitToolOutputs = new BetaThreadsRunsSubmitToolOutputsService(transport);
    }

    /// <summary>Cancels a run that is `in_progress`.</summary>
    public async Task<RunObject> Cancel(string threadId, string runId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        if (string.IsNullOrEmpty(runId))
        {
            throw new ArgumentException("Expected a non-empty value for runId.", nameof(runId));
        }
        return await _transport.RequestAsync<RunObject>(HttpMethod.Post, $"/threads/{threadId}/runs/{runId}/cancel", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Create a run.</summary>
    public async Task<RunObject> Create(string threadId, CreateRunRequest body, List<string>? include = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        var query = new Dictionary<string, object?>
        {
            ["include[]"] = include,
        };
        return await _transport.RequestAsync<RunObject>(HttpMethod.Post, $"/threads/{threadId}/runs", query: query, body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Returns a list of runs belonging to a thread.</summary>
    public async Task<CursorPage<RunObject>> List(string threadId, long? limit = null, BetaThreadsRunsListOrder? order = null, string? after = null, string? before = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["order"] = order,
            ["after"] = after,
            ["before"] = before,
        };
        return await _transport.RequestAsync<CursorPage<RunObject>>(HttpMethod.Get, $"/threads/{threadId}/runs", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Retrieves a run.</summary>
    public async Task<RunObject> Retrieve(string threadId, string runId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        if (string.IsNullOrEmpty(runId))
        {
            throw new ArgumentException("Expected a non-empty value for runId.", nameof(runId));
        }
        return await _transport.RequestAsync<RunObject>(HttpMethod.Get, $"/threads/{threadId}/runs/{runId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Modifies a run.</summary>
    public async Task<RunObject> Update(string threadId, string runId, ModifyRunRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        if (string.IsNullOrEmpty(runId))
        {
            throw new ArgumentException("Expected a non-empty value for runId.", nameof(runId));
        }
        return await _transport.RequestAsync<RunObject>(HttpMethod.Post, $"/threads/{threadId}/runs/{runId}", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class BetaThreadsRunsStepsService
{
    private readonly Transport _transport;

    internal BetaThreadsRunsStepsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Returns a list of run steps belonging to a run.</summary>
    public async Task<CursorPage<RunStepObject>> List(string threadId, string runId, long? limit = null, BetaThreadsRunsStepsListOrder? order = null, string? after = null, string? before = null, List<string>? include = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        if (string.IsNullOrEmpty(runId))
        {
            throw new ArgumentException("Expected a non-empty value for runId.", nameof(runId));
        }
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["order"] = order,
            ["after"] = after,
            ["before"] = before,
            ["include[]"] = include,
        };
        return await _transport.RequestAsync<CursorPage<RunStepObject>>(HttpMethod.Get, $"/threads/{threadId}/runs/{runId}/steps", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Retrieves a run step.</summary>
    public async Task<RunStepObject> Retrieve(string threadId, string runId, string stepId, List<string>? include = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        if (string.IsNullOrEmpty(runId))
        {
            throw new ArgumentException("Expected a non-empty value for runId.", nameof(runId));
        }
        if (string.IsNullOrEmpty(stepId))
        {
            throw new ArgumentException("Expected a non-empty value for stepId.", nameof(stepId));
        }
        var query = new Dictionary<string, object?>
        {
            ["include[]"] = include,
        };
        return await _transport.RequestAsync<RunStepObject>(HttpMethod.Get, $"/threads/{threadId}/runs/{runId}/steps/{stepId}", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class BetaThreadsRunsSubmitToolOutputsService
{
    private readonly Transport _transport;

    internal BetaThreadsRunsSubmitToolOutputsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>When a run has the `status: "requires_action"` and `required_action.type` is `submit_tool_outputs`, this endpoint can be used to submit the outputs from the tool calls once they're all completed. All outputs must be submitted in a single request.</summary>
    public async Task<RunObject> Create(string threadId, string runId, SubmitToolOutputsRunRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(threadId))
        {
            throw new ArgumentException("Expected a non-empty value for threadId.", nameof(threadId));
        }
        if (string.IsNullOrEmpty(runId))
        {
            throw new ArgumentException("Expected a non-empty value for runId.", nameof(runId));
        }
        return await _transport.RequestAsync<RunObject>(HttpMethod.Post, $"/threads/{threadId}/runs/{runId}/submit_tool_outputs", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

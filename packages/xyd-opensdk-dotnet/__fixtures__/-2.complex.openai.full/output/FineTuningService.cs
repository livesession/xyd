// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Threading;
using System.Threading.Tasks;

namespace Example.Openai;

public sealed class FineTuningService
{
    private readonly Transport _transport;
    public FineTuningAlphaService Alpha { get; }
    public FineTuningCheckpointsService Checkpoints { get; }
    public FineTuningJobsService Jobs { get; }

    internal FineTuningService(Transport transport)
    {
        _transport = transport;
        Alpha = new FineTuningAlphaService(transport);
        Checkpoints = new FineTuningCheckpointsService(transport);
        Jobs = new FineTuningJobsService(transport);
    }
}

public sealed class FineTuningAlphaService
{
    private readonly Transport _transport;
    public FineTuningAlphaGradersService Graders { get; }

    internal FineTuningAlphaService(Transport transport)
    {
        _transport = transport;
        Graders = new FineTuningAlphaGradersService(transport);
    }
}

public sealed class FineTuningAlphaGradersService
{
    private readonly Transport _transport;
    public FineTuningAlphaGradersRunService Run { get; }
    public FineTuningAlphaGradersValidateService Validate { get; }

    internal FineTuningAlphaGradersService(Transport transport)
    {
        _transport = transport;
        Run = new FineTuningAlphaGradersRunService(transport);
        Validate = new FineTuningAlphaGradersValidateService(transport);
    }
}

public sealed class FineTuningAlphaGradersRunService
{
    private readonly Transport _transport;

    internal FineTuningAlphaGradersRunService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Run a grader.</summary>
    public async Task<RunGraderResponse> Create(RunGraderRequest body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<RunGraderResponse>(HttpMethod.Post, "/fine_tuning/alpha/graders/run", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class FineTuningAlphaGradersValidateService
{
    private readonly Transport _transport;

    internal FineTuningAlphaGradersValidateService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Validate a grader.</summary>
    public async Task<ValidateGraderResponse> Create(ValidateGraderRequest body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<ValidateGraderResponse>(HttpMethod.Post, "/fine_tuning/alpha/graders/validate", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class FineTuningCheckpointsService
{
    private readonly Transport _transport;
    public FineTuningCheckpointsPermissionsService Permissions { get; }

    internal FineTuningCheckpointsService(Transport transport)
    {
        _transport = transport;
        Permissions = new FineTuningCheckpointsPermissionsService(transport);
    }
}

public sealed class FineTuningCheckpointsPermissionsService
{
    private readonly Transport _transport;

    internal FineTuningCheckpointsPermissionsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// **NOTE:** Calling this endpoint requires an [admin API key](../admin-api-keys).
    /// This enables organization owners to share fine-tuned models with other projects in their organization.
    /// </summary>
    public async Task<CursorPage<FineTuningCheckpointPermission>> Create(string fineTunedModelCheckpoint, CreateFineTuningCheckpointPermissionRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(fineTunedModelCheckpoint))
        {
            throw new ArgumentException("Expected a non-empty value for fineTunedModelCheckpoint.", nameof(fineTunedModelCheckpoint));
        }
        return await _transport.RequestAsync<CursorPage<FineTuningCheckpointPermission>>(HttpMethod.Post, $"/fine_tuning/checkpoints/{fineTunedModelCheckpoint}/permissions", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// **NOTE:** This endpoint requires an [admin API key](../admin-api-keys).
    /// Organization owners can use this endpoint to delete a permission for a fine-tuned model checkpoint.
    /// </summary>
    public async Task<DeleteFineTuningCheckpointPermissionResponse> Delete(string fineTunedModelCheckpoint, string permissionId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(fineTunedModelCheckpoint))
        {
            throw new ArgumentException("Expected a non-empty value for fineTunedModelCheckpoint.", nameof(fineTunedModelCheckpoint));
        }
        if (string.IsNullOrEmpty(permissionId))
        {
            throw new ArgumentException("Expected a non-empty value for permissionId.", nameof(permissionId));
        }
        return await _transport.RequestAsync<DeleteFineTuningCheckpointPermissionResponse>(HttpMethod.Delete, $"/fine_tuning/checkpoints/{fineTunedModelCheckpoint}/permissions/{permissionId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// **NOTE:** This endpoint requires an [admin API key](../admin-api-keys).
    /// Organization owners can use this endpoint to view all permissions for a fine-tuned model checkpoint.
    /// </summary>
    public async Task<CursorPage<FineTuningCheckpointPermission>> List(string fineTunedModelCheckpoint, string? projectId = null, string? after = null, long? limit = null, FineTuningCheckpointsPermissionsListOrder? order = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(fineTunedModelCheckpoint))
        {
            throw new ArgumentException("Expected a non-empty value for fineTunedModelCheckpoint.", nameof(fineTunedModelCheckpoint));
        }
        var query = new Dictionary<string, object?>
        {
            ["project_id"] = projectId,
            ["after"] = after,
            ["limit"] = limit,
            ["order"] = order,
        };
        return await _transport.RequestAsync<CursorPage<FineTuningCheckpointPermission>>(HttpMethod.Get, $"/fine_tuning/checkpoints/{fineTunedModelCheckpoint}/permissions", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class FineTuningJobsService
{
    private readonly Transport _transport;
    public FineTuningJobsCheckpointsService Checkpoints { get; }

    internal FineTuningJobsService(Transport transport)
    {
        _transport = transport;
        Checkpoints = new FineTuningJobsCheckpointsService(transport);
    }

    /// <summary>Immediately cancel a fine-tune job.</summary>
    public async Task<FineTuningJob> Cancel(string fineTuningJobId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(fineTuningJobId))
        {
            throw new ArgumentException("Expected a non-empty value for fineTuningJobId.", nameof(fineTuningJobId));
        }
        return await _transport.RequestAsync<FineTuningJob>(HttpMethod.Post, $"/fine_tuning/jobs/{fineTuningJobId}/cancel", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// Creates a fine-tuning job which begins the process of creating a new model from a given dataset.
    /// Response includes details of the enqueued job including job status and the name of the fine-tuned models once complete.
    /// [Learn more about fine-tuning](/docs/guides/model-optimization)
    /// </summary>
    public async Task<FineTuningJob> Create(CreateFineTuningJobRequest body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<FineTuningJob>(HttpMethod.Post, "/fine_tuning/jobs", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>List your organization's fine-tuning jobs</summary>
    public async Task<CursorPage<FineTuningJob>> List(string? after = null, long? limit = null, Dictionary<string, string>? metadata = null, CancellationToken cancellationToken = default)
    {
        var query = new Dictionary<string, object?>
        {
            ["after"] = after,
            ["limit"] = limit,
        };
        if (metadata != null)
        {
            foreach (var entry in metadata)
            {
                query[$"metadata[{entry.Key}]"] = entry.Value;
            }
        }
        return await _transport.RequestAsync<CursorPage<FineTuningJob>>(HttpMethod.Get, "/fine_tuning/jobs", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Get status updates for a fine-tuning job.</summary>
    public async Task<CursorPage<FineTuningJobEvent>> ListEvents(string fineTuningJobId, string? after = null, long? limit = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(fineTuningJobId))
        {
            throw new ArgumentException("Expected a non-empty value for fineTuningJobId.", nameof(fineTuningJobId));
        }
        var query = new Dictionary<string, object?>
        {
            ["after"] = after,
            ["limit"] = limit,
        };
        return await _transport.RequestAsync<CursorPage<FineTuningJobEvent>>(HttpMethod.Get, $"/fine_tuning/jobs/{fineTuningJobId}/events", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Pause a fine-tune job.</summary>
    public async Task<FineTuningJob> Pause(string fineTuningJobId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(fineTuningJobId))
        {
            throw new ArgumentException("Expected a non-empty value for fineTuningJobId.", nameof(fineTuningJobId));
        }
        return await _transport.RequestAsync<FineTuningJob>(HttpMethod.Post, $"/fine_tuning/jobs/{fineTuningJobId}/pause", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Resume a fine-tune job.</summary>
    public async Task<FineTuningJob> Resume(string fineTuningJobId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(fineTuningJobId))
        {
            throw new ArgumentException("Expected a non-empty value for fineTuningJobId.", nameof(fineTuningJobId));
        }
        return await _transport.RequestAsync<FineTuningJob>(HttpMethod.Post, $"/fine_tuning/jobs/{fineTuningJobId}/resume", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// Get info about a fine-tuning job.
    /// [Learn more about fine-tuning](/docs/guides/model-optimization)
    /// </summary>
    public async Task<FineTuningJob> Retrieve(string fineTuningJobId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(fineTuningJobId))
        {
            throw new ArgumentException("Expected a non-empty value for fineTuningJobId.", nameof(fineTuningJobId));
        }
        return await _transport.RequestAsync<FineTuningJob>(HttpMethod.Get, $"/fine_tuning/jobs/{fineTuningJobId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class FineTuningJobsCheckpointsService
{
    private readonly Transport _transport;

    internal FineTuningJobsCheckpointsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>List checkpoints for a fine-tuning job.</summary>
    public async Task<CursorPage<FineTuningJobCheckpoint>> List(string fineTuningJobId, string? after = null, long? limit = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(fineTuningJobId))
        {
            throw new ArgumentException("Expected a non-empty value for fineTuningJobId.", nameof(fineTuningJobId));
        }
        var query = new Dictionary<string, object?>
        {
            ["after"] = after,
            ["limit"] = limit,
        };
        return await _transport.RequestAsync<CursorPage<FineTuningJobCheckpoint>>(HttpMethod.Get, $"/fine_tuning/jobs/{fineTuningJobId}/checkpoints", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

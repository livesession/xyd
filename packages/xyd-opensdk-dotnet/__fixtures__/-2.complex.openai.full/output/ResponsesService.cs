// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Threading;
using System.Threading.Tasks;

namespace Example.Openai;

public sealed class ResponsesService
{
    private readonly Transport _transport;
    public ResponsesCompactService Compact { get; }
    public ResponsesInputItemsService InputItems { get; }
    public ResponsesInputTokensService InputTokens { get; }

    internal ResponsesService(Transport transport)
    {
        _transport = transport;
        Compact = new ResponsesCompactService(transport);
        InputItems = new ResponsesInputItemsService(transport);
        InputTokens = new ResponsesInputTokensService(transport);
    }

    /// <summary>
    /// Cancels a model response with the given ID. Only responses created with
    /// the `background` parameter set to `true` can be cancelled.
    /// [Learn more](/docs/guides/background).
    /// </summary>
    public async Task<Response> Cancel(string responseId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(responseId))
        {
            throw new ArgumentException("Expected a non-empty value for responseId.", nameof(responseId));
        }
        return await _transport.RequestAsync<Response>(HttpMethod.Post, $"/responses/{responseId}/cancel", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// Creates a model response. Provide [text](/docs/guides/text) or
    /// [image](/docs/guides/images) inputs to generate [text](/docs/guides/text)
    /// or [JSON](/docs/guides/structured-outputs) outputs. Have the model call
    /// your own [custom code](/docs/guides/function-calling) or use built-in
    /// [tools](/docs/guides/tools) like [web search](/docs/guides/tools-web-search)
    /// or [file search](/docs/guides/tools-file-search) to use your own data
    /// as input for the model's response.
    /// </summary>
    public async Task<Response> Create(CreateResponse body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<Response>(HttpMethod.Post, "/responses", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Deletes a model response with the given ID.</summary>
    public async Task Delete(string responseId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(responseId))
        {
            throw new ArgumentException("Expected a non-empty value for responseId.", nameof(responseId));
        }
        await _transport.RequestAsync(HttpMethod.Delete, $"/responses/{responseId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Retrieves a model response with the given ID.</summary>
    public async Task<Response> Retrieve(string responseId, List<IncludeEnum>? include = null, bool? stream = null, long? startingAfter = null, bool? includeObfuscation = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(responseId))
        {
            throw new ArgumentException("Expected a non-empty value for responseId.", nameof(responseId));
        }
        var query = new Dictionary<string, object?>
        {
            ["include"] = include,
            ["stream"] = stream,
            ["starting_after"] = startingAfter,
            ["include_obfuscation"] = includeObfuscation,
        };
        return await _transport.RequestAsync<Response>(HttpMethod.Get, $"/responses/{responseId}", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class ResponsesCompactService
{
    private readonly Transport _transport;

    internal ResponsesCompactService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Compact a conversation. Returns a compacted response object.
    /// Learn when and how to compact long-running conversations in the [conversation state guide](/docs/guides/conversation-state#managing-the-context-window). For ZDR-compatible compaction details, see [Compaction (advanced)](/docs/guides/conversation-state#compaction-advanced).
    /// </summary>
    public async Task<CompactResource> Create(CompactResponseMethodPublicBody? body = null, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<CompactResource>(HttpMethod.Post, "/responses/compact", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class ResponsesInputItemsService
{
    private readonly Transport _transport;

    internal ResponsesInputItemsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Returns a list of input items for a given response.</summary>
    public async Task<CursorPage<object>> List(string responseId, long? limit = null, ResponsesInputItemsListOrder? order = null, string? after = null, List<IncludeEnum>? include = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(responseId))
        {
            throw new ArgumentException("Expected a non-empty value for responseId.", nameof(responseId));
        }
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["order"] = order,
            ["after"] = after,
            ["include"] = include,
        };
        return await _transport.RequestAsync<CursorPage<object>>(HttpMethod.Get, $"/responses/{responseId}/input_items", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class ResponsesInputTokensService
{
    private readonly Transport _transport;

    internal ResponsesInputTokensService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Returns input token counts of the request.
    /// Returns an object with `object` set to `response.input_tokens` and an `input_tokens` count.
    /// </summary>
    public async Task<TokenCountsResource> Count(TokenCountsBody? body = null, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<TokenCountsResource>(HttpMethod.Post, "/responses/input_tokens", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Threading;
using System.Threading.Tasks;

namespace Example.Openai;

public sealed class UploadsService
{
    private readonly Transport _transport;
    public UploadsPartsService Parts { get; }

    internal UploadsService(Transport transport)
    {
        _transport = transport;
        Parts = new UploadsPartsService(transport);
    }

    /// <summary>
    /// Cancels the Upload. No Parts may be added after an Upload is cancelled.
    /// Returns the Upload object with status `cancelled`.
    /// </summary>
    public async Task<Upload> Cancel(string uploadId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(uploadId))
        {
            throw new ArgumentException("Expected a non-empty value for uploadId.", nameof(uploadId));
        }
        return await _transport.RequestAsync<Upload>(HttpMethod.Post, $"/uploads/{uploadId}/cancel", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// Completes the [Upload](/docs/api-reference/uploads/object).
    /// Within the returned Upload object, there is a nested [File](/docs/api-reference/files/object) object that is ready to use in the rest of the platform.
    /// You can specify the order of the Parts by passing in an ordered list of the Part IDs.
    /// The number of bytes uploaded upon completion must match the number of bytes initially specified when creating the Upload object. No Parts may be added after an Upload is completed.
    /// Returns the Upload object with status `completed`, including an additional `file` property containing the created usable File object.
    /// </summary>
    public async Task<Upload> Complete(string uploadId, CompleteUploadRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(uploadId))
        {
            throw new ArgumentException("Expected a non-empty value for uploadId.", nameof(uploadId));
        }
        return await _transport.RequestAsync<Upload>(HttpMethod.Post, $"/uploads/{uploadId}/complete", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>
    /// Creates an intermediate [Upload](/docs/api-reference/uploads/object) object
    /// that you can add [Parts](/docs/api-reference/uploads/part-object) to.
    /// Currently, an Upload can accept at most 8 GB in total and expires after an
    /// hour after you create it.
    /// Once you complete the Upload, we will create a
    /// [File](/docs/api-reference/files/object) object that contains all the parts
    /// you uploaded. This File is usable in the rest of our platform as a regular
    /// File object.
    /// For certain `purpose` values, the correct `mime_type` must be specified.
    /// Please refer to documentation for the
    /// [supported MIME types for your use case](/docs/assistants/tools/file-search#supported-files).
    /// For guidance on the proper filename extensions for each purpose, please
    /// follow the documentation on [creating a
    /// File](/docs/api-reference/files/create).
    /// Returns the Upload object with status `pending`.
    /// </summary>
    public async Task<Upload> Create(CreateUploadRequest body, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<Upload>(HttpMethod.Post, "/uploads", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class UploadsPartsService
{
    private readonly Transport _transport;

    internal UploadsPartsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Adds a [Part](/docs/api-reference/uploads/part-object) to an [Upload](/docs/api-reference/uploads/object) object. A Part represents a chunk of bytes from the file you are trying to upload.
    /// Each Part can be at most 64 MB, and you can add Parts until you hit the Upload maximum of 8 GB.
    /// It is possible to add multiple Parts in parallel. You can decide the intended order of the Parts when you [complete the Upload](/docs/api-reference/uploads/complete).
    /// </summary>
    public async Task<UploadPart> Create(string uploadId, AddUploadPartRequest body, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(uploadId))
        {
            throw new ArgumentException("Expected a non-empty value for uploadId.", nameof(uploadId));
        }
        var bodyMap = new Dictionary<string, object?>
        {
            ["data"] = body.Data,
        };
        return await _transport.RequestAsync<UploadPart>(HttpMethod.Post, $"/uploads/{uploadId}/parts", body: bodyMap, encoding: "multipart", cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Threading;
using System.Threading.Tasks;

namespace Example.Openai;

public sealed class ConversationsService
{
    private readonly Transport _transport;
    public ConversationsItemsService Items { get; }

    internal ConversationsService(Transport transport)
    {
        _transport = transport;
        Items = new ConversationsItemsService(transport);
    }

    /// <summary>Create a conversation.</summary>
    public async Task<ConversationResource> Create(CreateConversationBody? body = null, CancellationToken cancellationToken = default)
    {
        return await _transport.RequestAsync<ConversationResource>(HttpMethod.Post, "/conversations", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Delete a conversation. Items in the conversation will not be deleted.</summary>
    public async Task<DeletedConversationResource> Delete(string conversationId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(conversationId))
        {
            throw new ArgumentException("Expected a non-empty value for conversationId.", nameof(conversationId));
        }
        return await _transport.RequestAsync<DeletedConversationResource>(HttpMethod.Delete, $"/conversations/{conversationId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Get a conversation</summary>
    public async Task<ConversationResource> Retrieve(string conversationId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(conversationId))
        {
            throw new ArgumentException("Expected a non-empty value for conversationId.", nameof(conversationId));
        }
        return await _transport.RequestAsync<ConversationResource>(HttpMethod.Get, $"/conversations/{conversationId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Update a conversation</summary>
    public async Task<ConversationResource> Update(string conversationId, UpdateConversationBody? body = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(conversationId))
        {
            throw new ArgumentException("Expected a non-empty value for conversationId.", nameof(conversationId));
        }
        return await _transport.RequestAsync<ConversationResource>(HttpMethod.Post, $"/conversations/{conversationId}", body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

public sealed class ConversationsItemsService
{
    private readonly Transport _transport;

    internal ConversationsItemsService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>Create items in a conversation with the given ID.</summary>
    public async Task<CursorPage<object>> Create(string conversationId, ConversationsItemsCreateRequest body, List<IncludeEnum>? include = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(conversationId))
        {
            throw new ArgumentException("Expected a non-empty value for conversationId.", nameof(conversationId));
        }
        var query = new Dictionary<string, object?>
        {
            ["include"] = include,
        };
        return await _transport.RequestAsync<CursorPage<object>>(HttpMethod.Post, $"/conversations/{conversationId}/items", query: query, body: body, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Delete an item from a conversation with the given IDs.</summary>
    public async Task<ConversationResource> Delete(string conversationId, string itemId, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(conversationId))
        {
            throw new ArgumentException("Expected a non-empty value for conversationId.", nameof(conversationId));
        }
        if (string.IsNullOrEmpty(itemId))
        {
            throw new ArgumentException("Expected a non-empty value for itemId.", nameof(itemId));
        }
        return await _transport.RequestAsync<ConversationResource>(HttpMethod.Delete, $"/conversations/{conversationId}/items/{itemId}", cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>List all items for a conversation with the given ID.</summary>
    public async Task<CursorPage<object>> List(string conversationId, long? limit = null, ConversationsItemsListOrder? order = null, string? after = null, List<IncludeEnum>? include = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(conversationId))
        {
            throw new ArgumentException("Expected a non-empty value for conversationId.", nameof(conversationId));
        }
        var query = new Dictionary<string, object?>
        {
            ["limit"] = limit,
            ["order"] = order,
            ["after"] = after,
            ["include"] = include,
        };
        return await _transport.RequestAsync<CursorPage<object>>(HttpMethod.Get, $"/conversations/{conversationId}/items", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }

    /// <summary>Get a single item from a conversation with the given IDs.</summary>
    public async Task<object?> Retrieve(string conversationId, string itemId, List<IncludeEnum>? include = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(conversationId))
        {
            throw new ArgumentException("Expected a non-empty value for conversationId.", nameof(conversationId));
        }
        if (string.IsNullOrEmpty(itemId))
        {
            throw new ArgumentException("Expected a non-empty value for itemId.", nameof(itemId));
        }
        var query = new Dictionary<string, object?>
        {
            ["include"] = include,
        };
        string content = await _transport.RequestStringAsync(HttpMethod.Get, $"/conversations/{conversationId}/items/{itemId}", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
        return ConversationItemUnion.Decode(content);
    }
}

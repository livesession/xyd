// <auto-generated>
//     Code generated by opensdk. DO NOT EDIT.
// </auto-generated>

using System;
using System.Collections.Generic;
using System.Net.Http;
using System.Threading;
using System.Threading.Tasks;

namespace Example.Openai;

public sealed class ChatService
{
    private readonly Transport _transport;
    public ChatCompletionsService Completions { get; }

    internal ChatService(Transport transport)
    {
        _transport = transport;
        Completions = new ChatCompletionsService(transport);
    }
}

public sealed class ChatCompletionsService
{
    private readonly Transport _transport;
    public ChatCompletionsMessagesService Messages { get; }

    internal ChatCompletionsService(Transport transport)
    {
        _transport = transport;
        Messages = new ChatCompletionsMessagesService(transport);
    }
}

public sealed class ChatCompletionsMessagesService
{
    private readonly Transport _transport;

    internal ChatCompletionsMessagesService(Transport transport)
    {
        _transport = transport;
    }

    /// <summary>
    /// Get the messages in a stored chat completion. Only Chat Completions that
    /// have been created with the `store` parameter set to `true` will be
    /// returned.
    /// </summary>
    public async Task<CursorPage<ChatCompletionMessageListDataItem>> List(string completionId, string? after = null, long? limit = null, ChatCompletionsMessagesListOrder? order = null, CancellationToken cancellationToken = default)
    {
        if (string.IsNullOrEmpty(completionId))
        {
            throw new ArgumentException("Expected a non-empty value for completionId.", nameof(completionId));
        }
        var query = new Dictionary<string, object?>
        {
            ["after"] = after,
            ["limit"] = limit,
            ["order"] = order,
        };
        return await _transport.RequestAsync<CursorPage<ChatCompletionMessageListDataItem>>(HttpMethod.Get, $"/chat/completions/{completionId}/messages", query: query, cancellationToken: cancellationToken).ConfigureAwait(false);
    }
}

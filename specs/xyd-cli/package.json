{
  "name": "@xyd-js/cli-spec",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "TypeSpec source of truth for the xyd CLI surface, compiled into dist/opencli.json (the committed opencli.json is consumed by the TS xyd-cli; the Rust crate syncs its own copy via regen --spec).",
  "exports": {
    "./opencli.json": "./opencli.json"
  },
  "files": [
    "main.tsp",
    "globals.tsp",
    "commands/**/*.tsp",
    "opencli.json"
  ],
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "@typespec/compiler": "^1.13.0",
    "@xyd-js/typespec-opencli": "workspace:*"
  }
}

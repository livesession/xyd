# Point the dashboard at the platform-api. When unset, app/data/api.ts falls
# back to the built-in typed fixtures (offline dev). Read server-side only
# (RR loaders run on the server), so no VITE_ prefix / client exposure.
APITOOLCHAIN_API_URL=http://localhost:8788

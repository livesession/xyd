import type { Config } from "@react-router/dev/config";

export default {
  // React Router framework mode. Server-side render by default;
  // set to `false` for SPA mode.
  ssr: true,
} satisfies Config;

# Themes

This page documents the theme system in xyd - how themes are resolved, installed, loaded at runtime, and rendered.

## Architecture

```mermaid
graph TB
    CFG["docs.json\n(theme.name)"] --> DOCUMAN["xyd-documan\n(resolve theme version)"]
    DOCUMAN --> INSTALL["postWorkspaceSetup\n(install theme package)"]
    INSTALL --> HOST[".xyd/host/node_modules/\n@xyd-js/theme-{name}"]
    HOST --> VITE["virtual:xyd-theme\n(Vite plugin in plugin-docs)"]
    VITE --> IMPORT["import Theme from\n'@xyd-js/theme-{name}'"]
    IMPORT --> INSTANCE["new Theme()\n(extends BaseTheme)"]
    INSTANCE --> LAYOUT["Theme.Layout()"]
    INSTANCE --> PAGE["Theme.Page()"]

    style CFG fill:#81ecec,color:#333,stroke:#00cec9
    style DOCUMAN fill:#6c5ce7,color:#fff,stroke:#5a4bd4
    style INSTALL fill:#6c5ce7,color:#fff,stroke:#5a4bd4
    style HOST fill:#a29bfe,color:#fff,stroke:#8b83e8
    style VITE fill:#a29bfe,color:#fff,stroke:#8b83e8
    style IMPORT fill:#00b894,color:#fff,stroke:#009a7a
    style INSTANCE fill:#fd79a8,color:#fff,stroke:#d4608a
    style LAYOUT fill:#fd79a8,color:#fff,stroke:#d4608a
    style PAGE fill:#fd79a8,color:#fff,stroke:#d4608a
```

## Theme Resolution

When `xyd dev` or `xyd build` starts, `xyd-documan` resolves the theme package and version:

```mermaid
graph LR
    A["settings.theme.name\n(e.g. 'cosmo')"] --> B{"Built-in?"}
    B -->|yes| C["Use baked version\nfrom theme package.json"]
    B -->|no| D{"In user's\npackage.json?"}
    D -->|yes| E["Use declared version"]
    D -->|no| F["Use 'latest'"]
    C --> G["Install to\n.xyd/host/node_modules/"]
    E --> G
    F --> G

    style A fill:#81ecec,color:#333,stroke:#00cec9
    style B fill:#6c5ce7,color:#fff,stroke:#5a4bd4
    style C fill:#00b894,color:#fff,stroke:#009a7a
    style D fill:#6c5ce7,color:#fff,stroke:#5a4bd4
    style E fill:#00b894,color:#fff,stroke:#009a7a
    style F fill:#00b894,color:#fff,stroke:#009a7a
    style G fill:#fd79a8,color:#fff,stroke:#d4608a
```

### Built-in theme versions

`xyd-documan` imports each built-in theme's `package.json` at build time and bakes their versions into a lookup map:

| Theme | Package |
|-------|---------|
| poetry (default) | `@xyd-js/theme-poetry` |
| cosmo | `@xyd-js/theme-cosmo` |
| opener | `@xyd-js/theme-opener` |
| picasso | `@xyd-js/theme-picasso` |
| gusto | `@xyd-js/theme-gusto` |
| solar | `@xyd-js/theme-solar` |

This ensures users always get a compatible theme version without declaring it in their `package.json`.

### Custom themes

For themes not in the built-in list, `xyd-documan` checks the user's `package.json` for a declared version. If none is found, it installs `latest`.

## Installation

During `postWorkspaceSetup`, the resolved theme package is installed into `.xyd/host/node_modules/` alongside other framework dependencies. The package manager is auto-detected (bun, pnpm, or npm).

## Virtual Module: `virtual:xyd-theme`

The `xyd-plugin-docs` Vite plugin generates `virtual:xyd-theme` at build time:

```mermaid
graph LR
    subgraph "plugin-docs Vite plugin"
        RESOLVE["resolveId('virtual:xyd-theme')"]
        LOAD["load() → generate import code"]
    end

    subgraph "Generated module"
        CODE["import Theme from '@xyd-js/theme-{name}';\nexport default new Theme();"]
    end

    RESOLVE --> LOAD --> CODE

    style RESOLVE fill:#a29bfe,color:#fff,stroke:#8b83e8
    style LOAD fill:#a29bfe,color:#fff,stroke:#8b83e8
    style CODE fill:#00b894,color:#fff,stroke:#009a7a
```

In dev mode (`XYD_DEV_MODE`), it resolves the theme from the local monorepo source instead of an installed package.

## Theme CSS

Two additional virtual modules handle theme styles:

| Virtual Module | Purpose |
|---|---|
| `virtual:xyd-theme/index.css` | Theme's compiled stylesheet |
| `virtual:xyd-theme-override/index.css` | User appearance overrides via `generateUserCss()` |

## BaseTheme Class

All themes extend `BaseTheme` and implement rendering methods:

| Method | Purpose |
|--------|---------|
| `Layout` | Root structure — navbar, sidebar, footer, content area |
| `Page` | Content area — main content, TOC, breadcrumbs, nav links |
| `Navbar` | Top navigation bar |
| `Sidebar` | Left sidebar with navigation tree |
| `Content` | Main content rendering |
| `ContentNav` | Right sidebar with TOC |
| `Footer` | Site-wide footer |
| `Breadcrumbs` | Breadcrumb navigation |
| `NavLinks` | Previous/next page navigation |

## Configuration

```json
{
  "theme": {
    "name": "poetry",
    "appearance": {
      "colorScheme": "auto",
      "colorSchemeButton": true,
      "colors": {
        "primary": "#6c5ce7"
      },
      "cssTokens": {},
      "fonts": {}
    }
  }
}
```

| Property | Type | Description |
|----------|------|-------------|
| `name` | string | Theme identifier (built-in or custom package) |
| `appearance.colorScheme` | `"light" \| "dark" \| "auto"` | Color scheme mode |
| `appearance.colorSchemeButton` | boolean | Show light/dark toggle |
| `appearance.colors.primary` | string | Primary color (generates variants) |
| `appearance.cssTokens` | object | Override CSS custom properties |
| `appearance.fonts` | object | Custom font configuration |

## Rendering Flow

```mermaid
graph TB
    subgraph Host["xyd-host (root.tsx)"]
        IMPORT["import Theme from\n'virtual:xyd-theme'"]
    end

    subgraph Layout["Theme.Layout()"]
        SETTINGS["useSettings()\nuseAppearance()"]
        NAVBAR["Navbar"]
        SIDEBAR["Sidebar"]
        FOOTER["Footer"]
    end

    subgraph Page["Theme.Page()"]
        HOOKS["useContentComponent()\nuseToC()"]
        CONTENT["Content"]
        CONTENTNAV["ContentNav"]
        BREADCRUMBS["Breadcrumbs"]
        NAVLINKS["NavLinks"]
    end

    IMPORT --> SETTINGS
    SETTINGS --> NAVBAR
    SETTINGS --> SIDEBAR
    SETTINGS --> FOOTER
    IMPORT --> HOOKS
    HOOKS --> CONTENT
    HOOKS --> CONTENTNAV
    HOOKS --> BREADCRUMBS
    HOOKS --> NAVLINKS

    style Host fill:none,stroke:#6c5ce7,stroke-width:2px,color:#6c5ce7
    style Layout fill:none,stroke:#00b894,stroke-width:2px,color:#00b894
    style Page fill:none,stroke:#fd79a8,stroke-width:2px,color:#fd79a8
    style IMPORT fill:#6c5ce7,color:#fff,stroke:#5a4bd4
    style SETTINGS fill:#00b894,color:#fff,stroke:#009a7a
    style NAVBAR fill:#00b894,color:#fff,stroke:#009a7a
    style SIDEBAR fill:#00b894,color:#fff,stroke:#009a7a
    style FOOTER fill:#00b894,color:#fff,stroke:#009a7a
    style HOOKS fill:#fd79a8,color:#fff,stroke:#d4608a
    style CONTENT fill:#fd79a8,color:#fff,stroke:#d4608a
    style CONTENTNAV fill:#fd79a8,color:#fff,stroke:#d4608a
    style BREADCRUMBS fill:#fd79a8,color:#fff,stroke:#d4608a
    style NAVLINKS fill:#fd79a8,color:#fff,stroke:#d4608a
```

## Hot Reload

During development, theme changes trigger different reload strategies:

| Change | Strategy |
|--------|----------|
| `theme.name` changed | Full server restart (new theme package) |
| `theme.appearance` changed | HMR via `virtual:xyd-theme-override/index.css` |
| Theme source code (dev mode) | Vite HMR |